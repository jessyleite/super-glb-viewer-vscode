import { w as L, H as Oe, S as Bl, N as bs, P as Fl, b as Ul, y as Dl, c as Te, e as F, C as ye, V as je, R as Ds, L as Ze, d as ct, f as Se, M as dt, F as bh, h as Sh, B as Ge, i as Ns, D as Ot, j as Br, W as Dn, T as Nt, k as Ps, l as Nh, m as Rh, n as Ah, o as ra, A as vh, p as Eh, q as Ch, s as wh, t as Mh, u as Bh, v as Fh, x as Uh, z as Dh, E as Ys, U as $e, G as js, I as Ss, J as Ue, K as _e, O as De, Q as Pl, X as ei, Y as ti, Z as Gt, _ as kt, $ as Rt, a0 as si, a1 as ri, a2 as Ll, a3 as Il, a4 as Ol, a5 as Vt, a6 as na, a7 as ia, a8 as ni, a9 as Gl, aa as vt, ab as Ht, ac as Pn, ad as Ph, ae as Lh, af as gs, ag as is, ah as Zs, ai as Pt, aj as jt, ak as Ih, al as H, am as Oh, an as Gh, ao as oa, ap as aa, aq as fo, ar as Ln, as as kh, at as q, au as Kr, av as Vh, aw as kl, ax as $a, ay as ua, az as kr, aA as $h, aB as In, aC as ii, aD as Vr, aE as oi, aF as Rs, aG as po, aH as $r, aI as Vl, aJ as Qe, aK as $l, aL as On, aM as nr, aN as zl, aO as Qr, aP as la, aQ as Wl, aR as Hl, aS as za, aT as ir, aU as zh, aV as ql, aW as Xl, aX as Kl, aY as Ql, aZ as Yl, a_ as jl, a$ as Zl, b0 as Jl, b1 as ec, b2 as tc, b3 as sc, b4 as rc, b5 as Wh, b6 as Hh, b7 as qh, b8 as nc, b9 as ss, ba as Hs, bb as qs, bc as Xs, bd as Lt, be as ic, bf as oc, bg as ac, bh as uc, bi as lc, bj as cc, bk as dc, bl as hc, bm as ca, bn as An, bo as vn, bp as En, bq as Cn, br as Wa, bs as Ha, bt as qa, bu as Xa, bv as go, bw as mo, bx as xo, by as To, bz as yo, bA as Gn, bB as _o, bC as bo, bD as So, bE as No, bF as Ro, bG as Ao, bH as vo, bI as Eo, bJ as Co, bK as wo, bL as Mo, bM as Bo, bN as Fo, bO as Uo, bP as Do, bQ as Po, bR as Lo, bS as Io, bT as kn, bU as Oo, bV as fc, bW as Xh, bX as Kh, bY as Qh, bZ as Yh, b_ as jh, b$ as Zh, c0 as Jh, c1 as ef, c2 as tf, c3 as sf, c4 as rf, c5 as nf, c6 as of, c7 as ai, c8 as da, c9 as af, ca as uf, cb as lf, cc as ha, cd as Ka, ce as cf, cf as df, cg as hf, ch as ff, ci as pf, cj as gf, ck as mf, cl as xf, cm as Tf, cn as yf, co as _f, cp as bf, cq as Sf, cr as Nf, cs as Rf, ct as Af, cu as vf, cv as Ef, cw as pc, cx as gc, cy as Cf, cz as wf, cA as Mf, cB as Bf, cC as mc, cD as xc, cE as Ff, cF as Uf, cG as Df, cH as Tc, cI as Pf, cJ as Lf, cK as Vn, cL as $n, cM as Si, cN as Ni, cO as If, cP as Of, cQ as Gf, cR as kf, cS as Vf, cT as fa, cU as $f, cV as Ri, cW as zf, cX as Wf, cY as Hf } from "./index--tWw_-Xm.js";
import { cZ as FN, c_ as UN, c$ as DN, d0 as PN, d1 as LN, d2 as IN, d3 as ON, d4 as GN, d5 as kN, d6 as VN, d7 as $N, d8 as zN, d9 as WN, da as HN, db as qN, dc as XN, dd as KN, de as QN, df as YN, dg as jN, dh as ZN, di as JN, dj as e0, dk as t0, dl as s0, dm as r0, dn as n0, dp as i0, dq as o0, dr as a0, ds as u0, dt as l0, du as c0, dv as d0, dw as h0, dx as f0, dy as p0, dz as g0, dA as m0, dB as x0, dC as T0, dD as y0, dE as _0, dF as b0, dG as S0, dH as N0, dI as R0, dJ as A0, dK as v0, dL as E0, dM as C0, dN as w0, dO as M0, dP as B0, dQ as F0, dR as U0, dS as D0, dT as P0, dU as L0, dV as I0, dW as O0, dX as G0, dY as k0, dZ as V0, d_ as $0, d$ as z0, e0 as W0, e1 as H0, e2 as q0, e3 as X0, e4 as K0, e5 as Q0, e6 as Y0, e7 as j0, e8 as Z0, e9 as J0, ea as eR, eb as tR, ec as sR, ed as rR, ee as nR, ef as iR, eg as oR, eh as aR, ei as uR, ej as lR, ek as cR, el as dR, em as hR, en as fR } from "./index--tWw_-Xm.js";
const qf = [
  "alphaMap",
  "alphaTest",
  "anisotropy",
  "anisotropyMap",
  "anisotropyRotation",
  "aoMap",
  "aoMapIntensity",
  "attenuationColor",
  "attenuationDistance",
  "bumpMap",
  "clearcoat",
  "clearcoatMap",
  "clearcoatNormalMap",
  "clearcoatNormalScale",
  "clearcoatRoughness",
  "color",
  "dispersion",
  "displacementMap",
  "emissive",
  "emissiveIntensity",
  "emissiveMap",
  "envMap",
  "envMapIntensity",
  "gradientMap",
  "ior",
  "iridescence",
  "iridescenceIOR",
  "iridescenceMap",
  "iridescenceThicknessMap",
  "lightMap",
  "lightMapIntensity",
  "map",
  "matcap",
  "metalness",
  "metalnessMap",
  "normalMap",
  "normalScale",
  "opacity",
  "roughness",
  "roughnessMap",
  "sheen",
  "sheenColor",
  "sheenColorMap",
  "sheenRoughnessMap",
  "shininess",
  "specular",
  "specularColor",
  "specularColorMap",
  "specularIntensity",
  "specularIntensityMap",
  "specularMap",
  "thickness",
  "transmission",
  "transmissionMap"
], Ai = /* @__PURE__ */ new WeakMap(), Qa = /* @__PURE__ */ new WeakMap(), Ya = /* @__PURE__ */ new WeakMap();
class Xf {
  /**
   * Constructs a new node material observer.
   *
   * @param {NodeBuilder} builder - The node builder.
   */
  constructor(e) {
    this.renderObjects = /* @__PURE__ */ new WeakMap(), this.hasNode = this.containsNode(e), this.hasAnimation = e.object.isSkinnedMesh === !0, this.refreshUniforms = qf, this.renderId = 0;
  }
  /**
   * Returns `true` if the given render object is verified for the first time of this observer.
   *
   * @param {RenderObject} renderObject - The render object.
   * @return {boolean} Whether the given render object is verified for the first time of this observer.
   */
  firstInitialization(e) {
    return this.renderObjects.has(e) === !1 ? (this.getRenderObjectData(e), !0) : !1;
  }
  /**
   * Returns `true` if the current rendering produces motion vectors.
   *
   * @param {Renderer} renderer - The renderer.
   * @return {boolean} Whether the current rendering produces motion vectors or not.
   */
  needsVelocity(e) {
    const t = e.getMRT();
    return t !== null && t.has("velocity");
  }
  /**
   * Returns monitoring data for the given render object.
   *
   * @param {RenderObject} renderObject - The render object.
   * @return {Object} The monitoring data.
   */
  getRenderObjectData(e) {
    let t = this.renderObjects.get(e);
    if (t === void 0) {
      const { geometry: s, object: r } = e;
      if (t = {
        geometryId: s.id,
        worldMatrix: r.matrixWorld.clone()
      }, r.center && (t.center = r.center.clone()), r.morphTargetInfluences && (t.morphTargetInfluences = r.morphTargetInfluences.slice()), e.bundle !== null && (t.version = e.bundle.version), e.material.transmission > 0) {
        const { width: n, height: i } = e.context;
        t.bufferWidth = n, t.bufferHeight = i;
      }
      t.lights = this.getLightsData(e.lightsNode.getLights()), this.renderObjects.set(e, t);
    }
    return t;
  }
  /**
   * Returns an attribute data structure holding the attributes versions for
   * monitoring.
   *
   * @param {Object} attributes - The geometry attributes.
   * @return {Object} An object for monitoring the versions of attributes.
   */
  getAttributesData(e) {
    const t = {};
    for (const s in e) {
      const r = e[s];
      t[s] = {
        id: r.id,
        version: r.version
      };
    }
    return t;
  }
  /**
   * Returns `true` if the node builder's material uses
   * node properties.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {boolean} Whether the node builder's material uses node properties or not.
   */
  containsNode(e) {
    const t = e.material;
    for (const s in t)
      if (t[s] && t[s].isNode)
        return !0;
    return !!(e.context.modelViewMatrix || e.context.modelNormalViewMatrix || e.context.getAO || e.context.getShadow);
  }
  /**
   * Returns a geometry data structure holding the geometry property values for
   * monitoring.
   *
   * @param {BufferGeometry} geometry - The geometry.
   * @return {Object} An object for monitoring geometry properties.
   */
  getGeometryData(e) {
    let t = Ya.get(e);
    return t === void 0 && (t = {
      _renderId: -1,
      _equal: !1,
      attributes: this.getAttributesData(e.attributes),
      indexId: e.index ? e.index.id : null,
      indexVersion: e.index ? e.index.version : null,
      drawRange: { start: e.drawRange.start, count: e.drawRange.count }
    }, Ya.set(e, t)), t;
  }
  /**
   * Returns a material data structure holding the material property values for
   * monitoring.
   *
   * @param {Material} material - The material.
   * @return {Object} An object for monitoring material properties.
   */
  getMaterialData(e) {
    let t = Qa.get(e);
    if (t === void 0) {
      t = { _renderId: -1, _equal: !1 };
      for (const s of this.refreshUniforms) {
        const r = e[s];
        r != null && (typeof r == "object" && r.clone !== void 0 ? r.isTexture === !0 ? t[s] = { id: r.id, version: r.version } : t[s] = r.clone() : t[s] = r);
      }
      Qa.set(e, t);
    }
    return t;
  }
  /**
   * Returns `true` if the given render object has not changed its state.
   *
   * @param {RenderObject} renderObject - The render object.
   * @param {Array<Light>} lightsData - The current material lights.
   * @param {number} renderId - The current render ID.
   * @return {boolean} Whether the given render object has changed its state or not.
   */
  equals(e, t, s) {
    const { object: r, material: n, geometry: i } = e, o = this.getRenderObjectData(e);
    if (o.worldMatrix.equals(r.matrixWorld) !== !0)
      return o.worldMatrix.copy(r.matrixWorld), !1;
    const u = this.getMaterialData(e.material);
    if (u._renderId !== s) {
      u._renderId = s;
      for (const c in u) {
        const d = u[c], h = n[c];
        if (c !== "_renderId" && c !== "_equal") {
          if (d.equals !== void 0) {
            if (d.equals(h) === !1)
              return d.copy(h), u._equal = !1, !1;
          } else if (h.isTexture === !0) {
            if (d.id !== h.id || d.version !== h.version)
              return d.id = h.id, d.version = h.version, u._equal = !1, !1;
          } else if (d !== h)
            return u[c] = h, u._equal = !1, !1;
        }
      }
      if (u.transmission > 0) {
        const { width: c, height: d } = e.context;
        if (o.bufferWidth !== c || o.bufferHeight !== d)
          return o.bufferWidth = c, o.bufferHeight = d, u._equal = !1, !1;
      }
      u._equal = !0;
    } else if (u._equal === !1) return !1;
    if (o.geometryId !== i.id)
      return o.geometryId = i.id, !1;
    const l = this.getGeometryData(e.geometry);
    if (l._renderId !== s) {
      l._renderId = s;
      const c = i.attributes, d = l.attributes;
      let h = 0, f = 0;
      for (const v in c) h++;
      for (const v in d) {
        f++;
        const C = d[v], w = c[v];
        if (w === void 0)
          return delete d[v], l._equal = !1, !1;
        if (C.id !== w.id || C.version !== w.version)
          return C.id = w.id, C.version = w.version, l._equal = !1, !1;
      }
      if (f !== h)
        return l.attributes = this.getAttributesData(c), l._equal = !1, !1;
      const p = i.index, g = l.indexId, m = l.indexVersion, y = p ? p.id : null, _ = p ? p.version : null;
      if (g !== y || m !== _)
        return l.indexId = y, l.indexVersion = _, l._equal = !1, !1;
      if (l.drawRange.start !== i.drawRange.start || l.drawRange.count !== i.drawRange.count)
        return l.drawRange.start = i.drawRange.start, l.drawRange.count = i.drawRange.count, l._equal = !1, !1;
      l._equal = !0;
    } else if (l._equal === !1) return !1;
    if (o.morphTargetInfluences) {
      let c = !1;
      for (let d = 0; d < o.morphTargetInfluences.length; d++)
        o.morphTargetInfluences[d] !== r.morphTargetInfluences[d] && (o.morphTargetInfluences[d] = r.morphTargetInfluences[d], c = !0);
      if (c) return !1;
    }
    if (o.lights) {
      for (let c = 0; c < t.length; c++)
        if (o.lights[c].map !== t[c].map)
          return !1;
    }
    return o.center && o.center.equals(r.center) === !1 ? (o.center.copy(r.center), !0) : (e.bundle !== null && (o.version = e.bundle.version), !0);
  }
  /**
   * Returns the lights data for the given material lights.
   *
   * @param {Array<Light>} materialLights - The material lights.
   * @return {Array<Object>} The lights data for the given material lights.
   */
  getLightsData(e) {
    const t = [];
    for (const s of e)
      s.isSpotLight === !0 && s.map !== null && t.push({ map: s.map.version });
    return t;
  }
  /**
   * Returns the lights for the given lights node and render ID.
   *
   * @param {LightsNode} lightsNode - The lights node.
   * @param {number} renderId - The render ID.
   * @return {Array<Object>} The lights for the given lights node and render ID.
   */
  getLights(e, t) {
    if (Ai.has(e)) {
      const r = Ai.get(e);
      if (r.renderId === t)
        return r.lightsData;
    }
    const s = this.getLightsData(e.getLights());
    return Ai.set(e, { renderId: t, lightsData: s }), s;
  }
  /**
   * Checks if the given render object requires a refresh.
   *
   * @param {RenderObject} renderObject - The render object.
   * @param {NodeFrame} nodeFrame - The current node frame.
   * @return {boolean} Whether the given render object requires a refresh or not.
   */
  needsRefresh(e, t) {
    if (this.hasNode || this.hasAnimation || this.firstInitialization(e) || this.needsVelocity(t.renderer))
      return !0;
    const { renderId: s } = t;
    if (this.renderId !== s)
      return this.renderId = s, !0;
    const r = e.object.static === !0, n = e.bundle !== null && e.bundle.static === !0 && this.getRenderObjectData(e).version === e.bundle.version;
    if (r || n)
      return !1;
    const i = this.getLights(e.lightsNode, s);
    return this.equals(e, i, s) !== !0;
  }
}
const Kf = [
  /^StackTrace\.js$/,
  /^TSLCore\.js$/,
  /^.*Node\.js$/,
  /^three\.webgpu.*\.js$/
];
function Qf(a) {
  const e = /(?:at\s+(.+?)\s+\()?(?:(.+?)@)?([^@\s()]+):(\d+):(\d+)/;
  return a.split(`
`).map((t) => {
    const s = t.match(e);
    if (!s) return null;
    const r = s[1] || s[2] || "", n = s[3].split("?")[0], i = parseInt(s[4], 10), o = parseInt(s[5], 10), u = n.split("/").pop();
    return {
      fn: r,
      file: u,
      line: i,
      column: o
    };
  }).filter((t) => t && !Kf.some((s) => s.test(t.file)));
}
class Pe {
  /**
   * Creates a StackTrace instance by capturing and filtering the current stack trace.
   *
   * @param {Error|string|null} stackMessage - An optional stack trace to use instead of capturing a new one.
   */
  constructor(e = null) {
    this.isStackTrace = !0, this.stack = Qf(e || new Error().stack);
  }
  /**
   * Returns a formatted location string of the top stack frame.
   *
   * @returns {string} The formatted stack trace message.
   */
  getLocation() {
    if (this.stack.length === 0)
      return "[Unknown location]";
    const e = this.stack[0], t = e.fn;
    return `${t ? `"${t}()" at ` : ""}"${e.file}:${e.line}"`;
  }
  /**
   * Returns the full error message including the stack trace.
   *
   * @param {string} message - The error message.
   * @returns {string} The full error message with stack trace.
   */
  getError(e) {
    if (this.stack.length === 0)
      return e;
    const t = this.stack.map((s) => {
      const r = `${s.file}:${s.line}:${s.column}`;
      return s.fn ? `    at ${s.fn} (${r})` : `    at ${r}`;
    }).join(`
`);
    return `${e}
${t}`;
  }
}
function pa(a, e = 0) {
  let t = 3735928559 ^ e, s = 1103547991 ^ e;
  if (Array.isArray(a))
    for (let r = 0, n; r < a.length; r++)
      n = a[r], t = Math.imul(t ^ n, 2654435761), s = Math.imul(s ^ n, 1597334677);
  else
    for (let r = 0, n; r < a.length; r++)
      n = a.charCodeAt(r), t = Math.imul(t ^ n, 2654435761), s = Math.imul(s ^ n, 1597334677);
  return t = Math.imul(t ^ t >>> 16, 2246822507), t ^= Math.imul(s ^ s >>> 13, 3266489909), s = Math.imul(s ^ s >>> 16, 2246822507), s ^= Math.imul(t ^ t >>> 13, 3266489909), 4294967296 * (2097151 & s) + (t >>> 0);
}
const lr = (a) => pa(a), Yr = (a) => pa(a), Fr = (...a) => pa(a), Yf = /* @__PURE__ */ new Map([
  [1, "float"],
  [2, "vec2"],
  [3, "vec3"],
  [4, "vec4"],
  [9, "mat3"],
  [16, "mat4"]
]), ja = /* @__PURE__ */ new WeakMap();
function yc(a) {
  return Yf.get(a);
}
function zn(a) {
  if (a == null) return null;
  const e = typeof a;
  return a.isNode === !0 ? "node" : e === "number" ? "float" : e === "boolean" ? "bool" : e === "string" ? "string" : e === "function" ? "shader" : a.isVector2 === !0 ? "vec2" : a.isVector3 === !0 ? "vec3" : a.isVector4 === !0 ? "vec4" : a.isMatrix2 === !0 ? "mat2" : a.isMatrix3 === !0 ? "mat3" : a.isMatrix4 === !0 ? "mat4" : a.isColor === !0 ? "color" : a instanceof ArrayBuffer ? "ArrayBuffer" : null;
}
function ga(a, ...e) {
  const t = a ? a.slice(-4) : void 0;
  return e.length === 1 && (t === "vec2" ? e = [e[0], e[0]] : t === "vec3" ? e = [e[0], e[0], e[0]] : t === "vec4" && (e = [e[0], e[0], e[0], e[0]])), a === "color" ? new Ht(...e) : t === "vec2" ? new je(...e) : t === "vec3" ? new H(...e) : t === "vec4" ? new Se(...e) : t === "mat2" ? new mc(...e) : t === "mat3" ? new Kr(...e) : t === "mat4" ? new dt(...e) : a === "bool" ? e[0] || !1 : a === "float" || a === "int" || a === "uint" ? e[0] || 0 : a === "string" ? e[0] || "" : a === "ArrayBuffer" ? Zf(e[0]) : null;
}
function _c(a) {
  let e = ja.get(a);
  return e === void 0 && (e = {}, ja.set(a, e)), e;
}
function jf(a) {
  let e = "";
  const t = new Uint8Array(a);
  for (let s = 0; s < t.length; s++)
    e += String.fromCharCode(t[s]);
  return btoa(e);
}
function Zf(a) {
  return Uint8Array.from(atob(a), (e) => e.charCodeAt(0)).buffer;
}
const tn = {
  VERTEX: "vertex"
}, z = {
  NONE: "none",
  FRAME: "frame",
  RENDER: "render",
  OBJECT: "object"
}, nt = {
  READ_ONLY: "readOnly",
  WRITE_ONLY: "writeOnly",
  READ_WRITE: "readWrite"
}, Jf = ["fragment", "vertex"], vi = ["setup", "analyze", "generate"], Ei = [...Jf, "compute"], cr = ["x", "y", "z", "w"], ep = {
  analyze: "setup",
  generate: "analyze"
};
let tp = 0;
class G extends ni {
  static get type() {
    return "Node";
  }
  /**
   * Constructs a new node.
   *
   * @param {?string} nodeType - The node type.
   */
  constructor(e = null) {
    super(), this.nodeType = e, this.updateType = z.NONE, this.updateBeforeType = z.NONE, this.updateAfterType = z.NONE, this.version = 0, this.name = "", this.global = !1, this.parents = !1, this.isNode = !0, this._beforeNodes = null, this._cacheKey = null, this._uuid = null, this._cacheKeyVersion = 0, this.id = tp++, this.stackTrace = null, G.captureStackTrace === !0 && (this.stackTrace = new Pe());
  }
  /**
   * Set this property to `true` when the node should be regenerated.
   *
   * @type {boolean}
   * @default false
   * @param {boolean} value
   */
  set needsUpdate(e) {
    e === !0 && this.version++;
  }
  /**
   * The UUID of the node.
   *
   * @type {string}
   * @readonly
   */
  get uuid() {
    return this._uuid === null && (this._uuid = Bf.generateUUID()), this._uuid;
  }
  /**
   * The type of the class. The value is usually the constructor name.
   *
   * @type {string}
  	 * @readonly
   */
  get type() {
    return this.constructor.type;
  }
  /**
   * Convenient method for defining {@link Node#update}.
   *
   * @param {Function} callback - The update method.
   * @param {string} updateType - The update type.
   * @return {Node} A reference to this node.
   */
  onUpdate(e, t) {
    return this.updateType = t, this.update = e.bind(this), this;
  }
  /**
   * Convenient method for defining {@link Node#update}. Similar to {@link Node#onUpdate}, but
   * this method automatically sets the update type to `FRAME`.
   *
   * @param {Function} callback - The update method.
   * @return {Node} A reference to this node.
   */
  onFrameUpdate(e) {
    return this.onUpdate(e, z.FRAME);
  }
  /**
   * Convenient method for defining {@link Node#update}. Similar to {@link Node#onUpdate}, but
   * this method automatically sets the update type to `RENDER`.
   *
   * @param {Function} callback - The update method.
   * @return {Node} A reference to this node.
   */
  onRenderUpdate(e) {
    return this.onUpdate(e, z.RENDER);
  }
  /**
   * Convenient method for defining {@link Node#update}. Similar to {@link Node#onUpdate}, but
   * this method automatically sets the update type to `OBJECT`.
   *
   * @param {Function} callback - The update method.
   * @return {Node} A reference to this node.
   */
  onObjectUpdate(e) {
    return this.onUpdate(e, z.OBJECT);
  }
  /**
   * Convenient method for defining {@link Node#updateReference}.
   *
   * @param {Function} callback - The update method.
   * @return {Node} A reference to this node.
   */
  onReference(e) {
    return this.updateReference = e.bind(this), this;
  }
  /**
   * Nodes might refer to other objects like materials. This method allows to dynamically update the reference
   * to such objects based on a given state (e.g. the current node frame or builder).
   *
   * @param {any} state - This method can be invocated in different contexts so `state` can refer to any object type.
   * @return {any} The updated reference.
   */
  updateReference() {
    return this;
  }
  /**
   * By default this method returns the value of the {@link Node#global} flag. This method
   * can be overwritten in derived classes if an analytical way is required to determine the
   * global cache referring to the current shader-stage.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {boolean} Whether this node is global or not.
   */
  isGlobal() {
    return this.global;
  }
  /**
   * Generator function that can be used to iterate over the child nodes.
   *
   * @generator
   * @yields {Node} A child node.
   */
  *getChildren() {
    for (const { childNode: e } of this._getChildren())
      yield e;
  }
  /**
   * Calling this method dispatches the `dispose` event. This event can be used
   * to register event listeners for clean up tasks.
   */
  dispose() {
    this.dispatchEvent({ type: "dispose" });
  }
  /**
   * Callback for {@link Node#traverse}.
   *
   * @callback traverseCallback
   * @param {Node} node - The current node.
   */
  /**
   * Can be used to traverse through the node's hierarchy.
   *
   * @param {traverseCallback} callback - A callback that is executed per node.
   */
  traverse(e) {
    e(this);
    for (const t of this.getChildren())
      t.traverse(e);
  }
  /**
   * Returns the child nodes of this node.
   *
   * @private
   * @param {Set<Node>} [ignores=new Set()] - A set of nodes to ignore during the search to avoid circular references.
   * @returns {Array<Object>} An array of objects describing the child nodes.
   */
  _getChildren(e = /* @__PURE__ */ new Set()) {
    const t = [];
    e.add(this);
    for (const s of Object.getOwnPropertyNames(this)) {
      const r = this[s];
      if (!(s.startsWith("_") === !0 || e.has(r))) {
        if (Array.isArray(r) === !0)
          for (let n = 0; n < r.length; n++) {
            const i = r[n];
            i && i.isNode === !0 && t.push({ property: s, index: n, childNode: i });
          }
        else if (r && r.isNode === !0)
          t.push({ property: s, childNode: r });
        else if (r && Object.getPrototypeOf(r) === Object.prototype)
          for (const n in r) {
            if (n.startsWith("_") === !0) continue;
            const i = r[n];
            i && i.isNode === !0 && t.push({ property: s, index: n, childNode: i });
          }
      }
    }
    return t;
  }
  /**
   * Returns the cache key for this node.
   *
   * @param {boolean} [force=false] - When set to `true`, a recomputation of the cache key is forced.
   * @param {Set<Node>} [ignores=null] - A set of nodes to ignore during the computation of the cache key.
   * @return {number} The cache key of the node.
   */
  getCacheKey(e = !1, t = null) {
    if (e = e || this.version !== this._cacheKeyVersion, e === !0 || this._cacheKey === null) {
      t === null && (t = /* @__PURE__ */ new Set());
      const s = [];
      for (const { property: r, childNode: n } of this._getChildren(t))
        s.push(lr(r.slice(0, -4)), n.getCacheKey(e, t));
      this._cacheKey = Fr(Yr(s), this.customCacheKey()), this._cacheKeyVersion = this.version;
    }
    return this._cacheKey;
  }
  /**
   * Generate a custom cache key for this node.
   *
   * @return {number} The cache key of the node.
   */
  customCacheKey() {
    return this.id;
  }
  /**
   * Returns the references to this node which is by default `this`.
   *
   * @return {Node} A reference to this node.
   */
  getScope() {
    return this;
  }
  /**
   * Returns the hash of the node which is used to identify the node. By default it's
   * the {@link Node#uuid} however derived node classes might have to overwrite this method
   * depending on their implementation.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The hash.
   */
  getHash() {
    return String(this.id);
  }
  /**
   * Returns the update type of {@link Node#update}.
   *
   * @return {NodeUpdateType} The update type.
   */
  getUpdateType() {
    return this.updateType;
  }
  /**
   * Returns the update type of {@link Node#updateBefore}.
   *
   * @return {NodeUpdateType} The update type.
   */
  getUpdateBeforeType() {
    return this.updateBeforeType;
  }
  /**
   * Returns the update type of {@link Node#updateAfter}.
   *
   * @return {NodeUpdateType} The update type.
   */
  getUpdateAfterType() {
    return this.updateAfterType;
  }
  /**
   * Certain types are composed of multiple elements. For example a `vec3`
   * is composed of three `float` values. This method returns the type of
   * these elements.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The type of the node.
   */
  getElementType(e) {
    const t = this.getNodeType(e);
    return e.getElementType(t);
  }
  /**
   * Returns the node member type for the given name.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {string} name - The name of the member.
   * @return {string} The type of the node.
   */
  getMemberType() {
    return "void";
  }
  /**
   * Returns the node's type.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {string} [output=null] - The output of the node.
   * @return {string} The type of the node.
   */
  getNodeType(e, t = null) {
    const s = e.getDataFromNode(this);
    let r;
    return t !== null ? (s.typeFromOutput = s.typeFromOutput || {}, r = s.typeFromOutput[t], r === void 0 && (r = this.generateNodeType(e, t), s.typeFromOutput[t] = r)) : (r = s.type, r === void 0 && (r = this.generateNodeType(e), s.type = r)), r;
  }
  /**
   * Returns the node's type.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {string} [output=null] - The output of the node.
   * @return {string} The type of the node.
   */
  generateNodeType(e, t = null) {
    const s = e.getNodeProperties(this);
    return s.outputNode ? s.outputNode.getNodeType(e, t) : this.nodeType;
  }
  /**
   * This method is used during the build process of a node and ensures
   * equal nodes are not built multiple times but just once. For example if
   * `attribute( 'uv' )` is used multiple times by the user, the build
   * process makes sure to process just the first node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {Node} The shared node if possible. Otherwise `this` is returned.
   */
  getShared(e) {
    const t = this.getHash(e);
    return e.getNodeFromHash(t) || this;
  }
  /**
   * Returns the number of elements in the node array.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {?number} The number of elements in the node array.
   */
  getArrayCount() {
    return null;
  }
  /**
   * Represents the setup stage which is the first step of the build process, see {@link Node#build} method.
   * This method is often overwritten in derived modules to prepare the node which is used as a node's output/result.
   * If an output node is prepared, then it must be returned in the `return` statement of the derived module's setup function.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {?Node} The output node.
   */
  setup(e) {
    const t = e.getNodeProperties(this);
    let s = 0;
    for (const r of this.getChildren())
      t["node" + s++] = r;
    return t.outputNode || null;
  }
  /**
   * Represents the analyze stage which is the second step of the build process, see {@link Node#build} method.
   * This stage analyzes the node hierarchy and ensures descendent nodes are built.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {?Node} output - The target output node.
   */
  analyze(e, t = null) {
    const s = e.increaseUsage(this);
    if (this.parents === !0) {
      const r = e.getDataFromNode(this, "any");
      r.stages = r.stages || {}, r.stages[e.shaderStage] = r.stages[e.shaderStage] || [], r.stages[e.shaderStage].push(t);
    }
    if (s === 1) {
      const r = e.getNodeProperties(this);
      for (const n of Object.values(r))
        n && n.isNode === !0 && n.build(e, this);
    }
  }
  /**
   * Represents the generate stage which is the third step of the build process, see {@link Node#build} method.
   * This state builds the output node and returns the resulting shader string.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {?string} [output] - Can be used to define the output type.
   * @return {?string} The generated shader string.
   */
  generate(e, t) {
    const { outputNode: s } = e.getNodeProperties(this);
    if (s && s.isNode === !0)
      return s.build(e, t);
  }
  /**
   * The method can be implemented to update the node's internal state before it is used to render an object.
   * The {@link Node#updateBeforeType} property defines how often the update is executed.
   *
   * @abstract
   * @param {NodeFrame} frame - A reference to the current node frame.
   * @return {?boolean} An optional bool that indicates whether the implementation actually performed an update or not (e.g. due to caching).
   */
  updateBefore() {
    L("Abstract function.");
  }
  /**
   * The method can be implemented to update the node's internal state after it was used to render an object.
   * The {@link Node#updateAfterType} property defines how often the update is executed.
   *
   * @abstract
   * @param {NodeFrame} frame - A reference to the current node frame.
   * @return {?boolean} An optional bool that indicates whether the implementation actually performed an update or not (e.g. due to caching).
   */
  updateAfter() {
    L("Abstract function.");
  }
  /**
   * The method can be implemented to update the node's internal state when it is used to render an object.
   * The {@link Node#updateType} property defines how often the update is executed.
   *
   * @abstract
   * @param {NodeFrame} frame - A reference to the current node frame.
   * @return {?boolean} An optional bool that indicates whether the implementation actually performed an update or not (e.g. due to caching).
   */
  update() {
    L("Abstract function.");
  }
  before(e) {
    return this._beforeNodes === null && (this._beforeNodes = []), this._beforeNodes.push(e), this;
  }
  /**
   * This method performs the build of a node. The behavior and return value depend on the current build stage:
   * - **setup**: Prepares the node and its children for the build process. This process can also create new nodes. Returns the node itself or a variant.
   * - **analyze**: Analyzes the node hierarchy for optimizations in the code generation stage. Returns `null`.
   * - **generate**: Generates the shader code for the node. Returns the generated shader string.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {?(string|Node)} [output=null] - Can be used to define the output type.
   * @return {?(Node|string)} The result of the build process, depending on the build stage.
   */
  build(e, t = null) {
    const s = this.getShared(e);
    if (this !== s)
      return s.build(e, t);
    if (this._beforeNodes !== null) {
      const u = this._beforeNodes;
      this._beforeNodes = null;
      for (const l of u)
        l.build(e, t);
      this._beforeNodes = u;
    }
    const r = e.getDataFromNode(this);
    r.buildStages = r.buildStages || {}, r.buildStages[e.buildStage] = !0;
    const n = ep[e.buildStage];
    if (n && r.buildStages[n] !== !0) {
      const u = e.getBuildStage();
      e.setBuildStage(n), this.build(e), e.setBuildStage(u);
    }
    e.addChain(this);
    let i = null;
    const o = e.getBuildStage();
    if (o === "setup") {
      e.addNode(this), this.updateReference(e);
      const u = e.getNodeProperties(this);
      if (u.initialized !== !0) {
        u.initialized = !0, u.outputNode = this.setup(e) || u.outputNode || null;
        for (const l of Object.values(u))
          if (l && l.isNode === !0) {
            if (l.parents === !0) {
              const c = e.getNodeProperties(l);
              c.parents = c.parents || [], c.parents.push(this);
            }
            l.build(e);
          }
        e.addSequentialNode(this);
      }
      i = u.outputNode;
    } else if (o === "analyze")
      this.analyze(e, t);
    else if (o === "generate") {
      if (this.generate.length < 2) {
        const l = this.getNodeType(e), c = e.getDataFromNode(this);
        i = c.snippet, i === void 0 ? c.generated === void 0 ? (c.generated = !0, i = this.generate(e) || "", c.snippet = i) : (L("Node: Recursion detected.", this), i = "/* Recursion detected. */") : c.flowCodes !== void 0 && e.context.nodeBlock !== void 0 && e.addFlowCodeHierarchy(this, e.context.nodeBlock), i = e.format(i, l, t);
      } else
        i = this.generate(e, t) || "";
      i === "" && t !== null && t !== "void" && t !== "OutputType" && (F(`TSL: Invalid generated code, expected a "${t}".`), i = e.generateConst(t));
    }
    return e.removeChain(this), i;
  }
  /**
   * Returns the child nodes as a JSON object.
   *
   * @return {Generator<Object>} An iterable list of serialized child objects as JSON.
   */
  getSerializeChildren() {
    return this._getChildren();
  }
  /**
   * Serializes the node to JSON.
   *
   * @param {Object} json - The output JSON object.
   */
  serialize(e) {
    const t = this.getSerializeChildren(), s = {};
    for (const { property: r, index: n, childNode: i } of t)
      n !== void 0 ? (s[r] === void 0 && (s[r] = Number.isInteger(n) ? [] : {}), s[r][n] = i.toJSON(e.meta).uuid) : s[r] = i.toJSON(e.meta).uuid;
    Object.keys(s).length > 0 && (e.inputNodes = s);
  }
  /**
   * Deserializes the node from the given JSON.
   *
   * @param {Object} json - The JSON object.
   */
  deserialize(e) {
    if (e.inputNodes !== void 0) {
      const t = e.meta.nodes;
      for (const s in e.inputNodes)
        if (Array.isArray(e.inputNodes[s])) {
          const r = [];
          for (const n of e.inputNodes[s])
            r.push(t[n]);
          this[s] = r;
        } else if (typeof e.inputNodes[s] == "object") {
          const r = {};
          for (const n in e.inputNodes[s]) {
            const i = e.inputNodes[s][n];
            r[n] = t[i];
          }
          this[s] = r;
        } else {
          const r = e.inputNodes[s];
          this[s] = t[r];
        }
    }
  }
  /**
   * Serializes the node into the three.js JSON Object/Scene format.
   *
   * @param {?Object} meta - An optional JSON object that already holds serialized data from other scene objects.
   * @return {Object} The serialized node.
   */
  toJSON(e) {
    const { uuid: t, type: s } = this, r = e === void 0 || typeof e == "string";
    r && (e = {
      textures: {},
      images: {},
      nodes: {}
    });
    let n = e.nodes[t];
    n === void 0 && (n = {
      uuid: t,
      type: s,
      meta: e,
      metadata: {
        version: 4.7,
        type: "Node",
        generator: "Node.toJSON"
      }
    }, r !== !0 && (e.nodes[n.uuid] = n), this.serialize(n), delete n.meta);
    function i(o) {
      const u = [];
      for (const l in o) {
        const c = o[l];
        delete c.metadata, u.push(c);
      }
      return u;
    }
    if (r) {
      const o = i(e.textures), u = i(e.images), l = i(e.nodes);
      o.length > 0 && (n.textures = o), u.length > 0 && (n.images = u), l.length > 0 && (n.nodes = l);
    }
    return n;
  }
}
G.captureStackTrace = !1;
class dr extends G {
  // @TODO: If extending from TempNode it breaks webgpu_compute
  static get type() {
    return "ArrayElementNode";
  }
  /**
   * Constructs an array element node.
   *
   * @param {Node} node - The array-like node.
   * @param {Node} indexNode - The index node that defines the element access.
   */
  constructor(e, t) {
    super(), this.node = e, this.indexNode = t, this.isArrayElementNode = !0;
  }
  /**
   * This method is overwritten since the node type is inferred from the array-like node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The node type.
   */
  generateNodeType(e) {
    return this.node.getElementType(e);
  }
  /**
   * This method is overwritten since the member type is inferred from the array-like node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {string} name - The member name.
   * @return {string} The member type.
   */
  getMemberType(e, t) {
    return this.node.getMemberType(e, t);
  }
  generate(e) {
    const t = this.indexNode.getNodeType(e), s = this.node.build(e), r = this.indexNode.build(e, !e.isVector(t) && e.isInteger(t) ? t : "uint");
    return `${s}[ ${r} ]`;
  }
}
class bc extends G {
  static get type() {
    return "ConvertNode";
  }
  /**
   * Constructs a new convert node.
   *
   * @param {Node} node - The node which type should be converted.
   * @param {string} convertTo - The target node type. Multiple types can be defined by separating them with a `|` sign.
   */
  constructor(e, t) {
    super(), this.node = e, this.convertTo = t;
  }
  /**
   * This method is overwritten since the implementation tries to infer the best
   * matching type from the {@link ConvertNode#convertTo} property.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The node type.
   */
  generateNodeType(e) {
    const t = this.node.getNodeType(e);
    let s = null;
    for (const r of this.convertTo.split("|"))
      (s === null || e.getTypeLength(t) === e.getTypeLength(r)) && (s = r);
    return s;
  }
  serialize(e) {
    super.serialize(e), e.convertTo = this.convertTo;
  }
  deserialize(e) {
    super.deserialize(e), this.convertTo = e.convertTo;
  }
  generate(e, t) {
    const s = this.node, r = this.getNodeType(e), n = s.build(e, r);
    return e.format(n, r, t);
  }
}
class Re extends G {
  static get type() {
    return "TempNode";
  }
  /**
   * Constructs a temp node.
   *
   * @param {?string} nodeType - The node type.
   */
  constructor(e = null) {
    super(e), this.isTempNode = !0;
  }
  /**
   * Whether this node is used more than once in context of other nodes.
   *
   * @param {NodeBuilder} builder - The node builder.
   * @return {boolean} A flag that indicates if there is more than one dependency to other nodes.
   */
  hasDependencies(e) {
    return e.getDataFromNode(this).usageCount > 1;
  }
  build(e, t) {
    if (e.getBuildStage() === "generate") {
      const r = e.getVectorType(this.getNodeType(e, t)), n = e.getDataFromNode(this);
      if (n.propertyName !== void 0)
        return e.format(n.propertyName, r, t);
      if (r !== "void" && t !== "void" && this.hasDependencies(e)) {
        const i = super.build(e, r), o = e.getVarFromNode(this, null, r), u = e.getPropertyName(o);
        return e.addLineFlowCode(`${u} = ${i}`, this), n.snippet = i, n.propertyName = u, e.format(n.propertyName, r, t);
      }
    }
    return super.build(e, t);
  }
}
class sp extends Re {
  static get type() {
    return "JoinNode";
  }
  /**
   * Constructs a new join node.
   *
   * @param {Array<Node>} nodes - An array of nodes that should be joined.
   * @param {?string} [nodeType=null] - The node type.
   */
  constructor(e = [], t = null) {
    super(t), this.nodes = e;
  }
  /**
   * This method is overwritten since the node type must be inferred from the
   * joined data length if not explicitly defined.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The node type.
   */
  generateNodeType(e) {
    return this.nodeType !== null ? e.getVectorType(this.nodeType) : e.getTypeFromLength(this.nodes.reduce((t, s) => t + e.getTypeLength(s.getNodeType(e)), 0));
  }
  generate(e, t) {
    const s = this.getNodeType(e), r = e.getTypeLength(s), n = this.nodes, i = e.getComponentType(s), o = [];
    let u = 0;
    for (const c of n) {
      if (u >= r) {
        F(`TSL: Length of parameters exceeds maximum length of function '${s}()' type.`, this.stackTrace);
        break;
      }
      let d = c.getNodeType(e), h = e.getTypeLength(d), f;
      if (u + h > r && (F(`TSL: Length of '${s}()' data exceeds maximum length of output type.`, this.stackTrace), h = r - u, d = e.getTypeFromLength(h)), u += h, f = c.build(e, d), e.getComponentType(d) !== i) {
        const g = e.getTypeFromLength(h, i);
        f = e.format(f, d, g);
      }
      o.push(f);
    }
    const l = `${e.getType(s)}( ${o.join(", ")} )`;
    return e.format(l, s, t);
  }
}
const rp = cr.join("");
class np extends G {
  static get type() {
    return "SplitNode";
  }
  /**
   * Constructs a new split node.
   *
   * @param {Node} node - The node that should be accessed.
   * @param {string} [components='x'] - The components that should be accessed.
   */
  constructor(e, t = "x") {
    super(), this.node = e, this.components = t, this.isSplitNode = !0;
  }
  /**
   * Returns the vector length which is computed based on the requested components.
   *
   * @return {number} The vector length.
   */
  getVectorLength() {
    let e = this.components.length;
    for (const t of this.components)
      e = Math.max(cr.indexOf(t) + 1, e);
    return e;
  }
  /**
   * Returns the component type of the node's type.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The component type.
   */
  getComponentType(e) {
    return e.getComponentType(this.node.getNodeType(e));
  }
  /**
   * This method is overwritten since the node type is inferred from requested components.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The node type.
   */
  generateNodeType(e) {
    return e.getTypeFromLength(this.components.length, this.getComponentType(e));
  }
  /**
   * Returns the scope of the node.
   *
   * @return {Node} The scope of the node.
   */
  getScope() {
    return this.node.getScope();
  }
  generate(e, t) {
    const s = this.node, r = e.getTypeLength(s.getNodeType(e));
    let n = null;
    if (r > 1) {
      let i = null;
      this.getVectorLength() >= r && (i = e.getTypeFromLength(this.getVectorLength(), this.getComponentType(e)));
      const u = s.build(e, i);
      this.components.length === r && this.components === rp.slice(0, this.components.length) ? n = e.format(u, i, t) : n = e.format(`${u}.${this.components}`, this.getNodeType(e), t);
    } else
      n = s.build(e, t);
    return n;
  }
  serialize(e) {
    super.serialize(e), e.components = this.components;
  }
  deserialize(e) {
    super.deserialize(e), this.components = e.components;
  }
}
class ip extends Re {
  static get type() {
    return "SetNode";
  }
  /**
   * Constructs a new set node.
   *
   * @param {Node} sourceNode - The node that should be updated.
   * @param {string} components - The components that should be updated.
   * @param {Node} targetNode - The value node.
   */
  constructor(e, t, s) {
    super(), this.sourceNode = e, this.components = t, this.targetNode = s;
  }
  /**
   * This method is overwritten since the node type is inferred from {@link SetNode#sourceNode}.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The node type.
   */
  generateNodeType(e) {
    return this.sourceNode.getNodeType(e);
  }
  generate(e) {
    const { sourceNode: t, components: s, targetNode: r } = this, n = this.getNodeType(e), i = e.getComponentType(r.getNodeType(e)), o = e.getTypeFromLength(s.length, i), u = r.build(e, o), l = t.build(e, n), c = e.getTypeLength(n), d = [];
    for (let h = 0; h < c; h++) {
      const f = cr[h];
      f === s[0] ? (d.push(u), h += s.length - 1) : d.push(l + "." + f);
    }
    return `${e.getType(n)}( ${d.join(", ")} )`;
  }
}
class op extends Re {
  static get type() {
    return "FlipNode";
  }
  /**
   * Constructs a new flip node.
   *
   * @param {Node} sourceNode - The node which component(s) should be flipped.
   * @param {string} components - The components that should be flipped e.g. `'x'` or `'xy'`.
   */
  constructor(e, t) {
    super(), this.sourceNode = e, this.components = t;
  }
  /**
   * This method is overwritten since the node type is inferred from the source node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The node type.
   */
  generateNodeType(e) {
    return this.sourceNode.getNodeType(e);
  }
  generate(e) {
    const { components: t, sourceNode: s } = this, r = this.getNodeType(e), n = s.build(e), i = e.getVarFromNode(this), o = e.getPropertyName(i);
    e.addLineFlowCode(o + " = " + n, this);
    const u = e.getTypeLength(r), l = [];
    let c = 0;
    for (let d = 0; d < u; d++) {
      const h = cr[d];
      h === t[c] ? (l.push("1.0 - " + (o + "." + h)), c++) : l.push(o + "." + h);
    }
    return `${e.getType(r)}( ${l.join(", ")} )`;
  }
}
class ma extends G {
  static get type() {
    return "InputNode";
  }
  /**
   * Constructs a new input node.
   *
   * @param {any} value - The value of this node. This can be any JS primitive, functions, array buffers or even three.js objects (vector, matrices, colors).
   * @param {?string} nodeType - The node type. If no explicit type is defined, the node tries to derive the type from its value.
   */
  constructor(e, t = null) {
    super(t), this.isInputNode = !0, this.value = e, this.precision = null;
  }
  generateNodeType() {
    return this.nodeType === null ? zn(this.value) : this.nodeType;
  }
  /**
   * Returns the input type of the node which is by default the node type. Derived modules
   * might overwrite this method and use a fixed type or compute one analytically.
   *
   * A typical example for different input and node types are textures. The input type of a
   * normal RGBA texture is `texture` whereas its node type is `vec4`.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The input type.
   */
  getInputType(e) {
    return this.getNodeType(e);
  }
  /**
   * Sets the precision to the given value. The method can be
   * overwritten in derived classes if the final precision must be computed
   * analytically.
   *
   * @param {('low'|'medium'|'high')} precision - The precision of the input value in the shader.
   * @return {InputNode} A reference to this node.
   */
  setPrecision(e) {
    return this.precision = e, this;
  }
  serialize(e) {
    super.serialize(e), e.value = this.value, this.value && this.value.toArray && (e.value = this.value.toArray()), e.valueType = zn(this.value), e.nodeType = this.nodeType, e.valueType === "ArrayBuffer" && (e.value = jf(e.value)), e.precision = this.precision;
  }
  deserialize(e) {
    super.deserialize(e), this.nodeType = e.nodeType, this.value = Array.isArray(e.value) ? ga(e.valueType, ...e.value) : e.value, this.precision = e.precision || null, this.value && this.value.fromArray && (this.value = this.value.fromArray(e.value));
  }
  generate() {
    L("Abstract function.");
  }
}
const Za = /float|u?int/;
class Bt extends ma {
  static get type() {
    return "ConstNode";
  }
  /**
   * Constructs a new input node.
   *
   * @param {any} value - The value of this node. Usually a JS primitive or three.js object (vector, matrix, color).
   * @param {?string} nodeType - The node type. If no explicit type is defined, the node tries to derive the type from its value.
   */
  constructor(e, t = null) {
    super(e, t), this.isConstNode = !0;
  }
  /**
   * Generates the shader string of the value with the current node builder.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The generated value as a shader string.
   */
  generateConst(e) {
    return e.generateConst(this.getNodeType(e), this.value);
  }
  generate(e, t) {
    const s = this.getNodeType(e);
    return Za.test(s) && Za.test(t) ? e.generateConst(t, this.value) : e.format(this.generateConst(e), s, t);
  }
}
class ap extends G {
  static get type() {
    return "MemberNode";
  }
  /**
   * Constructs a member node.
   *
   * @param {Node} structNode - The struct node.
   * @param {string} property - The property name.
   */
  constructor(e, t) {
    super(), this.structNode = e, this.property = t, this.isMemberNode = !0;
  }
  hasMember(e) {
    return this.structNode.isMemberNode && this.structNode.hasMember(e) === !1 ? !1 : this.structNode.getMemberType(e, this.property) !== "void";
  }
  generateNodeType(e) {
    return this.hasMember(e) === !1 ? "float" : this.structNode.getMemberType(e, this.property);
  }
  getMemberType(e, t) {
    if (this.hasMember(e) === !1)
      return "float";
    const s = this.getNodeType(e);
    return e.getStructTypeNode(s).getMemberType(e, t);
  }
  generate(e) {
    if (this.hasMember(e) === !1) {
      L(`TSL: Member "${this.property}" does not exist in struct.`, this.stackTrace);
      const s = this.getNodeType(e);
      return e.generateConst(s);
    }
    return this.structNode.build(e) + "." + this.property;
  }
}
let ws = null;
const Go = /* @__PURE__ */ new Map();
function S(a, e) {
  if (Go.has(a)) {
    L(`TSL: Redefinition of method chaining '${a}'.`);
    return;
  }
  if (typeof e != "function") throw new Error(`THREE.TSL: Node element ${a} is not a function`);
  Go.set(a, e), a !== "assign" && (G.prototype[a] = function(...t) {
    return this.isStackNode ? this.addToStack(e(...t)) : e(this, ...t);
  }, G.prototype[a + "Assign"] = function(...t) {
    return this.isStackNode ? this.assign(t[0], e(...t)) : this.assign(e(this, ...t));
  });
}
const up = (a) => a.replace(/r|s/g, "x").replace(/g|t/g, "y").replace(/b|p/g, "z").replace(/a|q/g, "w"), Ja = (a) => up(a).split("").sort().join("");
G.prototype.assign = function(...a) {
  if (this.isStackNode !== !0)
    return ws !== null ? ws.assign(this, ...a) : F("TSL: No stack defined for assign operation. Make sure the assign is inside a Fn().", new Pe()), this;
  {
    const e = Go.get("assign");
    return this.addToStack(e(...a));
  }
};
G.prototype.toVarIntent = function() {
  return this;
};
G.prototype.get = function(a) {
  return new ap(this, a);
};
const Ur = {};
function sn(a, e, t) {
  Ur[a] = Ur[e] = Ur[t] = {
    get() {
      this._cache = this._cache || {};
      let i = this._cache[a];
      return i === void 0 && (i = new np(this, a), this._cache[a] = i), i;
    },
    set(i) {
      this[a].assign(O(i));
    }
  };
  const s = a.toUpperCase(), r = e.toUpperCase(), n = t.toUpperCase();
  G.prototype["set" + s] = G.prototype["set" + r] = G.prototype["set" + n] = function(i) {
    const o = Ja(a);
    return new ip(this, o, O(i));
  }, G.prototype["flip" + s] = G.prototype["flip" + r] = G.prototype["flip" + n] = function() {
    const i = Ja(a);
    return new op(this, i);
  };
}
const ft = ["x", "y", "z", "w"], pt = ["r", "g", "b", "a"], gt = ["s", "t", "p", "q"];
for (let a = 0; a < 4; a++) {
  let e = ft[a], t = pt[a], s = gt[a];
  sn(e, t, s);
  for (let r = 0; r < 4; r++) {
    e = ft[a] + ft[r], t = pt[a] + pt[r], s = gt[a] + gt[r], sn(e, t, s);
    for (let n = 0; n < 4; n++) {
      e = ft[a] + ft[r] + ft[n], t = pt[a] + pt[r] + pt[n], s = gt[a] + gt[r] + gt[n], sn(e, t, s);
      for (let i = 0; i < 4; i++)
        e = ft[a] + ft[r] + ft[n] + ft[i], t = pt[a] + pt[r] + pt[n] + pt[i], s = gt[a] + gt[r] + gt[n] + gt[i], sn(e, t, s);
    }
  }
}
for (let a = 0; a < 32; a++)
  Ur[a] = {
    get() {
      this._cache = this._cache || {};
      let e = this._cache[a];
      return e === void 0 && (e = new dr(this, new Bt(a, "uint")), this._cache[a] = e), e;
    },
    set(e) {
      this[a].assign(O(e));
    }
  };
Object.defineProperties(G.prototype, Ur);
const eu = /* @__PURE__ */ new WeakMap(), lp = function(a, e = null) {
  const t = zn(a);
  return t === "node" ? a : e === null && (t === "float" || t === "boolean") || t && t !== "shader" && t !== "string" ? O(ko(a, e)) : t === "shader" ? a.isFn ? a : A(a) : a;
}, cp = function(a, e = null) {
  for (const t in a)
    a[t] = O(a[t], e);
  return a;
}, dp = function(a, e = null) {
  const t = a.length;
  for (let s = 0; s < t; s++)
    a[s] = O(a[s], e);
  return a;
}, Sc = function(a, e = null, t = null, s = null) {
  function r(c) {
    return s !== null ? (c = O(Object.assign(c, s)), s.intent === !0 && (c = c.toVarIntent())) : c = O(c), c;
  }
  let n, i = e, o, u;
  function l(c) {
    let d;
    return i ? d = /[a-z]/i.test(i) ? i + "()" : i : d = a.type, o !== void 0 && c.length < o ? (F(`TSL: "${d}" parameter length is less than minimum required.`, new Pe()), c.concat(new Array(o - c.length).fill(0))) : u !== void 0 && c.length > u ? (F(`TSL: "${d}" parameter length exceeds limit.`, new Pe()), c.slice(0, u)) : c;
  }
  return e === null ? n = (...c) => r(new a(...Js(l(c)))) : t !== null ? (t = O(t), n = (...c) => r(new a(e, ...Js(l(c)), t))) : n = (...c) => r(new a(e, ...Js(l(c)))), n.setParameterLength = (...c) => (c.length === 1 ? o = u = c[0] : c.length === 2 && ([o, u] = c), n), n.setName = (c) => (i = c, n), n;
}, hp = function(a, ...e) {
  return new a(...Js(e));
};
class fp extends G {
  constructor(e, t) {
    super(), this.shaderNode = e, this.rawInputs = t, this.isShaderCallNodeInternal = !0;
  }
  generateNodeType(e) {
    return this.shaderNode.nodeType || this.getOutputNode(e).getNodeType(e);
  }
  getElementType(e) {
    return this.getOutputNode(e).getElementType(e);
  }
  getMemberType(e, t) {
    return this.getOutputNode(e).getMemberType(e, t);
  }
  call(e) {
    const { shaderNode: t, rawInputs: s } = this, r = e.getNodeProperties(t), n = e.getClosestSubBuild(t.subBuilds) || "", i = n || "default";
    if (r[i])
      return r[i];
    const o = e.subBuildFn, u = e.fnCall;
    e.subBuildFn = n, e.fnCall = this;
    let l = null;
    if (t.layout) {
      let c = eu.get(e.constructor);
      c === void 0 && (c = /* @__PURE__ */ new WeakMap(), eu.set(e.constructor, c));
      let d = c.get(t);
      d === void 0 && (d = O(e.buildFunctionNode(t)), c.set(t, d)), e.addInclude(d);
      const h = s ? pp(s) : null;
      l = O(d.call(h));
    } else {
      const c = new Proxy(e, {
        get: (g, m, y) => {
          let _;
          return Symbol.iterator === m ? _ = function* () {
            yield void 0;
          } : _ = Reflect.get(g, m, y), _;
        }
      }), d = s ? gp(s) : null, h = Array.isArray(s) ? s.length > 0 : s !== null, f = t.jsFunc, p = h || f.length > 1 ? f(d, c) : f(c);
      l = O(p);
    }
    return e.subBuildFn = o, e.fnCall = u, t.once && (r[i] = l), l;
  }
  setupOutput(e) {
    return e.addStack(), e.stack.outputNode = this.call(e), e.removeStack();
  }
  getOutputNode(e) {
    const t = e.getNodeProperties(this), s = e.getSubBuildOutput(this);
    return t[s] = t[s] || this.setupOutput(e), t[s].subBuild = e.getClosestSubBuild(this), t[s];
  }
  build(e, t = null) {
    let s = null;
    const r = e.getBuildStage(), n = e.getNodeProperties(this), i = e.getSubBuildOutput(this), o = this.getOutputNode(e), u = e.fnCall;
    if (e.fnCall = this, r === "setup") {
      const l = e.getSubBuildProperty("initialized", this);
      if (n[l] !== !0 && (n[l] = !0, n[i] = this.getOutputNode(e), n[i].build(e), this.shaderNode.subBuilds))
        for (const c of e.chaining) {
          const d = e.getDataFromNode(c, "any");
          d.subBuilds = d.subBuilds || /* @__PURE__ */ new Set();
          for (const h of this.shaderNode.subBuilds)
            d.subBuilds.add(h);
        }
      s = n[i];
    } else r === "analyze" ? o.build(e, t) : r === "generate" && (s = o.build(e, t) || "");
    return e.fnCall = u, s;
  }
}
function pp(a) {
  let e;
  return _a(a), a[0] && (a[0].isNode || Object.getPrototypeOf(a[0]) !== Object.prototype) ? e = [...a] : e = a[0], e;
}
function gp(a) {
  let e = 0;
  return _a(a), new Proxy(a, {
    get: (t, s, r) => {
      let n;
      if (s === "length")
        return n = a.length, n;
      if (Symbol.iterator === s)
        n = function* () {
          for (const i of a)
            yield O(i);
        };
      else {
        if (a.length > 0)
          if (Object.getPrototypeOf(a[0]) === Object.prototype) {
            const i = a[0];
            i[s] === void 0 ? n = i[e++] : n = Reflect.get(i, s, r);
          } else a[0] instanceof G && (a[s] === void 0 ? n = a[e++] : n = Reflect.get(a, s, r));
        else
          n = Reflect.get(t, s, r);
        n = O(n);
      }
      return n;
    }
  });
}
class mp extends G {
  constructor(e, t) {
    super(t), this.jsFunc = e, this.layout = null, this.global = !0, this.once = !1;
  }
  setLayout(e) {
    return this.layout = e, this;
  }
  getLayout() {
    return this.layout;
  }
  call(e = null) {
    return new fp(this, e);
  }
  setup() {
    return this.call();
  }
}
const xp = [!1, !0], Tp = [0, 1, 2, 3], yp = [-1, -2], Nc = [0.5, 1.5, 1 / 3, 1e-6, 1e6, Math.PI, Math.PI * 2, 1 / Math.PI, 2 / Math.PI, 1 / (Math.PI * 2), Math.PI / 2], xa = /* @__PURE__ */ new Map();
for (const a of xp) xa.set(a, new Bt(a));
const Ta = /* @__PURE__ */ new Map();
for (const a of Tp) Ta.set(a, new Bt(a, "uint"));
const ya = new Map([...Ta].map((a) => new Bt(a.value, "int")));
for (const a of yp) ya.set(a, new Bt(a, "int"));
const ui = new Map([...ya].map((a) => new Bt(a.value)));
for (const a of Nc) ui.set(a, new Bt(a));
for (const a of Nc) ui.set(-a, new Bt(-a));
const li = { bool: xa, uint: Ta, ints: ya, float: ui }, tu = new Map([...xa, ...ui]), ko = (a, e) => tu.has(a) ? tu.get(a) : a.isNode === !0 ? a : new Bt(a, e), Ae = function(a, e = null) {
  return (...t) => {
    for (const r of t)
      if (r === void 0)
        return F(`TSL: Invalid parameter for the type "${a}".`, new Pe()), new Bt(0, a);
    if ((t.length === 0 || !["bool", "float", "int", "uint"].includes(a) && t.every((r) => {
      const n = typeof r;
      return n !== "object" && n !== "function";
    })) && (t = [ga(a, ...t)]), t.length === 1 && e !== null && e.has(t[0]))
      return rn(e.get(t[0]));
    if (t.length === 1) {
      const r = ko(t[0], a);
      return r.nodeType === a ? rn(r) : rn(new bc(r, a));
    }
    const s = t.map((r) => ko(r));
    return rn(new sp(s, a));
  };
};
function Vo(a) {
  return a && a.isNode && a.traverse((e) => {
    e.isConstNode && (a = e.value);
  }), !!a;
}
const _p = (a) => a != null ? a.nodeType || a.convertTo || (typeof a == "string" ? a : null) : null;
function Cr(a, e) {
  return new mp(a, e);
}
const O = (a, e = null) => (
  /* new */
  lp(a, e)
), rn = (a, e = null) => (
  /* new */
  O(a, e).toVarIntent()
), _a = (a, e = null) => new cp(a, e), Js = (a, e = null) => new dp(a, e), le = (a, e = null, t = null, s = null) => new Sc(a, e, t, s), B = (a, ...e) => new hp(a, ...e), D = (a, e = null, t = null, s = {}) => new Sc(a, e, t, { ...s, intent: !0 });
let bp = 0;
class Sp extends G {
  constructor(e, t = null) {
    super();
    let s = null;
    t !== null && (typeof t == "object" ? s = t.return : (typeof t == "string" ? s = t : F("TSL: Invalid layout type.", new Pe()), t = null)), this.shaderNode = new Cr(e, s), t !== null && this.setLayout(t), this.isFn = !0;
  }
  setLayout(e) {
    const t = this.shaderNode.nodeType;
    if (typeof e.inputs != "object") {
      const s = {
        name: "fn" + bp++,
        type: t,
        inputs: []
      };
      for (const r in e)
        r !== "return" && s.inputs.push({
          name: r,
          type: e[r]
        });
      e = s;
    }
    return this.shaderNode.setLayout(e), this;
  }
  generateNodeType(e) {
    return this.shaderNode.getNodeType(e) || "float";
  }
  call(...e) {
    const t = this.shaderNode.call(e);
    return this.shaderNode.nodeType === "void" && t.toStack(), t.toVarIntent();
  }
  once(e = null) {
    return this.shaderNode.once = !0, this.shaderNode.subBuilds = e, this;
  }
  generate(e) {
    const t = this.getNodeType(e);
    return F('TSL: "Fn()" was declared but not invoked. Try calling it like "Fn()( ...params )".', this.stackTrace), e.generateConst(t);
  }
}
function A(a, e = null) {
  const t = new Sp(a, e);
  return new Proxy(() => {
  }, {
    apply(s, r, n) {
      return t.call(...n);
    },
    get(s, r, n) {
      return Reflect.get(t, r, n);
    },
    set(s, r, n, i) {
      return Reflect.set(t, r, n, i);
    }
  });
}
const Wn = (a) => {
  ws = a;
}, Rc = () => ws, ue = (...a) => ws.If(...a);
function Ac(a) {
  return ws && ws.addToStack(a), a;
}
S("toStack", Ac);
const Np = new Ae("color"), b = new Ae("float", li.float), pe = new Ae("int", li.ints), Q = new Ae("uint", li.uint), ci = new Ae("bool", li.bool), I = new Ae("vec2"), yt = new Ae("ivec2"), vc = new Ae("uvec2"), Rp = new Ae("bvec2"), N = new Ae("vec3"), Ec = new Ae("ivec3"), Cc = new Ae("uvec3"), Ap = new Ae("bvec3"), W = new Ae("vec4"), wc = new Ae("ivec4"), Mc = new Ae("uvec4"), vp = new Ae("bvec4"), ba = new Ae("mat2"), Me = new Ae("mat3"), As = new Ae("mat4");
S("toColor", Np);
S("toFloat", b);
S("toInt", pe);
S("toUint", Q);
S("toBool", ci);
S("toVec2", I);
S("toIVec2", yt);
S("toUVec2", vc);
S("toBVec2", Rp);
S("toVec3", N);
S("toIVec3", Ec);
S("toUVec3", Cc);
S("toBVec3", Ap);
S("toVec4", W);
S("toIVec4", wc);
S("toUVec4", Mc);
S("toBVec4", vp);
S("toMat2", ba);
S("toMat3", Me);
S("toMat4", As);
const Ep = /* @__PURE__ */ le(dr).setParameterLength(2), Cp = (a, e) => new bc(O(a), e);
S("element", Ep);
S("convert", Cp);
S("append", (a) => (L("TSL: .append() has been renamed to .toStack().", new Pe()), Ac(a)));
class ee extends G {
  static get type() {
    return "PropertyNode";
  }
  /**
   * Constructs a new property node.
   *
   * @param {string} nodeType - The type of the node.
   * @param {?string} [name=null] - The name of the property in the shader.
   * @param {boolean} [varying=false] - Whether this property is a varying or not.
   */
  constructor(e, t = null, s = !1) {
    super(e), this.name = t, this.varying = s, this.isPropertyNode = !0, this.global = !0;
  }
  customCacheKey() {
    return lr(this.type + ":" + (this.name || "") + ":" + (this.varying ? "1" : "0"));
  }
  getHash(e) {
    return this.name || super.getHash(e);
  }
  generate(e) {
    let t;
    return this.varying === !0 ? (t = e.getVaryingFromNode(this, this.name), t.needsInterpolation = !0) : t = e.getVarFromNode(this, this.name), e.getPropertyName(t);
  }
}
const rs = (a, e) => new ee(a, e), Hn = (a, e) => new ee(a, e, !0), re = /* @__PURE__ */ B(ee, "vec4", "DiffuseColor"), Ws = /* @__PURE__ */ B(ee, "vec3", "DiffuseContribution"), su = /* @__PURE__ */ B(ee, "vec3", "EmissiveColor"), It = /* @__PURE__ */ B(ee, "float", "Roughness"), es = /* @__PURE__ */ B(ee, "float", "Metalness"), $o = /* @__PURE__ */ B(ee, "float", "Clearcoat"), Dr = /* @__PURE__ */ B(ee, "float", "ClearcoatRoughness"), et = /* @__PURE__ */ B(ee, "vec3", "Sheen"), ms = /* @__PURE__ */ B(ee, "float", "SheenRoughness"), Sa = /* @__PURE__ */ B(ee, "float", "Iridescence"), zo = /* @__PURE__ */ B(ee, "float", "IridescenceIOR"), Wo = /* @__PURE__ */ B(ee, "float", "IridescenceThickness"), Ho = /* @__PURE__ */ B(ee, "float", "AlphaT"), ps = /* @__PURE__ */ B(ee, "float", "Anisotropy"), wn = /* @__PURE__ */ B(ee, "vec3", "AnisotropyT"), er = /* @__PURE__ */ B(ee, "vec3", "AnisotropyB"), Ms = /* @__PURE__ */ B(ee, "color", "SpecularColor"), Ks = /* @__PURE__ */ B(ee, "color", "SpecularColorBlended"), Qs = /* @__PURE__ */ B(ee, "float", "SpecularF90"), qo = /* @__PURE__ */ B(ee, "float", "Shininess"), Pr = /* @__PURE__ */ B(ee, "vec4", "Output"), Ci = /* @__PURE__ */ B(ee, "float", "dashSize"), ru = /* @__PURE__ */ B(ee, "float", "gapSize"), Mn = /* @__PURE__ */ B(ee, "float", "IOR"), Xo = /* @__PURE__ */ B(ee, "float", "Transmission"), Bc = /* @__PURE__ */ B(ee, "float", "Thickness"), Fc = /* @__PURE__ */ B(ee, "float", "AttenuationDistance"), Uc = /* @__PURE__ */ B(ee, "color", "AttenuationColor"), Dc = /* @__PURE__ */ B(ee, "float", "Dispersion");
class Pc extends G {
  static get type() {
    return "UniformGroupNode";
  }
  /**
   * Constructs a new uniform group node.
   *
   * @param {string} name - The name of the uniform group node.
   * @param {boolean} [shared=false] - Whether this uniform group node is shared or not.
   * @param {number} [order=1] - Influences the internal sorting.
   * @param {string|null} [updateType=null] - The update type of the uniform group node.
   */
  constructor(e, t = !1, s = 1, r = null) {
    super("string"), this.name = e, this.shared = t, this.order = s, this.updateType = r, this.isUniformGroup = !0;
  }
  /**
   * Marks the uniform group node as needing an update.
   * This will trigger the necessary updates in the rendering process.
   */
  update() {
    this.needsUpdate = !0;
  }
  /**
   * Serializes the uniform group node to a JSON object.
   *
   * @param {Object} data - The object to store the serialized data.
   */
  serialize(e) {
    super.serialize(e), e.name = this.name, e.version = this.version, e.shared = this.shared;
  }
  /**
   * Deserializes the uniform group node from a JSON object.
   *
   * @param {Object} data - The object containing the serialized data.
   */
  deserialize(e) {
    super.deserialize(e), this.name = e.name, this.version = e.version, this.shared = e.shared;
  }
}
const wp = (a, e = 1, t = null) => new Pc(a, !1, e, t), Na = (a, e = 0, t = null) => new Pc(a, !0, e, t);
z.FRAME;
const V = /* @__PURE__ */ Na("render", 0, z.RENDER), Mp = /* @__PURE__ */ wp("object", 1, z.OBJECT);
class jr extends ma {
  static get type() {
    return "UniformNode";
  }
  /**
   * Constructs a new uniform node.
   *
   * @param {any} value - The value of this node. Usually a JS primitive or three.js object (vector, matrix, color, texture).
   * @param {?string} nodeType - The node type. If no explicit type is defined, the node tries to derive the type from its value.
   */
  constructor(e, t = null) {
    super(e, t), this.isUniformNode = !0, this.name = "", this.groupNode = Mp;
  }
  /**
   * Sets the {@link UniformNode#name} property.
   *
   * @param {string} name - The name of the uniform.
   * @return {UniformNode} A reference to this node.
   */
  setName(e) {
    return this.name = e, this;
  }
  /**
   * Sets the {@link UniformNode#name} property.
   *
   * @deprecated
   * @param {string} name - The name of the uniform.
   * @return {UniformNode} A reference to this node.
   */
  label(e) {
    return L('TSL: "label()" has been deprecated. Use "setName()" instead.', new Pe()), this.setName(e);
  }
  /**
   * Sets the {@link UniformNode#groupNode} property.
   *
   * @param {UniformGroupNode} group - The uniform group.
   * @return {UniformNode} A reference to this node.
   */
  setGroup(e) {
    return this.groupNode = e, this;
  }
  /**
   * Returns the {@link UniformNode#groupNode}.
   *
   * @return {UniformGroupNode} The uniform group.
   */
  getGroup() {
    return this.groupNode;
  }
  /**
   * By default, this method returns the result of {@link Node#getHash} but derived
   * classes might overwrite this method with a different implementation.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The uniform hash.
   */
  getUniformHash(e) {
    return this.getHash(e);
  }
  onUpdate(e, t) {
    return e = e.bind(this), super.onUpdate((s) => {
      const r = e(s, this);
      r !== void 0 && (this.value = r);
    }, t);
  }
  getInputType(e) {
    let t = super.getInputType(e);
    return t === "bool" && (t = "uint"), t;
  }
  generate(e, t) {
    const s = this.getNodeType(e), r = this.getUniformHash(e);
    let n = e.getNodeFromHash(r);
    n === void 0 && (e.setHashNode(this, r), n = this);
    const i = n.getInputType(e), o = e.getUniformFromNode(n, i, e.shaderStage, this.name || e.context.nodeName), u = e.getPropertyName(o);
    e.context.nodeName !== void 0 && delete e.context.nodeName;
    let l = u;
    if (s === "bool") {
      const c = e.getDataFromNode(this);
      let d = c.propertyName;
      if (d === void 0) {
        const h = e.getVarFromNode(this, null, "bool");
        d = e.getPropertyName(h), c.propertyName = d, l = e.format(u, i, s), e.addLineFlowCode(`${d} = ${l}`, this);
      }
      l = d;
    }
    return e.format(l, s, t);
  }
}
const k = (a, e) => {
  const t = _p(e || a);
  if (t === a && (a = ga(t)), a && a.isNode === !0) {
    let s = a.value;
    a.traverse((r) => {
      r.isConstNode === !0 && (s = r.value);
    }), a = s;
  }
  return new jr(a, t);
};
class nu extends Re {
  static get type() {
    return "ArrayNode";
  }
  /**
   * Constructs a new array node.
   *
   * @param {?string} nodeType - The data type of the elements.
   * @param {number} count - Size of the array.
   * @param {?Array<Node>} [values=null] - Array default values.
   */
  constructor(e, t, s = null) {
    super(e), this.count = t, this.values = s, this.isArrayNode = !0;
  }
  /**
   * Returns the number of elements in the node array.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {number} The number of elements in the node array.
   */
  getArrayCount() {
    return this.count;
  }
  /**
   * Returns the node's type.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The type of the node.
   */
  generateNodeType(e) {
    return this.nodeType === null ? this.values[0].getNodeType(e) : this.nodeType;
  }
  /**
   * Returns the node's type.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The type of the node.
   */
  getElementType(e) {
    return this.getNodeType(e);
  }
  /**
   * Returns the type of a member variable.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {string} name - The name of the member variable.
   * @return {string} The type of the member variable.
   */
  getMemberType(e, t) {
    return this.nodeType === null ? this.values[0].getMemberType(e, t) : super.getMemberType(e, t);
  }
  /**
   * This method builds the output node and returns the resulting array as a shader string.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The generated shader string.
   */
  generate(e) {
    const t = this.getNodeType(e);
    return e.generateArray(t, this.count, this.values);
  }
}
const Bp = (...a) => {
  let e;
  if (a.length === 1) {
    const t = a[0];
    e = new nu(null, t.length, t);
  } else {
    const t = a[0], s = a[1];
    e = new nu(t, s);
  }
  return O(e);
};
S("toArray", (a, e) => Bp(Array(e).fill(a)));
class Fp extends Re {
  static get type() {
    return "AssignNode";
  }
  /**
   * Constructs a new assign node.
   *
   * @param {Node} targetNode - The target node.
   * @param {Node} sourceNode - The source type.
   */
  constructor(e, t) {
    super(), this.targetNode = e, this.sourceNode = t, this.isAssignNode = !0;
  }
  /**
   * Whether this node is used more than once in context of other nodes. This method
   * is overwritten since it always returns `false` (assigns are unique).
   *
   * @return {boolean} A flag that indicates if there is more than one dependency to other nodes. Always `false`.
   */
  hasDependencies() {
    return !1;
  }
  generateNodeType(e, t) {
    return t !== "void" ? this.targetNode.getNodeType(e) : "void";
  }
  /**
   * Whether a split is required when assigning source to target. This can happen when the component length of
   * target and source data type does not match.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {boolean} Whether a split is required when assigning source to target.
   */
  needsSplitAssign(e) {
    const { targetNode: t } = this;
    if (e.isAvailable("swizzleAssign") === !1 && t.isSplitNode && t.components.length > 1) {
      const s = e.getTypeLength(t.node.getNodeType(e));
      return cr.join("").slice(0, s) !== t.components;
    }
    return !1;
  }
  setup(e) {
    const { targetNode: t, sourceNode: s } = this, r = t.getScope(), n = e.getDataFromNode(r);
    n.assign = !0;
    const i = e.getNodeProperties(this);
    i.sourceNode = s, i.targetNode = t.context({ assign: !0 });
  }
  generate(e, t) {
    const { targetNode: s, sourceNode: r } = e.getNodeProperties(this), n = this.needsSplitAssign(e), i = s.build(e), o = s.getNodeType(e), u = r.build(e, o), l = r.getNodeType(e), c = e.getDataFromNode(this);
    let d;
    if (c.initialized === !0)
      t !== "void" && (d = i);
    else if (n) {
      const h = e.getVarFromNode(this, null, o), f = e.getPropertyName(h);
      e.addLineFlowCode(`${f} = ${u}`, this);
      const p = s.node, m = p.node.context({ assign: !0 }).build(e);
      for (let y = 0; y < p.components.length; y++) {
        const _ = p.components[y];
        e.addLineFlowCode(`${m}.${_} = ${f}[ ${y} ]`, this);
      }
      t !== "void" && (d = i);
    } else
      d = `${i} = ${u}`, (t === "void" || l === "void") && (e.addLineFlowCode(d, this), t !== "void" && (d = i));
    return c.initialized = !0, e.format(d, o, t);
  }
}
const Up = /* @__PURE__ */ le(Fp).setParameterLength(2);
S("assign", Up);
class Dp extends Re {
  static get type() {
    return "FunctionCallNode";
  }
  /**
   * Constructs a new function call node.
   *
   * @param {?FunctionNode} functionNode - The function node.
   * @param {Object<string, Node>} [parameters={}] - The parameters for the function call.
   */
  constructor(e = null, t = {}) {
    super(), this.functionNode = e, this.parameters = t;
  }
  /**
   * Sets the parameters of the function call node.
   *
   * @param {Object<string, Node>} parameters - The parameters to set.
   * @return {FunctionCallNode} A reference to this node.
   */
  setParameters(e) {
    return this.parameters = e, this;
  }
  /**
   * Returns the parameters of the function call node.
   *
   * @return {Object<string, Node>} The parameters of this node.
   */
  getParameters() {
    return this.parameters;
  }
  /**
   * Returns the type of this function call node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @returns {string} The type of this node.
   */
  generateNodeType(e) {
    return this.functionNode.getNodeType(e);
  }
  /**
   * Returns the function node of this function call node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {string} [name] - The name of the member.
   * @returns {string} The type of the member.
   */
  getMemberType(e, t) {
    return this.functionNode.getMemberType(e, t);
  }
  generate(e) {
    const t = [], s = this.functionNode, r = s.getInputs(e), n = this.parameters, i = (u, l) => {
      const c = l.type, d = c === "pointer";
      let h;
      return d ? h = "&" + u.build(e) : h = u.build(e, c), h;
    };
    if (Array.isArray(n)) {
      if (n.length > r.length)
        F("TSL: The number of provided parameters exceeds the expected number of inputs in 'Fn()'."), n.length = r.length;
      else if (n.length < r.length)
        for (F("TSL: The number of provided parameters is less than the expected number of inputs in 'Fn()'."); n.length < r.length; )
          n.push(b(0));
      for (let u = 0; u < n.length; u++)
        t.push(i(n[u], r[u]));
    } else
      for (const u of r) {
        const l = n[u.name];
        l !== void 0 ? t.push(i(l, u)) : (F(`TSL: Input '${u.name}' not found in 'Fn()'.`), t.push(i(b(0), u)));
      }
    return `${s.build(e, "property")}( ${t.join(", ")} )`;
  }
}
const Pp = (a, ...e) => (e = e.length > 1 || e[0] && e[0].isNode === !0 ? Js(e) : _a(e[0]), new Dp(O(a), e));
S("call", Pp);
const Lp = {
  "==": "equal",
  "!=": "notEqual",
  "<": "lessThan",
  ">": "greaterThan",
  "<=": "lessThanEqual",
  ">=": "greaterThanEqual",
  "%": "mod"
};
class xe extends Re {
  static get type() {
    return "OperatorNode";
  }
  /**
   * Constructs a new operator node.
   *
   * @param {string} op - The operator.
   * @param {Node} aNode - The first input.
   * @param {Node} bNode - The second input.
   * @param {...Node} params - Additional input parameters.
   */
  constructor(e, t, s, ...r) {
    if (super(), r.length > 0) {
      let n = new xe(e, t, s);
      for (let i = 0; i < r.length - 1; i++)
        n = new xe(e, n, r[i]);
      t = n, s = r[r.length - 1];
    }
    this.op = e, this.aNode = t, this.bNode = s, this.isOperatorNode = !0;
  }
  /**
   * Returns the operator method name.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {string} output - The output type.
   * @returns {string} The operator method name.
   */
  getOperatorMethod(e, t) {
    return e.getMethod(Lp[this.op], t);
  }
  /**
   * This method is overwritten since the node type is inferred from the operator
   * and the input node types.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {?string} [output=null] - The output type.
   * @return {string} The node type.
   */
  generateNodeType(e, t = null) {
    const s = this.op, r = this.aNode, n = this.bNode, i = r.getNodeType(e), o = n ? n.getNodeType(e) : null;
    if (i === "void" || o === "void")
      return t || "void";
    if (s === "%")
      return i;
    if (s === "~" || s === "&" || s === "|" || s === "^" || s === ">>" || s === "<<")
      return e.getIntegerType(i);
    if (s === "!" || s === "&&" || s === "||" || s === "^^")
      return "bool";
    if (s === "==" || s === "!=" || s === "<" || s === ">" || s === "<=" || s === ">=") {
      const u = Math.max(e.getTypeLength(i), e.getTypeLength(o));
      return u > 1 ? `bvec${u}` : "bool";
    } else {
      if (e.isMatrix(i)) {
        if (o === "float")
          return i;
        if (e.isVector(o))
          return e.getVectorFromMatrix(i);
        if (e.isMatrix(o))
          return i;
      } else if (e.isMatrix(o)) {
        if (i === "float")
          return o;
        if (e.isVector(i))
          return e.getVectorFromMatrix(o);
      }
      return e.getTypeLength(o) > e.getTypeLength(i) ? o : i;
    }
  }
  generate(e, t) {
    const s = this.op, { aNode: r, bNode: n } = this, i = this.getNodeType(e, t);
    let o = null, u = null;
    i !== "void" ? (o = r.getNodeType(e), u = n ? n.getNodeType(e) : null, s === "<" || s === ">" || s === "<=" || s === ">=" || s === "==" || s === "!=" ? e.isVector(o) ? u = o : e.isVector(u) ? o = u : o !== u && (o = u = "float") : s === ">>" || s === "<<" ? (o = i, u = e.changeComponentType(u, "uint")) : s === "%" ? (o = i, u = e.isInteger(o) && e.isInteger(u) ? u : o) : e.isMatrix(o) ? u === "float" ? u = "float" : e.isVector(u) ? u = e.getVectorFromMatrix(o) : e.isMatrix(u) || (o = u = i) : e.isMatrix(u) ? o === "float" ? o = "float" : e.isVector(o) ? o = e.getVectorFromMatrix(u) : o = u = i : o = u = i) : o = u = i;
    const l = r.build(e, o), c = n ? n.build(e, u) : null, d = e.getFunctionOperator(s);
    if (t !== "void") {
      const h = e.renderer.coordinateSystem === Dn;
      if (s === "==" || s === "!=" || s === "<" || s === ">" || s === "<=" || s === ">=")
        return h ? e.isVector(o) ? e.format(`${this.getOperatorMethod(e, t)}( ${l}, ${c} )`, i, t) : e.format(`( ${l} ${s} ${c} )`, i, t) : e.format(`( ${l} ${s} ${c} )`, i, t);
      if (s === "%")
        return e.isInteger(u) ? e.format(`( ${l} % ${c} )`, i, t) : e.format(`${this.getOperatorMethod(e, i)}( ${l}, ${c} )`, i, t);
      if (s === "!" || s === "~")
        return e.format(`(${s}${l})`, o, t);
      if (d)
        return e.format(`${d}( ${l}, ${c} )`, i, t);
      if (e.isMatrix(o) && u === "float")
        return e.format(`( ${c} ${s} ${l} )`, i, t);
      if (o === "float" && e.isMatrix(u))
        return e.format(`${l} ${s} ${c}`, i, t);
      {
        let f = `( ${l} ${s} ${c} )`;
        return !h && i === "bool" && e.isVector(o) && e.isVector(u) && (f = `all${f}`), e.format(f, i, t);
      }
    } else if (o !== "void")
      return d ? e.format(`${d}( ${l}, ${c} )`, i, t) : e.isMatrix(o) && u === "float" ? e.format(`${c} ${s} ${l}`, i, t) : e.format(`${l} ${s} ${c}`, i, t);
  }
  serialize(e) {
    super.serialize(e), e.op = this.op;
  }
  deserialize(e) {
    super.deserialize(e), this.op = e.op;
  }
}
const We = /* @__PURE__ */ D(xe, "+").setParameterLength(2, 1 / 0).setName("add"), ke = /* @__PURE__ */ D(xe, "-").setParameterLength(2, 1 / 0).setName("sub"), X = /* @__PURE__ */ D(xe, "*").setParameterLength(2, 1 / 0).setName("mul"), Ct = /* @__PURE__ */ D(xe, "/").setParameterLength(2, 1 / 0).setName("div"), Ra = /* @__PURE__ */ D(xe, "%").setParameterLength(2).setName("mod"), Ip = /* @__PURE__ */ D(xe, "==").setParameterLength(2).setName("equal"), Op = /* @__PURE__ */ D(xe, "!=").setParameterLength(2).setName("notEqual"), Gp = /* @__PURE__ */ D(xe, "<").setParameterLength(2).setName("lessThan"), kp = /* @__PURE__ */ D(xe, ">").setParameterLength(2).setName("greaterThan"), Vp = /* @__PURE__ */ D(xe, "<=").setParameterLength(2).setName("lessThanEqual"), $p = /* @__PURE__ */ D(xe, ">=").setParameterLength(2).setName("greaterThanEqual"), zp = /* @__PURE__ */ D(xe, "&&").setParameterLength(2, 1 / 0).setName("and"), Wp = /* @__PURE__ */ D(xe, "||").setParameterLength(2, 1 / 0).setName("or"), Hp = /* @__PURE__ */ D(xe, "!").setParameterLength(1).setName("not"), qp = /* @__PURE__ */ D(xe, "^^").setParameterLength(2).setName("xor"), Xp = /* @__PURE__ */ D(xe, "&").setParameterLength(2).setName("bitAnd"), Kp = /* @__PURE__ */ D(xe, "~").setParameterLength(1).setName("bitNot"), Qp = /* @__PURE__ */ D(xe, "|").setParameterLength(2).setName("bitOr"), Yp = /* @__PURE__ */ D(xe, "^").setParameterLength(2).setName("bitXor"), jp = /* @__PURE__ */ D(xe, "<<").setParameterLength(2).setName("shiftLeft"), Zp = /* @__PURE__ */ D(xe, ">>").setParameterLength(2).setName("shiftRight"), Jp = A(([a]) => (a.addAssign(1), a)), eg = A(([a]) => (a.subAssign(1), a)), tg = /* @__PURE__ */ A(([a]) => {
  const e = pe(a).toConst();
  return a.addAssign(1), e;
}), sg = /* @__PURE__ */ A(([a]) => {
  const e = pe(a).toConst();
  return a.subAssign(1), e;
});
S("add", We);
S("sub", ke);
S("mul", X);
S("div", Ct);
S("mod", Ra);
S("equal", Ip);
S("notEqual", Op);
S("lessThan", Gp);
S("greaterThan", kp);
S("lessThanEqual", Vp);
S("greaterThanEqual", $p);
S("and", zp);
S("or", Wp);
S("not", Hp);
S("xor", qp);
S("bitAnd", Xp);
S("bitNot", Kp);
S("bitOr", Qp);
S("bitXor", Yp);
S("shiftLeft", jp);
S("shiftRight", Zp);
S("incrementBefore", Jp);
S("decrementBefore", eg);
S("increment", tg);
S("decrement", sg);
const rg = (a, e) => (L('TSL: "modInt()" is deprecated. Use "mod( int( ... ) )" instead.', new Pe()), Ra(pe(a), pe(e)));
S("modInt", rg);
class T extends Re {
  static get type() {
    return "MathNode";
  }
  /**
   * Constructs a new math node.
   *
   * @param {string} method - The method name.
   * @param {Node} aNode - The first input.
   * @param {?Node} [bNode=null] - The second input.
   * @param {?Node} [cNode=null] - The third input.
   */
  constructor(e, t, s = null, r = null) {
    if (super(), (e === T.MAX || e === T.MIN) && arguments.length > 3) {
      let n = new T(e, t, s);
      for (let i = 2; i < arguments.length - 1; i++)
        n = new T(e, n, arguments[i]);
      t = n, s = arguments[arguments.length - 1], r = null;
    }
    this.method = e, this.aNode = t, this.bNode = s, this.cNode = r, this.isMathNode = !0;
  }
  /**
   * The input type is inferred from the node types of the input nodes.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The input type.
   */
  getInputType(e) {
    const t = this.aNode.getNodeType(e), s = this.bNode ? this.bNode.getNodeType(e) : null, r = this.cNode ? this.cNode.getNodeType(e) : null, n = e.isMatrix(t) ? 0 : e.getTypeLength(t), i = e.isMatrix(s) ? 0 : e.getTypeLength(s), o = e.isMatrix(r) ? 0 : e.getTypeLength(r);
    return n > i && n > o ? t : i > o ? s : o > n ? r : t;
  }
  /**
   * The selected method as well as the input type determine the node type of this node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The node type.
   */
  generateNodeType(e) {
    const t = this.method;
    return t === T.LENGTH || t === T.DISTANCE || t === T.DOT ? "float" : t === T.CROSS ? "vec3" : t === T.ALL || t === T.ANY ? "bool" : t === T.EQUALS ? e.changeComponentType(this.aNode.getNodeType(e), "bool") : this.getInputType(e);
  }
  setup(e) {
    const { aNode: t, bNode: s, method: r } = this;
    let n = null;
    if (r === T.ONE_MINUS)
      n = ke(1, t);
    else if (r === T.RECIPROCAL)
      n = Ct(1, t);
    else if (r === T.DIFFERENCE)
      n = Xe(ke(t, s));
    else if (r === T.TRANSFORM_DIRECTION) {
      let i = t, o = s;
      e.isMatrix(i.getNodeType(e)) ? o = W(N(o), 0) : i = W(N(i), 0);
      const u = X(i, o).xyz;
      n = Et(u);
    }
    return n !== null ? n : super.setup(e);
  }
  generate(e, t) {
    if (e.getNodeProperties(this).outputNode)
      return super.generate(e, t);
    let r = this.method;
    const n = this.getNodeType(e), i = this.getInputType(e), o = this.aNode, u = this.bNode, l = this.cNode, c = e.renderer.coordinateSystem;
    if (r === T.NEGATE)
      return e.format("( - " + o.build(e, i) + " )", n, t);
    {
      const d = [];
      return r === T.CROSS ? d.push(
        o.build(e, n),
        u.build(e, n)
      ) : c === Dn && r === T.STEP ? d.push(
        o.build(e, e.getTypeLength(o.getNodeType(e)) === 1 ? "float" : i),
        u.build(e, i)
      ) : c === Dn && (r === T.MIN || r === T.MAX) ? d.push(
        o.build(e, i),
        u.build(e, e.getTypeLength(u.getNodeType(e)) === 1 ? "float" : i)
      ) : r === T.REFRACT ? d.push(
        o.build(e, i),
        u.build(e, i),
        l.build(e, "float")
      ) : r === T.MIX ? d.push(
        o.build(e, i),
        u.build(e, i),
        l.build(e, e.getTypeLength(l.getNodeType(e)) === 1 ? "float" : i)
      ) : (c === kr && r === T.ATAN && u !== null && (r = "atan2"), e.shaderStage !== "fragment" && (r === T.DFDX || r === T.DFDY) && (L(`TSL: '${r}' is not supported in the ${e.shaderStage} stage.`, this.stackTrace), r = "/*" + r + "*/"), d.push(o.build(e, i)), u !== null && d.push(u.build(e, i)), l !== null && d.push(l.build(e, i))), e.format(`${e.getMethod(r, n)}( ${d.join(", ")} )`, n, t);
    }
  }
  serialize(e) {
    super.serialize(e), e.method = this.method;
  }
  deserialize(e) {
    super.deserialize(e), this.method = e.method;
  }
}
T.ALL = "all";
T.ANY = "any";
T.RADIANS = "radians";
T.DEGREES = "degrees";
T.EXP = "exp";
T.EXP2 = "exp2";
T.LOG = "log";
T.LOG2 = "log2";
T.SQRT = "sqrt";
T.INVERSE_SQRT = "inversesqrt";
T.FLOOR = "floor";
T.CEIL = "ceil";
T.NORMALIZE = "normalize";
T.FRACT = "fract";
T.SIN = "sin";
T.SINH = "sinh";
T.COS = "cos";
T.COSH = "cosh";
T.TAN = "tan";
T.TANH = "tanh";
T.ASIN = "asin";
T.ASINH = "asinh";
T.ACOS = "acos";
T.ACOSH = "acosh";
T.ATAN = "atan";
T.ATANH = "atanh";
T.ABS = "abs";
T.SIGN = "sign";
T.LENGTH = "length";
T.NEGATE = "negate";
T.ONE_MINUS = "oneMinus";
T.DFDX = "dFdx";
T.DFDY = "dFdy";
T.ROUND = "round";
T.RECIPROCAL = "reciprocal";
T.TRUNC = "trunc";
T.FWIDTH = "fwidth";
T.TRANSPOSE = "transpose";
T.DETERMINANT = "determinant";
T.INVERSE = "inverse";
T.EQUALS = "equals";
T.MIN = "min";
T.MAX = "max";
T.STEP = "step";
T.REFLECT = "reflect";
T.DISTANCE = "distance";
T.DIFFERENCE = "difference";
T.DOT = "dot";
T.CROSS = "cross";
T.POW = "pow";
T.TRANSFORM_DIRECTION = "transformDirection";
T.MIX = "mix";
T.CLAMP = "clamp";
T.REFRACT = "refract";
T.SMOOTHSTEP = "smoothstep";
T.FACEFORWARD = "faceforward";
const Aa = /* @__PURE__ */ b(1e-6), ng = /* @__PURE__ */ b(Math.PI), ig = /* @__PURE__ */ D(T, T.ALL).setParameterLength(1), og = /* @__PURE__ */ D(T, T.ANY).setParameterLength(1), ag = /* @__PURE__ */ D(T, T.RADIANS).setParameterLength(1), ug = /* @__PURE__ */ D(T, T.DEGREES).setParameterLength(1), Lc = /* @__PURE__ */ D(T, T.EXP).setParameterLength(1), zr = /* @__PURE__ */ D(T, T.EXP2).setParameterLength(1), Ic = /* @__PURE__ */ D(T, T.LOG).setParameterLength(1), $t = /* @__PURE__ */ D(T, T.LOG2).setParameterLength(1), ns = /* @__PURE__ */ D(T, T.SQRT).setParameterLength(1), lg = /* @__PURE__ */ D(T, T.INVERSE_SQRT).setParameterLength(1), vs = /* @__PURE__ */ D(T, T.FLOOR).setParameterLength(1), va = /* @__PURE__ */ D(T, T.CEIL).setParameterLength(1), Et = /* @__PURE__ */ D(T, T.NORMALIZE).setParameterLength(1), wt = /* @__PURE__ */ D(T, T.FRACT).setParameterLength(1), qe = /* @__PURE__ */ D(T, T.SIN).setParameterLength(1), cg = /* @__PURE__ */ D(T, T.SINH).setParameterLength(1), _t = /* @__PURE__ */ D(T, T.COS).setParameterLength(1), dg = /* @__PURE__ */ D(T, T.COSH).setParameterLength(1), hg = /* @__PURE__ */ D(T, T.TAN).setParameterLength(1), fg = /* @__PURE__ */ D(T, T.TANH).setParameterLength(1), pg = /* @__PURE__ */ D(T, T.ASIN).setParameterLength(1), gg = /* @__PURE__ */ D(T, T.ASINH).setParameterLength(1), Oc = /* @__PURE__ */ D(T, T.ACOS).setParameterLength(1), mg = /* @__PURE__ */ D(T, T.ACOSH).setParameterLength(1), xg = /* @__PURE__ */ D(T, T.ATAN).setParameterLength(1, 2), Tg = /* @__PURE__ */ D(T, T.ATANH).setParameterLength(1), Xe = /* @__PURE__ */ D(T, T.ABS).setParameterLength(1), Gc = /* @__PURE__ */ D(T, T.SIGN).setParameterLength(1), zt = /* @__PURE__ */ D(T, T.LENGTH).setParameterLength(1), kc = /* @__PURE__ */ D(T, T.NEGATE).setParameterLength(1), yg = /* @__PURE__ */ D(T, T.ONE_MINUS).setParameterLength(1), Vc = /* @__PURE__ */ D(T, T.DFDX).setParameterLength(1), $c = /* @__PURE__ */ D(T, T.DFDY).setParameterLength(1), _g = /* @__PURE__ */ D(T, T.ROUND).setParameterLength(1), bg = /* @__PURE__ */ D(T, T.RECIPROCAL).setParameterLength(1), Sg = /* @__PURE__ */ D(T, T.TRUNC).setParameterLength(1), zc = /* @__PURE__ */ D(T, T.FWIDTH).setParameterLength(1), Ng = /* @__PURE__ */ D(T, T.TRANSPOSE).setParameterLength(1), Rg = /* @__PURE__ */ D(T, T.DETERMINANT).setParameterLength(1), Ag = /* @__PURE__ */ D(T, T.INVERSE).setParameterLength(1), or = /* @__PURE__ */ D(T, T.MIN).setParameterLength(2, 1 / 0), Be = /* @__PURE__ */ D(T, T.MAX).setParameterLength(2, 1 / 0), Wr = /* @__PURE__ */ D(T, T.STEP).setParameterLength(2), vg = /* @__PURE__ */ D(T, T.REFLECT).setParameterLength(2), Eg = /* @__PURE__ */ D(T, T.DISTANCE).setParameterLength(2), Cg = /* @__PURE__ */ D(T, T.DIFFERENCE).setParameterLength(2), Bs = /* @__PURE__ */ D(T, T.DOT).setParameterLength(2), Fs = /* @__PURE__ */ D(T, T.CROSS).setParameterLength(2), di = /* @__PURE__ */ D(T, T.POW).setParameterLength(2), Wc = (a) => X(a, a), wg = (a) => X(a, a, a), Hc = (a) => X(a, a, a, a), Mg = /* @__PURE__ */ D(T, T.TRANSFORM_DIRECTION).setParameterLength(2), Bg = (a) => X(Gc(a), di(Xe(a), 1 / 3)), qc = (a) => Bs(a, a), ne = /* @__PURE__ */ D(T, T.MIX).setParameterLength(3), os = (a, e = 0, t = 1) => new T(T.CLAMP, O(a), O(e), O(t)), Ea = (a) => os(a), Xc = /* @__PURE__ */ D(T, T.REFRACT).setParameterLength(3), Wt = /* @__PURE__ */ D(T, T.SMOOTHSTEP).setParameterLength(3), Fg = /* @__PURE__ */ D(T, T.FACEFORWARD).setParameterLength(3), Ug = /* @__PURE__ */ A(([a]) => {
  const s = 43758.5453, r = Bs(a.xy, I(12.9898, 78.233)), n = Ra(r, ng);
  return wt(qe(n).mul(s));
}), Dg = (a, e, t) => ne(e, t, a), Pg = (a, e, t) => Wt(e, t, a), Lg = (a, e) => Wr(e, a);
S("all", ig);
S("any", og);
S("radians", ag);
S("degrees", ug);
S("exp", Lc);
S("exp2", zr);
S("log", Ic);
S("log2", $t);
S("sqrt", ns);
S("inverseSqrt", lg);
S("floor", vs);
S("ceil", va);
S("normalize", Et);
S("fract", wt);
S("sin", qe);
S("sinh", cg);
S("cos", _t);
S("cosh", dg);
S("tan", hg);
S("tanh", fg);
S("asin", pg);
S("asinh", gg);
S("acos", Oc);
S("acosh", mg);
S("atan", xg);
S("atanh", Tg);
S("abs", Xe);
S("sign", Gc);
S("length", zt);
S("lengthSq", qc);
S("negate", kc);
S("oneMinus", yg);
S("dFdx", Vc);
S("dFdy", $c);
S("round", _g);
S("reciprocal", bg);
S("trunc", Sg);
S("fwidth", zc);
S("min", or);
S("max", Be);
S("step", Lg);
S("reflect", vg);
S("distance", Eg);
S("dot", Bs);
S("cross", Fs);
S("pow", di);
S("pow2", Wc);
S("pow3", wg);
S("pow4", Hc);
S("transformDirection", Mg);
S("mix", Dg);
S("clamp", os);
S("refract", Xc);
S("smoothstep", Pg);
S("faceForward", Fg);
S("difference", Cg);
S("saturate", Ea);
S("cbrt", Bg);
S("transpose", Ng);
S("determinant", Rg);
S("inverse", Ag);
S("rand", Ug);
class Ig extends G {
  static get type() {
    return "ConditionalNode";
  }
  /**
   * Constructs a new conditional node.
   *
   * @param {Node} condNode - The node that defines the condition.
   * @param {Node} ifNode - The node that is evaluate when the condition ends up `true`.
   * @param {?Node} [elseNode=null] - The node that is evaluate when the condition ends up `false`.
   */
  constructor(e, t, s = null) {
    super(), this.condNode = e, this.ifNode = t, this.elseNode = s;
  }
  /**
   * This method is overwritten since the node type is inferred from the if/else
   * nodes.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The node type.
   */
  generateNodeType(e) {
    const { ifNode: t, elseNode: s } = e.getNodeProperties(this);
    if (t === void 0)
      return e.flowBuildStage(this, "setup"), this.getNodeType(e);
    const r = t.getNodeType(e);
    if (s !== null) {
      const n = s.getNodeType(e);
      if (e.getTypeLength(n) > e.getTypeLength(r))
        return n;
    }
    return r;
  }
  setup(e) {
    const t = this.condNode, s = this.ifNode.isolate(), r = this.elseNode ? this.elseNode.isolate() : null, n = e.context.nodeBlock;
    e.getDataFromNode(s).parentNodeBlock = n, r !== null && (e.getDataFromNode(r).parentNodeBlock = n);
    const i = e.context.uniformFlow, o = e.getNodeProperties(this);
    o.condNode = t, o.ifNode = i ? s : s.context({ nodeBlock: s }), o.elseNode = r ? i ? r : r.context({ nodeBlock: r }) : null;
  }
  generate(e, t) {
    const s = this.getNodeType(e), r = e.getDataFromNode(this);
    if (r.nodeProperty !== void 0)
      return r.nodeProperty;
    const { condNode: n, ifNode: i, elseNode: o } = e.getNodeProperties(this), u = e.currentFunctionNode, l = t !== "void", c = l ? rs(s).build(e) : "";
    r.nodeProperty = c;
    const d = n.build(e, "bool");
    if (e.context.uniformFlow && o !== null) {
      const p = i.build(e, s), g = o.build(e, s), m = e.getTernary(d, p, g);
      return e.format(m, s, t);
    }
    e.addFlowCode(`
${e.tab}if ( ${d} ) {

`).addFlowTab();
    let f = i.build(e, s);
    if (f && (l ? f = c + " = " + f + ";" : (f = "return " + f + ";", u === null && (L("TSL: Return statement used in an inline 'Fn()'. Define a layout struct to allow return values.", this.stackTrace), f = "// " + f))), e.removeFlowTab().addFlowCode(e.tab + "	" + f + `

` + e.tab + "}"), o !== null) {
      e.addFlowCode(` else {

`).addFlowTab();
      let p = o.build(e, s);
      p && (l ? p = c + " = " + p + ";" : (p = "return " + p + ";", u === null && (L("TSL: Return statement used in an inline 'Fn()'. Define a layout struct to allow return values.", this.stackTrace), p = "// " + p))), e.removeFlowTab().addFlowCode(e.tab + "	" + p + `

` + e.tab + `}

`);
    } else
      e.addFlowCode(`

`);
    return e.format(c, s, t);
  }
}
const Ye = /* @__PURE__ */ le(Ig).setParameterLength(2, 3);
S("select", Ye);
class Kc extends G {
  static get type() {
    return "ContextNode";
  }
  /**
   * Constructs a new context node.
   *
   * @param {Node} node - The node whose context should be modified.
   * @param {Object} [value={}] - The modified context data.
   */
  constructor(e = null, t = {}) {
    super(), this.isContextNode = !0, this.node = e, this.value = t;
  }
  /**
   * This method is overwritten to ensure it returns the reference to {@link ContextNode#node}.
   *
   * @return {Node} A reference to {@link ContextNode#node}.
   */
  getScope() {
    return this.node.getScope();
  }
  /**
   * This method is overwritten to ensure it returns the type of {@link ContextNode#node}.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The node type.
   */
  generateNodeType(e) {
    return this.node.getNodeType(e);
  }
  /**
   * Gathers the context data from all parent context nodes.
   *
   * @return {Object} The gathered context data.
   */
  getFlowContextData() {
    const e = [];
    return this.traverse((t) => {
      t.isContextNode === !0 && e.push(t.value);
    }), Object.assign({}, ...e);
  }
  /**
   * This method is overwritten to ensure it returns the member type of {@link ContextNode#node}.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {string} name - The member name.
   * @returns {string} The member type.
   */
  getMemberType(e, t) {
    return this.node.getMemberType(e, t);
  }
  analyze(e) {
    const t = e.addContext(this.value);
    this.node.build(e), e.setContext(t);
  }
  setup(e) {
    const t = e.addContext(this.value);
    this.node.build(e), e.setContext(t);
  }
  generate(e, t) {
    const s = e.addContext(this.value), r = this.node.build(e, t);
    return e.setContext(s), r;
  }
}
const Ls = (a = null, e = {}) => {
  let t = a;
  return (t === null || t.isNode !== !0) && (e = t || e, t = null), new Kc(t, e);
}, Og = (a) => Ls(a, { uniformFlow: !0 }), Qc = (a, e) => Ls(a, { nodeName: e });
function Gg(a, e, t = null) {
  return Ls(t, {
    getShadow: ({ light: s, shadowColorNode: r }) => e === s ? r.mul(a) : r
  });
}
function kg(a, e = null) {
  return Ls(e, {
    getAO: (t, { material: s }) => s.transparent === !0 ? t : t !== null ? t.mul(a) : a
  });
}
function Vg(a, e) {
  return L('TSL: "label()" has been deprecated. Use "setName()" instead.'), Qc(a, e);
}
S("context", Ls);
S("label", Vg);
S("uniformFlow", Og);
S("setName", Qc);
S("builtinShadowContext", (a, e, t) => Gg(e, t, a));
S("builtinAOContext", (a, e) => kg(e, a));
class Bn extends G {
  static get type() {
    return "VarNode";
  }
  /**
   * Constructs a new variable node.
   *
   * @param {Node} node - The node for which a variable should be created.
   * @param {?string} [name=null] - The name of the variable in the shader.
   * @param {boolean} [readOnly=false] - The read-only flag.
   */
  constructor(e, t = null, s = !1) {
    super(), this.node = e, this.name = t, this.global = !0, this.isVarNode = !0, this.readOnly = s, this.parents = !0, this.intent = !1;
  }
  /**
   * Sets the intent flag for this node.
   *
   * This flag is used to indicate that this node is used for intent
   * and should not be built directly. Instead, it is used to indicate that
   * the node should be treated as a variable intent.
   *
   * It's useful for assigning variables without needing creating a new variable node.
   *
   * @param {boolean} value - The value to set for the intent flag.
   * @returns {VarNode} This node.
   */
  setIntent(e) {
    return this.intent = e, this;
  }
  /**
   * Checks if this node is used for intent.
   *
   * @param {NodeBuilder} builder - The node builder.
   * @returns {boolean} Whether this node is used for intent.
   */
  isIntent(e) {
    return e.getDataFromNode(this).forceDeclaration === !0 ? !1 : this.intent;
  }
  /**
   * Returns the intent flag of this node.
   *
   * @return {boolean} The intent flag.
   */
  getIntent() {
    return this.intent;
  }
  getMemberType(e, t) {
    return this.node.getMemberType(e, t);
  }
  getElementType(e) {
    return this.node.getElementType(e);
  }
  generateNodeType(e) {
    return this.node.getNodeType(e);
  }
  getArrayCount(e) {
    return this.node.getArrayCount(e);
  }
  isAssign(e) {
    return e.getDataFromNode(this).assign;
  }
  build(...e) {
    const t = e[0];
    if (this._hasStack(t) === !1 && t.buildStage === "setup" && (t.context.nodeLoop || t.context.nodeBlock)) {
      let s = !1;
      if (this.node.isShaderCallNodeInternal && this.node.shaderNode.getLayout() === null && t.fnCall && t.fnCall.shaderNode && t.getDataFromNode(this.node.shaderNode).hasLoop) {
        const i = t.getDataFromNode(this);
        i.forceDeclaration = !0, s = !0;
      }
      const r = t.getBaseStack();
      s ? r.addToStackBefore(this) : r.addToStack(this);
    }
    return this.isIntent(t) && this.isAssign(t) !== !0 ? this.node.build(...e) : super.build(...e);
  }
  generate(e) {
    const { node: t, name: s, readOnly: r } = this, { renderer: n } = e, i = n.backend.isWebGPUBackend === !0;
    let o = !1, u = !1;
    r && (o = e.isDeterministic(t), u = i ? r : o);
    const l = this.getNodeType(e);
    if (l == "void")
      return this.isIntent(e) !== !0 && F('TSL: ".toVar()" can not be used with void type.', this.stackTrace), t.build(e);
    const c = e.getVectorType(l), d = t.build(e, c), h = e.getVarFromNode(this, s, c, void 0, u), f = e.getPropertyName(h);
    let p = f;
    if (u)
      if (i)
        p = o ? `const ${f}` : `let ${f}`;
      else {
        const g = t.getArrayCount(e);
        p = `const ${e.getVar(h.type, f, g)}`;
      }
    return e.addLineFlowCode(`${p} = ${d}`, this), f;
  }
  _hasStack(e) {
    return e.getDataFromNode(this).stack !== void 0;
  }
}
const Ca = /* @__PURE__ */ le(Bn), $g = (a, e = null) => Ca(a, e).toStack(), zg = (a, e = null) => Ca(a, e, !0).toStack(), Wg = (a) => Ca(a).setIntent(!0).toStack();
S("toVar", $g);
S("toConst", zg);
S("toVarIntent", Wg);
class Hg extends G {
  static get type() {
    return "SubBuild";
  }
  constructor(e, t, s = null) {
    super(s), this.node = e, this.name = t, this.isSubBuildNode = !0;
  }
  generateNodeType(e) {
    if (this.nodeType !== null) return this.nodeType;
    e.addSubBuild(this.name);
    const t = this.node.getNodeType(e);
    return e.removeSubBuild(), t;
  }
  build(e, ...t) {
    e.addSubBuild(this.name);
    const s = this.node.build(e, ...t);
    return e.removeSubBuild(), s;
  }
}
const tr = (a, e, t = null) => new Hg(O(a), e, t);
class qg extends G {
  static get type() {
    return "VaryingNode";
  }
  /**
   * Constructs a new varying node.
   *
   * @param {Node} node - The node for which a varying should be created.
   * @param {?string} name - The name of the varying in the shader.
   */
  constructor(e, t = null) {
    super(), this.node = tr(e, "VERTEX"), this.name = t, this.isVaryingNode = !0, this.interpolationType = null, this.interpolationSampling = null, this.global = !0;
  }
  /**
   * Defines the interpolation type of the varying.
   *
   * @param {string} type - The interpolation type.
   * @param {?string} sampling - The interpolation sampling type
   * @return {VaryingNode} A reference to this node.
   */
  setInterpolation(e, t = null) {
    return this.interpolationType = e, this.interpolationSampling = t, this;
  }
  getHash(e) {
    return this.name || super.getHash(e);
  }
  generateNodeType(e) {
    return this.node.getNodeType(e);
  }
  /**
   * This method performs the setup of a varying node with the current node builder.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {NodeVarying} The node varying from the node builder.
   */
  setupVarying(e) {
    const t = e.getNodeProperties(this);
    let s = t.varying;
    if (s === void 0) {
      const r = this.name, n = this.getNodeType(e), i = this.interpolationType, o = this.interpolationSampling;
      t.varying = s = e.getVaryingFromNode(this, r, n, i, o), t.node = tr(this.node, "VERTEX");
    }
    return s.needsInterpolation || (s.needsInterpolation = e.shaderStage === "fragment"), s;
  }
  setup(e) {
    this.setupVarying(e), e.flowNodeFromShaderStage(tn.VERTEX, this.node);
  }
  analyze(e) {
    this.setupVarying(e), e.flowNodeFromShaderStage(tn.VERTEX, this.node);
  }
  generate(e) {
    const t = e.getSubBuildProperty("property", e.currentStack), s = e.getNodeProperties(this), r = this.setupVarying(e);
    if (s[t] === void 0) {
      const n = this.getNodeType(e), i = e.getPropertyName(r, tn.VERTEX);
      e.flowNodeFromShaderStage(tn.VERTEX, s.node, n, i), s[t] = i;
    }
    return e.getPropertyName(r);
  }
}
const Is = /* @__PURE__ */ le(qg).setParameterLength(1, 2), Xg = (a) => Is(a);
S("toVarying", Is);
S("toVertexStage", Xg);
const Kg = /* @__PURE__ */ A(([a]) => {
  const e = a.mul(0.9478672986).add(0.0521327014).pow(2.4), t = a.mul(0.0773993808), s = a.lessThanEqual(0.04045);
  return ne(e, t, s);
}).setLayout({
  name: "sRGBTransferEOTF",
  type: "vec3",
  inputs: [
    { name: "color", type: "vec3" }
  ]
}), Qg = /* @__PURE__ */ A(([a]) => {
  const e = a.pow(0.41666).mul(1.055).sub(0.055), t = a.mul(12.92), s = a.lessThanEqual(31308e-7);
  return ne(e, t, s);
}).setLayout({
  name: "sRGBTransferOETF",
  type: "vec3",
  inputs: [
    { name: "color", type: "vec3" }
  ]
}), wa = "WorkingColorSpace", Yg = "OutputColorSpace";
class Yc extends Re {
  static get type() {
    return "ColorSpaceNode";
  }
  /**
   * Constructs a new color space node.
   *
   * @param {Node} colorNode - Represents the color to convert.
   * @param {string} source - The source color space.
   * @param {string} target - The target color space.
   */
  constructor(e, t, s) {
    super("vec4"), this.colorNode = e, this.source = t, this.target = s;
  }
  /**
   * This method resolves the constants `WORKING_COLOR_SPACE` and
   * `OUTPUT_COLOR_SPACE` based on the current configuration of the
   * color management and renderer.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {string} colorSpace - The color space to resolve.
   * @return {string} The resolved color space.
   */
  resolveColorSpace(e, t) {
    return t === wa ? ye.workingColorSpace : t === Yg ? e.context.outputColorSpace || e.renderer.outputColorSpace : t;
  }
  setup(e) {
    const { colorNode: t } = this, s = this.resolveColorSpace(e, this.source), r = this.resolveColorSpace(e, this.target);
    let n = t;
    return ye.enabled === !1 || s === r || !s || !r || (ye.getTransfer(s) === q && (n = W(Kg(n.rgb), n.a)), ye.getPrimaries(s) !== ye.getPrimaries(r) && (n = W(
      Me(ye._getMatrix(new Kr(), s, r)).mul(n.rgb),
      n.a
    )), ye.getTransfer(r) === q && (n = W(Qg(n.rgb), n.a))), n;
  }
}
const jg = (a, e) => new Yc(O(a), wa, e), Ma = (a, e) => new Yc(O(a), e, wa);
S("workingToColorSpace", jg);
S("colorSpaceToWorking", Ma);
let Zg = class extends dr {
  static get type() {
    return "ReferenceElementNode";
  }
  /**
   * Constructs a new reference element node.
   *
   * @param {ReferenceBaseNode} referenceNode - The reference node.
   * @param {Node} indexNode - The index node that defines the element access.
   */
  constructor(e, t) {
    super(e, t), this.referenceNode = e, this.isReferenceElementNode = !0;
  }
  /**
   * This method is overwritten since the node type is inferred from
   * the uniform type of the reference node.
   *
   * @return {string} The node type.
   */
  generateNodeType() {
    return this.referenceNode.uniformType;
  }
  generate(e) {
    const t = super.generate(e), s = this.referenceNode.getNodeType(), r = this.getNodeType();
    return e.format(t, s, r);
  }
};
class jc extends G {
  static get type() {
    return "ReferenceBaseNode";
  }
  /**
   * Constructs a new reference base node.
   *
   * @param {string} property - The name of the property the node refers to.
   * @param {string} uniformType - The uniform type that should be used to represent the property value.
   * @param {?Object} [object=null] - The object the property belongs to.
   * @param {?number} [count=null] - When the linked property is an array-like, this parameter defines its length.
   */
  constructor(e, t, s = null, r = null) {
    super(), this.property = e, this.uniformType = t, this.object = s, this.count = r, this.properties = e.split("."), this.reference = s, this.node = null, this.group = null, this.updateType = z.OBJECT;
  }
  /**
   * Sets the uniform group for this reference node.
   *
   * @param {UniformGroupNode} group - The uniform group to set.
   * @return {ReferenceBaseNode} A reference to this node.
   */
  setGroup(e) {
    return this.group = e, this;
  }
  /**
   * When the referred property is array-like, this method can be used
   * to access elements via an index node.
   *
   * @param {IndexNode} indexNode - indexNode.
   * @return {ReferenceElementNode} A reference to an element.
   */
  element(e) {
    return new Zg(this, O(e));
  }
  /**
   * Sets the node type which automatically defines the internal
   * uniform type.
   *
   * @param {string} uniformType - The type to set.
   */
  setNodeType(e) {
    const t = k(null, e);
    this.group !== null && t.setGroup(this.group), this.node = t;
  }
  /**
   * This method is overwritten since the node type is inferred from
   * the type of the reference node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The node type.
   */
  generateNodeType(e) {
    return this.node === null && (this.updateReference(e), this.updateValue()), this.node.getNodeType(e);
  }
  /**
   * Returns the property value from the given referred object.
   *
   * @param {Object} [object=this.reference] - The object to retrieve the property value from.
   * @return {any} The value.
   */
  getValueFromReference(e = this.reference) {
    const { properties: t } = this;
    let s = e[t[0]];
    for (let r = 1; r < t.length; r++)
      s = s[t[r]];
    return s;
  }
  /**
   * Allows to update the reference based on the given state. The state is only
   * evaluated {@link ReferenceBaseNode#object} is not set.
   *
   * @param {(NodeFrame|NodeBuilder)} state - The current state.
   * @return {Object} The updated reference.
   */
  updateReference(e) {
    return this.reference = this.object !== null ? this.object : e.object, this.reference;
  }
  /**
   * The output of the reference node is the internal uniform node.
   *
   * @return {UniformNode} The output node.
   */
  setup() {
    return this.updateValue(), this.node;
  }
  /**
   * Overwritten to update the internal uniform value.
   *
   * @param {NodeFrame} frame - A reference to the current node frame.
   */
  update() {
    this.updateValue();
  }
  /**
   * Retrieves the value from the referred object property and uses it
   * to updated the internal uniform.
   */
  updateValue() {
    this.node === null && this.setNodeType(this.uniformType);
    const e = this.getValueFromReference();
    Array.isArray(e) ? this.node.array = e : this.node.value = e;
  }
}
const Jg = (a, e, t) => new jc(a, e, t);
class em extends jc {
  static get type() {
    return "RendererReferenceNode";
  }
  /**
   * Constructs a new renderer reference node.
   *
   * @param {string} property - The name of the property the node refers to.
   * @param {string} inputType - The uniform type that should be used to represent the property value.
   * @param {?Renderer} [renderer=null] - The renderer the property belongs to. When no renderer is set,
   * the node refers to the renderer of the current state.
   */
  constructor(e, t, s = null) {
    super(e, t, s), this.renderer = s, this.setGroup(V);
  }
  /**
   * Updates the reference based on the given state. The state is only evaluated
   * {@link RendererReferenceNode#renderer} is not set.
   *
   * @param {(NodeFrame|NodeBuilder)} state - The current state.
   * @return {Object} The updated reference.
   */
  updateReference(e) {
    return this.reference = this.renderer !== null ? this.renderer : e.renderer, this.reference;
  }
}
const tm = (a, e, t = null) => new em(a, e, t);
class sm extends Re {
  static get type() {
    return "ToneMappingNode";
  }
  /**
   * Constructs a new tone mapping node.
   *
   * @param {number} toneMapping - The tone mapping type.
   * @param {Node} exposureNode - The tone mapping exposure.
   * @param {Node} [colorNode=null] - The color node to process.
   */
  constructor(e, t = nm, s = null) {
    super("vec3"), this._toneMapping = e, this.exposureNode = t, this.colorNode = s;
  }
  /**
   * Overwrites the default `customCacheKey()` implementation by including the tone
   * mapping type into the cache key.
   *
   * @return {number} The hash.
   */
  customCacheKey() {
    return Fr(this._toneMapping);
  }
  /**
   * Sets the tone mapping type.
   *
   * @param {number} value - The tone mapping type.
   * @return {ToneMappingNode} A reference to this node.
   */
  setToneMapping(e) {
    return this._toneMapping = e, this;
  }
  /**
   * Gets the tone mapping type.
   *
   * @returns {number} The tone mapping type.
   */
  getToneMapping() {
    return this._toneMapping;
  }
  setup(e) {
    const t = this.colorNode || e.context.color, s = this._toneMapping;
    if (s === bs) return t;
    let r = null;
    const n = e.renderer.library.getToneMappingFunction(s);
    return n !== null ? r = W(n(t.rgb, this.exposureNode), t.a) : (F("ToneMappingNode: Unsupported Tone Mapping configuration.", s), r = t), r;
  }
}
const rm = (a, e, t) => new sm(a, O(e), O(t)), nm = /* @__PURE__ */ tm("toneMappingExposure", "float");
S("toneMapping", (a, e, t) => rm(e, t, a));
const iu = /* @__PURE__ */ new WeakMap();
function ou(a, e) {
  let t = iu.get(a);
  return t === void 0 && (t = new Wf(a, e), iu.set(a, t)), t;
}
class Xt extends ma {
  static get type() {
    return "BufferAttributeNode";
  }
  /**
   * Constructs a new buffer attribute node.
   *
   * @param {BufferAttribute|InterleavedBuffer|TypedArray} value - The attribute data.
   * @param {?string} [bufferType=null] - The buffer type (e.g. `'vec3'`).
   * @param {number} [bufferStride=0] - The buffer stride.
   * @param {number} [bufferOffset=0] - The buffer offset.
   */
  constructor(e, t = null, s = 0, r = 0) {
    super(e, t), this.isBufferNode = !0, this.bufferType = t, this.bufferStride = s, this.bufferOffset = r, this.usage = fa, this.instanced = !1, this.attribute = null, this.global = !0, e && e.isBufferAttribute === !0 && e.itemSize <= 4 && (this.attribute = e, this.usage = e.usage, this.instanced = e.isInstancedBufferAttribute);
  }
  /**
   * This method is overwritten since the attribute data might be shared
   * and thus the hash should be shared as well.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The hash.
   */
  getHash(e) {
    let t;
    if (this.bufferStride === 0 && this.bufferOffset === 0) {
      let s = e.globalCache.getData(this.value);
      s === void 0 && (s = {
        node: this
      }, e.globalCache.setData(this.value, s)), t = s.node.id;
    } else
      t = this.id;
    return String(t);
  }
  /**
   * This method is overwritten since the node type is inferred from
   * the buffer attribute.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The node type.
   */
  generateNodeType(e) {
    return this.bufferType === null && (this.bufferType = e.getTypeFromAttribute(this.attribute)), this.bufferType;
  }
  /**
   * Depending on which value was passed to the node, `setup()` behaves
   * differently. If no instance of `BufferAttribute` was passed, the method
   * creates an internal attribute and configures it respectively.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  setup(e) {
    if (this.attribute !== null) return;
    const t = this.getNodeType(e), s = e.getTypeLength(t), r = this.value, n = this.bufferStride || s, i = this.bufferOffset;
    let o;
    r.isInterleavedBuffer === !0 ? o = r : r.isBufferAttribute === !0 ? o = ou(r.array, n) : o = ou(r, n);
    const u = new zf(o, s, i);
    o.setUsage(this.usage), this.attribute = u, this.attribute.isInstancedBufferAttribute = this.instanced;
  }
  /**
   * Generates the code snippet of the buffer attribute node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The generated code snippet.
   */
  generate(e) {
    const t = this.getNodeType(e), s = e.getBufferAttributeFromNode(this, t), r = e.getPropertyName(s);
    let n = null;
    return e.shaderStage === "vertex" || e.shaderStage === "compute" ? (this.name = r, n = r) : n = Is(this).build(e, t), n;
  }
  /**
   * Overwrites the default implementation to return a fixed value `'bufferAttribute'`.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The input type.
   */
  getInputType() {
    return "bufferAttribute";
  }
  /**
   * Sets the `usage` property to the given value.
   *
   * @param {number} value - The usage to set.
   * @return {BufferAttributeNode} A reference to this node.
   */
  setUsage(e) {
    return this.usage = e, this.attribute && this.attribute.isBufferAttribute === !0 && (this.attribute.usage = e), this;
  }
  /**
   * Sets the `instanced` property to the given value.
   *
   * @param {boolean} value - The value to set.
   * @return {BufferAttributeNode} A reference to this node.
   */
  setInstanced(e) {
    return this.instanced = e, this;
  }
}
function Ba(a, e = null, t = 0, s = 0, r = fa, n = !1) {
  return e === "mat3" || e === null && a.itemSize === 9 ? Me(
    new Xt(a, "vec3", 9, 0).setUsage(r).setInstanced(n),
    new Xt(a, "vec3", 9, 3).setUsage(r).setInstanced(n),
    new Xt(a, "vec3", 9, 6).setUsage(r).setInstanced(n)
  ) : e === "mat4" || e === null && a.itemSize === 16 ? As(
    new Xt(a, "vec4", 16, 0).setUsage(r).setInstanced(n),
    new Xt(a, "vec4", 16, 4).setUsage(r).setInstanced(n),
    new Xt(a, "vec4", 16, 8).setUsage(r).setInstanced(n),
    new Xt(a, "vec4", 16, 12).setUsage(r).setInstanced(n)
  ) : new Xt(a, e, t, s).setUsage(r);
}
const Zc = (a, e = null, t = 0, s = 0) => Ba(a, e, t, s), au = (a, e = null, t = 0, s = 0) => Ba(a, e, t, s, fa, !0), uu = (a, e = null, t = 0, s = 0) => Ba(a, e, t, s, Ln, !0);
S("toAttribute", (a) => Zc(a.value));
class me extends G {
  static get type() {
    return "IndexNode";
  }
  /**
   * Constructs a new index node.
   *
   * @param {('vertex'|'instance'|'subgroup'|'invocationLocal'|'invocationGlobal'|'invocationSubgroup'|'draw')} scope - The scope of the index node.
   */
  constructor(e) {
    super("uint"), this.scope = e, this.isIndexNode = !0;
  }
  generate(e) {
    const t = this.getNodeType(e), s = this.scope;
    let r;
    if (s === me.VERTEX)
      r = e.getVertexIndex();
    else if (s === me.INSTANCE)
      r = e.getInstanceIndex();
    else if (s === me.DRAW)
      r = e.getDrawIndex();
    else if (s === me.INVOCATION_LOCAL)
      r = e.getInvocationLocalIndex();
    else if (s === me.INVOCATION_SUBGROUP)
      r = e.getInvocationSubgroupIndex();
    else if (s === me.SUBGROUP)
      r = e.getSubgroupIndex();
    else
      throw new Error("THREE.IndexNode: Unknown scope: " + s);
    let n;
    return e.shaderStage === "vertex" || e.shaderStage === "compute" ? n = r : n = Is(this).build(e, t), n;
  }
}
me.VERTEX = "vertex";
me.INSTANCE = "instance";
me.SUBGROUP = "subgroup";
me.INVOCATION_LOCAL = "invocationLocal";
me.INVOCATION_SUBGROUP = "invocationSubgroup";
me.DRAW = "draw";
const im = /* @__PURE__ */ B(me, me.VERTEX), sr = /* @__PURE__ */ B(me, me.INSTANCE);
me.SUBGROUP;
me.INVOCATION_SUBGROUP;
me.INVOCATION_LOCAL;
const om = /* @__PURE__ */ B(me, me.DRAW);
class am extends G {
  static get type() {
    return "ComputeNode";
  }
  /**
   * Constructs a new compute node.
   *
   * @param {Node} computeNode - The node that defines the compute shader logic.
   * @param {Array<number>} workgroupSize - An array defining the X, Y, and Z dimensions of the workgroup for compute shader execution.
   */
  constructor(e, t) {
    super("void"), this.isComputeNode = !0, this.computeNode = e, this.workgroupSize = t, this.count = null, this.dispatchSize = null, this.version = 1, this.name = "", this.updateBeforeType = z.OBJECT, this.onInitFunction = null, this.countNode = null;
  }
  /**
   * Executes the `dispose` event for this node.
   */
  dispose() {
    this.dispatchEvent({ type: "dispose" });
  }
  /**
   * Sets the {@link ComputeNode#name} property.
   *
   * @param {string} name - The name of the uniform.
   * @return {ComputeNode} A reference to this node.
   */
  setName(e) {
    return this.name = e, this;
  }
  /**
   * Sets the {@link ComputeNode#name} property.
   *
   * @deprecated
   * @param {string} name - The name of the uniform.
   * @return {ComputeNode} A reference to this node.
   */
  label(e) {
    return L('TSL: "label()" has been deprecated. Use "setName()" instead.', new Pe()), this.setName(e);
  }
  /**
   * Sets the callback to run during initialization.
   *
   * @param {Function} callback - The callback function.
   * @return {ComputeNode} A reference to this node.
   */
  onInit(e) {
    return this.onInitFunction = e, this;
  }
  /**
   * The method execute the compute for this node.
   *
   * @param {NodeFrame} frame - A reference to the current node frame.
   */
  updateBefore({ renderer: e }) {
    e.compute(this);
  }
  setup(e) {
    this.count !== null && this.countNode === null && (this.countNode = k(this.count, "uint").onObjectUpdate(() => this.count));
    const t = this.computeNode.build(e);
    if (t) {
      const s = e.getNodeProperties(this);
      s.outputComputeNode = t.outputNode, t.outputNode = null;
    }
    return t;
  }
  generate(e, t) {
    const { shaderStage: s } = e;
    if (s === "compute") {
      const r = this.computeNode.build(e, "void");
      if (r !== "" && e.addLineFlowCode(r, this), this.count !== null && e.allowEarlyReturns === !0) {
        const n = this.countNode.build(e, "uint"), i = sr.build(e, "uint");
        e.flow.code = `${e.tab}if ( ${i} >= ${n} ) { return; }

${e.flow.code}`;
      }
    } else {
      const n = e.getNodeProperties(this).outputComputeNode;
      if (n)
        return n.build(e, t);
    }
  }
}
const Jc = (a, e = [64]) => {
  (e.length === 0 || e.length > 3) && F("TSL: compute() workgroupSize must have 1, 2, or 3 elements", new Pe());
  for (let t = 0; t < e.length; t++) {
    const s = e[t];
    (typeof s != "number" || s <= 0 || !Number.isInteger(s)) && F(`TSL: compute() workgroupSize element at index [ ${t} ] must be a positive integer`, new Pe());
  }
  for (; e.length < 3; ) e.push(1);
  return new am(O(a), e);
}, um = (a, e, t) => {
  const s = Jc(a, t);
  return typeof e == "number" ? s.count = e : s.dispatchSize = e, s;
};
S("compute", um);
S("computeKernel", Jc);
class lm extends G {
  static get type() {
    return "IsolateNode";
  }
  /**
   * Constructs a new cache node.
   *
   * @param {Node} node - The node that should be cached.
   * @param {boolean} [parent=true] - Whether this node refers to a shared parent cache or not.
   */
  constructor(e, t = !0) {
    super(), this.node = e, this.parent = t, this.isIsolateNode = !0;
  }
  generateNodeType(e) {
    const t = e.getCache(), s = e.getCacheFromNode(this, this.parent);
    e.setCache(s);
    const r = this.node.getNodeType(e);
    return e.setCache(t), r;
  }
  build(e, ...t) {
    const s = e.getCache(), r = e.getCacheFromNode(this, this.parent);
    e.setCache(r);
    const n = this.node.build(e, ...t);
    return e.setCache(s), n;
  }
  setParent(e) {
    return this.parent = e, this;
  }
  getParent() {
    return this.parent;
  }
}
const Lr = (a) => new lm(O(a));
function cm(a, e = !0) {
  return L('TSL: "cache()" has been deprecated. Use "isolate()" instead.'), Lr(a).setParent(e);
}
S("cache", cm);
S("isolate", Lr);
class dm extends G {
  static get type() {
    return "BypassNode";
  }
  /**
   * Constructs a new bypass node.
   *
   * @param {Node} outputNode - The output node.
   * @param {Node} callNode - The call node.
   */
  constructor(e, t) {
    super(), this.isBypassNode = !0, this.outputNode = e, this.callNode = t;
  }
  generateNodeType(e) {
    return this.outputNode.getNodeType(e);
  }
  generate(e) {
    const t = this.callNode.build(e, "void");
    return t !== "" && e.addLineFlowCode(t, this), this.outputNode.build(e);
  }
}
const hm = /* @__PURE__ */ le(dm).setParameterLength(2);
S("bypass", hm);
const ed = /* @__PURE__ */ A(([a, e, t, s = b(0), r = b(1), n = ci(!1)]) => {
  let i = a.sub(e).div(t.sub(e));
  return Vo(n) && (i = i.clamp()), i.mul(r.sub(s)).add(s);
});
function fm(a, e, t, s = b(0), r = b(1)) {
  return ed(a, e, t, s, r, !0);
}
S("remap", ed);
S("remapClamp", fm);
class Fn extends G {
  static get type() {
    return "ExpressionNode";
  }
  /**
   * Constructs a new expression node.
   *
   * @param {string} [snippet=''] - The native code snippet.
   * @param {string} [nodeType='void'] - The node type.
   */
  constructor(e = "", t = "void") {
    super(t), this.snippet = e;
  }
  generate(e, t) {
    const s = this.getNodeType(e), r = this.snippet;
    if (s === "void")
      e.addLineFlowCode(r, this);
    else
      return e.format(r, s, t);
  }
}
const bt = /* @__PURE__ */ le(Fn).setParameterLength(1, 2), pm = (a) => (a ? Ye(a, bt("discard")) : bt("discard")).toStack();
S("discard", pm);
class gm extends Re {
  static get type() {
    return "RenderOutputNode";
  }
  /**
   * Constructs a new render output node.
   *
   * @param {Node} colorNode - The color node to process.
   * @param {?number} toneMapping - The tone mapping type.
   * @param {?string} outputColorSpace - The output color space.
   */
  constructor(e, t, s) {
    super("vec4"), this.colorNode = e, this._toneMapping = t, this.outputColorSpace = s, this.isRenderOutputNode = !0;
  }
  /**
   * Sets the tone mapping type.
   *
   * @param {number} value - The tone mapping type.
   * @return {ToneMappingNode} A reference to this node.
   */
  setToneMapping(e) {
    return this._toneMapping = e, this;
  }
  /**
   * Gets the tone mapping type.
   *
   * @returns {number} The tone mapping type.
   */
  getToneMapping() {
    return this._toneMapping;
  }
  setup({ context: e }) {
    let t = this.colorNode || e.color;
    const s = (this._toneMapping !== null ? this._toneMapping : e.toneMapping) || bs, r = (this.outputColorSpace !== null ? this.outputColorSpace : e.outputColorSpace) || ir;
    return s !== bs && (t = t.toneMapping(s)), r !== ir && r !== ye.workingColorSpace && (t = t.workingToColorSpace(r)), t;
  }
}
const td = (a, e = null, t = null) => new gm(O(a), e, t);
S("renderOutput", td);
class mm extends Re {
  static get type() {
    return "DebugNode";
  }
  constructor(e, t = null) {
    super(), this.node = e, this.callback = t;
  }
  generateNodeType(e) {
    return this.node.getNodeType(e);
  }
  setup(e) {
    return this.node.build(e);
  }
  analyze(e) {
    return this.node.build(e);
  }
  generate(e) {
    const t = this.callback, s = this.node.build(e);
    if (t !== null)
      t(e, s);
    else {
      const r = "--- TSL debug - " + e.shaderStage + " shader ---", n = "-".repeat(r.length);
      let i = "";
      i += "// #" + r + `#
`, i += e.flow.code.replace(/^\t/mg, "") + `
`, i += "/* ... */ " + s + ` /* ... */
`, i += "// #" + n + `#
`, Hf(i);
    }
    return s;
  }
}
const xm = (a, e = null) => new mm(O(a), e).toStack();
S("debug", xm);
class sd extends ni {
  /**
   * Creates a new InspectorBase.
   */
  constructor() {
    super(), this._renderer = null, this.currentFrame = null;
  }
  /**
   * Returns the node frame for the current renderer.
   *
   * @return {Object} The node frame.
   */
  get nodeFrame() {
    return this._renderer._nodes.nodeFrame;
  }
  /**
   * Sets the renderer for this inspector.
   *
   * @param {WebGLRenderer} renderer - The renderer to associate with this inspector.
   * @return {InspectorBase} This inspector instance.
   */
  setRenderer(e) {
    return this._renderer = e, this;
  }
  /**
   * Returns the renderer associated with this inspector.
   *
   * @return {WebGLRenderer} The associated renderer.
   */
  getRenderer() {
    return this._renderer;
  }
  /**
   * Initializes the inspector.
   */
  init() {
  }
  /**
   * Called when a frame begins.
   */
  begin() {
  }
  /**
   * Called when a frame ends.
   */
  finish() {
  }
  /**
   * Inspects a node.
   *
   * @param {Node} node - The node to inspect.
   */
  inspect() {
  }
  /**
   * When a compute operation is performed.
   *
   * @param {ComputeNode} computeNode - The compute node being executed.
   * @param {number|Array<number>} dispatchSizeOrCount - The dispatch size or count.
   */
  computeAsync() {
  }
  /**
   * Called when a compute operation begins.
   *
   * @param {string} uid - A unique identifier for the render context.
   * @param {ComputeNode} computeNode - The compute node being executed.
   */
  beginCompute() {
  }
  /**
   * Called when a compute operation ends.
   *
   * @param {string} uid - A unique identifier for the render context.
   * @param {ComputeNode} computeNode - The compute node being executed.
   */
  finishCompute() {
  }
  /**
   * Called when a render operation begins.
   *
   * @param {string} uid - A unique identifier for the render context.
   * @param {Scene} scene - The scene being rendered.
   * @param {Camera} camera - The camera being used for rendering.
   * @param {?WebGLRenderTarget} renderTarget - The render target, if any.
   */
  beginRender() {
  }
  /**
   * Called when an animation loop ends.
   *
   * @param {string} uid - A unique identifier for the render context.
   */
  finishRender() {
  }
  /**
   * Called when a texture copy operation is performed.
   *
   * @param {Texture} srcTexture - The source texture.
   * @param {Texture} dstTexture - The destination texture.
   */
  copyTextureToTexture() {
  }
  /**
   * Called when a framebuffer copy operation is performed.
   *
   * @param {Texture} framebufferTexture - The texture associated with the framebuffer.
   */
  copyFramebufferToTexture() {
  }
}
class Tm extends G {
  /**
   * Returns the type of the node.
   *
   * @returns {string}
   */
  static get type() {
    return "InspectorNode";
  }
  /**
   * Creates an InspectorNode.
   *
   * @param {Node} node - The node to inspect.
   * @param {string} [name=''] - Optional name for the inspector node.
   * @param {Function|null} [callback=null] - Optional callback to modify the node during setup.
   */
  constructor(e, t = "", s = null) {
    super(), this.node = e, this.name = t, this.callback = s, this.updateType = z.FRAME, this.isInspectorNode = !0;
  }
  /**
   * Returns the name of the inspector node.
   *
   * @returns {string}
   */
  getName() {
    return this.name || this.node.name;
  }
  /**
   * Updates the inspector node, allowing inspection of the wrapped node.
   *
   * @param {NodeFrame} frame - A reference to the current node frame.
   */
  update(e) {
    e.renderer.inspector.inspect(this);
  }
  /**
   * Returns the type of the wrapped node.
   *
   * @param {NodeBuilder} builder - The node builder.
   * @returns {string}
   */
  generateNodeType(e) {
    return this.node.getNodeType(e);
  }
  /**
   * Sets up the inspector node.
   *
   * @param {NodeBuilder} builder - The node builder.
   * @returns {Node} The setup node.
   */
  setup(e) {
    let t = this.node;
    return e.context.inspector === !0 && this.callback !== null && (t = this.callback(t)), e.renderer.backend.isWebGPUBackend !== !0 && e.renderer.inspector.constructor !== sd && Te('TSL: ".toInspector()" is only available with WebGPU.'), t;
  }
}
function ym(a, e = "", t = null) {
  return a = O(a), a.before(new Tm(a, e, t));
}
S("toInspector", ym);
class rd extends G {
  static get type() {
    return "AttributeNode";
  }
  /**
   * Constructs a new attribute node.
   *
   * @param {string} attributeName - The name of the attribute.
   * @param {?string} nodeType - The node type.
   */
  constructor(e, t = null) {
    super(t), this.global = !0, this._attributeName = e;
  }
  getHash(e) {
    return this.getAttributeName(e);
  }
  generateNodeType(e) {
    let t = this.nodeType;
    if (t === null) {
      const s = this.getAttributeName(e);
      if (e.hasGeometryAttribute(s)) {
        const r = e.geometry.getAttribute(s);
        t = e.getTypeFromAttribute(r);
      } else
        t = "float";
    }
    return t;
  }
  /**
   * Sets the attribute name to the given value. The method can be
   * overwritten in derived classes if the final name must be computed
   * analytically.
   *
   * @param {string} attributeName - The name of the attribute.
   * @return {AttributeNode} A reference to this node.
   */
  setAttributeName(e) {
    return this._attributeName = e, this;
  }
  /**
   * Returns the attribute name of this node. The method can be
   * overwritten in derived classes if the final name must be computed
   * analytically.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The attribute name.
   */
  getAttributeName() {
    return this._attributeName;
  }
  generate(e) {
    const t = this.getAttributeName(e), s = this.getNodeType(e);
    if (e.hasGeometryAttribute(t) === !0) {
      const n = e.geometry.getAttribute(t), i = e.getTypeFromAttribute(n), o = e.getAttribute(t, i);
      return e.shaderStage === "vertex" ? e.format(o.name, i, s) : Is(this).build(e, s);
    } else
      return L(`AttributeNode: Vertex attribute "${t}" not found on geometry.`), e.generateConst(s);
  }
  serialize(e) {
    super.serialize(e), e.global = this.global, e._attributeName = this._attributeName;
  }
  deserialize(e) {
    super.deserialize(e), this.global = e.global, this._attributeName = e._attributeName;
  }
}
const as = (a, e = null) => new rd(a, e), hr = (a = 0) => as("uv" + (a > 0 ? a : ""), "vec2");
class _m extends G {
  static get type() {
    return "TextureSizeNode";
  }
  /**
   * Constructs a new texture size node.
   *
   * @param {TextureNode} textureNode - A texture node which size should be retrieved.
   * @param {?Node<int>} [levelNode=null] - A level node which defines the requested mip.
   */
  constructor(e, t = null) {
    super("uvec2"), this.isTextureSizeNode = !0, this.textureNode = e, this.levelNode = t;
  }
  generate(e, t) {
    const s = this.textureNode.build(e, "property"), r = this.levelNode === null ? "0" : this.levelNode.build(e, "int");
    return e.format(`${e.getMethod("textureDimensions")}( ${s}, ${r} )`, this.getNodeType(e), t);
  }
}
const rr = /* @__PURE__ */ le(_m).setParameterLength(1, 2);
class bm extends jr {
  static get type() {
    return "MaxMipLevelNode";
  }
  /**
   * Constructs a new max mip level node.
   *
   * @param {TextureNode} textureNode - The texture node to compute the max mip level for.
   */
  constructor(e) {
    super(0), this._textureNode = e, this.updateType = z.FRAME;
  }
  /**
   * The texture node to compute the max mip level for.
   *
   * @readonly
   * @type {TextureNode}
   */
  get textureNode() {
    return this._textureNode;
  }
  /**
   * The texture.
   *
   * @readonly
   * @type {Texture}
   */
  get texture() {
    return this._textureNode.value;
  }
  update() {
    const e = this.texture, t = e.images, s = t && t.length > 0 ? t[0] && t[0].image || t[0] : e.image;
    if (s && s.width !== void 0) {
      const { width: r, height: n } = s;
      this.value = Math.log2(Math.max(r, n));
    }
  }
}
const Sm = /* @__PURE__ */ le(bm).setParameterLength(1);
class Nm extends Error {
  constructor(e, t = null) {
    super(e), this.name = "NodeError", this.stackTrace = t;
  }
}
const nd = /* @__PURE__ */ new da();
class Zr extends jr {
  static get type() {
    return "TextureNode";
  }
  /**
   * Constructs a new texture node.
   *
   * @param {Texture} [value=EmptyTexture] - The texture.
   * @param {?Node<vec2|vec3>} [uvNode=null] - The uv node.
   * @param {?Node<int>} [levelNode=null] - The level node.
   * @param {?Node<float>} [biasNode=null] - The bias node.
   */
  constructor(e = nd, t = null, s = null, r = null) {
    super(e), this.isTextureNode = !0, this.uvNode = t, this.levelNode = s, this.biasNode = r, this.compareNode = null, this.depthNode = null, this.gradNode = null, this.offsetNode = null, this.sampler = !0, this.updateMatrix = !1, this.updateType = z.NONE, this.referenceNode = null, this._value = e, this._matrixUniform = null, this._flipYUniform = null, this.setUpdateMatrix(t === null);
  }
  set value(e) {
    this.referenceNode ? this.referenceNode.value = e : this._value = e;
  }
  /**
   * The texture value.
   *
   * @type {Texture}
   */
  get value() {
    return this.referenceNode ? this.referenceNode.value : this._value;
  }
  /**
   * Overwritten since the uniform hash is defined by the texture's UUID.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The uniform hash.
   */
  getUniformHash() {
    return this.value.uuid;
  }
  /**
   * Overwritten since the node type is inferred from the texture type.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The node type.
   */
  generateNodeType() {
    return this.value.isDepthTexture === !0 ? "float" : this.value.type === _e ? "uvec4" : this.value.type === Ue ? "ivec4" : "vec4";
  }
  /**
   * Overwrites the default implementation to return a fixed value `'texture'`.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The input type.
   */
  getInputType() {
    return "texture";
  }
  /**
   * Returns a default uvs based on the current texture's channel.
   *
   * @return {AttributeNode<vec2>} The default uvs.
   */
  getDefaultUV() {
    return hr(this.value.channel);
  }
  /**
   * Overwritten to always return the texture reference of the node.
   *
   * @param {any} state - This method can be invocated in different contexts so `state` can refer to any object type.
   * @return {Texture} The texture reference.
   */
  updateReference() {
    return this.value;
  }
  /**
   * Transforms the given uv node with the texture transformation matrix.
   *
   * @param {Node} uvNode - The uv node to transform.
   * @return {Node} The transformed uv node.
   */
  getTransformedUV(e) {
    return this._matrixUniform === null && (this._matrixUniform = k(this.value.matrix)), this._matrixUniform.mul(N(e, 1)).xy;
  }
  /**
   * Defines whether the uv transformation matrix should automatically be updated or not.
   *
   * @param {boolean} value - The update toggle.
   * @return {TextureNode} A reference to this node.
   */
  setUpdateMatrix(e) {
    return this.updateMatrix = e, this;
  }
  /**
   * Setups the uv node. Depending on the backend as well as texture's image and type, it might be necessary
   * to modify the uv node for correct sampling.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {Node} uvNode - The uv node to setup.
   * @return {Node} The updated uv node.
   */
  setupUV(e, t) {
    return e.isFlipY() && (this._flipYUniform === null && (this._flipYUniform = k(!1)), t = t.toVar(), this.sampler ? t = this._flipYUniform.select(t.flipY(), t) : t = this._flipYUniform.select(t.setY(pe(rr(this, this.levelNode).y).sub(t.y).sub(1)), t)), t;
  }
  /**
   * Setups texture node by preparing the internal nodes for code generation.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  setup(e) {
    const t = e.getNodeProperties(this);
    t.referenceNode = this.referenceNode;
    const s = this.value;
    if (!s || s.isTexture !== !0)
      throw new Nm("THREE.TSL: `texture( value )` function expects a valid instance of THREE.Texture().", this.stackTrace);
    const r = A(() => {
      let u = this.uvNode;
      return (u === null || e.context.forceUVContext === !0) && e.context.getUV && (u = e.context.getUV(this, e)), u || (u = this.getDefaultUV()), this.updateMatrix === !0 && (u = this.getTransformedUV(u)), u = this.setupUV(e, u), this.updateType = this._matrixUniform !== null || this._flipYUniform !== null ? z.OBJECT : z.NONE, u;
    })();
    let n = this.levelNode;
    n === null && e.context.getTextureLevel && (n = e.context.getTextureLevel(this));
    let i = null, o = null;
    if (this.compareNode !== null)
      if (e.renderer.hasCompatibility(Ps.TEXTURE_COMPARE))
        i = this.compareNode;
      else {
        const u = s.compareFunction;
        u === null || u === la || u === Qr || u === On || u === nr ? o = this.compareNode : (i = this.compareNode, Te('TSL: Only "LessCompare", "LessEqualCompare", "GreaterCompare" and "GreaterEqualCompare" are supported for depth texture comparison fallback.'));
      }
    t.uvNode = r, t.levelNode = n, t.biasNode = this.biasNode, t.compareNode = i, t.compareStepNode = o, t.gradNode = this.gradNode, t.depthNode = this.depthNode, t.offsetNode = this.offsetNode;
  }
  /**
   * Generates the uv code snippet.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {Node} uvNode - The uv node to generate code for.
   * @return {string} The generated code snippet.
   */
  generateUV(e, t) {
    return t.build(e, this.sampler === !0 ? "vec2" : "ivec2");
  }
  /**
   * Generates the offset code snippet.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {Node} offsetNode - The offset node to generate code for.
   * @return {string} The generated code snippet.
   */
  generateOffset(e, t) {
    return t.build(e, "ivec2");
  }
  /**
   * Generates the snippet for the texture sampling.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {string} textureProperty - The texture property.
   * @param {string} uvSnippet - The uv snippet.
   * @param {?string} levelSnippet - The level snippet.
   * @param {?string} biasSnippet - The bias snippet.
   * @param {?string} depthSnippet - The depth snippet.
   * @param {?string} compareSnippet - The compare snippet.
   * @param {?Array<string>} gradSnippet - The grad snippet.
   * @param {?string} offsetSnippet - The offset snippet.
   * @return {string} The generated code snippet.
   */
  generateSnippet(e, t, s, r, n, i, o, u, l) {
    const c = this.value;
    let d;
    return n ? d = e.generateTextureBias(c, t, s, n, i, l) : u ? d = e.generateTextureGrad(c, t, s, u, i, l) : o ? d = e.generateTextureCompare(c, t, s, o, i, l) : this.sampler === !1 ? d = e.generateTextureLoad(c, t, s, r, i, l) : r ? d = e.generateTextureLevel(c, t, s, r, i, l) : d = e.generateTexture(c, t, s, i, l), d;
  }
  /**
   * Generates the code snippet of the texture node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {string} output - The current output.
   * @return {string} The generated code snippet.
   */
  generate(e, t) {
    const s = this.value, r = e.getNodeProperties(this), n = super.generate(e, "property");
    if (/^sampler/.test(t))
      return n + "_sampler";
    if (e.isReference(t))
      return n;
    {
      const i = e.getDataFromNode(this), o = this.getNodeType(e);
      let u = i.propertyName;
      if (u === void 0) {
        const { uvNode: c, levelNode: d, biasNode: h, compareNode: f, compareStepNode: p, depthNode: g, gradNode: m, offsetNode: y } = r, _ = this.generateUV(e, c), v = d ? d.build(e, "float") : null, C = h ? h.build(e, "float") : null, w = g ? g.build(e, "int") : null, M = f ? f.build(e, "float") : null, E = p ? p.build(e, "float") : null, U = m ? [m[0].build(e, "vec2"), m[1].build(e, "vec2")] : null, P = y ? this.generateOffset(e, y) : null;
        let $ = w;
        $ === null && s.isArrayTexture && this.isTexture3DNode !== !0 && ($ = "0");
        const Y = e.getVarFromNode(this);
        u = e.getPropertyName(Y);
        let ie = this.generateSnippet(e, n, _, v, C, $, M, U, P);
        if (E !== null) {
          const se = s.compareFunction;
          se === On || se === nr ? ie = Wr(bt(ie, o), bt(E, "float")).build(e, o) : ie = Wr(bt(E, "float"), bt(ie, o)).build(e, o);
        }
        e.addLineFlowCode(`${u} = ${ie}`, this), i.snippet = ie, i.propertyName = u;
      }
      let l = u;
      return e.needsToWorkingColorSpace(s) && (l = Ma(bt(l, o), s.colorSpace).setup(e).build(e, o)), e.format(l, o, t);
    }
  }
  /**
   * Sets the sampler value.
   *
   * @param {boolean} value - The sampler value to set.
   * @return {TextureNode} A reference to this texture node.
   */
  setSampler(e) {
    return this.sampler = e, this;
  }
  /**
   * Returns the sampler value.
   *
   * @return {boolean} The sampler value.
   */
  getSampler() {
    return this.sampler;
  }
  // @TODO: Move to TSL
  /**
   * Samples the texture with the given uv node.
   *
   * @param {Node} uvNode - The uv node.
   * @return {TextureNode} A texture node representing the texture sample.
   */
  sample(e) {
    const t = this.clone();
    return t.uvNode = O(e), t.referenceNode = this.getBase(), O(t);
  }
  /**
   * TSL function for creating a texture node that fetches/loads texels without interpolation.
   *
   * @param {Node<uvec2>} uvNode - The uv node.
   * @returns {TextureNode} A texture node representing the texture load.
   */
  load(e) {
    return this.sample(e).setSampler(!1);
  }
  /**
   * Samples a blurred version of the texture by defining an internal bias.
   *
   * @param {Node<float>} amountNode - How blurred the texture should be.
   * @return {TextureNode} A texture node representing the texture sample.
   */
  blur(e) {
    const t = this.clone();
    t.biasNode = O(e).mul(Sm(t)), t.referenceNode = this.getBase();
    const s = t.value;
    return t.generateMipmaps === !1 && (s && s.generateMipmaps === !1 || s.minFilter === Qe || s.magFilter === Qe) && (L("TSL: texture().blur() requires mipmaps and sampling. Use .generateMipmaps=true and .minFilter/.magFilter=THREE.LinearFilter in the Texture."), t.biasNode = null), O(t);
  }
  /**
   * Samples a specific mip of the texture.
   *
   * @param {Node<int>} levelNode - The mip level to sample.
   * @return {TextureNode} A texture node representing the texture sample.
   */
  level(e) {
    const t = this.clone();
    return t.levelNode = O(e), t.referenceNode = this.getBase(), O(t);
  }
  /**
   * Returns the texture size of the requested level.
   *
   * @param {Node<int>} levelNode - The level to compute the size for.
   * @return {TextureSizeNode} The texture size.
   */
  size(e) {
    return rr(this, e);
  }
  /**
   * Samples the texture with the given bias.
   *
   * @param {Node<float>} biasNode - The bias node.
   * @return {TextureNode} A texture node representing the texture sample.
   */
  bias(e) {
    const t = this.clone();
    return t.biasNode = O(e), t.referenceNode = this.getBase(), O(t);
  }
  /**
   * Returns the base texture of this node.
   * @return {TextureNode} The base texture node.
   */
  getBase() {
    return this.referenceNode ? this.referenceNode.getBase() : this;
  }
  /**
   * Samples the texture by executing a compare operation.
   *
   * @param {Node<float>} compareNode - The node that defines the compare value.
   * @return {TextureNode} A texture node representing the texture sample.
   */
  compare(e) {
    const t = this.clone();
    return t.compareNode = O(e), t.referenceNode = this.getBase(), O(t);
  }
  /**
   * Samples the texture using an explicit gradient.
   *
   * @param {Node<vec2>} gradNodeX - The gradX node.
   * @param {Node<vec2>} gradNodeY - The gradY node.
   * @return {TextureNode} A texture node representing the texture sample.
   */
  grad(e, t) {
    const s = this.clone();
    return s.gradNode = [O(e), O(t)], s.referenceNode = this.getBase(), O(s);
  }
  /**
   * Samples the texture by defining a depth node.
   *
   * @param {Node<int>} depthNode - The depth node.
   * @return {TextureNode} A texture node representing the texture sample.
   */
  depth(e) {
    const t = this.clone();
    return t.depthNode = O(e), t.referenceNode = this.getBase(), O(t);
  }
  /**
   * Samples the texture by defining an offset node.
   *
   * @param {Node<ivec2>} offsetNode - The offset node.
   * @return {TextureNode} A texture node representing the texture sample.
   */
  offset(e) {
    const t = this.clone();
    return t.offsetNode = O(e), t.referenceNode = this.getBase(), O(t);
  }
  // --
  serialize(e) {
    super.serialize(e), e.value = this.value.toJSON(e.meta).uuid, e.sampler = this.sampler, e.updateMatrix = this.updateMatrix, e.updateType = this.updateType;
  }
  deserialize(e) {
    super.deserialize(e), this.value = e.meta.textures[e.value], this.sampler = e.sampler, this.updateMatrix = e.updateMatrix, this.updateType = e.updateType;
  }
  /**
   * The update is used to implement the update of the uv transformation matrix.
   */
  update() {
    const e = this.value, t = this._matrixUniform;
    t !== null && (t.value = e.matrix), e.matrixAutoUpdate === !0 && e.updateMatrix();
    const s = this._flipYUniform;
    s !== null && (s.value = e.image instanceof ImageBitmap && e.flipY === !0 || e.isRenderTargetTexture === !0 || e.isFramebufferTexture === !0 || e.isDepthTexture === !0);
  }
  /**
   * Clones the texture node.
   *
   * @return {TextureNode} The cloned texture node.
   */
  clone() {
    const e = new this.constructor(this.value, this.uvNode, this.levelNode, this.biasNode);
    return e.sampler = this.sampler, e.depthNode = this.depthNode, e.compareNode = this.compareNode, e.gradNode = this.gradNode, e.offsetNode = this.offsetNode, e;
  }
}
const Rm = /* @__PURE__ */ le(Zr).setParameterLength(1, 4).setName("texture"), ce = (a = nd, e = null, t = null, s = null) => {
  let r;
  return a && a.isTextureNode === !0 ? (r = O(a.clone()), r.referenceNode = a.getBase(), e !== null && (r.uvNode = O(e)), t !== null && (r.levelNode = O(t)), s !== null && (r.biasNode = O(s))) : r = Rm(a, e, t, s), r;
}, tt = (...a) => ce(...a).setSampler(!1);
class Fa extends jr {
  static get type() {
    return "BufferNode";
  }
  /**
   * Constructs a new buffer node.
   *
   * @param {Array<number>} value - Array-like buffer data.
   * @param {string} bufferType - The data type of the buffer.
   * @param {number} [bufferCount=0] - The count of buffer elements.
   */
  constructor(e, t, s = 0) {
    super(e, t), this.isBufferNode = !0, this.bufferType = t, this.bufferCount = s, this.updateRanges = [];
  }
  /**
   * Adds a range of data in the data array to be updated on the GPU.
   *
   * @param {number} start - Position at which to start update.
   * @param {number} count - The number of components to update.
   */
  addUpdateRange(e, t) {
    this.updateRanges.push({ start: e, count: t });
  }
  /**
   * Clears the update ranges.
   */
  clearUpdateRanges() {
    this.updateRanges.length = 0;
  }
  /**
   * The data type of the buffer elements.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The element type.
   */
  getElementType(e) {
    return this.getNodeType(e);
  }
  /**
   * Overwrites the default implementation to return a fixed value `'buffer'`.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The input type.
   */
  getInputType() {
    return "buffer";
  }
}
const id = (a, e, t) => new Fa(a, e, t);
class Am extends dr {
  static get type() {
    return "UniformArrayElementNode";
  }
  /**
   * Constructs a new buffer node.
   *
   * @param {UniformArrayNode} uniformArrayNode - The uniform array node to access.
   * @param {IndexNode} indexNode - The index data that define the position of the accessed element in the array.
   */
  constructor(e, t) {
    super(e, t), this.isArrayBufferElementNode = !0;
  }
  generate(e) {
    const t = super.generate(e), s = this.getNodeType(e), r = this.node.getPaddedType();
    return e.format(t, r, s);
  }
}
class vm extends Fa {
  static get type() {
    return "UniformArrayNode";
  }
  /**
   * Constructs a new uniform array node.
   *
   * @param {Array<any>} value - Array holding the buffer data.
   * @param {?string} [elementType=null] - The data type of a buffer element.
   */
  constructor(e, t = null) {
    super(null), this.array = e, this.elementType = t === null ? zn(e[0]) : t, this.paddedType = this.getPaddedType(), this.updateType = z.RENDER, this.isArrayBufferNode = !0;
  }
  /**
   * This method is overwritten since the node type is inferred from the
   * {@link UniformArrayNode#paddedType}.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The node type.
   */
  generateNodeType() {
    return this.paddedType;
  }
  /**
   * The data type of the array elements.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The element type.
   */
  getElementType() {
    return this.elementType;
  }
  /**
   * Returns the padded type based on the element type.
   *
   * @return {string} The padded type.
   */
  getPaddedType() {
    const e = this.elementType;
    let t = "vec4";
    return e === "mat2" ? t = "mat2" : /mat/.test(e) === !0 ? t = "mat4" : e.charAt(0) === "i" ? t = "ivec4" : e.charAt(0) === "u" && (t = "uvec4"), t;
  }
  /**
   * The update makes sure to correctly transfer the data from the (complex) objects
   * in the array to the internal, correctly padded value buffer.
   *
   * @param {NodeFrame} frame - A reference to the current node frame.
   */
  update() {
    const { array: e, value: t } = this, s = this.elementType;
    if (s === "float" || s === "int" || s === "uint")
      for (let r = 0; r < e.length; r++) {
        const n = r * 4;
        t[n] = e[r];
      }
    else if (s === "color")
      for (let r = 0; r < e.length; r++) {
        const n = r * 4, i = e[r];
        t[n] = i.r, t[n + 1] = i.g, t[n + 2] = i.b || 0;
      }
    else if (s === "mat2")
      for (let r = 0; r < e.length; r++) {
        const n = r * 4, i = e[r];
        t[n] = i.elements[0], t[n + 1] = i.elements[1], t[n + 2] = i.elements[2], t[n + 3] = i.elements[3];
      }
    else if (s === "mat3")
      for (let r = 0; r < e.length; r++) {
        const n = r * 16, i = e[r];
        t[n] = i.elements[0], t[n + 1] = i.elements[1], t[n + 2] = i.elements[2], t[n + 4] = i.elements[3], t[n + 5] = i.elements[4], t[n + 6] = i.elements[5], t[n + 8] = i.elements[6], t[n + 9] = i.elements[7], t[n + 10] = i.elements[8], t[n + 15] = 1;
      }
    else if (s === "mat4")
      for (let r = 0; r < e.length; r++) {
        const n = r * 16, i = e[r];
        for (let o = 0; o < i.elements.length; o++)
          t[n + o] = i.elements[o];
      }
    else
      for (let r = 0; r < e.length; r++) {
        const n = r * 4, i = e[r];
        t[n] = i.x, t[n + 1] = i.y, t[n + 2] = i.z || 0, t[n + 3] = i.w || 0;
      }
  }
  /**
   * Implement the value buffer creation based on the array data.
   *
   * @param {NodeBuilder} builder - A reference to the current node builder.
   * @return {null}
   */
  setup(e) {
    const t = this.array.length, s = this.elementType;
    let r = Float32Array;
    const n = this.paddedType, i = e.getTypeLength(n);
    return s.charAt(0) === "i" && (r = Int32Array), s.charAt(0) === "u" && (r = Uint32Array), this.value = new r(t * i), this.bufferCount = t, this.bufferType = n, this.update(), super.setup(e);
  }
  /**
   * Overwrites the default `element()` method to provide element access
   * based on {@link UniformArrayNode}.
   *
   * @param {IndexNode} indexNode - The index node.
   * @return {UniformArrayElementNode}
   */
  element(e) {
    return new Am(this, O(e));
  }
}
const st = (a, e) => new vm(a, e);
class Em extends G {
  /**
   * Constructs a new builtin node.
   *
   * @param {string} name - The name of the built-in shader variable.
   */
  constructor(e) {
    super("float"), this.name = e, this.isBuiltinNode = !0;
  }
  /**
   * Generates the code snippet of the builtin node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The generated code snippet.
   */
  generate() {
    return this.name;
  }
}
const fr = le(Em).setParameterLength(1);
let xr, Tr;
class de extends G {
  static get type() {
    return "ScreenNode";
  }
  /**
   * Constructs a new screen node.
   *
   * @param {('coordinate'|'viewport'|'size'|'uv'|'dpr')} scope - The node's scope.
   */
  constructor(e) {
    super(), this.scope = e, this._output = null, this.isViewportNode = !0;
  }
  /**
   * This method is overwritten since the node type depends on the selected scope.
   *
   * @return {('float'|'vec2'|'vec4')} The node type.
   */
  generateNodeType() {
    return this.scope === de.DPR ? "float" : this.scope === de.VIEWPORT ? "vec4" : "vec2";
  }
  /**
   * This method is overwritten since the node's update type depends on the selected scope.
   *
   * @return {NodeUpdateType} The update type.
   */
  getUpdateType() {
    let e = z.NONE;
    return (this.scope === de.SIZE || this.scope === de.VIEWPORT || this.scope === de.DPR) && (e = z.RENDER), this.updateType = e, e;
  }
  /**
   * `ScreenNode` implements {@link Node#update} to retrieve viewport and size information
   * from the current renderer.
   *
   * @param {NodeFrame} frame - A reference to the current node frame.
   */
  update({ renderer: e }) {
    const t = e.getRenderTarget();
    this.scope === de.VIEWPORT ? t !== null ? Tr.copy(t.viewport) : (e.getViewport(Tr), Tr.multiplyScalar(e.getPixelRatio())) : this.scope === de.DPR ? this._output.value = e.getPixelRatio() : t !== null ? (xr.width = t.width, xr.height = t.height) : e.getDrawingBufferSize(xr);
  }
  setup() {
    const e = this.scope;
    let t = null;
    return e === de.SIZE ? t = k(xr || (xr = new je())) : e === de.VIEWPORT ? t = k(Tr || (Tr = new Se())) : e === de.DPR ? t = k(1) : t = I(pr.div(Ko)), this._output = t, t;
  }
  generate(e) {
    if (this.scope === de.COORDINATE) {
      let t = e.getFragCoord();
      if (e.isFlipY()) {
        const s = e.getNodeProperties(Ko).outputNode.build(e);
        t = `${e.getType("vec2")}( ${t}.x, ${s}.y - ${t}.y )`;
      }
      return t;
    }
    return super.generate(e);
  }
}
de.COORDINATE = "coordinate";
de.VIEWPORT = "viewport";
de.SIZE = "size";
de.UV = "uv";
de.DPR = "dpr";
const Cm = /* @__PURE__ */ B(de, de.DPR), Es = /* @__PURE__ */ B(de, de.UV), Ko = /* @__PURE__ */ B(de, de.SIZE), pr = /* @__PURE__ */ B(de, de.COORDINATE), od = /* @__PURE__ */ B(de, de.VIEWPORT), wm = od.zw;
od.xy;
let wi = null, nn = null, Mi = null, on = null, Bi = null, an = null, Fi = null, un = null;
const hi = /* @__PURE__ */ k(0, "uint").setName("u_cameraIndex").setGroup(Na("cameraIndex")).toVarying("v_cameraIndex"), xs = /* @__PURE__ */ k("float").setName("cameraNear").setGroup(V).onRenderUpdate(({ camera: a }) => a.near), Ts = /* @__PURE__ */ k("float").setName("cameraFar").setGroup(V).onRenderUpdate(({ camera: a }) => a.far), Ir = /* @__PURE__ */ A(({ camera: a }) => {
  let e;
  if (a.isArrayCamera && a.cameras.length > 0) {
    const t = [];
    for (const s of a.cameras)
      t.push(s.projectionMatrix);
    nn === null ? nn = st(t).setGroup(V).setName("cameraProjectionMatrices") : nn.array = t, e = nn.element(a.isMultiViewCamera ? fr("gl_ViewID_OVR") : hi).toConst("cameraProjectionMatrix");
  } else
    wi === null && (wi = k(a.projectionMatrix).setName("cameraProjectionMatrix").setGroup(V).onRenderUpdate(({ camera: t }) => t.projectionMatrix)), e = wi;
  return e;
}).once()(), Mm = /* @__PURE__ */ A(({ camera: a }) => {
  let e;
  if (a.isArrayCamera && a.cameras.length > 0) {
    const t = [];
    for (const s of a.cameras)
      t.push(s.projectionMatrixInverse);
    on === null ? on = st(t).setGroup(V).setName("cameraProjectionMatricesInverse") : on.array = t, e = on.element(a.isMultiViewCamera ? fr("gl_ViewID_OVR") : hi).toConst("cameraProjectionMatrixInverse");
  } else
    Mi === null && (Mi = k(a.projectionMatrixInverse).setName("cameraProjectionMatrixInverse").setGroup(V).onRenderUpdate(({ camera: t }) => t.projectionMatrixInverse)), e = Mi;
  return e;
}).once()(), qt = /* @__PURE__ */ A(({ camera: a }) => {
  let e;
  if (a.isArrayCamera && a.cameras.length > 0) {
    const t = [];
    for (const s of a.cameras)
      t.push(s.matrixWorldInverse);
    an === null ? an = st(t).setGroup(V).setName("cameraViewMatrices") : an.array = t, e = an.element(a.isMultiViewCamera ? fr("gl_ViewID_OVR") : hi).toConst("cameraViewMatrix");
  } else
    Bi === null && (Bi = k(a.matrixWorldInverse).setName("cameraViewMatrix").setGroup(V).onRenderUpdate(({ camera: t }) => t.matrixWorldInverse)), e = Bi;
  return e;
}).once()(), Bm = /* @__PURE__ */ A(({ camera: a }) => {
  let e;
  if (a.isArrayCamera && a.cameras.length > 0) {
    const t = [];
    for (let s = 0, r = a.cameras.length; s < r; s++)
      t.push(new H());
    un === null ? un = st(t).setGroup(V).setName("cameraPositions").onRenderUpdate(({ camera: s }, r) => {
      const n = s.cameras, i = r.array;
      for (let o = 0, u = n.length; o < u; o++)
        i[o].setFromMatrixPosition(n[o].matrixWorld);
    }) : un.array = t, e = un.element(a.isMultiViewCamera ? fr("gl_ViewID_OVR") : hi).toConst("cameraPosition");
  } else
    Fi === null && (Fi = k(new H()).setName("cameraPosition").setGroup(V).onRenderUpdate(({ camera: t }, s) => s.value.setFromMatrixPosition(t.matrixWorld))), e = Fi;
  return e;
}).once()(), lu = /* @__PURE__ */ new Lf();
class fe extends G {
  static get type() {
    return "Object3DNode";
  }
  /**
   * Constructs a new object 3D node.
   *
   * @param {('position'|'viewPosition'|'direction'|'scale'|'worldMatrix')} scope - The node represents a different type of transformation depending on the scope.
   * @param {?Object3D} [object3d=null] - The 3D object.
   */
  constructor(e, t = null) {
    super(), this.scope = e, this.object3d = t, this.updateType = z.OBJECT, this.uniformNode = new jr(null);
  }
  /**
   * Overwritten since the node type is inferred from the scope.
   *
   * @return {('mat4'|'vec3'|'float')} The node type.
   */
  generateNodeType() {
    const e = this.scope;
    if (e === fe.WORLD_MATRIX)
      return "mat4";
    if (e === fe.POSITION || e === fe.VIEW_POSITION || e === fe.DIRECTION || e === fe.SCALE)
      return "vec3";
    if (e === fe.RADIUS)
      return "float";
  }
  /**
   * Updates the uniform value depending on the scope.
   *
   * @param {NodeFrame} frame - The current node frame.
   */
  update(e) {
    const t = this.object3d, s = this.uniformNode, r = this.scope;
    if (r === fe.WORLD_MATRIX)
      s.value = t.matrixWorld;
    else if (r === fe.POSITION)
      s.value = s.value || new H(), s.value.setFromMatrixPosition(t.matrixWorld);
    else if (r === fe.SCALE)
      s.value = s.value || new H(), s.value.setFromMatrixScale(t.matrixWorld);
    else if (r === fe.DIRECTION)
      s.value = s.value || new H(), t.getWorldDirection(s.value);
    else if (r === fe.VIEW_POSITION) {
      const n = e.camera;
      s.value = s.value || new H(), s.value.setFromMatrixPosition(t.matrixWorld), s.value.applyMatrix4(n.matrixWorldInverse);
    } else if (r === fe.RADIUS) {
      const n = e.object.geometry;
      n.boundingSphere === null && n.computeBoundingSphere(), lu.copy(n.boundingSphere).applyMatrix4(t.matrixWorld), s.value = lu.radius;
    }
  }
  /**
   * Generates the code snippet of the uniform node. The node type of the uniform
   * node also depends on the selected scope.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The generated code snippet.
   */
  generate(e) {
    const t = this.scope;
    return t === fe.WORLD_MATRIX ? this.uniformNode.nodeType = "mat4" : t === fe.POSITION || t === fe.VIEW_POSITION || t === fe.DIRECTION || t === fe.SCALE ? this.uniformNode.nodeType = "vec3" : t === fe.RADIUS && (this.uniformNode.nodeType = "float"), this.uniformNode.build(e);
  }
  serialize(e) {
    super.serialize(e), e.scope = this.scope;
  }
  deserialize(e) {
    super.deserialize(e), this.scope = e.scope;
  }
}
fe.WORLD_MATRIX = "worldMatrix";
fe.POSITION = "position";
fe.SCALE = "scale";
fe.VIEW_POSITION = "viewPosition";
fe.DIRECTION = "direction";
fe.RADIUS = "radius";
class at extends fe {
  static get type() {
    return "ModelNode";
  }
  /**
   * Constructs a new object model node.
   *
   * @param {('position'|'viewPosition'|'direction'|'scale'|'worldMatrix')} scope - The node represents a different type of transformation depending on the scope.
   */
  constructor(e) {
    super(e);
  }
  /**
   * Extracts the model reference from the frame state and then
   * updates the uniform value depending on the scope.
   *
   * @param {NodeFrame} frame - The current node frame.
   */
  update(e) {
    this.object3d = e.object, super.update(e);
  }
}
at.DIRECTION;
const Us = /* @__PURE__ */ B(at, at.WORLD_MATRIX);
at.POSITION;
at.SCALE;
at.VIEW_POSITION;
at.RADIUS;
const Fm = /* @__PURE__ */ k(new Kr()).onObjectUpdate(({ object: a }, e) => e.value.getNormalMatrix(a.matrixWorld)), Jr = /* @__PURE__ */ A((a) => a.context.modelViewMatrix || Um).once()().toVar("modelViewMatrix"), Um = /* @__PURE__ */ qt.mul(Us), cu = /* @__PURE__ */ A((a) => (a.context.isHighPrecisionModelViewMatrix = !0, k("mat4").onObjectUpdate(({ object: e, camera: t }) => e.modelViewMatrix.multiplyMatrices(t.matrixWorldInverse, e.matrixWorld)))).once()().toVar("highpModelViewMatrix"), du = /* @__PURE__ */ A((a) => {
  const e = a.context.isHighPrecisionModelViewMatrix;
  return k("mat3").onObjectUpdate(({ object: t, camera: s }) => (e !== !0 && t.modelViewMatrix.multiplyMatrices(s.matrixWorldInverse, t.matrixWorld), t.normalMatrix.getNormalMatrix(t.modelViewMatrix)));
}).once()().toVar("highpModelNormalViewMatrix"), Dm = /* @__PURE__ */ A((a) => a.shaderStage !== "fragment" ? (Te("TSL: `clipSpace` is only available in fragment stage."), W()) : a.context.clipSpace.toVarying("v_clipSpace")).once()(), fi = /* @__PURE__ */ as("position", "vec3"), ve = /* @__PURE__ */ fi.toVarying("positionLocal"), qn = /* @__PURE__ */ fi.toVarying("positionPrevious"), ar = /* @__PURE__ */ A((a) => Us.mul(ve).xyz.toVarying(a.getSubBuildProperty("v_positionWorld")), "vec3").once(["POSITION"])(), ad = /* @__PURE__ */ A(() => ve.transformDirection(Us).toVarying("v_positionWorldDirection").normalize().toVar("positionWorldDirection"), "vec3").once(["POSITION"])(), be = /* @__PURE__ */ A((a) => {
  if (a.shaderStage === "fragment" && a.material.vertexNode) {
    const e = Mm.mul(Dm);
    return e.xyz.div(e.w).toVar("positionView");
  }
  return a.context.setupPositionView().toVarying("v_positionView");
}, "vec3").once(["POSITION", "VERTEX"])(), he = /* @__PURE__ */ A((a) => {
  let e;
  return a.camera.isOrthographicCamera ? e = N(0, 0, 1) : e = be.negate().toVarying("v_positionViewDirection").normalize(), e.toVar("positionViewDirection");
}, "vec3").once(["POSITION"])();
class Pm extends G {
  static get type() {
    return "FrontFacingNode";
  }
  /**
   * Constructs a new front facing node.
   */
  constructor() {
    super("bool"), this.isFrontFacingNode = !0;
  }
  generate(e) {
    if (e.shaderStage !== "fragment") return "true";
    const { material: t } = e;
    return t.side === Ge ? "false" : e.getFrontFacing();
  }
}
const Lm = /* @__PURE__ */ B(Pm), ud = /* @__PURE__ */ b(Lm).mul(2).sub(1), en = /* @__PURE__ */ A(([a], { material: e }) => {
  const t = e.side;
  return t === Ge ? a = a.mul(-1) : t === Ot && (a = a.mul(ud)), a;
}), ld = /* @__PURE__ */ as("normal", "vec3"), Mt = /* @__PURE__ */ A((a) => a.geometry.hasAttribute("normal") === !1 ? (L('TSL: Vertex attribute "normal" not found on geometry.'), N(0, 1, 0)) : ld, "vec3").once()().toVar("normalLocal"), Im = /* @__PURE__ */ be.dFdx().cross(be.dFdy()).normalize().toVar("normalFlat"), Hr = /* @__PURE__ */ A((a) => {
  let e;
  return a.isFlatShading() ? e = Im : e = cd(Mt).toVarying("v_normalViewGeometry").normalize(), e;
}, "vec3").once()().toVar("normalViewGeometry"), Om = /* @__PURE__ */ A((a) => {
  let e = Hr.transformDirection(qt);
  return a.isFlatShading() !== !0 && (e = e.toVarying("v_normalWorldGeometry")), e.normalize().toVar("normalWorldGeometry");
}, "vec3").once()(), Z = /* @__PURE__ */ A((a) => {
  let e;
  return a.subBuildFn === "NORMAL" || a.subBuildFn === "VERTEX" ? (e = Hr, a.isFlatShading() !== !0 && (e = en(e))) : e = a.context.setupNormal().context({ getUV: null, getTextureLevel: null }), e;
}, "vec3").once(["NORMAL", "VERTEX"])().toVar("normalView"), gr = /* @__PURE__ */ Z.transformDirection(qt).toVar("normalWorld"), ys = /* @__PURE__ */ A(({ subBuildFn: a, context: e }) => {
  let t;
  return a === "NORMAL" || a === "VERTEX" ? t = Z : t = e.setupClearcoatNormal().context({ getUV: null, getTextureLevel: null }), t;
}, "vec3").once(["NORMAL", "VERTEX"])().toVar("clearcoatNormalView"), Gm = /* @__PURE__ */ A(([a, e = Us]) => {
  const t = Me(e), s = a.div(N(t[0].dot(t[0]), t[1].dot(t[1]), t[2].dot(t[2])));
  return t.mul(s).xyz;
}), cd = /* @__PURE__ */ A(([a], e) => {
  const t = e.context.modelNormalViewMatrix;
  if (t)
    return t.transformDirection(a);
  const s = Fm.mul(a);
  return qt.transformDirection(s);
});
A(() => (L('TSL: "transformedNormalView" is deprecated. Use "normalView" instead.'), Z)).once(["NORMAL", "VERTEX"])();
A(() => (L('TSL: "transformedNormalWorld" is deprecated. Use "normalWorld" instead.'), gr)).once(["NORMAL", "VERTEX"])();
A(() => (L('TSL: "transformedClearcoatNormalView" is deprecated. Use "clearcoatNormalView" instead.'), ys)).once(["NORMAL", "VERTEX"])();
const Ui = /* @__PURE__ */ new dt(), km = /* @__PURE__ */ k(0).onReference(({ material: a }) => a).onObjectUpdate(({ material: a }) => a.refractionRatio), Di = /* @__PURE__ */ k(1).onReference(({ material: a }) => a).onObjectUpdate(function({ material: a, scene: e }) {
  return a.envMap ? a.envMapIntensity : e.environmentIntensity;
}), dd = /* @__PURE__ */ k(new dt()).onReference(function(a) {
  return a.material;
}).onObjectUpdate(function({ material: a, scene: e }) {
  const t = e.environment !== null && a.envMap === null ? e.environmentRotation : a.envMapRotation;
  return t ? Ui.makeRotationFromEuler(t).transpose() : Ui.identity(), Ui;
}), Vm = /* @__PURE__ */ he.negate().reflect(Z), $m = /* @__PURE__ */ he.negate().refract(Z, km), zm = /* @__PURE__ */ Vm.transformDirection(qt).toVar("reflectVector"), Wm = /* @__PURE__ */ $m.transformDirection(qt).toVar("reflectVector"), Hm = /* @__PURE__ */ new ai();
class qm extends Zr {
  static get type() {
    return "CubeTextureNode";
  }
  /**
   * Constructs a new cube texture node.
   *
   * @param {CubeTexture} value - The cube texture.
   * @param {?Node<vec3>} [uvNode=null] - The uv node.
   * @param {?Node<int>} [levelNode=null] - The level node.
   * @param {?Node<float>} [biasNode=null] - The bias node.
   */
  constructor(e, t = null, s = null, r = null) {
    super(e, t, s, r), this.isCubeTextureNode = !0;
  }
  /**
   * Overwrites the default implementation to return the appropriate cube texture type.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The input type.
   */
  getInputType() {
    return this.value.isDepthTexture === !0 ? "cubeDepthTexture" : "cubeTexture";
  }
  /**
   * Returns a default uvs based on the mapping type of the cube texture.
   *
   * @return {Node<vec3>} The default uv attribute.
   */
  getDefaultUV() {
    const e = this.value;
    return e.mapping === Vn ? zm : e.mapping === $n ? Wm : (F('CubeTextureNode: Mapping "%s" not supported.', e.mapping), N(0, 0, 0));
  }
  /**
   * Overwritten with an empty implementation since the `updateMatrix` flag is ignored
   * for cube textures. The uv transformation matrix is not applied to cube textures.
   *
   * @param {boolean} value - The update toggle.
   */
  setUpdateMatrix() {
  }
  // Ignore .updateMatrix for CubeTextureNode
  /**
   * Setups the uv node. Depending on the backend as well as the texture type, it might be necessary
   * to modify the uv node for correct sampling.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {Node} uvNode - The uv node to setup.
   * @return {Node} The updated uv node.
   */
  setupUV(e, t) {
    const s = this.value;
    return s.isDepthTexture === !0 ? e.renderer.coordinateSystem === kr ? N(t.x, t.y.negate(), t.z) : t : (t = dd.mul(t), (e.renderer.coordinateSystem === kr || !s.isRenderTargetTexture) && (t = N(t.x.negate(), t.yz)), t);
  }
  /**
   * Generates the uv code snippet.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {Node} cubeUV - The uv node to generate code for.
   * @return {string} The generated code snippet.
   */
  generateUV(e, t) {
    return t.build(e, this.sampler === !0 ? "vec3" : "ivec3");
  }
}
const Xm = /* @__PURE__ */ le(qm).setParameterLength(1, 4).setName("cubeTexture"), Ve = (a = Hm, e = null, t = null, s = null) => {
  let r;
  return a && a.isCubeTextureNode === !0 ? (r = O(a.clone()), r.referenceNode = a, e !== null && (r.uvNode = O(e)), t !== null && (r.levelNode = O(t)), s !== null && (r.biasNode = O(s))) : r = Xm(a, e, t, s), r;
};
class Km extends dr {
  static get type() {
    return "ReferenceElementNode";
  }
  /**
   * Constructs a new reference element node.
   *
   * @param {?ReferenceNode} referenceNode - The reference node.
   * @param {Node} indexNode - The index node that defines the element access.
   */
  constructor(e, t) {
    super(e, t), this.referenceNode = e, this.isReferenceElementNode = !0;
  }
  /**
   * This method is overwritten since the node type is inferred from
   * the uniform type of the reference node.
   *
   * @return {string} The node type.
   */
  generateNodeType() {
    return this.referenceNode.uniformType;
  }
  generate(e) {
    const t = super.generate(e), s = this.referenceNode.getNodeType(e), r = this.getNodeType(e);
    return e.format(t, s, r);
  }
}
class Ua extends G {
  static get type() {
    return "ReferenceNode";
  }
  /**
   * Constructs a new reference node.
   *
   * @param {string} property - The name of the property the node refers to.
   * @param {string} uniformType - The uniform type that should be used to represent the property value.
   * @param {?Object} [object=null] - The object the property belongs to.
   * @param {?number} [count=null] - When the linked property is an array-like, this parameter defines its length.
   */
  constructor(e, t, s = null, r = null) {
    super(), this.property = e, this.uniformType = t, this.object = s, this.count = r, this.properties = e.split("."), this.reference = s, this.node = null, this.group = null, this.name = null, this.updateType = z.OBJECT;
  }
  /**
   * When the referred property is array-like, this method can be used
   * to access elements via an index node.
   *
   * @param {IndexNode} indexNode - indexNode.
   * @return {ReferenceElementNode} A reference to an element.
   */
  element(e) {
    return new Km(this, O(e));
  }
  /**
   * Sets the uniform group for this reference node.
   *
   * @param {UniformGroupNode} group - The uniform group to set.
   * @return {ReferenceNode} A reference to this node.
   */
  setGroup(e) {
    return this.group = e, this;
  }
  /**
   * Sets the name for the internal uniform.
   *
   * @param {string} name - The label to set.
   * @return {ReferenceNode} A reference to this node.
   */
  setName(e) {
    return this.name = e, this;
  }
  /**
   * Sets the label for the internal uniform.
   *
   * @deprecated
   * @param {string} name - The label to set.
   * @return {ReferenceNode} A reference to this node.
   */
  label(e) {
    return L('TSL: "label()" has been deprecated. Use "setName()" instead.'), this.setName(e);
  }
  /**
   * Sets the node type which automatically defines the internal
   * uniform type.
   *
   * @param {string} uniformType - The type to set.
   */
  setNodeType(e) {
    let t = null;
    this.count !== null ? t = id(null, e, this.count) : Array.isArray(this.getValueFromReference()) ? t = st(null, e) : e === "texture" ? t = ce(null) : e === "cubeTexture" ? t = Ve(null) : t = k(null, e), this.group !== null && t.setGroup(this.group), this.name !== null && t.setName(this.name), this.node = t;
  }
  /**
   * This method is overwritten since the node type is inferred from
   * the type of the reference node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The node type.
   */
  generateNodeType(e) {
    return this.node === null && (this.updateReference(e), this.updateValue()), this.node.getNodeType(e);
  }
  /**
   * Returns the property value from the given referred object.
   *
   * @param {Object} [object=this.reference] - The object to retrieve the property value from.
   * @return {any} The value.
   */
  getValueFromReference(e = this.reference) {
    const { properties: t } = this;
    let s = e[t[0]];
    for (let r = 1; r < t.length; r++)
      s = s[t[r]];
    return s;
  }
  /**
   * Allows to update the reference based on the given state. The state is only
   * evaluated {@link ReferenceNode#object} is not set.
   *
   * @param {(NodeFrame|NodeBuilder)} state - The current state.
   * @return {Object} The updated reference.
   */
  updateReference(e) {
    return this.reference = this.object !== null ? this.object : e.object, this.reference;
  }
  /**
   * The output of the reference node is the internal uniform node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {UniformNode} The output node.
   */
  setup() {
    return this.updateValue(), this.node;
  }
  /**
   * Overwritten to update the internal uniform value.
   *
   * @param {NodeFrame} frame - A reference to the current node frame.
   */
  update() {
    this.updateValue();
  }
  /**
   * Retrieves the value from the referred object property and uses it
   * to updated the internal uniform.
   */
  updateValue() {
    this.node === null && this.setNodeType(this.uniformType);
    const e = this.getValueFromReference();
    Array.isArray(e) ? this.node.array = e : this.node.value = e;
  }
}
const ge = (a, e, t) => new Ua(a, e, t), hu = (a, e, t, s) => new Ua(a, e, s, t);
class Qm extends Ua {
  static get type() {
    return "MaterialReferenceNode";
  }
  /**
   * Constructs a new material reference node.
   *
   * @param {string} property - The name of the property the node refers to.
   * @param {string} inputType - The uniform type that should be used to represent the property value.
   * @param {?Material} [material=null] - The material the property belongs to. When no material is set,
   * the node refers to the material of the current rendered object.
   */
  constructor(e, t, s = null) {
    super(e, t, s), this.material = s, this.isMaterialReferenceNode = !0;
  }
  /**
   * Updates the reference based on the given state. The state is only evaluated
   * {@link MaterialReferenceNode#material} is not set.
   *
   * @param {(NodeFrame|NodeBuilder)} state - The current state.
   * @return {Object} The updated reference.
   */
  updateReference(e) {
    return this.reference = this.material !== null ? this.material : e.material, this.reference;
  }
}
const Zt = (a, e, t = null) => new Qm(a, e, t), hd = hr(), Ym = be.dFdx(), jm = be.dFdy(), fd = hd.dFdx(), pd = hd.dFdy(), gd = Z, md = jm.cross(gd), xd = gd.cross(Ym), Qo = md.mul(fd.x).add(xd.mul(pd.x)), Yo = md.mul(fd.y).add(xd.mul(pd.y)), fu = Qo.dot(Qo).max(Yo.dot(Yo)), Td = fu.equal(0).select(0, fu.inverseSqrt()), Zm = /* @__PURE__ */ Qo.mul(Td).toVar("tangentViewFrame"), Jm = /* @__PURE__ */ Yo.mul(Td).toVar("bitangentViewFrame"), yd = /* @__PURE__ */ as("tangent", "vec4"), Xn = /* @__PURE__ */ yd.xyz.toVar("tangentLocal"), _d = /* @__PURE__ */ A((a) => {
  let e;
  return a.subBuildFn === "VERTEX" || a.geometry.hasAttribute("tangent") ? e = Jr.mul(W(Xn, 0)).xyz.toVarying("v_tangentView").normalize() : e = Zm, a.isFlatShading() !== !0 && (e = en(e)), e;
}, "vec3").once(["NORMAL", "VERTEX"])().toVar("tangentView"), ex = /* @__PURE__ */ A(([a, e], t) => {
  let s = a.mul(yd.w).xyz;
  return t.subBuildFn === "NORMAL" && t.isFlatShading() !== !0 && (s = s.toVarying(e)), s;
}).once(["NORMAL"]), tx = /* @__PURE__ */ A((a) => {
  let e;
  return a.subBuildFn === "VERTEX" || a.geometry.hasAttribute("tangent") ? e = ex(Z.cross(_d), "v_bitangentView").normalize() : e = Jm, a.isFlatShading() !== !0 && (e = en(e)), e;
}, "vec3").once(["NORMAL", "VERTEX"])().toVar("bitangentView"), wr = /* @__PURE__ */ Me(_d, tx, Z).toVar("TBNViewMatrix"), sx = /* @__PURE__ */ A(() => {
  let a = er.cross(he);
  return a = a.cross(er).normalize(), a = ne(a, Z, ps.mul(It.oneMinus()).oneMinus().pow2().pow2()).normalize(), a;
}).once()(), rx = (a) => O(a).mul(0.5).add(0.5), pu = (a) => N(a, ns(Ea(b(1).sub(Bs(a, a)))));
class nx extends Re {
  static get type() {
    return "NormalMapNode";
  }
  /**
   * Constructs a new normal map node.
   *
   * @param {Node<vec3>} node - Represents the normal map data.
   * @param {?Node<vec2>} [scaleNode=null] - Controls the intensity of the effect.
   */
  constructor(e, t = null) {
    super("vec3"), this.node = e, this.scaleNode = t, this.normalMapType = Si, this.unpackNormalMode = Ni;
  }
  setup(e) {
    const { normalMapType: t, scaleNode: s, unpackNormalMode: r } = this;
    let n = this.node.mul(2).sub(1);
    if (t === Si ? r === Tc ? n = pu(n.xy) : r === If ? n = pu(n.yw) : r !== Ni && F(`THREE.NodeMaterial: Unexpected unpack normal mode: ${r}`) : r !== Ni && F(`THREE.NodeMaterial: Normal map type '${t}' is not compatible with unpack normal mode '${r}'`), s !== null) {
      let o = s;
      e.isFlatShading() === !0 && (o = en(o)), n = N(n.xy.mul(o), n.z);
    }
    let i = null;
    return t === Of ? i = cd(n) : t === Si ? i = wr.mul(n).normalize() : (F(`NodeMaterial: Unsupported normal map type: ${t}`), i = Z), i;
  }
}
const gu = /* @__PURE__ */ le(nx).setParameterLength(1, 2), ix = A(({ textureNode: a, bumpScale: e }) => {
  const t = (r) => a.isolate().context({ getUV: (n) => r(n.uvNode || hr()), forceUVContext: !0 }), s = b(t((r) => r));
  return I(
    b(t((r) => r.add(r.dFdx()))).sub(s),
    b(t((r) => r.add(r.dFdy()))).sub(s)
  ).mul(e);
}), ox = A((a) => {
  const { surf_pos: e, surf_norm: t, dHdxy: s } = a, r = e.dFdx().normalize(), n = e.dFdy().normalize(), i = t, o = n.cross(i), u = i.cross(r), l = r.dot(o).mul(ud), c = l.sign().mul(s.x.mul(o).add(s.y.mul(u)));
  return l.abs().mul(t).sub(c).normalize();
});
class ax extends Re {
  static get type() {
    return "BumpMapNode";
  }
  /**
   * Constructs a new bump map node.
   *
   * @param {Node<float>} textureNode - Represents the bump map data.
   * @param {?Node<float>} [scaleNode=null] - Controls the intensity of the bump effect.
   */
  constructor(e, t = null) {
    super("vec3"), this.textureNode = e, this.scaleNode = t;
  }
  setup() {
    const e = this.scaleNode !== null ? this.scaleNode : 1, t = ix({ textureNode: this.textureNode, bumpScale: e });
    return ox({
      surf_pos: be,
      surf_norm: Z,
      dHdxy: t
    });
  }
}
const ux = /* @__PURE__ */ le(ax).setParameterLength(1, 2), mu = /* @__PURE__ */ new Map();
class R extends G {
  static get type() {
    return "MaterialNode";
  }
  /**
   * Constructs a new material node.
   *
   * @param {string} scope - The scope defines what kind of material property is referred by the node.
   */
  constructor(e) {
    super(), this.scope = e;
  }
  /**
   * Returns a cached reference node for the given property and type.
   *
   * @param {string} property - The name of the material property.
   * @param {string} type - The uniform type of the property.
   * @return {MaterialReferenceNode} A material reference node representing the property access.
   */
  getCache(e, t) {
    let s = mu.get(e);
    return s === void 0 && (s = Zt(e, t), mu.set(e, s)), s;
  }
  /**
   * Returns a float-typed material reference node for the given property name.
   *
   * @param {string} property - The name of the material property.
   * @return {MaterialReferenceNode<float>} A material reference node representing the property access.
   */
  getFloat(e) {
    return this.getCache(e, "float");
  }
  /**
   * Returns a color-typed material reference node for the given property name.
   *
   * @param {string} property - The name of the material property.
   * @return {MaterialReferenceNode<color>} A material reference node representing the property access.
   */
  getColor(e) {
    return this.getCache(e, "color");
  }
  /**
   * Returns a texture-typed material reference node for the given property name.
   *
   * @param {string} property - The name of the material property.
   * @return {MaterialReferenceNode} A material reference node representing the property access.
   */
  getTexture(e) {
    return this.getCache(e === "map" ? "map" : e + "Map", "texture");
  }
  /**
   * The node setup is done depending on the selected scope. Multiple material properties
   * might be grouped into a single node composition if they logically belong together.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {Node} The node representing the selected scope.
   */
  setup(e) {
    const t = e.context.material, s = this.scope;
    let r = null;
    if (s === R.COLOR) {
      const n = t.color !== void 0 ? this.getColor(s) : N();
      t.map && t.map.isTexture === !0 ? r = n.mul(this.getTexture("map")) : r = n;
    } else if (s === R.OPACITY) {
      const n = this.getFloat(s);
      t.alphaMap && t.alphaMap.isTexture === !0 ? r = n.mul(this.getTexture("alpha")) : r = n;
    } else if (s === R.SPECULAR_STRENGTH)
      t.specularMap && t.specularMap.isTexture === !0 ? r = this.getTexture("specular").r : r = b(1);
    else if (s === R.SPECULAR_INTENSITY) {
      const n = this.getFloat(s);
      t.specularIntensityMap && t.specularIntensityMap.isTexture === !0 ? r = n.mul(this.getTexture(s).a) : r = n;
    } else if (s === R.SPECULAR_COLOR) {
      const n = this.getColor(s);
      t.specularColorMap && t.specularColorMap.isTexture === !0 ? r = n.mul(this.getTexture(s).rgb) : r = n;
    } else if (s === R.ROUGHNESS) {
      const n = this.getFloat(s);
      t.roughnessMap && t.roughnessMap.isTexture === !0 ? r = n.mul(this.getTexture(s).g) : r = n;
    } else if (s === R.METALNESS) {
      const n = this.getFloat(s);
      t.metalnessMap && t.metalnessMap.isTexture === !0 ? r = n.mul(this.getTexture(s).b) : r = n;
    } else if (s === R.EMISSIVE) {
      const n = this.getFloat("emissiveIntensity"), i = this.getColor(s).mul(n);
      t.emissiveMap && t.emissiveMap.isTexture === !0 ? r = i.mul(this.getTexture(s)) : r = i;
    } else if (s === R.NORMAL)
      t.normalMap ? (r = gu(this.getTexture("normal"), this.getCache("normalScale", "vec2")), r.normalMapType = t.normalMapType, (t.normalMap.format == Rt || t.normalMap.format == kn || t.normalMap.format == Gn) && (r.unpackNormalMode = Tc)) : t.bumpMap ? r = ux(this.getTexture("bump").r, this.getFloat("bumpScale")) : r = Z;
    else if (s === R.CLEARCOAT) {
      const n = this.getFloat(s);
      t.clearcoatMap && t.clearcoatMap.isTexture === !0 ? r = n.mul(this.getTexture(s).r) : r = n;
    } else if (s === R.CLEARCOAT_ROUGHNESS) {
      const n = this.getFloat(s);
      t.clearcoatRoughnessMap && t.clearcoatRoughnessMap.isTexture === !0 ? r = n.mul(this.getTexture(s).r) : r = n;
    } else if (s === R.CLEARCOAT_NORMAL)
      t.clearcoatNormalMap ? r = gu(this.getTexture(s), this.getCache(s + "Scale", "vec2")) : r = Z;
    else if (s === R.SHEEN) {
      const n = this.getColor("sheenColor").mul(this.getFloat("sheen"));
      t.sheenColorMap && t.sheenColorMap.isTexture === !0 ? r = n.mul(this.getTexture("sheenColor").rgb) : r = n;
    } else if (s === R.SHEEN_ROUGHNESS) {
      const n = this.getFloat(s);
      t.sheenRoughnessMap && t.sheenRoughnessMap.isTexture === !0 ? r = n.mul(this.getTexture(s).a) : r = n, r = r.clamp(1e-4, 1);
    } else if (s === R.ANISOTROPY)
      if (t.anisotropyMap && t.anisotropyMap.isTexture === !0) {
        const n = this.getTexture(s);
        r = ba(yr.x, yr.y, yr.y.negate(), yr.x).mul(n.rg.mul(2).sub(I(1)).normalize().mul(n.b));
      } else
        r = yr;
    else if (s === R.IRIDESCENCE_THICKNESS) {
      const n = ge("1", "float", t.iridescenceThicknessRange);
      if (t.iridescenceThicknessMap) {
        const i = ge("0", "float", t.iridescenceThicknessRange);
        r = n.sub(i).mul(this.getTexture(s).g).add(i);
      } else
        r = n;
    } else if (s === R.TRANSMISSION) {
      const n = this.getFloat(s);
      t.transmissionMap ? r = n.mul(this.getTexture(s).r) : r = n;
    } else if (s === R.THICKNESS) {
      const n = this.getFloat(s);
      t.thicknessMap ? r = n.mul(this.getTexture(s).g) : r = n;
    } else if (s === R.IOR)
      r = this.getFloat(s);
    else if (s === R.LIGHT_MAP)
      r = this.getTexture(s).rgb.mul(this.getFloat("lightMapIntensity"));
    else if (s === R.AO)
      r = this.getTexture(s).r.sub(1).mul(this.getFloat("aoMapIntensity")).add(1);
    else if (s === R.LINE_DASH_OFFSET)
      r = t.dashOffset ? this.getFloat(s) : b(0);
    else {
      const n = this.getNodeType(e);
      r = this.getCache(s, n);
    }
    return r;
  }
}
R.ALPHA_TEST = "alphaTest";
R.COLOR = "color";
R.OPACITY = "opacity";
R.SHININESS = "shininess";
R.SPECULAR = "specular";
R.SPECULAR_STRENGTH = "specularStrength";
R.SPECULAR_INTENSITY = "specularIntensity";
R.SPECULAR_COLOR = "specularColor";
R.REFLECTIVITY = "reflectivity";
R.ROUGHNESS = "roughness";
R.METALNESS = "metalness";
R.NORMAL = "normal";
R.CLEARCOAT = "clearcoat";
R.CLEARCOAT_ROUGHNESS = "clearcoatRoughness";
R.CLEARCOAT_NORMAL = "clearcoatNormal";
R.EMISSIVE = "emissive";
R.ROTATION = "rotation";
R.SHEEN = "sheen";
R.SHEEN_ROUGHNESS = "sheenRoughness";
R.ANISOTROPY = "anisotropy";
R.IRIDESCENCE = "iridescence";
R.IRIDESCENCE_IOR = "iridescenceIOR";
R.IRIDESCENCE_THICKNESS = "iridescenceThickness";
R.IOR = "ior";
R.TRANSMISSION = "transmission";
R.THICKNESS = "thickness";
R.ATTENUATION_DISTANCE = "attenuationDistance";
R.ATTENUATION_COLOR = "attenuationColor";
R.LINE_SCALE = "scale";
R.LINE_DASH_SIZE = "dashSize";
R.LINE_GAP_SIZE = "gapSize";
R.LINE_WIDTH = "linewidth";
R.LINE_DASH_OFFSET = "dashOffset";
R.POINT_SIZE = "size";
R.DISPERSION = "dispersion";
R.LIGHT_MAP = "light";
R.AO = "ao";
const lx = /* @__PURE__ */ B(R, R.ALPHA_TEST), cx = /* @__PURE__ */ B(R, R.COLOR), dx = /* @__PURE__ */ B(R, R.SHININESS), hx = /* @__PURE__ */ B(R, R.EMISSIVE), bd = /* @__PURE__ */ B(R, R.OPACITY), fx = /* @__PURE__ */ B(R, R.SPECULAR), xu = /* @__PURE__ */ B(R, R.SPECULAR_INTENSITY), px = /* @__PURE__ */ B(R, R.SPECULAR_COLOR), Un = /* @__PURE__ */ B(R, R.SPECULAR_STRENGTH), Pi = /* @__PURE__ */ B(R, R.REFLECTIVITY), gx = /* @__PURE__ */ B(R, R.ROUGHNESS), mx = /* @__PURE__ */ B(R, R.METALNESS), xx = /* @__PURE__ */ B(R, R.NORMAL), Tx = /* @__PURE__ */ B(R, R.CLEARCOAT), yx = /* @__PURE__ */ B(R, R.CLEARCOAT_ROUGHNESS), _x = /* @__PURE__ */ B(R, R.CLEARCOAT_NORMAL), bx = /* @__PURE__ */ B(R, R.ROTATION), Sx = /* @__PURE__ */ B(R, R.SHEEN), Nx = /* @__PURE__ */ B(R, R.SHEEN_ROUGHNESS), Rx = /* @__PURE__ */ B(R, R.ANISOTROPY), Ax = /* @__PURE__ */ B(R, R.IRIDESCENCE), vx = /* @__PURE__ */ B(R, R.IRIDESCENCE_IOR), Ex = /* @__PURE__ */ B(R, R.IRIDESCENCE_THICKNESS), Cx = /* @__PURE__ */ B(R, R.TRANSMISSION), wx = /* @__PURE__ */ B(R, R.THICKNESS), Mx = /* @__PURE__ */ B(R, R.IOR), Bx = /* @__PURE__ */ B(R, R.ATTENUATION_DISTANCE), Fx = /* @__PURE__ */ B(R, R.ATTENUATION_COLOR), Ux = /* @__PURE__ */ B(R, R.LINE_SCALE), Dx = /* @__PURE__ */ B(R, R.LINE_DASH_SIZE), Px = /* @__PURE__ */ B(R, R.LINE_GAP_SIZE);
R.LINE_WIDTH;
const Lx = /* @__PURE__ */ B(R, R.LINE_DASH_OFFSET), Ix = /* @__PURE__ */ B(R, R.POINT_SIZE), Ox = /* @__PURE__ */ B(R, R.DISPERSION), Sd = /* @__PURE__ */ B(R, R.LIGHT_MAP), Gx = /* @__PURE__ */ B(R, R.AO), yr = /* @__PURE__ */ k(new je()).onReference(function(a) {
  return a.material;
}).onRenderUpdate(function({ material: a }) {
  this.value.set(a.anisotropy * Math.cos(a.anisotropyRotation), a.anisotropy * Math.sin(a.anisotropyRotation));
}), kx = /* @__PURE__ */ A((a) => a.context.setupModelViewProjection(), "vec4").once()().toVarying("v_modelViewProjection");
class Vx extends dr {
  static get type() {
    return "StorageArrayElementNode";
  }
  /**
   * Constructs storage buffer element node.
   *
   * @param {StorageBufferNode} storageBufferNode - The storage buffer node.
   * @param {Node} indexNode - The index node that defines the element access.
   */
  constructor(e, t) {
    super(e, t), this.isStorageArrayElementNode = !0;
  }
  /**
   * The storage buffer node.
   *
   * @param {Node} value
   * @type {StorageBufferNode}
   */
  set storageBufferNode(e) {
    this.node = e;
  }
  get storageBufferNode() {
    return this.node;
  }
  getMemberType(e, t) {
    const s = this.storageBufferNode.structTypeNode;
    return s ? s.getMemberType(e, t) : "void";
  }
  setup(e) {
    return e.isAvailable("storageBuffer") === !1 && this.node.isPBO === !0 && e.setupPBO(this.node), super.setup(e);
  }
  generate(e, t) {
    let s;
    const r = e.context.assign;
    if (e.isAvailable("storageBuffer") === !1 ? this.node.isPBO === !0 && r !== !0 && (this.node.value.isInstancedBufferAttribute || e.shaderStage !== "compute") ? s = e.generatePBO(this) : s = this.node.build(e) : s = super.generate(e), r !== !0) {
      const n = this.getNodeType(e);
      s = e.format(s, n, t);
    }
    return s;
  }
}
const $x = /* @__PURE__ */ le(Vx).setParameterLength(2);
class zx extends Fa {
  static get type() {
    return "StorageBufferNode";
  }
  /**
   * Constructs a new storage buffer node.
   *
   * @param {StorageBufferAttribute|StorageInstancedBufferAttribute|BufferAttribute} value - The buffer data.
   * @param {?(string|Struct)} [bufferType=null] - The buffer type (e.g. `'vec3'`).
   * @param {number} [bufferCount=0] - The buffer count.
   */
  constructor(e, t = null, s = 0) {
    let r, n = null;
    t && t.isStruct ? (r = "struct", n = t.layout, (e.isStorageBufferAttribute || e.isStorageInstancedBufferAttribute) && (s = e.count)) : t === null && (e.isStorageBufferAttribute || e.isStorageInstancedBufferAttribute) ? (r = yc(e.itemSize), s = e.count) : r = t, super(e, r, s), this.isStorageBufferNode = !0, this.structTypeNode = n, this.access = nt.READ_WRITE, this.isAtomic = !1, this.isPBO = !1, this._attribute = null, this._varying = null, this.global = !0, e.isStorageBufferAttribute !== !0 && e.isStorageInstancedBufferAttribute !== !0 && (e.isInstancedBufferAttribute ? e.isStorageInstancedBufferAttribute = !0 : e.isStorageBufferAttribute = !0);
  }
  /**
   * This method is overwritten since the buffer data might be shared
   * and thus the hash should be shared as well.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The hash.
   */
  getHash(e) {
    let t;
    if (this.bufferCount === 0) {
      let s = e.globalCache.getData(this.value);
      s === void 0 && (s = {
        node: this
      }, e.globalCache.setData(this.value, s)), t = s.node.id;
    } else
      t = this.id;
    return String(t);
  }
  /**
   * Overwrites the default implementation to return a fixed value `'indirectStorageBuffer'` or `'storageBuffer'`.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The input type.
   */
  getInputType() {
    return this.value.isIndirectStorageBufferAttribute ? "indirectStorageBuffer" : "storageBuffer";
  }
  /**
   * Enables element access with the given index node.
   *
   * @param {IndexNode} indexNode - The index node.
   * @return {StorageArrayElementNode} A node representing the element access.
   */
  element(e) {
    return $x(this, e);
  }
  /**
   * Defines whether this node is a PBO or not. Only relevant for WebGL.
   *
   * @param {boolean} value - The value so set.
   * @return {StorageBufferNode} A reference to this node.
   */
  setPBO(e) {
    return this.isPBO = e, this;
  }
  /**
   * Returns the `isPBO` value.
   *
   * @return {boolean} Whether the node represents a PBO or not.
   */
  getPBO() {
    return this.isPBO;
  }
  /**
   * Defines the node access.
   *
   * @param {string} value - The node access.
   * @return {StorageBufferNode} A reference to this node.
   */
  setAccess(e) {
    return this.access = e, this;
  }
  /**
   * Convenience method for configuring a read-only node access.
   *
   * @return {StorageBufferNode} A reference to this node.
   */
  toReadOnly() {
    return this.setAccess(nt.READ_ONLY);
  }
  /**
   * Defines whether the node is atomic or not.
   *
   * @param {boolean} value - The atomic flag.
   * @return {StorageBufferNode} A reference to this node.
   */
  setAtomic(e) {
    return this.isAtomic = e, this;
  }
  /**
   * Convenience method for making this node atomic.
   *
   * @return {StorageBufferNode} A reference to this node.
   */
  toAtomic() {
    return this.setAtomic(!0);
  }
  /**
   * Returns attribute data for this storage buffer node.
   *
   * @return {{attribute: BufferAttributeNode, varying: VaryingNode}} The attribute data.
   */
  getAttributeData() {
    return this._attribute === null && (this._attribute = Zc(this.value), this._varying = Is(this._attribute)), {
      attribute: this._attribute,
      varying: this._varying
    };
  }
  /**
   * This method is overwritten since the node type from the availability of storage buffers
   * and the attribute data.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The node type.
   */
  generateNodeType(e) {
    if (this.structTypeNode !== null)
      return this.structTypeNode.getNodeType(e);
    if (e.isAvailable("storageBuffer") || e.isAvailable("indirectStorageBuffer"))
      return super.generateNodeType(e);
    const { attribute: t } = this.getAttributeData();
    return t.getNodeType(e);
  }
  /**
   * Returns the type of a member of the struct.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {string} name - The name of the member.
   * @return {string} The type of the member.
   */
  getMemberType(e, t) {
    return this.structTypeNode !== null ? this.structTypeNode.getMemberType(e, t) : "void";
  }
  /**
   * Generates the code snippet of the storage buffer node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The generated code snippet.
   */
  generate(e) {
    if (this.structTypeNode !== null && this.structTypeNode.build(e), e.isAvailable("storageBuffer") || e.isAvailable("indirectStorageBuffer"))
      return super.generate(e);
    const { attribute: t, varying: s } = this.getAttributeData(), r = s.build(e);
    return e.registerTransform(r, t), r;
  }
}
const Tu = (a, e = null, t = 0) => new zx(a, e, t);
class Wx extends G {
  static get type() {
    return "InstanceNode";
  }
  /**
   * Constructs a new instance node.
   *
   * @param {number} count - The number of instances.
   * @param {InstancedBufferAttribute|StorageInstancedBufferAttribute} instanceMatrix - Instanced buffer attribute representing the instance transformations.
   * @param {?InstancedBufferAttribute|StorageInstancedBufferAttribute} instanceColor - Instanced buffer attribute representing the instance colors.
   */
  constructor(e, t, s = null) {
    super("void"), this.count = e, this.instanceMatrix = t, this.instanceColor = s, this.instanceMatrixNode = null, this.instanceColorNode = null, this.updateType = z.FRAME, this.buffer = null, this.bufferColor = null, this.previousInstanceMatrixNode = null;
  }
  /**
   * Tracks whether the matrix data is provided via a storage buffer.
   *
   * @type {boolean}
   */
  get isStorageMatrix() {
    const { instanceMatrix: e } = this;
    return e && e.isStorageInstancedBufferAttribute === !0;
  }
  /**
   * Tracks whether the color data is provided via a storage buffer.
   *
   * @type {boolean}
   */
  get isStorageColor() {
    const { instanceColor: e } = this;
    return e && e.isStorageInstancedBufferAttribute === !0;
  }
  /**
   * Setups the internal buffers and nodes and assigns the transformed vertex data
   * to predefined node variables for accumulation. That follows the same patterns
   * like with morph and skinning nodes.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  setup(e) {
    let { instanceMatrixNode: t, instanceColorNode: s } = this;
    t === null && (t = this._createInstanceMatrixNode(!0, e), this.instanceMatrixNode = t);
    const { instanceColor: r, isStorageColor: n } = this;
    if (r && s === null) {
      if (n)
        s = Tu(r, "vec3", Math.max(r.count, 1)).element(sr);
      else {
        const o = new Gf(r.array, 3), u = r.usage === Ln ? uu : au;
        this.bufferColor = o, s = N(u(o, "vec3", 3, 0));
      }
      this.instanceColorNode = s;
    }
    const i = t.mul(ve).xyz;
    if (ve.assign(i), e.needsPreviousData() && qn.assign(this.getPreviousInstancedPosition(e)), e.hasGeometryAttribute("normal")) {
      const o = Gm(Mt, t);
      Mt.assign(o);
    }
    this.instanceColorNode !== null && Hn("vec3", "vInstanceColor").assign(this.instanceColorNode);
  }
  /**
   * Checks if the internal buffers require an update.
   *
   * @param {NodeFrame} frame - The current node frame.
   */
  update(e) {
    this.buffer !== null && this.isStorageMatrix !== !0 && (this.buffer.clearUpdateRanges(), this.buffer.updateRanges.push(...this.instanceMatrix.updateRanges), this.instanceMatrix.version !== this.buffer.version && (this.buffer.version = this.instanceMatrix.version)), this.instanceColor && this.bufferColor !== null && this.isStorageColor !== !0 && (this.bufferColor.clearUpdateRanges(), this.bufferColor.updateRanges.push(...this.instanceColor.updateRanges), this.instanceColor.version !== this.bufferColor.version && (this.bufferColor.version = this.instanceColor.version)), this.previousInstanceMatrixNode !== null && e.object.previousInstanceMatrix.array.set(this.instanceMatrix.array);
  }
  /**
   * Computes the transformed/instanced vertex position of the previous frame.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {Node<vec3>} The instanced position from the previous frame.
   */
  getPreviousInstancedPosition(e) {
    const t = e.object;
    return this.previousInstanceMatrixNode === null && (t.previousInstanceMatrix = this.instanceMatrix.clone(), this.previousInstanceMatrixNode = this._createInstanceMatrixNode(!1, e)), this.previousInstanceMatrixNode.mul(qn).xyz;
  }
  /**
   * Creates a node representing the instance matrix data.
   *
   * @private
   * @param {boolean} assignBuffer - Whether the created interleaved buffer should be assigned to the `buffer` member or not.
   * @param {NodeBuilder} builder - A reference to the current node builder.
   * @return {Node} The instance matrix node.
   */
  _createInstanceMatrixNode(e, t) {
    let s;
    const { instanceMatrix: r } = this, { count: n } = r;
    if (this.isStorageMatrix)
      s = Tu(r, "mat4", Math.max(n, 1)).element(sr);
    else if (n * 16 * 4 <= t.getUniformBufferLimit())
      s = id(r.array, "mat4", Math.max(n, 1)).element(sr);
    else {
      const o = new kf(r.array, 16, 1);
      e === !0 && (this.buffer = o);
      const u = r.usage === Ln ? uu : au, l = [
        u(o, "vec4", 16, 0),
        u(o, "vec4", 16, 4),
        u(o, "vec4", 16, 8),
        u(o, "vec4", 16, 12)
      ];
      s = As(...l);
    }
    return s;
  }
}
class Hx extends Wx {
  static get type() {
    return "InstancedMeshNode";
  }
  /**
   * Constructs a new instanced mesh node.
   *
   * @param {InstancedMesh} instancedMesh - The instanced mesh.
   */
  constructor(e) {
    const { count: t, instanceMatrix: s, instanceColor: r } = e;
    super(t, s, r), this.instancedMesh = e;
  }
}
const qx = /* @__PURE__ */ le(Hx).setParameterLength(1);
class Xx extends G {
  static get type() {
    return "BatchNode";
  }
  /**
   * Constructs a new batch node.
   *
   * @param {BatchedMesh} batchMesh - A reference to batched mesh.
   */
  constructor(e) {
    super("void"), this.batchMesh = e, this.batchingIdNode = null;
  }
  /**
   * Setups the internal buffers and nodes and assigns the transformed vertex data
   * to predefined node variables for accumulation. That follows the same patterns
   * like with morph and skinning nodes.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  setup(e) {
    this.batchingIdNode === null && (e.getDrawIndex() === null ? this.batchingIdNode = sr : this.batchingIdNode = om);
    const s = A(([p]) => {
      const g = pe(rr(tt(this.batchMesh._indirectTexture), 0).x).toConst(), m = pe(p).mod(g).toConst(), y = pe(p).div(g).toConst();
      return tt(this.batchMesh._indirectTexture, yt(m, y)).x;
    }).setLayout({
      name: "getIndirectIndex",
      type: "uint",
      inputs: [
        { name: "id", type: "int" }
      ]
    })(pe(this.batchingIdNode)), r = this.batchMesh._matricesTexture, n = pe(rr(tt(r), 0).x).toConst(), i = b(s).mul(4).toInt().toConst(), o = i.mod(n).toConst(), u = i.div(n).toConst(), l = As(
      tt(r, yt(o, u)),
      tt(r, yt(o.add(1), u)),
      tt(r, yt(o.add(2), u)),
      tt(r, yt(o.add(3), u))
    ), c = this.batchMesh._colorsTexture;
    if (c !== null) {
      const g = A(([m]) => {
        const y = pe(rr(tt(c), 0).x).toConst(), _ = m, v = _.mod(y).toConst(), C = _.div(y).toConst();
        return tt(c, yt(v, C)).rgb;
      }).setLayout({
        name: "getBatchingColor",
        type: "vec3",
        inputs: [
          { name: "id", type: "int" }
        ]
      })(s);
      Hn("vec3", "vBatchColor").assign(g);
    }
    const d = Me(l);
    ve.assign(l.mul(ve));
    const h = Mt.div(N(d[0].dot(d[0]), d[1].dot(d[1]), d[2].dot(d[2]))), f = d.mul(h).xyz;
    Mt.assign(f), e.hasGeometryAttribute("tangent") && Xn.mulAssign(d);
  }
}
const Kx = /* @__PURE__ */ le(Xx).setParameterLength(1), yu = /* @__PURE__ */ new WeakMap();
class Qx extends G {
  static get type() {
    return "SkinningNode";
  }
  /**
   * Constructs a new skinning node.
   *
   * @param {SkinnedMesh} skinnedMesh - The skinned mesh.
   */
  constructor(e) {
    super("void"), this.skinnedMesh = e, this.updateType = z.OBJECT, this.skinIndexNode = as("skinIndex", "uvec4"), this.skinWeightNode = as("skinWeight", "vec4"), this.bindMatrixNode = ge("bindMatrix", "mat4"), this.bindMatrixInverseNode = ge("bindMatrixInverse", "mat4"), this.boneMatricesNode = hu("skeleton.boneMatrices", "mat4", e.skeleton.bones.length), this.positionNode = ve, this.toPositionNode = ve, this.previousBoneMatricesNode = null;
  }
  /**
   * Transforms the given vertex position via skinning.
   *
   * @param {Node} [boneMatrices=this.boneMatricesNode] - The bone matrices
   * @param {Node<vec3>} [position=this.positionNode] - The vertex position in local space.
   * @return {Node<vec3>} The transformed vertex position.
   */
  getSkinnedPosition(e = this.boneMatricesNode, t = this.positionNode) {
    const { skinIndexNode: s, skinWeightNode: r, bindMatrixNode: n, bindMatrixInverseNode: i } = this, o = e.element(s.x), u = e.element(s.y), l = e.element(s.z), c = e.element(s.w), d = n.mul(t), h = We(
      o.mul(r.x).mul(d),
      u.mul(r.y).mul(d),
      l.mul(r.z).mul(d),
      c.mul(r.w).mul(d)
    );
    return i.mul(h).xyz;
  }
  /**
   * Transforms the given vertex normal and tangent via skinning.
   *
   * @param {Node} [boneMatrices=this.boneMatricesNode] - The bone matrices
   * @param {Node<vec3>} [normal=normalLocal] - The vertex normal in local space.
   * @param {Node<vec3>} [tangent=tangentLocal] - The vertex tangent in local space.
   * @return {{skinNormal: Node<vec3>, skinTangent:Node<vec3>}} The transformed vertex normal and tangent.
   */
  getSkinnedNormalAndTangent(e = this.boneMatricesNode, t = Mt, s = Xn) {
    const { skinIndexNode: r, skinWeightNode: n, bindMatrixNode: i, bindMatrixInverseNode: o } = this, u = e.element(r.x), l = e.element(r.y), c = e.element(r.z), d = e.element(r.w);
    let h = We(
      n.x.mul(u),
      n.y.mul(l),
      n.z.mul(c),
      n.w.mul(d)
    );
    h = o.mul(h).mul(i);
    const f = h.transformDirection(t).xyz, p = h.transformDirection(s).xyz;
    return { skinNormal: f, skinTangent: p };
  }
  /**
   * Computes the transformed/skinned vertex position of the previous frame.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {Node<vec3>} The skinned position from the previous frame.
   */
  getPreviousSkinnedPosition(e) {
    const t = e.object;
    return this.previousBoneMatricesNode === null && (t.skeleton.previousBoneMatrices = new Float32Array(t.skeleton.boneMatrices), this.previousBoneMatricesNode = hu("skeleton.previousBoneMatrices", "mat4", t.skeleton.bones.length)), this.getSkinnedPosition(this.previousBoneMatricesNode, qn);
  }
  /**
   * Setups the skinning node by assigning the transformed vertex data to predefined node variables.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {Node<vec3>} The transformed vertex position.
   */
  setup(e) {
    e.needsPreviousData() && qn.assign(this.getPreviousSkinnedPosition(e));
    const t = this.getSkinnedPosition();
    if (this.toPositionNode && this.toPositionNode.assign(t), e.hasGeometryAttribute("normal")) {
      const { skinNormal: s, skinTangent: r } = this.getSkinnedNormalAndTangent();
      Mt.assign(s), e.hasGeometryAttribute("tangent") && Xn.assign(r);
    }
    return t;
  }
  /**
   * Generates the code snippet of the skinning node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {string} output - The current output.
   * @return {string} The generated code snippet.
   */
  generate(e, t) {
    if (t !== "void")
      return super.generate(e, t);
  }
  /**
   * Updates the state of the skinned mesh by updating the skeleton once per frame.
   *
   * @param {NodeFrame} frame - The current node frame.
   */
  update(e) {
    const t = e.object && e.object.skeleton ? e.object.skeleton : this.skinnedMesh.skeleton;
    yu.get(t) !== e.frameId && (yu.set(t, e.frameId), this.previousBoneMatricesNode !== null && (t.previousBoneMatrices === null && (t.previousBoneMatrices = new Float32Array(t.boneMatrices)), t.previousBoneMatrices.set(t.boneMatrices)), t.update());
  }
}
const Yx = (a) => new Qx(a);
class jx extends G {
  static get type() {
    return "LoopNode";
  }
  /**
   * Constructs a new loop node.
   *
   * @param {Array<any>} params - Depending on the loop type, array holds different parameterization values for the loop.
   */
  constructor(e = []) {
    super("void"), this.params = e;
  }
  /**
   * Returns a loop variable name based on an index. The pattern is
   * `0` = `i`, `1`= `j`, `2`= `k` and so on.
   *
   * @param {number} index - The index.
   * @return {string} The loop variable name.
   */
  getVarName(e) {
    return String.fromCharCode(105 + e);
  }
  /**
   * Returns properties about this node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {Object} The node properties.
   */
  getProperties(e) {
    const t = e.getNodeProperties(this);
    if (t.stackNode !== void 0) return t;
    const s = {};
    for (let o = 0, u = this.params.length - 1; o < u; o++) {
      const l = this.params[o], c = l.isNode !== !0 && l.name || this.getVarName(o), d = l.isNode !== !0 && l.type || "int";
      s[c] = bt(c, d);
    }
    const r = e.addStack(), n = this.params[this.params.length - 1](s);
    t.returnsNode = n.context({ nodeLoop: n }), t.stackNode = r;
    const i = this.params[0];
    if (i.isNode !== !0 && typeof i.update == "function") {
      const o = A(this.params[0].update)(s);
      t.updateNode = o.context({ nodeLoop: o });
    }
    return e.removeStack(), t;
  }
  setup(e) {
    if (this.getProperties(e), e.fnCall) {
      const t = e.getDataFromNode(e.fnCall.shaderNode);
      t.hasLoop = !0;
    }
  }
  generate(e) {
    const t = this.getProperties(e), s = this.params, r = t.stackNode;
    for (let i = 0, o = s.length - 1; i < o; i++) {
      const u = s[i];
      let l = !1, c = null, d = null, h = null, f = null, p = null, g = null;
      u.isNode ? u.getNodeType(e) === "bool" ? (l = !0, f = "bool", d = u.build(e, f)) : (f = "int", h = this.getVarName(i), c = "0", d = u.build(e, f), p = "<") : (f = u.type || "int", h = u.name || this.getVarName(i), c = u.start, d = u.end, p = u.condition, g = u.update, typeof c == "number" ? c = e.generateConst(f, c) : c && c.isNode && (c = c.build(e, f)), typeof d == "number" ? d = e.generateConst(f, d) : d && d.isNode && (d = d.build(e, f)), c !== void 0 && d === void 0 ? (c = c + " - 1", d = "0", p = ">=") : d !== void 0 && c === void 0 && (c = "0", p = "<"), p === void 0 && (Number(c) > Number(d) ? p = ">=" : p = "<"));
      let m;
      if (l)
        m = `while ( ${d} )`;
      else {
        const y = { start: c, end: d }, _ = y.start, v = y.end;
        let C;
        const w = () => p.includes("<") ? "+=" : "-=";
        if (g != null)
          switch (typeof g) {
            case "function":
              C = e.flowStagesNode(t.updateNode, "void").code.replace(/\t|;/g, "");
              break;
            case "number":
              C = h + " " + w() + " " + e.generateConst(f, g);
              break;
            case "string":
              C = h + " " + g;
              break;
            default:
              g.isNode ? C = h + " " + w() + " " + g.build(e) : (F("TSL: 'Loop( { update: ... } )' is not a function, string or number.", this.stackTrace), C = "break /* invalid update */");
          }
        else
          f === "int" || f === "uint" ? g = p.includes("<") ? "++" : "--" : g = w() + " 1.", C = h + " " + g;
        const M = e.getVar(f, h) + " = " + _, E = h + " " + p + " " + v;
        m = `for ( ${M}; ${E}; ${C} )`;
      }
      e.addFlowCode((i === 0 ? `
` : "") + e.tab + m + ` {

`).addFlowTab();
    }
    const n = r.build(e, "void");
    t.returnsNode.build(e, "void"), e.removeFlowTab().addFlowCode(`
` + e.tab + n);
    for (let i = 0, o = this.params.length - 1; i < o; i++)
      e.addFlowCode((i === 0 ? "" : e.tab) + `}

`).removeFlowTab();
    e.addFlowTab();
  }
}
const rt = (...a) => new jx(Js(a, "int")).toStack(), Zx = () => bt("break").toStack(), Li = /* @__PURE__ */ new WeakMap(), He = /* @__PURE__ */ new Se(), _u = /* @__PURE__ */ A(({ bufferMap: a, influence: e, stride: t, width: s, depth: r, offset: n }) => {
  const i = pe(im).mul(t).add(n), o = i.div(s), u = i.sub(o.mul(s));
  return tt(a, yt(u, o)).depth(r).xyz.mul(e);
});
function Jx(a) {
  const e = a.morphAttributes.position !== void 0, t = a.morphAttributes.normal !== void 0, s = a.morphAttributes.color !== void 0, r = a.morphAttributes.position || a.morphAttributes.normal || a.morphAttributes.color, n = r !== void 0 ? r.length : 0;
  let i = Li.get(a);
  if (i === void 0 || i.count !== n) {
    let y = function() {
      g.dispose(), Li.delete(a), a.removeEventListener("dispose", y);
    };
    i !== void 0 && i.texture.dispose();
    const o = a.morphAttributes.position || [], u = a.morphAttributes.normal || [], l = a.morphAttributes.color || [];
    let c = 0;
    e === !0 && (c = 1), t === !0 && (c = 2), s === !0 && (c = 3);
    let d = a.attributes.position.count * c, h = 1;
    const f = 4096;
    d > f && (h = Math.ceil(d / f), d = f);
    const p = new Float32Array(d * h * 4 * n), g = new Vf(p, d, h, n);
    g.type = De, g.needsUpdate = !0;
    const m = c * 4;
    for (let _ = 0; _ < n; _++) {
      const v = o[_], C = u[_], w = l[_], M = d * h * 4 * _;
      for (let E = 0; E < v.count; E++) {
        const U = E * m;
        e === !0 && (He.fromBufferAttribute(v, E), p[M + U + 0] = He.x, p[M + U + 1] = He.y, p[M + U + 2] = He.z, p[M + U + 3] = 0), t === !0 && (He.fromBufferAttribute(C, E), p[M + U + 4] = He.x, p[M + U + 5] = He.y, p[M + U + 6] = He.z, p[M + U + 7] = 0), s === !0 && (He.fromBufferAttribute(w, E), p[M + U + 8] = He.x, p[M + U + 9] = He.y, p[M + U + 10] = He.z, p[M + U + 11] = w.itemSize === 4 ? He.w : 1);
      }
    }
    i = {
      count: n,
      texture: g,
      stride: c,
      size: new je(d, h)
    }, Li.set(a, i), a.addEventListener("dispose", y);
  }
  return i;
}
class eT extends G {
  static get type() {
    return "MorphNode";
  }
  /**
   * Constructs a new morph node.
   *
   * @param {Mesh} mesh - The mesh holding the morph targets.
   */
  constructor(e) {
    super("void"), this.mesh = e, this.morphBaseInfluence = k(1), this.updateType = z.OBJECT;
  }
  /**
   * Setups the morph node by assigning the transformed vertex data to predefined node variables.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  setup(e) {
    const { geometry: t } = e, s = t.morphAttributes.position !== void 0, r = t.hasAttribute("normal") && t.morphAttributes.normal !== void 0, n = t.morphAttributes.position || t.morphAttributes.normal || t.morphAttributes.color, i = n !== void 0 ? n.length : 0, { texture: o, stride: u, size: l } = Jx(t);
    s === !0 && ve.mulAssign(this.morphBaseInfluence), r === !0 && Mt.mulAssign(this.morphBaseInfluence);
    const c = pe(l.width);
    rt(i, ({ i: d }) => {
      const h = b(0).toVar();
      this.mesh.count > 1 && this.mesh.morphTexture !== null && this.mesh.morphTexture !== void 0 ? h.assign(tt(this.mesh.morphTexture, yt(pe(d).add(1), pe(sr))).r) : h.assign(ge("morphTargetInfluences", "float").element(d).toVar()), ue(h.notEqual(0), () => {
        s === !0 && ve.addAssign(_u({
          bufferMap: o,
          influence: h,
          stride: u,
          width: c,
          depth: d,
          offset: pe(0)
        })), r === !0 && Mt.addAssign(_u({
          bufferMap: o,
          influence: h,
          stride: u,
          width: c,
          depth: d,
          offset: pe(1)
        }));
      });
    });
  }
  /**
   * Updates the state of the morphed mesh by updating the base influence.
   *
   * @param {NodeFrame} frame - The current node frame.
   */
  update() {
    const e = this.morphBaseInfluence;
    this.mesh.geometry.morphTargetsRelative ? e.value = 1 : e.value = 1 - this.mesh.morphTargetInfluences.reduce((t, s) => t + s, 0);
  }
}
const tT = /* @__PURE__ */ le(eT).setParameterLength(1);
class mr extends G {
  static get type() {
    return "LightingNode";
  }
  /**
   * Constructs a new lighting node.
   */
  constructor() {
    super("vec3"), this.isLightingNode = !0;
  }
}
class sT extends mr {
  static get type() {
    return "AONode";
  }
  /**
   * Constructs a new AO node.
   *
   * @param {?Node<float>} [aoNode=null] - The ambient occlusion node.
   */
  constructor(e = null) {
    super(), this.aoNode = e;
  }
  setup(e) {
    e.context.ambientOcclusion.mulAssign(this.aoNode);
  }
}
class rT extends Kc {
  static get type() {
    return "LightingContextNode";
  }
  /**
   * Constructs a new lighting context node.
   *
   * @param {LightsNode} lightsNode - The lights node.
   * @param {?LightingModel} [lightingModel=null] - The current lighting model.
   * @param {?Node<vec3>} [backdropNode=null] - A backdrop node.
   * @param {?Node<float>} [backdropAlphaNode=null] - A backdrop alpha node.
   */
  constructor(e, t = null, s = null, r = null) {
    super(e), this.lightingModel = t, this.backdropNode = s, this.backdropAlphaNode = r, this._value = null;
  }
  /**
   * Returns a lighting context object.
   *
   * @return {{
   * radiance: Node<vec3>,
   * irradiance: Node<vec3>,
   * iblIrradiance: Node<vec3>,
   * ambientOcclusion: Node<float>,
   * reflectedLight: {directDiffuse: Node<vec3>, directSpecular: Node<vec3>, indirectDiffuse: Node<vec3>, indirectSpecular: Node<vec3>},
   * backdrop: Node<vec3>,
   * backdropAlpha: Node<float>
   * }} The lighting context object.
   */
  getContext() {
    const { backdropNode: e, backdropAlphaNode: t } = this, s = N().toVar("directDiffuse"), r = N().toVar("directSpecular"), n = N().toVar("indirectDiffuse"), i = N().toVar("indirectSpecular"), o = {
      directDiffuse: s,
      directSpecular: r,
      indirectDiffuse: n,
      indirectSpecular: i
    };
    return {
      radiance: N().toVar("radiance"),
      irradiance: N().toVar("irradiance"),
      iblIrradiance: N().toVar("iblIrradiance"),
      ambientOcclusion: b(1).toVar("ambientOcclusion"),
      reflectedLight: o,
      backdrop: e,
      backdropAlpha: t
    };
  }
  setup(e) {
    return this.value = this._value || (this._value = this.getContext()), this.value.lightingModel = this.lightingModel || e.context.lightingModel, super.setup(e);
  }
}
const nT = /* @__PURE__ */ le(rT);
class iT extends mr {
  static get type() {
    return "IrradianceNode";
  }
  /**
   * Constructs a new irradiance node.
   *
   * @param {Node<vec3>} node - A node contributing irradiance.
   */
  constructor(e) {
    super(), this.node = e;
  }
  setup(e) {
    e.context.irradiance.addAssign(this.node);
  }
}
const ds = /* @__PURE__ */ new je();
class Nd extends Zr {
  static get type() {
    return "ViewportTextureNode";
  }
  /**
   * Constructs a new viewport texture node.
   *
   * @param {Node} [uvNode=screenUV] - The uv node.
   * @param {?Node} [levelNode=null] - The level node.
   * @param {?Texture} [framebufferTexture=null] - A framebuffer texture holding the viewport data. If not provided, a framebuffer texture is created automatically.
   */
  constructor(e = Es, t = null, s = null) {
    let r = null;
    s === null ? (r = new Gl(), r.minFilter = Rs, s = r) : r = s, super(s, e, t), this.generateMipmaps = !1, this.defaultFramebuffer = r, this.isOutputTextureNode = !0, this.updateBeforeType = z.RENDER, this._cacheTextures = /* @__PURE__ */ new WeakMap();
  }
  /**
   * This methods returns a texture for the given render target or canvas target reference.
   *
   * To avoid rendering errors, `ViewportTextureNode` must use unique framebuffer textures
   * for different render contexts.
   *
   * @param {?(RenderTarget|CanvasTarget)} [reference=null] - The render target or canvas target reference.
   * @return {Texture} The framebuffer texture.
   */
  getTextureForReference(e = null) {
    let t, s;
    if (this.referenceNode ? (t = this.referenceNode.defaultFramebuffer, s = this.referenceNode._cacheTextures) : (t = this.defaultFramebuffer, s = this._cacheTextures), e === null)
      return t;
    if (s.has(e) === !1) {
      const r = t.clone();
      s.set(e, r);
    }
    return s.get(e);
  }
  updateReference(e) {
    const t = e.renderer, s = t.getRenderTarget(), r = t.getCanvasTarget(), n = s || r;
    return this.value = this.getTextureForReference(n), this.value;
  }
  updateBefore(e) {
    const t = e.renderer, s = t.getRenderTarget(), r = t.getCanvasTarget(), n = s || r;
    n === null ? t.getDrawingBufferSize(ds) : n.getDrawingBufferSize ? n.getDrawingBufferSize(ds) : ds.set(n.width, n.height);
    const i = this.getTextureForReference(n);
    (i.image.width !== ds.width || i.image.height !== ds.height) && (i.image.width = ds.width, i.image.height = ds.height, i.needsUpdate = !0);
    const o = i.generateMipmaps;
    i.generateMipmaps = this.generateMipmaps, t.copyFramebufferToTexture(i), i.generateMipmaps = o;
  }
  clone() {
    const e = new this.constructor(this.uvNode, this.levelNode, this.value);
    return e.generateMipmaps = this.generateMipmaps, e;
  }
}
const Rd = /* @__PURE__ */ le(Nd, null, null, { generateMipmaps: !0 }).setParameterLength(0, 3), oT = /* @__PURE__ */ Rd(), aT = (a = Es, e = null) => oT.sample(a, e);
let Ii = null;
class uT extends Nd {
  static get type() {
    return "ViewportDepthTextureNode";
  }
  /**
   * Constructs a new viewport depth texture node.
   *
   * @param {Node} [uvNode=screenUV] - The uv node.
   * @param {?Node} [levelNode=null] - The level node.
   * @param {?DepthTexture} [depthTexture=null] - A depth texture. If not provided, uses a shared depth texture.
   */
  constructor(e = Es, t = null, s = null) {
    s === null && (Ii === null && (Ii = new vt()), s = Ii), super(e, t, s);
  }
}
const lT = /* @__PURE__ */ le(uT).setParameterLength(0, 3);
class ze extends G {
  static get type() {
    return "ViewportDepthNode";
  }
  /**
   * Constructs a new viewport depth node.
   *
   * @param {('depth'|'depthBase'|'linearDepth')} scope - The node's scope.
   * @param {?Node} [valueNode=null] - The value node.
   */
  constructor(e, t = null) {
    super("float"), this.scope = e, this.valueNode = t, this.isViewportDepthNode = !0;
  }
  generate(e) {
    const { scope: t } = this;
    return t === ze.DEPTH_BASE ? e.getFragDepth() : super.generate(e);
  }
  setup({ camera: e }) {
    const { scope: t } = this, s = this.valueNode;
    let r = null;
    if (t === ze.DEPTH_BASE)
      s !== null && (r = Cd().assign(s));
    else if (t === ze.DEPTH)
      e.isPerspectiveCamera ? r = Ad(be.z, xs, Ts) : r = Or(be.z, xs, Ts);
    else if (t === ze.LINEAR_DEPTH)
      if (s !== null)
        if (e.isPerspectiveCamera) {
          const n = vd(s, xs, Ts);
          r = Or(n, xs, Ts);
        } else
          r = s;
      else
        r = Or(be.z, xs, Ts);
    return r;
  }
}
ze.DEPTH_BASE = "depthBase";
ze.DEPTH = "depth";
ze.LINEAR_DEPTH = "linearDepth";
const Or = (a, e, t) => a.add(e).div(e.sub(t)), Ad = (a, e, t) => e.add(a).mul(t).div(t.sub(e).mul(a)), cT = (a, e, t) => e.mul(a.add(t)).div(a.mul(e.sub(t))), vd = /* @__PURE__ */ A(([a, e, t], s) => s.renderer.reversedDepthBuffer === !0 ? e.mul(t).div(e.sub(t).mul(a).sub(e)) : e.mul(t).div(t.sub(e).mul(a).sub(t))), Ed = (a, e, t) => {
  e = e.max(1e-6).toVar();
  const s = $t(a.negate().div(e)), r = $t(t.div(e));
  return s.div(r);
}, Cd = /* @__PURE__ */ le(ze, ze.DEPTH_BASE), wd = /* @__PURE__ */ B(ze, ze.DEPTH), dT = /* @__PURE__ */ le(ze, ze.LINEAR_DEPTH).setParameterLength(0, 1);
lT();
wd.assign = (a) => Cd(a);
class ot extends G {
  static get type() {
    return "ClippingNode";
  }
  /**
   * Constructs a new clipping node.
   *
   * @param {('default'|'hardware'|'alphaToCoverage')} [scope='default'] - The node's scope. Similar to other nodes,
   * the selected scope influences the behavior of the node and what type of code is generated.
   */
  constructor(e = ot.DEFAULT) {
    super(), this.scope = e;
  }
  /**
   * Setups the node depending on the selected scope.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {Node} The result node.
   */
  setup(e) {
    super.setup(e);
    const t = e.clippingContext, { intersectionPlanes: s, unionPlanes: r } = t;
    return this.hardwareClipping = e.material.hardwareClipping, this.scope === ot.ALPHA_TO_COVERAGE ? this.setupAlphaToCoverage(s, r) : this.scope === ot.HARDWARE ? this.setupHardwareClipping(r, e) : this.setupDefault(s, r);
  }
  /**
   * Setups alpha to coverage.
   *
   * @param {Array<Vector4>} intersectionPlanes - The intersection planes.
   * @param {Array<Vector4>} unionPlanes - The union planes.
   * @return {Node} The result node.
   */
  setupAlphaToCoverage(e, t) {
    return A(() => {
      const s = b().toVar("distanceToPlane"), r = b().toVar("distanceToGradient"), n = b(1).toVar("clipOpacity"), i = t.length;
      if (this.hardwareClipping === !1 && i > 0) {
        const u = st(t).setGroup(V);
        rt(i, ({ i: l }) => {
          const c = u.element(l);
          s.assign(be.dot(c.xyz).negate().add(c.w)), r.assign(s.fwidth().div(2)), n.mulAssign(Wt(r.negate(), r, s));
        });
      }
      const o = e.length;
      if (o > 0) {
        const u = st(e).setGroup(V), l = b(1).toVar("intersectionClipOpacity");
        rt(o, ({ i: c }) => {
          const d = u.element(c);
          s.assign(be.dot(d.xyz).negate().add(d.w)), r.assign(s.fwidth().div(2)), l.mulAssign(Wt(r.negate(), r, s).oneMinus());
        }), n.mulAssign(l.oneMinus());
      }
      re.a.mulAssign(n), re.a.equal(0).discard();
    })();
  }
  /**
   * Setups the default clipping.
   *
   * @param {Array<Vector4>} intersectionPlanes - The intersection planes.
   * @param {Array<Vector4>} unionPlanes - The union planes.
   * @return {Node} The result node.
   */
  setupDefault(e, t) {
    return A(() => {
      const s = t.length;
      if (this.hardwareClipping === !1 && s > 0) {
        const n = st(t).setGroup(V);
        rt(s, ({ i }) => {
          const o = n.element(i);
          be.dot(o.xyz).greaterThan(o.w).discard();
        });
      }
      const r = e.length;
      if (r > 0) {
        const n = st(e).setGroup(V), i = ci(!0).toVar("clipped");
        rt(r, ({ i: o }) => {
          const u = n.element(o);
          i.assign(be.dot(u.xyz).greaterThan(u.w).and(i));
        }), i.discard();
      }
    })();
  }
  /**
   * Setups hardware clipping.
   *
   * @param {Array<Vector4>} unionPlanes - The union planes.
   * @param {NodeBuilder} builder - The current node builder.
   * @return {Node} The result node.
   */
  setupHardwareClipping(e, t) {
    const s = e.length;
    return t.enableHardwareClipping(s), A(() => {
      const r = st(e).setGroup(V), n = fr(t.getClipDistance());
      rt(s, ({ i }) => {
        const o = r.element(i), u = be.dot(o.xyz).sub(o.w).negate();
        n.element(i).assign(u);
      });
    })();
  }
}
ot.ALPHA_TO_COVERAGE = "alphaToCoverage";
ot.DEFAULT = "default";
ot.HARDWARE = "hardware";
const hT = () => new ot(), fT = () => new ot(ot.ALPHA_TO_COVERAGE), pT = () => new ot(ot.HARDWARE), gT = 0.05, bu = /* @__PURE__ */ A(([a]) => wt(X(1e4, qe(X(17, a.x).add(X(0.1, a.y)))).mul(We(0.1, Xe(qe(X(13, a.y).add(a.x))))))), Su = /* @__PURE__ */ A(([a]) => bu(I(bu(a.xy), a.z))), mT = /* @__PURE__ */ A(([a]) => {
  const e = Be(
    zt(Vc(a.xyz)),
    zt($c(a.xyz))
  ), t = b(1).div(b(gT).mul(e)).toVar("pixScale"), s = I(
    zr(vs($t(t))),
    zr(va($t(t)))
  ), r = I(
    Su(vs(s.x.mul(a.xyz))),
    Su(vs(s.y.mul(a.xyz)))
  ), n = wt($t(t)), i = We(X(n.oneMinus(), r.x), X(n, r.y)), o = or(n, n.oneMinus()), u = N(
    i.mul(i).div(X(2, o).mul(ke(1, o))),
    i.sub(X(0.5, o)).div(ke(1, o)),
    ke(1, ke(1, i).mul(ke(1, i)).div(X(2, o).mul(ke(1, o))))
  ), l = i.lessThan(o.oneMinus()).select(i.lessThan(o).select(u.x, u.y), u.z);
  return os(l, 1e-6, 1);
}).setLayout({
  name: "getAlphaHashThreshold",
  type: "float",
  inputs: [
    { name: "position", type: "vec3" }
  ]
});
class xT extends rd {
  static get type() {
    return "VertexColorNode";
  }
  /**
   * Constructs a new vertex color node.
   *
   * @param {number} index - The attribute index.
   */
  constructor(e) {
    super(null, "vec4"), this.isVertexColorNode = !0, this.index = e;
  }
  /**
   * Overwrites the default implementation by honoring the attribute index.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The attribute name.
   */
  getAttributeName() {
    const e = this.index;
    return "color" + (e > 0 ? e : "");
  }
  generate(e) {
    const t = this.getAttributeName(e), s = e.hasGeometryAttribute(t);
    let r;
    return s === !0 ? r = super.generate(e) : r = e.generateConst(this.nodeType, new Se(1, 1, 1, 1)), r;
  }
  serialize(e) {
    super.serialize(e), e.index = this.index;
  }
  deserialize(e) {
    super.deserialize(e), this.index = e.index;
  }
}
const TT = (a = 0) => new xT(a), yT = /* @__PURE__ */ A(([a]) => W(a.rgb.mul(a.a), a.a), { color: "vec4", return: "vec4" });
class Ne extends $a {
  static get type() {
    return "NodeMaterial";
  }
  /**
   * Represents the type of the node material.
   *
   * @type {string}
   */
  get type() {
    return this.constructor.type;
  }
  set type(e) {
  }
  /**
   * Constructs a new node material.
   */
  constructor() {
    super(), this.isNodeMaterial = !0, this.fog = !0, this.lights = !1, this.hardwareClipping = !1, this.lightsNode = null, this.envNode = null, this.aoNode = null, this.colorNode = null, this.normalNode = null, this.opacityNode = null, this.backdropNode = null, this.backdropAlphaNode = null, this.alphaTestNode = null, this.maskNode = null, this.maskShadowNode = null, this.positionNode = null, this.geometryNode = null, this.depthNode = null, this.receivedShadowPositionNode = null, this.castShadowPositionNode = null, this.receivedShadowNode = null, this.castShadowNode = null, this.outputNode = null, this.mrtNode = null, this.fragmentNode = null, this.vertexNode = null, this.contextNode = null;
  }
  /**
   * Returns an array of child nodes for this material.
   *
   * @private
   * @returns {Array<{property: string, childNode: Node}>}
   */
  _getNodeChildren() {
    const e = [];
    for (const t of Object.getOwnPropertyNames(this)) {
      if (t.startsWith("_") === !0) continue;
      const s = this[t];
      s && s.isNode === !0 && e.push({ property: t, childNode: s });
    }
    return e;
  }
  /**
   * Allows to define a custom cache key that influence the material key computation
   * for render objects.
   *
   * @return {string} The custom cache key.
   */
  customProgramCacheKey() {
    const e = [];
    for (const { property: t, childNode: s } of this._getNodeChildren())
      e.push(lr(t.slice(0, -4)), s.getCacheKey());
    return this.type + Yr(e);
  }
  /**
   * Builds this material with the given node builder.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  build(e) {
    this.setup(e);
  }
  /**
   * Setups a node material observer with the given builder.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {NodeMaterialObserver} The node material observer.
   */
  setupObserver(e) {
    return new Xf(e);
  }
  /**
   * Setups the vertex and fragment stage of this node material.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  setup(e) {
    e.context.setupNormal = () => tr(this.setupNormal(e), "NORMAL", "vec3"), e.context.setupPositionView = () => this.setupPositionView(e), e.context.setupModelViewProjection = () => this.setupModelViewProjection(e);
    const t = e.renderer, s = t.getRenderTarget();
    e.addStack();
    const r = this.setupVertex(e), n = tr(this.vertexNode || r, "VERTEX");
    e.context.clipSpace = n, e.stack.outputNode = n, this.setupHardwareClipping(e), this.geometryNode !== null && (e.stack.outputNode = e.stack.outputNode.bypass(this.geometryNode)), e.addFlow("vertex", e.removeStack()), e.addStack();
    let i;
    const o = this.setupClipping(e);
    if ((this.depthWrite === !0 || this.depthTest === !0) && (s !== null ? s.depthBuffer === !0 && this.setupDepth(e) : t.depth === !0 && this.setupDepth(e)), this.fragmentNode === null) {
      this.setupDiffuseColor(e), this.setupVariants(e);
      const u = this.setupLighting(e);
      o !== null && e.stack.addToStack(o);
      const l = W(u, re.a).max(0);
      i = this.setupOutput(e, l), Pr.assign(i);
      const c = this.outputNode !== null;
      if (c && (i = this.outputNode), e.context.getOutput && (i = e.context.getOutput(i, e)), s !== null) {
        const d = t.getMRT(), h = this.mrtNode;
        d !== null ? (c && Pr.assign(i), i = d, h !== null && (i = d.merge(h))) : h !== null && (i = h);
      }
    } else {
      let u = this.fragmentNode;
      u.isOutputStructNode !== !0 && (u = W(u)), i = this.setupOutput(e, u);
    }
    e.stack.outputNode = i, e.addFlow("fragment", e.removeStack()), e.observer = this.setupObserver(e);
  }
  /**
   * Setups the clipping node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {ClippingNode} The clipping node.
   */
  setupClipping(e) {
    if (e.clippingContext === null) return null;
    const { unionPlanes: t, intersectionPlanes: s } = e.clippingContext;
    let r = null;
    if (t.length > 0 || s.length > 0) {
      const n = e.renderer.currentSamples;
      this.alphaToCoverage && n > 1 ? r = fT() : e.stack.addToStack(hT());
    }
    return r;
  }
  /**
   * Setups the hardware clipping if available on the current device.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  setupHardwareClipping(e) {
    if (this.hardwareClipping = !1, e.clippingContext === null) return;
    const t = e.clippingContext.unionPlanes.length;
    t > 0 && t <= 8 && e.isAvailable("clipDistance") && (e.stack.addToStack(pT()), this.hardwareClipping = !0);
  }
  /**
   * Setups the depth of this material.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  setupDepth(e) {
    const { renderer: t, camera: s } = e;
    let r = this.depthNode;
    if (r === null) {
      const n = t.getMRT();
      n && n.has("depth") ? r = n.get("depth") : t.logarithmicDepthBuffer === !0 && (s.isPerspectiveCamera ? r = Ed(be.z, xs, Ts) : r = Or(be.z, xs, Ts));
    }
    r !== null && wd.assign(r).toStack();
  }
  /**
   * Setups the position node in view space. This method exists
   * so derived node materials can modify the implementation e.g. sprite materials.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {Node<vec3>} The position in view space.
   */
  setupPositionView() {
    return Jr.mul(ve).xyz;
  }
  /**
   * Setups the position in clip space.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {Node<vec4>} The position in view space.
   */
  setupModelViewProjection() {
    return Ir.mul(be);
  }
  /**
   * Setups the logic for the vertex stage.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {Node<vec4>} The position in clip space.
   */
  setupVertex(e) {
    return e.addStack(), this.setupPosition(e), e.context.position = e.removeStack(), kx;
  }
  /**
   * Setups the computation of the position in local space.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {Node<vec3>} The position in local space.
   */
  setupPosition(e) {
    const { object: t, geometry: s } = e;
    if ((s.morphAttributes.position || s.morphAttributes.normal || s.morphAttributes.color) && tT(t).toStack(), t.isSkinnedMesh === !0 && Yx(t).toStack(), this.displacementMap) {
      const r = Zt("displacementMap", "texture"), n = Zt("displacementScale", "float"), i = Zt("displacementBias", "float");
      ve.addAssign(Mt.normalize().mul(r.x.mul(n).add(i)));
    }
    return t.isBatchedMesh && Kx(t).toStack(), t.isInstancedMesh && t.instanceMatrix && t.instanceMatrix.isInstancedBufferAttribute === !0 && qx(t).toStack(), this.positionNode !== null && ve.assign(tr(this.positionNode, "POSITION", "vec3")), ve;
  }
  /**
   * Setups the computation of the material's diffuse color.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {BufferGeometry} geometry - The geometry.
   */
  setupDiffuseColor(e) {
    const { object: t, geometry: s } = e;
    this.maskNode !== null && ci(this.maskNode).not().discard();
    let r = this.colorNode ? W(this.colorNode) : cx;
    this.vertexColors === !0 && s.hasAttribute("color") && (r = r.mul(TT())), t.instanceColor && (r = Hn("vec3", "vInstanceColor").mul(r)), t.isBatchedMesh && t._colorsTexture && (r = Hn("vec3", "vBatchColor").mul(r)), re.assign(r);
    const n = this.opacityNode ? b(this.opacityNode) : bd;
    re.a.assign(re.a.mul(n));
    let i = null;
    (this.alphaTestNode !== null || this.alphaTest > 0) && (i = this.alphaTestNode !== null ? b(this.alphaTestNode) : lx, this.alphaToCoverage === !0 ? (re.a = Wt(i, i.add(zc(re.a)), re.a), re.a.lessThanEqual(0).discard()) : re.a.lessThanEqual(i).discard()), this.alphaHash === !0 && re.a.lessThan(mT(ve)).discard(), e.isOpaque() && re.a.assign(1);
  }
  /**
   * Abstract interface method that can be implemented by derived materials
   * to setup material-specific node variables.
   *
   * @abstract
   * @param {NodeBuilder} builder - The current node builder.
   */
  setupVariants() {
  }
  /**
   * Setups the outgoing light node variable
   *
   * @return {Node<vec3>} The outgoing light node.
   */
  setupOutgoingLight() {
    return this.lights === !0 ? N(0) : re.rgb;
  }
  /**
   * Setups the normal node from the material.
   *
   * @return {Node<vec3>} The normal node.
   */
  setupNormal() {
    return this.normalNode ? N(this.normalNode) : xx;
  }
  /**
   * Setups the environment node from the material.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {Node<vec4>} The environment node.
   */
  setupEnvironment() {
    let e = null;
    return this.envNode ? e = this.envNode : this.envMap && (e = this.envMap.isCubeTexture ? Zt("envMap", "cubeTexture") : Zt("envMap", "texture")), e;
  }
  /**
   * Setups the light map node from the material.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {Node<vec3>} The light map node.
   */
  setupLightMap(e) {
    let t = null;
    return e.material.lightMap && (t = new iT(Sd)), t;
  }
  /**
   * Setups the lights node based on the scene, environment and material.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {LightsNode} The lights node.
   */
  setupLights(e) {
    const t = [], s = this.setupEnvironment(e);
    s && s.isLightingNode && t.push(s);
    const r = this.setupLightMap(e);
    r && r.isLightingNode && t.push(r);
    let n = this.aoNode;
    n === null && e.material.aoMap && (n = Gx), e.context.getAO && (n = e.context.getAO(n, e)), n && t.push(new sT(n));
    let i = this.lightsNode || e.lightsNode;
    return t.length > 0 && (i = e.renderer.lighting.createNode([...i.getLights(), ...t])), i;
  }
  /**
   * This method should be implemented by most derived materials
   * since it defines the material's lighting model.
   *
   * @abstract
   * @param {NodeBuilder} builder - The current node builder.
   * @return {LightingModel} The lighting model.
   */
  setupLightingModel() {
  }
  /**
   * Setups the outgoing light node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {Node<vec3>} The outgoing light node.
   */
  setupLighting(e) {
    const { material: t } = e, { backdropNode: s, backdropAlphaNode: r, emissiveNode: n } = this, o = this.lights === !0 || this.lightsNode !== null ? this.setupLights(e) : null;
    let u = this.setupOutgoingLight(e);
    if (o && o.getScope().hasLights) {
      const l = this.setupLightingModel(e) || null;
      u = nT(o, l, s, r);
    } else s !== null && (u = N(r !== null ? ne(u, s, r) : s));
    return (n && n.isNode === !0 || t.emissive && t.emissive.isColor === !0) && (su.assign(N(n || hx)), u = u.add(su)), u;
  }
  /**
   * Setup the fog.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {Node<vec4>} outputNode - The existing output node.
   * @return {Node<vec4>} The output node.
   */
  setupFog(e, t) {
    const s = e.fogNode;
    return s && (Pr.assign(t), t = W(s.toVar())), t;
  }
  /**
   * Setups premultiplied alpha.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {Node<vec4>} outputNode - The existing output node.
   * @return {Node<vec4>} The output node.
   */
  setupPremultipliedAlpha(e, t) {
    return yT(t);
  }
  /**
   * Setups the output node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {Node<vec4>} outputNode - The existing output node.
   * @return {Node<vec4>} The output node.
   */
  setupOutput(e, t) {
    return this.fog === !0 && (t = this.setupFog(e, t)), this.premultipliedAlpha === !0 && (t = this.setupPremultipliedAlpha(e, t)), t;
  }
  /**
   * Most classic material types have a node pendant e.g. for `MeshBasicMaterial`
   * there is `MeshBasicNodeMaterial`. This utility method is intended for
   * defining all material properties of the classic type in the node type.
   *
   * @param {Material} material - The material to copy properties with their values to this node material.
   */
  setDefaultValues(e) {
    for (const s in e) {
      const r = e[s];
      this[s] === void 0 && (this[s] = r, r && r.clone && (this[s] = r.clone()));
    }
    const t = Object.getOwnPropertyDescriptors(e.constructor.prototype);
    for (const s in t)
      Object.getOwnPropertyDescriptor(this.constructor.prototype, s) === void 0 && t[s].get !== void 0 && Object.defineProperty(this.constructor.prototype, s, t[s]);
  }
  /**
   * Serializes this material to JSON.
   *
   * @param {?(Object|string)} meta - The meta information for serialization.
   * @return {Object} The serialized node.
   */
  toJSON(e) {
    const t = e === void 0 || typeof e == "string";
    t && (e = {
      textures: {},
      images: {},
      nodes: {}
    });
    const s = $a.prototype.toJSON.call(this, e);
    s.inputNodes = {};
    for (const { property: n, childNode: i } of this._getNodeChildren())
      s.inputNodes[n] = i.toJSON(e).uuid;
    function r(n) {
      const i = [];
      for (const o in n) {
        const u = n[o];
        delete u.metadata, i.push(u);
      }
      return i;
    }
    if (t) {
      const n = r(e.textures), i = r(e.images), o = r(e.nodes);
      n.length > 0 && (s.textures = n), i.length > 0 && (s.images = i), o.length > 0 && (s.nodes = o);
    }
    return s;
  }
  /**
   * Copies the properties of the given node material to this instance.
   *
   * @param {NodeMaterial} source - The material to copy.
   * @return {NodeMaterial} A reference to this node material.
   */
  copy(e) {
    return this.lightsNode = e.lightsNode, this.envNode = e.envNode, this.aoNode = e.aoNode, this.colorNode = e.colorNode, this.normalNode = e.normalNode, this.opacityNode = e.opacityNode, this.backdropNode = e.backdropNode, this.backdropAlphaNode = e.backdropAlphaNode, this.alphaTestNode = e.alphaTestNode, this.maskNode = e.maskNode, this.maskShadowNode = e.maskShadowNode, this.positionNode = e.positionNode, this.geometryNode = e.geometryNode, this.depthNode = e.depthNode, this.receivedShadowPositionNode = e.receivedShadowPositionNode, this.castShadowPositionNode = e.castShadowPositionNode, this.receivedShadowNode = e.receivedShadowNode, this.castShadowNode = e.castShadowNode, this.outputNode = e.outputNode, this.mrtNode = e.mrtNode, this.fragmentNode = e.fragmentNode, this.vertexNode = e.vertexNode, this.contextNode = e.contextNode, super.copy(e);
  }
}
const _T = /* @__PURE__ */ new ef();
class bT extends Ne {
  static get type() {
    return "LineBasicNodeMaterial";
  }
  /**
   * Constructs a new line basic node material.
   *
   * @param {Object} [parameters] - The configuration parameter.
   */
  constructor(e) {
    super(), this.isLineBasicNodeMaterial = !0, this.setDefaultValues(_T), this.setValues(e);
  }
}
const ST = /* @__PURE__ */ new tf();
class NT extends Ne {
  static get type() {
    return "LineDashedNodeMaterial";
  }
  /**
   * Constructs a new line dashed node material.
   *
   * @param {Object} [parameters] - The configuration parameter.
   */
  constructor(e) {
    super(), this.isLineDashedNodeMaterial = !0, this.setDefaultValues(ST), this.dashOffset = 0, this.offsetNode = null, this.dashScaleNode = null, this.dashSizeNode = null, this.gapSizeNode = null, this.setValues(e);
  }
  /**
   * Setups the dash specific node variables.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  setupVariants() {
    const e = this.offsetNode ? b(this.offsetNode) : Lx, t = this.dashScaleNode ? b(this.dashScaleNode) : Ux, s = this.dashSizeNode ? b(this.dashSizeNode) : Dx, r = this.gapSizeNode ? b(this.gapSizeNode) : Px;
    Ci.assign(s), ru.assign(r);
    const n = Is(as("lineDistance").mul(t));
    (e ? n.add(e) : n).mod(Ci.add(ru)).greaterThan(Ci).discard();
  }
}
const RT = /* @__PURE__ */ new Zh();
class AT extends Ne {
  static get type() {
    return "MeshNormalNodeMaterial";
  }
  /**
   * Constructs a new mesh normal node material.
   *
   * @param {Object} [parameters] - The configuration parameter.
   */
  constructor(e) {
    super(), this.isMeshNormalNodeMaterial = !0, this.setDefaultValues(RT), this.setValues(e);
  }
  /**
   * Overwrites the default implementation by computing the diffuse color
   * based on the normal data.
   */
  setupDiffuseColor() {
    const e = this.opacityNode ? b(this.opacityNode) : bd;
    re.assign(Ma(W(rx(Z), e), Bl));
  }
}
const Md = /* @__PURE__ */ A(([a = ad]) => {
  const e = a.z.atan(a.x).mul(1 / (Math.PI * 2)).add(0.5), t = a.y.clamp(-1, 1).asin().mul(1 / Math.PI).add(0.5);
  return I(e, t);
});
class Bd extends Ds {
  /**
   * Constructs a new cube render target.
   *
   * @param {number} [size=1] - The size of the render target.
   * @param {RenderTarget~Options} [options] - The configuration object.
   */
  constructor(e = 1, t = {}) {
    super(e, e, t), this.isCubeRenderTarget = !0;
    const s = { width: e, height: e, depth: 1 }, r = [s, s, s, s, s, s];
    this.texture = new ai(r), this._setTextureOptions(t), this.texture.isRenderTargetTexture = !0;
  }
  /**
   * Converts the given equirectangular texture to a cube map.
   *
   * @param {Renderer} renderer - The renderer.
   * @param {Texture} texture - The equirectangular texture.
   * @return {CubeRenderTarget} A reference to this cube render target.
   */
  fromEquirectangularTexture(e, t) {
    const s = t.minFilter, r = t.generateMipmaps;
    t.generateMipmaps = !0, this.texture.type = t.type, this.texture.colorSpace = t.colorSpace, this.texture.generateMipmaps = t.generateMipmaps, this.texture.minFilter = t.minFilter, this.texture.magFilter = t.magFilter;
    const n = new xc(5, 5, 5), i = Md(ad), o = new Ne();
    o.colorNode = ce(t, i, 0), o.side = Ge, o.blending = ss;
    const u = new is(n, o), l = new Ul();
    l.add(u), t.minFilter === Rs && (t.minFilter = Ze);
    const c = new Ff(1, 10, this), d = e.getMRT();
    return e.setMRT(null), c.update(e, l), e.setMRT(d), t.minFilter = s, t.generateMipmaps = r, u.geometry.dispose(), u.material.dispose(), this;
  }
  /**
   * Clears this cube render target.
   *
   * @param {Renderer} renderer - The renderer.
   * @param {boolean} [color=true] - Whether the color buffer should be cleared or not.
   * @param {boolean} [depth=true] - Whether the depth buffer should be cleared or not.
   * @param {boolean} [stencil=true] - Whether the stencil buffer should be cleared or not.
   */
  clear(e, t = !0, s = !0, r = !0) {
    const n = e.getRenderTarget();
    for (let i = 0; i < 6; i++)
      e.setRenderTarget(this, i), e.clear(t, s, r);
    e.setRenderTarget(n);
  }
}
const Gr = /* @__PURE__ */ new WeakMap();
class vT extends Re {
  static get type() {
    return "CubeMapNode";
  }
  /**
   * Constructs a new cube map node.
   *
   * @param {Node} envNode - The node representing the environment map.
   */
  constructor(e) {
    super("vec3"), this.envNode = e, this._cubeTexture = null, this._cubeTextureNode = Ve(null);
    const t = new ai();
    t.isRenderTargetTexture = !0, this._defaultTexture = t, this.updateBeforeType = z.RENDER;
  }
  updateBefore(e) {
    const { renderer: t, material: s } = e, r = this.envNode;
    if (r.isTextureNode || r.isMaterialReferenceNode) {
      const n = r.isTextureNode ? r.value : s[r.property];
      if (n && n.isTexture) {
        const i = n.mapping;
        if (i === oa || i === aa) {
          if (Gr.has(n)) {
            const o = Gr.get(n);
            Nu(o, n.mapping), this._cubeTexture = o;
          } else {
            const o = n.image;
            if (ET(o)) {
              const u = new Bd(o.height);
              u.fromEquirectangularTexture(t, n), Nu(u.texture, n.mapping), this._cubeTexture = u.texture, Gr.set(n, u.texture), n.addEventListener("dispose", Fd);
            } else
              this._cubeTexture = this._defaultTexture;
          }
          this._cubeTextureNode.value = this._cubeTexture;
        } else
          this._cubeTextureNode = this.envNode;
      }
    }
  }
  setup(e) {
    return this.updateBefore(e), this._cubeTextureNode;
  }
}
function ET(a) {
  return a == null ? !1 : a.height > 0;
}
function Fd(a) {
  const e = a.target;
  e.removeEventListener("dispose", Fd);
  const t = Gr.get(e);
  t !== void 0 && (Gr.delete(e), t.dispose());
}
function Nu(a, e) {
  e === oa ? a.mapping = Vn : e === aa && (a.mapping = $n);
}
const Ud = /* @__PURE__ */ le(vT).setParameterLength(1);
class Da extends mr {
  static get type() {
    return "BasicEnvironmentNode";
  }
  /**
   * Constructs a new basic environment node.
   *
   * @param {Node} [envNode=null] - A node representing the environment.
   */
  constructor(e = null) {
    super(), this.envNode = e;
  }
  setup(e) {
    e.context.environment = Ud(this.envNode);
  }
}
class CT extends mr {
  static get type() {
    return "BasicLightMapNode";
  }
  /**
   * Constructs a new basic light map node.
   *
   * @param {?Node<vec3>} [lightMapNode=null] - The light map node.
   */
  constructor(e = null) {
    super(), this.lightMapNode = e;
  }
  setup(e) {
    const t = b(1 / Math.PI);
    e.context.irradianceLightMap = this.lightMapNode.mul(t);
  }
}
class pi {
  /**
   * This method is intended for setting up lighting model and context data
   * which are later used in the evaluation process.
   *
   * @abstract
   * @param {NodeBuilder} builder - The current node builder.
   */
  start(e) {
    e.lightsNode.setupLights(e, e.lightsNode.getLightNodes(e)), this.indirect(e);
  }
  /**
   * This method is intended for executing final tasks like final updates
   * to the outgoing light.
   *
   * @abstract
   * @param {NodeBuilder} builder - The current node builder.
   */
  finish() {
  }
  /**
   * This method is intended for implementing the direct light term and
   * executed during the build process of directional, point and spot light nodes.
   *
   * @abstract
   * @param {Object} lightData - The light data.
   * @param {NodeBuilder} builder - The current node builder.
   */
  direct() {
  }
  /**
   * This method is intended for implementing the direct light term for
   * rect area light nodes.
   *
   * @abstract
   * @param {Object} lightData - The light data.
   * @param {NodeBuilder} builder - The current node builder.
   */
  directRectArea() {
  }
  /**
   * This method is intended for implementing the indirect light term.
   *
   * @abstract
   * @param {NodeBuilder} builder - The current node builder.
   */
  indirect() {
  }
  /**
   * This method is intended for implementing the ambient occlusion term.
   * Unlike other methods, this method must be called manually by the lighting
   * model in its indirect term.
   *
   * @abstract
   * @param {NodeBuilder} builder - The current node builder.
   */
  ambientOcclusion() {
  }
}
class Dd extends pi {
  /**
   * Constructs a new basic lighting model.
   */
  constructor() {
    super();
  }
  /**
   * Implements the baked indirect lighting with its modulation.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  indirect({ context: e }) {
    const t = e.ambientOcclusion, s = e.reflectedLight, r = e.irradianceLightMap;
    s.indirectDiffuse.assign(W(0)), r ? s.indirectDiffuse.addAssign(r) : s.indirectDiffuse.addAssign(W(1, 1, 1, 0)), s.indirectDiffuse.mulAssign(t), s.indirectDiffuse.mulAssign(re.rgb);
  }
  /**
   * Implements the environment mapping.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  finish(e) {
    const { material: t, context: s } = e, r = s.outgoingLight, n = e.context.environment;
    if (n)
      switch (t.combine) {
        case Mf:
          r.rgb.assign(ne(r.rgb, r.rgb.mul(n.rgb), Un.mul(Pi)));
          break;
        case wf:
          r.rgb.assign(ne(r.rgb, n.rgb, Un.mul(Pi)));
          break;
        case Cf:
          r.rgb.addAssign(n.rgb.mul(Un.mul(Pi)));
          break;
        default:
          L("BasicLightingModel: Unsupported .combine value:", t.combine);
          break;
      }
  }
}
const wT = /* @__PURE__ */ new gs();
class MT extends Ne {
  static get type() {
    return "MeshBasicNodeMaterial";
  }
  /**
   * Constructs a new mesh basic node material.
   *
   * @param {Object} [parameters] - The configuration parameter.
   */
  constructor(e) {
    super(), this.isMeshBasicNodeMaterial = !0, this.lights = !0, this.setDefaultValues(wT), this.setValues(e);
  }
  /**
   * Basic materials are not affected by normal and bump maps so we
   * return by default {@link normalViewGeometry}.
   *
   * @return {Node<vec3>} The normal node.
   */
  setupNormal() {
    return en(Hr);
  }
  /**
   * Overwritten since this type of material uses {@link BasicEnvironmentNode}
   * to implement the default environment mapping.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {?BasicEnvironmentNode<vec3>} The environment node.
   */
  setupEnvironment(e) {
    const t = super.setupEnvironment(e);
    return t ? new Da(t) : null;
  }
  /**
   * This method must be overwritten since light maps are evaluated
   * with a special scaling factor for basic materials.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {?BasicLightMapNode<vec3>} The light map node.
   */
  setupLightMap(e) {
    let t = null;
    return e.material.lightMap && (t = new CT(Sd)), t;
  }
  /**
   * The material overwrites this method because `lights` is set to `true` but
   * we still want to return the diffuse color as the outgoing light.
   *
   * @return {Node<vec3>} The outgoing light node.
   */
  setupOutgoingLight() {
    return re.rgb;
  }
  /**
   * Setups the lighting model.
   *
   * @return {BasicLightingModel} The lighting model.
   */
  setupLightingModel() {
    return new Dd();
  }
}
const qr = /* @__PURE__ */ A(({ f0: a, f90: e, dotVH: t }) => {
  const s = t.mul(-5.55473).sub(6.98316).mul(t).exp2();
  return a.mul(s.oneMinus()).add(e.mul(s));
}), ur = /* @__PURE__ */ A((a) => a.diffuseColor.mul(1 / Math.PI)), BT = () => b(0.25), FT = /* @__PURE__ */ A(({ dotNH: a }) => qo.mul(b(0.5)).add(1).mul(b(1 / Math.PI)).mul(a.pow(qo))), UT = /* @__PURE__ */ A(({ lightDirection: a }) => {
  const e = a.add(he).normalize(), t = Z.dot(e).clamp(), s = he.dot(e).clamp(), r = qr({ f0: Ms, f90: 1, dotVH: s }), n = BT(), i = FT({ dotNH: t });
  return r.mul(n).mul(i);
});
class Pd extends Dd {
  /**
   * Constructs a new phong lighting model.
   *
   * @param {boolean} [specular=true] - Whether specular is supported or not.
   */
  constructor(e = !0) {
    super(), this.specular = e;
  }
  /**
   * Implements the direct lighting. The specular portion is optional an can be controlled
   * with the {@link PhongLightingModel#specular} flag.
   *
   * @param {Object} lightData - The light data.
   */
  direct({ lightDirection: e, lightColor: t, reflectedLight: s }) {
    const n = Z.dot(e).clamp().mul(t);
    s.directDiffuse.addAssign(n.mul(ur({ diffuseColor: re.rgb }))), this.specular === !0 && s.directSpecular.addAssign(n.mul(UT({ lightDirection: e })).mul(Un));
  }
  /**
   * Implements the indirect lighting.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  indirect(e) {
    const { ambientOcclusion: t, irradiance: s, reflectedLight: r } = e.context;
    r.indirectDiffuse.addAssign(s.mul(ur({ diffuseColor: re }))), r.indirectDiffuse.mulAssign(t);
  }
}
const DT = /* @__PURE__ */ new jh();
class PT extends Ne {
  static get type() {
    return "MeshLambertNodeMaterial";
  }
  /**
   * Constructs a new mesh lambert node material.
   *
   * @param {Object} [parameters] - The configuration parameter.
   */
  constructor(e) {
    super(), this.isMeshLambertNodeMaterial = !0, this.lights = !0, this.setDefaultValues(DT), this.setValues(e);
  }
  /**
   * Overwritten since this type of material uses {@link BasicEnvironmentNode}
   * to implement the default environment mapping.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {?BasicEnvironmentNode<vec3>} The environment node.
   */
  setupEnvironment(e) {
    const t = super.setupEnvironment(e);
    return t ? new Da(t) : null;
  }
  /**
   * Setups the lighting model.
   *
   * @return {PhongLightingModel} The lighting model.
   */
  setupLightingModel() {
    return new Pd(!1);
  }
}
const LT = /* @__PURE__ */ new Xh();
class IT extends Ne {
  static get type() {
    return "MeshPhongNodeMaterial";
  }
  /**
   * Constructs a new mesh lambert node material.
   *
   * @param {Object} [parameters] - The configuration parameter.
   */
  constructor(e) {
    super(), this.isMeshPhongNodeMaterial = !0, this.lights = !0, this.shininessNode = null, this.specularNode = null, this.setDefaultValues(LT), this.setValues(e);
  }
  /**
   * Overwritten since this type of material uses {@link BasicEnvironmentNode}
   * to implement the default environment mapping.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {?BasicEnvironmentNode<vec3>} The environment node.
   */
  setupEnvironment(e) {
    const t = super.setupEnvironment(e);
    return t ? new Da(t) : null;
  }
  /**
   * Setups the lighting model.
   *
   * @return {PhongLightingModel} The lighting model.
   */
  setupLightingModel() {
    return new Pd();
  }
  /**
   * Setups the phong specific node variables.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  setupVariants() {
    const e = (this.shininessNode ? b(this.shininessNode) : dx).max(1e-4);
    qo.assign(e);
    const t = this.specularNode || fx;
    Ms.assign(t);
  }
  copy(e) {
    return this.shininessNode = e.shininessNode, this.specularNode = e.specularNode, super.copy(e);
  }
}
const OT = /* @__PURE__ */ A((a) => {
  if (a.geometry.hasAttribute("normal") === !1)
    return b(0);
  const e = Hr.dFdx().abs().max(Hr.dFdy().abs());
  return e.x.max(e.y).max(e.z);
}), Ld = /* @__PURE__ */ A((a) => {
  const { roughness: e } = a, t = OT();
  let s = e.max(0.0525);
  return s = s.add(t), s = s.min(1), s;
}), GT = /* @__PURE__ */ A(({ alpha: a, dotNL: e, dotNV: t }) => {
  const s = a.pow2(), r = e.mul(s.add(s.oneMinus().mul(t.pow2())).sqrt()), n = t.mul(s.add(s.oneMinus().mul(e.pow2())).sqrt());
  return Ct(0.5, r.add(n).max(Aa));
}).setLayout({
  name: "V_GGX_SmithCorrelated",
  type: "float",
  inputs: [
    { name: "alpha", type: "float" },
    { name: "dotNL", type: "float" },
    { name: "dotNV", type: "float" }
  ]
}), kT = /* @__PURE__ */ A(({ alphaT: a, alphaB: e, dotTV: t, dotBV: s, dotTL: r, dotBL: n, dotNV: i, dotNL: o }) => {
  const u = o.mul(N(a.mul(t), e.mul(s), i).length()), l = i.mul(N(a.mul(r), e.mul(n), o).length());
  return Ct(0.5, u.add(l).max(Aa));
}).setLayout({
  name: "V_GGX_SmithCorrelated_Anisotropic",
  type: "float",
  inputs: [
    { name: "alphaT", type: "float", qualifier: "in" },
    { name: "alphaB", type: "float", qualifier: "in" },
    { name: "dotTV", type: "float", qualifier: "in" },
    { name: "dotBV", type: "float", qualifier: "in" },
    { name: "dotTL", type: "float", qualifier: "in" },
    { name: "dotBL", type: "float", qualifier: "in" },
    { name: "dotNV", type: "float", qualifier: "in" },
    { name: "dotNL", type: "float", qualifier: "in" }
  ]
}), VT = /* @__PURE__ */ A(({ alpha: a, dotNH: e }) => {
  const t = a.pow2(), s = e.pow2().mul(t.oneMinus()).oneMinus();
  return t.div(s.pow2()).mul(1 / Math.PI);
}).setLayout({
  name: "D_GGX",
  type: "float",
  inputs: [
    { name: "alpha", type: "float" },
    { name: "dotNH", type: "float" }
  ]
}), $T = /* @__PURE__ */ b(1 / Math.PI), zT = /* @__PURE__ */ A(({ alphaT: a, alphaB: e, dotNH: t, dotTH: s, dotBH: r }) => {
  const n = a.mul(e), i = N(e.mul(s), a.mul(r), n.mul(t)), o = i.dot(i), u = n.div(o);
  return $T.mul(n.mul(u.pow2()));
}).setLayout({
  name: "D_GGX_Anisotropic",
  type: "float",
  inputs: [
    { name: "alphaT", type: "float", qualifier: "in" },
    { name: "alphaB", type: "float", qualifier: "in" },
    { name: "dotNH", type: "float", qualifier: "in" },
    { name: "dotTH", type: "float", qualifier: "in" },
    { name: "dotBH", type: "float", qualifier: "in" }
  ]
}), Id = /* @__PURE__ */ A(({ lightDirection: a, f0: e, f90: t, roughness: s, f: r, normalView: n = Z, USE_IRIDESCENCE: i, USE_ANISOTROPY: o }) => {
  const u = s.pow2(), l = a.add(he).normalize(), c = n.dot(a).clamp(), d = n.dot(he).clamp(), h = n.dot(l).clamp(), f = he.dot(l).clamp();
  let p = qr({ f0: e, f90: t, dotVH: f }), g, m;
  if (Vo(i) && (p = Sa.mix(p, r)), Vo(o)) {
    const y = wn.dot(a), _ = wn.dot(he), v = wn.dot(l), C = er.dot(a), w = er.dot(he), M = er.dot(l);
    g = kT({ alphaT: Ho, alphaB: u, dotTV: _, dotBV: w, dotTL: y, dotBL: C, dotNV: d, dotNL: c }), m = zT({ alphaT: Ho, alphaB: u, dotNH: h, dotTH: v, dotBH: M });
  } else
    g = GT({ alpha: u, dotNL: c, dotNV: d }), m = VT({ alpha: u, dotNH: h });
  return p.mul(g).mul(m);
}), WT = new Uint16Array([
  12469,
  15057,
  12620,
  14925,
  13266,
  14620,
  13807,
  14376,
  14323,
  13990,
  14545,
  13625,
  14713,
  13328,
  14840,
  12882,
  14931,
  12528,
  14996,
  12233,
  15039,
  11829,
  15066,
  11525,
  15080,
  11295,
  15085,
  10976,
  15082,
  10705,
  15073,
  10495,
  13880,
  14564,
  13898,
  14542,
  13977,
  14430,
  14158,
  14124,
  14393,
  13732,
  14556,
  13410,
  14702,
  12996,
  14814,
  12596,
  14891,
  12291,
  14937,
  11834,
  14957,
  11489,
  14958,
  11194,
  14943,
  10803,
  14921,
  10506,
  14893,
  10278,
  14858,
  9960,
  14484,
  14039,
  14487,
  14025,
  14499,
  13941,
  14524,
  13740,
  14574,
  13468,
  14654,
  13106,
  14743,
  12678,
  14818,
  12344,
  14867,
  11893,
  14889,
  11509,
  14893,
  11180,
  14881,
  10751,
  14852,
  10428,
  14812,
  10128,
  14765,
  9754,
  14712,
  9466,
  14764,
  13480,
  14764,
  13475,
  14766,
  13440,
  14766,
  13347,
  14769,
  13070,
  14786,
  12713,
  14816,
  12387,
  14844,
  11957,
  14860,
  11549,
  14868,
  11215,
  14855,
  10751,
  14825,
  10403,
  14782,
  10044,
  14729,
  9651,
  14666,
  9352,
  14599,
  9029,
  14967,
  12835,
  14966,
  12831,
  14963,
  12804,
  14954,
  12723,
  14936,
  12564,
  14917,
  12347,
  14900,
  11958,
  14886,
  11569,
  14878,
  11247,
  14859,
  10765,
  14828,
  10401,
  14784,
  10011,
  14727,
  9600,
  14660,
  9289,
  14586,
  8893,
  14508,
  8533,
  15111,
  12234,
  15110,
  12234,
  15104,
  12216,
  15092,
  12156,
  15067,
  12010,
  15028,
  11776,
  14981,
  11500,
  14942,
  11205,
  14902,
  10752,
  14861,
  10393,
  14812,
  9991,
  14752,
  9570,
  14682,
  9252,
  14603,
  8808,
  14519,
  8445,
  14431,
  8145,
  15209,
  11449,
  15208,
  11451,
  15202,
  11451,
  15190,
  11438,
  15163,
  11384,
  15117,
  11274,
  15055,
  10979,
  14994,
  10648,
  14932,
  10343,
  14871,
  9936,
  14803,
  9532,
  14729,
  9218,
  14645,
  8742,
  14556,
  8381,
  14461,
  8020,
  14365,
  7603,
  15273,
  10603,
  15272,
  10607,
  15267,
  10619,
  15256,
  10631,
  15231,
  10614,
  15182,
  10535,
  15118,
  10389,
  15042,
  10167,
  14963,
  9787,
  14883,
  9447,
  14800,
  9115,
  14710,
  8665,
  14615,
  8318,
  14514,
  7911,
  14411,
  7507,
  14279,
  7198,
  15314,
  9675,
  15313,
  9683,
  15309,
  9712,
  15298,
  9759,
  15277,
  9797,
  15229,
  9773,
  15166,
  9668,
  15084,
  9487,
  14995,
  9274,
  14898,
  8910,
  14800,
  8539,
  14697,
  8234,
  14590,
  7790,
  14479,
  7409,
  14367,
  7067,
  14178,
  6621,
  15337,
  8619,
  15337,
  8631,
  15333,
  8677,
  15325,
  8769,
  15305,
  8871,
  15264,
  8940,
  15202,
  8909,
  15119,
  8775,
  15022,
  8565,
  14916,
  8328,
  14804,
  8009,
  14688,
  7614,
  14569,
  7287,
  14448,
  6888,
  14321,
  6483,
  14088,
  6171,
  15350,
  7402,
  15350,
  7419,
  15347,
  7480,
  15340,
  7613,
  15322,
  7804,
  15287,
  7973,
  15229,
  8057,
  15148,
  8012,
  15046,
  7846,
  14933,
  7611,
  14810,
  7357,
  14682,
  7069,
  14552,
  6656,
  14421,
  6316,
  14251,
  5948,
  14007,
  5528,
  15356,
  5942,
  15356,
  5977,
  15353,
  6119,
  15348,
  6294,
  15332,
  6551,
  15302,
  6824,
  15249,
  7044,
  15171,
  7122,
  15070,
  7050,
  14949,
  6861,
  14818,
  6611,
  14679,
  6349,
  14538,
  6067,
  14398,
  5651,
  14189,
  5311,
  13935,
  4958,
  15359,
  4123,
  15359,
  4153,
  15356,
  4296,
  15353,
  4646,
  15338,
  5160,
  15311,
  5508,
  15263,
  5829,
  15188,
  6042,
  15088,
  6094,
  14966,
  6001,
  14826,
  5796,
  14678,
  5543,
  14527,
  5287,
  14377,
  4985,
  14133,
  4586,
  13869,
  4257,
  15360,
  1563,
  15360,
  1642,
  15358,
  2076,
  15354,
  2636,
  15341,
  3350,
  15317,
  4019,
  15273,
  4429,
  15203,
  4732,
  15105,
  4911,
  14981,
  4932,
  14836,
  4818,
  14679,
  4621,
  14517,
  4386,
  14359,
  4156,
  14083,
  3795,
  13808,
  3437,
  15360,
  122,
  15360,
  137,
  15358,
  285,
  15355,
  636,
  15344,
  1274,
  15322,
  2177,
  15281,
  2765,
  15215,
  3223,
  15120,
  3451,
  14995,
  3569,
  14846,
  3567,
  14681,
  3466,
  14511,
  3305,
  14344,
  3121,
  14037,
  2800,
  13753,
  2467,
  15360,
  0,
  15360,
  1,
  15359,
  21,
  15355,
  89,
  15346,
  253,
  15325,
  479,
  15287,
  796,
  15225,
  1148,
  15133,
  1492,
  15008,
  1749,
  14856,
  1882,
  14685,
  1886,
  14506,
  1783,
  14324,
  1608,
  13996,
  1398,
  13702,
  1183
]);
let mt = null;
const Kn = /* @__PURE__ */ A(({ roughness: a, dotNV: e }) => {
  mt === null && (mt = new fc(WT, 16, 16, Rt, Oe), mt.name = "DFG_LUT", mt.minFilter = Ze, mt.magFilter = Ze, mt.wrapS = Vr, mt.wrapT = Vr, mt.generateMipmaps = !1, mt.needsUpdate = !0);
  const t = I(a, e);
  return ce(mt, t).rg;
}), HT = /* @__PURE__ */ A(({ lightDirection: a, f0: e, f90: t, roughness: s, f: r, USE_IRIDESCENCE: n, USE_ANISOTROPY: i }) => {
  const o = Id({ lightDirection: a, f0: e, f90: t, roughness: s, f: r, USE_IRIDESCENCE: n, USE_ANISOTROPY: i }), u = Z.dot(a).clamp(), l = Z.dot(he).clamp(), c = Kn({ roughness: s, dotNV: l }), d = Kn({ roughness: s, dotNV: u }), h = e.mul(c.x).add(t.mul(c.y)), f = e.mul(d.x).add(t.mul(d.y)), p = c.x.add(c.y), g = d.x.add(d.y), m = b(1).sub(p), y = b(1).sub(g), _ = e.add(e.oneMinus().mul(0.047619)), v = h.mul(f).mul(_).div(b(1).sub(m.mul(y).mul(_).mul(_)).add(Aa)), C = m.mul(y), w = v.mul(C);
  return o.add(w);
}), Od = /* @__PURE__ */ A((a) => {
  const { dotNV: e, specularColor: t, specularF90: s, roughness: r } = a, n = Kn({ dotNV: e, roughness: r });
  return t.mul(n.x).add(s.mul(n.y));
}), Ru = /* @__PURE__ */ A(({ f: a, f90: e, dotVH: t }) => {
  const s = t.oneMinus().saturate(), r = s.mul(s), n = s.mul(r, r).clamp(0, 0.9999);
  return a.sub(N(e).mul(n)).div(n.oneMinus());
}).setLayout({
  name: "Schlick_to_F0",
  type: "vec3",
  inputs: [
    { name: "f", type: "vec3" },
    { name: "f90", type: "float" },
    { name: "dotVH", type: "float" }
  ]
}), qT = /* @__PURE__ */ A(({ roughness: a, dotNH: e }) => {
  const t = a.pow2(), s = b(1).div(t), n = e.pow2().oneMinus().max(78125e-7);
  return b(2).add(s).mul(n.pow(s.mul(0.5))).div(2 * Math.PI);
}).setLayout({
  name: "D_Charlie",
  type: "float",
  inputs: [
    { name: "roughness", type: "float" },
    { name: "dotNH", type: "float" }
  ]
}), XT = /* @__PURE__ */ A(({ dotNV: a, dotNL: e }) => b(1).div(b(4).mul(e.add(a).sub(e.mul(a))))).setLayout({
  name: "V_Neubelt",
  type: "float",
  inputs: [
    { name: "dotNV", type: "float" },
    { name: "dotNL", type: "float" }
  ]
}), KT = /* @__PURE__ */ A(({ lightDirection: a }) => {
  const e = a.add(he).normalize(), t = Z.dot(a).clamp(), s = Z.dot(he).clamp(), r = Z.dot(e).clamp(), n = qT({ roughness: ms, dotNH: r }), i = XT({ dotNV: s, dotNL: t });
  return et.mul(n).mul(i);
}), Au = /* @__PURE__ */ A(({ N: a, V: e, roughness: t }) => {
  const n = 78125e-7, i = a.dot(e).saturate(), o = I(t, i.oneMinus().sqrt());
  return o.assign(o.mul(0.984375).add(n)), o;
}).setLayout({
  name: "LTC_Uv",
  type: "vec2",
  inputs: [
    { name: "N", type: "vec3" },
    { name: "V", type: "vec3" },
    { name: "roughness", type: "float" }
  ]
}), QT = /* @__PURE__ */ A(({ f: a }) => {
  const e = a.length();
  return Be(e.mul(e).add(a.z).div(e.add(1)), 0);
}).setLayout({
  name: "LTC_ClippedSphereFormFactor",
  type: "float",
  inputs: [
    { name: "f", type: "vec3" }
  ]
}), ln = /* @__PURE__ */ A(({ v1: a, v2: e }) => {
  const t = a.dot(e), s = t.abs().toVar(), r = s.mul(0.0145206).add(0.4965155).mul(s).add(0.8543985).toVar(), n = s.add(4.1616724).mul(s).add(3.417594).toVar(), i = r.div(n), o = t.greaterThan(0).select(i, Be(t.mul(t).oneMinus(), 1e-7).inverseSqrt().mul(0.5).sub(i));
  return a.cross(e).mul(o);
}).setLayout({
  name: "LTC_EdgeVectorFormFactor",
  type: "vec3",
  inputs: [
    { name: "v1", type: "vec3" },
    { name: "v2", type: "vec3" }
  ]
}), Oi = /* @__PURE__ */ A(({ N: a, V: e, P: t, mInv: s, p0: r, p1: n, p2: i, p3: o }) => {
  const u = n.sub(r).toVar(), l = o.sub(r).toVar(), c = u.cross(l), d = N().toVar();
  return ue(c.dot(t.sub(r)).greaterThanEqual(0), () => {
    const h = e.sub(a.mul(e.dot(a))).normalize(), f = a.cross(h).negate(), p = s.mul(Me(h, f, a).transpose()).toVar(), g = p.mul(r.sub(t)).normalize().toVar(), m = p.mul(n.sub(t)).normalize().toVar(), y = p.mul(i.sub(t)).normalize().toVar(), _ = p.mul(o.sub(t)).normalize().toVar(), v = N(0).toVar();
    v.addAssign(ln({ v1: g, v2: m })), v.addAssign(ln({ v1: m, v2: y })), v.addAssign(ln({ v1: y, v2: _ })), v.addAssign(ln({ v1: _, v2: g })), d.assign(N(QT({ f: v })));
  }), d;
}).setLayout({
  name: "LTC_Evaluate",
  type: "vec3",
  inputs: [
    { name: "N", type: "vec3" },
    { name: "V", type: "vec3" },
    { name: "P", type: "vec3" },
    { name: "mInv", type: "mat3" },
    { name: "p0", type: "vec3" },
    { name: "p1", type: "vec3" },
    { name: "p2", type: "vec3" },
    { name: "p3", type: "vec3" }
  ]
}), gi = 1 / 6, Gd = (a) => X(gi, X(a, X(a, a.negate().add(3)).sub(3)).add(1)), jo = (a) => X(gi, X(a, X(a, X(3, a).sub(6))).add(4)), kd = (a) => X(gi, X(a, X(a, X(-3, a).add(3)).add(3)).add(1)), Zo = (a) => X(gi, di(a, 3)), vu = (a) => Gd(a).add(jo(a)), Eu = (a) => kd(a).add(Zo(a)), Cu = (a) => We(-1, jo(a).div(Gd(a).add(jo(a)))), wu = (a) => We(1, Zo(a).div(kd(a).add(Zo(a)))), Mu = (a, e, t) => {
  const s = a.uvNode, r = X(s, e.zw).add(0.5), n = vs(r), i = wt(r), o = vu(i.x), u = Eu(i.x), l = Cu(i.x), c = wu(i.x), d = Cu(i.y), h = wu(i.y), f = I(n.x.add(l), n.y.add(d)).sub(0.5).mul(e.xy), p = I(n.x.add(c), n.y.add(d)).sub(0.5).mul(e.xy), g = I(n.x.add(l), n.y.add(h)).sub(0.5).mul(e.xy), m = I(n.x.add(c), n.y.add(h)).sub(0.5).mul(e.xy), y = vu(i.y).mul(We(o.mul(a.sample(f).level(t)), u.mul(a.sample(p).level(t)))), _ = Eu(i.y).mul(We(o.mul(a.sample(g).level(t)), u.mul(a.sample(m).level(t))));
  return y.add(_);
}, YT = /* @__PURE__ */ A(([a, e]) => {
  const t = I(a.size(pe(e))), s = I(a.size(pe(e.add(1)))), r = Ct(1, t), n = Ct(1, s), i = Mu(a, W(r, t), vs(e)), o = Mu(a, W(n, s), va(e));
  return wt(e).mix(i, o);
}), Bu = /* @__PURE__ */ A(([a, e, t, s, r]) => {
  const n = N(Xc(e.negate(), Et(a), Ct(1, s))), i = N(
    zt(r[0].xyz),
    zt(r[1].xyz),
    zt(r[2].xyz)
  );
  return Et(n).mul(t.mul(i));
}).setLayout({
  name: "getVolumeTransmissionRay",
  type: "vec3",
  inputs: [
    { name: "n", type: "vec3" },
    { name: "v", type: "vec3" },
    { name: "thickness", type: "float" },
    { name: "ior", type: "float" },
    { name: "modelMatrix", type: "mat4" }
  ]
}), jT = /* @__PURE__ */ A(([a, e]) => a.mul(os(e.mul(2).sub(2), 0, 1))).setLayout({
  name: "applyIorToRoughness",
  type: "float",
  inputs: [
    { name: "roughness", type: "float" },
    { name: "ior", type: "float" }
  ]
}), ZT = /* @__PURE__ */ Rd(), JT = /* @__PURE__ */ aT(), Fu = /* @__PURE__ */ A(([a, e, t], { material: s }) => {
  const n = (s.side === Ge ? ZT : JT).sample(a), i = $t(Ko.x).mul(jT(e, t));
  return YT(n, i);
}), Uu = /* @__PURE__ */ A(([a, e, t]) => (ue(t.notEqual(0), () => {
  const s = Ic(e).negate().div(t);
  return Lc(s.negate().mul(a));
}), N(1))).setLayout({
  name: "volumeAttenuation",
  type: "vec3",
  inputs: [
    { name: "transmissionDistance", type: "float" },
    { name: "attenuationColor", type: "vec3" },
    { name: "attenuationDistance", type: "float" }
  ]
}), ey = /* @__PURE__ */ A(([a, e, t, s, r, n, i, o, u, l, c, d, h, f, p]) => {
  let g, m;
  if (p) {
    g = W().toVar(), m = N().toVar();
    const w = c.sub(1).mul(p.mul(0.025)), M = N(c.sub(w), c, c.add(w));
    rt({ start: 0, end: 3 }, ({ i: E }) => {
      const U = M.element(E), P = Bu(a, e, d, U, o), $ = i.add(P), Y = l.mul(u.mul(W($, 1))), ie = I(Y.xy.div(Y.w)).toVar();
      ie.addAssign(1), ie.divAssign(2), ie.assign(I(ie.x, ie.y.oneMinus()));
      const se = Fu(ie, t, U);
      g.element(E).assign(se.element(E)), g.a.addAssign(se.a), m.element(E).assign(s.element(E).mul(Uu(zt(P), h, f).element(E)));
    }), g.a.divAssign(3);
  } else {
    const w = Bu(a, e, d, c, o), M = i.add(w), E = l.mul(u.mul(W(M, 1))), U = I(E.xy.div(E.w)).toVar();
    U.addAssign(1), U.divAssign(2), U.assign(I(U.x, U.y.oneMinus())), g = Fu(U, t, c), m = s.mul(Uu(zt(w), h, f));
  }
  const y = m.rgb.mul(g.rgb), _ = a.dot(e).clamp(), v = N(Od({
    // n, v, specularColor, specularF90, roughness
    dotNV: _,
    specularColor: r,
    specularF90: n,
    roughness: t
  })), C = m.r.add(m.g, m.b).div(3);
  return W(v.oneMinus().mul(y), g.a.oneMinus().mul(C).oneMinus());
}), ty = /* @__PURE__ */ Me(
  3.2404542,
  -0.969266,
  0.0556434,
  -1.5371385,
  1.8760108,
  -0.2040259,
  -0.4985314,
  0.041556,
  1.0572252
), sy = (a) => {
  const e = a.sqrt();
  return N(1).add(e).div(N(1).sub(e));
}, Du = (a, e) => a.sub(e).div(a.add(e)).pow2(), ry = (a, e) => {
  const t = a.mul(2 * Math.PI * 1e-9), s = N(54856e-17, 44201e-17, 52481e-17), r = N(1681e3, 1795300, 2208400), n = N(43278e5, 93046e5, 66121e5), i = b(9747e-17 * Math.sqrt(2 * Math.PI * 45282e5)).mul(t.mul(2239900).add(e.x).cos()).mul(t.pow2().mul(-45282e5).exp());
  let o = s.mul(n.mul(2 * Math.PI).sqrt()).mul(r.mul(t).add(e).cos()).mul(t.pow2().negate().mul(n).exp());
  return o = N(o.x.add(i), o.y, o.z).div(10685e-11), ty.mul(o);
}, Pu = /* @__PURE__ */ A(({ outsideIOR: a, eta2: e, cosTheta1: t, thinFilmThickness: s, baseF0: r }) => {
  const n = ne(a, e, Wt(0, 0.03, s)), o = a.div(n).pow2().mul(t.pow2().oneMinus()).oneMinus();
  ue(o.lessThan(0), () => N(1));
  const u = o.sqrt(), l = Du(n, a), c = qr({ f0: l, f90: 1, dotVH: t }), d = c.oneMinus(), h = n.lessThan(a).select(Math.PI, 0), f = b(Math.PI).sub(h), p = sy(r.clamp(0, 0.9999)), g = Du(p, n.toVec3()), m = qr({ f0: g, f90: 1, dotVH: u }), y = N(
    p.x.lessThan(n).select(Math.PI, 0),
    p.y.lessThan(n).select(Math.PI, 0),
    p.z.lessThan(n).select(Math.PI, 0)
  ), _ = n.mul(s, u, 2), v = N(f).add(y), C = c.mul(m).clamp(1e-5, 0.9999), w = C.sqrt(), M = d.pow2().mul(m).div(N(1).sub(C)), U = c.add(M).toVar(), P = M.sub(d).toVar();
  return rt({ start: 1, end: 2, condition: "<=", name: "m" }, ({ m: $ }) => {
    P.mulAssign(w);
    const Y = ry(b($).mul(_), b($).mul(v)).mul(2);
    U.addAssign(P.mul(Y));
  }), U.max(N(0));
}).setLayout({
  name: "evalIridescence",
  type: "vec3",
  inputs: [
    { name: "outsideIOR", type: "float" },
    { name: "eta2", type: "float" },
    { name: "cosTheta1", type: "float" },
    { name: "thinFilmThickness", type: "float" },
    { name: "baseF0", type: "vec3" }
  ]
}), _r = /* @__PURE__ */ A(({ normal: a, viewDir: e, roughness: t }) => {
  const s = a.dot(e).saturate(), r = t.mul(t), n = t.add(0.1).reciprocal(), i = b(-1.9362).add(t.mul(1.0678)).add(r.mul(0.4573)).sub(n.mul(0.8469)), o = b(-0.6014).add(t.mul(0.5538)).sub(r.mul(0.467)).sub(n.mul(0.1255));
  return i.mul(s).add(o).exp().saturate();
}), br = N(0.04), cn = b(1);
class Vd extends pi {
  /**
   * Constructs a new physical lighting model.
   *
   * @param {boolean} [clearcoat=false] - Whether clearcoat is supported or not.
   * @param {boolean} [sheen=false] - Whether sheen is supported or not.
   * @param {boolean} [iridescence=false] - Whether iridescence is supported or not.
   * @param {boolean} [anisotropy=false] - Whether anisotropy is supported or not.
   * @param {boolean} [transmission=false] - Whether transmission is supported or not.
   * @param {boolean} [dispersion=false] - Whether dispersion is supported or not.
   */
  constructor(e = !1, t = !1, s = !1, r = !1, n = !1, i = !1) {
    super(), this.clearcoat = e, this.sheen = t, this.iridescence = s, this.anisotropy = r, this.transmission = n, this.dispersion = i, this.clearcoatRadiance = null, this.clearcoatSpecularDirect = null, this.clearcoatSpecularIndirect = null, this.sheenSpecularDirect = null, this.sheenSpecularIndirect = null, this.iridescenceFresnel = null, this.iridescenceF0 = null, this.iridescenceF0Dielectric = null, this.iridescenceF0Metallic = null;
  }
  /**
   * Depending on what features are requested, the method prepares certain node variables
   * which are later used for lighting computations.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  start(e) {
    if (this.clearcoat === !0 && (this.clearcoatRadiance = N().toVar("clearcoatRadiance"), this.clearcoatSpecularDirect = N().toVar("clearcoatSpecularDirect"), this.clearcoatSpecularIndirect = N().toVar("clearcoatSpecularIndirect")), this.sheen === !0 && (this.sheenSpecularDirect = N().toVar("sheenSpecularDirect"), this.sheenSpecularIndirect = N().toVar("sheenSpecularIndirect")), this.iridescence === !0) {
      const t = Z.dot(he).clamp(), s = Pu({
        outsideIOR: b(1),
        eta2: zo,
        cosTheta1: t,
        thinFilmThickness: Wo,
        baseF0: Ms
      }), r = Pu({
        outsideIOR: b(1),
        eta2: zo,
        cosTheta1: t,
        thinFilmThickness: Wo,
        baseF0: re.rgb
      });
      this.iridescenceFresnel = ne(s, r, es), this.iridescenceF0Dielectric = Ru({ f: s, f90: 1, dotVH: t }), this.iridescenceF0Metallic = Ru({ f: r, f90: 1, dotVH: t }), this.iridescenceF0 = ne(this.iridescenceF0Dielectric, this.iridescenceF0Metallic, es);
    }
    if (this.transmission === !0) {
      const t = ar, s = Bm.sub(ar).normalize(), r = gr, n = e.context;
      n.backdrop = ey(
        r,
        s,
        It,
        Ws,
        Ks,
        Qs,
        // specularF90
        t,
        // positionWorld
        Us,
        // modelMatrix
        qt,
        // viewMatrix
        Ir,
        // projMatrix
        Mn,
        Bc,
        Uc,
        Fc,
        this.dispersion ? Dc : null
      ), n.backdropAlpha = Xo, re.a.mulAssign(ne(1, n.backdrop.a, Xo));
    }
    super.start(e);
  }
  // Fdez-Agüera's "Multiple-Scattering Microfacet Model for Real-Time Image Based Lighting"
  // Approximates multi-scattering in order to preserve energy.
  // http://www.jcgt.org/published/0008/01/03/
  computeMultiscattering(e, t, s, r, n = null) {
    const i = Z.dot(he).clamp(), o = Kn({ roughness: It, dotNV: i }), u = n ? Sa.mix(r, n) : r, l = u.mul(o.x).add(s.mul(o.y)), d = o.x.add(o.y).oneMinus(), h = u.add(u.oneMinus().mul(0.047619)), f = l.mul(h).div(d.mul(h).oneMinus());
    e.addAssign(l), t.addAssign(f.mul(d));
  }
  /**
   * Implements the direct light.
   *
   * @param {Object} lightData - The light data.
   * @param {NodeBuilder} builder - The current node builder.
   */
  direct({ lightDirection: e, lightColor: t, reflectedLight: s }) {
    const n = Z.dot(e).clamp().mul(t).toVar();
    if (this.sheen === !0) {
      this.sheenSpecularDirect.addAssign(n.mul(KT({ lightDirection: e })));
      const i = _r({ normal: Z, viewDir: he, roughness: ms }), o = _r({ normal: Z, viewDir: e, roughness: ms }), u = et.r.max(et.g).max(et.b).mul(i.max(o)).oneMinus();
      n.mulAssign(u);
    }
    if (this.clearcoat === !0) {
      const o = ys.dot(e).clamp().mul(t);
      this.clearcoatSpecularDirect.addAssign(o.mul(Id({ lightDirection: e, f0: br, f90: cn, roughness: Dr, normalView: ys })));
    }
    s.directDiffuse.addAssign(n.mul(ur({ diffuseColor: Ws }))), s.directSpecular.addAssign(n.mul(HT({ lightDirection: e, f0: Ks, f90: 1, roughness: It, f: this.iridescenceFresnel, USE_IRIDESCENCE: this.iridescence, USE_ANISOTROPY: this.anisotropy })));
  }
  /**
   * This method is intended for implementing the direct light term for
   * rect area light nodes.
   *
   * @param {Object} input - The input data.
   * @param {NodeBuilder} builder - The current node builder.
   */
  directRectArea({ lightColor: e, lightPosition: t, halfWidth: s, halfHeight: r, reflectedLight: n, ltc_1: i, ltc_2: o }) {
    const u = t.add(s).sub(r), l = t.sub(s).sub(r), c = t.sub(s).add(r), d = t.add(s).add(r), h = Z, f = he, p = be.toVar(), g = Au({ N: h, V: f, roughness: It }), m = i.sample(g).toVar(), y = o.sample(g).toVar(), _ = Me(
      N(m.x, 0, m.y),
      N(0, 1, 0),
      N(m.z, 0, m.w)
    ).toVar(), v = Ks.mul(y.x).add(Qs.sub(Ks).mul(y.y)).toVar();
    if (n.directSpecular.addAssign(e.mul(v).mul(Oi({ N: h, V: f, P: p, mInv: _, p0: u, p1: l, p2: c, p3: d }))), n.directDiffuse.addAssign(e.mul(Ws).mul(Oi({ N: h, V: f, P: p, mInv: Me(1, 0, 0, 0, 1, 0, 0, 0, 1), p0: u, p1: l, p2: c, p3: d }))), this.clearcoat === !0) {
      const C = ys, w = Au({ N: C, V: f, roughness: Dr }), M = i.sample(w), E = o.sample(w), U = Me(
        N(M.x, 0, M.y),
        N(0, 1, 0),
        N(M.z, 0, M.w)
      ), P = br.mul(E.x).add(cn.sub(br).mul(E.y));
      this.clearcoatSpecularDirect.addAssign(e.mul(P).mul(Oi({ N: C, V: f, P: p, mInv: U, p0: u, p1: l, p2: c, p3: d })));
    }
  }
  /**
   * Implements the indirect lighting.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  indirect(e) {
    this.indirectDiffuse(e), this.indirectSpecular(e), this.ambientOcclusion(e);
  }
  /**
   * Implements the indirect diffuse term.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  indirectDiffuse(e) {
    const { irradiance: t, reflectedLight: s } = e.context, r = t.mul(ur({ diffuseColor: Ws })).toVar();
    if (this.sheen === !0) {
      const n = _r({ normal: Z, viewDir: he, roughness: ms }), i = et.r.max(et.g).max(et.b).mul(n).oneMinus();
      r.mulAssign(i);
    }
    s.indirectDiffuse.addAssign(r);
  }
  /**
   * Implements the indirect specular term.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  indirectSpecular(e) {
    const { radiance: t, iblIrradiance: s, reflectedLight: r } = e.context;
    if (this.sheen === !0 && this.sheenSpecularIndirect.addAssign(s.mul(
      et,
      _r({
        normal: Z,
        viewDir: he,
        roughness: ms
      })
    )), this.clearcoat === !0) {
      const m = ys.dot(he).clamp(), y = Od({
        dotNV: m,
        specularColor: br,
        specularF90: cn,
        roughness: Dr
      });
      this.clearcoatSpecularIndirect.addAssign(this.clearcoatRadiance.mul(y));
    }
    const n = N().toVar("singleScatteringDielectric"), i = N().toVar("multiScatteringDielectric"), o = N().toVar("singleScatteringMetallic"), u = N().toVar("multiScatteringMetallic");
    this.computeMultiscattering(n, i, Qs, Ms, this.iridescenceF0Dielectric), this.computeMultiscattering(o, u, Qs, re.rgb, this.iridescenceF0Metallic);
    const l = ne(n, o, es), c = ne(i, u, es), d = n.add(i), h = Ws.mul(d.oneMinus()), f = s.mul(1 / Math.PI), p = t.mul(l).add(c.mul(f)).toVar(), g = h.mul(f).toVar();
    if (this.sheen === !0) {
      const m = _r({ normal: Z, viewDir: he, roughness: ms }), y = et.r.max(et.g).max(et.b).mul(m).oneMinus();
      p.mulAssign(y), g.mulAssign(y);
    }
    r.indirectSpecular.addAssign(p), r.indirectDiffuse.addAssign(g);
  }
  /**
   * Implements the ambient occlusion term.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  ambientOcclusion(e) {
    const { ambientOcclusion: t, reflectedLight: s } = e.context, n = Z.dot(he).clamp().add(t), i = It.mul(-16).oneMinus().negate().exp2(), o = t.sub(n.pow(i).oneMinus()).clamp();
    this.clearcoat === !0 && this.clearcoatSpecularIndirect.mulAssign(t), this.sheen === !0 && this.sheenSpecularIndirect.mulAssign(t), s.indirectDiffuse.mulAssign(t), s.indirectSpecular.mulAssign(o);
  }
  /**
   * Used for final lighting accumulations depending on the requested features.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  finish({ context: e }) {
    const { outgoingLight: t } = e;
    if (this.clearcoat === !0) {
      const s = ys.dot(he).clamp(), r = qr({
        dotVH: s,
        f0: br,
        f90: cn
      }), n = t.mul($o.mul(r).oneMinus()).add(this.clearcoatSpecularDirect.add(this.clearcoatSpecularIndirect).mul($o));
      t.assign(n);
    }
    if (this.sheen === !0) {
      const s = t.add(this.sheenSpecularDirect, this.sheenSpecularIndirect.mul(1 / Math.PI));
      t.assign(s);
    }
  }
}
const Lu = /* @__PURE__ */ b(1), Jo = /* @__PURE__ */ b(-2), dn = /* @__PURE__ */ b(0.8), Gi = /* @__PURE__ */ b(-1), hn = /* @__PURE__ */ b(0.4), ki = /* @__PURE__ */ b(2), fn = /* @__PURE__ */ b(0.305), Vi = /* @__PURE__ */ b(3), Iu = /* @__PURE__ */ b(0.21), ny = /* @__PURE__ */ b(4), Ou = /* @__PURE__ */ b(4), iy = /* @__PURE__ */ b(16), oy = /* @__PURE__ */ A(([a]) => {
  const e = N(Xe(a)).toVar(), t = b(-1).toVar();
  return ue(e.x.greaterThan(e.z), () => {
    ue(e.x.greaterThan(e.y), () => {
      t.assign(Ye(a.x.greaterThan(0), 0, 3));
    }).Else(() => {
      t.assign(Ye(a.y.greaterThan(0), 1, 4));
    });
  }).Else(() => {
    ue(e.z.greaterThan(e.y), () => {
      t.assign(Ye(a.z.greaterThan(0), 2, 5));
    }).Else(() => {
      t.assign(Ye(a.y.greaterThan(0), 1, 4));
    });
  }), t;
}).setLayout({
  name: "getFace",
  type: "float",
  inputs: [
    { name: "direction", type: "vec3" }
  ]
}), ay = /* @__PURE__ */ A(([a, e]) => {
  const t = I().toVar();
  return ue(e.equal(0), () => {
    t.assign(I(a.z, a.y).div(Xe(a.x)));
  }).ElseIf(e.equal(1), () => {
    t.assign(I(a.x.negate(), a.z.negate()).div(Xe(a.y)));
  }).ElseIf(e.equal(2), () => {
    t.assign(I(a.x.negate(), a.y).div(Xe(a.z)));
  }).ElseIf(e.equal(3), () => {
    t.assign(I(a.z.negate(), a.y).div(Xe(a.x)));
  }).ElseIf(e.equal(4), () => {
    t.assign(I(a.x.negate(), a.z).div(Xe(a.y)));
  }).Else(() => {
    t.assign(I(a.x, a.y).div(Xe(a.z)));
  }), X(0.5, t.add(1));
}).setLayout({
  name: "getUV",
  type: "vec2",
  inputs: [
    { name: "direction", type: "vec3" },
    { name: "face", type: "float" }
  ]
}), uy = /* @__PURE__ */ A(([a]) => {
  const e = b(0).toVar();
  return ue(a.greaterThanEqual(dn), () => {
    e.assign(Lu.sub(a).mul(Gi.sub(Jo)).div(Lu.sub(dn)).add(Jo));
  }).ElseIf(a.greaterThanEqual(hn), () => {
    e.assign(dn.sub(a).mul(ki.sub(Gi)).div(dn.sub(hn)).add(Gi));
  }).ElseIf(a.greaterThanEqual(fn), () => {
    e.assign(hn.sub(a).mul(Vi.sub(ki)).div(hn.sub(fn)).add(ki));
  }).ElseIf(a.greaterThanEqual(Iu), () => {
    e.assign(fn.sub(a).mul(ny.sub(Vi)).div(fn.sub(Iu)).add(Vi));
  }).Else(() => {
    e.assign(b(-2).mul($t(X(1.16, a))));
  }), e;
}).setLayout({
  name: "roughnessToMip",
  type: "float",
  inputs: [
    { name: "roughness", type: "float" }
  ]
}), ly = /* @__PURE__ */ A(([a, e]) => {
  const t = a.toVar();
  t.assign(X(2, t).sub(1));
  const s = N(t, 1).toVar();
  return ue(e.equal(0), () => {
    s.assign(s.zyx);
  }).ElseIf(e.equal(1), () => {
    s.assign(s.xzy), s.xz.mulAssign(-1);
  }).ElseIf(e.equal(2), () => {
    s.x.mulAssign(-1);
  }).ElseIf(e.equal(3), () => {
    s.assign(s.zyx), s.xz.mulAssign(-1);
  }).ElseIf(e.equal(4), () => {
    s.assign(s.xzy), s.xy.mulAssign(-1);
  }).ElseIf(e.equal(5), () => {
    s.z.mulAssign(-1);
  }), s;
}).setLayout({
  name: "getDirection",
  type: "vec3",
  inputs: [
    { name: "uv", type: "vec2" },
    { name: "face", type: "float" }
  ]
}), cy = /* @__PURE__ */ A(([a, e, t, s, r, n]) => {
  const i = b(t), o = N(e), u = os(uy(i), Jo, n), l = wt(u), c = vs(u), d = N(Xr(a, o, c, s, r, n)).toVar();
  return ue(l.notEqual(0), () => {
    const h = N(Xr(a, o, c.add(1), s, r, n)).toVar();
    d.assign(ne(d, h, l));
  }), d;
}), Xr = /* @__PURE__ */ A(([a, e, t, s, r, n]) => {
  const i = b(t).toVar(), o = N(e), u = b(oy(o)).toVar(), l = b(Be(Ou.sub(i), 0)).toVar();
  i.assign(Be(i, Ou));
  const c = b(zr(i)).toVar(), d = I(ay(o, u).mul(c.sub(2)).add(1)).toVar();
  return ue(u.greaterThan(2), () => {
    d.y.addAssign(c), u.subAssign(3);
  }), d.x.addAssign(u.mul(c)), d.x.addAssign(l.mul(X(3, iy))), d.y.addAssign(X(4, zr(n).sub(c))), d.x.mulAssign(s), d.y.mulAssign(r), a.sample(d).grad(I(), I());
}), $i = /* @__PURE__ */ A(({ envMap: a, mipInt: e, outputDirection: t, theta: s, axis: r, CUBEUV_TEXEL_WIDTH: n, CUBEUV_TEXEL_HEIGHT: i, CUBEUV_MAX_MIP: o }) => {
  const u = _t(s), l = t.mul(u).add(r.cross(t).mul(qe(s))).add(r.mul(r.dot(t).mul(u.oneMinus())));
  return Xr(a, l, e, n, i, o);
}), dy = /* @__PURE__ */ A(({ n: a, latitudinal: e, poleAxis: t, outputDirection: s, weights: r, samples: n, dTheta: i, mipInt: o, envMap: u, CUBEUV_TEXEL_WIDTH: l, CUBEUV_TEXEL_HEIGHT: c, CUBEUV_MAX_MIP: d }) => {
  const h = N(Ye(e, t, Fs(t, s))).toVar();
  ue(h.equal(N(0)), () => {
    h.assign(N(s.z, 0, s.x.negate()));
  }), h.assign(Et(h));
  const f = N().toVar();
  return f.addAssign(r.element(0).mul($i({ theta: 0, axis: h, outputDirection: s, mipInt: o, envMap: u, CUBEUV_TEXEL_WIDTH: l, CUBEUV_TEXEL_HEIGHT: c, CUBEUV_MAX_MIP: d }))), rt({ start: pe(1), end: a }, ({ i: p }) => {
    ue(p.greaterThanEqual(n), () => {
      Zx();
    });
    const g = b(i.mul(b(p))).toVar();
    f.addAssign(r.element(p).mul($i({ theta: g.mul(-1), axis: h, outputDirection: s, mipInt: o, envMap: u, CUBEUV_TEXEL_WIDTH: l, CUBEUV_TEXEL_HEIGHT: c, CUBEUV_MAX_MIP: d }))), f.addAssign(r.element(p).mul($i({ theta: g, axis: h, outputDirection: s, mipInt: o, envMap: u, CUBEUV_TEXEL_WIDTH: l, CUBEUV_TEXEL_HEIGHT: c, CUBEUV_MAX_MIP: d })));
  }), W(f, 1);
}), hy = /* @__PURE__ */ A(([a]) => {
  const e = Q(a).toVar();
  return e.assign(e.shiftLeft(Q(16)).bitOr(e.shiftRight(Q(16)))), e.assign(e.bitAnd(Q(1431655765)).shiftLeft(Q(1)).bitOr(e.bitAnd(Q(2863311530)).shiftRight(Q(1)))), e.assign(e.bitAnd(Q(858993459)).shiftLeft(Q(2)).bitOr(e.bitAnd(Q(3435973836)).shiftRight(Q(2)))), e.assign(e.bitAnd(Q(252645135)).shiftLeft(Q(4)).bitOr(e.bitAnd(Q(4042322160)).shiftRight(Q(4)))), e.assign(e.bitAnd(Q(16711935)).shiftLeft(Q(8)).bitOr(e.bitAnd(Q(4278255360)).shiftRight(Q(8)))), b(e).mul(23283064365386963e-26);
}), fy = /* @__PURE__ */ A(([a, e]) => I(b(a).div(b(e)), hy(a))), py = /* @__PURE__ */ A(([a, e, t]) => {
  const s = t.mul(t).toConst(), r = N(1, 0, 0).toConst(), n = Fs(e, r).toConst(), i = ns(a.x).toConst(), o = X(2, 3.14159265359).mul(a.y).toConst(), u = i.mul(_t(o)).toConst(), l = i.mul(qe(o)).toVar(), c = X(0.5, e.z.add(1)).toConst();
  l.assign(c.oneMinus().mul(ns(u.mul(u).oneMinus())).add(c.mul(l)));
  const d = r.mul(u).add(n.mul(l)).add(e.mul(ns(Be(0, u.mul(u).add(l.mul(l)).oneMinus()))));
  return Et(N(s.mul(d.x), s.mul(d.y), Be(0, d.z)));
}), gy = /* @__PURE__ */ A(({ roughness: a, mipInt: e, envMap: t, N_immutable: s, GGX_SAMPLES: r, CUBEUV_TEXEL_WIDTH: n, CUBEUV_TEXEL_HEIGHT: i, CUBEUV_MAX_MIP: o }) => {
  const u = N(s).toVar(), l = N(0).toVar(), c = b(0).toVar();
  return ue(a.lessThan(1e-3), () => {
    l.assign(Xr(t, u, e, n, i, o));
  }).Else(() => {
    const d = Ye(Xe(u.z).lessThan(0.999), N(0, 0, 1), N(1, 0, 0)), h = Et(Fs(d, u)).toVar(), f = Fs(u, h).toVar();
    rt({ start: Q(0), end: r }, ({ i: p }) => {
      const g = fy(p, r), m = py(g, N(0, 0, 1), a), y = Et(h.mul(m.x).add(f.mul(m.y)).add(u.mul(m.z))), _ = Et(y.mul(Bs(u, y).mul(2)).sub(u)), v = Be(Bs(u, _), 0);
      ue(v.greaterThan(0), () => {
        const C = Xr(t, _, e, n, i, o);
        l.addAssign(C.mul(v)), c.addAssign(v);
      });
    }), ue(c.greaterThan(0), () => {
      l.assign(l.div(c));
    });
  }), W(l, 1);
}), ts = 4, Gu = [0.125, 0.215, 0.35, 0.446, 0.526, 0.582], _s = 20, my = 512, Sr = /* @__PURE__ */ new kl(-1, 1, 1, -1, 0, 1), xy = /* @__PURE__ */ new Pn(90, 1), ku = /* @__PURE__ */ new Ht();
let zi = null, Wi = 0, Hi = 0;
const Ty = /* @__PURE__ */ new H(), Qn = /* @__PURE__ */ new WeakMap(), yy = [
  3,
  1,
  5,
  0,
  4,
  2
], qi = /* @__PURE__ */ ly(hr(), as("faceIndex")).normalize(), mi = /* @__PURE__ */ N(qi.x, qi.y, qi.z);
class _y {
  /**
   * Constructs a new PMREM generator.
   *
   * @param {Renderer} renderer - The renderer.
   */
  constructor(e) {
    this._renderer = e, this._pingPongRenderTarget = null, this._lodMax = 0, this._cubeSize = 0, this._sizeLods = [], this._sigmas = [], this._lodMeshes = [], this._blurMaterial = null, this._ggxMaterial = null, this._cubemapMaterial = null, this._equirectMaterial = null, this._backgroundBox = null;
  }
  get _hasInitialized() {
    return this._renderer.hasInitialized();
  }
  /**
   * Generates a PMREM from a supplied Scene, which can be faster than using an
   * image if networking bandwidth is low. Optional sigma specifies a blur radius
   * in radians to be applied to the scene before PMREM generation. Optional near
   * and far planes ensure the scene is rendered in its entirety.
   *
   * @param {Scene} scene - The scene to be captured.
   * @param {number} [sigma=0] - The blur radius in radians.
   * @param {number} [near=0.1] - The near plane distance.
   * @param {number} [far=100] - The far plane distance.
   * @param {Object} [options={}] - The configuration options.
   * @param {number} [options.size=256] - The texture size of the PMREM.
   * @param {Vector3} [options.renderTarget=origin] - The position of the internal cube camera that renders the scene.
   * @param {?RenderTarget} [options.renderTarget=null] - The render target to use.
   * @return {RenderTarget} The resulting PMREM.
   * @see {@link PMREMGenerator#fromScene}
   */
  fromScene(e, t = 0, s = 0.1, r = 100, n = {}) {
    const {
      size: i = 256,
      position: o = Ty,
      renderTarget: u = null
    } = n;
    if (this._setSize(i), this._hasInitialized === !1) {
      L('PMREMGenerator: ".fromScene()" called before the backend is initialized. Try using "await renderer.init()" instead.');
      const c = u || this._allocateTarget();
      return n.renderTarget = c, this.fromSceneAsync(e, t, s, r, n), c;
    }
    zi = this._renderer.getRenderTarget(), Wi = this._renderer.getActiveCubeFace(), Hi = this._renderer.getActiveMipmapLevel();
    const l = u || this._allocateTarget();
    return l.depthBuffer = !0, this._init(l), this._sceneToCubeUV(e, s, r, l, o), t > 0 && this._blur(l, 0, 0, t), this._applyPMREM(l), this._cleanup(l), l;
  }
  /**
   * Generates a PMREM from a supplied Scene, which can be faster than using an
   * image if networking bandwidth is low. Optional sigma specifies a blur radius
   * in radians to be applied to the scene before PMREM generation. Optional near
   * and far planes ensure the scene is rendered in its entirety (the cubeCamera
   * is placed at the origin).
   *
   * @deprecated
   * @param {Scene} scene - The scene to be captured.
   * @param {number} [sigma=0] - The blur radius in radians.
   * @param {number} [near=0.1] - The near plane distance.
   * @param {number} [far=100] - The far plane distance.
   * @param {Object} [options={}] - The configuration options.
   * @param {number} [options.size=256] - The texture size of the PMREM.
   * @param {Vector3} [options.position=origin] - The position of the internal cube camera that renders the scene.
   * @param {?RenderTarget} [options.renderTarget=null] - The render target to use.
   * @return {Promise<RenderTarget>} A Promise that resolve with the PMREM when the generation has been finished.
   * @see {@link PMREMGenerator#fromScene}
   */
  async fromSceneAsync(e, t = 0, s = 0.1, r = 100, n = {}) {
    return Te('PMREMGenerator: ".fromSceneAsync()" is deprecated. Use "await renderer.init()" instead.'), await this._renderer.init(), this.fromScene(e, t, s, r, n);
  }
  /**
   * Generates a PMREM from an equirectangular texture, which can be either LDR
   * or HDR. The ideal input image size is 1k (1024 x 512),
   * as this matches best with the 256 x 256 cubemap output.
   *
   * @param {Texture} equirectangular - The equirectangular texture to be converted.
   * @param {?RenderTarget} [renderTarget=null] - The render target to use.
   * @return {RenderTarget} The resulting PMREM.
   * @see {@link PMREMGenerator#fromEquirectangularAsync}
   */
  fromEquirectangular(e, t = null) {
    if (this._hasInitialized === !1) {
      L('PMREMGenerator: .fromEquirectangular() called before the backend is initialized. Try using "await renderer.init()" instead.'), this._setSizeFromTexture(e);
      const s = t || this._allocateTarget();
      return this.fromEquirectangularAsync(e, s), s;
    }
    return this._fromTexture(e, t);
  }
  /**
   * Generates a PMREM from an equirectangular texture, which can be either LDR
   * or HDR. The ideal input image size is 1k (1024 x 512),
   * as this matches best with the 256 x 256 cubemap output.
   *
   * @deprecated
   * @param {Texture} equirectangular - The equirectangular texture to be converted.
   * @param {?RenderTarget} [renderTarget=null] - The render target to use.
   * @return {Promise<RenderTarget>} The resulting PMREM.
   * @see {@link PMREMGenerator#fromEquirectangular}
   */
  async fromEquirectangularAsync(e, t = null) {
    return Te('PMREMGenerator: ".fromEquirectangularAsync()" is deprecated. Use "await renderer.init()" instead.'), await this._renderer.init(), this._fromTexture(e, t);
  }
  /**
   * Generates a PMREM from an cubemap texture, which can be either LDR
   * or HDR. The ideal input cube size is 256 x 256,
   * as this matches best with the 256 x 256 cubemap output.
   *
   * @param {Texture} cubemap - The cubemap texture to be converted.
   * @param {?RenderTarget} [renderTarget=null] - The render target to use.
   * @return {RenderTarget} The resulting PMREM.
   * @see {@link PMREMGenerator#fromCubemapAsync}
   */
  fromCubemap(e, t = null) {
    if (this._hasInitialized === !1) {
      L("PMREMGenerator: .fromCubemap() called before the backend is initialized. Try using .fromCubemapAsync() instead."), this._setSizeFromTexture(e);
      const s = t || this._allocateTarget();
      return this.fromCubemapAsync(e, t), s;
    }
    return this._fromTexture(e, t);
  }
  /**
   * Generates a PMREM from an cubemap texture, which can be either LDR
   * or HDR. The ideal input cube size is 256 x 256,
   * with the 256 x 256 cubemap output.
   *
   * @deprecated
   * @param {Texture} cubemap - The cubemap texture to be converted.
   * @param {?RenderTarget} [renderTarget=null] - The render target to use.
   * @return {Promise<RenderTarget>} The resulting PMREM.
   * @see {@link PMREMGenerator#fromCubemap}
   */
  async fromCubemapAsync(e, t = null) {
    return Te('PMREMGenerator: ".fromCubemapAsync()" is deprecated. Use "await renderer.init()" instead.'), await this._renderer.init(), this._fromTexture(e, t);
  }
  /**
   * Pre-compiles the cubemap shader. You can get faster start-up by invoking this method during
   * your texture's network fetch for increased concurrency.
   *
   * @returns {Promise}
   */
  async compileCubemapShader() {
    this._cubemapMaterial === null && (this._cubemapMaterial = $u(), await this._compileMaterial(this._cubemapMaterial));
  }
  /**
   * Pre-compiles the equirectangular shader. You can get faster start-up by invoking this method during
   * your texture's network fetch for increased concurrency.
   *
   * @returns {Promise}
   */
  async compileEquirectangularShader() {
    this._equirectMaterial === null && (this._equirectMaterial = zu(), await this._compileMaterial(this._equirectMaterial));
  }
  /**
   * Disposes of the PMREMGenerator's internal memory. Note that PMREMGenerator is a static class,
   * so you should not need more than one PMREMGenerator object. If you do, calling dispose() on
   * one of them will cause any others to also become unusable.
   */
  dispose() {
    this._dispose(), this._cubemapMaterial !== null && this._cubemapMaterial.dispose(), this._equirectMaterial !== null && this._equirectMaterial.dispose(), this._backgroundBox !== null && (this._backgroundBox.geometry.dispose(), this._backgroundBox.material.dispose());
  }
  // private interface
  _setSizeFromTexture(e) {
    e.mapping === Vn || e.mapping === $n ? this._setSize(e.image.length === 0 ? 16 : e.image[0].width || e.image[0].image.width) : this._setSize(e.image.width / 4);
  }
  _setSize(e) {
    this._lodMax = Math.floor(Math.log2(e)), this._cubeSize = Math.pow(2, this._lodMax);
  }
  _dispose() {
    this._blurMaterial !== null && this._blurMaterial.dispose(), this._ggxMaterial !== null && this._ggxMaterial.dispose(), this._pingPongRenderTarget !== null && this._pingPongRenderTarget.dispose();
    for (let e = 0; e < this._lodMeshes.length; e++)
      this._lodMeshes[e].geometry.dispose();
  }
  _cleanup(e) {
    this._renderer.setRenderTarget(zi, Wi, Hi), e.scissorTest = !1, this._setViewport(e, 0, 0, e.width, e.height);
  }
  _fromTexture(e, t) {
    this._setSizeFromTexture(e), zi = this._renderer.getRenderTarget(), Wi = this._renderer.getActiveCubeFace(), Hi = this._renderer.getActiveMipmapLevel();
    const s = t || this._allocateTarget();
    return this._init(s), this._textureToCubeUV(e, s), this._applyPMREM(s), this._cleanup(s), s;
  }
  _allocateTarget() {
    const e = 3 * Math.max(this._cubeSize, 112), t = 4 * this._cubeSize;
    return Vu(e, t);
  }
  _init(e) {
    if (this._pingPongRenderTarget === null || this._pingPongRenderTarget.width !== e.width || this._pingPongRenderTarget.height !== e.height) {
      this._pingPongRenderTarget !== null && this._dispose(), this._pingPongRenderTarget = Vu(e.width, e.height);
      const { _lodMax: t } = this;
      ({ lodMeshes: this._lodMeshes, sizeLods: this._sizeLods, sigmas: this._sigmas } = by(t)), this._blurMaterial = Sy(t, e.width, e.height), this._ggxMaterial = Ny(t, e.width, e.height);
    }
  }
  async _compileMaterial(e) {
    const t = new is(new ha(), e);
    await this._renderer.compile(t, Sr);
  }
  _sceneToCubeUV(e, t, s, r, n) {
    const i = xy;
    i.near = t, i.far = s;
    const o = [1, 1, 1, 1, -1, 1], u = [1, -1, 1, -1, 1, -1], l = this._renderer, c = l.autoClear;
    l.getClearColor(ku), l.autoClear = !1, this._backgroundBox === null && (this._backgroundBox = new is(
      new xc(),
      new gs({
        name: "PMREM.Background",
        side: Ge,
        depthWrite: !1,
        depthTest: !1
      })
    ));
    const d = this._backgroundBox, h = d.material;
    let f = !1;
    const p = e.background;
    p ? p.isColor && (h.color.copy(p), e.background = null, f = !0) : (h.color.copy(ku), f = !0), l.setRenderTarget(r), l.clear(), f && l.render(d, i);
    for (let g = 0; g < 6; g++) {
      const m = g % 3;
      m === 0 ? (i.up.set(0, o[g], 0), i.position.set(n.x, n.y, n.z), i.lookAt(n.x + u[g], n.y, n.z)) : m === 1 ? (i.up.set(0, 0, o[g]), i.position.set(n.x, n.y, n.z), i.lookAt(n.x, n.y + u[g], n.z)) : (i.up.set(0, o[g], 0), i.position.set(n.x, n.y, n.z), i.lookAt(n.x, n.y, n.z + u[g]));
      const y = this._cubeSize;
      this._setViewport(r, m * y, g > 2 ? y : 0, y, y), l.render(e, i);
    }
    l.autoClear = c, e.background = p;
  }
  _textureToCubeUV(e, t) {
    const s = this._renderer, r = e.mapping === Vn || e.mapping === $n;
    r ? this._cubemapMaterial === null && (this._cubemapMaterial = $u(e)) : this._equirectMaterial === null && (this._equirectMaterial = zu(e));
    const n = r ? this._cubemapMaterial : this._equirectMaterial;
    n.fragmentNode.value = e;
    const i = this._lodMeshes[0];
    i.material = n;
    const o = this._cubeSize;
    this._setViewport(t, 0, 0, 3 * o, 2 * o), s.setRenderTarget(t), s.render(i, Sr);
  }
  _applyPMREM(e) {
    const t = this._renderer, s = t.autoClear;
    t.autoClear = !1;
    const r = this._lodMeshes.length;
    for (let n = 1; n < r; n++)
      this._applyGGXFilter(e, n - 1, n);
    t.autoClear = s;
  }
  /**
   * Applies GGX VNDF importance sampling filter to generate a prefiltered environment map.
   * Uses Monte Carlo integration with VNDF importance sampling to accurately represent the
   * GGX BRDF for physically-based rendering. Reads from the previous LOD level and
   * applies incremental roughness filtering to avoid over-blurring.
   *
   * @private
   * @param {RenderTarget} cubeUVRenderTarget
   * @param {number} lodIn - Source LOD level to read from
   * @param {number} lodOut - Target LOD level to write to
   */
  _applyGGXFilter(e, t, s) {
    const r = this._renderer, n = this._pingPongRenderTarget, i = this._ggxMaterial, o = this._lodMeshes[s];
    o.material = i;
    const u = Qn.get(i), l = s / (this._lodMeshes.length - 1), c = t / (this._lodMeshes.length - 1), d = Math.sqrt(l * l - c * c), h = 0 + l * 1.25, f = d * h, { _lodMax: p } = this, g = this._sizeLods[s], m = 3 * g * (s > p - ts ? s - p + ts : 0), y = 4 * (this._cubeSize - g);
    e.texture.frame = (e.texture.frame || 0) + 1, u.envMap.value = e.texture, u.roughness.value = f, u.mipInt.value = p - t, this._setViewport(n, m, y, 3 * g, 2 * g), r.setRenderTarget(n), r.render(o, Sr), n.texture.frame = (n.texture.frame || 0) + 1, u.envMap.value = n.texture, u.roughness.value = 0, u.mipInt.value = p - s, this._setViewport(e, m, y, 3 * g, 2 * g), r.setRenderTarget(e), r.render(o, Sr);
  }
  /**
   * This is a two-pass Gaussian blur for a cubemap. Normally this is done
   * vertically and horizontally, but this breaks down on a cube. Here we apply
   * the blur latitudinally (around the poles), and then longitudinally (towards
   * the poles) to approximate the orthogonally-separable blur. It is least
   * accurate at the poles, but still does a decent job.
   *
   * Used for initial scene blur in fromScene() method when sigma > 0.
   *
   * @private
   * @param {RenderTarget} cubeUVRenderTarget - The cubemap render target.
   * @param {number} lodIn - The input level-of-detail.
   * @param {number} lodOut - The output level-of-detail.
   * @param {number} sigma - The blur radius in radians.
   * @param {Vector3} [poleAxis] - The pole axis.
   */
  _blur(e, t, s, r, n) {
    const i = this._pingPongRenderTarget;
    this._halfBlur(
      e,
      i,
      t,
      s,
      r,
      "latitudinal",
      n
    ), this._halfBlur(
      i,
      e,
      s,
      s,
      r,
      "longitudinal",
      n
    );
  }
  _halfBlur(e, t, s, r, n, i, o) {
    const u = this._renderer, l = this._blurMaterial;
    i !== "latitudinal" && i !== "longitudinal" && F("blur direction must be either latitudinal or longitudinal!");
    const c = 3, d = this._lodMeshes[r];
    d.material = l;
    const h = Qn.get(l), f = this._sizeLods[s] - 1, p = isFinite(n) ? Math.PI / (2 * f) : 2 * Math.PI / (2 * _s - 1), g = n / p, m = isFinite(n) ? 1 + Math.floor(c * g) : _s;
    m > _s && L(`sigmaRadians, ${n}, is too large and will clip, as it requested ${m} samples when the maximum is set to ${_s}`);
    const y = [];
    let _ = 0;
    for (let E = 0; E < _s; ++E) {
      const U = E / g, P = Math.exp(-U * U / 2);
      y.push(P), E === 0 ? _ += P : E < m && (_ += 2 * P);
    }
    for (let E = 0; E < y.length; E++)
      y[E] = y[E] / _;
    e.texture.frame = (e.texture.frame || 0) + 1, h.envMap.value = e.texture, h.samples.value = m, h.weights.array = y, h.latitudinal.value = i === "latitudinal" ? 1 : 0, o && (h.poleAxis.value = o);
    const { _lodMax: v } = this;
    h.dTheta.value = p, h.mipInt.value = v - s;
    const C = this._sizeLods[r], w = 3 * C * (r > v - ts ? r - v + ts : 0), M = 4 * (this._cubeSize - C);
    this._setViewport(t, w, M, 3 * C, 2 * C), u.setRenderTarget(t), u.render(d, Sr);
  }
  _setViewport(e, t, s, r, n) {
    this._renderer.isWebGLRenderer ? (e.viewport.set(t, e.height - n - s, r, n), e.scissor.set(t, e.height - n - s, r, n)) : (e.viewport.set(t, s, r, n), e.scissor.set(t, s, r, n));
  }
}
function by(a) {
  const e = [], t = [], s = [];
  let r = a;
  const n = a - ts + 1 + Gu.length;
  for (let i = 0; i < n; i++) {
    const o = Math.pow(2, r);
    e.push(o);
    let u = 1 / o;
    i > a - ts ? u = Gu[i - a + ts - 1] : i === 0 && (u = 0), t.push(u);
    const l = 1 / (o - 2), c = -l, d = 1 + l, h = [c, c, d, c, d, d, c, c, d, d, c, d], f = 6, p = 6, g = 3, m = 2, y = 1, _ = new Float32Array(g * p * f), v = new Float32Array(m * p * f), C = new Float32Array(y * p * f);
    for (let M = 0; M < f; M++) {
      const E = M % 3 * 2 / 3 - 1, U = M > 2 ? 0 : -1, P = [
        E,
        U,
        0,
        E + 2 / 3,
        U,
        0,
        E + 2 / 3,
        U + 1,
        0,
        E,
        U,
        0,
        E + 2 / 3,
        U + 1,
        0,
        E,
        U + 1,
        0
      ], $ = yy[M];
      _.set(P, g * p * $), v.set(h, m * p * $);
      const Y = [$, $, $, $, $, $];
      C.set(Y, y * p * $);
    }
    const w = new ha();
    w.setAttribute("position", new Ri(_, g)), w.setAttribute("uv", new Ri(v, m)), w.setAttribute("faceIndex", new Ri(C, y)), s.push(new is(w, null)), r > ts && r--;
  }
  return { lodMeshes: s, sizeLods: e, sigmas: t };
}
function Vu(a, e) {
  const t = {
    magFilter: Ze,
    minFilter: Ze,
    generateMipmaps: !1,
    type: Oe,
    format: ct,
    colorSpace: $f
    //depthBuffer: false
  }, s = new Ds(a, e, t);
  return s.texture.mapping = fo, s.texture.name = "PMREM.cubeUv", s.texture.isPMREMTexture = !0, s.scissorTest = !0, s;
}
function xi(a) {
  const e = new Ne();
  return e.depthTest = !1, e.depthWrite = !1, e.blending = ss, e.name = `PMREM_${a}`, e;
}
function Sy(a, e, t) {
  const s = st(new Array(_s).fill(0)), r = k(new H(0, 1, 0)), n = k(0), i = b(_s), o = k(0), u = k(1), l = ce(), c = k(0), d = b(1 / e), h = b(1 / t), f = b(a), p = {
    n: i,
    latitudinal: o,
    weights: s,
    poleAxis: r,
    outputDirection: mi,
    dTheta: n,
    samples: u,
    envMap: l,
    mipInt: c,
    CUBEUV_TEXEL_WIDTH: d,
    CUBEUV_TEXEL_HEIGHT: h,
    CUBEUV_MAX_MIP: f
  }, g = xi("blur");
  return g.fragmentNode = dy({ ...p, latitudinal: o.equal(1) }), Qn.set(g, p), g;
}
function Ny(a, e, t) {
  const s = ce(), r = k(0), n = k(0), i = b(1 / e), o = b(1 / t), u = b(a), l = {
    envMap: s,
    roughness: r,
    mipInt: n,
    CUBEUV_TEXEL_WIDTH: i,
    CUBEUV_TEXEL_HEIGHT: o,
    CUBEUV_MAX_MIP: u
  }, c = xi("ggx");
  return c.fragmentNode = gy({
    ...l,
    N_immutable: mi,
    GGX_SAMPLES: Q(my)
  }), Qn.set(c, l), c;
}
function $u(a) {
  const e = xi("cubemap");
  return e.fragmentNode = Ve(a, mi), e;
}
function zu(a) {
  const e = xi("equirect");
  return e.fragmentNode = ce(a, Md(mi), 0), e;
}
const Wu = /* @__PURE__ */ new WeakMap();
function Ry(a) {
  const e = Math.log2(a) - 2, t = 1 / a;
  return { texelWidth: 1 / (3 * Math.max(Math.pow(2, e), 112)), texelHeight: t, maxMip: e };
}
function Ay(a, e, t) {
  const s = vy(e);
  let r = s.get(a);
  if ((r !== void 0 ? r.pmremVersion : -1) !== a.pmremVersion) {
    const i = a.image;
    if (a.isCubeTexture)
      if (Cy(i))
        r = t.fromCubemap(a, r);
      else
        return null;
    else if (wy(i))
      r = t.fromEquirectangular(a, r);
    else
      return null;
    r.pmremVersion = a.pmremVersion, s.set(a, r);
  }
  return r.texture;
}
function vy(a) {
  let e = Wu.get(a);
  return e === void 0 && (e = /* @__PURE__ */ new WeakMap(), Wu.set(a, e)), e;
}
class Ey extends Re {
  static get type() {
    return "PMREMNode";
  }
  /**
   * Constructs a new function overloading node.
   *
   * @param {Texture} value - The input texture.
   * @param {Node<vec2>} [uvNode=null] - The uv node.
   * @param {Node<float>} [levelNode=null] - The level node.
   */
  constructor(e, t = null, s = null) {
    super("vec3"), this._value = e, this._pmrem = null, this.uvNode = t, this.levelNode = s, this._generator = null;
    const r = new da();
    r.isRenderTargetTexture = !0, this._texture = ce(r), this._width = k(0), this._height = k(0), this._maxMip = k(0), this.updateBeforeType = z.RENDER;
  }
  set value(e) {
    this._value = e, this._pmrem = null;
  }
  /**
   * The node's texture value.
   *
   * @type {Texture}
   */
  get value() {
    return this._value;
  }
  /**
   * Uses the given PMREM texture to update internal values.
   *
   * @param {Texture} texture - The PMREM texture.
   */
  updateFromTexture(e) {
    const t = Ry(e.image.height);
    this._texture.value = e, this._width.value = t.texelWidth, this._height.value = t.texelHeight, this._maxMip.value = t.maxMip;
  }
  updateBefore(e) {
    let t = this._pmrem;
    const s = t ? t.pmremVersion : -1, r = this._value;
    s !== r.pmremVersion && (r.isPMREMTexture === !0 ? t = r : t = Ay(r, e.renderer, this._generator), t !== null && (this._pmrem = t, this.updateFromTexture(t)));
  }
  setup(e) {
    this._generator === null && (this._generator = new _y(e.renderer)), this.updateBefore(e);
    let t = this.uvNode;
    t === null && e.context.getUV && (t = e.context.getUV(this, e)), t = dd.mul(N(t.x, t.y.negate(), t.z));
    let s = this.levelNode;
    return s === null && e.context.getTextureLevel && (s = e.context.getTextureLevel(this)), cy(this._texture, t, s, this._width, this._height, this._maxMip);
  }
  dispose() {
    super.dispose(), this._generator !== null && this._generator.dispose();
  }
}
function Cy(a) {
  if (a == null) return !1;
  let e = 0;
  const t = 6;
  for (let s = 0; s < t; s++)
    a[s] !== void 0 && e++;
  return e === t;
}
function wy(a) {
  return a == null ? !1 : a.height > 0;
}
const $d = /* @__PURE__ */ le(Ey).setParameterLength(1, 3), Hu = /* @__PURE__ */ new WeakMap();
class My extends mr {
  static get type() {
    return "EnvironmentNode";
  }
  /**
   * Constructs a new environment node.
   *
   * @param {Node} [envNode=null] - A node representing the environment.
   */
  constructor(e = null) {
    super(), this.envNode = e;
  }
  setup(e) {
    const { material: t } = e;
    let s = this.envNode;
    if (s.isTextureNode || s.isMaterialReferenceNode) {
      const d = s.isTextureNode ? s.value : t[s.property], h = this._getPMREMNodeCache(e.renderer);
      let f = h.get(d);
      f === void 0 && (f = $d(d), h.set(d, f)), s = f;
    }
    const n = t.useAnisotropy === !0 || t.anisotropy > 0 ? sx : Z, i = s.context(qu(It, n)).mul(Di), o = s.context(By(gr)).mul(Math.PI).mul(Di), u = Lr(i), l = Lr(o);
    e.context.radiance.addAssign(u), e.context.iblIrradiance.addAssign(l);
    const c = e.context.lightingModel.clearcoatRadiance;
    if (c) {
      const d = s.context(qu(Dr, ys)).mul(Di), h = Lr(d);
      c.addAssign(h);
    }
  }
  /**
   * Returns the PMREM node cache of the current renderer.
   *
   * @private
   * @param {Renderer} renderer - The current renderer.
   * @return {WeakMap} The node cache.
   */
  _getPMREMNodeCache(e) {
    let t = Hu.get(e);
    return t === void 0 && (t = /* @__PURE__ */ new WeakMap(), Hu.set(e, t)), t;
  }
}
const qu = (a, e) => {
  let t = null;
  return {
    getUV: () => (t === null && (t = he.negate().reflect(e), t = Hc(a).mix(t, e).normalize(), t = t.transformDirection(qt)), t),
    getTextureLevel: () => a
  };
}, By = (a) => ({
  getUV: () => a,
  getTextureLevel: () => b(1)
}), Fy = /* @__PURE__ */ new Kh();
class zd extends Ne {
  static get type() {
    return "MeshStandardNodeMaterial";
  }
  /**
   * Constructs a new mesh standard node material.
   *
   * @param {Object} [parameters] - The configuration parameter.
   */
  constructor(e) {
    super(), this.isMeshStandardNodeMaterial = !0, this.lights = !0, this.emissiveNode = null, this.metalnessNode = null, this.roughnessNode = null, this.setDefaultValues(Fy), this.setValues(e);
  }
  /**
   * Overwritten since this type of material uses {@link EnvironmentNode}
   * to implement the PBR (PMREM based) environment mapping. Besides, the
   * method honors `Scene.environment`.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {?EnvironmentNode<vec3>} The environment node.
   */
  setupEnvironment(e) {
    let t = super.setupEnvironment(e);
    return t === null && e.environmentNode && (t = e.environmentNode), t ? new My(t) : null;
  }
  /**
   * Setups the lighting model.
   *
   * @return {PhysicalLightingModel} The lighting model.
   */
  setupLightingModel() {
    return new Vd();
  }
  /**
   * Setups the specular related node variables.
   */
  setupSpecular() {
    const e = ne(N(0.04), re.rgb, es);
    Ms.assign(N(0.04)), Ks.assign(e), Qs.assign(1);
  }
  /**
   * Setups the standard specific node variables.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  setupVariants() {
    const e = this.metalnessNode ? b(this.metalnessNode) : mx;
    es.assign(e);
    let t = this.roughnessNode ? b(this.roughnessNode) : gx;
    t = Ld({ roughness: t }), It.assign(t), this.setupSpecular(), Ws.assign(re.rgb.mul(e.oneMinus()));
  }
  copy(e) {
    return this.emissiveNode = e.emissiveNode, this.metalnessNode = e.metalnessNode, this.roughnessNode = e.roughnessNode, super.copy(e);
  }
}
const Uy = /* @__PURE__ */ new Qh();
class Dy extends zd {
  static get type() {
    return "MeshPhysicalNodeMaterial";
  }
  /**
   * Constructs a new mesh physical node material.
   *
   * @param {Object} [parameters] - The configuration parameter.
   */
  constructor(e) {
    super(), this.isMeshPhysicalNodeMaterial = !0, this.clearcoatNode = null, this.clearcoatRoughnessNode = null, this.clearcoatNormalNode = null, this.sheenNode = null, this.sheenRoughnessNode = null, this.iridescenceNode = null, this.iridescenceIORNode = null, this.iridescenceThicknessNode = null, this.specularIntensityNode = null, this.specularColorNode = null, this.iorNode = null, this.transmissionNode = null, this.thicknessNode = null, this.attenuationDistanceNode = null, this.attenuationColorNode = null, this.dispersionNode = null, this.anisotropyNode = null, this.setDefaultValues(Uy), this.setValues(e);
  }
  /**
   * Whether the lighting model should use clearcoat or not.
   *
   * @type {boolean}
   * @default true
   */
  get useClearcoat() {
    return this.clearcoat > 0 || this.clearcoatNode !== null;
  }
  /**
   * Whether the lighting model should use iridescence or not.
   *
   * @type {boolean}
   * @default true
   */
  get useIridescence() {
    return this.iridescence > 0 || this.iridescenceNode !== null;
  }
  /**
   * Whether the lighting model should use sheen or not.
   *
   * @type {boolean}
   * @default true
   */
  get useSheen() {
    return this.sheen > 0 || this.sheenNode !== null;
  }
  /**
   * Whether the lighting model should use anisotropy or not.
   *
   * @type {boolean}
   * @default true
   */
  get useAnisotropy() {
    return this.anisotropy > 0 || this.anisotropyNode !== null;
  }
  /**
   * Whether the lighting model should use transmission or not.
   *
   * @type {boolean}
   * @default true
   */
  get useTransmission() {
    return this.transmission > 0 || this.transmissionNode !== null;
  }
  /**
   * Whether the lighting model should use dispersion or not.
   *
   * @type {boolean}
   * @default true
   */
  get useDispersion() {
    return this.dispersion > 0 || this.dispersionNode !== null;
  }
  /**
   * Setups the specular related node variables.
   */
  setupSpecular() {
    const e = this.iorNode ? b(this.iorNode) : Mx;
    Mn.assign(e), Ms.assign(or(Wc(Mn.sub(1).div(Mn.add(1))).mul(px), N(1)).mul(xu)), Ks.assign(ne(Ms, re.rgb, es)), Qs.assign(ne(xu, 1, es));
  }
  /**
   * Setups the lighting model.
   *
   * @return {PhysicalLightingModel} The lighting model.
   */
  setupLightingModel() {
    return new Vd(this.useClearcoat, this.useSheen, this.useIridescence, this.useAnisotropy, this.useTransmission, this.useDispersion);
  }
  /**
   * Setups the physical specific node variables.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  setupVariants(e) {
    if (super.setupVariants(e), this.useClearcoat) {
      const t = this.clearcoatNode ? b(this.clearcoatNode) : Tx, s = this.clearcoatRoughnessNode ? b(this.clearcoatRoughnessNode) : yx;
      $o.assign(t), Dr.assign(Ld({ roughness: s }));
    }
    if (this.useSheen) {
      const t = this.sheenNode ? N(this.sheenNode) : Sx, s = this.sheenRoughnessNode ? b(this.sheenRoughnessNode) : Nx;
      et.assign(t), ms.assign(s);
    }
    if (this.useIridescence) {
      const t = this.iridescenceNode ? b(this.iridescenceNode) : Ax, s = this.iridescenceIORNode ? b(this.iridescenceIORNode) : vx, r = this.iridescenceThicknessNode ? b(this.iridescenceThicknessNode) : Ex;
      Sa.assign(t), zo.assign(s), Wo.assign(r);
    }
    if (this.useAnisotropy) {
      const t = (this.anisotropyNode ? I(this.anisotropyNode) : Rx).toVar();
      ps.assign(t.length()), ue(ps.equal(0), () => {
        t.assign(I(1, 0));
      }).Else(() => {
        t.divAssign(I(ps)), ps.assign(ps.saturate());
      }), Ho.assign(ps.pow2().mix(It.pow2(), 1)), wn.assign(wr[0].mul(t.x).add(wr[1].mul(t.y))), er.assign(wr[1].mul(t.x).sub(wr[0].mul(t.y)));
    }
    if (this.useTransmission) {
      const t = this.transmissionNode ? b(this.transmissionNode) : Cx, s = this.thicknessNode ? b(this.thicknessNode) : wx, r = this.attenuationDistanceNode ? b(this.attenuationDistanceNode) : Bx, n = this.attenuationColorNode ? N(this.attenuationColorNode) : Fx;
      if (Xo.assign(t), Bc.assign(s), Fc.assign(r), Uc.assign(n), this.useDispersion) {
        const i = this.dispersionNode ? b(this.dispersionNode) : Ox;
        Dc.assign(i);
      }
    }
  }
  /**
   * Setups the clearcoat normal node.
   *
   * @return {Node<vec3>} The clearcoat normal.
   */
  setupClearcoatNormal() {
    return this.clearcoatNormalNode ? N(this.clearcoatNormalNode) : _x;
  }
  setup(e) {
    e.context.setupClearcoatNormal = () => tr(this.setupClearcoatNormal(e), "NORMAL", "vec3"), super.setup(e);
  }
  copy(e) {
    return this.clearcoatNode = e.clearcoatNode, this.clearcoatRoughnessNode = e.clearcoatRoughnessNode, this.clearcoatNormalNode = e.clearcoatNormalNode, this.sheenNode = e.sheenNode, this.sheenRoughnessNode = e.sheenRoughnessNode, this.iridescenceNode = e.iridescenceNode, this.iridescenceIORNode = e.iridescenceIORNode, this.iridescenceThicknessNode = e.iridescenceThicknessNode, this.specularIntensityNode = e.specularIntensityNode, this.specularColorNode = e.specularColorNode, this.iorNode = e.iorNode, this.transmissionNode = e.transmissionNode, this.thicknessNode = e.thicknessNode, this.attenuationDistanceNode = e.attenuationDistanceNode, this.attenuationColorNode = e.attenuationColorNode, this.dispersionNode = e.dispersionNode, this.anisotropyNode = e.anisotropyNode, super.copy(e);
  }
}
const Py = /* @__PURE__ */ A(({ normal: a, lightDirection: e, builder: t }) => {
  const s = a.dot(e), r = I(s.mul(0.5).add(0.5), 0);
  if (t.material.gradientMap) {
    const n = Zt("gradientMap", "texture").context({ getUV: () => r });
    return N(n.r);
  } else {
    const n = r.fwidth().mul(0.5);
    return ne(N(0.7), N(1), Wt(b(0.7).sub(n.x), b(0.7).add(n.x), r.x));
  }
});
class Ly extends pi {
  /**
   * Implements the direct lighting. Instead of using a conventional smooth irradiance, the irradiance is
   * reduced to a small number of discrete shades to create a comic-like, flat look.
   *
   * @param {Object} lightData - The light data.
   * @param {NodeBuilder} builder - The current node builder.
   */
  direct({ lightDirection: e, lightColor: t, reflectedLight: s }, r) {
    const n = Py({ normal: ld, lightDirection: e, builder: r }).mul(t);
    s.directDiffuse.addAssign(n.mul(ur({ diffuseColor: re.rgb })));
  }
  /**
   * Implements the indirect lighting.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  indirect(e) {
    const { ambientOcclusion: t, irradiance: s, reflectedLight: r } = e.context;
    r.indirectDiffuse.addAssign(s.mul(ur({ diffuseColor: re }))), r.indirectDiffuse.mulAssign(t);
  }
}
const Iy = /* @__PURE__ */ new Yh();
class Oy extends Ne {
  static get type() {
    return "MeshToonNodeMaterial";
  }
  /**
   * Constructs a new mesh toon node material.
   *
   * @param {Object} [parameters] - The configuration parameter.
   */
  constructor(e) {
    super(), this.isMeshToonNodeMaterial = !0, this.lights = !0, this.setDefaultValues(Iy), this.setValues(e);
  }
  /**
   * Setups the lighting model.
   *
   * @return {ToonLightingModel} The lighting model.
   */
  setupLightingModel() {
    return new Ly();
  }
}
const Gy = /* @__PURE__ */ A(() => {
  const a = N(he.z, 0, he.x.negate()).normalize(), e = he.cross(a);
  return I(a.dot(Z), e.dot(Z)).mul(0.495).add(0.5);
}).once(["NORMAL", "VERTEX"])().toVar("matcapUV"), ky = /* @__PURE__ */ new Jh();
class Vy extends Ne {
  static get type() {
    return "MeshMatcapNodeMaterial";
  }
  /**
   * Constructs a new mesh normal node material.
   *
   * @param {Object} [parameters] - The configuration parameter.
   */
  constructor(e) {
    super(), this.isMeshMatcapNodeMaterial = !0, this.setDefaultValues(ky), this.setValues(e);
  }
  /**
   * Setups the matcap specific node variables.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  setupVariants(e) {
    const t = Gy;
    let s;
    e.material.matcap ? s = Zt("matcap", "texture").context({ getUV: () => t }) : s = N(ne(0.2, 0.8, t.y)), re.rgb.mulAssign(s.rgb);
  }
}
class $y extends Re {
  static get type() {
    return "RotateNode";
  }
  /**
   * Constructs a new rotate node.
   *
   * @param {Node} positionNode - The position node.
   * @param {Node} rotationNode - Represents the rotation that is applied to the position node. Depending
   * on whether the position data are 2D or 3D, the rotation is expressed a single float value or an Euler value.
   */
  constructor(e, t) {
    super(), this.positionNode = e, this.rotationNode = t;
  }
  /**
   * The type of the {@link RotateNode#positionNode} defines the node's type.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The node's type.
   */
  generateNodeType(e) {
    return this.positionNode.getNodeType(e);
  }
  setup(e) {
    const { rotationNode: t, positionNode: s } = this;
    if (this.getNodeType(e) === "vec2") {
      const n = t.cos(), i = t.sin();
      return ba(
        n,
        i,
        i.negate(),
        n
      ).mul(s);
    } else {
      const n = t, i = As(W(1, 0, 0, 0), W(0, _t(n.x), qe(n.x).negate(), 0), W(0, qe(n.x), _t(n.x), 0), W(0, 0, 0, 1)), o = As(W(_t(n.y), 0, qe(n.y), 0), W(0, 1, 0, 0), W(qe(n.y).negate(), 0, _t(n.y), 0), W(0, 0, 0, 1)), u = As(W(_t(n.z), qe(n.z).negate(), 0, 0), W(qe(n.z), _t(n.z), 0, 0), W(0, 0, 1, 0), W(0, 0, 0, 1));
      return i.mul(o).mul(u).mul(W(s, 1)).xyz;
    }
  }
}
const Wd = /* @__PURE__ */ le($y).setParameterLength(2), zy = /* @__PURE__ */ new rf();
class Hd extends Ne {
  static get type() {
    return "SpriteNodeMaterial";
  }
  /**
   * Constructs a new sprite node material.
   *
   * @param {Object} [parameters] - The configuration parameter.
   */
  constructor(e) {
    super(), this.isSpriteNodeMaterial = !0, this._useSizeAttenuation = !0, this.positionNode = null, this.rotationNode = null, this.scaleNode = null, this.transparent = !0, this.setDefaultValues(zy), this.setValues(e);
  }
  /**
   * Setups the position node in view space. This method implements
   * the sprite specific vertex shader.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {Node<vec3>} The position in view space.
   */
  setupPositionView(e) {
    const { object: t, camera: s } = e, { positionNode: r, rotationNode: n, scaleNode: i, sizeAttenuation: o } = this, u = Jr.mul(N(r || 0));
    let l = I(Us[0].xyz.length(), Us[1].xyz.length());
    i !== null && (l = l.mul(I(i))), s.isPerspectiveCamera && o === !1 && (l = l.mul(u.z.negate()));
    let c = fi.xy;
    if (t.center && t.center.isVector2 === !0) {
      const f = Jg("center", "vec2", t);
      c = c.sub(f.sub(0.5));
    }
    c = c.mul(l);
    const d = b(n || bx), h = Wd(c, d);
    return W(u.xy.add(h), u.zw);
  }
  copy(e) {
    return this.positionNode = e.positionNode, this.rotationNode = e.rotationNode, this.scaleNode = e.scaleNode, super.copy(e);
  }
  /**
   * Whether to use size attenuation or not.
   *
   * @type {boolean}
   * @default true
   */
  get sizeAttenuation() {
    return this._useSizeAttenuation;
  }
  set sizeAttenuation(e) {
    this._useSizeAttenuation !== e && (this._useSizeAttenuation = e, this.needsUpdate = !0);
  }
}
const Wy = /* @__PURE__ */ new sf(), Hy = /* @__PURE__ */ new je();
class qy extends Hd {
  static get type() {
    return "PointsNodeMaterial";
  }
  /**
   * Constructs a new points node material.
   *
   * @param {Object} [parameters] - The configuration parameter.
   */
  constructor(e) {
    super(), this.sizeNode = null, this.isPointsNodeMaterial = !0, this.setDefaultValues(Wy), this.setValues(e);
  }
  setupPositionView() {
    const { positionNode: e } = this;
    return Jr.mul(N(e || ve)).xyz;
  }
  setupVertexSprite(e) {
    const { material: t, camera: s } = e, { rotationNode: r, scaleNode: n, sizeNode: i, sizeAttenuation: o } = this;
    let u = super.setupVertex(e);
    if (t.isNodeMaterial !== !0)
      return u;
    let l = i !== null ? I(i) : Ix;
    l = l.mul(Cm), s.isPerspectiveCamera && o === !0 && (l = l.mul(Xy.div(be.z.negate()))), n && n.isNode && (l = l.mul(I(n)));
    let c = fi.xy;
    if (r && r.isNode) {
      const d = b(r);
      c = Wd(c, d);
    }
    return c = c.mul(l), c = c.div(wm.div(2)), c = c.mul(u.w), u = u.add(W(c, 0, 0)), u;
  }
  setupVertex(e) {
    return e.object.isPoints ? super.setupVertex(e) : this.setupVertexSprite(e);
  }
  /**
   * Whether alpha to coverage should be used or not.
   *
   * @type {boolean}
   * @default true
   */
  get alphaToCoverage() {
    return this._useAlphaToCoverage;
  }
  set alphaToCoverage(e) {
    this._useAlphaToCoverage !== e && (this._useAlphaToCoverage = e, this.needsUpdate = !0);
  }
}
const Xy = /* @__PURE__ */ k(1).onFrameUpdate(function({ renderer: a }) {
  const e = a.getSize(Hy);
  this.value = 0.5 * e.y;
});
class Ky extends pi {
  /**
   * Constructs a new shadow mask model.
   */
  constructor() {
    super(), this.shadowNode = b(1).toVar("shadowMask");
  }
  /**
   * Only used to save the shadow mask.
   *
   * @param {Object} input - The input data.
   */
  direct({ lightNode: e }) {
    e.shadowNode !== null && this.shadowNode.mulAssign(e.shadowNode);
  }
  /**
   * Uses the shadow mask to produce the final color.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  finish({ context: e }) {
    re.a.mulAssign(this.shadowNode.oneMinus()), e.outgoingLight.rgb.assign(re.rgb);
  }
}
const Qy = /* @__PURE__ */ new nf();
class Yy extends Ne {
  static get type() {
    return "ShadowNodeMaterial";
  }
  /**
   * Constructs a new shadow node material.
   *
   * @param {Object} [parameters] - The configuration parameter.
   */
  constructor(e) {
    super(), this.isShadowNodeMaterial = !0, this.lights = !0, this.transparent = !0, this.setDefaultValues(Qy), this.setValues(e);
  }
  /**
   * Setups the lighting model.
   *
   * @return {ShadowMaskModel} The lighting model.
   */
  setupLightingModel() {
    return new Ky();
  }
}
rs("vec3");
rs("vec3");
rs("vec3");
class jy {
  /**
   * Constructs a new animation loop management component.
   *
   * @param {Renderer} renderer - A reference to the main renderer.
   * @param {NodeManager} nodes - Renderer component for managing nodes related logic.
   * @param {Info} info - Renderer component for managing metrics and monitoring data.
   */
  constructor(e, t, s) {
    this.renderer = e, this.nodes = t, this.info = s, this._context = typeof self < "u" ? self : null, this._animationLoop = null, this._requestId = null;
  }
  /**
   * Starts the internal animation loop.
   */
  start() {
    const e = (t, s) => {
      this._requestId = this._context.requestAnimationFrame(e), this.info.autoReset === !0 && this.info.reset(), this.nodes.nodeFrame.update(), this.info.frame = this.nodes.nodeFrame.frameId, this.renderer._inspector.begin(), this._animationLoop !== null && this._animationLoop(t, s), this.renderer._inspector.finish();
    };
    e();
  }
  /**
   * Stops the internal animation loop.
   */
  stop() {
    this._context !== null && this._context.cancelAnimationFrame(this._requestId), this._requestId = null;
  }
  /**
   * Returns the user-level animation loop.
   *
   * @return {?Function} The animation loop.
   */
  getAnimationLoop() {
    return this._animationLoop;
  }
  /**
   * Defines the user-level animation loop.
   *
   * @param {?Function} callback - The animation loop.
   */
  setAnimationLoop(e) {
    this._animationLoop = e;
  }
  /**
   * Returns the animation context.
   *
   * @return {Window|XRSession} The animation context.
   */
  getContext() {
    return this._context;
  }
  /**
   * Defines the context in which `requestAnimationFrame()` is executed.
   *
   * @param {Window|XRSession} context - The context to set.
   */
  setContext(e) {
    this._context = e;
  }
  /**
   * Frees all internal resources and stops the animation loop.
   */
  dispose() {
    this.stop();
  }
}
class us {
  /**
   * Constructs a new Chain Map.
   */
  constructor() {
    this.weakMaps = {};
  }
  /**
   * Returns the Weak Map for the given keys.
   *
   * @param {Array<Object>} keys - List of keys.
   * @return {WeakMap} The weak map.
   */
  _getWeakMap(e) {
    const t = e.length;
    let s = this.weakMaps[t];
    return s === void 0 && (s = /* @__PURE__ */ new WeakMap(), this.weakMaps[t] = s), s;
  }
  /**
   * Returns the value for the given array of keys.
   *
   * @param {Array<Object>} keys - List of keys.
   * @return {any} The value. Returns `undefined` if no value was found.
   */
  get(e) {
    let t = this._getWeakMap(e);
    for (let s = 0; s < e.length - 1; s++)
      if (t = t.get(e[s]), t === void 0) return;
    return t.get(e[e.length - 1]);
  }
  /**
   * Sets the value for the given keys.
   *
   * @param {Array<Object>} keys - List of keys.
   * @param {any} value - The value to set.
   * @return {ChainMap} A reference to this Chain Map.
   */
  set(e, t) {
    let s = this._getWeakMap(e);
    for (let r = 0; r < e.length - 1; r++) {
      const n = e[r];
      s.has(n) === !1 && s.set(n, /* @__PURE__ */ new WeakMap()), s = s.get(n);
    }
    return s.set(e[e.length - 1], t), this;
  }
  /**
   * Deletes a value for the given keys.
   *
   * @param {Array<Object>} keys - The keys.
   * @return {boolean} Returns `true` if the value has been removed successfully and `false` if the value has not be found.
   */
  delete(e) {
    let t = this._getWeakMap(e);
    for (let s = 0; s < e.length - 1; s++)
      if (t = t.get(e[s]), t === void 0) return !1;
    return t.delete(e[e.length - 1]);
  }
}
let Zy = 0;
function Jy(a) {
  const e = Object.keys(a);
  let t = Object.getPrototypeOf(a);
  for (; t; ) {
    const s = Object.getOwnPropertyDescriptors(t);
    for (const r in s)
      if (s[r] !== void 0) {
        const n = s[r];
        n && typeof n.get == "function" && e.push(r);
      }
    t = Object.getPrototypeOf(t);
  }
  return e;
}
class e_ {
  /**
   * Constructs a new render object.
   *
   * @param {NodeManager} nodes - Renderer component for managing nodes related logic.
   * @param {Geometries} geometries - Renderer component for managing geometries.
   * @param {Renderer} renderer - The renderer.
   * @param {Object3D} object - The 3D object.
   * @param {Material} material - The 3D object's material.
   * @param {Scene} scene - The scene the 3D object belongs to.
   * @param {Camera} camera - The camera the object should be rendered with.
   * @param {LightsNode} lightsNode - The lights node.
   * @param {RenderContext} renderContext - The render context.
   * @param {ClippingContext} clippingContext - The clipping context.
   */
  constructor(e, t, s, r, n, i, o, u, l, c) {
    this.id = Zy++, this._nodes = e, this._geometries = t, this.renderer = s, this.object = r, this.material = n, this.scene = i, this.camera = o, this.lightsNode = u, this.context = l, this.geometry = r.geometry, this.version = n.version, this.drawRange = null, this.attributes = null, this.attributesId = null, this.pipeline = null, this.group = null, this.vertexBuffers = null, this.drawParams = null, this.bundle = null, this.clippingContext = c, this.clippingContextCacheKey = c !== null ? c.cacheKey : "", this.initialNodesCacheKey = this.getDynamicCacheKey(), this.initialCacheKey = this.getCacheKey(), this._nodeBuilderState = null, this._bindings = null, this._monitor = null, this.onDispose = null, this.isRenderObject = !0, this.onMaterialDispose = () => {
      this.dispose();
    }, this.onGeometryDispose = () => {
      this.attributes = null, this.attributesId = null;
    }, this.material.addEventListener("dispose", this.onMaterialDispose), this.geometry.addEventListener("dispose", this.onGeometryDispose);
  }
  /**
   * Updates the clipping context.
   *
   * @param {ClippingContext} context - The clipping context to set.
   */
  updateClipping(e) {
    this.clippingContext = e;
  }
  /**
   * Whether the clipping requires an update or not.
   *
   * @type {boolean}
   * @readonly
   */
  get clippingNeedsUpdate() {
    return this.clippingContext === null || this.clippingContext.cacheKey === this.clippingContextCacheKey ? !1 : (this.clippingContextCacheKey = this.clippingContext.cacheKey, !0);
  }
  /**
   * The number of clipping planes defined in context of hardware clipping.
   *
   * @type {number}
   * @readonly
   */
  get hardwareClippingPlanes() {
    return this.material.hardwareClipping === !0 ? this.clippingContext.unionClippingCount : 0;
  }
  /**
   * Returns the node builder state of this render object.
   *
   * @return {NodeBuilderState} The node builder state.
   */
  getNodeBuilderState() {
    return this._nodeBuilderState || (this._nodeBuilderState = this._nodes.getForRender(this));
  }
  /**
   * Returns the node material observer of this render object.
   *
   * @return {NodeMaterialObserver} The node material observer.
   */
  getMonitor() {
    return this._monitor || (this._monitor = this.getNodeBuilderState().observer);
  }
  /**
   * Returns an array of bind groups of this render object.
   *
   * @return {Array<BindGroup>} The bindings.
   */
  getBindings() {
    return this._bindings || (this._bindings = this.getNodeBuilderState().createBindings());
  }
  /**
   * Returns a binding group by group name of this render object.
   *
   * @param {string} name - The name of the binding group.
   * @return {?BindGroup} The bindings.
   */
  getBindingGroup(e) {
    for (const t of this.getBindings())
      if (t.name === e)
        return t;
  }
  /**
   * Returns the index of the render object's geometry.
   *
   * @return {?BufferAttribute} The index. Returns `null` for non-indexed geometries.
   */
  getIndex() {
    return this._geometries.getIndex(this);
  }
  /**
   * Returns the indirect buffer attribute.
   *
   * @return {?BufferAttribute} The indirect attribute. `null` if no indirect drawing is used.
   */
  getIndirect() {
    return this._geometries.getIndirect(this);
  }
  /**
   * Returns the byte offset into the indirect attribute buffer.
   *
   * @return {number|Array<number>} The byte offset into the indirect attribute buffer.
   */
  getIndirectOffset() {
    return this._geometries.getIndirectOffset(this);
  }
  /**
   * Returns an array that acts as a key for identifying the render object in a chain map.
   *
   * @return {Array<Object>} An array with object references.
   */
  getChainArray() {
    return [this.object, this.material, this.context, this.lightsNode];
  }
  /**
   * This method is used when the geometry of a 3D object has been exchanged and the
   * respective render object now requires an update.
   *
   * @param {BufferGeometry} geometry - The geometry to set.
   */
  setGeometry(e) {
    this.geometry = e, this.attributes = null, this.attributesId = null;
  }
  /**
   * Returns the buffer attributes of the render object. The returned array holds
   * attribute definitions on geometry and node level.
   *
   * @return {Array<BufferAttribute>} An array with buffer attributes.
   */
  getAttributes() {
    if (this.attributes !== null) return this.attributes;
    const e = this.getNodeBuilderState().nodeAttributes, t = this.geometry, s = [], r = /* @__PURE__ */ new Set(), n = {};
    for (const i of e) {
      let o;
      if (i.node && i.node.attribute ? o = i.node.attribute : (o = t.getAttribute(i.name), n[i.name] = o.id), o === void 0) continue;
      s.push(o);
      const u = o.isInterleavedBufferAttribute ? o.data : o;
      r.add(u);
    }
    return this.attributes = s, this.attributesId = n, this.vertexBuffers = Array.from(r.values()), s;
  }
  /**
   * Returns the vertex buffers of the render object.
   *
   * @return {Array<BufferAttribute|InterleavedBuffer>} An array with buffer attribute or interleaved buffers.
   */
  getVertexBuffers() {
    return this.vertexBuffers === null && this.getAttributes(), this.vertexBuffers;
  }
  /**
   * Returns the draw parameters for the render object.
   *
   * @return {?{vertexCount: number, firstVertex: number, instanceCount: number, firstInstance: number}} The draw parameters.
   */
  getDrawParameters() {
    const { object: e, material: t, geometry: s, group: r, drawRange: n } = this, i = this.drawParams || (this.drawParams = {
      vertexCount: 0,
      firstVertex: 0,
      instanceCount: 0,
      firstInstance: 0
    }), o = this.getIndex(), u = o !== null;
    let l = 1;
    if (s.isInstancedBufferGeometry === !0 ? l = s.instanceCount : e.count !== void 0 && (l = Math.max(0, e.count)), l === 0) return null;
    if (i.instanceCount = l, e.isBatchedMesh === !0) return i;
    let c = 1;
    t.wireframe === !0 && !e.isPoints && !e.isLineSegments && !e.isLine && !e.isLineLoop && (c = 2);
    let d = n.start * c, h = (n.start + n.count) * c;
    r !== null && (d = Math.max(d, r.start * c), h = Math.min(h, (r.start + r.count) * c));
    const f = s.attributes.position;
    let p = 1 / 0;
    u ? p = o.count : f != null && (p = f.count), d = Math.max(d, 0), h = Math.min(h, p);
    const g = h - d;
    return g < 0 || g === 1 / 0 ? null : (i.vertexCount = g, i.firstVertex = d, i);
  }
  /**
   * Returns the render object's geometry cache key.
   *
   * The geometry cache key is part of the material cache key.
   *
   * @return {string} The geometry cache key.
   */
  getGeometryCacheKey() {
    const { geometry: e } = this;
    let t = "";
    for (const s of Object.keys(e.attributes).sort()) {
      const r = e.attributes[s];
      t += s + ",", r.data && (t += r.data.stride + ","), r.offset && (t += r.offset + ","), r.itemSize && (t += r.itemSize + ","), r.normalized && (t += "n,");
    }
    for (const s of Object.keys(e.morphAttributes).sort()) {
      const r = e.morphAttributes[s];
      t += "morph-" + s + ",";
      for (let n = 0, i = r.length; n < i; n++) {
        const o = r[n];
        t += o.id + ",";
      }
    }
    return e.index && (t += "index,"), t;
  }
  /**
   * Returns the render object's material cache key.
   *
   * The material cache key is part of the render object cache key.
   *
   * @return {number} The material cache key.
   */
  getMaterialCacheKey() {
    const { object: e, material: t, renderer: s } = this;
    let r = t.customProgramCacheKey();
    for (const n of Jy(t)) {
      if (/^(is[A-Z]|_)|^(visible|version|uuid|name|opacity|userData)$/.test(n)) continue;
      const i = t[n];
      let o;
      if (i !== null) {
        const u = typeof i;
        u === "number" ? o = i !== 0 ? "1" : "0" : u === "object" ? (o = "{", i.isTexture && (o += i.mapping, s.backend.isWebGPUBackend === !0 && (o += i.magFilter, o += i.minFilter, o += i.wrapS, o += i.wrapT, o += i.wrapR)), o += "}") : o = String(i);
      } else
        o = String(i);
      r += /*property + ':' +*/
      o + ",";
    }
    return r += this.clippingContextCacheKey + ",", e.geometry && (r += this.getGeometryCacheKey()), e.skeleton && (r += e.skeleton.bones.length + ","), e.isBatchedMesh && (r += e._matricesTexture.uuid + ",", e._colorsTexture !== null && (r += e._colorsTexture.uuid + ",")), (e.isInstancedMesh || e.count > 1 || Array.isArray(e.morphTargetInfluences)) && (r += e.uuid + ","), r += this.context.id + ",", r += e.receiveShadow + ",", lr(r);
  }
  /**
   * Whether the geometry requires an update or not.
   *
   * @type {boolean}
   * @readonly
   */
  get needsGeometryUpdate() {
    if (this.geometry.id !== this.object.geometry.id) return !0;
    if (this.attributes !== null) {
      const e = this.attributesId;
      for (const t in e) {
        const s = this.geometry.getAttribute(t);
        if (s === void 0 || e[t] !== s.id)
          return !0;
      }
    }
    return !1;
  }
  /**
   * Whether the render object requires an update or not.
   *
   * Note: There are two distinct places where render objects are checked for an update.
   *
   * 1. In `RenderObjects.get()` which is executed when the render object is request. This
   * method checks the `needsUpdate` flag and recreates the render object if necessary.
   * 2. In `Renderer._renderObjectDirect()` right after getting the render object via
   * `RenderObjects.get()`. The render object's NodeMaterialObserver is then used to detect
   * a need for a refresh due to material, geometry or object related value changes.
   *
   * TODO: Investigate if it's possible to merge both steps so there is only a single place
   * that performs the 'needsUpdate' check.
   *
   * @type {boolean}
   * @readonly
   */
  get needsUpdate() {
    return (
      /*this.object.static !== true &&*/
      this.initialNodesCacheKey !== this.getDynamicCacheKey() || this.clippingNeedsUpdate
    );
  }
  /**
   * Returns the dynamic cache key which represents a key that is computed per draw command.
   *
   * @return {number} The cache key.
   */
  getDynamicCacheKey() {
    let e = 0;
    return this.material.isShadowPassMaterial !== !0 && (e = this._nodes.getCacheKey(this.scene, this.lightsNode)), this.camera.isArrayCamera && (e = Fr(e, this.camera.cameras.length)), this.object.receiveShadow && (e = Fr(e, 1)), e = Fr(e, this.renderer.contextNode.id, this.renderer.contextNode.version), e;
  }
  /**
   * Returns the render object's cache key.
   *
   * @return {number} The cache key.
   */
  getCacheKey() {
    return this.getMaterialCacheKey() + this.getDynamicCacheKey();
  }
  /**
   * Frees internal resources.
   */
  dispose() {
    this.material.removeEventListener("dispose", this.onMaterialDispose), this.geometry.removeEventListener("dispose", this.onGeometryDispose), this.onDispose();
  }
}
const xt = [];
class t_ {
  /**
   * Constructs a new render object management component.
   *
   * @param {Renderer} renderer - The renderer.
   * @param {NodeManager} nodes - Renderer component for managing nodes related logic.
   * @param {Geometries} geometries - Renderer component for managing geometries.
   * @param {Pipelines} pipelines - Renderer component for managing pipelines.
   * @param {Bindings} bindings - Renderer component for managing bindings.
   * @param {Info} info - Renderer component for managing metrics and monitoring data.
   */
  constructor(e, t, s, r, n, i) {
    this.renderer = e, this.nodes = t, this.geometries = s, this.pipelines = r, this.bindings = n, this.info = i, this.chainMaps = {};
  }
  /**
   * Returns a render object for the given object and state data.
   *
   * @param {Object3D} object - The 3D object.
   * @param {Material} material - The 3D object's material.
   * @param {Scene} scene - The scene the 3D object belongs to.
   * @param {Camera} camera - The camera the 3D object should be rendered with.
   * @param {LightsNode} lightsNode - The lights node.
   * @param {RenderContext} renderContext - The render context.
   * @param {ClippingContext} clippingContext - The clipping context.
   * @param {string} [passId] - An optional ID for identifying the pass.
   * @return {RenderObject} The render object.
   */
  get(e, t, s, r, n, i, o, u) {
    const l = this.getChainMap(u);
    xt[0] = e, xt[1] = t, xt[2] = i, xt[3] = n;
    let c = l.get(xt);
    return c === void 0 ? (c = this.createRenderObject(this.nodes, this.geometries, this.renderer, e, t, s, r, n, i, o, u), l.set(xt, c)) : (c.camera = r, c.updateClipping(o), c.needsGeometryUpdate && c.setGeometry(e.geometry), (c.version !== t.version || c.needsUpdate) && (c.initialCacheKey !== c.getCacheKey() ? (c.dispose(), c = this.get(e, t, s, r, n, i, o, u)) : c.version = t.version)), xt[0] = null, xt[1] = null, xt[2] = null, xt[3] = null, c;
  }
  /**
   * Returns a chain map for the given pass ID.
   *
   * @param {string} [passId='default'] - The pass ID.
   * @return {ChainMap} The chain map.
   */
  getChainMap(e = "default") {
    return this.chainMaps[e] || (this.chainMaps[e] = new us());
  }
  /**
   * Frees internal resources.
   */
  dispose() {
    this.chainMaps = {};
  }
  /**
   * Factory method for creating render objects with the given list of parameters.
   *
   * @param {NodeManager} nodes - Renderer component for managing nodes related logic.
   * @param {Geometries} geometries - Renderer component for managing geometries.
   * @param {Renderer} renderer - The renderer.
   * @param {Object3D} object - The 3D object.
   * @param {Material} material - The object's material.
   * @param {Scene} scene - The scene the 3D object belongs to.
   * @param {Camera} camera - The camera the object should be rendered with.
   * @param {LightsNode} lightsNode - The lights node.
   * @param {RenderContext} renderContext - The render context.
   * @param {ClippingContext} clippingContext - The clipping context.
   * @param {string} [passId] - An optional ID for identifying the pass.
   * @return {RenderObject} The render object.
   */
  createRenderObject(e, t, s, r, n, i, o, u, l, c, d) {
    const h = this.getChainMap(d), f = new e_(e, t, s, r, n, i, o, u, l, c);
    return f.onDispose = () => {
      this.pipelines.delete(f), this.bindings.deleteForRender(f), this.nodes.delete(f), h.delete(f.getChainArray());
    }, f;
  }
}
class ls {
  /**
   * Constructs a new data map.
   */
  constructor() {
    this.data = /* @__PURE__ */ new WeakMap();
  }
  /**
   * Returns the dictionary for the given object.
   *
   * @param {Object} object - The object.
   * @return {Object} The dictionary.
   */
  get(e) {
    let t = this.data.get(e);
    return t === void 0 && (t = {}, this.data.set(e, t)), t;
  }
  /**
   * Deletes the dictionary for the given object.
   *
   * @param {Object} object - The object.
   * @return {?Object} The deleted dictionary.
   */
  delete(e) {
    let t = null;
    return this.data.has(e) && (t = this.data.get(e), this.data.delete(e)), t;
  }
  /**
   * Returns `true` if the given object has a dictionary defined.
   *
   * @param {Object} object - The object to test.
   * @return {boolean} Whether a dictionary is defined or not.
   */
  has(e) {
    return this.data.has(e);
  }
  /**
   * Frees internal resources.
   */
  dispose() {
    this.data = /* @__PURE__ */ new WeakMap();
  }
}
const it = {
  VERTEX: 1,
  INDEX: 2,
  STORAGE: 3,
  INDIRECT: 4
}, Jt = 16, s_ = 211, r_ = 212;
class n_ extends ls {
  /**
   * Constructs a new attribute management component.
   *
   * @param {Backend} backend - The renderer's backend.
   * @param {Info} info - Renderer component for managing metrics and monitoring data.
   */
  constructor(e, t) {
    super(), this.backend = e, this.info = t;
  }
  /**
   * Deletes the data for the given attribute.
   *
   * @param {BufferAttribute} attribute - The attribute.
   * @return {?Object} The deleted attribute data.
   */
  delete(e) {
    const t = super.delete(e);
    return t !== null && (this.backend.destroyAttribute(e), this.info.destroyAttribute(e)), t;
  }
  /**
   * Updates the given attribute. This method creates attribute buffers
   * for new attributes and updates data for existing ones.
   *
   * @param {BufferAttribute} attribute - The attribute to update.
   * @param {number} type - The attribute type.
   */
  update(e, t) {
    const s = this.get(e);
    if (s.version === void 0)
      t === it.VERTEX ? (this.backend.createAttribute(e), this.info.createAttribute(e)) : t === it.INDEX ? (this.backend.createIndexAttribute(e), this.info.createIndexAttribute(e)) : t === it.STORAGE ? (this.backend.createStorageAttribute(e), this.info.createStorageAttribute(e)) : t === it.INDIRECT && (this.backend.createIndirectStorageAttribute(e), this.info.createIndirectStorageAttribute(e)), s.version = this._getBufferAttribute(e).version;
    else {
      const r = this._getBufferAttribute(e);
      (s.version < r.version || r.usage === Ln) && (this.backend.updateAttribute(e), s.version = r.version);
    }
  }
  /**
   * Utility method for handling interleaved buffer attributes correctly.
   * To process them, their `InterleavedBuffer` is returned.
   *
   * @param {BufferAttribute} attribute - The attribute.
   * @return {BufferAttribute|InterleavedBuffer}
   */
  _getBufferAttribute(e) {
    return e.isInterleavedBufferAttribute && (e = e.data), e;
  }
}
function qd(a) {
  return a.index !== null ? a.index.version : a.attributes.position.version;
}
function Xd(a) {
  return a.index !== null ? a.index.id : a.attributes.position.id;
}
function Xu(a) {
  const e = [], t = a.index, s = a.attributes.position;
  if (t !== null) {
    const n = t.array;
    for (let i = 0, o = n.length; i < o; i += 3) {
      const u = n[i + 0], l = n[i + 1], c = n[i + 2];
      e.push(u, l, l, c, c, u);
    }
  } else {
    const n = s.array;
    for (let i = 0, o = n.length / 3 - 1; i < o; i += 3) {
      const u = i + 0, l = i + 1, c = i + 2;
      e.push(u, l, l, c, c, u);
    }
  }
  const r = new (s.count >= 65535 ? uf : lf)(e, 1);
  return r.version = qd(a), r.__id = Xd(a), r;
}
class i_ extends ls {
  /**
   * Constructs a new geometry management component.
   *
   * @param {Attributes} attributes - Renderer component for managing attributes.
   * @param {Info} info - Renderer component for managing metrics and monitoring data.
   */
  constructor(e, t) {
    super(), this.attributes = e, this.info = t, this.wireframes = /* @__PURE__ */ new WeakMap(), this.attributeCall = /* @__PURE__ */ new WeakMap(), this._geometryDisposeListeners = /* @__PURE__ */ new Map();
  }
  /**
   * Returns `true` if the given render object has an initialized geometry.
   *
   * @param {RenderObject} renderObject - The render object.
   * @return {boolean} Whether if the given render object has an initialized geometry or not.
   */
  has(e) {
    const t = e.geometry;
    return super.has(t) && this.get(t).initialized === !0;
  }
  /**
   * Prepares the geometry of the given render object for rendering.
   *
   * @param {RenderObject} renderObject - The render object.
   */
  updateForRender(e) {
    this.has(e) === !1 && this.initGeometry(e), this.updateAttributes(e);
  }
  /**
   * Initializes the geometry of the given render object.
   *
   * @param {RenderObject} renderObject - The render object.
   */
  initGeometry(e) {
    const t = e.geometry, s = this.get(t);
    s.initialized = !0, this.info.memory.geometries++;
    const r = () => {
      this.info.memory.geometries--;
      const n = t.index, i = e.getAttributes();
      n !== null && this.attributes.delete(n);
      for (const u of i)
        this.attributes.delete(u);
      const o = this.wireframes.get(t);
      o !== void 0 && this.attributes.delete(o), t.removeEventListener("dispose", r), this._geometryDisposeListeners.delete(t);
    };
    t.addEventListener("dispose", r), this._geometryDisposeListeners.set(t, r);
  }
  /**
   * Updates the geometry attributes of the given render object.
   *
   * @param {RenderObject} renderObject - The render object.
   */
  updateAttributes(e) {
    const t = e.getAttributes();
    for (const n of t)
      n.isStorageBufferAttribute || n.isStorageInstancedBufferAttribute ? this.updateAttribute(n, it.STORAGE) : this.updateAttribute(n, it.VERTEX);
    const s = this.getIndex(e);
    s !== null && this.updateAttribute(s, it.INDEX);
    const r = e.geometry.indirect;
    r !== null && this.updateAttribute(r, it.INDIRECT);
  }
  /**
   * Updates the given attribute.
   *
   * @param {BufferAttribute} attribute - The attribute to update.
   * @param {number} type - The attribute type.
   */
  updateAttribute(e, t) {
    const s = this.info.render.calls;
    e.isInterleavedBufferAttribute ? this.attributeCall.get(e) === void 0 ? (this.attributes.update(e, t), this.attributeCall.set(e, s)) : this.attributeCall.get(e.data) !== s && (this.attributes.update(e, t), this.attributeCall.set(e.data, s), this.attributeCall.set(e, s)) : this.attributeCall.get(e) !== s && (this.attributes.update(e, t), this.attributeCall.set(e, s));
  }
  /**
   * Returns the indirect buffer attribute of the given render object.
   *
   * @param {RenderObject} renderObject - The render object.
   * @return {?BufferAttribute} The indirect attribute. `null` if no indirect drawing is used.
   */
  getIndirect(e) {
    return e.geometry.indirect;
  }
  /**
   * Returns the byte offset into the indirect attribute buffer of the given render object.
   *
   * @param {RenderObject} renderObject - The render object.
   * @return {number} The byte offset into the indirect attribute buffer.
   */
  getIndirectOffset(e) {
    return e.geometry.indirectOffset;
  }
  /**
   * Returns the index of the given render object's geometry. This is implemented
   * in a method to return a wireframe index if necessary.
   *
   * @param {RenderObject} renderObject - The render object.
   * @return {?BufferAttribute} The index. Returns `null` for non-indexed geometries.
   */
  getIndex(e) {
    const { geometry: t, material: s } = e;
    let r = t.index;
    if (s.wireframe === !0) {
      const n = this.wireframes;
      let i = n.get(t);
      i === void 0 ? (i = Xu(t), n.set(t, i)) : (i.version !== qd(t) || i.__id !== Xd(t)) && (this.attributes.delete(i), i = Xu(t), n.set(t, i)), r = i;
    }
    return r;
  }
  dispose() {
    for (const [e, t] of this._geometryDisposeListeners.entries())
      e.removeEventListener("dispose", t);
    this._geometryDisposeListeners.clear();
  }
}
class o_ {
  /**
   * Constructs a new info component.
   */
  constructor() {
    this.autoReset = !0, this.frame = 0, this.calls = 0, this.render = {
      calls: 0,
      frameCalls: 0,
      drawCalls: 0,
      triangles: 0,
      points: 0,
      lines: 0,
      timestamp: 0
    }, this.compute = {
      calls: 0,
      frameCalls: 0,
      timestamp: 0
    }, this.memory = {
      geometries: 0,
      textures: 0,
      attributes: 0,
      indexAttributes: 0,
      storageAttributes: 0,
      indirectStorageAttributes: 0,
      readbackBuffers: 0,
      programs: 0,
      renderTargets: 0,
      total: 0,
      texturesSize: 0,
      attributesSize: 0,
      indexAttributesSize: 0,
      storageAttributesSize: 0,
      indirectStorageAttributesSize: 0,
      readbackBuffersSize: 0,
      programsSize: 0
    }, this.memoryMap = /* @__PURE__ */ new Map();
  }
  /**
   * This method should be executed per draw call and updates the corresponding metrics.
   *
   * @param {Object3D} object - The 3D object that is going to be rendered.
   * @param {number} count - The vertex or index count.
   * @param {number} instanceCount - The instance count.
   */
  update(e, t, s) {
    this.render.drawCalls++, e.isMesh || e.isSprite ? this.render.triangles += s * (t / 3) : e.isPoints ? this.render.points += s * t : e.isLineSegments ? this.render.lines += s * (t / 2) : e.isLine ? this.render.lines += s * (t - 1) : F("WebGPUInfo: Unknown object type.");
  }
  /**
   * Resets frame related metrics.
   */
  reset() {
    this.render.drawCalls = 0, this.render.frameCalls = 0, this.compute.frameCalls = 0, this.render.triangles = 0, this.render.points = 0, this.render.lines = 0;
  }
  /**
   * Performs a complete reset of the object.
   */
  dispose() {
    this.reset(), this.calls = 0, this.render.calls = 0, this.compute.calls = 0, this.render.timestamp = 0, this.compute.timestamp = 0;
    for (const e in this.memory)
      this.memory[e] = 0;
    this.memoryMap.clear();
  }
  /**
   * Tracks texture memory explicitly, updating counts and byte tracking.
   *
   * @param {Texture} texture
   */
  createTexture(e) {
    const t = this._getTextureMemorySize(e);
    this.memoryMap.set(e, t), this.memory.textures++, this.memory.total += t, this.memory.texturesSize += t;
  }
  /**
   * Tracks texture memory explicitly, updating counts and byte tracking.
   *
   * @param {Texture} texture
   */
  destroyTexture(e) {
    const t = this.memoryMap.get(e) || 0;
    this.memoryMap.delete(e), this.memory.textures--, this.memory.total -= t, this.memory.texturesSize -= t;
  }
  /**
   * Tracks attribute memory explicitly, updating counts and byte tracking.
   *
   * @param {BufferAttribute} attribute
   * @param {string} type - type of attribute
   * @private
   */
  _createAttribute(e, t) {
    const s = this._getAttributeMemorySize(e);
    this.memoryMap.set(e, { size: s, type: t }), this.memory[t]++, this.memory.total += s, this.memory[t + "Size"] += s;
  }
  /**
   * Tracks a regular attribute memory explicitly.
   *
   * @param {BufferAttribute} attribute - The attribute to track.
   */
  createAttribute(e) {
    this._createAttribute(e, "attributes");
  }
  /**
   * Tracks an index attribute memory explicitly.
   *
   * @param {BufferAttribute} attribute - The index attribute to track.
   */
  createIndexAttribute(e) {
    this._createAttribute(e, "indexAttributes");
  }
  /**
   * Tracks a storage attribute memory explicitly.
   *
   * @param {BufferAttribute} attribute - The storage attribute to track.
   */
  createStorageAttribute(e) {
    this._createAttribute(e, "storageAttributes");
  }
  /**
   * Tracks an indirect storage attribute memory explicitly.
   *
   * @param {BufferAttribute} attribute - The indirect storage attribute to track.
   */
  createIndirectStorageAttribute(e) {
    this._createAttribute(e, "indirectStorageAttributes");
  }
  /**
   * Tracks attribute memory explicitly, updating counts and byte tracking.
   *
   * @param {BufferAttribute} attribute
   */
  destroyAttribute(e) {
    const t = this.memoryMap.get(e);
    t && (this.memoryMap.delete(e), this.memory[t.type]--, this.memory.total -= t.size, this.memory[t.type + "Size"] -= t.size);
  }
  /**
   * Tracks a readback buffer memory explicitly.
   *
   * @param {ReadbackBuffer} readbackBuffer - The readback buffer to track.
   */
  createReadbackBuffer(e) {
    const t = e.maxByteLength;
    this.memoryMap.set(e, { size: t, type: "readbackBuffers" }), this.memory.readbackBuffers++, this.memory.total += t, this.memory.readbackBuffersSize += t;
  }
  /**
   * Tracks a readback buffer memory explicitly.
   *
   * @param {ReadbackBuffer} readbackBuffer - The readback buffer to track.
   */
  destroyReadbackBuffer(e) {
    const { size: t } = this.memoryMap.get(e);
    this.memoryMap.delete(e), this.memory.readbackBuffers--, this.memory.total -= t, this.memory.readbackBuffersSize -= t;
  }
  /**
   * Tracks program memory explicitly, updating counts and byte tracking.
   *
   * @param {ProgrammableStage} program - The program to track.
   */
  createProgram(e) {
    const t = e.code.length;
    this.memoryMap.set(e, t), this.memory.programs++, this.memory.total += t, this.memory.programsSize += t;
  }
  /**
   * Tracks program memory explicitly, updating counts and byte tracking.
   *
   * @param {Object} program - The program to track.
   */
  destroyProgram(e) {
    const t = this.memoryMap.get(e) || 0;
    this.memoryMap.delete(e), this.memory.programs--, this.memory.total -= t, this.memory.programsSize -= t;
  }
  /**
   * Calculates the memory size of a texture in bytes.
   *
   * @param {Texture} texture - The texture to calculate the size for.
   * @return {number} The calculated size in bytes.
   * @private
   */
  _getTextureMemorySize(e) {
    if (e.isCompressedTexture)
      return 1;
    let t = 1;
    e.type === Ys || e.type === $e ? t = 1 : e.type === js || e.type === Ss || e.type === Oe ? t = 2 : (e.type === Ue || e.type === _e || e.type === De) && (t = 4);
    let s = 4;
    e.format === Pl || e.format === ei || e.format === ti || e.format === Gt || e.format === kt ? s = 1 : e.format === Rt || e.format === si ? s = 2 : (e.format === ri || e.format === Ll) && (s = 3);
    let r = t * s;
    e.type === Il || e.type === Ol ? r = 2 : (e.type === Vt || e.type === na || e.type === ia) && (r = 4);
    const n = e.width || 1, i = e.height || 1, o = e.isCubeTexture ? 6 : e.depth || 1;
    let u = n * i * o * r;
    const l = e.mipmaps;
    if (l && l.length > 0) {
      let c = 0;
      for (let d = 0; d < l.length; d++) {
        const h = l[d];
        if (h.data)
          c += h.data.byteLength;
        else {
          const f = h.width || Math.max(1, n >> d), p = h.height || Math.max(1, i >> d);
          c += f * p * o * r;
        }
      }
      u += c;
    } else e.generateMipmaps && (u = u * 1.333);
    return Math.round(u);
  }
  /**
   * Calculates the memory size of an attribute in bytes.
   *
   * @param {BufferAttribute} attribute - The attribute to calculate the size for.
   * @return {number} The calculated size in bytes.
   * @private
   */
  _getAttributeMemorySize(e) {
    return e.isInterleavedBufferAttribute && (e = e.data), e.array ? e.array.byteLength : e.count && e.itemSize ? e.count * e.itemSize * 4 : 0;
  }
}
class Kd {
  /**
   * Constructs a new pipeline.
   *
   * @param {string} cacheKey - The pipeline's cache key.
   */
  constructor(e) {
    this.cacheKey = e, this.usedTimes = 0;
  }
}
class a_ extends Kd {
  /**
   * Constructs a new render object pipeline.
   *
   * @param {string} cacheKey - The pipeline's cache key.
   * @param {ProgrammableStage} vertexProgram - The pipeline's vertex shader.
   * @param {ProgrammableStage} fragmentProgram - The pipeline's fragment shader.
   */
  constructor(e, t, s) {
    super(e), this.vertexProgram = t, this.fragmentProgram = s;
  }
}
class u_ extends Kd {
  /**
   * Constructs a new compute pipeline.
   *
   * @param {string} cacheKey - The pipeline's cache key.
   * @param {ProgrammableStage} computeProgram - The pipeline's compute shader.
   */
  constructor(e, t) {
    super(e), this.computeProgram = t, this.isComputePipeline = !0;
  }
}
let l_ = 0;
class Xi {
  /**
   * Constructs a new programmable stage.
   *
   * @param {string} code - The shader code.
   * @param {('vertex'|'fragment'|'compute')} stage - The type of stage.
   * @param {string} name - The name of the shader.
   * @param {?Array<Object>} [transforms=null] - The transforms (only relevant for compute stages with WebGL 2 which uses Transform Feedback).
   * @param {?Array<Object>} [attributes=null] - The attributes (only relevant for compute stages with WebGL 2 which uses Transform Feedback).
   */
  constructor(e, t, s, r = null, n = null) {
    this.id = l_++, this.code = e, this.stage = t, this.name = s, this.transforms = r, this.attributes = n, this.usedTimes = 0;
  }
}
class c_ extends ls {
  /**
   * Constructs a new pipeline management component.
   *
   * @param {Backend} backend - The renderer's backend.
   * @param {NodeManager} nodes - Renderer component for managing nodes related logic.
   * @param {Info} info - Renderer component for managing metrics and monitoring data.
   */
  constructor(e, t, s) {
    super(), this.backend = e, this.nodes = t, this.info = s, this.bindings = null, this.caches = /* @__PURE__ */ new Map(), this.programs = {
      vertex: /* @__PURE__ */ new Map(),
      fragment: /* @__PURE__ */ new Map(),
      compute: /* @__PURE__ */ new Map()
    };
  }
  /**
   * Returns a compute pipeline for the given compute node.
   *
   * @param {Node} computeNode - The compute node.
   * @param {Array<BindGroup>} bindings - The bindings.
   * @return {ComputePipeline} The compute pipeline.
   */
  getForCompute(e, t) {
    const { backend: s } = this, r = this.get(e);
    if (this._needsComputeUpdate(e)) {
      const n = r.pipeline;
      n && (n.usedTimes--, n.computeProgram.usedTimes--);
      const i = this.nodes.getForCompute(e);
      let o = this.programs.compute.get(i.computeShader);
      o === void 0 && (n && n.computeProgram.usedTimes === 0 && this._releaseProgram(n.computeProgram), o = new Xi(i.computeShader, "compute", e.name, i.transforms, i.nodeAttributes), this.programs.compute.set(i.computeShader, o), s.createProgram(o), this.info.createProgram(o));
      const u = this._getComputeCacheKey(e, o);
      let l = this.caches.get(u);
      l === void 0 && (n && n.usedTimes === 0 && this._releasePipeline(n), l = this._getComputePipeline(e, o, u, t)), l.usedTimes++, o.usedTimes++, r.version = e.version, r.pipeline = l;
    }
    return r.pipeline;
  }
  /**
   * Returns a render pipeline for the given render object.
   *
   * @param {RenderObject} renderObject - The render object.
   * @param {?Array<Promise>} [promises=null] - An array of compilation promises which is only relevant in context of `Renderer.compileAsync()`.
   * @return {RenderObjectPipeline} The render pipeline.
   */
  getForRender(e, t = null) {
    const { backend: s } = this, r = this.get(e);
    if (this._needsRenderUpdate(e)) {
      const n = r.pipeline;
      n && (n.usedTimes--, n.vertexProgram.usedTimes--, n.fragmentProgram.usedTimes--);
      const i = e.getNodeBuilderState(), o = e.material ? e.material.name : "";
      let u = this.programs.vertex.get(i.vertexShader);
      u === void 0 && (n && n.vertexProgram.usedTimes === 0 && this._releaseProgram(n.vertexProgram), u = new Xi(i.vertexShader, "vertex", o), this.programs.vertex.set(i.vertexShader, u), s.createProgram(u), this.info.createProgram(u));
      let l = this.programs.fragment.get(i.fragmentShader);
      l === void 0 && (n && n.fragmentProgram.usedTimes === 0 && this._releaseProgram(n.fragmentProgram), l = new Xi(i.fragmentShader, "fragment", o), this.programs.fragment.set(i.fragmentShader, l), s.createProgram(l), this.info.createProgram(l));
      const c = this._getRenderCacheKey(e, u, l);
      let d = this.caches.get(c);
      d === void 0 ? (n && n.usedTimes === 0 && this._releasePipeline(n), d = this._getRenderPipeline(e, u, l, c, t)) : e.pipeline = d, d.usedTimes++, u.usedTimes++, l.usedTimes++, r.pipeline = d;
    }
    return r.pipeline;
  }
  /**
   * Checks if the render pipeline for the given render object is ready for drawing.
   * Returns false if the GPU pipeline is still being compiled asynchronously.
   *
   * @param {RenderObject} renderObject - The render object.
   * @return {boolean} True if the pipeline is ready for drawing.
   */
  isReady(e) {
    const s = this.get(e).pipeline;
    if (s === void 0) return !1;
    const r = this.backend.get(s);
    return r.pipeline !== void 0 && r.pipeline !== null;
  }
  /**
   * Deletes the pipeline for the given render object.
   *
   * @param {RenderObject} object - The render object.
   * @return {?Object} The deleted dictionary.
   */
  delete(e) {
    const t = this.get(e).pipeline;
    return t && (t.usedTimes--, t.usedTimes === 0 && this._releasePipeline(t), t.isComputePipeline ? (t.computeProgram.usedTimes--, t.computeProgram.usedTimes === 0 && this._releaseProgram(t.computeProgram)) : (t.fragmentProgram.usedTimes--, t.vertexProgram.usedTimes--, t.vertexProgram.usedTimes === 0 && this._releaseProgram(t.vertexProgram), t.fragmentProgram.usedTimes === 0 && this._releaseProgram(t.fragmentProgram))), super.delete(e);
  }
  /**
   * Frees internal resources.
   */
  dispose() {
    super.dispose(), this.caches = /* @__PURE__ */ new Map(), this.programs = {
      vertex: /* @__PURE__ */ new Map(),
      fragment: /* @__PURE__ */ new Map(),
      compute: /* @__PURE__ */ new Map()
    };
  }
  /**
   * Updates the pipeline for the given render object.
   *
   * @param {RenderObject} renderObject - The render object.
   */
  updateForRender(e) {
    this.getForRender(e);
  }
  /**
   * Returns a compute pipeline for the given parameters.
   *
   * @private
   * @param {Node} computeNode - The compute node.
   * @param {ProgrammableStage} stageCompute - The programmable stage representing the compute shader.
   * @param {string} cacheKey - The cache key.
   * @param {Array<BindGroup>} bindings - The bindings.
   * @return {ComputePipeline} The compute pipeline.
   */
  _getComputePipeline(e, t, s, r) {
    s = s || this._getComputeCacheKey(e, t);
    let n = this.caches.get(s);
    return n === void 0 && (n = new u_(s, t), this.caches.set(s, n), this.backend.createComputePipeline(n, r)), n;
  }
  /**
   * Returns a render pipeline for the given parameters.
   *
   * @private
   * @param {RenderObject} renderObject - The render object.
   * @param {ProgrammableStage} stageVertex - The programmable stage representing the vertex shader.
   * @param {ProgrammableStage} stageFragment - The programmable stage representing the fragment shader.
   * @param {string} cacheKey - The cache key.
   * @param {?Array<Promise>} promises - An array of compilation promises which is only relevant in context of `Renderer.compileAsync()`.
   * @return {RenderObjectPipeline} The render pipeline.
   */
  _getRenderPipeline(e, t, s, r, n) {
    r = r || this._getRenderCacheKey(e, t, s);
    let i = this.caches.get(r);
    return i === void 0 && (i = new a_(r, t, s), this.caches.set(r, i), e.pipeline = i, this.backend.createRenderPipeline(e, n)), i;
  }
  /**
   * Computes a cache key representing a compute pipeline.
   *
   * @private
   * @param {Node} computeNode - The compute node.
   * @param {ProgrammableStage} stageCompute - The programmable stage representing the compute shader.
   * @return {string} The cache key.
   */
  _getComputeCacheKey(e, t) {
    return e.id + "," + t.id;
  }
  /**
   * Computes a cache key representing a render pipeline.
   *
   * @private
   * @param {RenderObject} renderObject - The render object.
   * @param {ProgrammableStage} stageVertex - The programmable stage representing the vertex shader.
   * @param {ProgrammableStage} stageFragment - The programmable stage representing the fragment shader.
   * @return {string} The cache key.
   */
  _getRenderCacheKey(e, t, s) {
    return t.id + "," + s.id + "," + this.backend.getRenderCacheKey(e);
  }
  /**
   * Releases the given pipeline.
   *
   * @private
   * @param {Pipeline} pipeline - The pipeline to release.
   */
  _releasePipeline(e) {
    this.caches.delete(e.cacheKey);
  }
  /**
   * Releases the shader program.
   *
   * @private
   * @param {Object} program - The shader program to release.
   */
  _releaseProgram(e) {
    const t = e.code, s = e.stage;
    this.programs[s].delete(t), this.info.destroyProgram(e);
  }
  /**
   * Returns `true` if the compute pipeline for the given compute node requires an update.
   *
   * @private
   * @param {Node} computeNode - The compute node.
   * @return {boolean} Whether the compute pipeline for the given compute node requires an update or not.
   */
  _needsComputeUpdate(e) {
    const t = this.get(e);
    return t.pipeline === void 0 || t.version !== e.version;
  }
  /**
   * Returns `true` if the render pipeline for the given render object requires an update.
   *
   * @private
   * @param {RenderObject} renderObject - The render object.
   * @return {boolean} Whether the render object for the given render object requires an update or not.
   */
  _needsRenderUpdate(e) {
    return this.get(e).pipeline === void 0 || this.backend.needsRenderUpdate(e);
  }
}
class d_ extends ls {
  /**
   * Constructs a new bindings management component.
   *
   * @param {Backend} backend - The renderer's backend.
   * @param {NodeManager} nodes - Renderer component for managing nodes related logic.
   * @param {Textures} textures - Renderer component for managing textures.
   * @param {Attributes} attributes - Renderer component for managing attributes.
   * @param {Pipelines} pipelines - Renderer component for managing pipelines.
   * @param {Info} info - Renderer component for managing metrics and monitoring data.
   */
  constructor(e, t, s, r, n, i) {
    super(), this.backend = e, this.textures = s, this.pipelines = n, this.attributes = r, this.nodes = t, this.info = i, this.pipelines.bindings = this;
  }
  /**
   * Returns the bind groups for the given render object.
   *
   * @param {RenderObject} renderObject - The render object.
   * @return {Array<BindGroup>} The bind groups.
   */
  getForRender(e) {
    const t = e.getBindings();
    for (const s of t) {
      const r = this.get(s);
      r.bindGroup === void 0 && (this._init(s), this.backend.createBindings(s, t, 0), r.bindGroup = s);
    }
    return t;
  }
  /**
   * Returns the bind groups for the given compute node.
   *
   * @param {Node} computeNode - The compute node.
   * @return {Array<BindGroup>} The bind groups.
   */
  getForCompute(e) {
    const t = this.nodes.getForCompute(e).bindings;
    for (const s of t) {
      const r = this.get(s);
      r.bindGroup === void 0 && (this._init(s), this.backend.createBindings(s, t, 0), r.bindGroup = s);
    }
    return t;
  }
  /**
   * Updates the bindings for the given compute node.
   *
   * @param {Node} computeNode - The compute node.
   */
  updateForCompute(e) {
    this._updateBindings(this.getForCompute(e));
  }
  /**
   * Updates the bindings for the given render object.
   *
   * @param {RenderObject} renderObject - The render object.
   */
  updateForRender(e) {
    this._updateBindings(this.getForRender(e));
  }
  /**
   * Deletes the bindings for the given compute node.
   *
   * @param {Node} computeNode - The compute node.
   */
  deleteForCompute(e) {
    const t = this.nodes.getForCompute(e).bindings;
    for (const s of t)
      this.backend.deleteBindGroupData(s), this.delete(s);
  }
  /**
   * Deletes the bindings for the given renderObject node.
   *
   * @param {RenderObject} renderObject - The renderObject.
   */
  deleteForRender(e) {
    const t = e.getBindings();
    for (const s of t)
      this.backend.deleteBindGroupData(s), this.delete(s);
  }
  /**
   * Updates the given array of bindings.
   *
   * @param {Array<BindGroup>} bindings - The bind groups.
   */
  _updateBindings(e) {
    for (const t of e)
      this._update(t, e);
  }
  /**
   * Initializes the given bind group.
   *
   * @param {BindGroup} bindGroup - The bind group to initialize.
   */
  _init(e) {
    for (const t of e.bindings)
      if (t.isSampledTexture)
        this.textures.updateTexture(t.texture);
      else if (t.isSampler)
        this.textures.updateSampler(t.texture);
      else if (t.isStorageBuffer) {
        const s = t.attribute, r = s.isIndirectStorageBufferAttribute ? it.INDIRECT : it.STORAGE;
        this.attributes.update(s, r);
      }
  }
  /**
   * Updates the given bind group.
   *
   * @param {BindGroup} bindGroup - The bind group to update.
   * @param {Array<BindGroup>} bindings - The bind groups.
   */
  _update(e, t) {
    const { backend: s } = this;
    let r = !1, n = !0, i = 0, o = 0;
    for (const u of e.bindings)
      if (this.nodes.updateGroup(u) !== !1) {
        if (u.isStorageBuffer) {
          const c = u.attribute, d = c.isIndirectStorageBufferAttribute ? it.INDIRECT : it.STORAGE, h = s.get(u);
          this.attributes.update(c, d), h.attribute !== c && (h.attribute = c, r = !0);
        }
        if (u.isUniformBuffer)
          u.update() && s.updateBinding(u);
        else if (u.isSampledTexture) {
          const c = u.update(), d = u.texture, h = this.textures.get(d);
          if (c && (this.textures.updateTexture(d), u.generation !== h.generation && (u.generation = h.generation, r = !0), h.bindGroups.add(e)), s.get(d).externalTexture !== void 0 || h.isDefaultTexture ? n = !1 : (i = i * 10 + d.id, o += d.version), d.isStorageTexture === !0 && d.mipmapsAutoUpdate === !0) {
            const p = this.get(d);
            u.store === !0 ? p.needsMipmap = !0 : this.textures.needsMipmaps(d) && p.needsMipmap === !0 && (this.backend.generateMipmaps(d), p.needsMipmap = !1);
          }
        } else if (u.isSampler && u.update()) {
          const d = this.textures.updateSampler(u.texture);
          u.samplerKey !== d && (u.samplerKey = d, r = !0);
        }
        u.isBuffer && u.updateRanges.length > 0 && u.clearUpdateRanges();
      }
    r === !0 && this.backend.updateBindings(e, t, n ? i : 0, o);
  }
}
function h_(a, e) {
  return a.groupOrder !== e.groupOrder ? a.groupOrder - e.groupOrder : a.renderOrder !== e.renderOrder ? a.renderOrder - e.renderOrder : a.z !== e.z ? a.z - e.z : a.id - e.id;
}
function Ku(a, e) {
  return a.groupOrder !== e.groupOrder ? a.groupOrder - e.groupOrder : a.renderOrder !== e.renderOrder ? a.renderOrder - e.renderOrder : a.z !== e.z ? e.z - a.z : a.id - e.id;
}
function Qu(a) {
  return (a.transmission > 0 || a.transmissionNode && a.transmissionNode.isNode) && a.side === Ot && a.forceSinglePass === !1;
}
class f_ {
  /**
   * Constructs a render list.
   *
   * @param {Lighting} lighting - The lighting management component.
   * @param {Scene} scene - The scene.
   * @param {Camera} camera - The camera the scene is rendered with.
   */
  constructor(e, t, s) {
    this.renderItems = [], this.renderItemsIndex = 0, this.opaque = [], this.transparentDoublePass = [], this.transparent = [], this.bundles = [], this.lightsNode = e.getNode(t, s), this.lightsArray = [], this.scene = t, this.camera = s, this.occlusionQueryCount = 0;
  }
  /**
   * This method is called right at the beginning of a render call
   * before the scene is analyzed. It prepares the internal data
   * structures for the upcoming render lists generation.
   *
   * @return {RenderList} A reference to this render list.
   */
  begin() {
    return this.renderItemsIndex = 0, this.opaque.length = 0, this.transparentDoublePass.length = 0, this.transparent.length = 0, this.bundles.length = 0, this.lightsArray.length = 0, this.occlusionQueryCount = 0, this;
  }
  /**
   * Returns a render item for the giving render item state. The state is defined
   * by a series of object-related parameters.
   *
   * The method avoids object creation by holding render items and reusing them in
   * subsequent render calls (just with different property values).
   *
   * @param {Object3D} object - The 3D object.
   * @param {BufferGeometry} geometry - The 3D object's geometry.
   * @param {Material} material - The 3D object's material.
   * @param {number} groupOrder - The current group order.
   * @param {number} z - Th 3D object's depth value (z value in clip space).
   * @param {?number} group - {?Object} group - Only relevant for objects using multiple materials. This represents a group entry from the respective `BufferGeometry`.
   * @param {ClippingContext} clippingContext - The current clipping context.
   * @return {Object} The render item.
   */
  getNextRenderItem(e, t, s, r, n, i, o) {
    let u = this.renderItems[this.renderItemsIndex];
    return u === void 0 ? (u = {
      id: e.id,
      object: e,
      geometry: t,
      material: s,
      groupOrder: r,
      renderOrder: e.renderOrder,
      z: n,
      group: i,
      clippingContext: o
    }, this.renderItems[this.renderItemsIndex] = u) : (u.id = e.id, u.object = e, u.geometry = t, u.material = s, u.groupOrder = r, u.renderOrder = e.renderOrder, u.z = n, u.group = i, u.clippingContext = o), this.renderItemsIndex++, u;
  }
  /**
   * Pushes the given object as a render item to the internal render lists.
   * The selected lists depend on the object properties.
   *
   * @param {Object3D} object - The 3D object.
   * @param {BufferGeometry} geometry - The 3D object's geometry.
   * @param {Material} material - The 3D object's material.
   * @param {number} groupOrder - The current group order.
   * @param {number} z - Th 3D object's depth value (z value in clip space).
   * @param {?number} group - {?Object} group - Only relevant for objects using multiple materials. This represents a group entry from the respective `BufferGeometry`.
   * @param {ClippingContext} clippingContext - The current clipping context.
   */
  push(e, t, s, r, n, i, o) {
    const u = this.getNextRenderItem(e, t, s, r, n, i, o);
    e.occlusionTest === !0 && this.occlusionQueryCount++, s.transparent === !0 || s.transmission > 0 || s.transmissionNode && s.transmissionNode.isNode || s.backdropNode && s.backdropNode.isNode ? (Qu(s) && this.transparentDoublePass.push(u), this.transparent.push(u)) : this.opaque.push(u);
  }
  /**
   * Inserts the given object as a render item at the start of the internal render lists.
   * The selected lists depend on the object properties.
   *
   * @param {Object3D} object - The 3D object.
   * @param {BufferGeometry} geometry - The 3D object's geometry.
   * @param {Material} material - The 3D object's material.
   * @param {number} groupOrder - The current group order.
   * @param {number} z - Th 3D object's depth value (z value in clip space).
   * @param {?number} group - {?Object} group - Only relevant for objects using multiple materials. This represents a group entry from the respective `BufferGeometry`.
   * @param {ClippingContext} clippingContext - The current clipping context.
   */
  unshift(e, t, s, r, n, i, o) {
    const u = this.getNextRenderItem(e, t, s, r, n, i, o);
    s.transparent === !0 || s.transmission > 0 || s.transmissionNode && s.transmissionNode.isNode || s.backdropNode && s.backdropNode.isNode ? (Qu(s) && this.transparentDoublePass.unshift(u), this.transparent.unshift(u)) : this.opaque.unshift(u);
  }
  /**
   * Pushes render bundle group data into the render list.
   *
   * @param {Object} group - Bundle group data.
   */
  pushBundle(e) {
    this.bundles.push(e);
  }
  /**
   * Pushes a light into the render list.
   *
   * @param {Light} light - The light.
   */
  pushLight(e) {
    this.lightsArray.push(e);
  }
  /**
   * Sorts the internal render lists.
   *
   * @param {?function(any, any): number} customOpaqueSort - A custom sort function for opaque objects.
   * @param {?function(any, any): number} customTransparentSort -  A custom sort function for transparent objects.
   */
  sort(e, t) {
    this.opaque.length > 1 && this.opaque.sort(e || h_), this.transparentDoublePass.length > 1 && this.transparentDoublePass.sort(t || Ku), this.transparent.length > 1 && this.transparent.sort(t || Ku);
  }
  /**
   * This method performs finalizing tasks right after the render lists
   * have been generated.
   */
  finish() {
    this.lightsNode.setLights(this.lightsArray);
    for (let e = this.renderItemsIndex, t = this.renderItems.length; e < t; e++) {
      const s = this.renderItems[e];
      if (s.id === null) break;
      s.id = null, s.object = null, s.geometry = null, s.material = null, s.groupOrder = null, s.renderOrder = null, s.z = null, s.group = null, s.clippingContext = null;
    }
  }
}
const ks = [];
class p_ {
  /**
   * Constructs a render lists management component.
   *
   * @param {Lighting} lighting - The lighting management component.
   */
  constructor(e) {
    this.lighting = e, this.lists = new us();
  }
  /**
   * Returns a render list for the given scene and camera.
   *
   * @param {Scene} scene - The scene.
   * @param {Camera} camera - The camera.
   * @return {RenderList} The render list.
   */
  get(e, t) {
    const s = this.lists;
    ks[0] = e, ks[1] = t;
    let r = s.get(ks);
    return r === void 0 && (r = new f_(this.lighting, e, t), s.set(ks, r)), ks[0] = null, ks[1] = null, r;
  }
  /**
   * Frees all internal resources.
   */
  dispose() {
    this.lists = new us();
  }
}
let g_ = 0;
class m_ {
  /**
   * Constructs a new render context.
   */
  constructor() {
    this.id = g_++, this.mrt = null, this.color = !0, this.clearColor = !0, this.clearColorValue = { r: 0, g: 0, b: 0, a: 1 }, this.depth = !0, this.clearDepth = !0, this.clearDepthValue = 1, this.stencil = !1, this.clearStencil = !0, this.clearStencilValue = 1, this.viewport = !1, this.viewportValue = new Se(), this.scissor = !1, this.scissorValue = new Se(), this.renderTarget = null, this.textures = null, this.depthTexture = null, this.activeCubeFace = 0, this.activeMipmapLevel = 0, this.sampleCount = 1, this.width = 0, this.height = 0, this.occlusionQueryCount = 0, this.clippingContext = null, this.camera = null, this.isRenderContext = !0;
  }
  /**
   * Returns the cache key of this render context.
   *
   * @return {number} The cache key.
   */
  getCacheKey() {
    return Qd(this);
  }
}
function Qd(a) {
  const { textures: e, activeCubeFace: t, activeMipmapLevel: s } = a, r = [t, s];
  for (const n of e)
    r.push(n.id);
  return Yr(r);
}
class x_ {
  /**
   * Constructs a new render context management component.
   *
   * @param {Renderer} renderer - The renderer.
   */
  constructor(e) {
    this.renderer = e, this._renderContexts = {};
  }
  /**
   * Returns a render context for the given scene, camera and render target.
   *
   * @param {?RenderTarget} [renderTarget=null] - The active render target.
   * @param {?MRTNode} [mrt=null] - The MRT configuration
   * @param {?number} [callDepth=0] - The call depth of the renderer.
   * @return {RenderContext} The render context.
   */
  get(e = null, t = null, s = 0) {
    let r;
    if (e === null)
      r = "default";
    else {
      const u = e.texture.format, l = e.texture.type;
      r = `${e.textures.length}:${u}:${l}:${e.samples}:${e.depthBuffer}:${e.stencilBuffer}`;
    }
    const n = t !== null ? t.id : "default", i = r + "-" + n + "-" + s;
    let o = this._renderContexts[i];
    return o === void 0 && (o = new m_(), o.mrt = t, this._renderContexts[i] = o), e !== null && (o.sampleCount = e.samples === 0 ? 1 : e.samples), o.clearDepthValue = this.renderer.getClearDepth(), o.clearStencilValue = this.renderer.getClearStencil(), o;
  }
  /**
   * Frees internal resources.
   */
  dispose() {
    this._renderContexts = {};
  }
}
const T_ = /* @__PURE__ */ new H();
class y_ extends ls {
  /**
   * Constructs a new texture management component.
   *
   * @param {Renderer} renderer - The renderer.
   * @param {Backend} backend - The renderer's backend.
   * @param {Info} info - Renderer component for managing metrics and monitoring data.
   */
  constructor(e, t, s) {
    super(), this.renderer = e, this.backend = t, this.info = s, this._htmlTextures = /* @__PURE__ */ new Set();
  }
  /**
   * Updates the given render target. Based on the given render target configuration,
   * it updates the texture states representing the attachments of the framebuffer.
   *
   * @param {RenderTarget} renderTarget - The render target to update.
   * @param {number} [activeMipmapLevel=0] - The active mipmap level.
   */
  updateRenderTarget(e, t = 0) {
    const s = this.get(e), r = e.samples === 0 ? 1 : e.samples, n = s.depthTextureMips || (s.depthTextureMips = {}), i = e.textures, o = this.getSize(i[0]), u = o.width >> t, l = o.height >> t;
    let c = e.depthTexture || n[t];
    const d = e.depthBuffer === !0 || e.stencilBuffer === !0;
    let h = !1;
    c === void 0 && d && (c = new vt(), c.format = e.stencilBuffer ? kt : Gt, c.type = e.stencilBuffer ? Vt : _e, c.image.width = u, c.image.height = l, c.image.depth = o.depth, c.renderTarget = e, c.isArrayTexture = e.multiview === !0 && o.depth > 1, n[t] = c), (s.width !== o.width || o.height !== s.height) && (h = !0, c && (c.needsUpdate = !0, c.image.width = u, c.image.height = l, c.image.depth = c.isArrayTexture ? c.image.depth : 1)), s.width = o.width, s.height = o.height, s.textures = i, s.depthTexture = c || null, s.depth = e.depthBuffer, s.stencil = e.stencilBuffer, s.renderTarget = e, s.sampleCount !== r && (h = !0, c && (c.needsUpdate = !0), s.sampleCount = r);
    const f = { sampleCount: r };
    if (e.isXRRenderTarget !== !0) {
      for (let p = 0; p < i.length; p++) {
        const g = i[p];
        h && (g.needsUpdate = !0), this.updateTexture(g, f);
      }
      c && this.updateTexture(c, f);
    }
    s.initialized !== !0 && (s.initialized = !0, this.info.memory.renderTargets++, s.onDispose = () => {
      this._destroyRenderTarget(e);
    }, e.addEventListener("dispose", s.onDispose));
  }
  /**
   * Updates the given texture. Depending on the texture state, this method
   * triggers the upload of texture data to the GPU memory. If the texture data are
   * not yet ready for the upload, it uses default texture data for as a placeholder.
   *
   * @param {Texture} texture - The texture to update.
   * @param {Object} [options={}] - The options.
   */
  updateTexture(e, t = {}) {
    const s = this.get(e);
    if (s.initialized === !0 && s.version === e.version) return;
    const r = e.isRenderTargetTexture || e.isDepthTexture || e.isFramebufferTexture, n = this.backend;
    if (r && s.initialized === !0 && n.destroyTexture(e), e.isFramebufferTexture) {
      const l = this.renderer.getRenderTarget();
      l ? e.type = l.texture.type : e.type = $e;
    }
    if (e.isHTMLTexture && e.image) {
      const l = this.renderer.domElement;
      if ("requestPaint" in l) {
        if (l.hasAttribute("layoutsubtree") || l.setAttribute("layoutsubtree", "true"), e.image.parentNode !== l && l.appendChild(e.image), this._htmlTextures.size === 0) {
          const c = this._htmlTextures;
          l.onpaint = (d) => {
            const h = d && d.changedElements;
            for (const f of c)
              (!h || h.includes(f.image)) && (f.needsUpdate = !0);
          };
        }
        this._htmlTextures.add(e);
      }
    }
    const { width: i, height: o, depth: u } = this.getSize(e);
    if (t.width = i, t.height = o, t.depth = u, t.needsMipmaps = this.needsMipmaps(e), t.levels = t.needsMipmaps ? this.getMipLevels(e, i, o) : 1, e.isCubeTexture && e.mipmaps.length > 0 && t.levels++, r || e.isStorageTexture === !0 || e.isExternalTexture === !0)
      n.createTexture(e, t), s.generation = e.version;
    else if (e.version > 0) {
      const l = e.image;
      if (l === void 0)
        L("Renderer: Texture marked for update but image is undefined.");
      else if (l.complete === !1)
        L("Renderer: Texture marked for update but image is incomplete.");
      else {
        if (e.images) {
          const d = [];
          for (const h of e.images)
            d.push(h);
          t.images = d;
        } else
          t.image = l;
        (s.isDefaultTexture === void 0 || s.isDefaultTexture === !0) && (n.createTexture(e, t), s.isDefaultTexture = !1, s.generation = e.version), e.source.dataReady === !0 && n.updateTexture(e, t);
        const c = e.isStorageTexture === !0 && e.mipmapsAutoUpdate === !1;
        t.needsMipmaps && e.mipmaps.length === 0 && !c && n.generateMipmaps(e), e.onUpdate && e.onUpdate(e);
      }
    } else
      n.createDefaultTexture(e), s.isDefaultTexture = !0, s.generation = e.version;
    s.initialized !== !0 && (s.initialized = !0, s.generation = e.version, s.bindGroups = /* @__PURE__ */ new Set(), this.info.createTexture(e), e.isVideoTexture && ye.enabled === !0 && ye.getTransfer(e.colorSpace) !== q && L("WebGPURenderer: Video textures must use a color space with a sRGB transfer function, e.g. SRGBColorSpace."), s.onDispose = () => {
      this._destroyTexture(e);
    }, e.addEventListener("dispose", s.onDispose)), s.version = e.version;
  }
  /**
   * Updates the sampler for the given texture. This method has no effect
   * for the WebGL backend since it has no concept of samplers. Texture
   * parameters are configured with the `texParameter()` command for each
   * texture.
   *
   * In WebGPU, samplers are objects like textures and it's possible to share
   * them when the texture parameters match.
   *
   * @param {Texture} texture - The texture to update the sampler for.
   * @return {string} The current sampler key.
   */
  updateSampler(e) {
    return this.backend.updateSampler(e);
  }
  /**
   * Computes the size of the given texture and writes the result
   * into the target vector. This vector is also returned by the
   * method.
   *
   * If no texture data are available for the compute yet, the method
   * returns default size values.
   *
   * @param {Texture} texture - The texture to compute the size for.
   * @param {Vector3} target - The target vector.
   * @return {Vector3} The target vector.
   */
  getSize(e, t = T_) {
    let s = e.images ? e.images[0] : e.image;
    return s ? (s.image !== void 0 && (s = s.image), e.isHTMLTexture ? (t.width = s.offsetWidth || 1, t.height = s.offsetHeight || 1, t.depth = 1) : typeof HTMLVideoElement < "u" && s instanceof HTMLVideoElement ? (t.width = s.videoWidth || 1, t.height = s.videoHeight || 1, t.depth = 1) : typeof VideoFrame < "u" && s instanceof VideoFrame ? (t.width = s.displayWidth || 1, t.height = s.displayHeight || 1, t.depth = 1) : (t.width = s.width || 1, t.height = s.height || 1, t.depth = e.isCubeTexture ? 6 : s.depth || 1)) : t.width = t.height = t.depth = 1, t;
  }
  /**
   * Computes the number of mipmap levels for the given texture.
   *
   * @param {Texture} texture - The texture.
   * @param {number} width - The texture's width.
   * @param {number} height - The texture's height.
   * @return {number} The number of mipmap levels.
   */
  getMipLevels(e, t, s) {
    let r;
    return e.mipmaps.length > 0 ? r = e.mipmaps.length : e.isCompressedTexture === !0 ? r = 1 : r = Math.floor(Math.log2(Math.max(t, s))) + 1, r;
  }
  /**
   * Returns `true` if the given texture makes use of mipmapping.
   *
   * @param {Texture} texture - The texture.
   * @return {boolean} Whether mipmaps are required or not.
   */
  needsMipmaps(e) {
    return e.generateMipmaps === !0 || e.mipmaps.length > 0;
  }
  /**
   * Frees internal resources when the given render target isn't
   * required anymore.
   *
   * @param {RenderTarget} renderTarget - The render target to destroy.
   */
  _destroyRenderTarget(e) {
    if (this.has(e) === !0) {
      const t = this.get(e), s = t.textures, r = t.depthTexture;
      e.removeEventListener("dispose", t.onDispose);
      for (let n = 0; n < s.length; n++)
        this._destroyTexture(s[n]);
      r && this._destroyTexture(r), this.delete(e), this.backend.delete(e), this.info.memory.renderTargets--;
    }
  }
  /**
   * Frees internal resource when the given texture isn't
   * required anymore.
   *
   * @param {Texture} texture - The texture to destroy.
   */
  _destroyTexture(e) {
    if (this.has(e) === !0) {
      const t = this.get(e);
      e.removeEventListener("dispose", t.onDispose);
      const s = t.isDefaultTexture;
      if (this.backend.destroyTexture(e, s), t.bindGroups)
        for (const r of t.bindGroups) {
          const n = this.backend.get(r);
          n.groups = void 0, n.versions = void 0;
        }
      this._htmlTextures.delete(e), this.delete(e), this.info.destroyTexture(e);
    }
  }
}
class Pa extends Ht {
  /**
   * Constructs a new four-component color.
   * You can also pass a single THREE.Color, hex or
   * string argument to this constructor.
   *
   * @param {number|string} [r=1] - The red value.
   * @param {number} [g=1] - The green value.
   * @param {number} [b=1] - The blue value.
   * @param {number} [a=1] - The alpha value.
   */
  constructor(e, t, s, r = 1) {
    super(e, t, s), this.a = r;
  }
  /**
   * Overwrites the default to honor alpha.
   * You can also pass a single THREE.Color, hex or
   * string argument to this method.
   *
   * @param {number|string|Color} r - The red value.
   * @param {number} [g] - The green value.
   * @param {number} [b] - The blue value.
   * @param {number} [a=1] - The alpha value.
   * @return {Color4} A reference to this object.
   */
  set(e, t, s, r = 1) {
    return this.a = r, super.set(e, t, s);
  }
  /**
   * Overwrites the default to honor alpha.
   *
   * @param {Color4} color - The color to copy.
   * @return {Color4} A reference to this object.
   */
  copy(e) {
    return e.a !== void 0 && (this.a = e.a), super.copy(e);
  }
  /**
   * Overwrites the default to honor alpha.
   *
   * @return {Color4} The cloned color.
   */
  clone() {
    return new this.constructor(this.r, this.g, this.b, this.a);
  }
}
class __ extends ee {
  static get type() {
    return "ParameterNode";
  }
  /**
   * Constructs a new parameter node.
   *
   * @param {string} nodeType - The type of the node.
   * @param {?string} [name=null] - The name of the parameter in the shader.
   */
  constructor(e, t = null) {
    super(e, t), this.isParameterNode = !0;
  }
  /**
   * Gets the type of a member variable in the parameter node.
   *
   * @param {NodeBuilder} builder - The node builder.
   * @param {string} name - The name of the member variable.
   * @returns {string}
   */
  getMemberType(e, t) {
    const s = this.getNodeType(e), r = e.getStructTypeNode(s);
    let n;
    return r !== null ? n = r.getMemberType(e, t) : (F(`TSL: Member "${t}" not found in struct "${s}".`, new Pe()), n = "float"), n;
  }
  getHash() {
    return String(this.id);
  }
  generate() {
    return this.name;
  }
}
class b_ extends G {
  static get type() {
    return "StackNode";
  }
  /**
   * Constructs a new stack node.
   *
   * @param {?StackNode} [parent=null] - The parent stack node.
   */
  constructor(e = null) {
    super(), this.nodes = [], this.outputNode = null, this.parent = e, this._currentCond = null, this._expressionNode = null, this._currentNode = null, this.isStackNode = !0;
  }
  getElementType(e) {
    return this.hasOutput(e) ? this.outputNode.getElementType(e) : "void";
  }
  generateNodeType(e) {
    return this.hasOutput(e) ? this.outputNode.getNodeType(e) : "void";
  }
  getMemberType(e, t) {
    return this.hasOutput(e) ? this.outputNode.getMemberType(e, t) : "void";
  }
  /**
   * Adds a node to this stack.
   *
   * @param {Node} node - The node to add.
   * @param {number} [index=this.nodes.length] - The index where the node should be added.
   * @return {StackNode} A reference to this stack node.
   */
  addToStack(e, t = this.nodes.length) {
    return e.isNode !== !0 ? (F("TSL: Invalid node added to stack.", new Pe()), this) : (this.nodes.splice(t, 0, e), this);
  }
  /**
   * Adds a node to the stack before the current node.
   *
   * @param {Node} node - The node to add.
   * @return {StackNode} A reference to this stack node.
   */
  addToStackBefore(e) {
    const t = this._currentNode ? this.nodes.indexOf(this._currentNode) : 0;
    return this.addToStack(e, t);
  }
  /**
   * Represent an `if` statement in TSL.
   *
   * @param {Node} boolNode - Represents the condition.
   * @param {Function} method - TSL code which is executed if the condition evaluates to `true`.
   * @return {StackNode} A reference to this stack node.
   */
  If(e, t) {
    const s = new Cr(t);
    return this._currentCond = Ye(e, s), this.addToStack(this._currentCond);
  }
  /**
   * Represent an `elseif` statement in TSL.
   *
   * @param {Node} boolNode - Represents the condition.
   * @param {Function} method - TSL code which is executed if the condition evaluates to `true`.
   * @return {StackNode} A reference to this stack node.
   */
  ElseIf(e, t) {
    const s = new Cr(t), r = Ye(e, s);
    return this._currentCond.elseNode = r, this._currentCond = r, this;
  }
  /**
   * Represent an `else` statement in TSL.
   *
   * @param {Function} method - TSL code which is executed in the `else` case.
   * @return {StackNode} A reference to this stack node.
   */
  Else(e) {
    return this._currentCond.elseNode = new Cr(e), this;
  }
  /**
   * Represents a `switch` statement in TSL.
   *
   * @param {any} expression - Represents the expression.
   * @param {Function} method - TSL code which is executed if the condition evaluates to `true`.
   * @return {StackNode} A reference to this stack node.
   */
  Switch(e) {
    return this._expressionNode = O(e), this;
  }
  /**
   * Represents a `case` statement in TSL. The TSL version accepts an arbitrary numbers of values.
   * The last parameter must be the callback method that should be executed in the `true` case.
   *
   * @param {...any} params - The values of the `Case()` statement as well as the callback method.
   * @return {StackNode} A reference to this stack node.
   */
  Case(...e) {
    const t = [];
    if (e.length >= 2)
      for (let o = 0; o < e.length - 1; o++)
        t.push(this._expressionNode.equal(O(e[o])));
    else
      F("TSL: Invalid parameter length. Case() requires at least two parameters.", new Pe());
    const s = e[e.length - 1], r = new Cr(s);
    let n = t[0];
    for (let o = 1; o < t.length; o++)
      n = n.or(t[o]);
    const i = Ye(n, r);
    return this._currentCond === null ? (this._currentCond = i, this.addToStack(this._currentCond)) : (this._currentCond.elseNode = i, this._currentCond = i, this);
  }
  /**
   * Represents the default code block of a Switch/Case statement.
   *
   * @param {Function} method - TSL code which is executed in the `else` case.
   * @return {StackNode} A reference to this stack node.
   */
  Default(e) {
    return this.Else(e), this;
  }
  setup(e) {
    const t = e.getNodeProperties(this);
    let s = 0;
    for (const r of this.getChildren())
      r.isVarNode && r.isIntent(e) && r.isAssign(e) !== !0 || (t["node" + s++] = r);
    return t.outputNode || null;
  }
  hasOutput(e) {
    return this.outputNode && this.outputNode.isNode && this.outputNode.getNodeType(e) !== "void";
  }
  build(e, ...t) {
    const s = Rc(), r = e.buildStage;
    Wn(this), e.setActiveStack(this);
    const n = (l) => {
      if (this._currentNode = l, !(l.isVarNode && l.isIntent(e) && l.isAssign(e) !== !0)) {
        if (r === "setup")
          l.build(e);
        else if (r === "analyze")
          l.build(e, this);
        else if (r === "generate") {
          const c = e.getDataFromNode(l, "any").stages, d = c && c[e.shaderStage];
          if (l.isVarNode && d && d.length === 1 && d[0] && d[0].isStackNode)
            return;
          l.build(e, "void");
        }
      }
    }, i = [...this.nodes];
    for (const l of i)
      n(l);
    this._currentNode = null;
    const o = this.nodes.filter((l) => i.indexOf(l) === -1);
    for (const l of o)
      n(l);
    let u;
    return this.hasOutput(e) ? u = this.outputNode.build(e, ...t) : u = super.build(e, ...t), Wn(s), e.removeActiveStack(this), u;
  }
}
const Ki = /* @__PURE__ */ le(b_).setParameterLength(0, 1);
class Yd extends Re {
  static get type() {
    return "BitcastNode";
  }
  /**
   * Constructs a new bitcast node.
   *
   * @param {Node} valueNode - The value to convert.
   * @param {string} conversionType - The type to convert to.
   * @param {?string} [inputType = null] - The expected input data type of the bitcast operation.
   */
  constructor(e, t, s = null) {
    super(), this.valueNode = e, this.conversionType = t, this.inputType = s, this.isBitcastNode = !0;
  }
  generateNodeType(e) {
    if (this.inputType !== null) {
      const t = this.valueNode.getNodeType(e), s = e.getTypeLength(t);
      return e.getTypeFromLength(s, this.conversionType);
    }
    return this.conversionType;
  }
  generate(e) {
    const t = this.getNodeType(e);
    let s = "";
    if (this.inputType !== null) {
      const r = this.valueNode.getNodeType(e);
      s = e.getTypeLength(r) === 1 ? this.inputType : e.changeComponentType(r, this.inputType);
    } else
      s = this.valueNode.getNodeType(e);
    return `${e.getBitcastMethod(t, s)}( ${this.valueNode.build(e, s)} )`;
  }
}
const S_ = /* @__PURE__ */ D(Yd).setParameterLength(2), N_ = (a) => new Yd(a, "uint", "float"), pn = {};
class Cs extends T {
  static get type() {
    return "BitcountNode";
  }
  /**
   * Constructs a new math node.
   *
   * @param {'countTrailingZeros'|'countLeadingZeros'|'countOneBits'} method - The method name.
   * @param {Node} aNode - The first input.
   */
  constructor(e, t) {
    super(e, t), this.isBitcountNode = !0;
  }
  /**
   * Casts the input value of the function to an integer if necessary.
   *
   * @private
   * @param {Node<uint>|Node<int>} inputNode - The input value.
   * @param {Node<uint>} outputNode - The output value.
   * @param {string} elementType - The type of the input value.
   */
  _resolveElementType(e, t, s) {
    s === "int" ? t.assign(S_(e, "uint")) : t.assign(e);
  }
  _returnDataNode(e) {
    switch (e) {
      case "uint":
        return Q;
      case "int":
        return pe;
      case "uvec2":
        return vc;
      case "uvec3":
        return Cc;
      case "uvec4":
        return Mc;
      case "ivec2":
        return yt;
      case "ivec3":
        return Ec;
      case "ivec4":
        return wc;
    }
  }
  /**
   * Creates and registers a reusable GLSL function that emulates the behavior of countTrailingZeros.
   *
   * @private
   * @param {string} method - The name of the function to create.
   * @param {string} elementType - The type of the input value.
   * @returns {Function} - The generated function
   */
  _createTrailingZerosBaseLayout(e, t) {
    const s = this._returnDataNode(t);
    return A(([n]) => {
      const i = Q(0);
      this._resolveElementType(n, i, t);
      const o = b(i.bitAnd(kc(i))), l = N_(o).shiftRight(23).sub(127);
      return s(l);
    }).setLayout({
      name: e,
      type: t,
      inputs: [
        { name: "value", type: t }
      ]
    });
  }
  /**
   * Creates and registers a reusable GLSL function that emulates the behavior of countLeadingZeros.
   *
   * @private
   * @param {string} method - The name of the function to create.
   * @param {string} elementType - The type of the input value.
   * @returns {Function} - The generated function
   */
  _createLeadingZerosBaseLayout(e, t) {
    const s = this._returnDataNode(t);
    return A(([n]) => {
      ue(n.equal(Q(0)), () => Q(32));
      const i = Q(0), o = Q(0);
      return this._resolveElementType(n, i, t), ue(i.shiftRight(16).equal(0), () => {
        o.addAssign(16), i.shiftLeftAssign(16);
      }), ue(i.shiftRight(24).equal(0), () => {
        o.addAssign(8), i.shiftLeftAssign(8);
      }), ue(i.shiftRight(28).equal(0), () => {
        o.addAssign(4), i.shiftLeftAssign(4);
      }), ue(i.shiftRight(30).equal(0), () => {
        o.addAssign(2), i.shiftLeftAssign(2);
      }), ue(i.shiftRight(31).equal(0), () => {
        o.addAssign(1);
      }), s(o);
    }).setLayout({
      name: e,
      type: t,
      inputs: [
        { name: "value", type: t }
      ]
    });
  }
  /**
   * Creates and registers a reusable GLSL function that emulates the behavior of countOneBits.
   *
   * @private
   * @param {string} method - The name of the function to create.
   * @param {string} elementType - The type of the input value.
   * @returns {Function} - The generated function
   */
  _createOneBitsBaseLayout(e, t) {
    const s = this._returnDataNode(t);
    return A(([n]) => {
      const i = Q(0);
      this._resolveElementType(n, i, t), i.assign(i.sub(i.shiftRight(Q(1)).bitAnd(Q(1431655765)))), i.assign(i.bitAnd(Q(858993459)).add(i.shiftRight(Q(2)).bitAnd(Q(858993459))));
      const o = i.add(i.shiftRight(Q(4))).bitAnd(Q(252645135)).mul(Q(16843009)).shiftRight(Q(24));
      return s(o);
    }).setLayout({
      name: e,
      type: t,
      inputs: [
        { name: "value", type: t }
      ]
    });
  }
  /**
   * Creates and registers a reusable GLSL function that emulates the behavior of the specified bitcount function.
   * including considerations for component-wise bitcounts on vector type inputs.
   *
   * @private
   * @param {string} method - The name of the function to create.
   * @param {string} inputType - The type of the input value.
   * @param {number} typeLength - The vec length of the input value.
   * @param {Function} baseFn - The base function that operates on an individual component of the vector.
   * @returns {Function} - The alias function for the specified bitcount method.
   */
  _createMainLayout(e, t, s, r) {
    const n = this._returnDataNode(t);
    return A(([o]) => {
      if (s === 1)
        return n(r(o));
      {
        const u = n(0), l = ["x", "y", "z", "w"];
        for (let c = 0; c < s; c++) {
          const d = l[c];
          u[d].assign(r(o[d]));
        }
        return u;
      }
    }).setLayout({
      name: e,
      type: t,
      inputs: [
        { name: "value", type: t }
      ]
    });
  }
  setup(e) {
    const { method: t, aNode: s } = this, { renderer: r } = e;
    if (r.backend.isWebGPUBackend)
      return super.setup(e);
    const n = this.getInputType(e), i = e.getElementType(n), o = e.getTypeLength(n), u = `${t}_base_${i}`, l = `${t}_${n}`;
    let c = pn[u];
    if (c === void 0) {
      switch (t) {
        case Cs.COUNT_LEADING_ZEROS: {
          c = this._createLeadingZerosBaseLayout(u, i);
          break;
        }
        case Cs.COUNT_TRAILING_ZEROS: {
          c = this._createTrailingZerosBaseLayout(u, i);
          break;
        }
        case Cs.COUNT_ONE_BITS: {
          c = this._createOneBitsBaseLayout(u, i);
          break;
        }
      }
      pn[u] = c;
    }
    let d = pn[l];
    return d === void 0 && (d = this._createMainLayout(l, n, o, c), pn[l] = d), A(() => d(
      s
    ))();
  }
}
Cs.COUNT_TRAILING_ZEROS = "countTrailingZeros";
Cs.COUNT_LEADING_ZEROS = "countLeadingZeros";
Cs.COUNT_ONE_BITS = "countOneBits";
new dt();
const R_ = new Ds();
Es.flipX();
R_.depthTexture = new vt(1, 1);
const Qi = /* @__PURE__ */ new kl(-1, 1, 1, -1, 0, 1);
class A_ extends ha {
  /**
   * Constructs a new quad geometry.
   *
   * @param {boolean} [flipY=false] - Whether the uv coordinates should be flipped along the vertical axis or not.
   */
  constructor(e = !1) {
    super();
    const t = e === !1 ? [0, -1, 0, 1, 2, 1] : [0, 2, 0, 0, 2, 0];
    this.setAttribute("position", new Ka([-1, 3, 0, -1, -1, 0, 3, -1, 0], 3)), this.setAttribute("uv", new Ka(t, 2));
  }
}
const v_ = /* @__PURE__ */ new A_();
class jd extends is {
  /**
   * Constructs a new quad mesh.
   *
   * @param {?Material} [material=null] - The material to render the quad mesh with.
   */
  constructor(e = null) {
    super(v_, e), this.camera = Qi, this.isQuadMesh = !0;
  }
  /**
   * Async version of `render()`.
   *
   * @async
   * @deprecated
   * @param {Renderer} renderer - The renderer.
   * @return {Promise} A Promise that resolves when the render has been finished.
   */
  async renderAsync(e) {
    Te('QuadMesh: "renderAsync()" has been deprecated. Use "render()" and "await renderer.init();" when creating the renderer.'), await e.init(), e.render(this, Qi);
  }
  /**
   * Renders the quad mesh
   *
   * @param {Renderer} renderer - The renderer.
   */
  render(e) {
    e.render(this, Qi);
  }
}
const Zd = A(([a]) => wt(b(52.9829189).mul(wt(Bs(a, I(0.06711056, 583715e-8)))))).setLayout({
  name: "interleavedGradientNoise",
  type: "float",
  inputs: [
    { name: "position", type: "vec2" }
  ]
}), St = A(([a, e, t]) => {
  const s = b(2.399963229728653), r = ns(b(a).add(0.5).div(b(e))), n = b(a).mul(s).add(t);
  return I(_t(n), qe(n)).mul(r);
}).setLayout({
  name: "vogelDiskSample",
  type: "vec2",
  inputs: [
    { name: "sampleIndex", type: "int" },
    { name: "samplesCount", type: "int" },
    { name: "phi", type: "float" }
  ]
});
class Ke extends G {
  static get type() {
    return "EventNode";
  }
  /**
   * Creates an EventNode.
   *
   * @param {string} eventType - The type of event
   * @param {Function} callback - The callback to execute on update.
   */
  constructor(e, t) {
    super("void"), this.eventType = e, this.callback = t, e === Ke.OBJECT ? this.updateType = z.OBJECT : e === Ke.MATERIAL ? this.updateType = z.RENDER : e === Ke.FRAME ? this.updateType = z.FRAME : e === Ke.BEFORE_OBJECT ? this.updateBeforeType = z.OBJECT : e === Ke.BEFORE_MATERIAL ? this.updateBeforeType = z.RENDER : e === Ke.BEFORE_FRAME && (this.updateBeforeType = z.FRAME);
  }
  update(e) {
    this.callback(e);
  }
  updateBefore(e) {
    this.callback(e);
  }
}
Ke.OBJECT = "object";
Ke.MATERIAL = "material";
Ke.FRAME = "frame";
Ke.BEFORE_OBJECT = "beforeObject";
Ke.BEFORE_MATERIAL = "beforeMaterial";
Ke.BEFORE_FRAME = "beforeFrame";
const Yi = /* @__PURE__ */ new dt(), E_ = /* @__PURE__ */ k(0).setGroup(V).onRenderUpdate(({ scene: a }) => a.backgroundBlurriness), Yu = /* @__PURE__ */ k(1).setGroup(V).onRenderUpdate(({ scene: a }) => a.backgroundIntensity), C_ = /* @__PURE__ */ k(new dt()).setGroup(V).onRenderUpdate(({ scene: a }) => {
  const e = a.background;
  return e !== null && e.isTexture && e.mapping !== af ? Yi.makeRotationFromEuler(a.backgroundRotation).transpose() : Yi.identity(), Yi;
});
A(({ texture: a, uv: e }) => {
  const s = N().toVar();
  return ue(e.x.lessThan(1e-4), () => {
    s.assign(N(1, 0, 0));
  }).ElseIf(e.y.lessThan(1e-4), () => {
    s.assign(N(0, 1, 0));
  }).ElseIf(e.z.lessThan(1e-4), () => {
    s.assign(N(0, 0, 1));
  }).ElseIf(e.x.greaterThan(1 - 1e-4), () => {
    s.assign(N(-1, 0, 0));
  }).ElseIf(e.y.greaterThan(1 - 1e-4), () => {
    s.assign(N(0, -1, 0));
  }).ElseIf(e.z.greaterThan(1 - 1e-4), () => {
    s.assign(N(0, 0, -1));
  }).Else(() => {
    const n = a.sample(e.add(N(-0.01, 0, 0))).r.sub(a.sample(e.add(N(0.01, 0, 0))).r), i = a.sample(e.add(N(0, -0.01, 0))).r.sub(a.sample(e.add(N(0, 0.01, 0))).r), o = a.sample(e.add(N(0, 0, -0.01))).r.sub(a.sample(e.add(N(0, 0, 0.01))).r);
    s.assign(N(n, i, o));
  }), s.normalize();
});
A(([a, e]) => a.mul(e).floor().div(e));
const gn = /* @__PURE__ */ new je();
class w_ extends Zr {
  static get type() {
    return "PassTextureNode";
  }
  /**
   * Constructs a new pass texture node.
   *
   * @param {PassNode} passNode - The pass node.
   * @param {Texture} texture - The output texture.
   */
  constructor(e, t) {
    super(t), this.passNode = e, this.isPassTextureNode = !0, this.setUpdateMatrix(!1);
  }
  setup(e) {
    const t = e.getNodeProperties(this);
    return t.passNode = this.passNode, super.setup(e);
  }
  clone() {
    return new this.constructor(this.passNode, this.value);
  }
}
class ju extends w_ {
  static get type() {
    return "PassMultipleTextureNode";
  }
  /**
   * Constructs a new pass texture node.
   *
   * @param {PassNode} passNode - The pass node.
   * @param {string} textureName - The output texture name.
   * @param {boolean} [previousTexture=false] - Whether previous frame data should be used or not.
   */
  constructor(e, t, s = !1) {
    super(e, null), this.textureName = t, this.previousTexture = s, this.isPassMultipleTextureNode = !0;
  }
  /**
   * Updates the texture reference of this node.
   */
  updateTexture() {
    this.value = this.previousTexture ? this.passNode.getPreviousTexture(this.textureName) : this.passNode.getTexture(this.textureName);
  }
  setup(e) {
    return this.updateTexture(), super.setup(e);
  }
  clone() {
    const e = new this.constructor(this.passNode, this.textureName, this.previousTexture);
    return e.uvNode = this.uvNode, e.levelNode = this.levelNode, e.biasNode = this.biasNode, e.sampler = this.sampler, e.depthNode = this.depthNode, e.compareNode = this.compareNode, e.gradNode = this.gradNode, e.offsetNode = this.offsetNode, e;
  }
}
class Ti extends Re {
  static get type() {
    return "PassNode";
  }
  /**
   * Constructs a new pass node.
   *
   * @param {('color'|'depth')} scope - The scope of the pass. The scope determines whether the node outputs color or depth.
   * @param {Scene} scene - A reference to the scene.
   * @param {Camera} camera - A reference to the camera.
   * @param {Object} options - Options for the internal render target.
   */
  constructor(e, t, s, r = {}) {
    super("vec4"), this.scope = e, this.scene = t, this.camera = s, this.options = r, this._pixelRatio = 1, this._width = 1, this._height = 1;
    const n = new vt();
    n.isRenderTargetTexture = !0, n.name = "depth";
    const i = new Ds(this._width * this._pixelRatio, this._height * this._pixelRatio, { type: Oe, ...r });
    i.texture.name = "output", i.depthTexture = n, this.renderTarget = i, this.overrideMaterial = null, this.transparent = !0, this.opaque = !0, this.contextNode = null, this._contextNodeCache = null, this._textures = {
      output: i.texture,
      depth: n
    }, this._textureNodes = {}, this._linearDepthNodes = {}, this._viewZNodes = {}, this._previousTextures = {}, this._previousTextureNodes = {}, this._cameraNear = k(0), this._cameraFar = k(0), this._mrt = null, this._layers = null, this._resolutionScale = 1, this._viewport = null, this._scissor = null, this.isPassNode = !0, this.updateBeforeType = z.FRAME, this.global = !0;
  }
  /**
   * Sets the resolution scale for the pass.
   * The resolution scale is a factor that is multiplied with the renderer's width and height.
   *
   * @param {number} resolutionScale - The resolution scale to set. A value of `1` means full resolution.
   * @return {PassNode} A reference to this pass.
   */
  setResolutionScale(e) {
    return this._resolutionScale = e, this;
  }
  /**
   * Gets the current resolution scale of the pass.
   *
   * @return {number} The current resolution scale. A value of `1` means full resolution.
   */
  getResolutionScale() {
    return this._resolutionScale;
  }
  /**
   * Sets the resolution for the pass.
   * The resolution is a factor that is multiplied with the renderer's width and height.
   *
   * @param {number} resolution - The resolution to set. A value of `1` means full resolution.
   * @return {PassNode} A reference to this pass.
   * @deprecated since r181. Use {@link PassNode#setResolutionScale `setResolutionScale()`} instead.
   */
  setResolution(e) {
    return L("PassNode: .setResolution() is deprecated. Use .setResolutionScale() instead."), this.setResolutionScale(e);
  }
  /**
   * Gets the current resolution of the pass.
   *
   * @return {number} The current resolution. A value of `1` means full resolution.
   * @deprecated since r181. Use {@link PassNode#getResolutionScale `getResolutionScale()`} instead.
   */
  getResolution() {
    return L("PassNode: .getResolution() is deprecated. Use .getResolutionScale() instead."), this.getResolutionScale();
  }
  /**
   * Sets the layer configuration that should be used when rendering the pass.
   *
   * @param {Layers} layers - The layers object to set.
   * @return {PassNode} A reference to this pass.
   */
  setLayers(e) {
    return this._layers = e, this;
  }
  /**
   * Gets the current layer configuration of the pass.
   *
   * @return {?Layers} .
   */
  getLayers() {
    return this._layers;
  }
  /**
   * Sets the given MRT node to setup MRT for this pass.
   *
   * @param {MRTNode} mrt - The MRT object.
   * @return {PassNode} A reference to this pass.
   */
  setMRT(e) {
    return this._mrt = e, this;
  }
  /**
   * Returns the current MRT node.
   *
   * @return {MRTNode} The current MRT node.
   */
  getMRT() {
    return this._mrt;
  }
  /**
   * Returns the texture for the given output name.
   *
   * @param {string} name - The output name to get the texture for.
   * @return {Texture} The texture.
   */
  getTexture(e) {
    let t = this._textures[e];
    return t === void 0 && (t = this.renderTarget.texture.clone(), t.name = e, this._textures[e] = t, this.renderTarget.textures.push(t)), t;
  }
  /**
   * Returns the texture holding the data of the previous frame for the given output name.
   *
   * @param {string} name - The output name to get the texture for.
   * @return {Texture} The texture holding the data of the previous frame.
   */
  getPreviousTexture(e) {
    let t = this._previousTextures[e];
    return t === void 0 && (t = this.getTexture(e).clone(), this._previousTextures[e] = t), t;
  }
  /**
   * Switches current and previous textures for the given output name.
   *
   * @param {string} name - The output name.
   */
  toggleTexture(e) {
    const t = this._previousTextures[e];
    if (t !== void 0) {
      const s = this._textures[e], r = this.renderTarget.textures.indexOf(s);
      this.renderTarget.textures[r] = t, this._textures[e] = t, this._previousTextures[e] = s, this._textureNodes[e].updateTexture(), this._previousTextureNodes[e].updateTexture();
    }
  }
  /**
   * Returns the texture node for the given output name.
   *
   * @param {string} [name='output'] - The output name to get the texture node for.
   * @return {TextureNode} The texture node.
   */
  getTextureNode(e = "output") {
    let t = this._textureNodes[e];
    return t === void 0 && (t = new ju(this, e), t.updateTexture(), this._textureNodes[e] = t), t;
  }
  /**
   * Returns the previous texture node for the given output name.
   *
   * @param {string} [name='output'] - The output name to get the previous texture node for.
   * @return {TextureNode} The previous texture node.
   */
  getPreviousTextureNode(e = "output") {
    let t = this._previousTextureNodes[e];
    return t === void 0 && (this._textureNodes[e] === void 0 && this.getTextureNode(e), t = new ju(this, e, !0), t.updateTexture(), this._previousTextureNodes[e] = t), t;
  }
  /**
   * Returns a viewZ node of this pass.
   *
   * @param {string} [name='depth'] - The output name to get the viewZ node for. In most cases the default `'depth'` can be used however the parameter exists for custom depth outputs.
   * @return {Node} The viewZ node.
   */
  getViewZNode(e = "depth") {
    let t = this._viewZNodes[e];
    if (t === void 0) {
      const s = this._cameraNear, r = this._cameraFar;
      this._viewZNodes[e] = t = vd(this.getTextureNode(e), s, r);
    }
    return t;
  }
  /**
   * Returns a linear depth node of this pass.
   *
   * @param {string} [name='depth'] - The output name to get the linear depth node for. In most cases the default `'depth'` can be used however the parameter exists for custom depth outputs.
   * @return {Node} The linear depth node.
   */
  getLinearDepthNode(e = "depth") {
    let t = this._linearDepthNodes[e];
    if (t === void 0) {
      const s = this._cameraNear, r = this._cameraFar, n = this.getViewZNode(e);
      this._linearDepthNodes[e] = t = Or(n, s, r);
    }
    return t;
  }
  /**
   * Precompiles the pass.
   *
   * Note that this method must be called after the pass configuration is complete.
   * So calls like `setMRT()` and `getTextureNode()` must proceed the precompilation.
   *
   * @async
   * @param {Renderer} renderer - The renderer.
   * @return {Promise} A Promise that resolves when the compile has been finished.
   * @see {@link Renderer#compileAsync}
   */
  async compileAsync(e) {
    const t = e.getRenderTarget(), s = e.getMRT();
    e.setRenderTarget(this.renderTarget), e.setMRT(this._mrt), await e.compileAsync(this.scene, this.camera), e.setRenderTarget(t), e.setMRT(s);
  }
  setup({ renderer: e }) {
    return this.renderTarget.samples = this.options.samples === void 0 ? e.samples : this.options.samples, this.renderTarget.texture.type = e.getOutputBufferType(), e.reversedDepthBuffer === !0 && (this.renderTarget.depthTexture.type = De), this.scope === Ti.COLOR ? this.getTextureNode() : this.getLinearDepthNode();
  }
  updateBefore(e) {
    const { renderer: t } = e, { scene: s } = this;
    let r, n;
    const i = t.getOutputRenderTarget();
    i && i.isXRRenderTarget === !0 ? (n = 1, r = t.xr.getCamera(), t.xr.updateCamera(r), gn.set(i.width, i.height)) : (r = this.camera, n = t.getPixelRatio(), t.getSize(gn)), this._pixelRatio = n, this.setSize(gn.width, gn.height);
    const o = t.getRenderTarget(), u = t.getMRT(), l = t.autoClear, c = t.transparent, d = t.opaque, h = r.layers.mask, f = t.contextNode, p = s.overrideMaterial;
    this._cameraNear.value = r.near, this._cameraFar.value = r.far, this._layers !== null && (r.layers.mask = this._layers.mask);
    for (const m in this._previousTextures)
      this.toggleTexture(m);
    this.overrideMaterial !== null && (s.overrideMaterial = this.overrideMaterial), t.setRenderTarget(this.renderTarget), t.setMRT(this._mrt), t.autoClear = !0, t.transparent = this.transparent, t.opaque = this.opaque, this.contextNode !== null && ((this._contextNodeCache === null || this._contextNodeCache.version !== this.version) && (this._contextNodeCache = {
      version: this.version,
      context: Ls({ ...t.contextNode.getFlowContextData(), ...this.contextNode.getFlowContextData() })
    }), t.contextNode = this._contextNodeCache.context);
    const g = s.name;
    s.name = this.name ? this.name : s.name, t.render(s, r), s.name = g, s.overrideMaterial = p, t.setRenderTarget(o), t.setMRT(u), t.autoClear = l, t.transparent = c, t.opaque = d, t.contextNode = f, r.layers.mask = h;
  }
  /**
   * Sets the size of the pass's render target. Honors the pixel ratio.
   *
   * @param {number} width - The width to set.
   * @param {number} height - The height to set.
   */
  setSize(e, t) {
    this._width = e, this._height = t;
    const s = Math.floor(this._width * this._pixelRatio * this._resolutionScale), r = Math.floor(this._height * this._pixelRatio * this._resolutionScale);
    this.renderTarget.setSize(s, r), this._scissor !== null ? (this.renderTarget.scissor.copy(this._scissor).multiplyScalar(this._pixelRatio * this._resolutionScale).floor(), this.renderTarget.scissorTest = !0) : this.renderTarget.scissorTest = !1, this._viewport !== null && this.renderTarget.viewport.copy(this._viewport).multiplyScalar(this._pixelRatio * this._resolutionScale).floor();
  }
  /**
   * This method allows to define the pass's scissor rectangle. By default, the scissor rectangle is kept
   * in sync with the pass's dimensions. To reverse the process and use auto-sizing again, call the method
   * with `null` as the single argument.
   *
   * @param {?(number | Vector4)} x - The horizontal coordinate for the lower left corner of the box in logical pixel unit.
   * Instead of passing four arguments, the method also works with a single four-dimensional vector.
   * @param {number} y - The vertical coordinate for the lower left corner of the box in logical pixel unit.
   * @param {number} width - The width of the scissor box in logical pixel unit.
   * @param {number} height - The height of the scissor box in logical pixel unit.
   */
  setScissor(e, t, s, r) {
    e === null ? this._scissor = null : (this._scissor === null && (this._scissor = new Se()), e.isVector4 ? this._scissor.copy(e) : this._scissor.set(e, t, s, r));
  }
  /**
   * This method allows to define the pass's viewport. By default, the viewport is kept in sync
   * with the pass's dimensions. To reverse the process and use auto-sizing again, call the method
   * with `null` as the single argument.
   *
   * @param {number | Vector4} x - The horizontal coordinate for the lower left corner of the viewport origin in logical pixel unit.
   * @param {number} y - The vertical coordinate for the lower left corner of the viewport origin  in logical pixel unit.
   * @param {number} width - The width of the viewport in logical pixel unit.
   * @param {number} height - The height of the viewport in logical pixel unit.
   */
  setViewport(e, t, s, r) {
    e === null ? this._viewport = null : (this._viewport === null && (this._viewport = new Se()), e.isVector4 ? this._viewport.copy(e) : this._viewport.set(e, t, s, r));
  }
  /**
   * Sets the pixel ratio the pass's render target and updates the size.
   *
   * @param {number} pixelRatio - The pixel ratio to set.
   */
  setPixelRatio(e) {
    this._pixelRatio = e, this.setSize(this._width, this._height);
  }
  /**
   * Frees internal resources. Should be called when the node is no longer in use.
   */
  dispose() {
    this.renderTarget.dispose();
  }
}
Ti.COLOR = "color";
Ti.DEPTH = "depth";
const M_ = /* @__PURE__ */ A(([a, e]) => a.mul(e).clamp()).setLayout({
  name: "linearToneMapping",
  type: "vec3",
  inputs: [
    { name: "color", type: "vec3" },
    { name: "exposure", type: "float" }
  ]
}), B_ = /* @__PURE__ */ A(([a, e]) => (a = a.mul(e), a.div(a.add(1)).clamp())).setLayout({
  name: "reinhardToneMapping",
  type: "vec3",
  inputs: [
    { name: "color", type: "vec3" },
    { name: "exposure", type: "float" }
  ]
}), F_ = /* @__PURE__ */ A(([a, e]) => {
  a = a.mul(e), a = a.sub(4e-3).max(0);
  const t = a.mul(a.mul(6.2).add(0.5)), s = a.mul(a.mul(6.2).add(1.7)).add(0.06);
  return t.div(s).pow(2.2);
}).setLayout({
  name: "cineonToneMapping",
  type: "vec3",
  inputs: [
    { name: "color", type: "vec3" },
    { name: "exposure", type: "float" }
  ]
}), U_ = /* @__PURE__ */ A(([a]) => {
  const e = a.mul(a.add(0.0245786)).sub(90537e-9), t = a.mul(a.add(0.432951).mul(0.983729)).add(0.238081);
  return e.div(t);
}), D_ = /* @__PURE__ */ A(([a, e]) => {
  const t = Me(
    0.59719,
    0.35458,
    0.04823,
    0.076,
    0.90834,
    0.01566,
    0.0284,
    0.13383,
    0.83777
  ), s = Me(
    1.60475,
    -0.53108,
    -0.07367,
    -0.10208,
    1.10813,
    -605e-5,
    -327e-5,
    -0.07276,
    1.07602
  );
  return a = a.mul(e).div(0.6), a = t.mul(a), a = U_(a), a = s.mul(a), a.clamp();
}).setLayout({
  name: "acesFilmicToneMapping",
  type: "vec3",
  inputs: [
    { name: "color", type: "vec3" },
    { name: "exposure", type: "float" }
  ]
}), P_ = /* @__PURE__ */ Me(N(1.6605, -0.1246, -0.0182), N(-0.5876, 1.1329, -0.1006), N(-0.0728, -83e-4, 1.1187)), L_ = /* @__PURE__ */ Me(N(0.6274, 0.0691, 0.0164), N(0.3293, 0.9195, 0.088), N(0.0433, 0.0113, 0.8956)), I_ = /* @__PURE__ */ A(([a]) => {
  const e = N(a).toVar(), t = N(e.mul(e)).toVar(), s = N(t.mul(t)).toVar();
  return b(15.5).mul(s.mul(t)).sub(X(40.14, s.mul(e))).add(X(31.96, s).sub(X(6.868, t.mul(e))).add(X(0.4298, t).add(X(0.1191, e).sub(232e-5))));
}), O_ = /* @__PURE__ */ A(([a, e]) => {
  const t = N(a).toVar(), s = Me(N(0.856627153315983, 0.137318972929847, 0.11189821299995), N(0.0951212405381588, 0.761241990602591, 0.0767994186031903), N(0.0482516061458583, 0.101439036467562, 0.811302368396859)), r = Me(N(1.1271005818144368, -0.1413297634984383, -0.14132976349843826), N(-0.11060664309660323, 1.157823702216272, -0.11060664309660294), N(-0.016493938717834573, -0.016493938717834257, 1.2519364065950405)), n = b(-12.47393), i = b(4.026069);
  return t.mulAssign(e), t.assign(L_.mul(t)), t.assign(s.mul(t)), t.assign(Be(t, 1e-10)), t.assign($t(t)), t.assign(t.sub(n).div(i.sub(n))), t.assign(os(t, 0, 1)), t.assign(I_(t)), t.assign(r.mul(t)), t.assign(di(Be(N(0), t), N(2.2))), t.assign(P_.mul(t)), t.assign(os(t, 0, 1)), t;
}).setLayout({
  name: "agxToneMapping",
  type: "vec3",
  inputs: [
    { name: "color", type: "vec3" },
    { name: "exposure", type: "float" }
  ]
}), G_ = /* @__PURE__ */ A(([a, e]) => {
  const t = b(0.76), s = b(0.15);
  a = a.mul(e);
  const r = or(a.r, or(a.g, a.b)), n = Ye(r.lessThan(0.08), r.sub(X(6.25, r.mul(r))), 0.04);
  a.subAssign(n);
  const i = Be(a.r, Be(a.g, a.b));
  ue(i.lessThan(t), () => a);
  const o = ke(1, t), u = ke(1, o.mul(o).div(i.add(o.sub(t))));
  a.mulAssign(u.div(i));
  const l = ke(1, Ct(1, s.mul(i.sub(u)).add(1)));
  return ne(a, N(u), l);
}).setLayout({
  name: "neutralToneMapping",
  type: "vec3",
  inputs: [
    { name: "color", type: "vec3" },
    { name: "exposure", type: "float" }
  ]
});
class Ee extends G {
  static get type() {
    return "CodeNode";
  }
  /**
   * Constructs a new code node.
   *
   * @param {string} [code=''] - The native code.
   * @param {Array<Node>} [includes=[]] - An array of includes.
   * @param {('js'|'wgsl'|'glsl')} [language=''] - The used language.
   */
  constructor(e = "", t = [], s = "") {
    super("code"), this.isCodeNode = !0, this.global = !0, this.code = e, this.includes = t, this.language = s;
  }
  /**
   * Sets the includes of this code node.
   *
   * @param {Array<Node>} includes - The includes to set.
   * @return {CodeNode} A reference to this node.
   */
  setIncludes(e) {
    return this.includes = e, this;
  }
  /**
   * Returns the includes of this code node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {Array<Node>} The includes.
   */
  getIncludes() {
    return this.includes;
  }
  generate(e) {
    const t = this.getIncludes(e);
    for (const r of t)
      r.build(e);
    const s = e.getCodeFromNode(this, this.getNodeType(e));
    return s.code = this.code, s.code;
  }
  serialize(e) {
    super.serialize(e), e.code = this.code, e.language = this.language;
  }
  deserialize(e) {
    super.deserialize(e), this.code = e.code, this.language = e.language;
  }
}
class k_ extends Ee {
  static get type() {
    return "FunctionNode";
  }
  /**
   * Constructs a new function node.
   *
   * @param {string} [code=''] - The native code.
   * @param {Array<Node>} [includes=[]] - An array of includes.
   * @param {('js'|'wgsl'|'glsl')} [language=''] - The used language.
   */
  constructor(e = "", t = [], s = "") {
    super(e, t, s);
  }
  /**
   * Returns the type of this function node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The type.
   */
  generateNodeType(e) {
    return this.getNodeFunction(e).type;
  }
  /**
   * Returns the type of a member of this function node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @param {string} name - The name of the member.
   * @return {string} The type of the member.
   */
  getMemberType(e, t) {
    const s = this.getNodeType(e);
    return e.getStructTypeNode(s).getMemberType(e, t);
  }
  /**
   * Returns the inputs of this function node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {Array<NodeFunctionInput>} The inputs.
   */
  getInputs(e) {
    return this.getNodeFunction(e).inputs;
  }
  /**
   * Returns the node function for this function node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {NodeFunction} The node function.
   */
  getNodeFunction(e) {
    const t = e.getDataFromNode(this);
    let s = t.nodeFunction;
    return s === void 0 && (s = e.parser.parseFunction(this.code), t.nodeFunction = s), s;
  }
  generate(e, t) {
    super.generate(e);
    const s = this.getNodeFunction(e), r = s.name, n = s.type, i = e.getCodeFromNode(this, n);
    r !== "" && (i.name = r);
    const o = e.getPropertyName(i), u = this.getNodeFunction(e).getCode(o);
    return i.code = u + `
`, t === "property" ? o : e.format(`${o}()`, n, t);
  }
}
function La(a) {
  let e;
  const t = a.context.getViewZ;
  return t !== void 0 && (e = t(this)), (e || be.z).negate();
}
const V_ = A(([a, e], t) => {
  const s = La(t);
  return Wt(a, e, s);
}), $_ = A(([a], e) => {
  const t = La(e);
  return a.mul(a, t, t).negate().exp().oneMinus();
});
A(([a, e], t) => {
  const s = La(t), n = e.sub(ar.y).max(0).toConst().mul(s).toConst();
  return a.mul(a, n, n).negate().exp().oneMinus();
});
const Zu = A(([a, e]) => W(e.toFloat().mix(Pr.rgb, a.toVec3()), Pr.a));
class z_ extends G {
  /**
   * Constructs a new barrier node.
   *
   * @param {string} scope - The scope defines the behavior of the node.
   */
  constructor(e) {
    super(), this.scope = e, this.isBarrierNode = !0;
  }
  setup(e) {
    e.allowEarlyReturns = !1, e.allowGlobalVariables = !1;
  }
  generate(e) {
    const { scope: t } = this, { renderer: s } = e;
    s.backend.isWebGLBackend === !0 ? e.addFlowCode(`	// ${t}Barrier 
`) : e.addLineFlowCode(`${t}Barrier()`, this);
  }
}
le(z_);
class Ft extends G {
  static get type() {
    return "AtomicFunctionNode";
  }
  /**
   * Constructs a new atomic function node.
   *
   * @param {string} method - The signature of the atomic function to construct.
   * @param {Node} pointerNode - An atomic variable or element of an atomic buffer.
   * @param {Node} valueNode - The value that mutates the atomic variable.
   */
  constructor(e, t, s) {
    super("uint"), this.method = e, this.pointerNode = t, this.valueNode = s, this.parents = !0;
  }
  /**
   * Overwrites the default implementation to return the type of
   * the pointer node.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The input type.
   */
  getInputType(e) {
    return this.pointerNode.getNodeType(e);
  }
  /**
   * Overwritten since the node type is inferred from the input type.
   *
   * @param {NodeBuilder} builder - The current node builder.
   * @return {string} The node type.
   */
  generateNodeType(e) {
    return this.getInputType(e);
  }
  generate(e) {
    const t = e.getNodeProperties(this), s = t.parents, r = this.method, n = this.getNodeType(e), i = this.getInputType(e), o = this.pointerNode, u = this.valueNode, l = [];
    l.push(`&${o.build(e, i)}`), u !== null && l.push(u.build(e, i));
    const c = `${e.getMethod(r, n)}( ${l.join(", ")} )`;
    if (s ? s.length === 1 && s[0].isStackNode === !0 : !1)
      e.addLineFlowCode(c, this);
    else
      return t.constNode === void 0 && (t.constNode = bt(c, n).toConst()), t.constNode.build(e);
  }
}
Ft.ATOMIC_LOAD = "atomicLoad";
Ft.ATOMIC_STORE = "atomicStore";
Ft.ATOMIC_ADD = "atomicAdd";
Ft.ATOMIC_SUB = "atomicSub";
Ft.ATOMIC_MAX = "atomicMax";
Ft.ATOMIC_MIN = "atomicMin";
Ft.ATOMIC_AND = "atomicAnd";
Ft.ATOMIC_OR = "atomicOr";
Ft.ATOMIC_XOR = "atomicXor";
le(Ft);
class J extends Re {
  static get type() {
    return "SubgroupFunctionNode";
  }
  /**
   * Constructs a new function node.
   *
   * @param {string} method - The subgroup/wave intrinsic method to construct.
   * @param {Node} [aNode=null] - The method's first argument.
   * @param {Node} [bNode=null] - The method's second argument.
   */
  constructor(e, t = null, s = null) {
    super(), this.method = e, this.aNode = t, this.bNode = s;
  }
  getInputType(e) {
    const t = this.aNode ? this.aNode.getNodeType(e) : null, s = this.bNode ? this.bNode.getNodeType(e) : null, r = e.isMatrix(t) ? 0 : e.getTypeLength(t), n = e.isMatrix(s) ? 0 : e.getTypeLength(s);
    return r > n ? t : s;
  }
  generateNodeType(e) {
    const t = this.method;
    return t === J.SUBGROUP_ELECT ? "bool" : t === J.SUBGROUP_BALLOT ? "uvec4" : this.getInputType(e);
  }
  generate(e, t) {
    const s = this.method, r = this.getNodeType(e), n = this.getInputType(e), i = this.aNode, o = this.bNode, u = [];
    if (s === J.SUBGROUP_BROADCAST || s === J.SUBGROUP_SHUFFLE || s === J.QUAD_BROADCAST) {
      const c = o.getNodeType(e);
      u.push(
        i.build(e, r),
        o.build(e, c === "float" ? "int" : r)
      );
    } else s === J.SUBGROUP_SHUFFLE_XOR || s === J.SUBGROUP_SHUFFLE_DOWN || s === J.SUBGROUP_SHUFFLE_UP ? u.push(
      i.build(e, r),
      o.build(e, "uint")
    ) : (i !== null && u.push(i.build(e, n)), o !== null && u.push(o.build(e, n)));
    const l = u.length === 0 ? "()" : `( ${u.join(", ")} )`;
    return e.format(`${e.getMethod(s, r)}${l}`, r, t);
  }
  serialize(e) {
    super.serialize(e), e.method = this.method;
  }
  deserialize(e) {
    super.deserialize(e), this.method = e.method;
  }
}
J.SUBGROUP_ELECT = "subgroupElect";
J.SUBGROUP_BALLOT = "subgroupBallot";
J.SUBGROUP_ADD = "subgroupAdd";
J.SUBGROUP_INCLUSIVE_ADD = "subgroupInclusiveAdd";
J.SUBGROUP_EXCLUSIVE_AND = "subgroupExclusiveAdd";
J.SUBGROUP_MUL = "subgroupMul";
J.SUBGROUP_INCLUSIVE_MUL = "subgroupInclusiveMul";
J.SUBGROUP_EXCLUSIVE_MUL = "subgroupExclusiveMul";
J.SUBGROUP_AND = "subgroupAnd";
J.SUBGROUP_OR = "subgroupOr";
J.SUBGROUP_XOR = "subgroupXor";
J.SUBGROUP_MIN = "subgroupMin";
J.SUBGROUP_MAX = "subgroupMax";
J.SUBGROUP_ALL = "subgroupAll";
J.SUBGROUP_ANY = "subgroupAny";
J.SUBGROUP_BROADCAST_FIRST = "subgroupBroadcastFirst";
J.QUAD_SWAP_X = "quadSwapX";
J.QUAD_SWAP_Y = "quadSwapY";
J.QUAD_SWAP_DIAGONAL = "quadSwapDiagonal";
J.SUBGROUP_BROADCAST = "subgroupBroadcast";
J.SUBGROUP_SHUFFLE = "subgroupShuffle";
J.SUBGROUP_SHUFFLE_XOR = "subgroupShuffleXor";
J.SUBGROUP_SHUFFLE_UP = "subgroupShuffleUp";
J.SUBGROUP_SHUFFLE_DOWN = "subgroupShuffleDown";
J.QUAD_BROADCAST = "quadBroadcast";
let mn;
function yi(a) {
  mn = mn || /* @__PURE__ */ new WeakMap();
  let e = mn.get(a);
  return e === void 0 && mn.set(a, e = {}), e;
}
function Ia(a) {
  const e = yi(a);
  return e.shadowMatrix || (e.shadowMatrix = k("mat4").setGroup(V).onRenderUpdate((t) => ((a.castShadow !== !0 || t.renderer.shadowMap.enabled === !1) && (a.shadow.camera.coordinateSystem !== t.camera.coordinateSystem && (a.shadow.camera.coordinateSystem = t.camera.coordinateSystem, a.shadow.camera.updateProjectionMatrix()), a.shadow.updateMatrices(a)), a.shadow.matrix)));
}
function W_(a, e = ar) {
  const t = Ia(a).mul(e);
  return t.xyz.div(t.w);
}
function Jd(a) {
  const e = yi(a);
  return e.position || (e.position = k(new H()).setGroup(V).onRenderUpdate((t, s) => s.value.setFromMatrixPosition(a.matrixWorld)));
}
function H_(a) {
  const e = yi(a);
  return e.targetPosition || (e.targetPosition = k(new H()).setGroup(V).onRenderUpdate((t, s) => s.value.setFromMatrixPosition(a.target.matrixWorld)));
}
function eh(a) {
  const e = yi(a);
  return e.viewPosition || (e.viewPosition = k(new H()).setGroup(V).onRenderUpdate(({ camera: t }, s) => {
    s.value = s.value || new H(), s.value.setFromMatrixPosition(a.matrixWorld), s.value.applyMatrix4(t.matrixWorldInverse);
  }));
}
const th = (a) => qt.transformDirection(Jd(a).sub(H_(a))), q_ = (a) => a.sort((e, t) => e.id - t.id), X_ = (a, e) => {
  for (const t of e)
    if (t.isAnalyticLightNode && t.light.id === a)
      return t;
  return null;
}, ji = /* @__PURE__ */ new WeakMap(), Nr = [];
class sh extends G {
  static get type() {
    return "LightsNode";
  }
  /**
   * Constructs a new lights node.
   */
  constructor() {
    super("vec3"), this.totalDiffuseNode = rs("vec3", "totalDiffuse"), this.totalSpecularNode = rs("vec3", "totalSpecular"), this.outgoingLightNode = rs("vec3", "outgoingLight"), this._lights = [], this._lightNodes = null, this._lightNodesHash = null, this.global = !0;
  }
  /**
   * Overwrites the default {@link Node#customCacheKey} implementation by including
   * light data into the cache key.
   *
   * @return {number} The custom cache key.
   */
  customCacheKey() {
    const e = this._lights;
    for (let s = 0; s < e.length; s++) {
      const r = e[s];
      if (Nr.push(r.id), Nr.push(r.castShadow ? 1 : 0), r.isSpotLight === !0) {
        const n = r.map !== null ? r.map.id : -1, i = r.colorNode ? r.colorNode.getCacheKey() : -1;
        Nr.push(n, i);
      }
    }
    const t = Yr(Nr);
    return Nr.length = 0, t;
  }
  /**
   * Computes a hash value for identifying the current light nodes setup.
   *
   * @param {NodeBuilder} builder - A reference to the current node builder.
   * @return {string} The computed hash.
   */
  getHash(e) {
    if (this._lightNodesHash === null) {
      this._lightNodes === null && this.setupLightsNode(e);
      const t = [];
      for (const s of this._lightNodes)
        t.push(s.getHash());
      this._lightNodesHash = "lights-" + t.join(",");
    }
    return this._lightNodesHash;
  }
  analyze(e) {
    const t = e.getNodeProperties(this);
    for (const s of t.nodes)
      s.build(e);
    t.outputNode.build(e);
  }
  /**
   * Creates lighting nodes for each scene light. This makes it possible to further
   * process lights in the node system.
   *
   * @param {NodeBuilder} builder - A reference to the current node builder.
   */
  setupLightsNode(e) {
    const t = [], s = this._lightNodes, r = q_(this._lights), n = e.renderer.library;
    for (const i of r)
      if (i.isNode)
        t.push(O(i));
      else {
        let o = null;
        if (s !== null && (o = X_(i.id, s)), o === null) {
          const u = n.getLightNodeClass(i.constructor);
          if (u === null) {
            L(`LightsNode.setupNodeLights: Light node not found for ${i.constructor.name}`);
            continue;
          }
          ji.has(i) === !1 && ji.set(i, new u(i)), o = ji.get(i);
        }
        t.push(o);
      }
    this._lightNodes = t;
  }
  /**
   * Sets up a direct light in the lighting model.
   *
   * @param {Object} builder - The builder object containing the context and stack.
   * @param {Object} lightNode - The light node.
   * @param {Object} lightData - The light object containing color and direction properties.
   */
  setupDirectLight(e, t, s) {
    const { lightingModel: r, reflectedLight: n } = e.context;
    r.direct({
      ...s,
      lightNode: t,
      reflectedLight: n
    }, e);
  }
  setupDirectRectAreaLight(e, t, s) {
    const { lightingModel: r, reflectedLight: n } = e.context;
    r.directRectArea({
      ...s,
      lightNode: t,
      reflectedLight: n
    }, e);
  }
  /**
   * Setups the internal lights by building all respective
   * light nodes.
   *
   * @param {NodeBuilder} builder - A reference to the current node builder.
   * @param {Array<LightingNode>} lightNodes - An array of lighting nodes.
   */
  setupLights(e, t) {
    for (const s of t)
      s.build(e);
  }
  getLightNodes(e) {
    return this._lightNodes === null && this.setupLightsNode(e), this._lightNodes;
  }
  /**
   * The implementation makes sure that for each light in the scene
   * there is a corresponding light node. By building the light nodes
   * and evaluating the lighting model the outgoing light is computed.
   *
   * @param {NodeBuilder} builder - A reference to the current node builder.
   * @return {Node<vec3>} A node representing the outgoing light.
   */
  setup(e) {
    const t = e.lightsNode;
    e.lightsNode = this;
    let s = this.outgoingLightNode;
    const r = e.context, n = r.lightingModel, i = e.getNodeProperties(this);
    if (n) {
      const { totalDiffuseNode: o, totalSpecularNode: u } = this;
      r.outgoingLight = s;
      const l = e.addStack();
      i.nodes = l.nodes, n.start(e);
      const { backdrop: c, backdropAlpha: d } = r, { directDiffuse: h, directSpecular: f, indirectDiffuse: p, indirectSpecular: g } = r.reflectedLight;
      let m = h.add(p);
      c !== null && (d !== null ? m = N(d.mix(m, c)) : m = N(c)), o.assign(m), u.assign(f.add(g)), s.assign(o.add(u)), n.finish(e), s = s.bypass(e.removeStack());
    } else
      i.nodes = [];
    return e.lightsNode = t, s;
  }
  /**
   * Configures this node with an array of lights.
   *
   * @param {Array<Light>} lights - An array of lights.
   * @return {LightsNode} A reference to this node.
   */
  setLights(e) {
    return this._lights = e, this._lightNodes = null, this._lightNodesHash = null, this;
  }
  /**
   * Returns an array of the scene's lights.
   *
   * @return {Array<Light>} The scene's lights.
   */
  getLights() {
    return this._lights;
  }
  /**
   * Whether the scene has lights or not.
   *
   * @type {boolean}
   */
  get hasLights() {
    return this._lights.length > 0;
  }
}
class K_ extends G {
  static get type() {
    return "ShadowBaseNode";
  }
  /**
   * Constructs a new shadow base node.
   *
   * @param {Light} light - The shadow casting light.
   */
  constructor(e) {
    super(), this.light = e, this.updateBeforeType = z.RENDER, this.isShadowBaseNode = !0;
  }
  /**
   * Setups the shadow position node which is by default the predefined TSL node object `shadowPositionWorld`.
   *
   * @param {NodeBuilder} object - A configuration object that must at least hold a material reference.
   */
  setupShadowPosition({ context: e, material: t }) {
    rh.assign(t.receivedShadowPositionNode || e.shadowPositionWorld || ar);
  }
}
const rh = /* @__PURE__ */ rs("vec3", "shadowPositionWorld");
function Q_(a, e = {}) {
  return e.toneMapping = a.toneMapping, e.toneMappingExposure = a.toneMappingExposure, e.outputColorSpace = a.outputColorSpace, e.renderTarget = a.getRenderTarget(), e.activeCubeFace = a.getActiveCubeFace(), e.activeMipmapLevel = a.getActiveMipmapLevel(), e.renderObjectFunction = a.getRenderObjectFunction(), e.pixelRatio = a.getPixelRatio(), e.mrt = a.getMRT(), e.clearColor = a.getClearColor(e.clearColor || new Ht()), e.clearAlpha = a.getClearAlpha(), e.autoClear = a.autoClear, e.scissorTest = a.getScissorTest(), e;
}
function Y_(a, e) {
  return e = Q_(a, e), a.setMRT(null), a.setRenderObjectFunction(null), a.setClearColor(0, 1), a.autoClear = !0, e;
}
function j_(a, e) {
  a.toneMapping = e.toneMapping, a.toneMappingExposure = e.toneMappingExposure, a.outputColorSpace = e.outputColorSpace, a.setRenderTarget(e.renderTarget, e.activeCubeFace, e.activeMipmapLevel), a.setRenderObjectFunction(e.renderObjectFunction), a.setPixelRatio(e.pixelRatio), a.setMRT(e.mrt), a.setClearColor(e.clearColor, e.clearAlpha), a.autoClear = e.autoClear, a.setScissorTest(e.scissorTest);
}
function Z_(a, e = {}) {
  return e.background = a.background, e.backgroundNode = a.backgroundNode, e.overrideMaterial = a.overrideMaterial, e;
}
function J_(a, e) {
  return e = Z_(a, e), a.background = null, a.backgroundNode = null, a.overrideMaterial = null, e;
}
function eb(a, e) {
  a.background = e.background, a.backgroundNode = e.backgroundNode, a.overrideMaterial = e.overrideMaterial;
}
function tb(a, e, t) {
  return t = Y_(a, t), t = J_(e, t), t;
}
function sb(a, e, t) {
  j_(a, t), eb(e, t);
}
const Yn = /* @__PURE__ */ new WeakMap(), rb = /* @__PURE__ */ A(({ depthTexture: a, shadowCoord: e, depthLayer: t }) => {
  let s = ce(a, e.xy).setName("t_basic");
  return a.isArrayTexture && (s = s.depth(t)), s.compare(e.z);
}), nb = /* @__PURE__ */ A(({ depthTexture: a, shadowCoord: e, shadow: t, depthLayer: s }) => {
  const r = (c, d) => {
    let h = ce(a, c);
    return a.isArrayTexture && (h = h.depth(s)), h.compare(d);
  }, n = ge("mapSize", "vec2", t).setGroup(V), i = ge("radius", "float", t).setGroup(V), o = I(1).div(n), u = i.mul(o.x), l = Zd(pr.xy).mul(6.28318530718);
  return We(
    r(e.xy.add(St(0, 5, l).mul(u)), e.z),
    r(e.xy.add(St(1, 5, l).mul(u)), e.z),
    r(e.xy.add(St(2, 5, l).mul(u)), e.z),
    r(e.xy.add(St(3, 5, l).mul(u)), e.z),
    r(e.xy.add(St(4, 5, l).mul(u)), e.z)
  ).mul(1 / 5);
}), ib = /* @__PURE__ */ A(({ depthTexture: a, shadowCoord: e, shadow: t, depthLayer: s }) => {
  const r = (d, h) => {
    let f = ce(a, d);
    return a.isArrayTexture && (f = f.depth(s)), f.compare(h);
  }, n = ge("mapSize", "vec2", t).setGroup(V), i = I(1).div(n), o = i.x, u = i.y, l = e.xy, c = wt(l.mul(n).add(0.5));
  return l.subAssign(c.mul(i)), We(
    r(l, e.z),
    r(l.add(I(o, 0)), e.z),
    r(l.add(I(0, u)), e.z),
    r(l.add(i), e.z),
    ne(
      r(l.add(I(o.negate(), 0)), e.z),
      r(l.add(I(o.mul(2), 0)), e.z),
      c.x
    ),
    ne(
      r(l.add(I(o.negate(), u)), e.z),
      r(l.add(I(o.mul(2), u)), e.z),
      c.x
    ),
    ne(
      r(l.add(I(0, u.negate())), e.z),
      r(l.add(I(0, u.mul(2))), e.z),
      c.y
    ),
    ne(
      r(l.add(I(o, u.negate())), e.z),
      r(l.add(I(o, u.mul(2))), e.z),
      c.y
    ),
    ne(
      ne(
        r(l.add(I(o.negate(), u.negate())), e.z),
        r(l.add(I(o.mul(2), u.negate())), e.z),
        c.x
      ),
      ne(
        r(l.add(I(o.negate(), u.mul(2))), e.z),
        r(l.add(I(o.mul(2), u.mul(2))), e.z),
        c.x
      ),
      c.y
    )
  ).mul(1 / 9);
}), ob = /* @__PURE__ */ A(({ depthTexture: a, shadowCoord: e, depthLayer: t }, s) => {
  let r = ce(a).sample(e.xy);
  a.isArrayTexture && (r = r.depth(t)), r = r.rg;
  const n = r.x, i = Be(1e-7, r.y.mul(r.y)), o = s.renderer.reversedDepthBuffer ? Wr(n, e.z) : Wr(e.z, n), u = b(1).toVar();
  return ue(o.notEqual(1), () => {
    const l = e.z.sub(n);
    let c = i.div(i.add(l.mul(l)));
    c = os(ke(c, 0.3).div(0.65)), u.assign(Be(o, c));
  }), u;
}), ab = (a) => {
  let e = Yn.get(a);
  return e === void 0 && (e = new Ne(), e.colorNode = W(0, 0, 0, 1), e.isShadowPassMaterial = !0, e.name = "ShadowMaterial", e.blending = ss, e.fog = !1, Yn.set(a, e)), e;
}, ub = (a) => {
  const e = Yn.get(a);
  e !== void 0 && (e.dispose(), Yn.delete(a));
}, Ju = /* @__PURE__ */ new us(), Vs = [], lb = (a, e, t, s) => {
  Vs[0] = a, Vs[1] = e;
  let r = Ju.get(Vs);
  return (r === void 0 || r.shadowType !== t || r.useVelocity !== s) && (r = (n, i, o, u, l, c, ...d) => {
    (n.castShadow === !0 || n.receiveShadow && t === Br) && (s && (_c(n).useVelocity = !0), n.onBeforeShadow(a, n, o, e.camera, u, i.overrideMaterial, c), a.renderObject(n, i, o, u, l, c, ...d), n.onAfterShadow(a, n, o, e.camera, u, i.overrideMaterial, c));
  }, r.shadowType = t, r.useVelocity = s, Ju.set(Vs, r)), Vs[0] = null, Vs[1] = null, r;
}, cb = /* @__PURE__ */ A(({ samples: a, radius: e, size: t, shadowPass: s, depthLayer: r }) => {
  const n = b(0).toVar("meanVertical"), i = b(0).toVar("squareMeanVertical"), o = a.lessThanEqual(b(1)).select(b(0), b(2).div(a.sub(1))), u = a.lessThanEqual(b(1)).select(b(0), b(-1));
  rt({ start: pe(0), end: pe(a), type: "int", condition: "<" }, ({ i: c }) => {
    const d = u.add(b(c).mul(o));
    let h = s.sample(We(pr.xy, I(0, d).mul(e)).div(t));
    s.value.isArrayTexture && (h = h.depth(r)), h = h.x, n.addAssign(h), i.addAssign(h.mul(h));
  }), n.divAssign(a), i.divAssign(a);
  const l = ns(i.sub(n.mul(n)).max(0));
  return I(n, l);
}), db = /* @__PURE__ */ A(({ samples: a, radius: e, size: t, shadowPass: s, depthLayer: r }) => {
  const n = b(0).toVar("meanHorizontal"), i = b(0).toVar("squareMeanHorizontal"), o = a.lessThanEqual(b(1)).select(b(0), b(2).div(a.sub(1))), u = a.lessThanEqual(b(1)).select(b(0), b(-1));
  rt({ start: pe(0), end: pe(a), type: "int", condition: "<" }, ({ i: c }) => {
    const d = u.add(b(c).mul(o));
    let h = s.sample(We(pr.xy, I(d, 0).mul(e)).div(t));
    s.value.isArrayTexture && (h = h.depth(r)), n.addAssign(h.x), i.addAssign(We(h.y.mul(h.y), h.x.mul(h.x)));
  }), n.divAssign(a), i.divAssign(a);
  const l = ns(i.sub(n.mul(n)).max(0));
  return I(n, l);
}), hb = [rb, nb, ib, ob];
let Zi;
const xn = /* @__PURE__ */ new jd();
class nh extends K_ {
  static get type() {
    return "ShadowNode";
  }
  /**
   * Constructs a new shadow node.
   *
   * @param {Light} light - The shadow casting light.
   * @param {?LightShadow} [shadow=null] - An optional light shadow.
   */
  constructor(e, t = null) {
    super(e), this.shadow = t || e.shadow, this.shadowMap = null, this.vsmShadowMapVertical = null, this.vsmShadowMapHorizontal = null, this.vsmMaterialVertical = null, this.vsmMaterialHorizontal = null, this._node = null, this._currentShadowType = null, this._cameraFrameId = /* @__PURE__ */ new WeakMap(), this.isShadowNode = !0, this.depthLayer = 0;
  }
  /**
   * Setups the shadow filtering.
   *
   * @param {NodeBuilder} builder - A reference to the current node builder.
   * @param {Object} inputs - A configuration object that defines the shadow filtering.
   * @param {Function} inputs.filterFn - This function defines the filtering type of the shadow map e.g. PCF.
   * @param {DepthTexture} inputs.depthTexture - A reference to the shadow map's texture data.
   * @param {Node<vec3>} inputs.shadowCoord - Shadow coordinates which are used to sample from the shadow map.
   * @param {LightShadow} inputs.shadow - The light shadow.
   * @return {Node<float>} The result node of the shadow filtering.
   */
  setupShadowFilter(e, { filterFn: t, depthTexture: s, shadowCoord: r, shadow: n, depthLayer: i }) {
    const o = r.x.greaterThanEqual(0).and(r.x.lessThanEqual(1)).and(r.y.greaterThanEqual(0)).and(r.y.lessThanEqual(1)).and(r.z.lessThanEqual(1)), u = t({ depthTexture: s, shadowCoord: r, shadow: n, depthLayer: i });
    return o.select(u, b(1));
  }
  /**
   * Setups the shadow coordinates.
   *
   * @param {NodeBuilder} builder - A reference to the current node builder.
   * @param {Node<vec3>} shadowPosition - A node representing the shadow position.
   * @return {Node<vec3>} The shadow coordinates.
   */
  setupShadowCoord(e, t) {
    const { shadow: s } = this, { renderer: r } = e, n = s.biasNode || ge("bias", "float", s).setGroup(V);
    let i = t, o;
    if (s.camera.isOrthographicCamera || r.logarithmicDepthBuffer !== !0)
      i = i.xyz.div(i.w), o = i.z;
    else {
      const u = i.w;
      i = i.xy.div(u);
      const l = ge("near", "float", s.camera).setGroup(V), c = ge("far", "float", s.camera).setGroup(V);
      o = Ed(u.negate(), l, c);
    }
    return i = N(
      i.x,
      i.y.oneMinus(),
      // follow webgpu standards
      r.reversedDepthBuffer ? o.sub(n) : o.add(n)
    ), i;
  }
  /**
   * Returns the shadow filtering function for the given shadow type.
   *
   * @param {number} type - The shadow type.
   * @return {Function} The filtering function.
   */
  getShadowFilterFn(e) {
    return hb[e];
  }
  setupRenderTarget(e, t) {
    const s = new vt(e.mapSize.width, e.mapSize.height);
    s.name = "ShadowDepthTexture", s.compareFunction = t.renderer.reversedDepthBuffer ? nr : Qr;
    const r = t.createRenderTarget(e.mapSize.width, e.mapSize.height);
    return r.texture.name = "ShadowMap", r.texture.type = e.mapType, r.depthTexture = s, { shadowMap: r, depthTexture: s };
  }
  /**
   * Setups the shadow output node.
   *
   * @param {NodeBuilder} builder - A reference to the current node builder.
   * @return {Node<vec3>} The shadow output node.
   */
  setupShadow(e) {
    const { renderer: t, camera: s } = e, { light: r, shadow: n } = this, { depthTexture: i, shadowMap: o } = this.setupRenderTarget(n, e), u = t.shadowMap.type, l = t.hasCompatibility(Ps.TEXTURE_COMPARE);
    if ((u === Fl || u === Pf) && l ? (i.minFilter = Ze, i.magFilter = Ze) : (i.minFilter = Qe, i.magFilter = Qe), n.camera.coordinateSystem = s.coordinateSystem, n.camera.updateProjectionMatrix(), u === Br && n.isPointLightShadow !== !0) {
      i.compareFunction = null, o.depth > 1 ? (o._vsmShadowMapVertical || (o._vsmShadowMapVertical = e.createRenderTarget(n.mapSize.width, n.mapSize.height, { format: Rt, type: Oe, depth: o.depth, depthBuffer: !1 }), o._vsmShadowMapVertical.texture.name = "VSMVertical"), this.vsmShadowMapVertical = o._vsmShadowMapVertical, o._vsmShadowMapHorizontal || (o._vsmShadowMapHorizontal = e.createRenderTarget(n.mapSize.width, n.mapSize.height, { format: Rt, type: Oe, depth: o.depth, depthBuffer: !1 }), o._vsmShadowMapHorizontal.texture.name = "VSMHorizontal"), this.vsmShadowMapHorizontal = o._vsmShadowMapHorizontal) : (this.vsmShadowMapVertical = e.createRenderTarget(n.mapSize.width, n.mapSize.height, { format: Rt, type: Oe, depthBuffer: !1 }), this.vsmShadowMapHorizontal = e.createRenderTarget(n.mapSize.width, n.mapSize.height, { format: Rt, type: Oe, depthBuffer: !1 }));
      let M = ce(i);
      i.isArrayTexture && (M = M.depth(this.depthLayer));
      let E = ce(this.vsmShadowMapVertical.texture);
      i.isArrayTexture && (E = E.depth(this.depthLayer));
      const U = ge("blurSamples", "float", n).setGroup(V), P = ge("radius", "float", n).setGroup(V), $ = ge("mapSize", "vec2", n).setGroup(V);
      let Y = this.vsmMaterialVertical || (this.vsmMaterialVertical = new Ne());
      Y.fragmentNode = cb({ samples: U, radius: P, size: $, shadowPass: M, depthLayer: this.depthLayer }).context(e.getSharedContext()), Y.name = "VSMVertical", Y = this.vsmMaterialHorizontal || (this.vsmMaterialHorizontal = new Ne()), Y.fragmentNode = db({ samples: U, radius: P, size: $, shadowPass: E, depthLayer: this.depthLayer }).context(e.getSharedContext()), Y.name = "VSMHorizontal";
    }
    const c = ge("intensity", "float", n).setGroup(V), d = ge("normalBias", "float", n).setGroup(V), h = Ia(r), f = gr.mul(d);
    let p;
    !t.highPrecision || e.material.receivedShadowPositionNode || e.context.shadowPositionWorld ? p = h.mul(rh.add(f)) : p = k("mat4").onObjectUpdate(({ object: E }, U) => U.value.multiplyMatrices(h.value, E.matrixWorld)).mul(ve).add(h.mul(W(f, 0)));
    const g = this.setupShadowCoord(e, p), m = n.filterNode || this.getShadowFilterFn(t.shadowMap.type) || null;
    if (m === null)
      throw new Error("THREE.WebGPURenderer: Shadow map type not supported yet.");
    const y = u === Br && n.isPointLightShadow !== !0 ? this.vsmShadowMapHorizontal.texture : i, _ = this.setupShadowFilter(e, { filterFn: m, shadowTexture: o.texture, depthTexture: y, shadowCoord: g, shadow: n, depthLayer: this.depthLayer });
    let v;
    t.shadowMap.transmitted === !0 && (o.texture.isCubeTexture ? v = Ve(o.texture, g.xyz) : (v = ce(o.texture, g), i.isArrayTexture && (v = v.depth(this.depthLayer))));
    let C;
    v ? C = ne(1, _.rgb.mix(v, 1), c.mul(v.a)).toVar() : C = ne(1, _, c).toVar(), this.shadowMap = o, this.shadow.map = o;
    const w = `${this.light.type} Shadow [ ${this.light.name || "ID: " + this.light.id} ]`;
    return v && C.toInspector(`${w} / Color`, () => this.shadowMap.texture.isCubeTexture ? Ve(this.shadowMap.texture) : ce(this.shadowMap.texture)), C.toInspector(`${w} / Depth`, () => this.shadowMap.texture.isCubeTexture ? Ve(this.shadowMap.texture).r.oneMinus() : tt(this.shadowMap.depthTexture, hr().mul(rr(ce(this.shadowMap.depthTexture)))).r.oneMinus());
  }
  /**
   * The implementation performs the setup of the output node. An output is only
   * produces if shadow mapping is globally enabled in the renderer.
   *
   * @param {NodeBuilder} builder - A reference to the current node builder.
   * @return {ShaderCallNodeInternal} The output node.
   */
  setup(e) {
    if (e.renderer.shadowMap.enabled !== !1)
      return A(() => {
        const t = e.renderer.shadowMap.type;
        this._currentShadowType !== t && (this._reset(), this._node = null);
        let s = this._node;
        return this.setupShadowPosition(e), s === null && (this._node = s = this.setupShadow(e), this._currentShadowType = t), e.material.receivedShadowNode && (s = e.material.receivedShadowNode(s)), s;
      })();
  }
  /**
   * Renders the shadow. The logic of this function could be included
   * into {@link ShadowNode#updateShadow} however more specialized shadow
   * nodes might require a custom shadow map rendering. By having a
   * dedicated method, it's easier to overwrite the default behavior.
   *
   * @param {NodeFrame} frame - A reference to the current node frame.
   */
  renderShadow(e) {
    const { shadow: t, shadowMap: s, light: r } = this, { renderer: n, scene: i } = e;
    t.updateMatrices(r), s.setSize(t.mapSize.width, t.mapSize.height, s.depth);
    const o = i.name;
    i.name = `Shadow Map [ ${r.name || "ID: " + r.id} ]`, n.render(i, t.camera), i.name = o;
  }
  /**
   * Updates the shadow.
   *
   * @param {NodeFrame} frame - A reference to the current node frame.
   */
  updateShadow(e) {
    const { shadowMap: t, light: s, shadow: r } = this, { renderer: n, scene: i, camera: o } = e, u = n.shadowMap.type, l = t.depthTexture.version;
    this._depthVersionCached = l;
    const c = r.camera.layers.mask;
    (r.camera.layers.mask & 4294967294) === 0 && (r.camera.layers.mask = o.layers.mask);
    const d = n.getRenderObjectFunction(), h = n.getMRT(), f = h ? h.has("velocity") : !1;
    Zi = tb(n, i, Zi), i.overrideMaterial = ab(s), n.setRenderObjectFunction(lb(n, r, u, f)), n.setClearColor(0, 0), n.setRenderTarget(t), this.renderShadow(e), n.setRenderObjectFunction(d), u === Br && r.isPointLightShadow !== !0 && this.vsmPass(n), r.camera.layers.mask = c, sb(n, i, Zi);
  }
  /**
   * For VSM additional render passes are required.
   *
   * @param {Renderer} renderer - A reference to the current renderer.
   */
  vsmPass(e) {
    const { shadow: t } = this, s = this.shadowMap.depth;
    this.vsmShadowMapVertical.setSize(t.mapSize.width, t.mapSize.height, s), this.vsmShadowMapHorizontal.setSize(t.mapSize.width, t.mapSize.height, s), e.setRenderTarget(this.vsmShadowMapVertical), xn.material = this.vsmMaterialVertical, xn.render(e), e.setRenderTarget(this.vsmShadowMapHorizontal), xn.material = this.vsmMaterialHorizontal, xn.render(e);
  }
  /**
   * Frees the internal resources of this shadow node.
   */
  dispose() {
    this._reset(), super.dispose();
  }
  /**
   * Resets the resouce state of this shadow node.
   *
   * @private
   */
  _reset() {
    this._currentShadowType = null, ub(this.light), this.shadowMap && (this.shadowMap.dispose(), this.shadowMap = null), this.vsmShadowMapVertical !== null && (this.vsmShadowMapVertical.dispose(), this.vsmShadowMapVertical = null, this.vsmMaterialVertical.dispose(), this.vsmMaterialVertical = null), this.vsmShadowMapHorizontal !== null && (this.vsmShadowMapHorizontal.dispose(), this.vsmShadowMapHorizontal = null, this.vsmMaterialHorizontal.dispose(), this.vsmMaterialHorizontal = null);
  }
  /**
   * The implementation performs the update of the shadow map if necessary.
   *
   * @param {NodeFrame} frame - A reference to the current node frame.
   */
  updateBefore(e) {
    const { shadow: t } = this;
    let s = t.needsUpdate || t.autoUpdate;
    s && (this._cameraFrameId[e.camera] === e.frameId && (s = !1), this._cameraFrameId[e.camera] = e.frameId), s && (this.updateShadow(e), this.shadowMap.depthTexture.version === this._depthVersionCached && (t.needsUpdate = !1));
  }
}
const fb = (a, e) => new nh(a, e), pb = /* @__PURE__ */ new Ht(), el = /* @__PURE__ */ new dt(), Rr = /* @__PURE__ */ new H(), Ji = /* @__PURE__ */ new H(), gb = [
  /* @__PURE__ */ new H(1, 0, 0),
  /* @__PURE__ */ new H(-1, 0, 0),
  /* @__PURE__ */ new H(0, -1, 0),
  /* @__PURE__ */ new H(0, 1, 0),
  /* @__PURE__ */ new H(0, 0, 1),
  /* @__PURE__ */ new H(0, 0, -1)
], mb = [
  /* @__PURE__ */ new H(0, -1, 0),
  /* @__PURE__ */ new H(0, -1, 0),
  /* @__PURE__ */ new H(0, 0, -1),
  /* @__PURE__ */ new H(0, 0, 1),
  /* @__PURE__ */ new H(0, -1, 0),
  /* @__PURE__ */ new H(0, -1, 0)
], xb = [
  /* @__PURE__ */ new H(1, 0, 0),
  /* @__PURE__ */ new H(-1, 0, 0),
  /* @__PURE__ */ new H(0, 1, 0),
  /* @__PURE__ */ new H(0, -1, 0),
  /* @__PURE__ */ new H(0, 0, 1),
  /* @__PURE__ */ new H(0, 0, -1)
], Tb = [
  /* @__PURE__ */ new H(0, -1, 0),
  /* @__PURE__ */ new H(0, -1, 0),
  /* @__PURE__ */ new H(0, 0, 1),
  /* @__PURE__ */ new H(0, 0, -1),
  /* @__PURE__ */ new H(0, -1, 0),
  /* @__PURE__ */ new H(0, -1, 0)
], yb = /* @__PURE__ */ A(({ depthTexture: a, bd3D: e, dp: t }) => Ve(a, e).compare(t)), _b = /* @__PURE__ */ A(({ depthTexture: a, bd3D: e, dp: t, shadow: s }) => {
  const r = ge("radius", "float", s).setGroup(V), n = ge("mapSize", "vec2", s).setGroup(V), i = r.div(n.x), o = Xe(e), u = Et(Fs(e, o.x.greaterThan(o.z).select(N(0, 1, 0), N(1, 0, 0)))), l = Fs(e, u), c = Zd(pr.xy).mul(6.28318530718), d = St(0, 5, c), h = St(1, 5, c), f = St(2, 5, c), p = St(3, 5, c), g = St(4, 5, c);
  return Ve(a, e.add(u.mul(d.x).add(l.mul(d.y)).mul(i))).compare(t).add(Ve(a, e.add(u.mul(h.x).add(l.mul(h.y)).mul(i))).compare(t)).add(Ve(a, e.add(u.mul(f.x).add(l.mul(f.y)).mul(i))).compare(t)).add(Ve(a, e.add(u.mul(p.x).add(l.mul(p.y)).mul(i))).compare(t)).add(Ve(a, e.add(u.mul(g.x).add(l.mul(g.y)).mul(i))).compare(t)).mul(1 / 5);
}), bb = /* @__PURE__ */ A(({ filterFn: a, depthTexture: e, shadowCoord: t, shadow: s }, r) => {
  const n = t.xyz.toConst(), i = n.abs().toConst(), o = i.x.max(i.y).max(i.z), u = k("float").setGroup(V).onRenderUpdate(() => s.camera.near), l = k("float").setGroup(V).onRenderUpdate(() => s.camera.far), c = ge("bias", "float", s).setGroup(V), d = b(1).toVar();
  return ue(o.sub(l).lessThanEqual(0).and(o.sub(u).greaterThanEqual(0)), () => {
    let h;
    r.renderer.reversedDepthBuffer ? (h = cT(o.negate(), u, l), h.subAssign(c)) : (h = Ad(o.negate(), u, l), h.addAssign(c));
    const f = n.normalize();
    d.assign(a({ depthTexture: e, bd3D: f, dp: h, shadow: s }));
  }), d;
});
class Sb extends nh {
  static get type() {
    return "PointShadowNode";
  }
  /**
   * Constructs a new point shadow node.
   *
   * @param {PointLight} light - The shadow casting point light.
   * @param {?PointLightShadow} [shadow=null] - An optional point light shadow.
   */
  constructor(e, t = null) {
    super(e, t);
  }
  /**
   * Overwrites the default implementation to return point light shadow specific
   * filtering functions.
   *
   * @param {number} type - The shadow type.
   * @return {Function} The filtering function.
   */
  getShadowFilterFn(e) {
    return e === Uf ? yb : _b;
  }
  /**
   * Overwrites the default implementation so the unaltered shadow position is used.
   *
   * @param {NodeBuilder} builder - A reference to the current node builder.
   * @param {Node<vec3>} shadowPosition - A node representing the shadow position.
   * @return {Node<vec3>} The shadow coordinates.
   */
  setupShadowCoord(e, t) {
    return t;
  }
  /**
   * Overwrites the default implementation to only use point light specific
   * shadow filter functions.
   *
   * @param {NodeBuilder} builder - A reference to the current node builder.
   * @param {Object} inputs - A configuration object that defines the shadow filtering.
   * @param {Function} inputs.filterFn - This function defines the filtering type of the shadow map e.g. PCF.
   * @param {DepthTexture} inputs.depthTexture - A reference to the shadow map's depth texture.
   * @param {Node<vec3>} inputs.shadowCoord - Shadow coordinates which are used to sample from the shadow map.
   * @param {LightShadow} inputs.shadow - The light shadow.
   * @return {Node<float>} The result node of the shadow filtering.
   */
  setupShadowFilter(e, { filterFn: t, depthTexture: s, shadowCoord: r, shadow: n }) {
    return bb({ filterFn: t, depthTexture: s, shadowCoord: r, shadow: n });
  }
  /**
   * Overwrites the default implementation to create a CubeRenderTarget with CubeDepthTexture.
   *
   * @param {LightShadow} shadow - The light shadow object.
   * @param {NodeBuilder} builder - A reference to the current node builder.
   * @return {Object} An object containing the shadow map and depth texture.
   */
  setupRenderTarget(e, t) {
    const s = new Df(e.mapSize.width);
    s.name = "PointShadowDepthTexture", s.compareFunction = t.renderer.reversedDepthBuffer ? nr : Qr;
    const r = t.createCubeRenderTarget(e.mapSize.width);
    return r.texture.name = "PointShadowMap", r.depthTexture = s, { shadowMap: r, depthTexture: s };
  }
  /**
   * Overwrites the default implementation with point light specific
   * rendering code.
   *
   * @param {NodeFrame} frame - A reference to the current node frame.
   */
  renderShadow(e) {
    const { shadow: t, shadowMap: s, light: r } = this, { renderer: n, scene: i } = e, o = t.camera, u = t.matrix, l = n.coordinateSystem === kr, c = l ? gb : xb, d = l ? mb : Tb;
    s.setSize(t.mapSize.width, t.mapSize.width);
    const h = n.autoClear, f = n.getClearColor(pb), p = n.getClearAlpha();
    n.autoClear = !1, n.setClearColor(t.clearColor, t.clearAlpha);
    for (let g = 0; g < 6; g++) {
      n.setRenderTarget(s, g), n.clear();
      const m = r.distance || o.far;
      m !== o.far && (o.far = m, o.updateProjectionMatrix()), Rr.setFromMatrixPosition(r.matrixWorld), o.position.copy(Rr), Ji.copy(o.position), Ji.add(c[g]), o.up.copy(d[g]), o.lookAt(Ji), o.updateMatrixWorld(), u.makeTranslation(-Rr.x, -Rr.y, -Rr.z), el.multiplyMatrices(o.projectionMatrix, o.matrixWorldInverse), t._frustum.setFromProjectionMatrix(el, o.coordinateSystem, o.reversedDepth);
      const y = i.name;
      i.name = `Point Light Shadow [ ${r.name || "ID: " + r.id} ] - Face ${g + 1}`, n.render(i, o), i.name = y;
    }
    n.autoClear = h, n.setClearColor(f, p);
  }
}
const Nb = (a, e) => new Sb(a, e);
class Os extends mr {
  static get type() {
    return "AnalyticLightNode";
  }
  /**
   * Constructs a new analytic light node.
   *
   * @param {?Light} [light=null] - The light source.
   */
  constructor(e = null) {
    super(), this.light = e, this.color = new Ht(), this.colorNode = e && e.colorNode || k(this.color).setGroup(V), this.baseColorNode = null, this.shadowNode = null, this.shadowColorNode = null, this.isAnalyticLightNode = !0, this.updateType = z.FRAME, e && e.shadow && (this._shadowDisposeListener = () => {
      this.disposeShadow();
    }, e.addEventListener("dispose", this._shadowDisposeListener));
  }
  dispose() {
    this._shadowDisposeListener && this.light.removeEventListener("dispose", this._shadowDisposeListener), super.dispose();
  }
  /**
   * Frees internal resources related to shadows.
   */
  disposeShadow() {
    this.shadowNode !== null && (this.shadowNode.dispose(), this.shadowNode = null), this.shadowColorNode = null, this.baseColorNode !== null && (this.colorNode = this.baseColorNode, this.baseColorNode = null);
  }
  getHash() {
    return this.light.uuid;
  }
  /**
   * Returns a node representing a direction vector which points from the current
   * position in view space to the light's position in view space.
   *
   * @param {NodeBuilder} builder - The builder object used for setting up the light.
   * @return {Node<vec3>} The light vector node.
   */
  getLightVector(e) {
    return eh(this.light).sub(e.context.positionView || be);
  }
  /**
   * Sets up the direct lighting for the analytic light node.
   *
   * @abstract
   * @param {NodeBuilder} builder - The builder object used for setting up the light.
   * @return {Object|undefined} The direct light data (color and direction).
   */
  setupDirect() {
  }
  /**
   * Sets up the direct rect area lighting for the analytic light node.
   *
   * @abstract
   * @param {NodeBuilder} builder - The builder object used for setting up the light.
   * @return {Object|undefined} The direct rect area light data.
   */
  setupDirectRectArea() {
  }
  /**
   * Setups the shadow node for this light. The method exists so concrete light classes
   * can setup different types of shadow nodes.
   *
   * @return {ShadowNode} The created shadow node.
   */
  setupShadowNode() {
    return fb(this.light);
  }
  /**
   * Setups the shadow for this light. This method is only executed if the light
   * cast shadows and the current build object receives shadows. It incorporates
   * shadows into the lighting computation.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  setupShadow(e) {
    const { renderer: t } = e;
    if (t.shadowMap.enabled === !1) return;
    let s = this.shadowColorNode;
    if (s === null) {
      const r = this.light.shadow.shadowNode;
      let n;
      r !== void 0 ? n = O(r) : n = this.setupShadowNode(), this.shadowNode = n, this.shadowColorNode = s = this.colorNode.mul(n), this.baseColorNode = this.colorNode;
    }
    e.context.getShadow && (s = e.context.getShadow(this, e)), this.colorNode = s;
  }
  /**
   * Unlike most other nodes, lighting nodes do not return a output node in {@link Node#setup}.
   * The main purpose of lighting nodes is to configure the current {@link LightingModel} and/or
   * invocate the respective interface methods.
   *
   * @param {NodeBuilder} builder - The current node builder.
   */
  setup(e) {
    this.colorNode = this.baseColorNode || this.colorNode, this.light.castShadow ? e.object.receiveShadow && this.setupShadow(e) : this.shadowNode !== null && (this.shadowNode.dispose(), this.shadowNode = null, this.shadowColorNode = null);
    const t = this.setupDirect(e), s = this.setupDirectRectArea(e);
    t && e.lightsNode.setupDirectLight(e, this, t), s && e.lightsNode.setupDirectRectAreaLight(e, this, s);
  }
  /**
   * The update method is used to update light uniforms per frame.
   * Potentially overwritten in concrete light nodes to update light
   * specific uniforms.
   *
   * @param {NodeFrame} frame - A reference to the current node frame.
   */
  update() {
    const { light: e } = this;
    this.color.copy(e.color).multiplyScalar(e.intensity);
  }
}
const ih = /* @__PURE__ */ A(({ lightDistance: a, cutoffDistance: e, decayExponent: t }) => {
  const s = a.pow(t).max(0.01).reciprocal();
  return e.greaterThan(0).select(
    s.mul(a.div(e).pow4().oneMinus().clamp().pow2()),
    s
  );
}), Rb = ({ color: a, lightVector: e, cutoffDistance: t, decayExponent: s }) => {
  const r = e.normalize(), n = e.length(), i = ih({
    lightDistance: n,
    cutoffDistance: t,
    decayExponent: s
  }), o = a.mul(i);
  return { lightDirection: r, lightColor: o };
};
class Ab extends Os {
  static get type() {
    return "PointLightNode";
  }
  /**
   * Constructs a new point light node.
   *
   * @param {?PointLight} [light=null] - The point light source.
   */
  constructor(e = null) {
    super(e), this.cutoffDistanceNode = k(0).setGroup(V), this.decayExponentNode = k(2).setGroup(V);
  }
  /**
   * Overwritten to updated point light specific uniforms.
   *
   * @param {NodeFrame} frame - A reference to the current node frame.
   */
  update(e) {
    const { light: t } = this;
    super.update(e), this.cutoffDistanceNode.value = t.distance, this.decayExponentNode.value = t.decay;
  }
  /**
   * Overwritten to setup point light specific shadow.
   *
   * @return {PointShadowNode}
   */
  setupShadowNode() {
    return Nb(this.light);
  }
  setupDirect(e) {
    return Rb({
      color: this.colorNode,
      lightVector: this.getLightVector(e),
      cutoffDistance: this.cutoffDistanceNode,
      decayExponent: this.decayExponentNode
    });
  }
}
A(([a = hr()], { renderer: e, material: t }) => {
  const s = qc(a.mul(2).sub(1));
  let r;
  if (t.alphaToCoverage && e.currentSamples > 0) {
    const n = b(s.fwidth()).toVar();
    r = Wt(n.oneMinus(), n.add(1), s).oneMinus();
  } else
    r = Ye(s.greaterThan(1), 0, 1);
  return r;
});
const vb = /* @__PURE__ */ A(([a, e]) => {
  const t = a.x, s = a.y, r = a.z;
  let n = e.element(0).mul(0.886227);
  return n = n.add(e.element(1).mul(2 * 0.511664).mul(s)), n = n.add(e.element(2).mul(2 * 0.511664).mul(r)), n = n.add(e.element(3).mul(2 * 0.511664).mul(t)), n = n.add(e.element(4).mul(2 * 0.429043).mul(t).mul(s)), n = n.add(e.element(5).mul(2 * 0.429043).mul(s).mul(r)), n = n.add(e.element(6).mul(r.mul(r).mul(0.743125).sub(0.247708))), n = n.add(e.element(7).mul(2 * 0.429043).mul(t).mul(r)), n = n.add(e.element(8).mul(0.429043).mul(X(t, t).sub(X(s, s)))), n;
}), ut = /* @__PURE__ */ new Pa();
class Eb extends ls {
  /**
   * Constructs a new background management component.
   *
   * @param {Renderer} renderer - The renderer.
   * @param {NodeManager} nodes - Renderer component for managing nodes related logic.
   */
  constructor(e, t) {
    super(), this.renderer = e, this.nodes = t;
  }
  /**
   * Updates the background for the given scene. Depending on how `Scene.background`
   * or `Scene.backgroundNode` are configured, this method might configure a simple clear
   * or add a mesh to the render list for rendering the background as a textured plane
   * or skybox.
   *
   * @param {Scene} scene - The scene.
   * @param {RenderList} renderList - The current render list.
   * @param {RenderContext} renderContext - The current render context.
   */
  update(e, t, s) {
    const r = this.renderer, n = this.nodes.getBackgroundNode(e) || e.background;
    let i = !1;
    if (n === null)
      r._clearColor.getRGB(ut), ut.a = r._clearColor.a;
    else if (n.isColor === !0)
      n.getRGB(ut), ut.a = 1, i = !0;
    else if (n.isNode === !0) {
      const u = this.get(e), l = n;
      ut.copy(r._clearColor);
      let c = u.backgroundMesh;
      if (c === void 0) {
        let v = function() {
          n.removeEventListener("dispose", v), c.material.dispose(), c.geometry.dispose();
        };
        const h = W(l).mul(Yu).context({
          // @TODO: Add Texture2D support using node context
          getUV: () => C_.mul(Om),
          getTextureLevel: () => E_
        }), f = Ir.element(3).element(3).equal(1), p = Ct(1, Ir.element(1).element(1)).mul(3), g = f.select(ve.mul(p), ve), m = Jr.mul(W(g, 0));
        let y = Ir.mul(W(m.xyz, 1));
        y = y.setZ(y.w);
        const _ = new Ne();
        _.name = "Background.material", _.side = Ge, _.depthTest = !1, _.depthWrite = !1, _.allowOverride = !1, _.fog = !1, _.lights = !1, _.vertexNode = y, _.colorNode = h, u.backgroundMeshNode = h, u.backgroundMesh = c = new is(new kh(1, 32, 32), _), c.frustumCulled = !1, c.name = "Background.mesh", n.addEventListener("dispose", v);
      }
      const d = l.getCacheKey();
      u.backgroundCacheKey !== d && (u.backgroundMeshNode.node = W(l).mul(Yu), u.backgroundMeshNode.needsUpdate = !0, c.material.needsUpdate = !0, u.backgroundCacheKey = d), t.unshift(c, c.geometry, c.material, 0, 0, null, null);
    } else
      F("Renderer: Unsupported background configuration.", n);
    const o = r.xr.getEnvironmentBlendMode();
    if (o === "additive" ? ut.set(0, 0, 0, 1) : o === "alpha-blend" && ut.set(0, 0, 0, 0), r.autoClear === !0 || i === !0) {
      const u = s.clearColorValue;
      u.r = ut.r, u.g = ut.g, u.b = ut.b, u.a = ut.a, (r.backend.isWebGLBackend === !0 || r.alpha === !0) && (u.r *= u.a, u.g *= u.a, u.b *= u.a), s.depthClearValue = r.getClearDepth(), s.stencilClearValue = r.getClearStencil(), s.clearColor = r.autoClearColor === !0, s.clearDepth = r.autoClearDepth === !0, s.clearStencil = r.autoClearStencil === !0;
    } else
      s.clearColor = !1, s.clearDepth = !1, s.clearStencil = !1;
  }
}
let Cb = 0;
class ea {
  /**
   * Constructs a new bind group.
   *
   * @param {string} name - The bind group's name.
   * @param {Array<Binding>} bindings - An array of bindings.
   * @param {number} index - The group index.
   */
  constructor(e = "", t = []) {
    this.name = e, this.bindings = t, this.id = Cb++;
  }
}
class wb {
  /**
   * Constructs a new node builder state.
   *
   * @param {string} vertexShader - The native vertex shader code.
   * @param {string} fragmentShader - The native fragment shader code.
   * @param {string} computeShader - The native compute shader code.
   * @param {Array<NodeAttribute>} nodeAttributes - An array of node attributes.
   * @param {Array<BindGroup>} bindings - An array of bind groups.
   * @param {Array<Node>} updateNodes - An array of nodes that implement their `update()` method.
   * @param {Array<Node>} updateBeforeNodes - An array of nodes that implement their `updateBefore()` method.
   * @param {Array<Node>} updateAfterNodes - An array of nodes that implement their `updateAfter()` method.
   * @param {NodeMaterialObserver} observer - A node material observer.
   * @param {Array<Object>} transforms - An array with transform attribute objects. Only relevant when using compute shaders with WebGL 2.
   */
  constructor(e, t, s, r, n, i, o, u, l, c = []) {
    this.vertexShader = e, this.fragmentShader = t, this.computeShader = s, this.transforms = c, this.nodeAttributes = r, this.bindings = n, this.updateNodes = i, this.updateBeforeNodes = o, this.updateAfterNodes = u, this.observer = l, this.usedTimes = 0;
  }
  /**
   * This method is used to create a array of bind groups based
   * on the existing bind groups of this state. Shared groups are
   * not cloned.
   *
   * @return {Array<BindGroup>} A array of bind groups.
   */
  createBindings() {
    const e = [];
    for (const t of this.bindings)
      if (t.bindings[0].groupNode.shared !== !0) {
        const r = new ea(t.name, []);
        e.push(r);
        for (const n of t.bindings)
          r.bindings.push(n.clone());
      } else
        e.push(t);
    return e;
  }
}
class tl {
  /**
   * Constructs a new node attribute.
   *
   * @param {string} name - The name of the attribute.
   * @param {string} type - The type of the attribute.
   * @param {?Node} node - An optional reference to the node.
   */
  constructor(e, t, s = null) {
    this.isNodeAttribute = !0, this.name = e, this.type = t, this.node = s;
  }
}
class Mb {
  /**
   * Constructs a new node uniform.
   *
   * @param {string} name - The name of the uniform.
   * @param {string} type - The type of the uniform.
   * @param {UniformNode} node - An reference to the node.
   */
  constructor(e, t, s) {
    this.isNodeUniform = !0, this.name = e, this.type = t, this.node = s;
  }
  /**
   * The value of the uniform node.
   *
   * @type {any}
   */
  get value() {
    return this.node.value;
  }
  set value(e) {
    this.node.value = e;
  }
  /**
   * The id of the uniform node.
   *
   * @type {number}
   */
  get id() {
    return this.node.id;
  }
  /**
   * The uniform node's group.
   *
   * @type {UniformGroupNode}
   */
  get groupNode() {
    return this.node.groupNode;
  }
}
class oh {
  /**
   * Constructs a new node variable.
   *
   * @param {string} name - The name of the variable.
   * @param {string} type - The type of the variable.
   * @param {boolean} [readOnly=false] - The read-only flag.
   * @param {?number} [count=null] - The size.
   */
  constructor(e, t, s = !1, r = null) {
    this.isNodeVar = !0, this.name = e, this.type = t, this.readOnly = s, this.count = r;
  }
}
class Bb extends oh {
  /**
   * Constructs a new node varying.
   *
   * @param {string} name - The name of the varying.
   * @param {string} type - The type of the varying.
   * @param {?string} interpolationType - The interpolation type of the varying.
   * @param {?string} interpolationSampling - The interpolation sampling type of the varying.
   */
  constructor(e, t, s = null, r = null) {
    super(e, t), this.needsInterpolation = !1, this.isNodeVarying = !0, this.interpolationType = s, this.interpolationSampling = r;
  }
}
class Fb {
  /**
   * Constructs a new code node.
   *
   * @param {string} name - The name of the code.
   * @param {string} type - The node type.
   * @param {string} [code=''] - The native shader code.
   */
  constructor(e, t, s = "") {
    this.name = e, this.type = t, this.code = s, Object.defineProperty(this, "isNodeCode", { value: !0 });
  }
}
let Ub = 0;
class eo {
  /**
   * Constructs a new node cache.
   *
   * @param {?NodeCache} parent - A reference to a parent cache.
   */
  constructor(e = null) {
    this.id = Ub++, this.nodesData = /* @__PURE__ */ new WeakMap(), this.parent = e;
  }
  /**
   * Returns the data for the given node.
   *
   * @param {Node} node - The node.
   * @return {?Object} The data for the node.
   */
  getData(e) {
    let t = this.nodesData.get(e);
    return t === void 0 && this.parent !== null && (t = this.parent.getData(e)), t;
  }
  /**
   * Sets the data for a given node.
   *
   * @param {Node} node - The node.
   * @param {Object} data - The data that should be cached.
   */
  setData(e, t) {
    this.nodesData.set(e, t);
  }
}
class Db {
  constructor(e, t) {
    this.name = e, this.members = t, this.output = !1;
  }
}
class cs {
  /**
   * Constructs a new uniform.
   *
   * @param {string} name - The uniform's name.
   * @param {any} value - The uniform's value.
   */
  constructor(e, t) {
    this.name = e, this.value = t, this.boundary = 0, this.itemSize = 0, this.offset = 0, this.index = -1;
  }
  /**
   * Sets the uniform's value.
   *
   * @param {any} value - The value to set.
   */
  setValue(e) {
    this.value = e;
  }
  /**
   * Returns the uniform's value.
   *
   * @return {any} The value.
   */
  getValue() {
    return this.value;
  }
}
class Pb extends cs {
  /**
   * Constructs a new Number uniform.
   *
   * @param {string} name - The uniform's name.
   * @param {number} value - The uniform's value.
   */
  constructor(e, t = 0) {
    super(e, t), this.isNumberUniform = !0, this.boundary = 4, this.itemSize = 1;
  }
}
class Lb extends cs {
  /**
   * Constructs a new Number uniform.
   *
   * @param {string} name - The uniform's name.
   * @param {Vector2} value - The uniform's value.
   */
  constructor(e, t = new je()) {
    super(e, t), this.isVector2Uniform = !0, this.boundary = 8, this.itemSize = 2;
  }
}
class Ib extends cs {
  /**
   * Constructs a new Number uniform.
   *
   * @param {string} name - The uniform's name.
   * @param {Vector3} value - The uniform's value.
   */
  constructor(e, t = new H()) {
    super(e, t), this.isVector3Uniform = !0, this.boundary = 16, this.itemSize = 3;
  }
}
class Ob extends cs {
  /**
   * Constructs a new Number uniform.
   *
   * @param {string} name - The uniform's name.
   * @param {Vector4} value - The uniform's value.
   */
  constructor(e, t = new Se()) {
    super(e, t), this.isVector4Uniform = !0, this.boundary = 16, this.itemSize = 4;
  }
}
class Gb extends cs {
  /**
   * Constructs a new Number uniform.
   *
   * @param {string} name - The uniform's name.
   * @param {Color} value - The uniform's value.
   */
  constructor(e, t = new Ht()) {
    super(e, t), this.isColorUniform = !0, this.boundary = 16, this.itemSize = 3;
  }
}
class kb extends cs {
  /**
   * Constructs a new Number uniform.
   *
   * @param {string} name - The uniform's name.
   * @param {Matrix2} value - The uniform's value.
   */
  constructor(e, t = new mc()) {
    super(e, t), this.isMatrix2Uniform = !0, this.boundary = 8, this.itemSize = 4;
  }
}
class Vb extends cs {
  /**
   * Constructs a new Number uniform.
   *
   * @param {string} name - The uniform's name.
   * @param {Matrix3} value - The uniform's value.
   */
  constructor(e, t = new Kr()) {
    super(e, t), this.isMatrix3Uniform = !0, this.boundary = 48, this.itemSize = 12;
  }
}
class $b extends cs {
  /**
   * Constructs a new Number uniform.
   *
   * @param {string} name - The uniform's name.
   * @param {Matrix4} value - The uniform's value.
   */
  constructor(e, t = new dt()) {
    super(e, t), this.isMatrix4Uniform = !0, this.boundary = 64, this.itemSize = 16;
  }
}
class zb extends Pb {
  /**
   * Constructs a new node-based Number uniform.
   *
   * @param {NodeUniform} nodeUniform - The node uniform.
   */
  constructor(e) {
    super(e.name, e.value), this.nodeUniform = e;
  }
  /**
   * Overwritten to return the value of the node uniform.
   *
   * @return {number} The value.
   */
  getValue() {
    return this.nodeUniform.value;
  }
  /**
   * Returns the node uniform data type.
   *
   * @return {string} The data type.
   */
  getType() {
    return this.nodeUniform.type;
  }
}
class Wb extends Lb {
  /**
   * Constructs a new node-based Vector2 uniform.
   *
   * @param {NodeUniform} nodeUniform - The node uniform.
   */
  constructor(e) {
    super(e.name, e.value), this.nodeUniform = e;
  }
  /**
   * Overwritten to return the value of the node uniform.
   *
   * @return {Vector2} The value.
   */
  getValue() {
    return this.nodeUniform.value;
  }
  /**
   * Returns the node uniform data type.
   *
   * @return {string} The data type.
   */
  getType() {
    return this.nodeUniform.type;
  }
}
class Hb extends Ib {
  /**
   * Constructs a new node-based Vector3 uniform.
   *
   * @param {NodeUniform} nodeUniform - The node uniform.
   */
  constructor(e) {
    super(e.name, e.value), this.nodeUniform = e;
  }
  /**
   * Overwritten to return the value of the node uniform.
   *
   * @return {Vector3} The value.
   */
  getValue() {
    return this.nodeUniform.value;
  }
  /**
   * Returns the node uniform data type.
   *
   * @return {string} The data type.
   */
  getType() {
    return this.nodeUniform.type;
  }
}
class qb extends Ob {
  /**
   * Constructs a new node-based Vector4 uniform.
   *
   * @param {NodeUniform} nodeUniform - The node uniform.
   */
  constructor(e) {
    super(e.name, e.value), this.nodeUniform = e;
  }
  /**
   * Overwritten to return the value of the node uniform.
   *
   * @return {Vector4} The value.
   */
  getValue() {
    return this.nodeUniform.value;
  }
  /**
   * Returns the node uniform data type.
   *
   * @return {string} The data type.
   */
  getType() {
    return this.nodeUniform.type;
  }
}
class Xb extends Gb {
  /**
   * Constructs a new node-based Color uniform.
   *
   * @param {NodeUniform} nodeUniform - The node uniform.
   */
  constructor(e) {
    super(e.name, e.value), this.nodeUniform = e;
  }
  /**
   * Overwritten to return the value of the node uniform.
   *
   * @return {Color} The value.
   */
  getValue() {
    return this.nodeUniform.value;
  }
  /**
   * Returns the node uniform data type.
   *
   * @return {string} The data type.
   */
  getType() {
    return this.nodeUniform.type;
  }
}
class Kb extends kb {
  /**
   * Constructs a new node-based Matrix2 uniform.
   *
   * @param {NodeUniform} nodeUniform - The node uniform.
   */
  constructor(e) {
    super(e.name, e.value), this.nodeUniform = e;
  }
  /**
   * Overwritten to return the value of the node uniform.
   *
   * @return {Matrix2} The value.
   */
  getValue() {
    return this.nodeUniform.value;
  }
  /**
   * Returns the node uniform data type.
   *
   * @return {string} The data type.
   */
  getType() {
    return this.nodeUniform.type;
  }
}
class Qb extends Vb {
  /**
   * Constructs a new node-based Matrix3 uniform.
   *
   * @param {NodeUniform} nodeUniform - The node uniform.
   */
  constructor(e) {
    super(e.name, e.value), this.nodeUniform = e;
  }
  /**
   * Overwritten to return the value of the node uniform.
   *
   * @return {Matrix3} The value.
   */
  getValue() {
    return this.nodeUniform.value;
  }
  /**
   * Returns the node uniform data type.
   *
   * @return {string} The data type.
   */
  getType() {
    return this.nodeUniform.type;
  }
}
class Yb extends $b {
  /**
   * Constructs a new node-based Matrix4 uniform.
   *
   * @param {NodeUniform} nodeUniform - The node uniform.
   */
  constructor(e) {
    super(e.name, e.value), this.nodeUniform = e;
  }
  /**
   * Overwritten to return the value of the node uniform.
   *
   * @return {Matrix4} The value.
   */
  getValue() {
    return this.nodeUniform.value;
  }
  /**
   * Returns the node uniform data type.
   *
   * @return {string} The data type.
   */
  getType() {
    return this.nodeUniform.type;
  }
}
let jb = 0;
const sl = /* @__PURE__ */ new WeakMap(), Zb = /* @__PURE__ */ new WeakMap(), Jb = /* @__PURE__ */ new Map([
  [Int8Array, "int"],
  [Int16Array, "int"],
  [Int32Array, "int"],
  [Uint8Array, "uint"],
  [Uint16Array, "uint"],
  [Uint32Array, "uint"],
  [Float32Array, "float"]
]), Tn = (a) => /e/g.test(a) ? String(a).replace(/\+/g, "") : (a = Number(a), a + (a % 1 ? "" : ".0"));
class ah {
  /**
   * Constructs a new node builder.
   *
   * @param {Object3D} object - The 3D object.
   * @param {Renderer} renderer - The current renderer.
   * @param {NodeParser} parser - A reference to a node parser.
   */
  constructor(e, t, s) {
    this.object = e, this.material = e && e.material || null, this.geometry = e && e.geometry || null, this.renderer = t, this.parser = s, this.scene = null, this.camera = null, this.nodes = [], this.sequentialNodes = [], this.updateNodes = [], this.updateBeforeNodes = [], this.updateAfterNodes = [], this.hashNodes = {}, this.observer = null, this.lightsNode = null, this.environmentNode = null, this.fogNode = null, this.clippingContext = null, this.vertexShader = null, this.fragmentShader = null, this.computeShader = null, this.flowNodes = { vertex: [], fragment: [], compute: [] }, this.flowCode = { vertex: "", fragment: "", compute: "" }, this.uniforms = { vertex: [], fragment: [], compute: [], index: 0 }, this.structs = { vertex: [], fragment: [], compute: [], index: 0 }, this.types = { vertex: [], fragment: [], compute: [], index: 0 }, this.bindings = { vertex: {}, fragment: {}, compute: {} }, this.bindingsIndexes = {}, this.bindGroups = null, this.attributes = [], this.bufferAttributes = [], this.varyings = [], this.codes = {}, this.vars = {}, this.declarations = {}, this.flow = { code: "" }, this.chaining = [], this.stack = Ki(), this.stacks = [], this.tab = "	", this.currentFunctionNode = null, this.context = {
      material: this.material
    }, this.cache = new eo(), this.globalCache = this.cache, this.flowsData = /* @__PURE__ */ new WeakMap(), this.shaderStage = null, this.buildStage = null, this.subBuildLayers = [], this.activeStacks = [], this.subBuildFn = null, this.fnCall = null, Object.defineProperty(this, "id", { value: jb++ });
  }
  /**
   * Whether the material is using flat shading or not.
   *
   * @returns {boolean} Whether the material is using flat shading or not.
   */
  isFlatShading() {
    return this.material.flatShading === !0 || this.geometry.hasAttribute("normal") === !1;
  }
  /**
   * Whether the material is opaque or not.
   *
   * @return {boolean} Whether the material is opaque or not.
   */
  isOpaque() {
    const e = this.material;
    return e.transparent === !1 && e.blending === Lt && e.alphaToCoverage === !1;
  }
  /**
   * Factory method for creating an instance of {@link RenderTarget} with the given
   * dimensions and options.
   *
   * @param {number} width - The width of the render target.
   * @param {number} height - The height of the render target.
   * @param {Object} options - The options of the render target.
   * @return {RenderTarget} The render target.
   */
  createRenderTarget(e, t, s) {
    return new Ds(e, t, s);
  }
  /**
   * Factory method for creating an instance of {@link CubeRenderTarget} with the given
   * dimensions and options.
   *
   * @param {number} size - The size of the cube render target.
   * @param {Object} options - The options of the cube render target.
   * @return {CubeRenderTarget} The cube render target.
   */
  createCubeRenderTarget(e, t) {
    return new Bd(e, t);
  }
  /**
   * Whether the given node is included in the internal array of nodes or not.
   *
   * @param {Node} node - The node to test.
   * @return {boolean} Whether the given node is included in the internal array of nodes or not.
   */
  includes(e) {
    return this.nodes.includes(e);
  }
  /**
   * Returns the output struct name which is required by
   * {@link OutputStructNode}.
   *
   * @abstract
   * @return {string} The name of the output struct.
   */
  getOutputStructName() {
  }
  /**
   * Returns a bind group for the given group name and binding.
   *
   * @private
   * @param {string} groupName - The group name.
   * @param {Array<NodeUniformsGroup>} bindings - List of bindings.
   * @return {BindGroup} The bind group
   */
  _getBindGroup(e, t) {
    const s = t[0].groupNode;
    let r = s.shared;
    if (r)
      for (let i = 1; i < t.length; i++)
        s !== t[i].groupNode && (r = !1);
    let n;
    if (r) {
      let i = "";
      for (const c of t)
        if (c.isNodeUniformsGroup) {
          c.uniforms.sort((d, h) => d.nodeUniform.node.id - h.nodeUniform.node.id);
          for (const d of c.uniforms)
            i += d.nodeUniform.node.id;
        } else
          i += c.nodeUniform.id;
      const o = this.renderer._currentRenderContext || this.renderer;
      let u = sl.get(o);
      u === void 0 && (u = /* @__PURE__ */ new Map(), sl.set(o, u));
      const l = lr(i);
      n = u.get(l), n === void 0 && (n = new ea(e, t), u.set(l, n));
    } else
      n = new ea(e, t);
    return n;
  }
  /**
   * Returns an array of node uniform groups for the given group name and shader stage.
   *
   * @param {string} groupName - The group name.
   * @param {('vertex'|'fragment'|'compute'|'any')} shaderStage - The shader stage.
   * @return {Array<NodeUniformsGroup>} The array of node uniform groups.
   */
  getBindGroupArray(e, t) {
    const s = this.bindings[t];
    let r = s[e];
    return r === void 0 && (this.bindingsIndexes[e] === void 0 && (this.bindingsIndexes[e] = { binding: 0, group: Object.keys(this.bindingsIndexes).length }), s[e] = r = []), r;
  }
  /**
   * Returns a list bindings of all shader stages separated by groups.
   *
   * @return {Array<BindGroup>} The list of bindings.
   */
  getBindings() {
    let e = this.bindGroups;
    if (e === null) {
      const t = {}, s = this.bindings;
      for (const r of Ei)
        for (const n in s[r]) {
          const i = s[r][n], o = t[n] || (t[n] = []);
          for (const u of i)
            o.includes(u) === !1 && o.push(u);
        }
      e = [];
      for (const r in t) {
        const n = t[r], i = this._getBindGroup(r, n);
        e.push(i);
      }
      this.bindGroups = e;
    }
    return e;
  }
  /**
   * Sorts the bind groups and updates {@link NodeBuilder#bindingsIndexes}.
   */
  sortBindingGroups() {
    const e = this.getBindings();
    e.sort((t, s) => t.bindings[0].groupNode.order - s.bindings[0].groupNode.order);
    for (let t = 0; t < e.length; t++) {
      const s = e[t];
      this.bindingsIndexes[s.name].group = t;
    }
  }
  /**
   * The builder maintains each node in a hash-based dictionary.
   * This method sets the given node (value) with the given hash (key) into this dictionary.
   *
   * @param {Node} node - The node to add.
   * @param {number} hash - The hash of the node.
   */
  setHashNode(e, t) {
    this.hashNodes[t] = e;
  }
  /**
   * Adds a node to this builder.
   *
   * @param {Node} node - The node to add.
   */
  addNode(e) {
    this.nodes.includes(e) === !1 && (this.nodes.push(e), this.setHashNode(e, e.getHash(this)));
  }
  /**
   * It is used to add Nodes that will be used as FRAME and RENDER events,
   * and need to follow a certain sequence in the calls to work correctly.
   * This function should be called after 'setup()' in the 'build()' process to ensure that the child nodes are processed first.
   *
   * @param {Node} node - The node to add.
   */
  addSequentialNode(e) {
    const t = e.getUpdateBeforeType(), s = e.getUpdateAfterType();
    (t !== z.NONE || s !== z.NONE) && this.sequentialNodes.includes(e) === !1 && this.sequentialNodes.push(e);
  }
  /**
   * Checks the update types of nodes
   */
  buildUpdateNodes() {
    for (const e of this.nodes)
      e.getUpdateType() !== z.NONE && this.updateNodes.push(e);
    for (const e of this.sequentialNodes) {
      const t = e.getUpdateBeforeType(), s = e.getUpdateAfterType();
      t !== z.NONE && this.updateBeforeNodes.push(e), s !== z.NONE && this.updateAfterNodes.push(e);
    }
  }
  /**
   * A reference the current node which is the
   * last node in the chain of nodes.
   *
   * @type {Node}
   */
  get currentNode() {
    return this.chaining[this.chaining.length - 1];
  }
  /**
   * Whether the given texture is filtered or not.
   *
   * @param {Texture} texture - The texture to check.
   * @return {boolean} Whether the given texture is filtered or not.
   */
  isFilteredTexture(e) {
    return e.magFilter === Ze || e.magFilter === po || e.magFilter === $r || e.magFilter === Rs || e.minFilter === Ze || e.minFilter === po || e.minFilter === $r || e.minFilter === Rs;
  }
  /**
   * Returns the maximum number of bytes available for uniform buffers.
   *
   * @return {number} The maximum number of bytes available for uniform buffers.
   */
  getUniformBufferLimit() {
    return this.renderer.backend.capabilities.getUniformBufferLimit();
  }
  /**
   * Adds the given node to the internal node chain.
   * This is used to check recursive calls in node-graph.
   *
   * @param {Node} node - The node to add.
   */
  addChain(e) {
    this.chaining.push(e);
  }
  /**
   * Removes the given node from the internal node chain.
   *
   * @param {Node} node - The node to remove.
   */
  removeChain(e) {
    if (this.chaining.pop() !== e)
      throw new Error("NodeBuilder: Invalid node chaining!");
  }
  /**
   * Returns the native shader method name for a given generic name. E.g.
   * the method name `textureDimensions` matches the WGSL name but must be
   * resolved to `textureSize` in GLSL.
   *
   * @abstract
   * @param {string} method - The method name to resolve.
   * @return {string} The resolved method name.
   */
  getMethod(e) {
    return e;
  }
  /**
   * Returns the native snippet for a ternary operation. E.g. GLSL would output
   * a ternary op as `cond ? x : y` whereas WGSL would output it as `select(y, x, cond)`
   *
   * @abstract
   * @param {string} condSnippet - The condition determining which expression gets resolved.
   * @param {string} ifSnippet - The expression to resolve to if the condition is true.
   * @param {string} elseSnippet - The expression to resolve to if the condition is false.
   * @return {string} The resolved method name.
   */
  getTernary() {
    return null;
  }
  /**
   * Returns a node for the given hash, see {@link NodeBuilder#setHashNode}.
   *
   * @param {number} hash - The hash of the node.
   * @return {Node} The found node.
   */
  getNodeFromHash(e) {
    return this.hashNodes[e];
  }
  /**
   * Adds the Node to a target flow so that it can generate code in the 'generate' process.
   *
   * @param {('vertex'|'fragment'|'compute')} shaderStage - The shader stage.
   * @param {Node} node - The node to add.
   * @return {Node} The node.
   */
  addFlow(e, t) {
    return this.flowNodes[e].push(t), t;
  }
  /**
   * Sets builder's context.
   *
   * @param {Object} context - The context to set.
   */
  setContext(e) {
    this.context = e;
  }
  /**
   * Returns the builder's current context.
   *
   * @return {Object} The builder's current context.
   */
  getContext() {
    return this.context;
  }
  /**
   * Adds context data to the builder's current context.
   *
   * @param {Object} context - The context to add.
   * @return {Object} The previous context.
   */
  addContext(e) {
    const t = this.getContext();
    return this.setContext({ ...this.context, ...e }), t;
  }
  /**
   * Gets a context used in shader construction that can be shared across different materials.
   * This is necessary since the renderer cache can reuse shaders generated in one material and use them in another.
   *
   * @return {Object} The builder's current context without material.
   */
  getSharedContext() {
    const e = { ...this.context };
    return delete e.material, delete e.getUV, delete e.getOutput, delete e.getTextureLevel, delete e.getAO, delete e.getShadow, e;
  }
  /**
   * Sets builder's cache.
   *
   * @param {NodeCache} cache - The cache to set.
   */
  setCache(e) {
    this.cache = e;
  }
  /**
   * Returns the builder's current cache.
   *
   * @return {NodeCache} The builder's current cache.
   */
  getCache() {
    return this.cache;
  }
  /**
   * Returns a cache for the given node.
   *
   * @param {Node} node - The node.
   * @param {boolean} [parent=true] - Whether this node refers to a shared parent cache or not.
   * @return {NodeCache} The cache.
   */
  getCacheFromNode(e, t = !0) {
    const s = this.getDataFromNode(e);
    return s.cache === void 0 && (s.cache = new eo(t ? this.getCache() : null)), s.cache;
  }
  /**
   * Whether the requested feature is available or not.
   *
   * @abstract
   * @param {string} name - The requested feature.
   * @return {boolean} Whether the requested feature is supported or not.
   */
  isAvailable() {
    return !1;
  }
  /**
   * Returns the vertexIndex input variable as a native shader string.
   *
   * @abstract
   * @return {string} The instanceIndex shader string.
   */
  getVertexIndex() {
    L("Abstract function.");
  }
  /**
   * Contextually returns either the vertex stage instance index builtin
   * or the linearized index of an compute invocation within a grid of workgroups.
   *
   * @abstract
   * @return {string} The instanceIndex shader string.
   */
  getInstanceIndex() {
    L("Abstract function.");
  }
  /**
   * Returns the drawIndex input variable as a native shader string.
   * Only relevant for WebGL and its `WEBGL_multi_draw` extension.
   *
   * @abstract
   * @return {?string} The drawIndex shader string.
   */
  getDrawIndex() {
    L("Abstract function.");
  }
  /**
   * Returns the frontFacing input variable as a native shader string.
   *
   * @abstract
   * @return {string} The frontFacing shader string.
   */
  getFrontFacing() {
    L("Abstract function.");
  }
  /**
   * Returns the fragCoord input variable as a native shader string.
   *
   * @abstract
   * @return {string} The fragCoord shader string.
   */
  getFragCoord() {
    L("Abstract function.");
  }
  /**
   * Whether to flip texture data along its vertical axis or not. WebGL needs
   * this method evaluate to `true`, WebGPU to `false`.
   *
   * @abstract
   * @return {boolean} Whether to flip texture data along its vertical axis or not.
   */
  isFlipY() {
    return !1;
  }
  /**
   * Calling this method increases the usage count for the given node by one.
   *
   * @param {Node} node - The node to increase the usage count for.
   * @return {number} The updated usage count.
   */
  increaseUsage(e) {
    const t = this.getDataFromNode(e);
    return t.usageCount = t.usageCount === void 0 ? 1 : t.usageCount + 1, t.usageCount;
  }
  /**
   * Generates a texture sample shader string for the given texture data.
   *
   * @abstract
   * @param {Texture} texture - The texture.
   * @param {string} textureProperty - The texture property name.
   * @param {string} uvSnippet - Snippet defining the texture coordinates.
   * @return {string} The generated shader string.
   */
  generateTexture() {
    L("Abstract function.");
  }
  /**
   * Generates a texture LOD shader string for the given texture data.
   *
   * @abstract
   * @param {Texture} texture - The texture.
   * @param {string} textureProperty - The texture property name.
   * @param {string} uvSnippet - Snippet defining the texture coordinates.
   * @param {?string} depthSnippet - Snippet defining the 0-based texture array index to sample.
   * @param {string} levelSnippet - Snippet defining the mip level.
   * @return {string} The generated shader string.
   */
  generateTextureLod() {
    L("Abstract function.");
  }
  /**
   * Generates the array declaration string.
   *
   * @param {string} type - The type.
   * @param {?number} [count] - The count.
   * @return {string} The generated value as a shader string.
   */
  generateArrayDeclaration(e, t) {
    return this.getType(e) + "[ " + t + " ]";
  }
  /**
   * Generates the array shader string for the given type and value.
   *
   * @param {string} type - The type.
   * @param {?number} [count] - The count.
   * @param {?Array<Node>} [values=null] - The default values.
   * @return {string} The generated value as a shader string.
   */
  generateArray(e, t, s = null) {
    let r = this.generateArrayDeclaration(e, t) + "( ";
    for (let n = 0; n < t; n++) {
      const i = s ? s[n] : null;
      i !== null ? r += i.build(this, e) : r += this.generateConst(e), n < t - 1 && (r += ", ");
    }
    return r += " )", r;
  }
  /**
   * Generates the struct shader string.
   *
   * @param {string} type - The type.
   * @param {Array<Object>} [membersLayout] - The count.
   * @param {?Array<Node>} [values=null] - The default values.
   * @return {string} The generated value as a shader string.
   */
  generateStruct(e, t, s = null) {
    const r = [];
    for (const n of t) {
      const { name: i, type: o } = n;
      s && s[i] && s[i].isNode ? r.push(s[i].build(this, o)) : r.push(this.generateConst(o));
    }
    return e + "( " + r.join(", ") + " )";
  }
  /**
   * Generates the shader string for the given type and value.
   *
   * @param {string} type - The type.
   * @param {?any} [value=null] - The value.
   * @return {string} The generated value as a shader string.
   */
  generateConst(e, t = null) {
    if (t === null && (e === "float" || e === "int" || e === "uint" ? t = 0 : e === "bool" ? t = !1 : e === "color" ? t = new Ht() : e === "vec2" || e === "uvec2" || e === "ivec2" ? t = new je() : e === "vec3" || e === "uvec3" || e === "ivec3" ? t = new H() : (e === "vec4" || e === "uvec4" || e === "ivec4") && (t = new Se())), e === "float") return Tn(t);
    if (e === "int") return `${Math.round(t)}`;
    if (e === "uint") return t >= 0 ? `${Math.round(t)}u` : "0u";
    if (e === "bool") return t ? "true" : "false";
    if (e === "color") return `${this.getType("vec3")}( ${Tn(t.r)}, ${Tn(t.g)}, ${Tn(t.b)} )`;
    const s = this.getTypeLength(e), r = this.getComponentType(e), n = (i) => this.generateConst(r, i);
    if (s === 2)
      return `${this.getType(e)}( ${n(t.x)}, ${n(t.y)} )`;
    if (s === 3)
      return `${this.getType(e)}( ${n(t.x)}, ${n(t.y)}, ${n(t.z)} )`;
    if (s === 4 && e !== "mat2")
      return `${this.getType(e)}( ${n(t.x)}, ${n(t.y)}, ${n(t.z)}, ${n(t.w)} )`;
    if (s >= 4 && t && (t.isMatrix2 || t.isMatrix3 || t.isMatrix4))
      return `${this.getType(e)}( ${t.elements.map(n).join(", ")} )`;
    if (s > 4)
      return `${this.getType(e)}()`;
    throw new Error(`NodeBuilder: Type '${e}' not found in generate constant attempt.`);
  }
  /**
   * It might be necessary to convert certain data types to different ones
   * so this method can be used to hide the conversion.
   *
   * @param {string} type - The type.
   * @return {string} The updated type.
   */
  getType(e) {
    return e === "color" ? "vec3" : e;
  }
  /**
   * Whether the given attribute name is defined in the geometry or not.
   *
   * @param {string} name - The attribute name.
   * @return {boolean} Whether the given attribute name is defined in the geometry.
   */
  hasGeometryAttribute(e) {
    return this.geometry && this.geometry.getAttribute(e) !== void 0;
  }
  /**
   * Returns a node attribute for the given name and type.
   *
   * @param {string} name - The attribute's name.
   * @param {string} type - The attribute's type.
   * @return {NodeAttribute} The node attribute.
   */
  getAttribute(e, t) {
    const s = this.attributes;
    for (const n of s)
      if (n.name === e)
        return n;
    const r = new tl(e, t);
    return this.registerDeclaration(r), s.push(r), r;
  }
  /**
   * Returns for the given node and shader stage the property name for the shader.
   *
   * @param {Node} node - The node.
   * @param {('vertex'|'fragment'|'compute'|'any')} shaderStage - The shader stage.
   * @return {string} The property name.
   */
  getPropertyName(e) {
    return e.name;
  }
  /**
   * Whether the given type is a vector type or not.
   *
   * @param {string} type - The type to check.
   * @return {boolean} Whether the given type is a vector type or not.
   */
  isVector(e) {
    return /vec\d/.test(e);
  }
  /**
   * Whether the given type is a matrix type or not.
   *
   * @param {string} type - The type to check.
   * @return {boolean} Whether the given type is a matrix type or not.
   */
  isMatrix(e) {
    return /mat\d/.test(e);
  }
  /**
   * Whether the given type is a reference type or not.
   *
   * @param {string} type - The type to check.
   * @return {boolean} Whether the given type is a reference type or not.
   */
  isReference(e) {
    return e === "void" || e === "property" || e === "sampler" || e === "samplerComparison" || e === "texture" || e === "cubeTexture" || e === "storageTexture" || e === "depthTexture" || e === "texture3D";
  }
  /**
   * Checks if the given texture requires a manual conversion to the working color space.
   *
   * @abstract
   * @param {Texture} texture - The texture to check.
   * @return {boolean} Whether the given texture requires a conversion to working color space or not.
   */
  needsToWorkingColorSpace() {
    return !1;
  }
  /**
   * Returns the component type of a given texture.
   *
   * @param {Texture} texture - The texture.
   * @return {string} The component type.
   */
  getComponentTypeFromTexture(e) {
    const t = e.type;
    if (e.isDataTexture) {
      if (t === Ue) return "int";
      if (t === _e) return "uint";
    }
    return "float";
  }
  /**
   * Returns the element type for a given type.
   *
   * @param {string} type - The type.
   * @return {string} The element type.
   */
  getElementType(e) {
    return e === "mat2" ? "vec2" : e === "mat3" ? "vec3" : e === "mat4" ? "vec4" : this.getComponentType(e);
  }
  /**
   * Returns the component type for a given type.
   *
   * @param {string} type - The type.
   * @return {string} The component type.
   */
  getComponentType(e) {
    if (e = this.getVectorType(e), e === "float" || e === "bool" || e === "int" || e === "uint") return e;
    const t = /(b|i|u|)(vec|mat)([2-4])/.exec(e);
    return t === null ? null : t[1] === "b" ? "bool" : t[1] === "i" ? "int" : t[1] === "u" ? "uint" : "float";
  }
  /**
   * Returns the vector type for a given type.
   *
   * @param {string} type - The type.
   * @return {string} The vector type.
   */
  getVectorType(e) {
    return e === "color" ? "vec3" : e === "texture" || e === "cubeTexture" || e === "storageTexture" || e === "texture3D" ? "vec4" : e;
  }
  /**
   * Returns the data type for the given the length and component type.
   *
   * @param {number} length - The length.
   * @param {string} [componentType='float'] - The component type.
   * @return {string} The type.
   */
  getTypeFromLength(e, t = "float") {
    if (e === 1) return t;
    let s = yc(e);
    const r = t === "float" ? "" : t[0];
    return /mat2/.test(t) === !0 && (s = s.replace("vec", "mat")), r + s;
  }
  /**
   * Returns the type for a given typed array.
   *
   * @param {TypedArray} array - The typed array.
   * @return {string} The type.
   */
  getTypeFromArray(e) {
    return Jb.get(e.constructor);
  }
  /**
   * Returns the type is an integer type.
   *
   * @param {string} type - The type.
   * @return {boolean} Whether the type is an integer type or not.
   */
  isInteger(e) {
    return /int|uint|(i|u)vec/.test(e);
  }
  /**
   * Returns the type for a given buffer attribute.
   *
   * @param {BufferAttribute} attribute - The buffer attribute.
   * @return {string} The type.
   */
  getTypeFromAttribute(e) {
    let t = e;
    e.isInterleavedBufferAttribute && (t = e.data);
    const s = t.array, r = e.itemSize, n = e.normalized;
    let i;
    return !(e instanceof gc) && n !== !0 && (i = this.getTypeFromArray(s)), this.getTypeFromLength(r, i);
  }
  /**
   * Returns the length for the given data type.
   *
   * @param {string} type - The data type.
   * @return {number} The length.
   */
  getTypeLength(e) {
    const t = this.getVectorType(e), s = /vec([2-4])/.exec(t);
    return s !== null ? Number(s[1]) : t === "float" || t === "bool" || t === "int" || t === "uint" ? 1 : /mat2/.test(e) === !0 ? 4 : /mat3/.test(e) === !0 ? 9 : /mat4/.test(e) === !0 ? 16 : 0;
  }
  /**
   * Returns the vector type for a given matrix type.
   *
   * @param {string} type - The matrix type.
   * @return {string} The vector type.
   */
  getVectorFromMatrix(e) {
    return e.replace("mat", "vec");
  }
  /**
   * For a given type this method changes the component type to the
   * given value. E.g. `vec4` should be changed to the new component type
   * `uint` which results in `uvec4`.
   *
   * @param {string} type - The type.
   * @param {string} newComponentType - The new component type.
   * @return {string} The new type.
   */
  changeComponentType(e, t) {
    return this.getTypeFromLength(this.getTypeLength(e), t);
  }
  /**
   * Returns the integer type pendant for the given type.
   *
   * @param {string} type - The type.
   * @return {string} The integer type.
   */
  getIntegerType(e) {
    const t = this.getComponentType(e);
    return t === "int" || t === "uint" ? e : this.changeComponentType(e, "int");
  }
  /**
   * Adds an active stack to the internal stack.
   *
   * @param {StackNode} stack - The stack node to add.
   */
  setActiveStack(e) {
    this.activeStacks.push(e);
  }
  /**
   * Removes the active stack from the internal stack.
   *
   * @param {StackNode} stack - The stack node to remove.
   */
  removeActiveStack(e) {
    if (this.activeStacks[this.activeStacks.length - 1] === e)
      this.activeStacks.pop();
    else
      throw new Error("NodeBuilder: Invalid active stack removal.");
  }
  /**
   * Returns the active stack.
   *
   * @return {StackNode} The active stack.
   */
  getActiveStack() {
    return this.activeStacks[this.activeStacks.length - 1];
  }
  /**
   * Returns the base stack.
   *
   * @return {StackNode} The base stack.
   */
  getBaseStack() {
    return this.activeStacks[0];
  }
  /**
   * Adds a stack node to the internal stack.
   *
   * @return {StackNode} The added stack node.
   */
  addStack() {
    this.stack = Ki(this.stack);
    const e = Rc();
    return this.stacks.push(e), Wn(this.stack), this.stack;
  }
  /**
   * Removes the last stack node from the internal stack.
   *
   * @return {StackNode} The removed stack node.
   */
  removeStack() {
    const e = this.stack;
    for (const t of e.nodes) {
      const s = this.getDataFromNode(t);
      s.stack = e;
    }
    return this.stack = e.parent, Wn(this.stacks.pop()), e;
  }
  /**
   * The builder maintains (cached) data for each node during the building process. This method
   * can be used to get these data for a specific shader stage and cache.
   *
   * @param {Node} node - The node to get the data for.
   * @param {('vertex'|'fragment'|'compute'|'any')} [shaderStage=this.shaderStage] - The shader stage.
   * @param {?NodeCache} cache - An optional cache.
   * @return {Object} The node data.
   */
  getDataFromNode(e, t = this.shaderStage, s = null) {
    s = s === null ? e.isGlobal(this) ? this.globalCache : this.cache : s;
    let r = s.getData(e);
    r === void 0 && (r = {}, s.setData(e, r)), r[t] === void 0 && (r[t] = {});
    let n = r[t];
    const i = r.any ? r.any.subBuilds : null, o = this.getClosestSubBuild(i);
    return o && (n.subBuildsCache === void 0 && (n.subBuildsCache = {}), n = n.subBuildsCache[o] || (n.subBuildsCache[o] = {}), n.subBuilds = i), n;
  }
  /**
   * Returns the properties for the given node and shader stage.
   *
   * Properties are typically used within a build stage to reference a node's
   * child node or nodes manually assigned to the properties in a separate build stage.
   * A typical usage pattern for defining nodes manually would be assigning dependency nodes
   * to the current node's properties in the setup stage and building those properties in the generate stage.
   *
   * @param {Node} node - The node to get the properties for.
   * @param {('vertex'|'fragment'|'compute'|'any')} [shaderStage='any'] - The shader stage.
   * @return {Object} The node properties.
   */
  getNodeProperties(e, t = "any") {
    const s = this.getDataFromNode(e, t);
    return s.properties || (s.properties = { outputNode: null });
  }
  /**
   * Returns an instance of {@link NodeAttribute} for the given buffer attribute node.
   *
   * @param {BufferAttributeNode} node - The buffer attribute node.
   * @param {string} type - The node type.
   * @return {NodeAttribute} The node attribute.
   */
  getBufferAttributeFromNode(e, t) {
    const s = this.getDataFromNode(e, "vertex");
    let r = s.bufferAttribute;
    if (r === void 0) {
      const n = this.uniforms.index++;
      r = new tl("nodeAttribute" + n, t, e), this.bufferAttributes.push(r), s.bufferAttribute = r;
    }
    return r;
  }
  /**
   * Returns an instance of {@link StructType} for the given struct name and shader stage
   * or null if not found.
   *
   * @param {string} name - The name of the struct.
   * @param {('vertex'|'fragment'|'compute'|'any')} [shaderStage=this.shaderStage] - The shader stage.
   * @return {?StructType} The struct type or null if not found.
   */
  getStructTypeNode(e, t = this.shaderStage) {
    return this.types[t][e] || null;
  }
  /**
   * Returns an instance of {@link StructType} for the given output struct node.
   *
   * @param {OutputStructNode} node - The output struct node.
   * @param {Array<Object>} membersLayout - The output struct types.
   * @param {?string} [name=null] - The name of the struct.
   * @param {('vertex'|'fragment'|'compute'|'any')} [shaderStage=this.shaderStage] - The shader stage.
   * @return {StructType} The struct type attribute.
   */
  getStructTypeFromNode(e, t, s = null, r = this.shaderStage) {
    const n = this.getDataFromNode(e, r, this.globalCache);
    let i = n.structType;
    if (i === void 0) {
      const o = this.structs.index++;
      s === null && (s = "StructType" + o), i = new Db(s, t), this.structs[r].push(i), this.types[r][s] = e, n.structType = i;
    }
    return i;
  }
  /**
   * Returns an instance of {@link StructType} for the given output struct node.
   *
   * @param {OutputStructNode} node - The output struct node.
   * @param {Array<Object>} membersLayout - The output struct types.
   * @return {StructType} The struct type attribute.
   */
  getOutputStructTypeFromNode(e, t) {
    const s = this.getStructTypeFromNode(e, t, "OutputType", "fragment");
    return s.output = !0, s;
  }
  /**
   * Returns an instance of {@link NodeUniform} for the given uniform node.
   *
   * @param {UniformNode} node - The uniform node.
   * @param {string} type - The uniform type.
   * @param {('vertex'|'fragment'|'compute'|'any')} [shaderStage=this.shaderStage] - The shader stage.
   * @param {?string} name - The name of the uniform.
   * @return {NodeUniform} The node uniform.
   */
  getUniformFromNode(e, t, s = this.shaderStage, r = null) {
    const n = this.getDataFromNode(e, s, this.globalCache);
    let i = n.uniform;
    if (i === void 0) {
      const o = this.uniforms.index++;
      i = new Mb(r || "nodeUniform" + o, t, e), this.uniforms[s].push(i), this.registerDeclaration(i), n.uniform = i;
    }
    return i;
  }
  /**
   * Returns an instance of {@link NodeVar} for the given variable node.
   *
   * @param {VarNode} node - The variable node.
   * @param {?string} name - The variable's name.
   * @param {string} [type=node.getNodeType( this )] - The variable's type.
   * @param {('vertex'|'fragment'|'compute'|'any')} [shaderStage=this.shaderStage] - The shader stage.
   * @param {boolean} [readOnly=false] - Whether the variable is read-only or not.
   *
   * @return {NodeVar} The node variable.
   */
  getVarFromNode(e, t = null, s = e.getNodeType(this), r = this.shaderStage, n = !1) {
    const i = this.getDataFromNode(e, r), o = this.getSubBuildProperty("variable", i.subBuilds);
    let u = i[o];
    if (u === void 0) {
      const l = n ? "_const" : "_var", c = this.vars[r] || (this.vars[r] = []), d = this.vars[l] || (this.vars[l] = 0);
      t === null && (t = (n ? "nodeConst" : "nodeVar") + d, this.vars[l]++), o !== "variable" && (t = this.getSubBuildProperty(t, i.subBuilds));
      const h = e.getArrayCount(this);
      u = new oh(t, s, n, h), n || c.push(u), this.registerDeclaration(u), i[o] = u;
    }
    return u;
  }
  /**
   * Returns whether a Node or its flow is deterministic, useful for use in `const`.
   *
   * @param {Node} node - The varying node.
   * @return {boolean} Returns true if deterministic.
   */
  isDeterministic(e) {
    if (e.isMathNode)
      return this.isDeterministic(e.aNode) && (e.bNode ? this.isDeterministic(e.bNode) : !0) && (e.cNode ? this.isDeterministic(e.cNode) : !0);
    if (e.isOperatorNode)
      return this.isDeterministic(e.aNode) && (e.bNode ? this.isDeterministic(e.bNode) : !0);
    if (e.isArrayNode) {
      if (e.values !== null) {
        for (const t of e.values)
          if (!this.isDeterministic(t))
            return !1;
      }
      return !0;
    } else if (e.isConstNode)
      return !0;
    return !1;
  }
  /**
   * Returns an instance of {@link NodeVarying} for the given varying node.
   *
   * @param {(VaryingNode|PropertyNode)} node - The varying node.
   * @param {?string} name - The varying's name.
   * @param {string} [type=node.getNodeType( this )] - The varying's type.
   * @param {?string} interpolationType - The interpolation type of the varying.
   * @param {?string} interpolationSampling - The interpolation sampling type of the varying.
   * @return {NodeVar} The node varying.
   */
  getVaryingFromNode(e, t = null, s = e.getNodeType(this), r = null, n = null) {
    const i = this.getDataFromNode(e, "any"), o = this.getSubBuildProperty("varying", i.subBuilds);
    let u = i[o];
    if (u === void 0) {
      const l = this.varyings, c = l.length;
      t === null && (t = "nodeVarying" + c), o !== "varying" && (t = this.getSubBuildProperty(t, i.subBuilds)), u = new Bb(t, s, r, n), l.push(u), this.registerDeclaration(u), i[o] = u;
    }
    return u;
  }
  /**
   * Registers a node declaration in the current shader stage.
   *
   * @param {Object} node - The node to be registered.
   */
  registerDeclaration(e) {
    const t = this.shaderStage, s = this.declarations[t] || (this.declarations[t] = {}), r = this.getPropertyName(e);
    let n = 1, i = r;
    for (; s[i] !== void 0; )
      i = r + "_" + n++;
    n > 1 && (e.name = i, L(`TSL: Declaration name '${r}' of '${e.type}' already in use. Renamed to '${i}'.`)), s[i] = e;
  }
  /**
   * Returns an instance of {@link NodeCode} for the given code node.
   *
   * @param {CodeNode} node - The code node.
   * @param {string} type - The node type.
   * @param {('vertex'|'fragment'|'compute'|'any')} [shaderStage=this.shaderStage] - The shader stage.
   * @return {NodeCode} The node code.
   */
  getCodeFromNode(e, t, s = this.shaderStage) {
    const r = this.getDataFromNode(e);
    let n = r.code;
    if (n === void 0) {
      const i = this.codes[s] || (this.codes[s] = []), o = i.length;
      n = new Fb("nodeCode" + o, t), i.push(n), r.code = n;
    }
    return n;
  }
  /**
  	 * Adds a code flow based on the code-block hierarchy.
  
  	 * This is used so that code-blocks like If,Else create their variables locally if the Node
  	 * is only used inside one of these conditionals in the current shader stage.
  	 *
  	 * @param {Node} node - The node to add.
  	 * @param {Node} nodeBlock - Node-based code-block. Usually 'ConditionalNode'.
  	 */
  addFlowCodeHierarchy(e, t) {
    const { flowCodes: s, flowCodeBlock: r } = this.getDataFromNode(e);
    let n = !0, i = t;
    for (; i; ) {
      if (r.get(i) === !0) {
        n = !1;
        break;
      }
      i = this.getDataFromNode(i).parentNodeBlock;
    }
    if (n)
      for (const o of s)
        this.addLineFlowCode(o);
  }
  /**
   * Add a inline-code to the current flow code-block.
   *
   * @param {Node} node - The node to add.
   * @param {string} code - The code to add.
   * @param {Node} nodeBlock - Current ConditionalNode
   */
  addLineFlowCodeBlock(e, t, s) {
    const r = this.getDataFromNode(e), n = r.flowCodes || (r.flowCodes = []), i = r.flowCodeBlock || (r.flowCodeBlock = /* @__PURE__ */ new WeakMap());
    n.push(t), i.set(s, !0);
  }
  /**
   * Add a inline-code to the current flow.
   *
   * @param {string} code - The code to add.
   * @param {?Node} [node= null] - Optional Node, can help the system understand if the Node is part of a code-block.
   * @return {NodeBuilder} A reference to this node builder.
   */
  addLineFlowCode(e, t = null) {
    return e === "" ? this : (t !== null && this.context.nodeBlock && this.addLineFlowCodeBlock(t, e, this.context.nodeBlock), e = this.tab + e, /;\s*$/.test(e) || (e = e + `;
`), this.flow.code += e, this);
  }
  /**
   * Adds a code to the current code flow.
   *
   * @param {string} code - Shader code.
   * @return {NodeBuilder} A reference to this node builder.
   */
  addFlowCode(e) {
    return this.flow.code += e, this;
  }
  /**
   * Add tab in the code that will be generated so that other snippets respect the current tabulation.
   * Typically used in codes with If,Else.
   *
   * @return {NodeBuilder} A reference to this node builder.
   */
  addFlowTab() {
    return this.tab += "	", this;
  }
  /**
   * Removes a tab.
   *
   * @return {NodeBuilder} A reference to this node builder.
   */
  removeFlowTab() {
    return this.tab = this.tab.slice(0, -1), this;
  }
  /**
   * Gets the current flow data based on a Node.
   *
   * @param {Node} node - Node that the flow was started.
   * @param {('vertex'|'fragment'|'compute'|'any')} shaderStage - The shader stage.
   * @return {Object} The flow data.
   */
  getFlowData(e) {
    return this.flowsData.get(e);
  }
  /**
   * Executes the node flow based on a root node to generate the final shader code.
   *
   * @param {Node} node - The node to execute.
   * @return {Object} The code flow.
   */
  flowNode(e) {
    const t = e.getNodeType(this), s = this.flowChildNode(e, t);
    return this.flowsData.set(e, s), s;
  }
  /**
   * Includes a node in the current function node.
   *
   * @param {Node} node - The node to include.
   * @returns {void}
   */
  addInclude(e) {
    this.currentFunctionNode !== null && this.currentFunctionNode.includes.push(e);
  }
  /**
   * Returns the native shader operator name for a given generic name.
   * It is a similar type of method like {@link NodeBuilder#getMethod}.
   *
   * @param {ShaderNodeInternal} shaderNode - The shader node to build the function node with.
   * @return {FunctionNode} The build function node.
   */
  buildFunctionNode(e) {
    const t = new k_(), s = this.currentFunctionNode;
    return this.currentFunctionNode = t, t.code = this.buildFunctionCode(e), this.currentFunctionNode = s, t;
  }
  /**
   * Generates a code flow based on a TSL function: Fn().
   *
   * @param {ShaderNodeInternal} shaderNode - A function code will be generated based on the input.
   * @return {Object}
   */
  flowShaderNode(e) {
    const t = e.layout, s = {
      [Symbol.iterator]() {
        let i = 0;
        const o = Object.values(this);
        return {
          next: () => ({
            value: o[i],
            done: i++ >= o.length
          })
        };
      }
    };
    for (const i of t.inputs)
      s[i.name] = new __(i.type, i.name);
    e.layout = null;
    const r = e.call(s), n = this.flowStagesNode(r, t.type);
    return e.layout = t, n;
  }
  /**
   * Executes the node in a specific build stage.
   *
   * This function can be used to arbitrarily execute the specified build stage
   * outside of the standard build process. For instance, if a node's type depends
   * on properties created by the 'setup' stage, then flowBuildStage(node, 'setup')
   * can be used to execute the setup build stage and access its generated nodes
   * before the standard build process begins.
   *
   * @param {Node} node - The node to execute.
   * @param {string} buildStage - The build stage to execute the node in.
   * @param {?(Node|string)} [output=null] - Expected output type. For example 'vec3'.
   * @return {?(Node|string)} The result of the node build.
   */
  flowBuildStage(e, t, s = null) {
    const r = this.getBuildStage();
    this.setBuildStage(t);
    const n = e.build(this, s);
    return this.setBuildStage(r), n;
  }
  /**
   * Runs the node flow through all the steps of creation, 'setup', 'analyze', 'generate'.
   *
   * @param {Node} node - The node to execute.
   * @param {?string} output - Expected output type. For example 'vec3'.
   * @return {Object}
   */
  flowStagesNode(e, t = null) {
    const s = this.flow, r = this.vars, n = this.declarations, i = this.cache, o = this.buildStage, u = this.stack, l = {
      code: ""
    };
    this.flow = l, this.vars = {}, this.declarations = {}, this.cache = new eo(), this.stack = Ki();
    for (const c of vi)
      this.setBuildStage(c), l.result = e.build(this, t);
    return l.vars = this.getVars(this.shaderStage), this.flow = s, this.vars = r, this.declarations = n, this.cache = i, this.stack = u, this.setBuildStage(o), l;
  }
  /**
   * Returns the native shader operator name for a given generic name.
   * It is a similar type of method like {@link NodeBuilder#getMethod}.
   *
   * @abstract
   * @param {string} op - The operator name to resolve.
   * @return {?string} The resolved operator name.
   */
  getFunctionOperator() {
    return null;
  }
  /**
   * Builds the given shader node.
   *
   * @abstract
   * @param {ShaderNodeInternal} shaderNode - The shader node.
   * @return {string} The function code.
   */
  buildFunctionCode() {
    L("Abstract function.");
  }
  /**
   * Generates a code flow based on a child Node.
   *
   * @param {Node} node - The node to execute.
   * @param {?string} output - Expected output type. For example 'vec3'.
   * @return {Object} The code flow.
   */
  flowChildNode(e, t = null) {
    const s = this.flow, r = {
      code: ""
    };
    return this.flow = r, r.result = e.build(this, t), this.flow = s, r;
  }
  /**
   * Executes a flow of code in a different stage.
   *
   * Some nodes like `varying()` have the ability to compute code in vertex-stage and
   * return the value in fragment-stage even if it is being executed in an input fragment.
   *
   * @param {('vertex'|'fragment'|'compute'|'any')} shaderStage - The shader stage.
   * @param {Node} node - The node to execute.
   * @param {?string} output - Expected output type. For example 'vec3'.
   * @param {?string} propertyName - The property name to assign the result.
   * @return {?(Object|Node)} The code flow or node.build() result.
   */
  flowNodeFromShaderStage(e, t, s = null, r = null) {
    const n = this.tab, i = this.cache, o = this.shaderStage, u = this.context;
    this.setShaderStage(e);
    const l = { ...this.context };
    delete l.nodeBlock, this.cache = this.globalCache, this.tab = "	", this.context = l;
    let c = null;
    if (this.buildStage === "generate") {
      const d = this.flowChildNode(t, s);
      r !== null && (d.code += `${this.tab + r} = ${d.result};
`), this.flowCode[e] = this.flowCode[e] + d.code, c = d;
    } else
      c = t.build(this);
    return this.setShaderStage(o), this.cache = i, this.tab = n, this.context = u, c;
  }
  /**
   * Returns an array holding all node attributes of this node builder.
   *
   * @return {Array<NodeAttribute>} The node attributes of this builder.
   */
  getAttributesArray() {
    return this.attributes.concat(this.bufferAttributes);
  }
  /**
   * Returns the attribute definitions as a shader string for the given shader stage.
   *
   * @abstract
   * @param {('vertex'|'fragment'|'compute'|'any')} shaderStage - The shader stage.
   * @return {string} The attribute code section.
   */
  getAttributes() {
    L("Abstract function.");
  }
  /**
   * Returns the varying definitions as a shader string for the given shader stage.
   *
   * @abstract
   * @param {('vertex'|'fragment'|'compute'|'any')} shaderStage - The shader stage.
   * @return {string} The varying code section.
   */
  getVaryings() {
    L("Abstract function.");
  }
  /**
   * Returns a single variable definition as a shader string for the given variable type and name.
   *
   * @param {string} type - The variable's type.
   * @param {string} name - The variable's name.
   * @param {?number} [count=null] - The array length.
   * @return {string} The shader string.
   */
  getVar(e, t, s = null) {
    return `${s !== null ? this.generateArrayDeclaration(e, s) : this.getType(e)} ${t}`;
  }
  /**
   * Returns the variable definitions as a shader string for the given shader stage.
   *
   * @param {('vertex'|'fragment'|'compute'|'any')} shaderStage - The shader stage.
   * @param {boolean} [global=false] - Whether the variables are global.
   * @return {string} The variable code section.
   */
  getVars(e, t = !1) {
    const s = [], r = this.vars[e];
    if (r !== void 0)
      for (const n of r)
        s.push(`${this.getVar(n.type, n.name, n.count)};`);
    return s.join(t ? `
` : `
	`);
  }
  /**
   * Returns the uniform definitions as a shader string for the given shader stage.
   *
   * @abstract
   * @param {('vertex'|'fragment'|'compute'|'any')} shaderStage - The shader stage.
   * @return {string} The uniform code section.
   */
  getUniforms() {
    L("Abstract function.");
  }
  /**
   * Returns the native code definitions as a shader string for the given shader stage.
   *
   * @param {('vertex'|'fragment'|'compute'|'any')} shaderStage - The shader stage.
   * @return {string} The native code section.
   */
  getCodes(e) {
    const t = this.codes[e];
    let s = "";
    if (t !== void 0)
      for (const r of t)
        s += r.code + `
`;
    return s;
  }
  /**
   * Returns the hash of this node builder.
   *
   * @return {string} The hash.
   */
  getHash() {
    return this.vertexShader + this.fragmentShader + this.computeShader;
  }
  /**
   * Sets the current shader stage.
   *
   * @param {?('vertex'|'fragment'|'compute'|'any')} shaderStage - The shader stage to set.
   */
  setShaderStage(e) {
    this.shaderStage = e;
  }
  /**
   * Returns the current shader stage.
   *
   * @return {?('vertex'|'fragment'|'compute'|'any')} The current shader stage.
   */
  getShaderStage() {
    return this.shaderStage;
  }
  /**
   * Sets the current build stage.
   *
   * @param {?('setup'|'analyze'|'generate')} buildStage - The build stage to set.
   */
  setBuildStage(e) {
    this.buildStage = e;
  }
  /**
   * Returns the current build stage.
   *
   * @return {?('setup'|'analyze'|'generate')} The current build stage.
   */
  getBuildStage() {
    return this.buildStage;
  }
  /**
   * Controls the code build of the shader stages.
   *
   * @abstract
   */
  buildCode() {
    L("Abstract function.");
  }
  /**
   * Returns the current sub-build layer.
   *
   * @return {SubBuildNode} The current sub-build layers.
   */
  get subBuild() {
    return this.subBuildLayers[this.subBuildLayers.length - 1] || null;
  }
  /**
   * Adds a sub-build layer to the node builder.
   *
   * @param {SubBuildNode} subBuild - The sub-build layer to add.
   */
  addSubBuild(e) {
    this.subBuildLayers.push(e);
  }
  /**
   * Removes the last sub-build layer from the node builder.
   *
   * @return {SubBuildNode} The removed sub-build layer.
   */
  removeSubBuild() {
    return this.subBuildLayers.pop();
  }
  /**
   * Returns the closest sub-build layer for the given data.
   *
   * @param {Node|Set<string>|Array<string>} data - The data to get the closest sub-build layer from.
   * @return {?string} The closest sub-build name or null if none found.
   */
  getClosestSubBuild(e) {
    let t;
    if (e && e.isNode ? e.isShaderCallNodeInternal ? t = e.shaderNode.subBuilds : e.isStackNode ? t = [e.subBuild] : t = this.getDataFromNode(e, "any").subBuilds : e instanceof Set ? t = [...e] : t = e, !t) return null;
    const s = this.subBuildLayers;
    for (let r = t.length - 1; r >= 0; r--) {
      const n = t[r];
      if (s.includes(n))
        return n;
    }
    return null;
  }
  /**
   * Returns the output node of a sub-build layer.
   *
   * @param {Node} node - The node to get the output from.
   * @return {string} The output node name.
   */
  getSubBuildOutput(e) {
    return this.getSubBuildProperty("outputNode", e);
  }
  /**
   * Returns the sub-build property name for the given property and node.
   *
   * @param {string} [property=''] - The property name.
   * @param {?Node} [node=null] - The node to get the sub-build from.
   * @return {string} The sub-build property name.
   */
  getSubBuildProperty(e = "", t = null) {
    let s;
    t !== null ? s = this.getClosestSubBuild(t) : s = this.subBuildFn;
    let r;
    return s ? r = e ? s + "_" + e : s : r = e, r;
  }
  /**
   * Prebuild the node builder.
   */
  prebuild() {
    const { object: e, renderer: t, material: s } = this;
    if (t.contextNode.isContextNode === !0 ? this.context = { ...this.context, ...t.contextNode.getFlowContextData() } : F('NodeBuilder: "renderer.contextNode" must be an instance of `context()`.'), s && s.contextNode && (s.contextNode.isContextNode === !0 ? this.context = { ...this.context, ...s.contextNode.getFlowContextData() } : F('NodeBuilder: "material.contextNode" must be an instance of `context()`.')), s !== null) {
      let r = t.library.fromMaterial(s);
      r === null && (F(`NodeBuilder: Material "${s.type}" is not compatible.`), r = new Ne()), r.build(this);
    } else
      this.addFlow("compute", e);
  }
  /**
   * Central build method which controls the build for the given object.
   *
   * @return {NodeBuilder} A reference to this node builder.
   */
  build() {
    this.prebuild();
    for (const e of vi) {
      this.setBuildStage(e), this.context.position && this.context.position.isNode && this.flowNodeFromShaderStage("vertex", this.context.position);
      for (const t of Ei) {
        this.setShaderStage(t);
        const s = this.flowNodes[t];
        for (const r of s)
          e === "generate" ? this.flowNode(r) : r.build(this);
      }
    }
    return this.setBuildStage(null), this.setShaderStage(null), this.buildCode(), this.buildUpdateNodes(), this;
  }
  /**
   * Async version of build() that yields to main thread between shader stages.
   * Use this in compileAsync() to prevent blocking the main thread.
   *
   * @return {Promise<NodeBuilder>} A promise that resolves to this node builder.
   */
  async buildAsync() {
    this.prebuild();
    for (const e of vi) {
      this.setBuildStage(e), this.context.position && this.context.position.isNode && this.flowNodeFromShaderStage("vertex", this.context.position);
      for (const t of Ei) {
        this.setShaderStage(t);
        const s = this.flowNodes[t];
        for (const r of s)
          e === "generate" ? this.flowNode(r) : r.build(this);
        await Dl();
      }
    }
    return this.setBuildStage(null), this.setShaderStage(null), this.buildCode(), this.buildUpdateNodes(), this;
  }
  /**
   * Returns shared data object for the given node.
   *
   * @param {Node} node - The node to get shared data from.
   * @return {Object} The shared data.
   */
  getSharedDataFromNode(e) {
    let t = Zb.get(e);
    return t === void 0 && (t = {}), t;
  }
  /**
   * Returns a uniform representation which is later used for UBO generation and rendering.
   *
   * @param {NodeUniform} uniformNode - The uniform node.
   * @param {string} type - The requested type.
   * @return {Uniform} The uniform.
   */
  getNodeUniform(e, t) {
    const s = this.getSharedDataFromNode(e);
    let r = s.cache;
    if (r === void 0) {
      if (t === "float" || t === "int" || t === "uint") r = new zb(e);
      else if (t === "vec2" || t === "ivec2" || t === "uvec2") r = new Wb(e);
      else if (t === "vec3" || t === "ivec3" || t === "uvec3") r = new Hb(e);
      else if (t === "vec4" || t === "ivec4" || t === "uvec4") r = new qb(e);
      else if (t === "color") r = new Xb(e);
      else if (t === "mat2") r = new Kb(e);
      else if (t === "mat3") r = new Qb(e);
      else if (t === "mat4") r = new Yb(e);
      else
        throw new Error(`Uniform "${t}" not implemented.`);
      s.cache = r;
    }
    return r;
  }
  /**
   * Formats the given shader snippet from a given type into another one. E.g.
   * this method might be used to convert a simple float string `"1.0"` into a
   * `vec3` representation: `"vec3<f32>( 1.0 )"`.
   *
   * @param {string} snippet - The shader snippet.
   * @param {string} fromType - The source type.
   * @param {string} toType - The target type.
   * @return {string} The updated shader string.
   */
  format(e, t, s) {
    if (t = this.getVectorType(t), s = this.getVectorType(s), t === s || s === null || this.isReference(s))
      return e;
    const r = this.getTypeLength(t), n = this.getTypeLength(s);
    return r === 16 && n === 9 ? `${this.getType(s)}( ${e}[ 0 ].xyz, ${e}[ 1 ].xyz, ${e}[ 2 ].xyz )` : r === 9 && n === 4 ? `${this.getType(s)}( ${e}[ 0 ].xy, ${e}[ 1 ].xy )` : r > 4 || n > 4 || n === 0 ? e : r === n ? `${this.getType(s)}( ${e} )` : r > n ? (e = s === "bool" ? `all( ${e} )` : `${e}.${"xyz".slice(0, n)}`, this.format(e, this.getTypeFromLength(n, this.getComponentType(t)), s)) : n === 4 && r > 1 ? `${this.getType(s)}( ${this.format(e, t, "vec3")}, 1.0 )` : r === 2 ? `${this.getType(s)}( ${this.format(e, t, "vec2")}, 0.0 )` : (r === 1 && n > 1 && t !== this.getComponentType(s) && (e = `${this.getType(this.getComponentType(s))}( ${e} )`), `${this.getType(s)}( ${e} )`);
  }
  /**
   * Returns a signature with the engine's current revision.
   *
   * @return {string} The signature.
   */
  getSignature() {
    return `// Three.js r${ua} - Node System
`;
  }
  /**
   * Returns `true` if data from the previous frame are required. Relevant
   * when computing motion vectors with {@link VelocityNode}.
   *
   * @return {boolean} Whether data from the previous frame are required or not.
   */
  needsPreviousData() {
    const e = this.renderer.getMRT();
    return e && e.has("velocity") || _c(this.object).useVelocity === !0;
  }
}
class rl {
  /**
   * Constructs a new node fame.
   */
  constructor() {
    this.time = 0, this.deltaTime = 0, this.frameId = 0, this.renderId = 0, this.updateMap = /* @__PURE__ */ new WeakMap(), this.updateBeforeMap = /* @__PURE__ */ new WeakMap(), this.updateAfterMap = /* @__PURE__ */ new WeakMap(), this.renderer = null, this.material = null, this.camera = null, this.object = null, this.scene = null;
  }
  /**
   * Returns a dictionary for a given node and update map which
   * is used to correctly call node update methods per frame or render.
   *
   * @private
   * @param {WeakMap<Node, Object>} referenceMap - The reference weak map.
   * @param {Node} nodeRef - The reference to the current node.
   * @return {Object<string,WeakMap<Object, number>>} The dictionary.
   */
  _getMaps(e, t) {
    let s = e.get(t);
    return s === void 0 && (s = {
      renderId: 0,
      frameId: 0
    }, e.set(t, s)), s;
  }
  /**
   * This method executes the {@link Node#updateBefore} for the given node.
   * It makes sure {@link Node#updateBeforeType} is honored meaning the update
   * is only executed once per frame, render or object depending on the update
   * type.
   *
   * @param {Node} node - The node that should be updated.
   */
  updateBeforeNode(e) {
    const t = e.getUpdateBeforeType(), s = e.updateReference(this);
    if (t === z.FRAME) {
      const r = this._getMaps(this.updateBeforeMap, s);
      if (r.frameId !== this.frameId) {
        const n = r.frameId;
        r.frameId = this.frameId, e.updateBefore(this) === !1 && (r.frameId = n);
      }
    } else if (t === z.RENDER) {
      const r = this._getMaps(this.updateBeforeMap, s);
      if (r.renderId !== this.renderId) {
        const n = r.renderId;
        r.renderId = this.renderId, e.updateBefore(this) === !1 && (r.renderId = n);
      }
    } else t === z.OBJECT && e.updateBefore(this);
  }
  /**
   * This method executes the {@link Node#updateAfter} for the given node.
   * It makes sure {@link Node#updateAfterType} is honored meaning the update
   * is only executed once per frame, render or object depending on the update
   * type.
   *
   * @param {Node} node - The node that should be updated.
   */
  updateAfterNode(e) {
    const t = e.getUpdateAfterType(), s = e.updateReference(this);
    if (t === z.FRAME) {
      const r = this._getMaps(this.updateAfterMap, s);
      r.frameId !== this.frameId && e.updateAfter(this) !== !1 && (r.frameId = this.frameId);
    } else if (t === z.RENDER) {
      const r = this._getMaps(this.updateAfterMap, s);
      r.renderId !== this.renderId && e.updateAfter(this) !== !1 && (r.renderId = this.renderId);
    } else t === z.OBJECT && e.updateAfter(this);
  }
  /**
   * This method executes the {@link Node#update} for the given node.
   * It makes sure {@link Node#updateType} is honored meaning the update
   * is only executed once per frame, render or object depending on the update
   * type.
   *
   * @param {Node} node - The node that should be updated.
   */
  updateNode(e) {
    const t = e.getUpdateType(), s = e.updateReference(this);
    if (t === z.FRAME) {
      const r = this._getMaps(this.updateMap, s);
      r.frameId !== this.frameId && e.update(this) !== !1 && (r.frameId = this.frameId);
    } else if (t === z.RENDER) {
      const r = this._getMaps(this.updateMap, s);
      r.renderId !== this.renderId && e.update(this) !== !1 && (r.renderId = this.renderId);
    } else t === z.OBJECT && e.update(this);
  }
  /**
   * Updates the internal state of the node frame. This method is
   * called by the renderer in its internal animation loop.
   */
  update() {
    this.frameId++, this.lastTime === void 0 && (this.lastTime = performance.now()), this.deltaTime = (performance.now() - this.lastTime) / 1e3, this.lastTime = performance.now(), this.time += this.deltaTime;
  }
}
class Oa {
  /**
   * Constructs a new node function input.
   *
   * @param {string} type - The input type.
   * @param {string} name - The input name.
   * @param {?number} [count=null] - If the input is an Array, count will be the length.
   * @param {('in'|'out'|'inout')} [qualifier=''] - The parameter qualifier (only relevant for GLSL).
   * @param {boolean} [isConst=false] - Whether the input uses a const qualifier or not (only relevant for GLSL).
   */
  constructor(e, t, s = null, r = "", n = !1) {
    this.type = e, this.name = t, this.count = s, this.qualifier = r, this.isConst = n;
  }
}
Oa.isNodeFunctionInput = !0;
class eS extends Os {
  static get type() {
    return "AmbientLightNode";
  }
  /**
   * Constructs a new ambient light node.
   *
   * @param {?AmbientLight} [light=null] - The ambient light source.
   */
  constructor(e = null) {
    super(e);
  }
  setup({ context: e }) {
    e.irradiance.addAssign(this.colorNode);
  }
}
class tS extends Os {
  static get type() {
    return "DirectionalLightNode";
  }
  /**
   * Constructs a new directional light node.
   *
   * @param {?DirectionalLight} [light=null] - The directional light source.
   */
  constructor(e = null) {
    super(e);
  }
  setupDirect() {
    const e = this.colorNode;
    return { lightDirection: th(this.light), lightColor: e };
  }
}
class sS extends Os {
  static get type() {
    return "HemisphereLightNode";
  }
  /**
   * Constructs a new hemisphere light node.
   *
   * @param {?HemisphereLight} [light=null] - The hemisphere light source.
   */
  constructor(e = null) {
    super(e), this.lightPositionNode = Jd(e), this.lightDirectionNode = this.lightPositionNode.normalize(), this.groundColorNode = k(new Ht()).setGroup(V);
  }
  /**
   * Overwritten to updated hemisphere light specific uniforms.
   *
   * @param {NodeFrame} frame - A reference to the current node frame.
   */
  update(e) {
    const { light: t } = this;
    super.update(e), this.lightPositionNode.object3d = t, this.groundColorNode.value.copy(t.groundColor).multiplyScalar(t.intensity);
  }
  setup(e) {
    const { colorNode: t, groundColorNode: s, lightDirectionNode: r } = this, i = gr.dot(r).mul(0.5).add(0.5), o = ne(s, t, i);
    e.context.irradiance.addAssign(o);
  }
}
class Ga extends Os {
  static get type() {
    return "SpotLightNode";
  }
  /**
   * Constructs a new spot light node.
   *
   * @param {?SpotLight} [light=null] - The spot light source.
   */
  constructor(e = null) {
    super(e), this.coneCosNode = k(0).setGroup(V), this.penumbraCosNode = k(0).setGroup(V), this.cutoffDistanceNode = k(0).setGroup(V), this.decayExponentNode = k(0).setGroup(V), this.colorNode = k(this.color).setGroup(V);
  }
  /**
   * Overwritten to updated spot light specific uniforms.
   *
   * @param {NodeFrame} frame - A reference to the current node frame.
   */
  update(e) {
    super.update(e);
    const { light: t } = this;
    this.coneCosNode.value = Math.cos(t.angle), this.penumbraCosNode.value = Math.cos(t.angle * (1 - t.penumbra)), this.cutoffDistanceNode.value = t.distance, this.decayExponentNode.value = t.decay;
  }
  /**
   * Computes the spot attenuation for the given angle.
   *
   * @param {NodeBuilder} builder - The node builder.
   * @param {Node<float>} angleCosine - The angle to compute the spot attenuation for.
   * @return {Node<float>} The spot attenuation.
   */
  getSpotAttenuation(e, t) {
    const { coneCosNode: s, penumbraCosNode: r } = this;
    return Wt(s, r, t);
  }
  getLightCoord(e) {
    const t = e.getNodeProperties(this);
    let s = t.projectionUV;
    return s === void 0 && (s = W_(this.light, e.context.positionWorld), t.projectionUV = s), s;
  }
  setupDirect(e) {
    const { colorNode: t, cutoffDistanceNode: s, decayExponentNode: r, light: n } = this, i = this.getLightVector(e), o = i.normalize(), u = o.dot(th(n)), l = this.getSpotAttenuation(e, u), c = i.length(), d = ih({
      lightDistance: c,
      cutoffDistance: s,
      decayExponent: r
    });
    let h = t.mul(l).mul(d), f, p;
    return n.colorNode ? (p = this.getLightCoord(e), f = n.colorNode(p)) : n.map && (p = this.getLightCoord(e), f = ce(n.map, p.xy).onRenderUpdate(() => n.map)), f && (h = p.mul(2).sub(1).abs().lessThan(1).all().select(h.mul(f), h)), { lightColor: h, lightDirection: o };
  }
}
class rS extends Ga {
  static get type() {
    return "IESSpotLightNode";
  }
  /**
   * Overwrites the default implementation to compute an IES conform spot attenuation.
   *
   * @param {NodeBuilder} builder - The node builder.
   * @param {Node<float>} angleCosine - The angle to compute the spot attenuation for.
   * @return {Node<float>} The spot attenuation.
   */
  getSpotAttenuation(e, t) {
    const s = this.light.iesMap;
    let r = null;
    if (s && s.isTexture === !0) {
      const n = t.acos().mul(1 / Math.PI);
      r = ce(s, I(n, 0), 0).r;
    } else
      r = super.getSpotAttenuation(t);
    return r;
  }
}
class nS extends Os {
  static get type() {
    return "LightProbeNode";
  }
  /**
   * Constructs a new light probe node.
   *
   * @param {?LightProbe} [light=null] - The light probe.
   */
  constructor(e = null) {
    super(e);
    const t = [];
    for (let s = 0; s < 9; s++) t.push(new H());
    this.lightProbe = st(t);
  }
  /**
   * Overwritten to updated light probe specific uniforms.
   *
   * @param {NodeFrame} frame - A reference to the current node frame.
   */
  update(e) {
    const { light: t } = this;
    super.update(e);
    for (let s = 0; s < 9; s++)
      this.lightProbe.array[s].copy(t.sh.coefficients[s]).multiplyScalar(t.intensity);
  }
  setup(e) {
    const t = vb(gr, this.lightProbe);
    e.context.irradiance.addAssign(t);
  }
}
const iS = /* @__PURE__ */ A(([a, e]) => {
  const t = a.abs().sub(e);
  return zt(Be(t, 0)).add(or(Be(t.x, t.y), 0));
});
class oS extends Ga {
  static get type() {
    return "ProjectorLightNode";
  }
  update(e) {
    super.update(e);
    const t = this.light;
    if (this.penumbraCosNode.value = Math.min(Math.cos(t.angle * (1 - t.penumbra)), 0.99999), t.aspect === null) {
      let s = 1;
      t.map !== null && (s = t.map.width / t.map.height), t.shadow.aspect = s;
    } else
      t.shadow.aspect = t.aspect;
  }
  /**
   * Overwrites the default implementation to compute projection attenuation.
   *
   * @param {NodeBuilder} builder - The node builder.
   * @return {Node<float>} The spot attenuation.
   */
  getSpotAttenuation(e) {
    const t = b(0), s = this.penumbraCosNode, r = Ia(this.light).mul(e.context.positionWorld || ar);
    return ue(r.w.greaterThan(0), () => {
      const n = r.xyz.div(r.w), i = iS(n.xy.sub(I(0.5)), I(0.5)), o = Ct(-1, ke(1, Oc(s)).sub(1));
      t.assign(Ea(i.mul(-2).mul(o)));
    }), t;
  }
}
const to = /* @__PURE__ */ new dt(), yn = /* @__PURE__ */ new dt();
let Ar = null;
class aS extends Os {
  static get type() {
    return "RectAreaLightNode";
  }
  /**
   * Constructs a new rect area light node.
   *
   * @param {?RectAreaLight} [light=null] - The rect area light source.
   */
  constructor(e = null) {
    super(e), this.halfHeight = k(new H()).setGroup(V), this.halfWidth = k(new H()).setGroup(V), this.updateType = z.RENDER;
  }
  /**
   * Overwritten to updated rect area light specific uniforms.
   *
   * @param {NodeFrame} frame - A reference to the current node frame.
   */
  update(e) {
    super.update(e);
    const { light: t } = this, s = e.camera.matrixWorldInverse;
    yn.identity(), to.copy(t.matrixWorld), to.premultiply(s), yn.extractRotation(to), this.halfWidth.value.set(t.width * 0.5, 0, 0), this.halfHeight.value.set(0, t.height * 0.5, 0), this.halfWidth.value.applyMatrix4(yn), this.halfHeight.value.applyMatrix4(yn);
  }
  setupDirectRectArea(e) {
    let t, s;
    e.isAvailable("float32Filterable") ? (t = ce(Ar.LTC_FLOAT_1), s = ce(Ar.LTC_FLOAT_2)) : (t = ce(Ar.LTC_HALF_1), s = ce(Ar.LTC_HALF_2));
    const { colorNode: r, light: n } = this, i = eh(n);
    return {
      lightColor: r,
      lightPosition: i,
      halfWidth: this.halfWidth,
      halfHeight: this.halfHeight,
      ltc_1: t,
      ltc_2: s
    };
  }
  /**
   * Used to configure the internal BRDF approximation texture data.
   *
   * @param {RectAreaLightTexturesLib} ltc - The BRDF approximation texture data.
   */
  static setLTC(e) {
    Ar = e;
  }
}
class uh {
  /**
   * The method parses the given native code an returns a node function.
   *
   * @abstract
   * @param {string} source - The native shader code.
   * @return {NodeFunction} A node function.
   */
  parseFunction() {
    L("Abstract function.");
  }
}
class ka {
  /**
   * Constructs a new node function.
   *
   * @param {string} type - The node type. This type is the return type of the node function.
   * @param {Array<NodeFunctionInput>} inputs - The function's inputs.
   * @param {string} [name=''] - The function's name.
   * @param {string} [precision=''] - The precision qualifier.
   */
  constructor(e, t, s = "", r = "") {
    this.type = e, this.inputs = t, this.name = s, this.precision = r;
  }
  /**
   * This method returns the native code of the node function.
   *
   * @abstract
   * @param {string} name - The function's name.
   * @return {string} A shader code.
   */
  getCode() {
    L("Abstract function.");
  }
}
ka.isNodeFunction = !0;
const uS = /^\s*(highp|mediump|lowp)?\s*([a-z_0-9]+)\s*([a-z_0-9]+)?\s*\(([\s\S]*?)\)/i, lS = /[a-z_0-9]+/ig, nl = "#pragma main", cS = (a) => {
  a = a.trim();
  const e = a.indexOf(nl), t = e !== -1 ? a.slice(e + nl.length) : a, s = t.match(uS);
  if (s !== null && s.length === 5) {
    const r = s[4], n = [];
    let i = null;
    for (; (i = lS.exec(r)) !== null; )
      n.push(i);
    const o = [];
    let u = 0;
    for (; u < n.length; ) {
      const p = n[u][0] === "const";
      p === !0 && u++;
      let g = n[u][0];
      g === "in" || g === "out" || g === "inout" ? u++ : g = "";
      const m = n[u++][0];
      let y = Number.parseInt(n[u][0]);
      Number.isNaN(y) === !1 ? u++ : y = null;
      const _ = n[u++][0];
      o.push(new Oa(m, _, y, g, p));
    }
    const l = t.substring(s[0].length), c = s[3] !== void 0 ? s[3] : "", d = s[2], h = s[1] !== void 0 ? s[1] : "", f = e !== -1 ? a.slice(0, e) : "";
    return {
      type: d,
      inputs: o,
      name: c,
      precision: h,
      inputsCode: r,
      blockCode: l,
      headerCode: f
    };
  } else
    throw new Error("FunctionNode: Function is not a GLSL code.");
};
class dS extends ka {
  /**
   * Constructs a new GLSL node function.
   *
   * @param {string} source - The GLSL source.
   */
  constructor(e) {
    const { type: t, inputs: s, name: r, precision: n, inputsCode: i, blockCode: o, headerCode: u } = cS(e);
    super(t, s, r, n), this.inputsCode = i, this.blockCode = o, this.headerCode = u;
  }
  /**
   * This method returns the GLSL code of the node function.
   *
   * @param {string} [name=this.name] - The function's name.
   * @return {string} The shader code.
   */
  getCode(e = this.name) {
    let t;
    const s = this.blockCode;
    if (s !== "") {
      const { type: r, inputsCode: n, headerCode: i, precision: o } = this;
      let u = `${r} ${e} ( ${n.trim()} )`;
      o !== "" && (u = `${o} ${u}`), t = i + u + s;
    } else
      t = "";
    return t;
  }
}
class hS extends uh {
  /**
   * The method parses the given GLSL code an returns a node function.
   *
   * @param {string} source - The GLSL code.
   * @return {GLSLNodeFunction} A node function.
   */
  parseFunction(e) {
    return new dS(e);
  }
}
const Je = [], Kt = [];
class fS extends ls {
  /**
   * Constructs a new nodes management component.
   *
   * @param {Renderer} renderer - The renderer.
   * @param {Backend} backend - The renderer's backend.
   */
  constructor(e, t) {
    super(), this.renderer = e, this.backend = t, this.nodeFrame = new rl(), this.nodeBuilderCache = /* @__PURE__ */ new Map(), this.callHashCache = new us(), this.groupsData = new us(), this._buildQueue = [], this._buildInProgress = !1, this.cacheLib = {};
  }
  /**
   * Returns `true` if the given node uniforms group must be updated or not.
   *
   * @param {NodeUniformsGroup} nodeUniformsGroup - The node uniforms group.
   * @return {boolean} Whether the node uniforms group requires an update or not.
   */
  updateGroup(e) {
    const t = e.groupNode;
    Je[0] = t, Je[1] = e;
    let s = this.groupsData.get(Je);
    return s === void 0 && this.groupsData.set(Je, s = {}), Je[0] = null, Je[1] = null, s.version !== t.version ? (s.version = t.version, !0) : !1;
  }
  /**
   * Returns the cache key for the given render object.
   *
   * @param {RenderObject} renderObject - The render object.
   * @return {number} The cache key.
   */
  getForRenderCacheKey(e) {
    return e.initialCacheKey;
  }
  /**
   * Creates a node builder configured for the given render object and material.
   *
   * @private
   * @param {RenderObject} renderObject - The render object.
   * @param {Material} material - The material to use.
   * @return {NodeBuilder} The configured node builder.
   */
  _createNodeBuilder(e, t) {
    const s = this.backend.createNodeBuilder(e.object, this.renderer);
    return s.scene = e.scene, s.material = t, s.camera = e.camera, s.context.material = t, s.lightsNode = e.lightsNode, s.environmentNode = this.getEnvironmentNode(e.scene), s.fogNode = this.getFogNode(e.scene), s.clippingContext = e.clippingContext, this.renderer.getOutputRenderTarget() && this.renderer.getOutputRenderTarget().multiview && s.enableMultiview(), s;
  }
  /**
   * Returns a node builder state for the given render object.
   *
   * @param {RenderObject} renderObject - The render object.
   * @param {boolean} [useAsync=false] - Whether to use async build with yielding.
   * @return {NodeBuilderState|Promise<NodeBuilderState>} The node builder state (or Promise if async).
   */
  getForRender(e, t = !1) {
    const s = this.get(e);
    let r = s.nodeBuilderState;
    if (r === void 0) {
      const { nodeBuilderCache: n } = this, i = this.getForRenderCacheKey(e);
      if (r = n.get(i), r === void 0) {
        const o = async () => {
          let u = this._createNodeBuilder(e, e.material);
          try {
            t ? await u.buildAsync() : u.build();
          } catch (l) {
            u = this._createNodeBuilder(e, new Ne()), t ? await u.buildAsync() : u.build(), F("TSL: " + l);
          }
          return u;
        };
        if (t)
          return o().then((u) => (r = this._createNodeBuilderState(u), n.set(i, r), r.usedTimes++, s.nodeBuilderState = r, r));
        {
          let u = this._createNodeBuilder(e, e.material);
          try {
            u.build();
          } catch (l) {
            u = this._createNodeBuilder(e, new Ne()), u.build();
            let c = l.stackTrace;
            !c && l.stack && (c = new Pe(l.stack)), F("TSL: " + l, c);
          }
          r = this._createNodeBuilderState(u), n.set(i, r);
        }
      }
      r.usedTimes++, s.nodeBuilderState = r;
    }
    return r;
  }
  /**
   * Async version of getForRender() that yields to main thread during build.
   * Use this in compileAsync() to prevent blocking the main thread.
   *
   * @param {RenderObject} renderObject - The render object.
   * @return {Promise<NodeBuilderState>} A promise that resolves to the node builder state.
   */
  getForRenderAsync(e) {
    const t = this.getForRender(e, !0);
    return t.then ? t : Promise.resolve(t);
  }
  /**
   * Returns nodeBuilderState if ready, null if pending async build.
   * Queues async build on first call for cache miss.
   * Use this in render() path to enable non-blocking compilation.
   *
   * @param {RenderObject} renderObject - The render object.
   * @return {?NodeBuilderState} The node builder state, or null if still building.
   */
  getForRenderDeferred(e) {
    const t = this.get(e);
    if (t.nodeBuilderState !== void 0)
      return t.nodeBuilderState;
    const s = this.getForRenderCacheKey(e), r = this.nodeBuilderCache.get(s);
    return r !== void 0 ? (r.usedTimes++, t.nodeBuilderState = r, r) : (t.pendingBuild !== !0 && (t.pendingBuild = !0, this._buildQueue.push(() => this.getForRenderAsync(e).then(() => {
      t.pendingBuild = !1;
    })), this._processBuildQueue()), null);
  }
  /**
   * Processes the build queue one item at a time.
   * This ensures builds don't all run simultaneously and freeze the main thread.
   *
   * @private
   */
  _processBuildQueue() {
    if (this._buildInProgress || this._buildQueue.length === 0)
      return;
    this._buildInProgress = !0, this._buildQueue.shift()().then(() => {
      this._buildInProgress = !1, this._processBuildQueue();
    });
  }
  /**
   * Deletes the given object from the internal data map
   *
   * @param {any} object - The object to delete.
   * @return {?Object} The deleted dictionary.
   */
  delete(e) {
    if (e.isRenderObject) {
      const t = this.get(e).nodeBuilderState;
      t !== void 0 && (t.usedTimes--, t.usedTimes === 0 && this.nodeBuilderCache.delete(this.getForRenderCacheKey(e)));
    }
    return super.delete(e);
  }
  /**
   * Returns a node builder state for the given compute node.
   *
   * @param {Node} computeNode - The compute node.
   * @return {NodeBuilderState} The node builder state.
   */
  getForCompute(e) {
    const t = this.get(e);
    let s = t.nodeBuilderState;
    if (s === void 0) {
      const r = this.backend.createNodeBuilder(e, this.renderer);
      r.build(), s = this._createNodeBuilderState(r), t.nodeBuilderState = s;
    }
    return s;
  }
  /**
   * Creates a node builder state for the given node builder.
   *
   * @private
   * @param {NodeBuilder} nodeBuilder - The node builder.
   * @return {NodeBuilderState} The node builder state.
   */
  _createNodeBuilderState(e) {
    return new wb(
      e.vertexShader,
      e.fragmentShader,
      e.computeShader,
      e.getAttributesArray(),
      e.getBindings(),
      e.updateNodes,
      e.updateBeforeNodes,
      e.updateAfterNodes,
      e.observer,
      e.transforms
    );
  }
  /**
   * Returns an environment node for the current configured
   * scene environment.
   *
   * @param {Scene} scene - The scene.
   * @return {Node} A node representing the current scene environment.
   */
  getEnvironmentNode(e) {
    this.updateEnvironment(e);
    let t = null;
    if (e.environmentNode && e.environmentNode.isNode)
      t = e.environmentNode;
    else {
      const s = this.get(e);
      s.environmentNode && (t = s.environmentNode);
    }
    return t;
  }
  /**
   * Returns a background node for the current configured
   * scene background.
   *
   * @param {Scene} scene - The scene.
   * @return {Node} A node representing the current scene background.
   */
  getBackgroundNode(e) {
    this.updateBackground(e);
    let t = null;
    if (e.backgroundNode && e.backgroundNode.isNode)
      t = e.backgroundNode;
    else {
      const s = this.get(e);
      s.backgroundNode && (t = s.backgroundNode);
    }
    return t;
  }
  /**
   * Returns a fog node for the current configured scene fog.
   *
   * @param {Scene} scene - The scene.
   * @return {Node} A node representing the current scene fog.
   */
  getFogNode(e) {
    return this.updateFog(e), e.fogNode || this.get(e).fogNode || null;
  }
  /**
   * Returns a cache key for the given scene and lights node.
   * This key is used by `RenderObject` as a part of the dynamic
   * cache key (a key that must be checked every time the render
   * objects is drawn).
   *
   * @param {Scene} scene - The scene.
   * @param {LightsNode} lightsNode - The lights node.
   * @return {number} The cache key.
   */
  getCacheKey(e, t) {
    Je[0] = e, Je[1] = t;
    const s = this.renderer.info.calls, r = this.callHashCache.get(Je) || {};
    if (r.callId !== s) {
      const n = this.getEnvironmentNode(e), i = this.getFogNode(e);
      t && Kt.push(t.getCacheKey(!0)), n && Kt.push(n.getCacheKey()), i && Kt.push(i.getCacheKey()), Kt.push(this.renderer.getOutputRenderTarget() && this.renderer.getOutputRenderTarget().multiview ? 1 : 0), Kt.push(this.renderer.shadowMap.enabled ? 1 : 0), Kt.push(this.renderer.shadowMap.type), r.callId = s, r.cacheKey = Yr(Kt), this.callHashCache.set(Je, r), Kt.length = 0;
    }
    return Je[0] = null, Je[1] = null, r.cacheKey;
  }
  /**
   * A boolean that indicates whether tone mapping should be enabled
   * or not.
   *
   * @type {boolean}
   */
  get isToneMappingState() {
    return !this.renderer.getRenderTarget();
  }
  /**
   * If a scene background is configured, this method makes sure to
   * represent the background with a corresponding node-based implementation.
   *
   * @param {Scene} scene - The scene.
   */
  updateBackground(e) {
    const t = this.get(e), s = e.background;
    if (s) {
      const r = e.backgroundBlurriness === 0 && t.backgroundBlurriness > 0 || e.backgroundBlurriness > 0 && t.backgroundBlurriness === 0;
      if (t.background !== s || r) {
        const n = this.getCacheNode("background", s, () => {
          if (s.isCubeTexture === !0 || s.mapping === oa || s.mapping === aa || s.mapping === fo) {
            if (e.backgroundBlurriness > 0 || s.mapping === fo)
              return $d(s);
            {
              let i;
              return s.isCubeTexture === !0 ? i = Ve(s) : i = ce(s), Ud(i);
            }
          } else {
            if (s.isTexture === !0)
              return ce(s, Es.flipY()).setUpdateMatrix(!0);
            s.isColor !== !0 && F("WebGPUNodes: Unsupported background configuration.", s);
          }
        }, r);
        t.backgroundNode = n, t.background = s, t.backgroundBlurriness = e.backgroundBlurriness;
      }
    } else t.backgroundNode && (delete t.backgroundNode, delete t.background);
  }
  /**
   * This method is part of the caching of nodes which are used to represents the
   * scene's background, fog or environment.
   *
   * @param {string} type - The type of object to cache.
   * @param {Object} object - The object.
   * @param {Function} callback - A callback that produces a node representation for the given object.
   * @param {boolean} [forceUpdate=false] - Whether an update should be enforced or not.
   * @return {Node} The node representation.
   */
  getCacheNode(e, t, s, r = !1) {
    const n = this.cacheLib[e] || (this.cacheLib[e] = /* @__PURE__ */ new WeakMap());
    let i = n.get(t);
    return (i === void 0 || r) && (i = s(), n.set(t, i)), i;
  }
  /**
   * If a scene fog is configured, this method makes sure to
   * represent the fog with a corresponding node-based implementation.
   *
   * @param {Scene} scene - The scene.
   */
  updateFog(e) {
    const t = this.get(e), s = e.fog;
    if (s) {
      if (t.fog !== s) {
        const r = this.getCacheNode("fog", s, () => {
          if (s.isFogExp2) {
            const n = ge("color", "color", s).setGroup(V), i = ge("density", "float", s).setGroup(V);
            return Zu(n, $_(i));
          } else if (s.isFog) {
            const n = ge("color", "color", s).setGroup(V), i = ge("near", "float", s).setGroup(V), o = ge("far", "float", s).setGroup(V);
            return Zu(n, V_(i, o));
          } else
            F("Renderer: Unsupported fog configuration.", s);
        });
        t.fogNode = r, t.fog = s;
      }
    } else
      delete t.fogNode, delete t.fog;
  }
  /**
   * If a scene environment is configured, this method makes sure to
   * represent the environment with a corresponding node-based implementation.
   *
   * @param {Scene} scene - The scene.
   */
  updateEnvironment(e) {
    const t = this.get(e), s = e.environment;
    if (s) {
      if (t.environment !== s) {
        const r = this.getCacheNode("environment", s, () => {
          if (s.isCubeTexture === !0)
            return Ve(s);
          if (s.isTexture === !0)
            return ce(s);
          F("Nodes: Unsupported environment configuration.", s);
        });
        t.environmentNode = r, t.environment = s;
      }
    } else t.environmentNode && (delete t.environmentNode, delete t.environment);
  }
  getNodeFrame(e = this.renderer, t = null, s = null, r = null, n = null) {
    const i = this.nodeFrame;
    return i.renderer = e, i.scene = t, i.object = s, i.camera = r, i.material = n, i;
  }
  getNodeFrameForRender(e) {
    return this.getNodeFrame(e.renderer, e.scene, e.object, e.camera, e.material);
  }
  /**
   * Returns the current output cache key.
   *
   * @return {string} The output cache key.
   */
  getOutputCacheKey() {
    const e = this.renderer;
    return e.toneMapping + "," + e.currentColorSpace + "," + e.xr.isPresenting;
  }
  /**
   * Returns a node that represents the output configuration (tone mapping and
   * color space) for the current target.
   *
   * @param {Texture} outputTarget - The output target.
   * @return {Node} The output node.
   */
  getOutputNode(e) {
    const t = this.renderer;
    return e.isArrayTexture ? ce(e, Es).depth(fr("gl_ViewID_OVR")).renderOutput(t.toneMapping, t.currentColorSpace) : ce(e, Es).renderOutput(t.toneMapping, t.currentColorSpace);
  }
  /**
   * Triggers the call of `updateBefore()` methods
   * for all nodes of the given render object.
   *
   * @param {RenderObject} renderObject - The render object.
   */
  updateBefore(e) {
    const t = e.getNodeBuilderState();
    for (const s of t.updateBeforeNodes)
      this.getNodeFrameForRender(e).updateBeforeNode(s);
  }
  /**
   * Triggers the call of `updateAfter()` methods
   * for all nodes of the given render object.
   *
   * @param {RenderObject} renderObject - The render object.
   */
  updateAfter(e) {
    const t = e.getNodeBuilderState();
    for (const s of t.updateAfterNodes)
      this.getNodeFrameForRender(e).updateAfterNode(s);
  }
  /**
   * Triggers the call of `update()` methods
   * for all nodes of the given compute node.
   *
   * @param {Node} computeNode - The compute node.
   */
  updateForCompute(e) {
    const t = this.getNodeFrame(), s = this.getForCompute(e);
    for (const r of s.updateNodes)
      t.updateNode(r);
  }
  /**
   * Triggers the call of `update()` methods
   * for all nodes of the given render object.
   *
   * @param {RenderObject} renderObject - The render object.
   */
  updateForRender(e) {
    const t = this.getNodeFrameForRender(e), s = e.getNodeBuilderState();
    for (const r of s.updateNodes)
      t.updateNode(r);
  }
  /**
   * Returns `true` if the given render object requires a refresh.
   *
   * @param {RenderObject} renderObject - The render object.
   * @return {boolean} Whether the given render object requires a refresh or not.
   */
  needsRefresh(e) {
    const t = this.getNodeFrameForRender(e);
    return e.getMonitor().needsRefresh(e, t);
  }
  /**
   * Frees the internal resources.
   */
  dispose() {
    super.dispose(), this.nodeFrame = new rl(), this.nodeBuilderCache = /* @__PURE__ */ new Map(), this.cacheLib = {};
  }
}
const so = /* @__PURE__ */ new Vh();
class jn {
  /**
   * Constructs a new clipping context.
   *
   * @param {?ClippingContext} [parentContext=null] - A reference to the parent clipping context.
   */
  constructor(e = null) {
    this.version = 0, this.clipIntersection = null, this.cacheKey = "", this.shadowPass = !1, this.viewNormalMatrix = new Kr(), this.clippingGroupContexts = /* @__PURE__ */ new WeakMap(), this.intersectionPlanes = [], this.unionPlanes = [], this.parentVersion = null, e !== null && (this.viewNormalMatrix = e.viewNormalMatrix, this.clippingGroupContexts = e.clippingGroupContexts, this.shadowPass = e.shadowPass, this.viewMatrix = e.viewMatrix);
  }
  /**
   * Projects the given source clipping planes and writes the result into the
   * destination array.
   *
   * @param {Array<Plane>} source - The source clipping planes.
   * @param {Array<Vector4>} destination - The destination.
   * @param {number} offset - The offset.
   */
  projectPlanes(e, t, s) {
    const r = e.length;
    for (let n = 0; n < r; n++) {
      so.copy(e[n]).applyMatrix4(this.viewMatrix, this.viewNormalMatrix);
      const i = t[s + n], o = so.normal;
      i.x = -o.x, i.y = -o.y, i.z = -o.z, i.w = so.constant;
    }
  }
  /**
   * Updates the root clipping context of a scene.
   *
   * @param {Scene} scene - The scene.
   * @param {Camera} camera - The camera that is used to render the scene.
   */
  updateGlobal(e, t) {
    this.shadowPass = e.overrideMaterial !== null && e.overrideMaterial.isShadowPassMaterial, this.viewMatrix = t.matrixWorldInverse, this.viewNormalMatrix.getNormalMatrix(this.viewMatrix);
  }
  /**
   * Updates the clipping context.
   *
   * @param {ClippingContext} parentContext - The parent context.
   * @param {ClippingGroup} clippingGroup - The clipping group this context belongs to.
   */
  update(e, t) {
    let s = !1;
    e.version !== this.parentVersion && (this.intersectionPlanes = Array.from(e.intersectionPlanes), this.unionPlanes = Array.from(e.unionPlanes), this.parentVersion = e.version), this.clipIntersection !== t.clipIntersection && (this.clipIntersection = t.clipIntersection, this.clipIntersection ? this.unionPlanes.length = e.unionPlanes.length : this.intersectionPlanes.length = e.intersectionPlanes.length);
    const r = t.clippingPlanes, n = r.length;
    let i, o;
    if (this.clipIntersection ? (i = this.intersectionPlanes, o = e.intersectionPlanes.length) : (i = this.unionPlanes, o = e.unionPlanes.length), i.length !== o + n) {
      i.length = o + n;
      for (let u = 0; u < n; u++)
        i[o + u] = new Se();
      s = !0;
    }
    this.projectPlanes(r, i, o), s && (this.version++, this.cacheKey = `${this.intersectionPlanes.length}:${this.unionPlanes.length}`);
  }
  /**
   * Returns a clipping context for the given clipping group.
   *
   * @param {ClippingGroup} clippingGroup - The clipping group.
   * @return {ClippingContext} The clipping context.
   */
  getGroupContext(e) {
    if (this.shadowPass && !e.clipShadows) return this;
    let t = this.clippingGroupContexts.get(e);
    return t === void 0 && (t = new jn(this), this.clippingGroupContexts.set(e, t)), t.update(this, e), t;
  }
  /**
   * The count of union clipping planes.
   *
   * @type {number}
   * @readonly
   */
  get unionClippingCount() {
    return this.unionPlanes.length;
  }
}
class pS {
  /**
   * Constructs a new bundle group.
   *
   * @param {BundleGroup} bundleGroup - The bundle group.
   * @param {Camera} camera - The camera the bundle group is rendered with.
   * @param {RenderContext} renderContext - The render context the bundle is rendered with.
   */
  constructor(e, t, s) {
    this.bundleGroup = e, this.camera = t, this.renderContext = s;
  }
}
const Qt = [];
class gS {
  /**
   * Constructs a new render bundle management component.
   */
  constructor() {
    this.bundles = new us();
  }
  /**
   * Returns a render bundle for the given bundle group and camera.
   *
   * @param {BundleGroup} bundleGroup - The bundle group.
   * @param {Camera} camera - The camera the bundle group is rendered with.
   * @param {RenderContext} renderContext - The render context the bundle is rendered with.
   * @return {RenderBundle} The render bundle.
   */
  get(e, t, s) {
    const r = this.bundles;
    Qt[0] = e, Qt[1] = t, Qt[2] = s;
    let n = r.get(Qt);
    return n === void 0 && (n = new pS(e, t, s), r.set(Qt, n)), Qt[0] = null, Qt[1] = null, Qt[2] = null, n;
  }
  /**
   * Frees all internal resources.
   */
  dispose() {
    this.bundles = new us();
  }
}
class lh {
  /**
   * Constructs a new node library.
   */
  constructor() {
    this.lightNodes = /* @__PURE__ */ new WeakMap(), this.materialNodes = /* @__PURE__ */ new Map(), this.toneMappingNodes = /* @__PURE__ */ new Map();
  }
  /**
   * Returns a matching node material instance for the given material object.
   *
   * This method also assigns/copies the properties of the given material object
   * to the node material. This is done to make sure the current material
   * configuration carries over to the node version.
   *
   * @param {Material} material - A material.
   * @return {NodeMaterial} The corresponding node material.
   */
  fromMaterial(e) {
    if (e.isNodeMaterial) return e;
    let t = null;
    const s = this.getMaterialNodeClass(e.type);
    if (s !== null) {
      t = new s();
      for (const r in e)
        t[r] = e[r];
    }
    return t;
  }
  /**
   * Adds a tone mapping node function for a tone mapping technique (constant).
   *
   * @param {Function} toneMappingNode - The tone mapping node function.
   * @param {number} toneMapping - The tone mapping.
   */
  addToneMapping(e, t) {
    this.addType(e, t, this.toneMappingNodes);
  }
  /**
   * Returns a tone mapping node function for a tone mapping technique (constant).
   *
   * @param {number} toneMapping - The tone mapping.
   * @return {?Function} The tone mapping node function. Returns `null` if no node function is found.
   */
  getToneMappingFunction(e) {
    return this.toneMappingNodes.get(e) || null;
  }
  /**
   * Returns a node material class definition for a material type.
   *
   * @param {string} materialType - The material type.
   * @return {?NodeMaterial.constructor} The node material class definition. Returns `null` if no node material is found.
   */
  getMaterialNodeClass(e) {
    return this.materialNodes.get(e) || null;
  }
  /**
   * Adds a node material class definition for a given material type.
   *
   * @param {NodeMaterial.constructor} materialNodeClass - The node material class definition.
   * @param {string} materialClassType - The material type.
   */
  addMaterial(e, t) {
    this.addType(e, t, this.materialNodes);
  }
  /**
   * Returns a light node class definition for a light class definition.
   *
   * @param {Light.constructor} light - The light class definition.
   * @return {?AnalyticLightNode.constructor} The light node class definition. Returns `null` if no light node is found.
   */
  getLightNodeClass(e) {
    return this.lightNodes.get(e) || null;
  }
  /**
   * Adds a light node class definition for a given light class definition.
   *
   * @param {AnalyticLightNode.constructor} lightNodeClass - The light node class definition.
   * @param {Light.constructor} lightClass - The light class definition.
   */
  addLight(e, t) {
    this.addClass(e, t, this.lightNodes);
  }
  /**
   * Adds a node class definition for the given type to the provided type library.
   *
   * @param {Node.constructor} nodeClass - The node class definition.
   * @param {number|string} type - The object type.
   * @param {Map<number|string,Node.constructor>} library - The type library.
   */
  addType(e, t, s) {
    if (s.has(t)) {
      L(`Redefinition of node ${t}`);
      return;
    }
    if (typeof e != "function") throw new Error(`Node class ${e.name} is not a class.`);
    if (typeof t == "function" || typeof t == "object") throw new Error(`Base class ${t} is not a class.`);
    s.set(t, e);
  }
  /**
   * Adds a node class definition for the given class definition to the provided type library.
   *
   * @param {Node.constructor} nodeClass - The node class definition.
   * @param {Node.constructor} baseClass - The class definition.
   * @param {WeakMap<Node.constructor, Node.constructor>} library - The type library.
   */
  addClass(e, t, s) {
    if (s.has(t)) {
      L(`Redefinition of node ${t.name}`);
      return;
    }
    if (typeof e != "function") throw new Error(`Node class ${e.name} is not a class.`);
    if (typeof t != "function") throw new Error(`Base class ${t.name} is not a class.`);
    s.set(t, e);
  }
}
const mS = /* @__PURE__ */ new sh(), il = /* @__PURE__ */ new WeakMap();
class xS {
  /**
   * Creates a new lights node for the given array of lights.
   *
   * @param {Array<Light>} lights - The render object.
   * @return {LightsNode} The lights node.
   */
  createNode(e = []) {
    return new sh().setLights(e);
  }
  /**
   * Returns a lights node for the given scene and camera.
   *
   * @param {Scene} scene - The scene.
   * @param {Camera} camera - The camera.
   * @return {LightsNode} The lights node.
   */
  getNode(e) {
    if (e.isQuadMesh) return mS;
    let t = il.get(e);
    return t === void 0 && (t = this.createNode(), il.set(e, t)), t;
  }
}
class Mr extends Ds {
  /**
   * Constructs a new XR render target.
   *
   * @param {number} [width=1] - The width of the render target.
   * @param {number} [height=1] - The height of the render target.
   * @param {Object} [options={}] - The configuration options.
   */
  constructor(e = 1, t = 1, s = {}) {
    super(e, t, s), this.isXRRenderTarget = !0, this._hasExternalTextures = !1, this._autoAllocateDepthBuffer = !0, this._isOpaqueFramebuffer = !1;
  }
  copy(e) {
    return super.copy(e), this._hasExternalTextures = e._hasExternalTextures, this._autoAllocateDepthBuffer = e._autoAllocateDepthBuffer, this._isOpaqueFramebuffer = e._isOpaqueFramebuffer, this;
  }
}
const ol = /* @__PURE__ */ new H(), al = /* @__PURE__ */ new H(), ul = /* @__PURE__ */ new WeakMap();
class TS extends ni {
  /**
   * Constructs a new XR manager.
   *
   * @param {Renderer} renderer - The renderer.
   * @param {boolean} [multiview=false] - Enables multiview if the device supports it.
   */
  constructor(e, t = !1) {
    super(), this.enabled = !1, this.isPresenting = !1, this.cameraAutoUpdate = !0, this._renderer = e, this._cameraL = new Pn(), this._cameraL.viewport = new Se(), this._cameraR = new Pn(), this._cameraR.viewport = new Se(), this._cameras = [this._cameraL, this._cameraR], this._cameraXR = new Ph(), this._currentDepthNear = null, this._currentDepthFar = null, this._controllers = [], this._controllerInputSources = [], this._xrRenderTarget = null, this._layers = [], this._sessionUsesLayers = !1, this._supportsGlBinding = typeof XRWebGLBinding < "u", this._createXRLayer = RS.bind(this), this._gl = null, this._currentAnimationContext = null, this._currentAnimationLoop = null, this._currentPixelRatio = null, this._currentSize = new je(), this._onSessionEvent = bS.bind(this), this._onSessionEnd = SS.bind(this), this._onInputSourcesChange = NS.bind(this), this._onAnimationFrame = AS.bind(this), this._referenceSpace = null, this._referenceSpaceType = "local-floor", this._customReferenceSpace = null, this._framebufferScaleFactor = 1, this._foveation = 1, this._session = null, this._glBaseLayer = null, this._glBinding = null, this._glProjLayer = null, this._xrFrame = null, this._supportsLayers = this._supportsGlBinding && "createProjectionLayer" in XRWebGLBinding.prototype, this._useMultiviewIfPossible = t, this._useMultiview = !1;
  }
  /**
   * Returns an instance of `THREE.Group` that represents the transformation
   * of a XR controller in target ray space. The requested controller is defined
   * by the given index.
   *
   * @param {number} index - The index of the XR controller.
   * @return {Group} A group that represents the controller's transformation.
   */
  getController(e) {
    return this._getController(e).getTargetRaySpace();
  }
  /**
   * Returns an instance of `THREE.Group` that represents the transformation
   * of a XR controller in grip space. The requested controller is defined
   * by the given index.
   *
   * @param {number} index - The index of the XR controller.
   * @return {Group} A group that represents the controller's transformation.
   */
  getControllerGrip(e) {
    return this._getController(e).getGripSpace();
  }
  /**
   * Returns an instance of `THREE.Group` that represents the transformation
   * of a XR controller in hand space. The requested controller is defined
   * by the given index.
   *
   * @param {number} index - The index of the XR controller.
   * @return {Group} A group that represents the controller's transformation.
   */
  getHand(e) {
    return this._getController(e).getHandSpace();
  }
  /**
   * Returns the foveation value.
   *
   * @return {number|undefined} The foveation value. Returns `undefined` if no base or projection layer is defined.
   */
  getFoveation() {
    if (!(this._glProjLayer === null && this._glBaseLayer === null))
      return this._foveation;
  }
  /**
   * Sets the foveation value.
   *
   * @param {number} foveation - A number in the range `[0,1]` where `0` means no foveation (full resolution)
   * and `1` means maximum foveation (the edges render at lower resolution).
   */
  setFoveation(e) {
    this._foveation = e, this._glProjLayer !== null && (this._glProjLayer.fixedFoveation = e), this._glBaseLayer !== null && this._glBaseLayer.fixedFoveation !== void 0 && (this._glBaseLayer.fixedFoveation = e);
  }
  /**
   * Returns the framebuffer scale factor.
   *
   * @return {number} The framebuffer scale factor.
   */
  getFramebufferScaleFactor() {
    return this._framebufferScaleFactor;
  }
  /**
   * Sets the framebuffer scale factor.
   *
   * This method can not be used during a XR session.
   *
   * @param {number} factor - The framebuffer scale factor.
   */
  setFramebufferScaleFactor(e) {
    this._framebufferScaleFactor = e, this.isPresenting === !0 && L("XRManager: Cannot change framebuffer scale while presenting.");
  }
  /**
   * Returns the reference space type.
   *
   * @return {XRReferenceSpaceType} The reference space type.
   */
  getReferenceSpaceType() {
    return this._referenceSpaceType;
  }
  /**
   * Sets the reference space type.
   *
   * This method can not be used during a XR session.
   *
   * @param {XRReferenceSpaceType} type - The reference space type.
   */
  setReferenceSpaceType(e) {
    this._referenceSpaceType = e, this.isPresenting === !0 && L("XRManager: Cannot change reference space type while presenting.");
  }
  /**
   * Returns the XR reference space.
   *
   * @return {XRReferenceSpace} The XR reference space.
   */
  getReferenceSpace() {
    return this._customReferenceSpace || this._referenceSpace;
  }
  /**
   * Sets a custom XR reference space.
   *
   * @param {XRReferenceSpace} space - The XR reference space.
   */
  setReferenceSpace(e) {
    this._customReferenceSpace = e;
  }
  /**
   * Returns the XR camera.
   *
   * @return {ArrayCamera} The XR camera.
   */
  getCamera() {
    return this._cameraXR;
  }
  /**
   * Returns the environment blend mode from the current XR session.
   *
   * @return {'opaque'|'additive'|'alpha-blend'|undefined} The environment blend mode. Returns `undefined` when used outside of a XR session.
   */
  getEnvironmentBlendMode() {
    if (this._session !== null)
      return this._session.environmentBlendMode;
  }
  /**
   * Returns the current XR binding.
   *
   * Creates a new binding if needed and the browser is
   * capable of doing so.
   *
   * @return {?XRWebGLBinding} The XR binding. Returns `null` if one cannot be created.
   */
  getBinding() {
    return this._glBinding === null && this._supportsGlBinding && (this._glBinding = new XRWebGLBinding(this._session, this._gl)), this._glBinding;
  }
  /**
   * Returns the current XR frame.
   *
   * @return {?XRFrame} The XR frame. Returns `null` when used outside a XR session.
   */
  getFrame() {
    return this._xrFrame;
  }
  /**
   * Returns `true` if the engine renders to a multiview target.
   *
   * @return {boolean} Whether the engine renders to a multiview render target or not.
   */
  useMultiview() {
    return this._useMultiview;
  }
  /**
   * This method can be used in XR applications to create a quadratic layer that presents a separate
   * rendered scene.
   *
   * @param {number} width - The width of the layer plane in world units.
   * @param {number} height - The height of the layer plane in world units.
   * @param {Vector3} translation - The position/translation of the layer plane in world units.
   * @param {Quaternion} quaternion - The orientation of the layer plane expressed as a quaternion.
   * @param {number} pixelwidth - The width of the layer's render target in pixels.
   * @param {number} pixelheight - The height of the layer's render target in pixels.
   * @param {Function} rendercall - A callback function that renders the layer. Similar to code in
   * the default animation loop, this method can be used to update/transform 3D object in the layer's scene.
   * @param {Object} [attributes={}] - Allows to configure the layer's render target.
   * @return {Mesh} A mesh representing the quadratic XR layer. This mesh should be added to the XR scene.
   */
  createQuadLayer(e, t, s, r, n, i, o, u = {}) {
    const l = new Lh(e, t), c = new Mr(
      n,
      i,
      {
        format: ct,
        type: $e,
        depthTexture: new vt(
          n,
          i,
          u.stencil ? Vt : _e,
          void 0,
          void 0,
          void 0,
          void 0,
          void 0,
          void 0,
          u.stencil ? kt : Gt
        ),
        stencilBuffer: u.stencil,
        resolveDepthBuffer: !1,
        resolveStencilBuffer: !1
      }
    );
    c._autoAllocateDepthBuffer = !0;
    const d = new gs({ color: 16777215, side: Ns });
    d.map = c.texture, d.map.offset.y = 1, d.map.repeat.y = -1;
    const h = new is(l, d);
    h.position.copy(s), h.quaternion.copy(r);
    const f = {
      type: "quad",
      width: e,
      height: t,
      translation: s,
      quaternion: r,
      pixelwidth: n,
      pixelheight: i,
      plane: h,
      material: d,
      rendercall: o,
      renderTarget: c
    };
    if (this._layers.push(f), this._session !== null) {
      f.plane.material = new gs({ color: 16777215, side: Ns }), f.plane.material.blending = Zs, f.plane.material.blendEquation = Pt, f.plane.material.blendSrc = jt, f.plane.material.blendDst = jt, f.xrlayer = this._createXRLayer(f);
      const p = this._session.renderState.layers;
      p.unshift(f.xrlayer), this._session.updateRenderState({ layers: p });
    } else
      c.isXRRenderTarget = !1;
    return h;
  }
  /**
   * This method can be used in XR applications to create a cylindrical layer that presents a separate
   * rendered scene.
   *
   * @param {number} radius - The radius of the cylinder in world units.
   * @param {number} centralAngle - The central angle of the cylinder in radians.
   * @param {number} aspectratio - The aspect ratio.
   * @param {Vector3} translation - The position/translation of the layer plane in world units.
   * @param {Quaternion} quaternion - The orientation of the layer plane expressed as a quaternion.
   * @param {number} pixelwidth - The width of the layer's render target in pixels.
   * @param {number} pixelheight - The height of the layer's render target in pixels.
   * @param {Function} rendercall - A callback function that renders the layer. Similar to code in
   * the default animation loop, this method can be used to update/transform 3D object in the layer's scene.
   * @param {Object} [attributes={}] - Allows to configure the layer's render target.
   * @return {Mesh} A mesh representing the cylindrical XR layer. This mesh should be added to the XR scene.
   */
  createCylinderLayer(e, t, s, r, n, i, o, u, l = {}) {
    const c = new Ih(e, e, e * t / s, 64, 64, !0, Math.PI - t / 2, t), d = new Mr(
      i,
      o,
      {
        format: ct,
        type: $e,
        depthTexture: new vt(
          i,
          o,
          l.stencil ? Vt : _e,
          void 0,
          void 0,
          void 0,
          void 0,
          void 0,
          void 0,
          l.stencil ? kt : Gt
        ),
        stencilBuffer: l.stencil,
        resolveDepthBuffer: !1,
        resolveStencilBuffer: !1
      }
    );
    d._autoAllocateDepthBuffer = !0;
    const h = new gs({ color: 16777215, side: Ge });
    h.map = d.texture, h.map.offset.y = 1, h.map.repeat.y = -1;
    const f = new is(c, h);
    f.position.copy(r), f.quaternion.copy(n);
    const p = {
      type: "cylinder",
      radius: e,
      centralAngle: t,
      aspectratio: s,
      translation: r,
      quaternion: n,
      pixelwidth: i,
      pixelheight: o,
      plane: f,
      material: h,
      rendercall: u,
      renderTarget: d
    };
    if (this._layers.push(p), this._session !== null) {
      p.plane.material = new gs({ color: 16777215, side: Ge }), p.plane.material.blending = Zs, p.plane.material.blendEquation = Pt, p.plane.material.blendSrc = jt, p.plane.material.blendDst = jt, p.xrlayer = this._createXRLayer(p);
      const g = this._session.renderState.layers;
      g.unshift(p.xrlayer), this._session.updateRenderState({ layers: g });
    } else
      d.isXRRenderTarget = !1;
    return f;
  }
  /**
   * Renders the XR layers that have been previously added to the scene.
   *
   * This method is usually called in your animation loop before rendering
   * the actual scene via `renderer.render( scene, camera );`.
   */
  renderLayers() {
    const e = new H(), t = new Oh(), s = this._renderer, r = this.isPresenting;
    this.isPresenting = !1;
    const n = new je();
    s.getSize(n);
    const i = s.getRenderTarget();
    for (const o of this._layers) {
      o.renderTarget.isXRRenderTarget = this._session !== null, o.renderTarget._hasExternalTextures = o.renderTarget.isXRRenderTarget;
      const u = s.contextNode;
      let l;
      if (o.renderTarget.isXRRenderTarget && this._sessionUsesLayers) {
        o.xrlayer.transform = new XRRigidTransform(o.plane.getWorldPosition(e), o.plane.getWorldQuaternion(t));
        const c = this._glBinding.getSubImage(o.xrlayer, this._xrFrame);
        s.backend.setXRRenderTargetTextures(
          o.renderTarget,
          c.colorTexture,
          void 0
        ), s._setXRLayerSize(o.renderTarget.width, o.renderTarget.height), l = ul.get(u), l === void 0 && (l = u.context({
          getOutput: (d) => td(d, s.toneMapping, s.outputColorSpace)
        }), ul.set(u, l));
      } else
        l = u;
      s.contextNode = l, s.setRenderTarget(o.renderTarget), o.rendercall(), s.contextNode = u;
    }
    s.setRenderTarget(i), s._setXRLayerSize(n.x, n.y), this.isPresenting = r;
  }
  /**
   * Returns the current XR session.
   *
   * @return {?XRSession} The XR session. Returns `null` when used outside a XR session.
   */
  getSession() {
    return this._session;
  }
  /**
   * After a XR session has been requested usually with one of the `*Button` modules, it
   * is injected into the renderer with this method. This method triggers the start of
   * the actual XR rendering.
   *
   * @async
   * @param {XRSession} session - The XR session to set.
   * @return {Promise} A Promise that resolves when the session has been set.
   */
  async setSession(e) {
    const t = this._renderer, s = t.backend;
    this._gl = t.getContext();
    const r = this._gl, n = r.getContextAttributes();
    if (this._session = e, e !== null) {
      if (s.isWebGPUBackend === !0) throw new Error('THREE.XRManager: XR is currently not supported with a WebGPU backend. Use WebGL by passing "{ forceWebGL: true }" to the constructor of the renderer.');
      if (e.addEventListener("select", this._onSessionEvent), e.addEventListener("selectstart", this._onSessionEvent), e.addEventListener("selectend", this._onSessionEvent), e.addEventListener("squeeze", this._onSessionEvent), e.addEventListener("squeezestart", this._onSessionEvent), e.addEventListener("squeezeend", this._onSessionEvent), e.addEventListener("end", this._onSessionEnd), e.addEventListener("inputsourceschange", this._onInputSourcesChange), await s.makeXRCompatible(), this._currentPixelRatio = t.getPixelRatio(), t.getSize(this._currentSize), this._currentAnimationContext = t._animation.getContext(), this._currentAnimationLoop = t._animation.getAnimationLoop(), t._animation.stop(), this._supportsLayers === !0) {
        let i = null, o = null, u = null;
        t.depth && (u = t.stencil ? r.DEPTH24_STENCIL8 : r.DEPTH_COMPONENT24, i = t.stencil ? kt : Gt, o = t.stencil ? Vt : _e);
        const l = {
          colorFormat: r.RGBA8,
          depthFormat: u,
          scaleFactor: this._framebufferScaleFactor,
          clearOnAccess: !1
        };
        this._useMultiviewIfPossible && t.hasFeature("OVR_multiview2") && (l.textureType = "texture-array", this._useMultiview = !0), this._glBinding = this.getBinding();
        const c = this._glBinding.createProjectionLayer(l), d = [c];
        this._glProjLayer = c, t.setPixelRatio(1), t._setXRLayerSize(c.textureWidth, c.textureHeight);
        const h = this._useMultiview ? 2 : 1, f = new vt(c.textureWidth, c.textureHeight, o, void 0, void 0, void 0, void 0, void 0, void 0, i, h);
        if (this._xrRenderTarget = new Mr(
          c.textureWidth,
          c.textureHeight,
          {
            format: ct,
            type: $e,
            colorSpace: t.outputColorSpace,
            depthTexture: f,
            stencilBuffer: t.stencil,
            samples: n.antialias ? 4 : 0,
            resolveDepthBuffer: c.ignoreDepthValues === !1,
            resolveStencilBuffer: c.ignoreDepthValues === !1,
            depth: this._useMultiview ? 2 : 1,
            multiview: this._useMultiview
          }
        ), this._xrRenderTarget._hasExternalTextures = !0, this._xrRenderTarget.depth = this._useMultiview ? 2 : 1, this._sessionUsesLayers = e.enabledFeatures.includes("layers"), this._referenceSpace = await e.requestReferenceSpace(this.getReferenceSpaceType()), this._sessionUsesLayers)
          for (const p of this._layers)
            p.plane.material = new gs({ color: 16777215, side: p.type === "cylinder" ? Ge : Ns }), p.plane.material.blending = Zs, p.plane.material.blendEquation = Pt, p.plane.material.blendSrc = jt, p.plane.material.blendDst = jt, p.xrlayer = this._createXRLayer(p), d.unshift(p.xrlayer);
        e.updateRenderState({ layers: d });
      } else {
        const i = {
          antialias: t.currentSamples > 0,
          alpha: !0,
          depth: t.depth,
          stencil: t.stencil,
          framebufferScaleFactor: this.getFramebufferScaleFactor()
        }, o = new XRWebGLLayer(e, r, i);
        this._glBaseLayer = o, e.updateRenderState({ baseLayer: o }), t.setPixelRatio(1), t._setXRLayerSize(o.framebufferWidth, o.framebufferHeight), this._xrRenderTarget = new Mr(
          o.framebufferWidth,
          o.framebufferHeight,
          {
            format: ct,
            type: $e,
            colorSpace: t.outputColorSpace,
            stencilBuffer: t.stencil,
            resolveDepthBuffer: o.ignoreDepthValues === !1,
            resolveStencilBuffer: o.ignoreDepthValues === !1
          }
        ), this._xrRenderTarget._isOpaqueFramebuffer = !0, this._referenceSpace = await e.requestReferenceSpace(this.getReferenceSpaceType());
      }
      this.setFoveation(this.getFoveation()), t._animation.setAnimationLoop(this._onAnimationFrame), t._animation.setContext(e), t._animation.start(), this.isPresenting = !0, this.dispatchEvent({ type: "sessionstart" });
    }
  }
  /**
   * This method is called by the renderer per frame and updates the XR camera
   * and it sub cameras based on the given camera. The given camera is the "user"
   * camera created on application level and used for non-XR rendering.
   *
   * @param {PerspectiveCamera} camera - The camera.
   */
  updateCamera(e) {
    const t = this._session;
    if (t === null) return;
    const s = e.near, r = e.far, n = this._cameraXR, i = this._cameraL, o = this._cameraR;
    n.near = o.near = i.near = s, n.far = o.far = i.far = r, n.isMultiViewCamera = this._useMultiview, (this._currentDepthNear !== n.near || this._currentDepthFar !== n.far) && (t.updateRenderState({
      depthNear: n.near,
      depthFar: n.far
    }), this._currentDepthNear = n.near, this._currentDepthFar = n.far), n.layers.mask = e.layers.mask | 6, i.layers.mask = n.layers.mask & -5, o.layers.mask = n.layers.mask & -3;
    const u = e.parent, l = n.cameras;
    ll(n, u);
    for (let c = 0; c < l.length; c++)
      ll(l[c], u);
    l.length === 2 ? yS(n, i, o) : n.projectionMatrix.copy(i.projectionMatrix), _S(e, n, u);
  }
  /**
   * Returns a WebXR controller for the given controller index.
   *
   * @private
   * @param {number} index - The controller index.
   * @return {WebXRController} The XR controller.
   */
  _getController(e) {
    let t = this._controllers[e];
    return t === void 0 && (t = new Gh(), this._controllers[e] = t), t;
  }
}
function yS(a, e, t) {
  ol.setFromMatrixPosition(e.matrixWorld), al.setFromMatrixPosition(t.matrixWorld);
  const s = ol.distanceTo(al), r = e.projectionMatrix.elements, n = t.projectionMatrix.elements, i = r[14] / (r[10] - 1), o = r[14] / (r[10] + 1), u = (r[9] + 1) / r[5], l = (r[9] - 1) / r[5], c = (r[8] - 1) / r[0], d = (n[8] + 1) / n[0], h = i * c, f = i * d, p = s / (-c + d), g = p * -c;
  if (e.matrixWorld.decompose(a.position, a.quaternion, a.scale), a.translateX(g), a.translateZ(p), a.matrixWorld.compose(a.position, a.quaternion, a.scale), a.matrixWorldInverse.copy(a.matrixWorld).invert(), r[10] === -1)
    a.projectionMatrix.copy(e.projectionMatrix), a.projectionMatrixInverse.copy(e.projectionMatrixInverse);
  else {
    const m = i + p, y = o + p, _ = h - g, v = f + (s - g), C = u * o / y * m, w = l * o / y * m;
    a.projectionMatrix.makePerspective(_, v, C, w, m, y), a.projectionMatrixInverse.copy(a.projectionMatrix).invert();
  }
}
function ll(a, e) {
  e === null ? a.matrixWorld.copy(a.matrix) : a.matrixWorld.multiplyMatrices(e.matrixWorld, a.matrix), a.matrixWorldInverse.copy(a.matrixWorld).invert();
}
function _S(a, e, t) {
  t === null ? a.matrix.copy(e.matrixWorld) : (a.matrix.copy(t.matrixWorld), a.matrix.invert(), a.matrix.multiply(e.matrixWorld)), a.matrix.decompose(a.position, a.quaternion, a.scale), a.updateMatrixWorld(!0), a.projectionMatrix.copy(e.projectionMatrix), a.projectionMatrixInverse.copy(e.projectionMatrixInverse), a.isPerspectiveCamera && (a.fov = of * 2 * Math.atan(1 / a.projectionMatrix.elements[5]), a.zoom = 1);
}
function bS(a) {
  const e = this._controllerInputSources.indexOf(a.inputSource);
  if (e === -1)
    return;
  const t = this._controllers[e];
  if (t !== void 0) {
    const s = this.getReferenceSpace();
    t.update(a.inputSource, a.frame, s), t.dispatchEvent({ type: a.type, data: a.inputSource });
  }
}
function SS() {
  const a = this._session, e = this._renderer;
  a.removeEventListener("select", this._onSessionEvent), a.removeEventListener("selectstart", this._onSessionEvent), a.removeEventListener("selectend", this._onSessionEvent), a.removeEventListener("squeeze", this._onSessionEvent), a.removeEventListener("squeezestart", this._onSessionEvent), a.removeEventListener("squeezeend", this._onSessionEvent), a.removeEventListener("end", this._onSessionEnd), a.removeEventListener("inputsourceschange", this._onInputSourcesChange);
  for (let t = 0; t < this._controllers.length; t++) {
    const s = this._controllerInputSources[t];
    s !== null && (this._controllerInputSources[t] = null, this._controllers[t].disconnect(s));
  }
  if (this._currentDepthNear = null, this._currentDepthFar = null, e._resetXRState(), this._session = null, this._xrRenderTarget = null, this._glBinding = null, this._glBaseLayer = null, this._glProjLayer = null, this._sessionUsesLayers === !0)
    for (const t of this._layers)
      t.renderTarget = new Mr(
        t.pixelwidth,
        t.pixelheight,
        {
          format: ct,
          type: $e,
          depthTexture: new vt(
            t.pixelwidth,
            t.pixelheight,
            t.stencilBuffer ? Vt : _e,
            void 0,
            void 0,
            void 0,
            void 0,
            void 0,
            void 0,
            t.stencilBuffer ? kt : Gt
          ),
          stencilBuffer: t.stencilBuffer,
          resolveDepthBuffer: !1,
          resolveStencilBuffer: !1
        }
      ), t.renderTarget.isXRRenderTarget = !1, t.plane.material = t.material, t.material.map = t.renderTarget.texture, t.material.map.offset.y = 1, t.material.map.repeat.y = -1, delete t.xrlayer;
  this.isPresenting = !1, this._useMultiview = !1, e._animation.stop(), e._animation.setAnimationLoop(this._currentAnimationLoop), e._animation.setContext(this._currentAnimationContext), e._animation.start(), e.setPixelRatio(this._currentPixelRatio), e.setSize(this._currentSize.width, this._currentSize.height, !1), this.dispatchEvent({ type: "sessionend" });
}
function NS(a) {
  const e = this._controllers, t = this._controllerInputSources;
  for (let s = 0; s < a.removed.length; s++) {
    const r = a.removed[s], n = t.indexOf(r);
    n >= 0 && (t[n] = null, e[n].disconnect(r));
  }
  for (let s = 0; s < a.added.length; s++) {
    const r = a.added[s];
    let n = t.indexOf(r);
    if (n === -1) {
      for (let o = 0; o < e.length; o++)
        if (o >= t.length) {
          t.push(r), n = o;
          break;
        } else if (t[o] === null) {
          t[o] = r, n = o;
          break;
        }
      if (n === -1) break;
    }
    const i = e[n];
    i && i.connect(r);
  }
}
function RS(a) {
  return a.type === "quad" ? this._glBinding.createQuadLayer({
    transform: new XRRigidTransform(a.translation, a.quaternion),
    width: a.width / 2,
    height: a.height / 2,
    space: this._referenceSpace,
    viewPixelWidth: a.pixelwidth,
    viewPixelHeight: a.pixelheight,
    clearOnAccess: !1
  }) : this._glBinding.createCylinderLayer({
    transform: new XRRigidTransform(a.translation, a.quaternion),
    radius: a.radius,
    centralAngle: a.centralAngle,
    aspectRatio: a.aspectRatio,
    space: this._referenceSpace,
    viewPixelWidth: a.pixelwidth,
    viewPixelHeight: a.pixelheight,
    clearOnAccess: !1
  });
}
function AS(a, e) {
  if (e === void 0) return;
  const t = this._cameraXR, s = this._renderer, r = s.backend, n = this._glBaseLayer, i = this.getReferenceSpace(), o = e.getViewerPose(i);
  if (this._xrFrame = e, o !== null) {
    const u = o.views;
    this._glBaseLayer !== null && r.setXRTarget(n.framebuffer);
    let l = !1;
    u.length !== t.cameras.length && (t.cameras.length = 0, l = !0);
    for (let c = 0; c < u.length; c++) {
      const d = u[c];
      let h;
      if (this._supportsLayers === !0) {
        const p = this._glBinding.getViewSubImage(this._glProjLayer, d);
        h = p.viewport, c === 0 && r.setXRRenderTargetTextures(
          this._xrRenderTarget,
          p.colorTexture,
          this._glProjLayer.ignoreDepthValues && !this._useMultiview ? void 0 : p.depthStencilTexture
        );
      } else
        h = n.getViewport(d);
      let f = this._cameras[c];
      f === void 0 && (f = new Pn(), f.layers.enable(c), f.viewport = new Se(), this._cameras[c] = f), f.matrix.fromArray(d.transform.matrix), f.matrix.decompose(f.position, f.quaternion, f.scale), f.projectionMatrix.fromArray(d.projectionMatrix), f.projectionMatrixInverse.copy(f.projectionMatrix).invert(), f.viewport.set(h.x, h.y, h.width, h.height), c === 0 && (t.matrix.copy(f.matrix), t.matrix.decompose(t.position, t.quaternion, t.scale)), l === !0 && t.cameras.push(f);
    }
    s.setOutputRenderTarget(this._xrRenderTarget);
  }
  for (let u = 0; u < this._controllers.length; u++) {
    const l = this._controllerInputSources[u], c = this._controllers[u];
    l !== null && c !== void 0 && c.update(l, e, i);
  }
  this._currentAnimationLoop && this._currentAnimationLoop(a, e), e.detectedPlanes && this.dispatchEvent({ type: "planesdetected", data: e }), this._xrFrame = null;
}
class vS extends ni {
  /**
   * Constructs a new CanvasTarget.
   *
   * @param {HTMLCanvasElement|OffscreenCanvas} domElement - The canvas element to render to.
   */
  constructor(e) {
    super(), this.domElement = e, this._pixelRatio = 1, this._width = this.domElement.width, this._height = this.domElement.height, this._viewport = new Se(0, 0, this._width, this._height), this._scissor = new Se(0, 0, this._width, this._height), this._scissorTest = !1, this.colorTexture = new Gl(), this.depthTexture = new vt();
  }
  /**
   * Returns the pixel ratio.
   *
   * @return {number} The pixel ratio.
   */
  getPixelRatio() {
    return this._pixelRatio;
  }
  /**
   * Returns the drawing buffer size in physical pixels. This method honors the pixel ratio.
   *
   * @param {Vector2} target - The method writes the result in this target object.
   * @return {Vector2} The drawing buffer size.
   */
  getDrawingBufferSize(e) {
    return e.set(this._width * this._pixelRatio, this._height * this._pixelRatio).floor();
  }
  /**
   * Returns the renderer's size in logical pixels. This method does not honor the pixel ratio.
   *
   * @param {Vector2} target - The method writes the result in this target object.
   * @return {Vector2} The renderer's size in logical pixels.
   */
  getSize(e) {
    return e.set(this._width, this._height);
  }
  /**
   * Sets the given pixel ratio and resizes the canvas if necessary.
   *
   * @param {number} [value=1] - The pixel ratio.
   */
  setPixelRatio(e = 1) {
    this._pixelRatio !== e && (this._pixelRatio = e, this.setSize(this._width, this._height, !1));
  }
  /**
   * This method allows to define the drawing buffer size by specifying
   * width, height and pixel ratio all at once. The size of the drawing
   * buffer is computed with this formula:
   * ```js
   * size.x = width * pixelRatio;
   * size.y = height * pixelRatio;
   * ```
   *
   * @param {number} width - The width in logical pixels.
   * @param {number} height - The height in logical pixels.
   * @param {number} pixelRatio - The pixel ratio.
   */
  setDrawingBufferSize(e, t, s) {
    this.xr && this.xr.isPresenting || (this._width = e, this._height = t, this._pixelRatio = s, this.domElement.width = Math.floor(e * s), this.domElement.height = Math.floor(t * s), this.setViewport(0, 0, e, t), this._dispatchResize());
  }
  /**
   * Sets the size of the renderer.
   *
   * @param {number} width - The width in logical pixels.
   * @param {number} height - The height in logical pixels.
   * @param {boolean} [updateStyle=true] - Whether to update the `style` attribute of the canvas or not.
   */
  setSize(e, t, s = !0) {
    this.xr && this.xr.isPresenting || (this._width = e, this._height = t, this.domElement.width = Math.floor(e * this._pixelRatio), this.domElement.height = Math.floor(t * this._pixelRatio), s === !0 && (this.domElement.style.width = e + "px", this.domElement.style.height = t + "px"), this.setViewport(0, 0, e, t), this._dispatchResize());
  }
  /**
   * Returns the scissor rectangle.
   *
   * @param {Vector4} target - The method writes the result in this target object.
   * @return {Vector4} The scissor rectangle.
   */
  getScissor(e) {
    const t = this._scissor;
    return e.x = t.x, e.y = t.y, e.width = t.width, e.height = t.height, e;
  }
  /**
   * Defines the scissor rectangle.
   *
   * @param {number | Vector4} x - The horizontal coordinate for the lower left corner of the box in logical pixel unit.
   * Instead of passing four arguments, the method also works with a single four-dimensional vector.
   * @param {number} y - The vertical coordinate for the lower left corner of the box in logical pixel unit.
   * @param {number} width - The width of the scissor box in logical pixel unit.
   * @param {number} height - The height of the scissor box in logical pixel unit.
   */
  setScissor(e, t, s, r) {
    const n = this._scissor;
    e.isVector4 ? n.copy(e) : n.set(e, t, s, r);
  }
  /**
   * Returns the scissor test value.
   *
   * @return {boolean} Whether the scissor test should be enabled or not.
   */
  getScissorTest() {
    return this._scissorTest;
  }
  /**
   * Defines the scissor test.
   *
   * @param {boolean} boolean - Whether the scissor test should be enabled or not.
   */
  setScissorTest(e) {
    this._scissorTest = e;
  }
  /**
   * Returns the viewport definition.
   *
   * @param {Vector4} target - The method writes the result in this target object.
   * @return {Vector4} The viewport definition.
   */
  getViewport(e) {
    return e.copy(this._viewport);
  }
  /**
   * Defines the viewport.
   *
   * @param {number | Vector4} x - The horizontal coordinate for the lower left corner of the viewport origin in logical pixel unit.
   * @param {number} y - The vertical coordinate for the lower left corner of the viewport origin  in logical pixel unit.
   * @param {number} width - The width of the viewport in logical pixel unit.
   * @param {number} height - The height of the viewport in logical pixel unit.
   * @param {number} minDepth - The minimum depth value of the viewport. WebGPU only.
   * @param {number} maxDepth - The maximum depth value of the viewport. WebGPU only.
   */
  setViewport(e, t, s, r, n = 0, i = 1) {
    const o = this._viewport;
    e.isVector4 ? o.copy(e) : o.set(e, t, s, r), o.minDepth = n, o.maxDepth = i;
  }
  /**
   * Dispatches the resize event.
   *
   * @private
   */
  _dispatchResize() {
    this.dispatchEvent({ type: "resize" });
  }
  /**
   * Frees the GPU-related resources allocated by this instance. Call this
   * method whenever this instance is no longer used in your app.
   *
   * @fires RenderTarget#dispose
   */
  dispose() {
    this.dispatchEvent({ type: "dispose" });
  }
}
const cl = /* @__PURE__ */ new Ul(), $s = /* @__PURE__ */ new je(), ro = /* @__PURE__ */ new Se(), no = /* @__PURE__ */ new Sh(), io = /* @__PURE__ */ new bh(), _n = /* @__PURE__ */ new dt(), Ut = /* @__PURE__ */ new Se(), ES = { [Ns]: Ge, [Ge]: Ns, [Ot]: Ot };
class CS {
  /**
   * Renderer options.
   *
   * @typedef {Object} Renderer~Options
   * @property {boolean} [logarithmicDepthBuffer=false] - Whether logarithmic depth buffer is enabled or not.
   * @property {boolean} [reversedDepthBuffer=false] - Whether reversed depth buffer is enabled or not.
   * @property {boolean} [alpha=true] - Whether the default framebuffer (which represents the final contents of the canvas) should be transparent or opaque.
   * @property {boolean} [depth=true] - Whether the default framebuffer should have a depth buffer or not.
   * @property {boolean} [stencil=false] - Whether the default framebuffer should have a stencil buffer or not.
   * @property {boolean} [antialias=false] - Whether MSAA as the default anti-aliasing should be enabled or not.
   * @property {number} [samples=0] - When `antialias` is `true`, `4` samples are used by default. This parameter can set to any other integer value than 0
   * to overwrite the default.
   * @property {?Function} [getFallback=null] - This callback function can be used to provide a fallback backend, if the primary backend can't be targeted.
   * @property {number} [outputBufferType=HalfFloatType] - Defines the type of output buffers. The default `HalfFloatType` is recommend for best
   * quality. To save memory and bandwidth, `UnsignedByteType` might be used. This will reduce rendering quality though.
   * @property {boolean} [multiview=false] - If set to `true`, the renderer will use multiview during WebXR rendering if supported.
   */
  /**
  	 * Constructs a new renderer.
  	 *
  	 * @param {Backend} backend - The backend the renderer is targeting (e.g. WebGPU or WebGL 2).
  	 * @param {Renderer~Options} [parameters] - The configuration parameter.
  
  	 */
  constructor(e, t = {}) {
    this.isRenderer = !0;
    const {
      logarithmicDepthBuffer: s = !1,
      reversedDepthBuffer: r = !1,
      alpha: n = !0,
      depth: i = !0,
      stencil: o = !1,
      antialias: u = !1,
      samples: l = 0,
      getFallback: c = null,
      outputBufferType: d = Oe,
      multiview: h = !1
    } = t;
    this.backend = e, this.autoClear = !0, this.autoClearColor = !0, this.autoClearDepth = !0, this.autoClearStencil = !0, this.alpha = n, this.logarithmicDepthBuffer = s, this.reversedDepthBuffer = r, this.outputColorSpace = Bl, this.toneMapping = bs, this.toneMappingExposure = 1, this.sortObjects = !0, this.depth = i, this.stencil = o, this.info = new o_(), this.contextNode = Ls(), this.library = new lh(), this.lighting = new xS(), this._samples = l || u === !0 ? 4 : 0, this._onCanvasTargetResize = this._onCanvasTargetResize.bind(this), this._canvasTarget = new vS(e.getDomElement()), this._canvasTarget.addEventListener("resize", this._onCanvasTargetResize), this._canvasTarget.isDefaultCanvasTarget = !0, this._inspector = new sd(), this._inspector.setRenderer(this), this._getFallback = c, this._attributes = null, this._geometries = null, this._nodes = null, this._animation = null, this._bindings = null, this._objects = null, this._pipelines = null, this._bundles = null, this._renderLists = null, this._renderContexts = null, this._textures = null, this._background = null, this._quadCache = /* @__PURE__ */ new Map(), this._currentRenderContext = null, this._opaqueSort = null, this._transparentSort = null, this._frameBufferTargets = /* @__PURE__ */ new Map();
    const f = this.alpha === !0 ? 0 : 1;
    this._clearColor = new Pa(0, 0, 0, f), this._clearDepth = 1, this._clearStencil = 0, this._renderTarget = null, this._activeCubeFace = 0, this._activeMipmapLevel = 0, this._outputRenderTarget = null, this._mrt = null, this._renderObjectFunction = null, this._currentRenderObjectFunction = null, this._currentRenderBundle = null, this._handleObjectFunction = this._renderObjectDirect, this._isDeviceLost = !1, this.onDeviceLost = this._onDeviceLost, this._outputBufferType = d, this._cacheShadowNodes = /* @__PURE__ */ new WeakMap(), this._initialized = !1, this._callDepth = -1, this._initPromise = null, this._compilationPromises = null, this.transparent = !0, this.opaque = !0, this.shadowMap = {
      enabled: !1,
      transmitted: !1,
      type: Fl
    }, this.xr = new TS(this, h), this.debug = {
      checkShaderErrors: !0,
      onShaderError: null,
      getShaderAsync: async (p, g, m) => {
        await this.compileAsync(p, g);
        const y = this._renderLists.get(p, g), _ = this._renderContexts.get(this._renderTarget, this._mrt), v = p.overrideMaterial || m.material, C = this._objects.get(m, v, p, g, y.lightsNode, _, _.clippingContext), { fragmentShader: w, vertexShader: M } = C.getNodeBuilderState();
        return { fragmentShader: w, vertexShader: M };
      }
    };
  }
  /**
   * Initializes the renderer so it is ready for usage.
   *
   * @async
   * @return {Promise<this>} A Promise that resolves when the renderer has been initialized.
   */
  async init() {
    return this._initPromise !== null ? this._initPromise : (this._initPromise = new Promise(async (e, t) => {
      let s = this.backend;
      try {
        await s.init(this);
      } catch (r) {
        if (this._getFallback !== null)
          try {
            this.backend = s = this._getFallback(r), await s.init(this);
          } catch (n) {
            t(n);
            return;
          }
        else {
          t(r);
          return;
        }
      }
      this._nodes = new fS(this, s), this._animation = new jy(this, this._nodes, this.info), this._attributes = new n_(s, this.info), this._background = new Eb(this, this._nodes), this._geometries = new i_(this._attributes, this.info), this._textures = new y_(this, s, this.info), this._pipelines = new c_(s, this._nodes, this.info), this._bindings = new d_(s, this._nodes, this._textures, this._attributes, this._pipelines, this.info), this._objects = new t_(this, this._nodes, this._geometries, this._pipelines, this._bindings, this.info), this._renderLists = new p_(this.lighting), this._bundles = new gS(), this._renderContexts = new x_(this), this._animation.start(), this._initialized = !0, this._inspector.init(), e(this);
    }), this._initPromise);
  }
  /**
   * A reference to the canvas element the renderer is drawing to.
   * This value of this property will automatically be created by
   * the renderer.
   *
   * @type {HTMLCanvasElement|OffscreenCanvas}
   */
  get domElement() {
    return this._canvasTarget.domElement;
  }
  /**
   * The coordinate system of the renderer. The value of this property
   * depends on the selected backend. Either `THREE.WebGLCoordinateSystem` or
   * `THREE.WebGPUCoordinateSystem`.
   *
   * @readonly
   * @type {number}
   */
  get coordinateSystem() {
    return this.backend.coordinateSystem;
  }
  /**
   * Compiles all materials in the given scene. This can be useful to avoid a
   * phenomenon which is called "shader compilation stutter", which occurs when
   * rendering an object with a new shader for the first time.
   *
   * If you want to add a 3D object to an existing scene, use the third optional
   * parameter for applying the target scene. Note that the (target) scene's lighting
   * and environment must be configured before calling this method.
   *
   * @async
   * @param {Object3D} scene - The scene or 3D object to precompile.
   * @param {Camera} camera - The camera that is used to render the scene.
   * @param {?Scene} targetScene - If the first argument is a 3D object, this parameter must represent the scene the 3D object is going to be added.
   * @return {Promise} A Promise that resolves when the compile has been finished.
   */
  async compileAsync(e, t, s = null) {
    if (this._isDeviceLost === !0) return;
    this._initialized === !1 && await this.init();
    const r = this._nodes.nodeFrame, n = r.renderId, i = this._currentRenderContext, o = this._currentRenderObjectFunction, u = this._handleObjectFunction, l = this._compilationPromises;
    s === null && (s = e);
    const c = e.isScene === !0 ? e : s.isScene === !0 ? s : cl, h = this.needsFrameBufferTarget && this._renderTarget === null ? this._getFrameBufferTarget() : this._renderTarget || this._outputRenderTarget, f = this._renderContexts.get(h, this._mrt), p = this._activeMipmapLevel, g = [];
    this._currentRenderContext = f, this._currentRenderObjectFunction = this.renderObject, this._handleObjectFunction = this._createObjectPipeline, this._compilationPromises = g, r.renderId++, r.update(), f.depth = this.depth, f.stencil = this.stencil, f.clippingContext || (f.clippingContext = new jn()), f.clippingContext.updateGlobal(c, t), c.onBeforeRender(this, e, t, h);
    const m = this._renderLists.get(c, t);
    if (m.begin(), this._projectObject(e, t, 0, m, f.clippingContext), s !== e && s.traverseVisible(function(w) {
      w.isLight && w.layers.test(t.layers) && m.pushLight(w);
    }), m.finish(), h !== null) {
      this._textures.updateRenderTarget(h, p);
      const w = this._textures.get(h);
      f.textures = w.textures, f.depthTexture = w.depthTexture;
    } else
      f.textures = null, f.depthTexture = null;
    s !== e ? this._background.update(s, m, f) : this._background.update(c, m, f);
    const y = m.opaque, _ = m.transparent, v = m.transparentDoublePass, C = m.lightsNode;
    this.opaque === !0 && y.length > 0 && this._renderObjects(y, t, c, C), this.transparent === !0 && _.length > 0 && this._renderTransparents(_, v, t, c, C), r.renderId = n, this._currentRenderContext = i, this._currentRenderObjectFunction = o, this._handleObjectFunction = u, this._compilationPromises = l;
    for (const w of g) {
      const M = this._objects.get(w.object, w.material, w.scene, w.camera, w.lightsNode, w.renderContext, w.clippingContext, w.passId);
      M.drawRange = w.object.geometry.drawRange, M.group = w.group, this._geometries.updateForRender(M), await this._nodes.getForRenderAsync(M), this._nodes.updateBefore(M), this._nodes.updateForRender(M), this._bindings.updateForRender(M);
      const E = [];
      this._pipelines.getForRender(M, E), E.length > 0 && await Promise.all(E), this._nodes.updateAfter(M), await Dl();
    }
  }
  /**
   * Renders the scene in an async fashion.
   *
   * @async
   * @deprecated
   * @param {Object3D} scene - The scene or 3D object to render.
   * @param {Camera} camera - The camera.
   * @return {Promise} A Promise that resolves when the render has been finished.
   */
  async renderAsync(e, t) {
    Te('Renderer: "renderAsync()" has been deprecated. Use "render()" and "await renderer.init();" when creating the renderer.'), await this.init(), this.render(e, t);
  }
  /**
   * Can be used to synchronize CPU operations with GPU tasks. So when this method is called,
   * the CPU waits for the GPU to complete its operation (e.g. a compute task).
   *
   * @async
   * @deprecated
   * @return {Promise} A Promise that resolves when synchronization has been finished.
   */
  async waitForGPU() {
    F("Renderer: waitForGPU() has been removed. Read https://github.com/mrdoob/three.js/issues/32012 for more information.");
  }
  //
  set inspector(e) {
    this._inspector !== null && this._inspector.setRenderer(null), this._inspector = e, this._inspector.setRenderer(this);
  }
  /**
   * The inspector instance. The inspector can be any class that extends from `InspectorBase`.
   *
   * @type {InspectorBase}
   */
  get inspector() {
    return this._inspector;
  }
  /**
   * Enables or disables high precision for model-view and normal-view matrices.
   * When enabled, will use CPU 64-bit precision for higher precision instead of GPU 32-bit for higher performance.
   *
   * NOTE: 64-bit precision is not compatible with `InstancedMesh` and `SkinnedMesh`.
   *
   * @param {boolean} value - Whether to enable or disable high precision.
   * @type {boolean}
   */
  set highPrecision(e) {
    const t = this.contextNode.value;
    e === !0 ? (t.modelViewMatrix = cu, t.modelNormalViewMatrix = du) : this.highPrecision && (delete t.modelViewMatrix, delete t.modelNormalViewMatrix);
  }
  /**
   * Returns whether high precision is enabled or not.
   *
   * @return {boolean} Whether high precision is enabled or not.
   * @type {boolean}
   */
  get highPrecision() {
    const e = this.contextNode.value;
    return e.modelViewMatrix === cu && e.modelNormalViewMatrix === du;
  }
  /**
   * Sets the given MRT configuration.
   *
   * @param {MRTNode} mrt - The MRT node to set.
   * @return {Renderer} A reference to this renderer.
   */
  setMRT(e) {
    return this._mrt = e, this;
  }
  /**
   * Returns the MRT configuration.
   *
   * @return {MRTNode} The MRT configuration.
   */
  getMRT() {
    return this._mrt;
  }
  /**
   * Returns the output buffer type.
   *
   * @return {number} The output buffer type.
   */
  getOutputBufferType() {
    return this._outputBufferType;
  }
  /**
   * Returns the output buffer type.
   *
   * @deprecated since r182. Use `.getOutputBufferType()` instead.
   * @return {number} The output buffer type.
   */
  getColorBufferType() {
    return Te('Renderer: ".getColorBufferType()" has been renamed to ".getOutputBufferType()".'), this.getOutputBufferType();
  }
  /**
   * Default implementation of the device lost callback.
   *
   * @private
   * @param {Object} info - Information about the context lost.
   */
  _onDeviceLost(e) {
    let t = `THREE.WebGPURenderer: ${e.api} Device Lost:

Message: ${e.message}`;
    e.reason && (t += `
Reason: ${e.reason}`), F(t), this._isDeviceLost = !0;
  }
  /**
   * Renders the given render bundle.
   *
   * @private
   * @param {Object} bundle - Render bundle data.
   * @param {Scene} sceneRef - The scene the render bundle belongs to.
   * @param {LightsNode} lightsNode - The lights node.
   */
  _renderBundle(e, t, s) {
    const { bundleGroup: r, camera: n, renderList: i } = e, o = this._currentRenderContext, u = this._bundles.get(r, n, o), l = this.backend.get(u), c = r.version !== l.version;
    if (c || l.bundleGPU === void 0) {
      this.backend.beginBundle(o), (l.renderObjects === void 0 || c) && (l.renderObjects = []), this._currentRenderBundle = u;
      const {
        transparentDoublePass: h,
        transparent: f,
        opaque: p
      } = i;
      this.opaque === !0 && p.length > 0 && this._renderObjects(p, n, t, s), this.transparent === !0 && f.length > 0 && this._renderTransparents(f, h, n, t, s), this._currentRenderBundle = null, this.backend.finishBundle(o, u), l.version = r.version;
    } else {
      const { renderObjects: h } = l;
      for (let f = 0, p = h.length; f < p; f++) {
        const g = h[f];
        this._nodes.needsRefresh(g) && (this._nodes.updateBefore(g), this._nodes.updateForRender(g), this._bindings.updateForRender(g), this._nodes.updateAfter(g));
      }
    }
    this.backend.addBundle(o, u);
  }
  /**
   * Renders the scene or 3D object with the given camera. This method can only be called
   * if the renderer has been initialized. When using `render()` inside an animation loop,
   * it's guaranteed the renderer will be initialized. The animation loop must be defined
   * with {@link Renderer#setAnimationLoop} though.
   *
   * For all other use cases (like when using on-demand rendering), you must call
   * {@link Renderer#init} before rendering.
   *
   * The target of the method is the default framebuffer (meaning the canvas)
   * or alternatively a render target when specified via `setRenderTarget()`.
   *
   * @param {Object3D} scene - The scene or 3D object to render.
   * @param {Camera} camera - The camera to render the scene with.
   */
  render(e, t) {
    if (this._initialized === !1)
      throw new Error('Renderer: .render() called before the backend is initialized. Use "await renderer.init();" before rendering.');
    this._renderScene(e, t);
  }
  /**
   * Returns whether the renderer has been initialized or not.
   *
   * @readonly
   * @return {boolean} Whether the renderer has been initialized or not.
   */
  get initialized() {
    return this._initialized;
  }
  /**
   * Returns an internal render target which is used when computing the output tone mapping
   * and color space conversion. Unlike in `WebGLRenderer`, this is done in a separate render
   * pass and not inline to achieve more correct results.
   *
   * @private
   * @return {?RenderTarget} The render target. The method returns `null` if no output conversion should be applied.
   */
  _getFrameBufferTarget() {
    const { currentToneMapping: e, currentColorSpace: t } = this, s = e !== bs, r = t !== ye.workingColorSpace;
    if (s === !1 && r === !1) return null;
    const { width: n, height: i } = this.getDrawingBufferSize($s), { depth: o, stencil: u } = this, l = this._outputRenderTarget || this._canvasTarget;
    let c = this._frameBufferTargets.get(l);
    if (c === void 0) {
      c = new Ds(n, i, {
        depthBuffer: o,
        stencilBuffer: u,
        type: this._outputBufferType,
        format: ct,
        colorSpace: ye.workingColorSpace,
        generateMipmaps: !1,
        minFilter: Ze,
        magFilter: Ze,
        samples: this.samples
      }), c.isPostProcessingRenderTarget = !0;
      const m = () => {
        l.removeEventListener("dispose", m), c.dispose(), this._frameBufferTargets.delete(l);
      };
      l.addEventListener("dispose", m), this._frameBufferTargets.set(l, c);
    }
    const d = this.getOutputRenderTarget();
    c.depthBuffer = o, c.stencilBuffer = u, d !== null ? c.setSize(d.width, d.height, d.depth) : c.setSize(n, i, 1);
    const h = this._outputRenderTarget ? this._outputRenderTarget.viewport : l._viewport, f = this._outputRenderTarget ? this._outputRenderTarget.scissor : l._scissor, p = this._outputRenderTarget ? 1 : l._pixelRatio, g = this._outputRenderTarget ? this._outputRenderTarget.scissorTest : l._scissorTest;
    return c.viewport.copy(h), c.scissor.copy(f), c.viewport.multiplyScalar(p), c.scissor.multiplyScalar(p), c.scissorTest = g, c.multiview = d !== null ? d.multiview : !1, c.resolveDepthBuffer = d !== null ? d.resolveDepthBuffer : !0, c._autoAllocateDepthBuffer = d !== null ? d._autoAllocateDepthBuffer : !1, c;
  }
  /**
   * Renders the scene or 3D object with the given camera.
   *
   * @private
   * @param {Object3D} scene - The scene or 3D object to render.
   * @param {Camera} camera - The camera to render the scene with.
   * @param {boolean} [useFrameBufferTarget=true] - Whether to use a framebuffer target or not.
   * @return {RenderContext} The current render context.
   */
  _renderScene(e, t, s = !0) {
    if (this._isDeviceLost === !0) return;
    const r = s ? this._getFrameBufferTarget() : null, n = this._nodes.nodeFrame, i = n.renderId, o = this._currentRenderContext, u = this._currentRenderObjectFunction, l = this._handleObjectFunction;
    this._callDepth++;
    const c = e.isScene === !0 ? e : cl, d = this._renderTarget || this._outputRenderTarget, h = this._activeCubeFace, f = this._activeMipmapLevel;
    let p;
    if (r !== null ? (p = r, this.setRenderTarget(p)) : p = d, p !== null && p.depthBuffer === !0) {
      const oe = this._textures.get(p);
      oe.depthInitialized !== !0 && ((this.autoClear === !1 || this.autoClear === !0 && this.autoClearDepth === !1) && this.clearDepth(), oe.depthInitialized = !0);
    }
    const g = this._renderContexts.get(p, this._mrt, this._callDepth);
    this._currentRenderContext = g, this._currentRenderObjectFunction = this._renderObjectFunction || this.renderObject, this._handleObjectFunction = this._renderObjectDirect, this.info.calls++, this.info.render.calls++, this.info.render.frameCalls++, n.renderId = this.info.calls, this.backend.updateTimeStampUID(g), this.inspector.beginRender(this.backend.getTimestampUID(g), e, t, p);
    const m = this.xr;
    if (m.isPresenting === !1) {
      let oe = !1;
      if (this.reversedDepthBuffer === !0 && t.reversedDepth !== !0) {
        if (t._reversedDepth = !0, t.isArrayCamera)
          for (const ae of t.cameras)
            ae._reversedDepth = !0;
        oe = !0;
      }
      const K = this.coordinateSystem;
      if (t.coordinateSystem !== K) {
        if (t.coordinateSystem = K, t.isArrayCamera)
          for (const ae of t.cameras)
            ae.coordinateSystem = K;
        oe = !0;
      }
      if (oe === !0 && (t.updateProjectionMatrix(), t.isArrayCamera))
        for (const ae of t.cameras)
          ae.updateProjectionMatrix();
    }
    e.matrixWorldAutoUpdate === !0 && e.updateMatrixWorld(), t.parent === null && t.matrixWorldAutoUpdate === !0 && t.updateMatrixWorld(), m.enabled === !0 && m.isPresenting === !0 && (m.cameraAutoUpdate === !0 && m.updateCamera(t), t = m.getCamera());
    const y = this._canvasTarget;
    let _ = y._viewport, v = y._scissor, C = y._pixelRatio;
    p !== null && (_ = p.viewport, v = p.scissor, C = 1), this.getDrawingBufferSize($s), ro.set(0, 0, $s.width, $s.height);
    const w = _.minDepth === void 0 ? 0 : _.minDepth, M = _.maxDepth === void 0 ? 1 : _.maxDepth;
    g.viewportValue.copy(_).multiplyScalar(C).floor(), g.viewportValue.width >>= f, g.viewportValue.height >>= f, g.viewportValue.minDepth = w, g.viewportValue.maxDepth = M, g.viewport = g.viewportValue.equals(ro) === !1, g.scissorValue.copy(v).multiplyScalar(C).floor(), g.scissor = y._scissorTest && g.scissorValue.equals(ro) === !1, g.scissorValue.width >>= f, g.scissorValue.height >>= f, g.clippingContext || (g.clippingContext = new jn()), g.clippingContext.updateGlobal(c, t), c.onBeforeRender(this, e, t, p);
    const E = t.isArrayCamera ? io : no;
    t.isArrayCamera || (_n.multiplyMatrices(t.projectionMatrix, t.matrixWorldInverse), E.setFromProjectionMatrix(_n, t.coordinateSystem, t.reversedDepth));
    const U = this._renderLists.get(e, t);
    if (U.begin(), this._projectObject(e, t, 0, U, g.clippingContext), U.finish(), this.sortObjects === !0 && U.sort(this._opaqueSort, this._transparentSort), p !== null) {
      this._textures.updateRenderTarget(p, f);
      const oe = this._textures.get(p);
      g.textures = oe.textures, g.depthTexture = oe.depthTexture, g.width = oe.width, g.height = oe.height, g.renderTarget = p, g.depth = p.depthBuffer, g.stencil = p.stencilBuffer;
    } else
      g.textures = null, g.depthTexture = null, g.width = $s.width, g.height = $s.height, g.depth = this.depth, g.stencil = this.stencil;
    g.width >>= f, g.height >>= f, g.activeCubeFace = h, g.activeMipmapLevel = f, g.occlusionQueryCount = U.occlusionQueryCount, g.scissorValue.max(Ut.set(0, 0, 0, 0)), g.scissorValue.x + g.scissorValue.width > g.width && (g.scissorValue.width = Math.max(g.width - g.scissorValue.x, 0)), g.scissorValue.y + g.scissorValue.height > g.height && (g.scissorValue.height = Math.max(g.height - g.scissorValue.y, 0)), this._background.update(c, U, g), g.camera = t, this.backend.beginRender(g);
    const {
      bundles: P,
      lightsNode: $,
      transparentDoublePass: Y,
      transparent: ie,
      opaque: se
    } = U;
    return P.length > 0 && this._renderBundles(P, c, $), this.opaque === !0 && se.length > 0 && this._renderObjects(se, t, c, $), this.transparent === !0 && ie.length > 0 && this._renderTransparents(ie, Y, t, c, $), this.backend.finishRender(g), n.renderId = i, this._currentRenderContext = o, this._currentRenderObjectFunction = u, this._handleObjectFunction = l, this._callDepth--, r !== null && (this.setRenderTarget(d, h, f), this._renderOutput(p)), c.onAfterRender(this, e, t, p), this.inspector.finishRender(this.backend.getTimestampUID(g)), g;
  }
  _setXRLayerSize(e, t) {
    this._canvasTarget._width = e, this._canvasTarget._height = t, this.setViewport(0, 0, e, t);
  }
  /**
   * The output pass performs tone mapping and color space conversion.
   *
   * @private
   * @param {RenderTarget} renderTarget - The current render target.
   */
  _renderOutput(e) {
    const t = this._nodes.getOutputCacheKey();
    let s = this._quadCache.get(e.texture), r;
    if (s === void 0) {
      r = new jd(new Ne()), r.name = "Output Color Transform", r.material.name = "outputColorTransform", r.material.fragmentNode = this._nodes.getOutputNode(e.texture), s = {
        quad: r,
        cacheKey: t
      }, this._quadCache.set(e.texture, s);
      const o = () => {
        r.material.dispose(), this._quadCache.delete(e.texture), e.texture.removeEventListener("dispose", o);
      };
      e.texture.addEventListener("dispose", o);
    } else
      r = s.quad, s.cacheKey !== t && (r.material.fragmentNode = this._nodes.getOutputNode(e.texture), r.material.needsUpdate = !0, s.cacheKey = t);
    const n = this.autoClear, i = this.xr.enabled;
    this.autoClear = !1, this.xr.enabled = !1, this._renderScene(r, r.camera, !1), this.autoClear = n, this.xr.enabled = i;
  }
  /**
   * Returns the maximum available anisotropy for texture filtering.
   *
   * @return {number} The maximum available anisotropy.
   */
  getMaxAnisotropy() {
    return this.backend.capabilities.getMaxAnisotropy();
  }
  /**
   * Returns the active cube face.
   *
   * @return {number} The active cube face.
   */
  getActiveCubeFace() {
    return this._activeCubeFace;
  }
  /**
   * Returns the active mipmap level.
   *
   * @return {number} The active mipmap level.
   */
  getActiveMipmapLevel() {
    return this._activeMipmapLevel;
  }
  /**
   * Applications are advised to always define the animation loop
   * with this method and not manually with `requestAnimationFrame()`
   * for best compatibility.
   *
   * @async
   * @param {?onAnimationCallback} callback - The application's animation loop.
   * @return {Promise} A Promise that resolves when the set has been executed.
   */
  async setAnimationLoop(e) {
    this._initialized === !1 && await this.init(), this._animation.setAnimationLoop(e);
  }
  /**
   * Returns the current animation loop callback.
   *
   * @return {?Function} The current animation loop callback.
   */
  getAnimationLoop() {
    return this._animation.getAnimationLoop();
  }
  /**
   * Can be used to transfer buffer data from a storage buffer attribute
   * from the GPU to the CPU in context of compute shaders.
   *
   * @async
   * @param {BufferAttribute} attribute - The storage buffer attribute to read frm.
   * @param {ReadbackBuffer|ArrayBuffer} target - The storage buffer attribute.
   * @param {number} offset - The storage buffer attribute.
   * @param {number} count - The offset from which to start reading the
   * @return {Promise<ArrayBuffer|ReadbackBuffer>} A promise that resolves with the buffer data when the data are ready.
   */
  async getArrayBufferAsync(e, t = null, s = 0, r = -1) {
    if (t !== null && t.isReadbackBuffer && this.info.memoryMap.has(t) === !1) {
      this.info.createReadbackBuffer(t);
      const n = () => {
        t.removeEventListener("dispose", n), this.info.destroyReadbackBuffer(t);
      };
      t.addEventListener("dispose", n);
    }
    if (s % 4 !== 0 || r > 0 && r % 4 !== 0)
      throw new Error('THREE.Renderer: "getArrayBufferAsync()" offset and count must be a multiple of 4.');
    return await this.backend.getArrayBufferAsync(e, t, s, r);
  }
  /**
   * Returns the rendering context.
   *
   * @return {GPUCanvasContext|WebGL2RenderingContext} The rendering context.
   */
  getContext() {
    return this.backend.getContext();
  }
  /**
   * Returns the pixel ratio.
   *
   * @return {number} The pixel ratio.
   */
  getPixelRatio() {
    return this._canvasTarget.getPixelRatio();
  }
  /**
   * Returns the drawing buffer size in physical pixels. This method honors the pixel ratio.
   *
   * @param {Vector2} target - The method writes the result in this target object.
   * @return {Vector2} The drawing buffer size.
   */
  getDrawingBufferSize(e) {
    return this._canvasTarget.getDrawingBufferSize(e);
  }
  /**
   * Returns the renderer's size in logical pixels. This method does not honor the pixel ratio.
   *
   * @param {Vector2} target - The method writes the result in this target object.
   * @return {Vector2} The renderer's size in logical pixels.
   */
  getSize(e) {
    return this._canvasTarget.getSize(e);
  }
  /**
   * Sets the given pixel ratio and resizes the canvas if necessary.
   *
   * @param {number} [value=1] - The pixel ratio.
   */
  setPixelRatio(e = 1) {
    this._canvasTarget.setPixelRatio(e);
  }
  /**
   * This method allows to define the drawing buffer size by specifying
   * width, height and pixel ratio all at once. The size of the drawing
   * buffer is computed with this formula:
   * ```js
   * size.x = width * pixelRatio;
   * size.y = height * pixelRatio;
   * ```
   *
   * @param {number} width - The width in logical pixels.
   * @param {number} height - The height in logical pixels.
   * @param {number} pixelRatio - The pixel ratio.
   */
  setDrawingBufferSize(e, t, s) {
    this.xr && this.xr.isPresenting || this._canvasTarget.setDrawingBufferSize(e, t, s);
  }
  /**
   * Sets the size of the renderer.
   *
   * @param {number} width - The width in logical pixels.
   * @param {number} height - The height in logical pixels.
   * @param {boolean} [updateStyle=true] - Whether to update the `style` attribute of the canvas or not.
   */
  setSize(e, t, s = !0) {
    this.xr && this.xr.isPresenting || this._canvasTarget.setSize(e, t, s);
  }
  /**
   * Defines a manual sort function for the opaque render list.
   * Pass `null` to use the default sort.
   *
   * @param {Function} method - The sort function.
   */
  setOpaqueSort(e) {
    this._opaqueSort = e;
  }
  /**
   * Defines a manual sort function for the transparent render list.
   * Pass `null` to use the default sort.
   *
   * @param {Function} method - The sort function.
   */
  setTransparentSort(e) {
    this._transparentSort = e;
  }
  /**
   * Returns the scissor rectangle.
   *
   * @param {Vector4} target - The method writes the result in this target object.
   * @return {Vector4} The scissor rectangle.
   */
  getScissor(e) {
    return this._canvasTarget.getScissor(e);
  }
  /**
   * Defines the scissor rectangle.
   *
   * @param {number | Vector4} x - The horizontal coordinate for the upper left corner of the box in logical pixel unit.
   * Instead of passing four arguments, the method also works with a single four-dimensional vector.
   * @param {number} y - The vertical coordinate for the upper left corner of the box in logical pixel unit.
   * @param {number} width - The width of the scissor box in logical pixel unit.
   * @param {number} height - The height of the scissor box in logical pixel unit.
   */
  setScissor(e, t, s, r) {
    this._canvasTarget.setScissor(e, t, s, r);
  }
  /**
   * Returns the scissor test value.
   *
   * @return {boolean} Whether the scissor test should be enabled or not.
   */
  getScissorTest() {
    return this._canvasTarget.getScissorTest();
  }
  /**
   * Defines the scissor test.
   *
   * @param {boolean} boolean - Whether the scissor test should be enabled or not.
   */
  setScissorTest(e) {
    this._canvasTarget.setScissorTest(e), this.backend.setScissorTest(e);
  }
  /**
   * Returns the viewport definition.
   *
   * @param {Vector4} target - The method writes the result in this target object.
   * @return {Vector4} The viewport definition.
   */
  getViewport(e) {
    return this._canvasTarget.getViewport(e);
  }
  /**
   * Defines the viewport.
   *
   * @param {number | Vector4} x - The horizontal coordinate for the upper left corner of the viewport origin in logical pixel unit.
   * @param {number} y - The vertical coordinate for the upper left corner of the viewport origin in logical pixel unit.
   * @param {number} width - The width of the viewport in logical pixel unit.
   * @param {number} height - The height of the viewport in logical pixel unit.
   * @param {number} minDepth - The minimum depth value of the viewport. WebGPU only.
   * @param {number} maxDepth - The maximum depth value of the viewport. WebGPU only.
   */
  setViewport(e, t, s, r, n = 0, i = 1) {
    this._canvasTarget.setViewport(e, t, s, r, n, i);
  }
  /**
   * Returns the clear color.
   *
   * @param {Color} target - The method writes the result in this target object.
   * @return {Color} The clear color.
   */
  getClearColor(e) {
    return e.copy(this._clearColor);
  }
  /**
   * Defines the clear color and optionally the clear alpha.
   *
   * @param {Color} color - The clear color.
   * @param {number} [alpha=1] - The clear alpha.
   */
  setClearColor(e, t = 1) {
    this._clearColor.set(e), this._clearColor.a = t;
  }
  /**
   * Returns the clear alpha.
   *
   * @return {number} The clear alpha.
   */
  getClearAlpha() {
    return this._clearColor.a;
  }
  /**
   * Defines the clear alpha.
   *
   * @param {number} alpha - The clear alpha.
   */
  setClearAlpha(e) {
    this._clearColor.a = e;
  }
  /**
   * Returns the clear depth.
   *
   * @return {number} The clear depth.
   */
  getClearDepth() {
    return this.reversedDepthBuffer === !0 ? 1 - this._clearDepth : this._clearDepth;
  }
  /**
   * Defines the clear depth.
   *
   * @param {number} depth - The clear depth.
   */
  setClearDepth(e) {
    this._clearDepth = e;
  }
  /**
   * Returns the clear stencil.
   *
   * @return {number} The clear stencil.
   */
  getClearStencil() {
    return this._clearStencil;
  }
  /**
   * Defines the clear stencil.
   *
   * @param {number} stencil - The clear stencil.
   */
  setClearStencil(e) {
    this._clearStencil = e;
  }
  /**
   * This method performs an occlusion query for the given 3D object.
   * It returns `true` if the given 3D object is fully occluded by other
   * 3D objects in the scene.
   *
   * @param {Object3D} object - The 3D object to test.
   * @return {boolean} Whether the 3D object is fully occluded or not.
   */
  isOccluded(e) {
    const t = this._currentRenderContext;
    return t && this.backend.isOccluded(t, e);
  }
  /**
   * Performs a manual clear operation. This method ignores `autoClear` properties.
   *
   * @param {boolean} [color=true] - Whether the color buffer should be cleared or not.
   * @param {boolean} [depth=true] - Whether the depth buffer should be cleared or not.
   * @param {boolean} [stencil=true] - Whether the stencil buffer should be cleared or not.
   */
  clear(e = !0, t = !0, s = !0) {
    if (this._initialized === !1)
      throw new Error('Renderer: .clear() called before the backend is initialized. Use "await renderer.init();" before before using this method.');
    const r = this._renderTarget || this._getFrameBufferTarget();
    let n = null;
    if (r !== null) {
      this._textures.updateRenderTarget(r);
      const i = this._textures.get(r);
      n = this._renderContexts.get(r, null, -1), n.textures = i.textures, n.depthTexture = i.depthTexture, n.width = i.width, n.height = i.height, n.renderTarget = r, n.depth = r.depthBuffer, n.stencil = r.stencilBuffer;
      const o = this.backend.getClearColor();
      n.clearColorValue.r = o.r, n.clearColorValue.g = o.g, n.clearColorValue.b = o.b, n.clearColorValue.a = o.a, n.clearDepthValue = this.getClearDepth(), n.clearStencilValue = this.getClearStencil(), n.activeCubeFace = this.getActiveCubeFace(), n.activeMipmapLevel = this.getActiveMipmapLevel(), r.depthBuffer === !0 && (i.depthInitialized = !0);
    }
    this.backend.clear(e, t, s, n), r !== null && this._renderTarget === null && this._renderOutput(r);
  }
  /**
   * Performs a manual clear operation of the color buffer. This method ignores `autoClear` properties.
   */
  clearColor() {
    this.clear(!0, !1, !1);
  }
  /**
   * Performs a manual clear operation of the depth buffer. This method ignores `autoClear` properties.
   */
  clearDepth() {
    this.clear(!1, !0, !1);
  }
  /**
   * Performs a manual clear operation of the stencil buffer. This method ignores `autoClear` properties.
   */
  clearStencil() {
    this.clear(!1, !1, !0);
  }
  /**
   * Async version of {@link Renderer#clear}.
   *
   * @async
   * @deprecated
   * @param {boolean} [color=true] - Whether the color buffer should be cleared or not.
   * @param {boolean} [depth=true] - Whether the depth buffer should be cleared or not.
   * @param {boolean} [stencil=true] - Whether the stencil buffer should be cleared or not.
   * @return {Promise} A Promise that resolves when the clear operation has been executed.
   */
  async clearAsync(e = !0, t = !0, s = !0) {
    Te('Renderer: "clearAsync()" has been deprecated. Use "clear()" and "await renderer.init();" when creating the renderer.'), await this.init(), this.clear(e, t, s);
  }
  /**
   * Async version of {@link Renderer#clearColor}.
   *
   * @async
   * @deprecated
   * @return {Promise} A Promise that resolves when the clear operation has been executed.
   */
  async clearColorAsync() {
    Te('Renderer: "clearColorAsync()" has been deprecated. Use "clearColor()" and "await renderer.init();" when creating the renderer.'), this.clear(!0, !1, !1);
  }
  /**
   * Async version of {@link Renderer#clearDepth}.
   *
   * @async
   * @deprecated
   * @return {Promise} A Promise that resolves when the clear operation has been executed.
   */
  async clearDepthAsync() {
    Te('Renderer: "clearDepthAsync()" has been deprecated. Use "clearDepth()" and "await renderer.init();" when creating the renderer.'), this.clear(!1, !0, !1);
  }
  /**
   * Async version of {@link Renderer#clearStencil}.
   *
   * @async
   * @deprecated
   * @return {Promise} A Promise that resolves when the clear operation has been executed.
   */
  async clearStencilAsync() {
    Te('Renderer: "clearStencilAsync()" has been deprecated. Use "clearStencil()" and "await renderer.init();" when creating the renderer.'), this.clear(!1, !1, !0);
  }
  /**
   * Returns `true` if a framebuffer target is needed to perform tone mapping or color space conversion.
   * If this is the case, the renderer allocates an internal render target for that purpose.
   *
   */
  get needsFrameBufferTarget() {
    const e = this.currentToneMapping !== bs, t = this.currentColorSpace !== ye.workingColorSpace;
    return e || t;
  }
  /**
   * The number of samples used for multi-sample anti-aliasing (MSAA).
   *
   * @type {number}
   * @default 0
   */
  get samples() {
    return this._samples;
  }
  /**
   * The current number of samples used for multi-sample anti-aliasing (MSAA).
   *
   * When rendering to a custom render target, the number of samples of that render target is used.
   * If the renderer needs an internal framebuffer target for tone mapping or color space conversion,
   * the number of samples is set to 0.
   *
   * @type {number}
   */
  get currentSamples() {
    let e = this._samples;
    return this._renderTarget !== null ? e = this._renderTarget.samples : this.needsFrameBufferTarget && (e = 0), e;
  }
  /**
   * The current tone mapping of the renderer. When not producing screen output,
   * the tone mapping is always `NoToneMapping`.
   *
   * @type {number}
   */
  get currentToneMapping() {
    return this.isOutputTarget ? this.toneMapping : bs;
  }
  /**
   * The current color space of the renderer. When not producing screen output,
   * the color space is always the working color space.
   *
   * @type {string}
   */
  get currentColorSpace() {
    return this.isOutputTarget ? this.outputColorSpace : ye.workingColorSpace;
  }
  /**
   * Returns `true` if the rendering settings are set to screen output.
   *
   * @returns {boolean} True if the current render target is the same of output render target or `null`, otherwise false.
   */
  get isOutputTarget() {
    return this._renderTarget === this._outputRenderTarget || this._renderTarget === null;
  }
  /**
   * Frees all internal resources of the renderer. Call this method if the renderer
   * is no longer in use by your app.
   */
  dispose() {
    if (this._initialized === !0) {
      this.info.dispose(), this.backend.dispose(), this._animation.dispose(), this._objects.dispose(), this._geometries.dispose(), this._pipelines.dispose(), this._nodes.dispose(), this._bindings.dispose(), this._renderLists.dispose(), this._renderContexts.dispose(), this._textures.dispose();
      for (const e of this._frameBufferTargets.keys())
        e.dispose();
      Object.values(this.backend.timestampQueryPool).forEach((e) => {
        e !== null && e.dispose();
      });
    }
    this.setRenderTarget(null), this.setAnimationLoop(null);
  }
  /**
   * Sets the given render target. Calling this method means the renderer does not
   * target the default framebuffer (meaning the canvas) anymore but a custom framebuffer.
   * Use `null` as the first argument to reset the state.
   *
   * @param {?RenderTarget} renderTarget - The render target to set.
   * @param {number} [activeCubeFace=0] - The active cube face.
   * @param {number} [activeMipmapLevel=0] - The active mipmap level.
   */
  setRenderTarget(e, t = 0, s = 0) {
    this._renderTarget = e, this._activeCubeFace = t, this._activeMipmapLevel = s;
  }
  /**
   * Returns the current render target.
   *
   * @return {?RenderTarget} The render target. Returns `null` if no render target is set.
   */
  getRenderTarget() {
    return this._renderTarget;
  }
  /**
   * Sets the output render target for the renderer.
   *
   * @param {?RenderTarget} renderTarget - The render target to set as the output target.
   */
  setOutputRenderTarget(e) {
    this._outputRenderTarget = e;
  }
  /**
   * Returns the current output target.
   *
   * @return {?RenderTarget} The current output render target. Returns `null` if no output target is set.
   */
  getOutputRenderTarget() {
    return this._outputRenderTarget;
  }
  /**
   * Sets the canvas target. The canvas target manages the HTML canvas
   * or the offscreen canvas the renderer draws into.
   *
   * @param {CanvasTarget} canvasTarget - The canvas target.
   */
  setCanvasTarget(e) {
    this._canvasTarget.removeEventListener("resize", this._onCanvasTargetResize), this._canvasTarget = e, this._canvasTarget.addEventListener("resize", this._onCanvasTargetResize);
  }
  /**
   * Returns the current canvas target.
   *
   * @return {CanvasTarget} The current canvas target.
   */
  getCanvasTarget() {
    return this._canvasTarget;
  }
  /**
   * Resets the renderer to the initial state before WebXR started.
   *
   * @private
   */
  _resetXRState() {
    this.backend.setXRTarget(null), this.setOutputRenderTarget(null), this.setRenderTarget(null);
    for (const e of this._frameBufferTargets.keys())
      e.dispose();
  }
  /**
   * Callback for {@link Renderer#setRenderObjectFunction}.
   *
   * @callback renderObjectFunction
   * @param {Object3D} object - The 3D object.
   * @param {Scene} scene - The scene the 3D object belongs to.
   * @param {Camera} camera - The camera the object should be rendered with.
   * @param {BufferGeometry} geometry - The object's geometry.
   * @param {Material} material - The object's material.
   * @param {?Object} group - Only relevant for objects using multiple materials. This represents a group entry from the respective `BufferGeometry`.
   * @param {LightsNode} lightsNode - The current lights node.
   * @param {ClippingContext} clippingContext - The clipping context.
   * @param {?string} [passId=null] - An optional ID for identifying the pass.
   */
  /**
   * Sets the given render object function. Calling this method overwrites the default implementation
   * which is {@link Renderer#renderObject}. Defining a custom function can be useful
   * if you want to modify the way objects are rendered. For example you can define things like "every
   * object that has material of a certain type should perform a pre-pass with a special overwrite material".
   * The custom function must always call `renderObject()` in its implementation.
   *
   * Use `null` as the first argument to reset the state.
   *
   * @param {?renderObjectFunction} renderObjectFunction - The render object function.
   */
  setRenderObjectFunction(e) {
    this._renderObjectFunction = e;
  }
  /**
   * Returns the current render object function.
   *
   * @return {?Function} The current render object function. Returns `null` if no function is set.
   */
  getRenderObjectFunction() {
    return this._renderObjectFunction;
  }
  /**
   * Execute a single or an array of compute nodes. This method can only be called
   * if the renderer has been initialized.
   *
   * @param {Node|Array<Node>} computeNodes - The compute node(s).
   * @param {number|Array<number>|IndirectStorageBufferAttribute} [dispatchSize=null]
   * - A single number representing count, or
   * - An array [x, y, z] representing dispatch size, or
   * - A IndirectStorageBufferAttribute for indirect dispatch size.
   * @return {Promise|undefined} A Promise that resolve when the compute has finished. Only returned when the renderer has not been initialized.
   */
  compute(e, t = null) {
    if (this._isDeviceLost === !0) return;
    if (this._initialized === !1)
      return L("Renderer: .compute() called before the backend is initialized. Try using .computeAsync() instead."), this.computeAsync(e, t);
    const s = this._nodes.nodeFrame, r = s.renderId;
    this.info.calls++, this.info.compute.calls++, this.info.compute.frameCalls++, s.renderId = this.info.calls, this.backend.updateTimeStampUID(e), this.inspector.beginCompute(this.backend.getTimestampUID(e), e);
    const n = this.backend, i = this._pipelines, o = this._bindings, u = this._nodes, l = Array.isArray(e) ? e : [e];
    if (l[0] === void 0 || l[0].isComputeNode !== !0)
      throw new Error("THREE.Renderer: .compute() expects a ComputeNode.");
    n.beginCompute(e);
    for (const c of l) {
      if (i.has(c) === !1) {
        const f = () => {
          c.removeEventListener("dispose", f), i.delete(c), o.deleteForCompute(c), u.delete(c);
        };
        c.addEventListener("dispose", f);
        const p = c.onInitFunction;
        p !== null && p.call(c, { renderer: this });
      }
      u.updateForCompute(c), o.updateForCompute(c);
      const d = o.getForCompute(c), h = i.getForCompute(c, d);
      n.compute(e, c, d, h, t);
    }
    n.finishCompute(e), s.renderId = r, this.inspector.finishCompute(this.backend.getTimestampUID(e));
  }
  /**
   * Execute a single or an array of compute nodes.
   *
   * @async
   * @param {Node|Array<Node>} computeNodes - The compute node(s).
   * @param {number|Array<number>|IndirectStorageBufferAttribute} [dispatchSize=null]
   * - A single number representing count, or
   * - An array [x, y, z] representing dispatch size, or
   * - A IndirectStorageBufferAttribute for indirect dispatch size.
   * @return {Promise} A Promise that resolve when the compute has finished.
   */
  async computeAsync(e, t = null) {
    this._initialized === !1 && await this.init(), this.compute(e, t);
  }
  /**
   * Checks if the given feature is supported by the selected backend.
   *
   * @async
   * @deprecated
   * @param {string} name - The feature's name.
   * @return {Promise<boolean>} A Promise that resolves with a bool that indicates whether the feature is supported or not.
   */
  async hasFeatureAsync(e) {
    return Te('Renderer: "hasFeatureAsync()" has been deprecated. Use "hasFeature()" and "await renderer.init();" when creating the renderer.'), await this.init(), this.hasFeature(e);
  }
  async resolveTimestampsAsync(e = "render") {
    return this._initialized === !1 && await this.init(), this.backend.resolveTimestampsAsync(e);
  }
  /**
   * Checks if the given feature is supported by the selected backend. If the
   * renderer has not been initialized, this method always returns `false`.
   *
   * @param {string} name - The feature's name.
   * @return {boolean} Whether the feature is supported or not.
   */
  hasFeature(e) {
    if (this._initialized === !1)
      throw new Error('Renderer: .hasFeature() called before the backend is initialized. Use "await renderer.init();" before before using this method.');
    return this.backend.hasFeature(e);
  }
  /**
   * Returns `true` when the renderer has been initialized.
   *
   * @return {boolean} Whether the renderer has been initialized or not.
   */
  hasInitialized() {
    return this._initialized;
  }
  /**
   * Initializes the given textures. Useful for preloading a texture rather than waiting until first render
   * (which can cause noticeable lags due to decode and GPU upload overhead).
   *
   * @async
   * @deprecated
   * @param {Texture} texture - The texture.
   * @return {Promise} A Promise that resolves when the texture has been initialized.
   */
  async initTextureAsync(e) {
    Te('Renderer: "initTextureAsync()" has been deprecated. Use "initTexture()" and "await renderer.init();" when creating the renderer.'), await this.init(), this.initTexture(e);
  }
  /**
   * Initializes the given texture. Useful for preloading a texture rather than waiting until first render
   * (which can cause noticeable lags due to decode and GPU upload overhead).
   *
   * This method can only be used if the renderer has been initialized.
   *
   * @param {Texture} texture - The texture.
   */
  initTexture(e) {
    if (this._initialized === !1)
      throw new Error('Renderer: .initTexture() called before the backend is initialized. Use "await renderer.init();" before before using this method.');
    this._textures.updateTexture(e);
  }
  /**
   * Initializes the given render target.
   *
   * @param {RenderTarget} renderTarget - The render target to intialize.
   */
  initRenderTarget(e) {
    if (this._initialized === !1)
      throw new Error('Renderer: .initRenderTarget() called before the backend is initialized. Use "await renderer.init();" before before using this method.');
    this._textures.updateRenderTarget(e);
    const t = this._textures.get(e), s = this._renderContexts.get(e);
    s.textures = t.textures, s.depthTexture = t.depthTexture, s.width = t.width, s.height = t.height, s.renderTarget = e, s.depth = e.depthBuffer, s.stencil = e.stencilBuffer, this.backend.initRenderTarget(s);
  }
  /**
   * Copies the current bound framebuffer into the given texture.
   *
   * @param {FramebufferTexture} framebufferTexture - The texture.
   * @param {?(Vector2|Vector4)} [rectangle=null] - A two or four dimensional vector that defines the rectangular portion of the framebuffer that should be copied.
   */
  copyFramebufferToTexture(e, t = null) {
    if (t !== null)
      if (t.isVector2)
        t = Ut.set(t.x, t.y, e.image.width, e.image.height).floor();
      else if (t.isVector4)
        t = Ut.copy(t).floor();
      else {
        F("Renderer.copyFramebufferToTexture: Invalid rectangle.");
        return;
      }
    else
      t = Ut.set(0, 0, e.image.width, e.image.height);
    let s = this._currentRenderContext, r;
    s !== null ? r = s.renderTarget : (r = this._renderTarget || this._getFrameBufferTarget(), r !== null && (this._textures.updateRenderTarget(r), s = this._textures.get(r))), this._textures.updateTexture(e, { renderTarget: r }), this.backend.copyFramebufferToTexture(e, s, t), this._inspector.copyFramebufferToTexture(e);
  }
  /**
   * Copies data of the given source texture into a destination texture.
   *
   * @param {Texture} srcTexture - The source texture.
   * @param {Texture} dstTexture - The destination texture.
   * @param {Box2|Box3} [srcRegion=null] - A bounding box which describes the source region. Can be two or three-dimensional.
   * @param {Vector2|Vector3} [dstPosition=null] - A vector that represents the origin of the destination region. Can be two or three-dimensional.
   * @param {number} [srcLevel=0] - The source mip level to copy from.
   * @param {number} [dstLevel=0] - The destination mip level to copy to.
   */
  copyTextureToTexture(e, t, s = null, r = null, n = 0, i = 0) {
    this._textures.updateTexture(e), this._textures.updateTexture(t), this.backend.copyTextureToTexture(e, t, s, r, n, i), this._inspector.copyTextureToTexture(e, t);
  }
  /**
   * Reads pixel data from the given render target.
   *
   * @async
   * @param {RenderTarget} renderTarget - The render target to read from.
   * @param {number} x - The `x` coordinate of the copy region's origin.
   * @param {number} y - The `y` coordinate of the copy region's origin.
   * @param {number} width - The width of the copy region.
   * @param {number} height - The height of the copy region.
   * @param {number} [textureIndex=0] - The texture index of a MRT render target.
   * @param {number} [faceIndex=0] - The active cube face index.
   * @return {Promise<TypedArray>} A Promise that resolves when the read has been finished. The resolve provides the read data as a typed array.
   */
  async readRenderTargetPixelsAsync(e, t, s, r, n, i = 0, o = 0) {
    return this.backend.copyTextureToBuffer(e.textures[i], t, s, r, n, o);
  }
  /**
   * Analyzes the given 3D object's hierarchy and builds render lists from the
   * processed hierarchy.
   *
   * @private
   * @param {Object3D} object - The 3D object to process (usually a scene).
   * @param {Camera} camera - The camera the object is rendered with.
   * @param {number} groupOrder - The group order is derived from the `renderOrder` of groups and is used to group 3D objects within groups.
   * @param {RenderList} renderList - The current render list.
   * @param {ClippingContext} clippingContext - The current clipping context.
   */
  _projectObject(e, t, s, r, n) {
    if (e.visible === !1) return;
    if (e.layers.test(t.layers)) {
      if (e.isGroup)
        s = e.renderOrder, e.isClippingGroup && e.enabled && (n = n.getGroupContext(e));
      else if (e.isLOD)
        e.autoUpdate === !0 && e.update(t);
      else if (e.isLight)
        r.pushLight(e);
      else if (e.isSprite) {
        const u = t.isArrayCamera ? io : no;
        if (!e.frustumCulled || u.intersectsSprite(e, t)) {
          this.sortObjects === !0 && Ut.setFromMatrixPosition(e.matrixWorld).applyMatrix4(_n);
          const { geometry: l, material: c } = e;
          c.visible && r.push(e, l, c, s, Ut.z, null, n);
        }
      } else if (e.isLineLoop)
        F("Renderer: Objects of type THREE.LineLoop are not supported. Please use THREE.Line or THREE.LineSegments.");
      else if (e.isMesh || e.isLine || e.isPoints) {
        const u = t.isArrayCamera ? io : no;
        if (!e.frustumCulled || u.intersectsObject(e, t)) {
          const { geometry: l, material: c } = e;
          if (this.sortObjects === !0 && (l.boundingSphere === null && l.computeBoundingSphere(), Ut.copy(l.boundingSphere.center).applyMatrix4(e.matrixWorld).applyMatrix4(_n)), Array.isArray(c)) {
            const d = l.groups;
            for (let h = 0, f = d.length; h < f; h++) {
              const p = d[h], g = c[p.materialIndex];
              g && g.visible && r.push(e, l, g, s, Ut.z, p, n);
            }
          } else c.visible && r.push(e, l, c, s, Ut.z, null, n);
        }
      }
    }
    if (e.isBundleGroup === !0 && this.backend.beginBundle !== void 0) {
      const u = r;
      r = this._renderLists.get(e, t), r.begin(), u.pushBundle({
        bundleGroup: e,
        camera: t,
        renderList: r
      }), r.finish();
    }
    const o = e.children;
    for (let u = 0, l = o.length; u < l; u++)
      this._projectObject(o[u], t, s, r, n);
  }
  /**
   * Renders the given render bundles.
   *
   * @private
   * @param {Array<Object>} bundles - Array with render bundle data.
   * @param {Scene} sceneRef - The scene the render bundles belong to.
   * @param {LightsNode} lightsNode - The current lights node.
   */
  _renderBundles(e, t, s) {
    for (const r of e)
      this._renderBundle(r, t, s);
  }
  /**
   * Renders the transparent objects from the given render lists.
   *
   * @private
   * @param {Array<Object>} renderList - The transparent render list.
   * @param {Array<Object>} doublePassList - The list of transparent objects which require a double pass (e.g. because of transmission).
   * @param {Camera} camera - The camera the render list should be rendered with.
   * @param {Scene} scene - The scene the render list belongs to.
   * @param {LightsNode} lightsNode - The current lights node.
   */
  _renderTransparents(e, t, s, r, n) {
    if (t.length > 0) {
      for (const { material: i } of t)
        i.side = Ge;
      this._renderObjects(t, s, r, n, "backSide");
      for (const { material: i } of t)
        i.side = Ns;
      this._renderObjects(e, s, r, n);
      for (const { material: i } of t)
        i.side = Ot;
    } else
      this._renderObjects(e, s, r, n);
  }
  /**
   * Renders the objects from the given render list.
   *
   * @private
   * @param {Array<Object>} renderList - The render list.
   * @param {Camera} camera - The camera the render list should be rendered with.
   * @param {Scene} scene - The scene the render list belongs to.
   * @param {LightsNode} lightsNode - The current lights node.
   * @param {?string} [passId=null] - An optional ID for identifying the pass.
   */
  _renderObjects(e, t, s, r, n = null) {
    for (let i = 0, o = e.length; i < o; i++) {
      const { object: u, geometry: l, material: c, group: d, clippingContext: h } = e[i];
      this._currentRenderObjectFunction(u, s, t, l, c, d, r, h, n);
    }
  }
  /**
   * Retrieves shadow nodes for the given material. This is used to setup shadow passes.
   * The result is cached per material and updated when the material's version changes.
   *
   * @private
   * @param {Material} material
   * @returns {Object} - The shadow nodes for the material.
   */
  _getShadowNodes(e) {
    const t = e.version;
    let s = this._cacheShadowNodes.get(e);
    if (s === void 0 || s.version !== t) {
      const r = e.map !== null, n = e.colorNode && e.colorNode.isNode, i = e.castShadowNode && e.castShadowNode.isNode, o = e.maskShadowNode && e.maskShadowNode.isNode || e.maskNode && e.maskNode.isNode;
      let u = null, l = null, c = null;
      if (r || n || i || o) {
        let d, h;
        if (i ? (d = e.castShadowNode.rgb, h = e.castShadowNode.a, this.shadowMap.transmitted !== !0 && Te("Renderer: `shadowMap.transmitted` needs to be set to `true` when using `material.castShadowNode`.")) : (d = N(0), h = b(1)), r && (h = h.mul(ge("map", "texture", e).a)), n && (h = h.mul(e.colorNode.a)), l = W(d, h), o) {
          const f = e.maskShadowNode || e.maskNode;
          l = A(([p]) => (f.not().discard(), p))(l);
        }
      }
      e.depthNode && e.depthNode.isNode && (c = e.depthNode), e.castShadowPositionNode && e.castShadowPositionNode.isNode ? u = e.castShadowPositionNode : e.positionNode && e.positionNode.isNode && (u = e.positionNode), s = {
        version: t,
        colorNode: l,
        depthNode: c,
        positionNode: u
      }, this._cacheShadowNodes.set(e, s);
    }
    return s;
  }
  /**
   * This method represents the default render object function that manages the render lifecycle
   * of the object.
   *
   * @param {Object3D} object - The 3D object.
   * @param {Scene} scene - The scene the 3D object belongs to.
   * @param {Camera} camera - The camera the object should be rendered with.
   * @param {BufferGeometry} geometry - The object's geometry.
   * @param {Material} material - The object's material.
   * @param {?Object} group - Only relevant for objects using multiple materials. This represents a group entry from the respective `BufferGeometry`.
   * @param {LightsNode} lightsNode - The current lights node.
   * @param {?ClippingContext} clippingContext - The clipping context.
   * @param {?string} [passId=null] - An optional ID for identifying the pass.
   */
  renderObject(e, t, s, r, n, i, o, u = null, l = null) {
    let c = !1, d, h, f, p;
    if (e.onBeforeRender(this, t, s, r, n, i), n.allowOverride === !0 && t.overrideMaterial !== null) {
      const g = t.overrideMaterial;
      if (c = !0, d = g.isNodeMaterial ? g.colorNode : null, h = g.isNodeMaterial ? g.depthNode : null, f = g.isNodeMaterial ? g.positionNode : null, p = t.overrideMaterial.side, n.positionNode && n.positionNode.isNode && (g.positionNode = n.positionNode), g.alphaTest = n.alphaTest, g.alphaMap = n.alphaMap, g.transparent = n.transparent || n.transmission > 0 || n.transmissionNode && n.transmissionNode.isNode || n.backdropNode && n.backdropNode.isNode, g.isShadowPassMaterial) {
        const { colorNode: m, depthNode: y, positionNode: _ } = this._getShadowNodes(n);
        this.shadowMap.type === Br ? g.side = n.shadowSide !== null ? n.shadowSide : n.side : g.side = n.shadowSide !== null ? n.shadowSide : ES[n.side], m !== null && (g.colorNode = m), y !== null && (g.depthNode = y), _ !== null && (g.positionNode = _);
      }
      n = g;
    }
    n.transparent === !0 && n.side === Ot && n.forceSinglePass === !1 ? (n.side = Ge, this._handleObjectFunction(e, n, t, s, o, i, u, "backSide"), n.side = Ns, this._handleObjectFunction(e, n, t, s, o, i, u, l), n.side = Ot) : this._handleObjectFunction(e, n, t, s, o, i, u, l), c && (t.overrideMaterial.colorNode = d, t.overrideMaterial.depthNode = h, t.overrideMaterial.positionNode = f, t.overrideMaterial.side = p), e.onAfterRender(this, t, s, r, n, i);
  }
  /**
   * Checks if the given compatibility is supported by the selected backend.
   *
   * @param {string} name - The compatibility's name.
   * @return {boolean} Whether the compatibility is supported or not.
   */
  hasCompatibility(e) {
    if (this._initialized === !1)
      throw new Error('Renderer: .hasCompatibility() called before the backend is initialized. Use "await renderer.init();" before using this method.');
    return this.backend.hasCompatibility(e);
  }
  /**
   * This method represents the default `_handleObjectFunction` implementation which creates
   * a render object from the given data and performs the draw command with the selected backend.
   *
   * @private
   * @param {Object3D} object - The 3D object.
   * @param {Material} material - The object's material.
   * @param {Scene} scene - The scene the 3D object belongs to.
   * @param {Camera} camera - The camera the object should be rendered with.
   * @param {LightsNode} lightsNode - The current lights node.
   * @param {?{start: number, count: number}} group - Only relevant for objects using multiple materials. This represents a group entry from the respective `BufferGeometry`.
   * @param {ClippingContext} clippingContext - The clipping context.
   * @param {string} [passId] - An optional ID for identifying the pass.
   */
  _renderObjectDirect(e, t, s, r, n, i, o, u) {
    const l = this._objects.get(e, t, s, r, n, this._currentRenderContext, o, u);
    l.drawRange = e.geometry.drawRange, l.group = i, this._currentRenderBundle !== null && (this.backend.get(this._currentRenderBundle).renderObjects.push(l), l.bundle = this._currentRenderBundle.bundleGroup);
    const c = this._nodes.needsRefresh(l);
    c && (this._nodes.updateBefore(l), this._geometries.updateForRender(l), this._nodes.updateForRender(l), this._bindings.updateForRender(l)), this._pipelines.updateForRender(l), this._pipelines.isReady(l) && (this.backend.draw(l, this.info), c && this._nodes.updateAfter(l));
  }
  /**
   * A different implementation for `_handleObjectFunction` which only makes sure the object is ready for rendering.
   * Used in `compileAsync()`.
   *
   * @private
   * @param {Object3D} object - The 3D object.
   * @param {Material} material - The object's material.
   * @param {Scene} scene - The scene the 3D object belongs to.
   * @param {Camera} camera - The camera the object should be rendered with.
   * @param {LightsNode} lightsNode - The current lights node.
   * @param {?{start: number, count: number}} group - Only relevant for objects using multiple materials. This represents a group entry from the respective `BufferGeometry`.
   * @param {ClippingContext} clippingContext - The clipping context.
   * @param {string} [passId] - An optional ID for identifying the pass.
   */
  _createObjectPipeline(e, t, s, r, n, i, o, u) {
    if (this._compilationPromises !== null) {
      this._compilationPromises.push({
        object: e,
        material: t,
        scene: s,
        camera: r,
        lightsNode: n,
        group: i,
        clippingContext: o,
        passId: u,
        renderContext: this._currentRenderContext
      });
      return;
    }
    const l = this._objects.get(e, t, s, r, n, this._currentRenderContext, o, u);
    l.drawRange = e.geometry.drawRange, l.group = i, this._nodes.updateBefore(l), this._geometries.updateForRender(l), this._nodes.updateForRender(l), this._bindings.updateForRender(l), this._pipelines.getForRender(l, this._compilationPromises), this._nodes.updateAfter(l);
  }
  /**
   * Callback when the canvas has been resized.
   *
   * @private
   */
  _onCanvasTargetResize() {
    this._initialized && this.backend.updateSize();
  }
  /**
   * Alias for `compileAsync()`.
   *
   * @method
   * @param {Object3D} scene - The scene or 3D object to precompile.
   * @param {Camera} camera - The camera that is used to render the scene.
   * @param {Scene} targetScene - If the first argument is a 3D object, this parameter must represent the scene the 3D object is going to be added.
   * @return {function(Object3D, Camera, ?Scene): Promise|undefined} A Promise that resolves when the compile has been finished.
   */
  get compile() {
    return this.compileAsync;
  }
}
class ch {
  /**
   * Constructs a new binding.
   *
   * @param {string} [name=''] - The binding's name.
   */
  constructor(e = "") {
    this.name = e, this.visibility = 0;
  }
  /**
   * Makes sure binding's resource is visible for the given shader stage.
   *
   * @param {number} visibility - The shader stage.
   */
  setVisibility(e) {
    this.visibility |= e;
  }
  /**
   * The shader stages in which the binding's resource is visible.
   *
   * @return {number} The visibility bitmask.
   */
  getVisibility() {
    return this.visibility;
  }
  /**
   * Clones the binding.
   *
   * @return {Binding} The cloned binding.
   */
  clone() {
    return Object.assign(new this.constructor(), this);
  }
}
function wS(a) {
  return a + (Jt - a % Jt) % Jt;
}
class dh extends ch {
  /**
   * Constructs a new buffer.
   *
   * @param {string} name - The buffer's name.
   * @param {TypedArray} [buffer=null] - The buffer.
   */
  constructor(e, t = null) {
    super(e), this.isBuffer = !0, this.bytesPerElement = Float32Array.BYTES_PER_ELEMENT, this._buffer = t, this._updateRanges = [];
  }
  /**
   * The array of update ranges.
   *
   * @type {Array<{start: number, count: number}>}
   */
  get updateRanges() {
    return this._updateRanges;
  }
  /**
   * Adds an update range.
   *
   * @param {number} start - The start index.
   * @param {number} count - The number of elements.
   */
  addUpdateRange(e, t) {
    this.updateRanges.push({ start: e, count: t });
  }
  /**
   * Clears all update ranges.
   */
  clearUpdateRanges() {
    this.updateRanges.length = 0;
  }
  /**
   * The buffer's byte length.
   *
   * @type {number}
   * @readonly
   */
  get byteLength() {
    return wS(this._buffer.byteLength);
  }
  /**
   * A reference to the internal buffer.
   *
   * @type {Float32Array}
   * @readonly
   */
  get buffer() {
    return this._buffer;
  }
  /**
   * Updates the binding.
   *
   * @return {boolean} Whether the buffer has been updated and must be
   * uploaded to the GPU.
   */
  update() {
    return !0;
  }
}
class hh extends dh {
  /**
   * Constructs a new uniform buffer.
   *
   * @param {string} name - The buffer's name.
   * @param {TypedArray} [buffer=null] - The buffer.
   */
  constructor(e, t = null) {
    super(e, t), this.isUniformBuffer = !0;
  }
}
let MS = 0;
class fh extends hh {
  /**
   * Constructs a new node-based uniform buffer.
   *
   * @param {BufferNode} nodeUniform - The uniform buffer node.
   * @param {UniformGroupNode} groupNode - The uniform group node.
   */
  constructor(e, t) {
    super("UniformBuffer_" + MS++, e ? e.value : null), this.nodeUniform = e, this.groupNode = t, this.isNodeUniformBuffer = !0;
  }
  /**
   * The array of update ranges.
   *
   * @param {Array<{start: number, count: number}>} value - The update ranges.
   */
  set updateRanges(e) {
    this.nodeUniform.updateRanges = e;
  }
  /**
   * The array of update ranges.
   *
   * @type {Array<{start: number, count: number}>}
   */
  get updateRanges() {
    return this.nodeUniform.updateRanges;
  }
  /**
   * Adds a range of data in the data array to be updated on the GPU.
   *
   * @param {number} start - Position at which to start update.
   * @param {number} count - The number of components to update.
   */
  addUpdateRange(e, t) {
    this.nodeUniform.addUpdateRange(e, t);
  }
  /**
   * Clears all update ranges.
   */
  clearUpdateRanges() {
    this.nodeUniform.clearUpdateRanges();
  }
  /**
   * The uniform buffer.
   *
   * @type {Float32Array}
   */
  get buffer() {
    return this.nodeUniform.value;
  }
}
class BS extends hh {
  /**
   * Constructs a new uniforms group.
   *
   * @param {string} name - The group's name.
   */
  constructor(e) {
    super(e), this.isUniformsGroup = !0, this._values = null, this.uniforms = [], this._updateRangeCache = /* @__PURE__ */ new Map();
  }
  /**
   * Adds a uniform's update range to this buffer.
   *
   * @param {Uniform} uniform - The uniform.
   */
  addUniformUpdateRange(e) {
    const t = e.index;
    if (this._updateRangeCache.has(t) !== !0) {
      const s = this.updateRanges, r = e.offset, n = e.itemSize, i = { start: r, count: n };
      s.push(i), this._updateRangeCache.set(t, i);
    }
  }
  /**
   * Clears all update ranges of this buffer.
   */
  clearUpdateRanges() {
    this._updateRangeCache.clear(), super.clearUpdateRanges();
  }
  /**
   * Adds a uniform to this group.
   *
   * @param {Uniform} uniform - The uniform to add.
   * @return {UniformsGroup} A reference to this group.
   */
  addUniform(e) {
    return this.uniforms.push(e), this;
  }
  /**
   * Removes a uniform from this group.
   *
   * @param {Uniform} uniform - The uniform to remove.
   * @return {UniformsGroup} A reference to this group.
   */
  removeUniform(e) {
    const t = this.uniforms.indexOf(e);
    return t !== -1 && this.uniforms.splice(t, 1), this;
  }
  /**
   * An array with the raw uniform values.
   *
   * @type {Array<number>}
   */
  get values() {
    return this._values === null && (this._values = Array.from(this.buffer)), this._values;
  }
  /**
   * A Float32 array buffer with the uniform values.
   *
   * @type {Float32Array}
   */
  get buffer() {
    let e = this._buffer;
    if (e === null) {
      const t = this.byteLength;
      e = new Float32Array(new ArrayBuffer(t)), this._buffer = e;
    }
    return e;
  }
  /**
   * The byte length of the buffer with correct buffer alignment.
   *
   * @type {number}
   */
  get byteLength() {
    const e = this.bytesPerElement;
    let t = 0;
    for (let s = 0, r = this.uniforms.length; s < r; s++) {
      const n = this.uniforms[s], i = n.boundary, o = n.itemSize * e, u = t % Jt, l = u % i, c = u + l;
      t += l, c !== 0 && Jt - c < o && (t += Jt - c), n.offset = t / e, n.index = s, t += o;
    }
    return Math.ceil(t / Jt) * Jt;
  }
  /**
   * Updates this group by updating each uniform object of
   * the internal uniform list. The uniform objects check if their
   * values has actually changed so this method only returns
   * `true` if there is a real value change.
   *
   * @return {boolean} Whether the uniforms have been updated and
   * must be uploaded to the GPU.
   */
  update() {
    let e = !1;
    for (const t of this.uniforms)
      this.updateByType(t) === !0 && (e = !0);
    return e;
  }
  /**
   * Updates a given uniform by calling an update method matching
   * the uniforms type.
   *
   * @param {Uniform} uniform - The uniform to update.
   * @return {boolean} Whether the uniform has been updated or not.
   */
  updateByType(e) {
    if (e.isNumberUniform) return this.updateNumber(e);
    if (e.isVector2Uniform) return this.updateVector2(e);
    if (e.isVector3Uniform) return this.updateVector3(e);
    if (e.isVector4Uniform) return this.updateVector4(e);
    if (e.isColorUniform) return this.updateColor(e);
    if (e.isMatrix3Uniform) return this.updateMatrix3(e);
    if (e.isMatrix4Uniform) return this.updateMatrix4(e);
    F("WebGPUUniformsGroup: Unsupported uniform type.", e);
  }
  /**
   * Updates a given Number uniform.
   *
   * @param {NumberUniform} uniform - The Number uniform.
   * @return {boolean} Whether the uniform has been updated or not.
   */
  updateNumber(e) {
    let t = !1;
    const s = this.values, r = e.getValue(), n = e.offset, i = e.getType();
    if (s[n] !== r) {
      const o = this._getBufferForType(i);
      o[n] = s[n] = r, t = !0, this.addUniformUpdateRange(e);
    }
    return t;
  }
  /**
   * Updates a given Vector2 uniform.
   *
   * @param {Vector2Uniform} uniform - The Vector2 uniform.
   * @return {boolean} Whether the uniform has been updated or not.
   */
  updateVector2(e) {
    let t = !1;
    const s = this.values, r = e.getValue(), n = e.offset, i = e.getType();
    if (s[n + 0] !== r.x || s[n + 1] !== r.y) {
      const o = this._getBufferForType(i);
      o[n + 0] = s[n + 0] = r.x, o[n + 1] = s[n + 1] = r.y, t = !0, this.addUniformUpdateRange(e);
    }
    return t;
  }
  /**
   * Updates a given Vector3 uniform.
   *
   * @param {Vector3Uniform} uniform - The Vector3 uniform.
   * @return {boolean} Whether the uniform has been updated or not.
   */
  updateVector3(e) {
    let t = !1;
    const s = this.values, r = e.getValue(), n = e.offset, i = e.getType();
    if (s[n + 0] !== r.x || s[n + 1] !== r.y || s[n + 2] !== r.z) {
      const o = this._getBufferForType(i);
      o[n + 0] = s[n + 0] = r.x, o[n + 1] = s[n + 1] = r.y, o[n + 2] = s[n + 2] = r.z, t = !0, this.addUniformUpdateRange(e);
    }
    return t;
  }
  /**
   * Updates a given Vector4 uniform.
   *
   * @param {Vector4Uniform} uniform - The Vector4 uniform.
   * @return {boolean} Whether the uniform has been updated or not.
   */
  updateVector4(e) {
    let t = !1;
    const s = this.values, r = e.getValue(), n = e.offset, i = e.getType();
    if (s[n + 0] !== r.x || s[n + 1] !== r.y || s[n + 2] !== r.z || s[n + 3] !== r.w) {
      const o = this._getBufferForType(i);
      o[n + 0] = s[n + 0] = r.x, o[n + 1] = s[n + 1] = r.y, o[n + 2] = s[n + 2] = r.z, o[n + 3] = s[n + 3] = r.w, t = !0, this.addUniformUpdateRange(e);
    }
    return t;
  }
  /**
   * Updates a given Color uniform.
   *
   * @param {ColorUniform} uniform - The Color uniform.
   * @return {boolean} Whether the uniform has been updated or not.
   */
  updateColor(e) {
    let t = !1;
    const s = this.values, r = e.getValue(), n = e.offset;
    if (s[n + 0] !== r.r || s[n + 1] !== r.g || s[n + 2] !== r.b) {
      const i = this.buffer;
      i[n + 0] = s[n + 0] = r.r, i[n + 1] = s[n + 1] = r.g, i[n + 2] = s[n + 2] = r.b, t = !0, this.addUniformUpdateRange(e);
    }
    return t;
  }
  /**
   * Updates a given Matrix3 uniform.
   *
   * @param {Matrix3Uniform} uniform - The Matrix3 uniform.
   * @return {boolean} Whether the uniform has been updated or not.
   */
  updateMatrix3(e) {
    let t = !1;
    const s = this.values, r = e.getValue().elements, n = e.offset;
    if (s[n + 0] !== r[0] || s[n + 1] !== r[1] || s[n + 2] !== r[2] || s[n + 4] !== r[3] || s[n + 5] !== r[4] || s[n + 6] !== r[5] || s[n + 8] !== r[6] || s[n + 9] !== r[7] || s[n + 10] !== r[8]) {
      const i = this.buffer;
      i[n + 0] = s[n + 0] = r[0], i[n + 1] = s[n + 1] = r[1], i[n + 2] = s[n + 2] = r[2], i[n + 4] = s[n + 4] = r[3], i[n + 5] = s[n + 5] = r[4], i[n + 6] = s[n + 6] = r[5], i[n + 8] = s[n + 8] = r[6], i[n + 9] = s[n + 9] = r[7], i[n + 10] = s[n + 10] = r[8], t = !0, this.addUniformUpdateRange(e);
    }
    return t;
  }
  /**
   * Updates a given Matrix4 uniform.
   *
   * @param {Matrix4Uniform} uniform - The Matrix4 uniform.
   * @return {boolean} Whether the uniform has been updated or not.
   */
  updateMatrix4(e) {
    let t = !1;
    const s = this.values, r = e.getValue().elements, n = e.offset;
    return US(s, r, n) === !1 && (this.buffer.set(r, n), FS(s, r, n), t = !0, this.addUniformUpdateRange(e)), t;
  }
  /**
   * Returns a typed array that matches the given data type.
   *
   * @private
   * @param {string} type - The data type.
   * @return {TypedArray} The typed array.
   */
  _getBufferForType(e) {
    return e === "int" || e === "ivec2" || e === "ivec3" || e === "ivec4" ? new Int32Array(this.buffer.buffer) : e === "uint" || e === "uvec2" || e === "uvec3" || e === "uvec4" ? new Uint32Array(this.buffer.buffer) : this.buffer;
  }
}
function FS(a, e, t) {
  for (let s = 0, r = e.length; s < r; s++)
    a[t + s] = e[s];
}
function US(a, e, t) {
  for (let s = 0, r = e.length; s < r; s++)
    if (a[t + s] !== e[s]) return !1;
  return !0;
}
let DS = 0;
class ph extends BS {
  /**
   * Constructs a new node-based uniforms group.
   *
   * @param {string} name - The group's name.
   * @param {UniformGroupNode} groupNode - The uniform group node.
   */
  constructor(e, t) {
    super(e), this.id = DS++, this.groupNode = t, this.isNodeUniformsGroup = !0;
  }
}
class gh extends ch {
  /**
   * Constructs a new sampler.
   *
   * @param {string} name - The samplers's name.
   * @param {?Texture} texture - The texture this binding is referring to.
   */
  constructor(e, t) {
    super(e), this._texture = null, this._onTextureDispose = () => {
      this.generation = null, this.version = -1;
    }, this.texture = t, this.version = t ? t.version : -1, this.generation = null, this.samplerKey = "", this.isSampler = !0;
  }
  /**
   * Sets the texture of this sampler.
   *
   * @param {Texture} value - The texture to set.
   */
  set texture(e) {
    this._texture !== e && (this._texture && this._texture.removeEventListener("dispose", this._onTextureDispose), this._texture = e, this.generation = null, this.version = -1, this._texture && this._texture.addEventListener("dispose", this._onTextureDispose));
  }
  /**
   * Gets the texture of this sampler.
   * @return {?Texture} The texture.
   */
  get texture() {
    return this._texture;
  }
  /**
   * Updates the binding.
   *
   * @return {boolean} Whether the texture has been updated and must be
   * uploaded to the GPU.
   */
  update() {
    const { texture: e, version: t } = this;
    return t !== e.version ? (this.version = e.version, !0) : !1;
  }
  clone() {
    const e = super.clone();
    return e._texture = null, e._onTextureDispose = () => {
      e.generation = null, e.version = -1;
    }, e.texture = this.texture, e;
  }
}
let PS = 0;
class LS extends gh {
  /**
   * Constructs a new sampled texture.
   *
   * @param {string} name - The sampled texture's name.
   * @param {?Texture} texture - The texture this binding is referring to.
   */
  constructor(e, t) {
    super(e, t), this.id = PS++, this.store = !1, this.mipLevel = 0, this.isSampledTexture = !0;
  }
}
class _i extends LS {
  /**
   * Constructs a new node-based sampled texture.
   *
   * @param {string} name - The textures's name.
   * @param {TextureNode} textureNode - The texture node.
   * @param {UniformGroupNode} groupNode - The uniform group node.
   * @param {?string} [access=null] - The access type.
   */
  constructor(e, t, s, r = null) {
    super(e, t ? t.value : null), this.textureNode = t, this.groupNode = s, this.access = r;
  }
  /**
   * Updates the binding.
   *
   * @return {boolean} Whether the texture has been updated and must be
   * uploaded to the GPU.
   */
  update() {
    const { textureNode: e } = this;
    return this.texture !== e.value ? (this.texture = e.value, !0) : super.update();
  }
}
class mh extends _i {
  /**
   * Constructs a new node-based sampled cube texture.
   *
   * @param {string} name - The textures's name.
   * @param {TextureNode} textureNode - The texture node.
   * @param {UniformGroupNode} groupNode - The uniform group node.
   * @param {?string} [access=null] - The access type.
   */
  constructor(e, t, s, r = null) {
    super(e, t, s, r), this.isSampledCubeTexture = !0;
  }
}
class ta extends _i {
  /**
   * Constructs a new node-based sampled 3D texture.
   *
   * @param {string} name - The textures's name.
   * @param {TextureNode} textureNode - The texture node.
   * @param {UniformGroupNode} groupNode - The uniform group node.
   * @param {?string} [access=null] - The access type.
   */
  constructor(e, t, s, r = null) {
    super(e, t, s, r), this.isSampledTexture3D = !0;
  }
}
const dl = {
  bitcast_int_uint: new Ee(
    /* glsl */
    "uint tsl_bitcast_int_to_uint ( int x ) { return floatBitsToUint( intBitsToFloat ( x ) ); }"
  ),
  bitcast_uint_int: new Ee(
    /* glsl */
    "uint tsl_bitcast_uint_to_int ( uint x ) { return floatBitsToInt( uintBitsToFloat ( x ) ); }"
  )
}, IS = {
  textureDimensions: "textureSize",
  equals: "equal",
  bitcast_float_int: "floatBitsToInt",
  bitcast_int_float: "intBitsToFloat",
  bitcast_uint_float: "uintBitsToFloat",
  bitcast_float_uint: "floatBitsToUint",
  bitcast_uint_int: "tsl_bitcast_uint_to_int",
  bitcast_int_uint: "tsl_bitcast_int_to_uint",
  floatpack_snorm_2x16: "packSnorm2x16",
  floatpack_unorm_2x16: "packUnorm2x16",
  floatpack_float16_2x16: "packHalf2x16",
  floatunpack_snorm_2x16: "unpackSnorm2x16",
  floatunpack_unorm_2x16: "unpackUnorm2x16",
  floatunpack_float16_2x16: "unpackHalf2x16"
}, hl = {
  low: "lowp",
  medium: "mediump",
  high: "highp"
}, fl = {
  swizzleAssign: !0,
  storageBuffer: !1
}, pl = {
  perspective: "smooth",
  linear: "noperspective"
}, gl = {
  centroid: "centroid"
}, ml = `
precision highp float;
precision highp int;
precision highp sampler2D;
precision highp sampler3D;
precision highp samplerCube;
precision highp sampler2DArray;

precision highp usampler2D;
precision highp usampler3D;
precision highp usamplerCube;
precision highp usampler2DArray;

precision highp isampler2D;
precision highp isampler3D;
precision highp isamplerCube;
precision highp isampler2DArray;

precision highp sampler2DShadow;
precision highp sampler2DArrayShadow;
precision highp samplerCubeShadow;
`;
class OS extends ah {
  /**
   * Constructs a new GLSL node builder renderer.
   *
   * @param {Object3D} object - The 3D object.
   * @param {Renderer} renderer - The renderer.
   */
  constructor(e, t) {
    super(e, t, new hS()), this.uniformGroups = {}, this.transforms = [], this.extensions = {}, this.builtins = { vertex: [], fragment: [], compute: [] };
  }
  /**
   * Checks if the given texture requires a manual conversion to the working color space.
   *
   * @param {Texture} texture - The texture to check.
   * @return {boolean} Whether the given texture requires a conversion to working color space or not.
   */
  needsToWorkingColorSpace(e) {
    return e.isVideoTexture === !0 && e.colorSpace !== ir;
  }
  /**
   * Includes the given method name into the current
   * function node.
   *
   * @private
   * @param {string} name - The method name to include.
   * @return {CodeNode} The respective code node.
   */
  _include(e) {
    const t = dl[e];
    return t.build(this), this.addInclude(t), t;
  }
  /**
   * Returns the native shader method name for a given generic name.
   *
   * @param {string} method - The method name to resolve.
   * @return {string} The resolved GLSL method name.
   */
  getMethod(e) {
    return dl[e] !== void 0 && this._include(e), IS[e] || e;
  }
  /**
   * Returns the bitcast method name for a given input and outputType.
   *
   * @param {string} type - The output type to bitcast to.
   * @param {string} inputType - The input type of the.
   * @return {string} The resolved WGSL bitcast invocation.
   */
  getBitcastMethod(e, t) {
    return this.getMethod(`bitcast_${t}_${e}`);
  }
  /**
   * Returns the float packing method name for a given numeric encoding.
   *
   * @param {string} encoding - The numeric encoding that describes how the float values are mapped to the integer range.
   * @returns {string} The resolved GLSL float packing method name.
   */
  getFloatPackingMethod(e) {
    return this.getMethod(`floatpack_${e}_2x16`);
  }
  /**
   * Returns the float unpacking method name for a given numeric encoding.
   *
   * @param {string} encoding - The numeric encoding that describes how the integer values are mapped to the float range.
   * @returns {string} The resolved GLSL float unpacking method name.
   */
  getFloatUnpackingMethod(e) {
    return this.getMethod(`floatunpack_${e}_2x16`);
  }
  /**
   * Returns the native snippet for a ternary operation.
   *
   * @param {string} condSnippet - The condition determining which expression gets resolved.
   * @param {string} ifSnippet - The expression to resolve to if the condition is true.
   * @param {string} elseSnippet - The expression to resolve to if the condition is false.
   * @return {string} The resolved method name.
   */
  getTernary(e, t, s) {
    return `${e} ? ${t} : ${s}`;
  }
  /**
   * Returns the output struct name. Not relevant for GLSL.
   *
   * @return {string}
   */
  getOutputStructName() {
    return "";
  }
  /**
   * Builds the given shader node.
   *
   * @param {ShaderNodeInternal} shaderNode - The shader node.
   * @return {string} The GLSL function code.
   */
  buildFunctionCode(e) {
    const t = e.layout, s = this.flowShaderNode(e), r = [];
    for (const i of t.inputs)
      r.push(this.getType(i.type) + " " + i.name);
    return `${this.getType(t.type)} ${t.name}( ${r.join(", ")} ) {

	${s.vars}

${s.code}
	return ${s.result};

}`;
  }
  /**
   * Setups the Pixel Buffer Object (PBO) for the given storage
   * buffer node.
   *
   * @param {StorageBufferNode} storageBufferNode - The storage buffer node.
   */
  setupPBO(e) {
    const t = e.value;
    if (t.pbo === void 0) {
      const s = t.array, r = t.count * t.itemSize, { itemSize: n } = t, i = t.array.constructor.name.toLowerCase().includes("int");
      let o = i ? ti : ei;
      n === 2 ? o = i ? si : Rt : n === 3 ? o = i ? Ll : ri : n === 4 && (o = i ? ca : ct);
      const u = {
        Float32Array: De,
        Uint8Array: $e,
        Uint16Array: Ss,
        Uint32Array: _e,
        Int8Array: Ys,
        Int16Array: js,
        Int32Array: Ue,
        Uint8ClampedArray: $e
      }, l = Math.pow(2, Math.ceil(Math.log2(Math.sqrt(r / n))));
      let c = Math.ceil(r / n / l);
      l * c * n < r && c++;
      const d = l * c * n, h = new s.constructor(d);
      h.set(s, 0), t.array = h;
      const f = new fc(t.array, l, c, o, u[t.array.constructor.name] || De);
      f.needsUpdate = !0, f.isPBOTexture = !0;
      const p = new Zr(f, null, null);
      p.setPrecision("high"), t.pboNode = p, t.pbo = p.value, this.getUniformFromNode(t.pboNode, "texture", this.shaderStage, this.context.nodeName);
    }
  }
  /**
   * Returns a GLSL snippet that represents the property name of the given node.
   *
   * @param {Node} node - The node.
   * @param {string} [shaderStage=this.shaderStage] - The shader stage this code snippet is generated for.
   * @return {string} The property name.
   */
  getPropertyName(e, t = this.shaderStage) {
    return e.isNodeUniform && e.node.isTextureNode !== !0 && e.node.isBufferNode !== !0 ? e.name : super.getPropertyName(e, t);
  }
  /**
   * Setups the Pixel Buffer Object (PBO) for the given storage
   * buffer node.
   *
   * @param {StorageArrayElementNode} storageArrayElementNode - The storage array element node.
   * @return {string} The property name.
   */
  generatePBO(e) {
    const { node: t, indexNode: s } = e, r = t.value;
    if (this.renderer.backend.has(r)) {
      const c = this.renderer.backend.get(r);
      c.pbo = r.pbo;
    }
    const n = this.getUniformFromNode(r.pboNode, "texture", this.shaderStage, this.context.nodeName), i = this.getPropertyName(n);
    this.increaseUsage(s);
    const o = s.build(this, "uint"), u = this.getDataFromNode(e);
    let l = u.propertyName;
    if (l === void 0) {
      const c = this.getVarFromNode(e);
      l = this.getPropertyName(c);
      const d = this.getDataFromNode(t);
      let h = d.propertySizeName;
      h === void 0 && (h = l + "Size", this.getVarFromNode(t, h, "uint"), this.addLineFlowCode(`${h} = uint( textureSize( ${i}, 0 ).x )`, e), d.propertySizeName = h);
      const { itemSize: f } = r, p = "." + cr.join("").slice(0, f), g = `ivec2(${o} % ${h}, ${o} / ${h})`, m = this.generateTextureLoad(null, i, g, "0", null, null);
      let y = "vec4";
      r.pbo.type === _e ? y = "uvec4" : r.pbo.type === Ue && (y = "ivec4"), this.addLineFlowCode(`${l} = ${y}(${m})${p}`, e), u.propertyName = l;
    }
    return l;
  }
  /**
   * Generates the GLSL snippet that reads a single texel from a texture without sampling or filtering.
   *
   * @param {?Texture} texture - The texture.
   * @param {string} textureProperty - The name of the texture uniform in the shader.
   * @param {string} uvIndexSnippet - A GLSL snippet that represents texture coordinates used for sampling.
   * @param {?string} levelSnippet - A GLSL snippet that represents the mip level, with level 0 containing a full size version of the texture.
   * @param {?string} depthSnippet - A GLSL snippet that represents the 0-based texture array index to sample.
   * @param {?string} offsetSnippet - A GLSL snippet that represents the offset that will be applied to the unnormalized texture coordinate before sampling the texture.
   * @return {string} The GLSL snippet.
   */
  generateTextureLoad(e, t, s, r, n, i) {
    r === null && (r = "0");
    let o;
    return n ? i ? o = `texelFetchOffset( ${t}, ivec3( ${s}, ${n} ), int( ${r} ), ${i} )` : o = `texelFetch( ${t}, ivec3( ${s}, ${n} ), int( ${r} ) )` : i ? o = `texelFetchOffset( ${t}, ${s}, int( ${r} ), ${i} )` : o = `texelFetch( ${t}, ${s}, int( ${r} ) )`, e !== null && e.isDepthTexture && (o += ".x"), o;
  }
  /**
   * Generates the GLSL snippet for sampling/loading the given texture.
   *
   * @param {Texture} texture - The texture.
   * @param {string} textureProperty - The name of the texture uniform in the shader.
   * @param {string} uvSnippet - A GLSL snippet that represents texture coordinates used for sampling.
   * @param {?string} depthSnippet -  A GLSL snippet that represents the 0-based texture array index to sample.
   * @param {?string} offsetSnippet - A GLSL snippet that represents the offset that will be applied to the unnormalized texture coordinate before sampling the texture.
   * @return {string} The GLSL snippet.
   */
  generateTexture(e, t, s, r, n) {
    return r && (s = `vec3( ${s}, ${r} )`), e.isDepthTexture ? n ? `textureOffset( ${t}, ${s}, ${n} ).x` : `texture( ${t}, ${s} ).x` : n ? `textureOffset( ${t}, ${s}, ${n} )` : `texture( ${t}, ${s} )`;
  }
  /**
   * Generates the GLSL snippet when sampling textures with explicit mip level.
   *
   * @param {Texture} texture - The texture.
   * @param {string} textureProperty - The name of the texture uniform in the shader.
   * @param {string} uvSnippet - A GLSL snippet that represents texture coordinates used for sampling.
   * @param {string} levelSnippet - A GLSL snippet that represents the mip level, with level 0 containing a full size version of the texture.
   * @param {?string} depthSnippet - A GLSL snippet that represents 0-based texture array index to sample.
   * @param {?string} offsetSnippet - A GLSL snippet that represents the offset that will be applied to the unnormalized texture coordinate before sampling the texture.
   * @return {string} The GLSL snippet.
   */
  generateTextureLevel(e, t, s, r, n, i) {
    return n && (s = `vec3( ${s}, ${n} )`), i ? `textureLodOffset( ${t}, ${s}, ${r}, ${i} )` : `textureLod( ${t}, ${s}, ${r} )`;
  }
  /**
   * Generates the GLSL snippet when sampling textures with a bias to the mip level.
   *
   * @param {Texture} texture - The texture.
   * @param {string} textureProperty - The name of the texture uniform in the shader.
   * @param {string} uvSnippet - A GLSL snippet that represents texture coordinates used for sampling.
   * @param {string} biasSnippet - A GLSL snippet that represents the bias to apply to the mip level before sampling.
   * @param {?string} depthSnippet - A GLSL snippet that represents 0-based texture array index to sample.
   * @param {?string} offsetSnippet - A GLSL snippet that represents the offset that will be applied to the unnormalized texture coordinate before sampling the texture.
   * @return {string} The GLSL snippet.
   */
  generateTextureBias(e, t, s, r, n, i) {
    return n && (s = `vec3( ${s}, ${n} )`), i ? `textureOffset( ${t}, ${s}, ${i}, ${r} )` : `texture( ${t}, ${s}, ${r} )`;
  }
  /**
   * Generates the GLSL snippet for sampling/loading the given texture using explicit gradients.
   *
   * @param {Texture} texture - The texture.
   * @param {string} textureProperty - The name of the texture uniform in the shader.
   * @param {string} uvSnippet - A GLSL snippet that represents texture coordinates used for sampling.
   * @param {Array<string>} gradSnippet - An array holding both gradient GLSL snippets.
   * @param {?string} depthSnippet - A GLSL snippet that represents 0-based texture array index to sample.
   * @param {?string} offsetSnippet - A GLSL snippet that represents the offset that will be applied to the unnormalized texture coordinate before sampling the texture.
   * @return {string} The GLSL snippet.
   */
  generateTextureGrad(e, t, s, r, n, i) {
    return n && (s = `vec3( ${s}, ${n} )`), i ? `textureGradOffset( ${t}, ${s}, ${r[0]}, ${r[1]}, ${i} )` : `textureGrad( ${t}, ${s}, ${r[0]}, ${r[1]} )`;
  }
  /**
   * Generates the GLSL snippet for sampling a depth texture and comparing the sampled depth values
   * against a reference value.
   *
   * @param {Texture} texture - The texture.
   * @param {string} textureProperty - The name of the texture uniform in the shader.
   * @param {string} uvSnippet - A GLSL snippet that represents texture coordinates used for sampling.
   * @param {string} compareSnippet -  A GLSL snippet that represents the reference value.
   * @param {?string} depthSnippet - A GLSL snippet that represents 0-based texture array index to sample.
   * @param {?string} offsetSnippet - A GLSL snippet that represents the offset that will be applied to the unnormalized texture coordinate before sampling the texture.
   * @param {string} [shaderStage=this.shaderStage] - The shader stage this code snippet is generated for.
   * @return {string} The GLSL snippet.
   */
  generateTextureCompare(e, t, s, r, n, i, o = this.shaderStage) {
    if (o === "fragment")
      return e.isCubeTexture ? `texture( ${t}, vec4( ${s}, ${r} ) )` : n ? i ? `textureOffset( ${t}, vec4( ${s}, ${n}, ${r} ), ${i} )` : `texture( ${t}, vec4( ${s}, ${n}, ${r} ) )` : i ? `textureOffset( ${t}, vec3( ${s}, ${r} ), ${i} )` : `texture( ${t}, vec3( ${s}, ${r} ) )`;
    F(`WebGPURenderer: THREE.DepthTexture.compareFunction() does not support ${o} shader.`);
  }
  /**
   * Returns the uniforms of the given shader stage as a GLSL string.
   *
   * @param {string} shaderStage - The shader stage.
   * @return {string} The GLSL snippet that defines the uniforms.
   */
  getUniforms(e) {
    const t = this.uniforms[e], s = [], r = {};
    for (const i of t) {
      let o = null, u = !1;
      if (i.type === "texture" || i.type === "texture3D") {
        const l = i.node.value;
        let c = "";
        (l.isDataTexture === !0 || l.isData3DTexture === !0) && (l.type === _e ? c = "u" : l.type === Ue && (c = "i")), i.type === "texture3D" && l.isArrayTexture === !1 ? o = `${c}sampler3D ${i.name};` : l.compareFunction ? l.isArrayTexture === !0 ? o = `sampler2DArrayShadow ${i.name};` : o = `sampler2DShadow ${i.name};` : l.isArrayTexture === !0 || l.isDataArrayTexture === !0 || l.isCompressedArrayTexture === !0 ? o = `${c}sampler2DArray ${i.name};` : o = `${c}sampler2D ${i.name};`;
      } else if (i.type === "cubeTexture")
        o = `samplerCube ${i.name};`;
      else if (i.type === "cubeDepthTexture")
        i.node.value.compareFunction ? o = `samplerCubeShadow ${i.name};` : o = `samplerCube ${i.name};`;
      else if (i.type === "buffer") {
        const l = i.node, c = this.getType(l.bufferType), d = l.bufferCount, h = d > 0 ? d : "";
        o = `${l.name} {
	${c} ${i.name}[${h}];
};
`;
      } else {
        const l = i.groupNode.name;
        if (r[l] === void 0) {
          const c = this.uniformGroups[l];
          if (c !== void 0) {
            const d = [];
            for (const h of c.uniforms) {
              const f = h.getType(), p = this.getVectorType(f), g = h.nodeUniform.node.precision;
              let m = `${p} ${h.name};`;
              g !== null && (m = hl[g] + " " + m), d.push("	" + m);
            }
            r[l] = d;
          }
        }
        u = !0;
      }
      if (!u) {
        const l = i.node.precision;
        l !== null && (o = hl[l] + " " + o), o = "uniform " + o, s.push(o);
      }
    }
    let n = "";
    for (const i in r) {
      const o = r[i];
      n += this._getGLSLUniformStruct(i, o.join(`
`)) + `
`;
    }
    return n += s.join(`
`), n;
  }
  /**
   * Returns the type for a given buffer attribute.
   *
   * @param {BufferAttribute} attribute - The buffer attribute.
   * @return {string} The type.
   */
  getTypeFromAttribute(e) {
    let t = super.getTypeFromAttribute(e);
    if (/^[iu]/.test(t) && e.gpuType !== Ue) {
      let s = e;
      e.isInterleavedBufferAttribute && (s = e.data);
      const r = s.array;
      r instanceof Uint32Array || r instanceof Int32Array || (t = t.slice(1));
    }
    return t;
  }
  /**
   * Returns the shader attributes of the given shader stage as a GLSL string.
   *
   * @param {string} shaderStage - The shader stage.
   * @return {string} The GLSL snippet that defines the shader attributes.
   */
  getAttributes(e) {
    let t = "";
    if (e === "vertex" || e === "compute") {
      const s = this.getAttributesArray();
      let r = 0;
      for (const n of s)
        t += `layout( location = ${r++} ) in ${n.type} ${n.name};
`;
    }
    return t;
  }
  /**
   * Returns the members of the given struct type node as a GLSL string.
   *
   * @param {StructTypeNode} struct - The struct type node.
   * @return {string} The GLSL snippet that defines the struct members.
   */
  getStructMembers(e) {
    const t = [];
    for (const s of e.members)
      t.push(`	${s.type} ${s.name};`);
    return t.join(`
`);
  }
  /**
   * Returns the structs of the given shader stage as a GLSL string.
   *
   * @param {string} shaderStage - The shader stage.
   * @return {string} The GLSL snippet that defines the structs.
   */
  getStructs(e) {
    const t = [], s = this.structs[e], r = [];
    for (const n of s)
      if (n.output)
        for (const i of n.members)
          r.push(`layout( location = ${i.index} ) out ${i.type} ${i.name};`);
      else {
        let i = "struct " + n.name + ` {
`;
        i += this.getStructMembers(n), i += `
};
`, t.push(i);
      }
    return r.length === 0 && r.push("layout( location = 0 ) out vec4 fragColor;"), `
` + r.join(`
`) + `

` + t.join(`
`);
  }
  /**
   * Returns the varyings of the given shader stage as a GLSL string.
   *
   * @param {string} shaderStage - The shader stage.
   * @return {string} The GLSL snippet that defines the varyings.
   */
  getVaryings(e) {
    let t = "";
    const s = this.varyings;
    if (e === "vertex" || e === "compute")
      for (const r of s) {
        e === "compute" && (r.needsInterpolation = !0);
        const n = this.getType(r.type);
        if (r.needsInterpolation)
          if (r.interpolationType) {
            const i = pl[r.interpolationType] || r.interpolationType, o = gl[r.interpolationSampling] || "";
            t += `${i} ${o} out ${n} ${r.name};
`;
          } else {
            const i = n.includes("int") || n.includes("uv") || n.includes("iv") ? "flat " : "";
            t += `${i}out ${n} ${r.name};
`;
          }
        else
          t += `${n} ${r.name};
`;
      }
    else if (e === "fragment") {
      for (const r of s)
        if (r.needsInterpolation) {
          const n = this.getType(r.type);
          if (r.interpolationType) {
            const i = pl[r.interpolationType] || r.interpolationType, o = gl[r.interpolationSampling] || "";
            t += `${i} ${o} in ${n} ${r.name};
`;
          } else {
            const i = n.includes("int") || n.includes("uv") || n.includes("iv") ? "flat " : "";
            t += `${i}in ${n} ${r.name};
`;
          }
        }
    }
    for (const r of this.builtins[e])
      t += `${r};
`;
    return t;
  }
  /**
   * Returns the vertex index builtin.
   *
   * @return {string} The vertex index.
   */
  getVertexIndex() {
    return "uint( gl_VertexID )";
  }
  /**
   * Contextually returns either the vertex stage instance index builtin
   * or the linearized index of an compute invocation within a grid of workgroups.
   *
   * @return {string} The instance index.
   */
  getInstanceIndex() {
    return "uint( gl_InstanceID )";
  }
  /**
   * Returns a builtin representing the index of an invocation within its workgroup.
   *
   * @return {string} The invocation local index.
   */
  getInvocationLocalIndex() {
    return `uint( gl_InstanceID ) % ${this.object.workgroupSize.reduce((s, r) => s * r, 1)}u`;
  }
  /**
   * Returns a builtin representing the size of a subgroup within the current shader.
   */
  getSubgroupSize() {
    F("GLSLNodeBuilder: WebGLBackend does not support the subgroupSize node");
  }
  /**
   * Returns a builtin representing the index of an invocation within its subgroup.
   */
  getInvocationSubgroupIndex() {
    F("GLSLNodeBuilder: WebGLBackend does not support the invocationSubgroupIndex node");
  }
  /**
   * Returns a builtin representing the index of the current invocation's subgroup within its workgroup.
   */
  getSubgroupIndex() {
    F("GLSLNodeBuilder: WebGLBackend does not support the subgroupIndex node");
  }
  /**
   * Returns the draw index builtin.
   *
   * @return {?string} The drawIndex shader string. Returns `null` if `WEBGL_multi_draw` isn't supported by the device.
   */
  getDrawIndex() {
    return this.renderer.backend.extensions.has("WEBGL_multi_draw") ? "uint( gl_DrawID )" : "nodeUniformDrawId";
  }
  /**
   * Returns the front facing builtin.
   *
   * @return {string} The front facing builtin.
   */
  getFrontFacing() {
    return "gl_FrontFacing";
  }
  /**
   * Returns the frag coord builtin.
   *
   * @return {string} The frag coord builtin.
   */
  getFragCoord() {
    return "gl_FragCoord.xy";
  }
  /**
   * Returns the frag depth builtin.
   *
   * @return {string} The frag depth builtin.
   */
  getFragDepth() {
    return "gl_FragDepth";
  }
  /**
   * Enables the given extension.
   *
   * @param {string} name - The extension name.
   * @param {string} behavior - The extension behavior.
   * @param {string} [shaderStage=this.shaderStage] - The shader stage.
   */
  enableExtension(e, t, s = this.shaderStage) {
    const r = this.extensions[s] || (this.extensions[s] = /* @__PURE__ */ new Map());
    r.has(e) === !1 && r.set(e, {
      name: e,
      behavior: t
    });
  }
  /**
   * Returns the enabled extensions of the given shader stage as a GLSL string.
   *
   * @param {string} shaderStage - The shader stage.
   * @return {string} The GLSL snippet that defines the enabled extensions.
   */
  getExtensions(e) {
    const t = [];
    if (e === "vertex") {
      const r = this.renderer.backend.extensions;
      this.object.isBatchedMesh && r.has("WEBGL_multi_draw") && this.enableExtension("GL_ANGLE_multi_draw", "require", e);
    }
    const s = this.extensions[e];
    if (s !== void 0)
      for (const { name: r, behavior: n } of s.values())
        t.push(`#extension ${r} : ${n}`);
    return t.join(`
`);
  }
  /**
   * Returns the clip distances builtin.
   *
   * @return {string} The clip distances builtin.
   */
  getClipDistance() {
    return "gl_ClipDistance";
  }
  /**
   * Whether the requested feature is available or not.
   *
   * @param {string} name - The requested feature.
   * @return {boolean} Whether the requested feature is supported or not.
   */
  isAvailable(e) {
    let t = fl[e];
    if (t === void 0) {
      let s;
      switch (t = !1, e) {
        case "float32Filterable":
          s = "OES_texture_float_linear";
          break;
        case "clipDistance":
          s = "WEBGL_clip_cull_distance";
          break;
      }
      if (s !== void 0) {
        const r = this.renderer.backend.extensions;
        r.has(s) && (r.get(s), t = !0);
      }
      fl[e] = t;
    }
    return t;
  }
  /**
   * Whether to flip texture data along its vertical axis or not.
   *
   * @return {boolean} Returns always `true` in context of GLSL.
   */
  isFlipY() {
    return !0;
  }
  /**
   * Enables hardware clipping.
   *
   * @param {string} planeCount - The clipping plane count.
   */
  enableHardwareClipping(e) {
    this.enableExtension("GL_ANGLE_clip_cull_distance", "require"), this.builtins.vertex.push(`out float gl_ClipDistance[ ${e} ]`);
  }
  /**
   * Enables multiview.
   */
  enableMultiview() {
    this.enableExtension("GL_OVR_multiview2", "require", "fragment"), this.enableExtension("GL_OVR_multiview2", "require", "vertex"), this.builtins.vertex.push("layout(num_views = 2) in");
  }
  /**
   * Registers a transform in context of Transform Feedback.
   *
   * @param {string} varyingName - The varying name.
   * @param {AttributeNode} attributeNode - The attribute node.
   */
  registerTransform(e, t) {
    this.transforms.push({ varyingName: e, attributeNode: t });
  }
  /**
   * Returns the transforms of the given shader stage as a GLSL string.
   *
   * @param {string} shaderStage - The shader stage.
   * @return {string} The GLSL snippet that defines the transforms.
   */
  getTransforms() {
    const e = this.transforms;
    let t = "";
    for (let s = 0; s < e.length; s++) {
      const r = e[s], n = this.getPropertyName(r.attributeNode);
      n && (t += `${r.varyingName} = ${n};
	`);
    }
    return t;
  }
  /**
   * Returns a GLSL struct based on the given name and variables.
   *
   * @private
   * @param {string} name - The struct name.
   * @param {string} vars - The struct variables.
   * @return {string} The GLSL snippet representing a struct.
   */
  _getGLSLUniformStruct(e, t) {
    return `
layout( std140 ) uniform ${e} {
${t}
};`;
  }
  /**
   * Returns a GLSL vertex shader based on the given shader data.
   *
   * @private
   * @param {Object} shaderData - The shader data.
   * @return {string} The vertex shader.
   */
  _getGLSLVertexCode(e) {
    return `#version 300 es

${this.getSignature()}

// extensions
${e.extensions}

// precision
${ml}

// uniforms
${e.uniforms}

// varyings
${e.varyings}

// attributes
${e.attributes}

// vars
${e.vars}

// codes
${e.codes}

void main() {

	// transforms
	${e.transforms}

	// flow
	${e.flow}

	gl_PointSize = 1.0;

}
`;
  }
  /**
   * Returns a GLSL fragment shader based on the given shader data.
   *
   * @private
   * @param {Object} shaderData - The shader data.
   * @return {string} The vertex shader.
   */
  _getGLSLFragmentCode(e) {
    return `#version 300 es

${this.getSignature()}

// extensions
${e.extensions}

// precision
${ml}

// structs
${e.structs}

// uniforms
${e.uniforms}

// varyings
${e.varyings}

// vars
${e.vars}

// codes
${e.codes}

void main() {

	// flow
	${e.flow}

}
`;
  }
  /**
   * Controls the code build of the shader stages.
   */
  buildCode() {
    const e = this.material !== null ? { fragment: {}, vertex: {} } : { compute: {} };
    this.sortBindingGroups();
    for (const t in e) {
      let s = `// code

`;
      s += this.flowCode[t];
      const r = this.flowNodes[t], n = r[r.length - 1];
      for (const o of r) {
        const u = this.getFlowData(
          o
          /*, shaderStage*/
        ), l = o.name;
        l && (s.length > 0 && (s += `
`), s += `	// flow -> ${l}
	`), s += `${u.code}
	`, o === n && t !== "compute" && (s += `// result
	`, t === "vertex" ? (s += "gl_Position = ", s += `${u.result};`) : t === "fragment" && (o.outputNode.isOutputStructNode || (s += "fragColor = ", s += `${u.result};`)));
      }
      const i = e[t];
      if (i.extensions = this.getExtensions(t), i.uniforms = this.getUniforms(t), i.attributes = this.getAttributes(t), i.varyings = this.getVaryings(t), i.vars = this.getVars(t, !0), i.structs = this.getStructs(t), i.codes = this.getCodes(t), i.transforms = this.getTransforms(t), i.flow = s, t === "vertex") {
        const o = this.renderer.backend.extensions;
        this.object.isBatchedMesh && o.has("WEBGL_multi_draw") === !1 && (i.uniforms += `
uniform uint nodeUniformDrawId;
`);
      }
    }
    this.material !== null ? (this.vertexShader = this._getGLSLVertexCode(e.vertex), this.fragmentShader = this._getGLSLFragmentCode(e.fragment)) : this.computeShader = this._getGLSLVertexCode(e.compute);
  }
  /**
   * This method is one of the more important ones since it's responsible
   * for generating a matching binding instance for the given uniform node.
   *
   * These bindings are later used in the renderer to create bind groups
   * and layouts.
   *
   * @param {UniformNode} node - The uniform node.
   * @param {string} type - The node data type.
   * @param {string} shaderStage - The shader stage.
   * @param {?string} [name=null] - An optional uniform name.
   * @return {NodeUniform} The node uniform object.
   */
  getUniformFromNode(e, t, s, r = null) {
    const n = super.getUniformFromNode(e, t, s, r), i = this.getDataFromNode(e, s, this.globalCache);
    let o = i.uniformGPU;
    if (o === void 0) {
      const u = e.groupNode, l = u.name, c = this.getBindGroupArray(l, s);
      if (t === "texture")
        o = new _i(n.name, n.node, u), c.push(o);
      else if (t === "cubeTexture" || t === "cubeDepthTexture")
        o = new mh(n.name, n.node, u), c.push(o);
      else if (t === "texture3D")
        o = new ta(n.name, n.node, u), c.push(o);
      else if (t === "buffer") {
        n.name = `buffer${e.id}`;
        const d = this.getSharedDataFromNode(e);
        let h = d.buffer;
        h === void 0 && (e.name = `NodeBuffer_${e.id}`, h = new fh(e, u), h.name = e.name, d.buffer = h), c.push(h), o = h;
      } else {
        let d = this.uniformGroups[l];
        d === void 0 ? (d = new ph(l, u), this.uniformGroups[l] = d, c.push(d)) : c.indexOf(d) === -1 && c.push(d), o = this.getNodeUniform(n, t);
        const h = o.name;
        d.uniforms.some((p) => p.name === h) || d.addUniform(o);
      }
      i.uniformGPU = o;
    }
    return n;
  }
}
let oo = null, zs = null;
class xh {
  /**
   * Constructs a new backend.
   *
   * @param {Object} parameters - An object holding parameters for the backend.
   */
  constructor(e = {}) {
    this.parameters = Object.assign({}, e), this.data = /* @__PURE__ */ new WeakMap(), this.renderer = null, this.domElement = null, this.timestampQueryPool = {
      [Nt.RENDER]: null,
      [Nt.COMPUTE]: null
    }, this.trackTimestamp = e.trackTimestamp === !0;
  }
  /**
   * Initializes the backend so it is ready for usage. Concrete backends
   * are supposed to implement their rendering context creation and related
   * operations in this method.
   *
   * @async
   * @param {Renderer} renderer - The renderer.
   * @return {Promise} A Promise that resolves when the backend has been initialized.
   */
  async init(e) {
    this.renderer = e;
  }
  /**
   * The coordinate system of the backend.
   *
   * @abstract
   * @type {number}
   * @readonly
   */
  get coordinateSystem() {
  }
  // render context
  /**
   * This method is executed at the beginning of a render call and
   * can be used by the backend to prepare the state for upcoming
   * draw calls.
   *
   * @abstract
   * @param {RenderContext} renderContext - The render context.
   */
  beginRender() {
  }
  /**
   * This method is executed at the end of a render call and
   * can be used by the backend to finalize work after draw
   * calls.
   *
   * @abstract
   * @param {RenderContext} renderContext - The render context.
   */
  finishRender() {
  }
  /**
   * This method is executed at the beginning of a compute call and
   * can be used by the backend to prepare the state for upcoming
   * compute tasks.
   *
   * @abstract
   * @param {Node|Array<Node>} computeGroup - The compute node(s).
   */
  beginCompute() {
  }
  /**
   * This method is executed at the end of a compute call and
   * can be used by the backend to finalize work after compute
   * tasks.
   *
   * @abstract
   * @param {Node|Array<Node>} computeGroup - The compute node(s).
   */
  finishCompute() {
  }
  // render object
  /**
   * Executes a draw command for the given render object.
   *
   * @abstract
   * @param {RenderObject} renderObject - The render object to draw.
   * @param {Info} info - Holds a series of statistical information about the GPU memory and the rendering process.
   */
  draw() {
  }
  // compute node
  /**
   * Executes a compute command for the given compute node.
   *
   * @abstract
   * @param {Node|Array<Node>} computeGroup - The group of compute nodes of a compute call. Can be a single compute node.
   * @param {Node} computeNode - The compute node.
   * @param {Array<BindGroup>} bindings - The bindings.
   * @param {ComputePipeline} computePipeline - The compute pipeline.
   */
  compute() {
  }
  // program
  /**
   * Creates a shader program from the given programmable stage.
   *
   * @abstract
   * @param {ProgrammableStage} program - The programmable stage.
   */
  createProgram() {
  }
  /**
   * Destroys the shader program of the given programmable stage.
   *
   * @abstract
   * @param {ProgrammableStage} program - The programmable stage.
   */
  destroyProgram() {
  }
  // bindings
  /**
   * Creates bindings from the given bind group definition.
   *
   * @abstract
   * @param {BindGroup} bindGroup - The bind group.
   * @param {Array<BindGroup>} bindings - Array of bind groups.
   * @param {number} cacheIndex - The cache index.
   * @param {number} version - The version.
   */
  createBindings() {
  }
  /**
   * Updates the given bind group definition.
   *
   * @abstract
   * @param {BindGroup} bindGroup - The bind group.
   * @param {Array<BindGroup>} bindings - Array of bind groups.
   * @param {number} cacheIndex - The cache index.
   * @param {number} version - The version.
   */
  updateBindings() {
  }
  /**
   * Updates a buffer binding.
   *
   * @abstract
   * @param {Buffer} binding - The buffer binding to update.
   */
  updateBinding() {
  }
  // pipeline
  /**
   * Creates a render pipeline for the given render object.
   *
   * @abstract
   * @param {RenderObject} renderObject - The render object.
   * @param {Array<Promise>} promises - An array of compilation promises which are used in `compileAsync()`.
   */
  createRenderPipeline() {
  }
  /**
   * Creates a compute pipeline for the given compute node.
   *
   * @abstract
   * @param {ComputePipeline} computePipeline - The compute pipeline.
   * @param {Array<BindGroup>} bindings - The bindings.
   */
  createComputePipeline() {
  }
  // cache key
  /**
   * Returns `true` if the render pipeline requires an update.
   *
   * @abstract
   * @param {RenderObject} renderObject - The render object.
   * @return {boolean} Whether the render pipeline requires an update or not.
   */
  needsRenderUpdate() {
  }
  /**
   * Returns a cache key that is used to identify render pipelines.
   *
   * @abstract
   * @param {RenderObject} renderObject - The render object.
   * @return {string} The cache key.
   */
  getRenderCacheKey() {
  }
  // node builder
  /**
   * Returns a node builder for the given render object.
   *
   * @abstract
   * @param {RenderObject} renderObject - The render object.
   * @param {Renderer} renderer - The renderer.
   * @return {NodeBuilder} The node builder.
   */
  createNodeBuilder() {
  }
  // textures
  /**
   * Updates a GPU sampler for the given texture.
   *
   * @abstract
   * @param {Texture} texture - The texture to update the sampler for.
   * @return {string} The current sampler key.
   */
  updateSampler() {
  }
  /**
   * Creates a default texture for the given texture that can be used
   * as a placeholder until the actual texture is ready for usage.
   *
   * @abstract
   * @param {Texture} texture - The texture to create a default texture for.
   */
  createDefaultTexture() {
  }
  /**
   * Defines a texture on the GPU for the given texture object.
   *
   * @abstract
   * @param {Texture} texture - The texture.
   * @param {Object} [options={}] - Optional configuration parameter.
   */
  createTexture() {
  }
  /**
   * Uploads the updated texture data to the GPU.
   *
   * @abstract
   * @param {Texture} texture - The texture.
   * @param {Object} [options={}] - Optional configuration parameter.
   */
  updateTexture() {
  }
  /**
   * Generates mipmaps for the given texture.
   *
   * @abstract
   * @param {Texture} texture - The texture.
   */
  generateMipmaps() {
  }
  /**
   * Destroys the GPU data for the given texture object.
   *
   * @abstract
   * @param {Texture} texture - The texture.
   * @param {boolean} [isDefaultTexture=false] - Whether the texture uses a default GPU texture or not.
   */
  destroyTexture() {
  }
  /**
   * Returns texture data as a typed array.
   *
   * @abstract
   * @async
   * @param {Texture} texture - The texture to copy.
   * @param {number} x - The x coordinate of the copy origin.
   * @param {number} y - The y coordinate of the copy origin.
   * @param {number} width - The width of the copy.
   * @param {number} height - The height of the copy.
   * @param {number} faceIndex - The face index.
   * @return {Promise<TypedArray>} A Promise that resolves with a typed array when the copy operation has finished.
   */
  async copyTextureToBuffer() {
  }
  /**
   * Copies data of the given source texture to the given destination texture.
   *
   * @abstract
   * @param {Texture} srcTexture - The source texture.
   * @param {Texture} dstTexture - The destination texture.
   * @param {?(Box3|Box2)} [srcRegion=null] - The region of the source texture to copy.
   * @param {?(Vector2|Vector3)} [dstPosition=null] - The destination position of the copy.
   * @param {number} [srcLevel=0] - The source mip level to copy from.
   * @param {number} [dstLevel=0] - The destination mip level to copy to.
   */
  copyTextureToTexture() {
  }
  /**
  * Copies the current bound framebuffer to the given texture.
  *
  * @abstract
  * @param {Texture} texture - The destination texture.
  * @param {RenderContext} renderContext - The render context.
  * @param {Vector4} rectangle - A four dimensional vector defining the origin and dimension of the copy.
  */
  copyFramebufferToTexture() {
  }
  // attributes
  /**
   * Creates the GPU buffer of a shader attribute.
   *
   * @abstract
   * @param {BufferAttribute} attribute - The buffer attribute.
   */
  createAttribute() {
  }
  /**
   * Creates the GPU buffer of an indexed shader attribute.
   *
   * @abstract
   * @param {BufferAttribute} attribute - The indexed buffer attribute.
   */
  createIndexAttribute() {
  }
  /**
   * Creates the GPU buffer of a storage attribute.
   *
   * @abstract
   * @param {BufferAttribute} attribute - The buffer attribute.
   */
  createStorageAttribute() {
  }
  /**
   * Updates the GPU buffer of a shader attribute.
   *
   * @abstract
   * @param {BufferAttribute} attribute - The buffer attribute to update.
   */
  updateAttribute() {
  }
  /**
   * Destroys the GPU buffer of a shader attribute.
   *
   * @abstract
   * @param {BufferAttribute} attribute - The buffer attribute to destroy.
   */
  destroyAttribute() {
  }
  // canvas
  /**
   * Returns the backend's rendering context.
   *
   * @abstract
   * @return {Object} The rendering context.
   */
  getContext() {
  }
  /**
   * Backends can use this method if they have to run
   * logic when the renderer gets resized.
   *
   * @abstract
   */
  updateSize() {
  }
  /**
   * Updates the viewport with the values from the given render context.
   *
   * @abstract
   * @param {RenderContext} renderContext - The render context.
   */
  updateViewport() {
  }
  // utils
  /**
   * Updates a unique identifier for the given render context that can be used
   * to allocate resources like occlusion queries or timestamp queries.
   *
   * @param {RenderContext|ComputeNode} abstractRenderContext - The render context.
   */
  updateTimeStampUID(e) {
    const t = this.get(e), s = this.renderer.info.frame;
    let r;
    e.isComputeNode === !0 ? r = "c:" + this.renderer.info.compute.frameCalls : r = "r:" + this.renderer.info.render.frameCalls, t.timestampUID = r + ":" + e.id + ":f" + s;
  }
  /**
   * Returns a unique identifier for the given render context that can be used
   * to allocate resources like occlusion queries or timestamp queries.
   *
   * @param {RenderContext|ComputeNode} abstractRenderContext - The render context.
   * @return {string} The unique identifier.
   */
  getTimestampUID(e) {
    return this.get(e).timestampUID;
  }
  /**
   * Returns all timestamp frames for the given type.
   *
   * @param {string} type - The type of the time stamp.
   * @return {Array<number>} The timestamp frames.
   */
  getTimestampFrames(e) {
    const t = this.timestampQueryPool[e];
    return t ? t.getTimestampFrames() : [];
  }
  /**
   * Returns the query pool for the given uid.
   *
   * @param {string} uid - The unique identifier.
   * @return {TimestampQueryPool} The query pool.
   */
  _getQueryPool(e) {
    const t = e.startsWith("c:") ? Nt.COMPUTE : Nt.RENDER;
    return this.timestampQueryPool[t];
  }
  /**
   * Returns the timestamp for the given uid.
   *
   * @param {string} uid - The unique identifier.
   * @return {number} The timestamp.
   */
  getTimestamp(e) {
    return this._getQueryPool(e).getTimestamp(e);
  }
  /**
   * Returns `true` if a timestamp for the given uid is available.
   *
   * @param {string} uid - The unique identifier.
   * @return {boolean} Whether the timestamp is available or not.
   */
  hasTimestamp(e) {
    return this._getQueryPool(e).hasTimestamp(e);
  }
  /**
   * Returns `true` if the given 3D object is fully occluded by other
   * 3D objects in the scene. Backends must implement this method by using
   * a Occlusion Query API.
   *
   * @abstract
   * @param {RenderContext} renderContext - The render context.
   * @param {Object3D} object - The 3D object to test.
   * @return {boolean} Whether the 3D object is fully occluded or not.
   */
  isOccluded() {
  }
  /**
   * Resolves the time stamp for the given render context and type.
   *
   * @async
   * @abstract
   * @param {string} [type='render'] - The type of the time stamp.
   * @return {Promise<number>} A Promise that resolves with the time stamp.
   */
  async resolveTimestampsAsync(e = "render") {
    if (!this.trackTimestamp) {
      Te("WebGPURenderer: Timestamp tracking is disabled.");
      return;
    }
    const t = this.timestampQueryPool[e];
    if (!t)
      return;
    const s = await t.resolveQueriesAsync();
    return this.renderer.info[e].timestamp = s, s;
  }
  /**
   * This method performs a readback operation by moving buffer data from
   * a storage buffer attribute from the GPU to the CPU.
   *
   * @async
   * @param {StorageBufferAttribute} attribute - The storage buffer attribute.
   * @return {Promise<ArrayBuffer>} A promise that resolves with the buffer data when the data are ready.
   */
  async getArrayBufferAsync() {
  }
  /**
   * Checks if the given feature is supported by the backend.
   *
   * @async
   * @abstract
   * @param {string} name - The feature's name.
   * @return {Promise<boolean>} A Promise that resolves with a bool that indicates whether the feature is supported or not.
   */
  async hasFeatureAsync() {
  }
  /**
   * Checks if the given feature is supported  by the backend.
   *
   * @abstract
   * @param {string} name - The feature's name.
   * @return {boolean} Whether the feature is supported or not.
   */
  hasFeature() {
  }
  /**
   * Returns the drawing buffer size.
   *
   * @return {Vector2} The drawing buffer size.
   */
  getDrawingBufferSize() {
    return oo = oo || new je(), this.renderer.getDrawingBufferSize(oo);
  }
  /**
   * Defines the scissor test.
   *
   * @abstract
   * @param {boolean} boolean - Whether the scissor test should be enabled or not.
   */
  setScissorTest() {
  }
  /**
   * Returns the clear color and alpha into a single
   * color object.
   *
   * @return {Color4} The clear color.
   */
  getClearColor() {
    const e = this.renderer;
    return zs = zs || new Pa(), e.getClearColor(zs), zs.getRGB(zs), zs;
  }
  /**
   * Returns the DOM element. If no DOM element exists, the backend
   * creates a new one.
   *
   * @return {HTMLCanvasElement} The DOM element.
   */
  getDomElement() {
    let e = this.domElement;
    return e === null && (e = this.parameters.canvas !== void 0 ? this.parameters.canvas : $h(), "setAttribute" in e && e.setAttribute("data-engine", `three.js r${ua} webgpu`), this.domElement = e), e;
  }
  /**
   * Checks if the backend has the given compatibility.
   *
   * @abstract
   * @param {string} name - The compatibility.
   * @return {boolean} Whether the backend has the given compatibility or not.
   */
  hasCompatibility() {
    return !1;
  }
  /**
   * Initializes the render target defined in the given render context.
   *
   * @abstract
   * @param {RenderContext} renderContext - The render context.
   */
  initRenderTarget() {
  }
  /**
   * Sets a dictionary for the given object into the
   * internal data structure.
   *
   * @param {Object} object - The object.
   * @param {Object} value - The dictionary to set.
   */
  set(e, t) {
    this.data.set(e, t);
  }
  /**
   * Returns the dictionary for the given object.
   *
   * @param {Object} object - The object.
   * @return {Object} The object's dictionary.
   */
  get(e) {
    let t = this.data.get(e);
    return t === void 0 && (t = {}, this.data.set(e, t)), t;
  }
  /**
   * Checks if the given object has a dictionary
   * with data defined.
   *
   * @param {Object} object - The object.
   * @return {boolean} Whether a dictionary for the given object as been defined or not.
   */
  has(e) {
    return this.data.has(e);
  }
  /**
   * Deletes an object from the internal data structure.
   *
   * @param {Object} object - The object to delete.
   */
  delete(e) {
    this.data.delete(e);
  }
  /**
   * Delete GPU data associated with a bind group.
   *
   * @abstract
   * @param {BindGroup} bindGroup - The bind group.
   */
  deleteBindGroupData() {
  }
  /**
   * Frees internal resources.
   *
   * @abstract
   */
  dispose() {
  }
}
let GS = 0;
class kS {
  constructor(e, t) {
    this.buffers = [e.bufferGPU, t], this.type = e.type, this.bufferType = e.bufferType, this.pbo = e.pbo, this.byteLength = e.byteLength, this.bytesPerElement = e.BYTES_PER_ELEMENT, this.version = e.version, this.isInteger = e.isInteger, this.activeBufferIndex = 0, this.baseId = e.id;
  }
  get id() {
    return `${this.baseId}|${this.activeBufferIndex}`;
  }
  get bufferGPU() {
    return this.buffers[this.activeBufferIndex];
  }
  get transformBuffer() {
    return this.buffers[this.activeBufferIndex ^ 1];
  }
  switchBuffers() {
    this.activeBufferIndex ^= 1;
  }
}
class VS {
  /**
   * Constructs a new utility object.
   *
   * @param {WebGLBackend} backend - The WebGL 2 backend.
   */
  constructor(e) {
    this.backend = e;
  }
  /**
   * Creates the GPU buffer for the given buffer attribute.
   *
   * @param {BufferAttribute} attribute - The buffer attribute.
   * @param {GLenum } bufferType - A flag that indicates the buffer type and thus binding point target.
   */
  createAttribute(e, t) {
    const s = this.backend, { gl: r } = s, n = e.array, i = e.usage || r.STATIC_DRAW, o = e.isInterleavedBufferAttribute ? e.data : e, u = s.get(o);
    let l = u.bufferGPU;
    l === void 0 && (l = this._createBuffer(r, t, n, i), u.bufferGPU = l, u.bufferType = t, u.version = o.version);
    let c;
    if (n instanceof Float32Array)
      c = r.FLOAT;
    else if (typeof Float16Array < "u" && n instanceof Float16Array)
      c = r.HALF_FLOAT;
    else if (n instanceof Uint16Array)
      e.isFloat16BufferAttribute ? c = r.HALF_FLOAT : c = r.UNSIGNED_SHORT;
    else if (n instanceof Int16Array)
      c = r.SHORT;
    else if (n instanceof Uint32Array)
      c = r.UNSIGNED_INT;
    else if (n instanceof Int32Array)
      c = r.INT;
    else if (n instanceof Int8Array)
      c = r.BYTE;
    else if (n instanceof Uint8Array)
      c = r.UNSIGNED_BYTE;
    else if (n instanceof Uint8ClampedArray)
      c = r.UNSIGNED_BYTE;
    else
      throw new Error("THREE.WebGLBackend: Unsupported buffer data format: " + n);
    let d = {
      bufferGPU: l,
      bufferType: t,
      type: c,
      byteLength: n.byteLength,
      bytesPerElement: n.BYTES_PER_ELEMENT,
      version: e.version,
      pbo: e.pbo,
      isInteger: c === r.INT || c === r.UNSIGNED_INT || e.gpuType === Ue,
      id: GS++
    };
    if (e.isStorageBufferAttribute || e.isStorageInstancedBufferAttribute) {
      const h = this._createBuffer(r, t, n, i);
      d = new kS(d, h);
    }
    s.set(e, d);
  }
  /**
   * Updates the GPU buffer of the given buffer attribute.
   *
   * @param {BufferAttribute} attribute - The buffer attribute.
   */
  updateAttribute(e) {
    const t = this.backend, { gl: s } = t, r = e.array, n = e.isInterleavedBufferAttribute ? e.data : e, i = t.get(n), o = i.bufferType, u = e.isInterleavedBufferAttribute ? e.data.updateRanges : e.updateRanges;
    if (s.bindBuffer(o, i.bufferGPU), u.length === 0)
      s.bufferSubData(o, 0, r);
    else {
      for (let l = 0, c = u.length; l < c; l++) {
        const d = u[l];
        s.bufferSubData(
          o,
          d.start * r.BYTES_PER_ELEMENT,
          r,
          d.start,
          d.count
        );
      }
      n.clearUpdateRanges();
    }
    s.bindBuffer(o, null), i.version = n.version;
  }
  /**
   * Destroys the GPU buffer of the given buffer attribute.
   *
   * @param {BufferAttribute} attribute - The buffer attribute.
   */
  destroyAttribute(e) {
    const t = this.backend, { gl: s } = t;
    e.isInterleavedBufferAttribute && t.delete(e.data);
    const r = t.get(e);
    s.deleteBuffer(r.bufferGPU), t.delete(e);
  }
  /**
   * This method performs a readback operation by moving buffer data from
   * a storage buffer attribute from the GPU to the CPU. ReadbackBuffer can
   * be used to retain and reuse handles to the intermediate buffers and prevent
   * new allocation.
   *
   * @async
   * @param {BufferAttribute} attribute - The storage buffer attribute to read frm.
   * @param {ReadbackBuffer|ArrayBuffer} target - The storage buffer attribute.
   * @param {number} offset - The storage buffer attribute.
   * @param {number} count - The offset from which to start reading the
   * @return {Promise<ArrayBuffer|ReadbackBuffer>} A promise that resolves with the buffer data when the data are ready.
   */
  async getArrayBufferAsync(e, t = null, s = 0, r = -1) {
    const n = this.backend, { gl: i } = n, o = e.isInterleavedBufferAttribute ? e.data : e, u = n.get(o), { bufferGPU: l } = u, c = r === -1 ? u.byteLength - s : r;
    let d;
    if (t === null)
      d = new Uint8Array(new ArrayBuffer(c));
    else if (t.isReadbackBuffer) {
      if (t._mapped === !0)
        throw new Error("WebGPURenderer: ReadbackBuffer must be released before being used again.");
      const h = () => {
        t.buffer = null, t._mapped = !1, t.removeEventListener("release", h), t.removeEventListener("dispose", h);
      };
      t.addEventListener("release", h), t.addEventListener("dispose", h), d = new Uint8Array(new ArrayBuffer(c)), t.buffer = d.buffer;
    } else
      d = new Uint8Array(t);
    return i.bindBuffer(i.COPY_READ_BUFFER, l), i.getBufferSubData(i.COPY_READ_BUFFER, s, d), i.bindBuffer(i.COPY_READ_BUFFER, null), i.bindBuffer(i.COPY_WRITE_BUFFER, null), t && t.isReadbackBuffer ? t : d.buffer;
  }
  /**
   * Creates a WebGL buffer with the given data.
   *
   * @private
   * @param {WebGL2RenderingContext} gl - The rendering context.
   * @param {GLenum } bufferType - A flag that indicates the buffer type and thus binding point target.
   * @param {TypedArray} array - The array of the buffer attribute.
   * @param {GLenum} usage - The usage.
   * @return {WebGLBuffer} The WebGL buffer.
   */
  _createBuffer(e, t, s, r) {
    const n = e.createBuffer();
    return e.bindBuffer(t, n), e.bufferData(t, s, r), e.bindBuffer(t, null), n;
  }
}
let vr, Dt;
class $S {
  /**
   * Constructs a new utility object.
   *
   * @param {WebGLBackend} backend - The WebGL 2 backend.
   */
  constructor(e) {
    this.backend = e, this.gl = this.backend.gl, this.enabled = {}, this.parameters = {}, this.currentFlipSided = null, this.currentCullFace = null, this.currentProgram = null, this.currentBlendingEnabled = !1, this.currentBlending = null, this.currentBlendSrc = null, this.currentBlendDst = null, this.currentBlendSrcAlpha = null, this.currentBlendDstAlpha = null, this.currentPremultipledAlpha = null, this.currentPolygonOffsetFactor = null, this.currentPolygonOffsetUnits = null, this.currentColorMask = null, this.currentDepthReversed = !1, this.currentDepthFunc = null, this.currentDepthMask = null, this.currentStencilFunc = null, this.currentStencilRef = null, this.currentStencilFuncMask = null, this.currentStencilFail = null, this.currentStencilZFail = null, this.currentStencilZPass = null, this.currentStencilMask = null, this.currentLineWidth = null, this.currentClippingPlanes = 0, this.currentVAO = null, this.currentIndex = null, this.currentBoundFramebuffers = {}, this.currentDrawbuffers = /* @__PURE__ */ new WeakMap(), this.maxTextures = this.gl.getParameter(this.gl.MAX_TEXTURE_IMAGE_UNITS), this.currentTextureSlot = null, this.currentBoundTextures = {}, this.currentBoundBufferBases = {}, this._init();
  }
  /**
   * Inits the state of the utility.
   *
   * @private
   */
  _init() {
    const e = this.gl;
    vr = {
      [Pt]: e.FUNC_ADD,
      [Xl]: e.FUNC_SUBTRACT,
      [ql]: e.FUNC_REVERSE_SUBTRACT
    }, Dt = {
      [jt]: e.ZERO,
      [rc]: e.ONE,
      [sc]: e.SRC_COLOR,
      [tc]: e.SRC_ALPHA,
      [ec]: e.SRC_ALPHA_SATURATE,
      [Jl]: e.DST_COLOR,
      [Zl]: e.DST_ALPHA,
      [jl]: e.ONE_MINUS_SRC_COLOR,
      [Yl]: e.ONE_MINUS_SRC_ALPHA,
      [Ql]: e.ONE_MINUS_DST_COLOR,
      [Kl]: e.ONE_MINUS_DST_ALPHA
    };
    const t = e.getParameter(e.SCISSOR_BOX), s = e.getParameter(e.VIEWPORT);
    this.currentScissor = new Se().fromArray(t), this.currentViewport = new Se().fromArray(s), this._tempVec4 = new Se();
  }
  /**
   * Enables the given WebGL capability.
   *
   * This method caches the capability state so
   * `gl.enable()` is only called when necessary.
   *
   * @param {GLenum} id - The capability to enable.
   */
  enable(e) {
    const { enabled: t } = this;
    t[e] !== !0 && (this.gl.enable(e), t[e] = !0);
  }
  /**
   * Disables the given WebGL capability.
   *
   * This method caches the capability state so
   * `gl.disable()` is only called when necessary.
   *
   * @param {GLenum} id - The capability to enable.
   */
  disable(e) {
    const { enabled: t } = this;
    t[e] !== !1 && (this.gl.disable(e), t[e] = !1);
  }
  /**
   * Specifies whether polygons are front- or back-facing
   * by setting the winding orientation.
   *
   * This method caches the state so `gl.frontFace()` is only
   * called when necessary.
   *
   * @param {boolean} flipSided - Whether triangles flipped their sides or not.
   */
  setFlipSided(e) {
    if (this.currentFlipSided !== e) {
      const { gl: t } = this;
      e ? t.frontFace(t.CW) : t.frontFace(t.CCW), this.currentFlipSided = e;
    }
  }
  /**
   * Specifies whether or not front- and/or back-facing
   * polygons can be culled.
   *
   * This method caches the state so `gl.cullFace()` is only
   * called when necessary.
   *
   * @param {number} cullFace - Defines which polygons are candidates for culling.
   */
  setCullFace(e) {
    const { gl: t } = this;
    e !== Wh ? (this.enable(t.CULL_FACE), e !== this.currentCullFace && (e === Hh ? t.cullFace(t.BACK) : e === qh ? t.cullFace(t.FRONT) : t.cullFace(t.FRONT_AND_BACK))) : this.disable(t.CULL_FACE), this.currentCullFace = e;
  }
  /**
   * Specifies the width of line primitives.
   *
   * This method caches the state so `gl.lineWidth()` is only
   * called when necessary.
   *
   * @param {number} width - The line width.
   */
  setLineWidth(e) {
    const { currentLineWidth: t, gl: s } = this;
    e !== t && (s.lineWidth(e), this.currentLineWidth = e);
  }
  setMRTBlending(e, t, s) {
    const r = this.gl, n = this.backend.drawBuffersIndexedExt;
    if (!n) {
      Te("WebGPURenderer: Multiple Render Targets (MRT) blending configuration is not fully supported in compatibility mode. The material blending will be used for all render targets.");
      return;
    }
    for (let i = 0; i < e.length; i++) {
      const o = e[i];
      let u = null;
      if (t !== null) {
        const l = t.getBlendMode(o.name);
        l.blending === nc ? u = s : l.blending !== ss && (u = l);
      } else
        u = s;
      u !== null ? this._setMRTBlendingIndex(i, u) : n.blendFuncSeparateiOES(i, r.ONE, r.ZERO, r.ONE, r.ZERO);
    }
  }
  /**
   * Applies blending configuration for a specific draw buffer index.
   *
   * @private
   * @param {number} index - The draw buffer index.
   * @param {Object} blending - The blending configuration (material or BlendMode).
   */
  _setMRTBlendingIndex(e, t) {
    const { gl: s } = this, r = this.backend.drawBuffersIndexedExt, n = t.blending, i = t.blendSrc, o = t.blendDst, u = t.blendEquation, l = t.premultipliedAlpha;
    if (n === Zs) {
      const c = t.blendSrcAlpha !== null ? t.blendSrcAlpha : i, d = t.blendDstAlpha !== null ? t.blendDstAlpha : o, h = t.blendEquationAlpha !== null ? t.blendEquationAlpha : u;
      r.blendEquationSeparateiOES(e, vr[u], vr[h]), r.blendFuncSeparateiOES(e, Dt[i], Dt[o], Dt[c], Dt[d]);
    } else if (r.blendEquationSeparateiOES(e, s.FUNC_ADD, s.FUNC_ADD), l)
      switch (n) {
        case Lt:
          r.blendFuncSeparateiOES(e, s.ONE, s.ONE_MINUS_SRC_ALPHA, s.ONE, s.ONE_MINUS_SRC_ALPHA);
          break;
        case Xs:
          r.blendFuncSeparateiOES(e, s.ONE, s.ONE, s.ONE, s.ONE);
          break;
        case qs:
          r.blendFuncSeparateiOES(e, s.ZERO, s.ONE_MINUS_SRC_COLOR, s.ZERO, s.ONE);
          break;
        case Hs:
          r.blendFuncSeparateiOES(e, s.DST_COLOR, s.ONE_MINUS_SRC_ALPHA, s.ZERO, s.ONE);
          break;
        default:
          r.blendFuncSeparateiOES(e, s.ONE, s.ONE_MINUS_SRC_ALPHA, s.ONE, s.ONE_MINUS_SRC_ALPHA);
          break;
      }
    else
      switch (n) {
        case Lt:
          r.blendFuncSeparateiOES(e, s.SRC_ALPHA, s.ONE_MINUS_SRC_ALPHA, s.ONE, s.ONE_MINUS_SRC_ALPHA);
          break;
        case Xs:
          r.blendFuncSeparateiOES(e, s.SRC_ALPHA, s.ONE, s.ONE, s.ONE);
          break;
        case qs:
          r.blendFuncSeparateiOES(e, s.ZERO, s.ONE_MINUS_SRC_COLOR, s.ZERO, s.ONE);
          break;
        case Hs:
          r.blendFuncSeparateiOES(e, s.DST_COLOR, s.ONE_MINUS_SRC_ALPHA, s.ZERO, s.ONE);
          break;
        default:
          r.blendFuncSeparateiOES(e, s.SRC_ALPHA, s.ONE_MINUS_SRC_ALPHA, s.ONE, s.ONE_MINUS_SRC_ALPHA);
          break;
      }
  }
  /**
   * Defines the blending.
   *
   * This method caches the state so `gl.blendEquation()`, `gl.blendEquationSeparate()`,
   * `gl.blendFunc()` and  `gl.blendFuncSeparate()` are only called when necessary.
   *
   * @param {number} blending - The blending type.
   * @param {number} blendEquation - The blending equation.
   * @param {number} blendSrc - Only relevant for custom blending. The RGB source blending factor.
   * @param {number} blendDst - Only relevant for custom blending. The RGB destination blending factor.
   * @param {number} blendEquationAlpha - Only relevant for custom blending. The blending equation for alpha.
   * @param {number} blendSrcAlpha - Only relevant for custom blending. The alpha source blending factor.
   * @param {number} blendDstAlpha - Only relevant for custom blending. The alpha destination blending factor.
   * @param {boolean} premultipliedAlpha - Whether premultiplied alpha is enabled or not.
   */
  setBlending(e, t, s, r, n, i, o, u) {
    const { gl: l } = this;
    if (e === ss) {
      this.currentBlendingEnabled === !0 && (this.disable(l.BLEND), this.currentBlendingEnabled = !1);
      return;
    }
    if (this.currentBlendingEnabled === !1 && (this.enable(l.BLEND), this.currentBlendingEnabled = !0), e !== Zs) {
      if (e !== this.currentBlending || u !== this.currentPremultipledAlpha) {
        if ((this.currentBlendEquation !== Pt || this.currentBlendEquationAlpha !== Pt) && (l.blendEquation(l.FUNC_ADD), this.currentBlendEquation = Pt, this.currentBlendEquationAlpha = Pt), u)
          switch (e) {
            case Lt:
              l.blendFuncSeparate(l.ONE, l.ONE_MINUS_SRC_ALPHA, l.ONE, l.ONE_MINUS_SRC_ALPHA);
              break;
            case Xs:
              l.blendFunc(l.ONE, l.ONE);
              break;
            case qs:
              l.blendFuncSeparate(l.ZERO, l.ONE_MINUS_SRC_COLOR, l.ZERO, l.ONE);
              break;
            case Hs:
              l.blendFuncSeparate(l.DST_COLOR, l.ONE_MINUS_SRC_ALPHA, l.ZERO, l.ONE);
              break;
            default:
              F("WebGLState: Invalid blending: ", e);
              break;
          }
        else
          switch (e) {
            case Lt:
              l.blendFuncSeparate(l.SRC_ALPHA, l.ONE_MINUS_SRC_ALPHA, l.ONE, l.ONE_MINUS_SRC_ALPHA);
              break;
            case Xs:
              l.blendFuncSeparate(l.SRC_ALPHA, l.ONE, l.ONE, l.ONE);
              break;
            case qs:
              F("WebGLState: SubtractiveBlending requires material.premultipliedAlpha = true");
              break;
            case Hs:
              F("WebGLState: MultiplyBlending requires material.premultipliedAlpha = true");
              break;
            default:
              F("WebGLState: Invalid blending: ", e);
              break;
          }
        this.currentBlendSrc = null, this.currentBlendDst = null, this.currentBlendSrcAlpha = null, this.currentBlendDstAlpha = null, this.currentBlending = e, this.currentPremultipledAlpha = u;
      }
      return;
    }
    n = n || t, i = i || s, o = o || r, (t !== this.currentBlendEquation || n !== this.currentBlendEquationAlpha) && (l.blendEquationSeparate(vr[t], vr[n]), this.currentBlendEquation = t, this.currentBlendEquationAlpha = n), (s !== this.currentBlendSrc || r !== this.currentBlendDst || i !== this.currentBlendSrcAlpha || o !== this.currentBlendDstAlpha) && (l.blendFuncSeparate(Dt[s], Dt[r], Dt[i], Dt[o]), this.currentBlendSrc = s, this.currentBlendDst = r, this.currentBlendSrcAlpha = i, this.currentBlendDstAlpha = o), this.currentBlending = e, this.currentPremultipledAlpha = !1;
  }
  /**
   * Specifies whether colors can be written when rendering
   * into a framebuffer or not.
   *
   * This method caches the state so `gl.colorMask()` is only
   * called when necessary.
   *
   * @param {boolean} colorMask - The color mask.
   */
  setColorMask(e) {
    this.currentColorMask !== e && (this.gl.colorMask(e, e, e, e), this.currentColorMask = e);
  }
  /**
   * Specifies whether the depth test is enabled or not.
   *
   * @param {boolean} depthTest - Whether the depth test is enabled or not.
   */
  setDepthTest(e) {
    const { gl: t } = this;
    e ? this.enable(t.DEPTH_TEST) : this.disable(t.DEPTH_TEST);
  }
  /**
   * Configures the WebGL state to use a reversed depth buffer.
   *
   * @param {boolean} reversed - Whether the depth buffer is reversed or not.
   */
  setReversedDepth(e) {
    if (this.currentDepthReversed !== e) {
      const t = this.backend.extensions.get("EXT_clip_control");
      e ? t.clipControlEXT(t.LOWER_LEFT_EXT, t.ZERO_TO_ONE_EXT) : t.clipControlEXT(t.LOWER_LEFT_EXT, t.NEGATIVE_ONE_TO_ONE_EXT), this.currentDepthReversed = e;
    }
  }
  /**
   * Specifies whether depth values can be written when rendering
   * into a framebuffer or not.
   *
   * This method caches the state so `gl.depthMask()` is only
   * called when necessary.
   *
   * @param {boolean} depthMask - The depth mask.
   */
  setDepthMask(e) {
    this.currentDepthMask !== e && (this.gl.depthMask(e), this.currentDepthMask = e);
  }
  /**
   * Specifies the depth compare function.
   *
   * This method caches the state so `gl.depthFunc()` is only
   * called when necessary.
   *
   * @param {number} depthFunc - The depth compare function.
   */
  setDepthFunc(e) {
    if (this.currentDepthReversed && (e = pc[e]), this.currentDepthFunc !== e) {
      const { gl: t } = this;
      switch (e) {
        case hc:
          t.depthFunc(t.NEVER);
          break;
        case dc:
          t.depthFunc(t.ALWAYS);
          break;
        case cc:
          t.depthFunc(t.LESS);
          break;
        case lc:
          t.depthFunc(t.LEQUAL);
          break;
        case uc:
          t.depthFunc(t.EQUAL);
          break;
        case ac:
          t.depthFunc(t.GEQUAL);
          break;
        case oc:
          t.depthFunc(t.GREATER);
          break;
        case ic:
          t.depthFunc(t.NOTEQUAL);
          break;
        default:
          t.depthFunc(t.LEQUAL);
      }
      this.currentDepthFunc = e;
    }
  }
  /**
   * Specifies the scissor box.
   *
   * @param {number} x - The x-coordinate of the lower left corner of the viewport.
   * @param {number} y - The y-coordinate of the lower left corner of the viewport.
   * @param {number} width - The width of the viewport.
   * @param {number} height - The height of the viewport.
   *
   */
  scissor(e, t, s, r) {
    const n = this._tempVec4.set(e, t, s, r);
    if (this.currentScissor.equals(n) === !1) {
      const { gl: i } = this;
      i.scissor(n.x, n.y, n.z, n.w), this.currentScissor.copy(n);
    }
  }
  /**
   * Specifies the viewport.
   *
   * @param {number} x - The x-coordinate of the lower left corner of the viewport.
   * @param {number} y - The y-coordinate of the lower left corner of the viewport.
   * @param {number} width - The width of the viewport.
   * @param {number} height - The height of the viewport.
   *
   */
  viewport(e, t, s, r) {
    const n = this._tempVec4.set(e, t, s, r);
    if (this.currentViewport.equals(n) === !1) {
      const { gl: i } = this;
      i.viewport(n.x, n.y, n.z, n.w), this.currentViewport.copy(n);
    }
  }
  /**
   * Defines the scissor test.
   *
   * @param {boolean} boolean - Whether the scissor test should be enabled or not.
   */
  setScissorTest(e) {
    const t = this.gl;
    e ? this.enable(t.SCISSOR_TEST) : this.disable(t.SCISSOR_TEST);
  }
  /**
   * Specifies whether the stencil test is enabled or not.
   *
   * @param {boolean} stencilTest - Whether the stencil test is enabled or not.
   */
  setStencilTest(e) {
    const { gl: t } = this;
    e ? this.enable(t.STENCIL_TEST) : this.disable(t.STENCIL_TEST);
  }
  /**
   * Specifies whether stencil values can be written when rendering
   * into a framebuffer or not.
   *
   * This method caches the state so `gl.stencilMask()` is only
   * called when necessary.
   *
   * @param {boolean} stencilMask - The stencil mask.
   */
  setStencilMask(e) {
    this.currentStencilMask !== e && (this.gl.stencilMask(e), this.currentStencilMask = e);
  }
  /**
   * Specifies whether the stencil test functions.
   *
   * This method caches the state so `gl.stencilFunc()` is only
   * called when necessary.
   *
   * @param {number} stencilFunc - The stencil compare function.
   * @param {number} stencilRef - The reference value for the stencil test.
   * @param {number} stencilMask - A bit-wise mask that is used to AND the reference value and the stored stencil value when the test is done.
   */
  setStencilFunc(e, t, s) {
    (this.currentStencilFunc !== e || this.currentStencilRef !== t || this.currentStencilFuncMask !== s) && (this.gl.stencilFunc(e, t, s), this.currentStencilFunc = e, this.currentStencilRef = t, this.currentStencilFuncMask = s);
  }
  /**
   * Specifies whether the stencil test operation.
   *
   * This method caches the state so `gl.stencilOp()` is only
   * called when necessary.
   *
   * @param {number} stencilFail - The function to use when the stencil test fails.
   * @param {number} stencilZFail - The function to use when the stencil test passes, but the depth test fail.
   * @param {number} stencilZPass - The function to use when both the stencil test and the depth test pass,
   * or when the stencil test passes and there is no depth buffer or depth testing is disabled.
   */
  setStencilOp(e, t, s) {
    (this.currentStencilFail !== e || this.currentStencilZFail !== t || this.currentStencilZPass !== s) && (this.gl.stencilOp(e, t, s), this.currentStencilFail = e, this.currentStencilZFail = t, this.currentStencilZPass = s);
  }
  /**
   * Configures the WebGL state for the given material.
   *
   * @param {Material} material - The material to configure the state for.
   * @param {number} frontFaceCW - Whether the front faces are counter-clockwise or not.
   * @param {number} hardwareClippingPlanes - The number of hardware clipping planes.
   */
  setMaterial(e, t, s) {
    const { gl: r } = this;
    e.side === Ot ? this.disable(r.CULL_FACE) : this.enable(r.CULL_FACE);
    let n = e.side === Ge;
    t && (n = !n), this.setFlipSided(n), e.blending === Lt && e.transparent === !1 ? this.setBlending(ss) : this.setBlending(e.blending, e.blendEquation, e.blendSrc, e.blendDst, e.blendEquationAlpha, e.blendSrcAlpha, e.blendDstAlpha, e.premultipliedAlpha), this.setDepthFunc(e.depthFunc), this.setDepthTest(e.depthTest), this.setDepthMask(e.depthWrite), this.setColorMask(e.colorWrite);
    const i = e.stencilWrite;
    if (this.setStencilTest(i), i && (this.setStencilMask(e.stencilWriteMask), this.setStencilFunc(e.stencilFunc, e.stencilRef, e.stencilFuncMask), this.setStencilOp(e.stencilFail, e.stencilZFail, e.stencilZPass)), this.setPolygonOffset(e.polygonOffset, e.polygonOffsetFactor, e.polygonOffsetUnits), e.alphaToCoverage === !0 && this.backend.renderer.currentSamples > 0 ? this.enable(r.SAMPLE_ALPHA_TO_COVERAGE) : this.disable(r.SAMPLE_ALPHA_TO_COVERAGE), s > 0 && this.currentClippingPlanes !== s)
      for (let u = 0; u < 8; u++)
        u < s ? this.enable(12288 + u) : this.disable(12288 + u);
  }
  /**
   * Specifies the polygon offset.
   *
   * This method caches the state so `gl.polygonOffset()` is only
   * called when necessary.
   *
   * @param {boolean} polygonOffset - Whether polygon offset is enabled or not.
   * @param {number} factor - The scale factor for the variable depth offset for each polygon.
   * @param {number} units - The multiplier by which an implementation-specific value is multiplied with to create a constant depth offset.
   */
  setPolygonOffset(e, t, s) {
    const { gl: r } = this;
    e ? (this.enable(r.POLYGON_OFFSET_FILL), (this.currentPolygonOffsetFactor !== t || this.currentPolygonOffsetUnits !== s) && (r.polygonOffset(t, s), this.currentPolygonOffsetFactor = t, this.currentPolygonOffsetUnits = s)) : this.disable(r.POLYGON_OFFSET_FILL);
  }
  /**
   * Defines the usage of the given WebGL program.
   *
   * This method caches the state so `gl.useProgram()` is only
   * called when necessary.
   *
   * @param {WebGLProgram} program - The WebGL program to use.
   * @return {boolean} Whether a program change has been executed or not.
   */
  useProgram(e) {
    return this.currentProgram !== e ? (this.gl.useProgram(e), this.currentProgram = e, !0) : !1;
  }
  /**
   * Sets the vertex state by binding the given VAO and element buffer.
   *
   * @param {WebGLVertexArrayObject} vao - The VAO.
   * @param {?WebGLBuffer} indexBuffer - The index buffer.
   * @return {boolean} Whether a vertex state has been changed or not.
   */
  setVertexState(e, t = null) {
    const s = this.gl;
    return this.currentVAO !== e || this.currentIndex !== t ? (s.bindVertexArray(e), t !== null && s.bindBuffer(s.ELEMENT_ARRAY_BUFFER, t), this.currentVAO = e, this.currentIndex = t, !0) : !1;
  }
  /**
   * Resets the vertex array state by resetting the VAO and element buffer.
   */
  resetVertexState() {
    const e = this.gl;
    e.bindVertexArray(null), e.bindBuffer(e.ELEMENT_ARRAY_BUFFER, null), this.currentVAO = null, this.currentIndex = null;
  }
  // framebuffer
  /**
   * Binds the given framebuffer.
   *
   * This method caches the state so `gl.bindFramebuffer()` is only
   * called when necessary.
   *
   * @param {number} target - The binding point (target).
   * @param {WebGLFramebuffer} framebuffer - The WebGL framebuffer to bind.
   * @return {boolean} Whether a bind has been executed or not.
   */
  bindFramebuffer(e, t) {
    const { gl: s, currentBoundFramebuffers: r } = this;
    return r[e] !== t ? (s.bindFramebuffer(e, t), r[e] = t, e === s.DRAW_FRAMEBUFFER && (r[s.FRAMEBUFFER] = t), e === s.FRAMEBUFFER && (r[s.DRAW_FRAMEBUFFER] = t), !0) : !1;
  }
  /**
   * Defines draw buffers to which fragment colors are written into.
   * Configures the MRT setup of custom framebuffers.
   *
   * This method caches the state so `gl.drawBuffers()` is only
   * called when necessary.
   *
   * @param {RenderContext} renderContext - The render context.
   * @param {WebGLFramebuffer} framebuffer - The WebGL framebuffer.
   */
  drawBuffers(e, t) {
    const { gl: s } = this;
    let r = [], n = !1;
    if (e.textures !== null) {
      r = this.currentDrawbuffers.get(t), r === void 0 && (r = [], this.currentDrawbuffers.set(t, r));
      const i = e.textures;
      if (r.length !== i.length || r[0] !== s.COLOR_ATTACHMENT0) {
        for (let o = 0, u = i.length; o < u; o++)
          r[o] = s.COLOR_ATTACHMENT0 + o;
        r.length = i.length, n = !0;
      }
    } else
      r[0] !== s.BACK && (r[0] = s.BACK, n = !0);
    n && s.drawBuffers(r);
  }
  // texture
  /**
   * Makes the given texture unit active.
   *
   * This method caches the state so `gl.activeTexture()` is only
   * called when necessary.
   *
   * @param {number} webglSlot - The texture unit to make active.
   */
  activeTexture(e) {
    const { gl: t, currentTextureSlot: s, maxTextures: r } = this;
    e === void 0 && (e = t.TEXTURE0 + r - 1), s !== e && (t.activeTexture(e), this.currentTextureSlot = e);
  }
  /**
   * Binds the given WebGL texture to a target.
   *
   * This method caches the state so `gl.bindTexture()` is only
   * called when necessary.
   *
   * @param {number} webglType - The binding point (target).
   * @param {WebGLTexture} webglTexture - The WebGL texture to bind.
   * @param {number} webglSlot - The texture.
   */
  bindTexture(e, t, s) {
    const { gl: r, currentTextureSlot: n, currentBoundTextures: i, maxTextures: o } = this;
    s === void 0 && (n === null ? s = r.TEXTURE0 + o - 1 : s = n);
    let u = i[s];
    u === void 0 && (u = { type: void 0, texture: void 0 }, i[s] = u), (u.type !== e || u.texture !== t) && (n !== s && (r.activeTexture(s), this.currentTextureSlot = s), r.bindTexture(e, t), u.type = e, u.texture = t);
  }
  /**
   * Binds a given WebGL buffer to a given binding point (target) at a given index.
   *
   * This method caches the state so `gl.bindBufferBase()` is only
   * called when necessary.
   *
   * @param {number} target - The target for the bind operation.
   * @param {number} index - The index of the target.
   * @param {WebGLBuffer} buffer - The WebGL buffer.
   * @return {boolean} Whether a bind has been executed or not.
   */
  bindBufferBase(e, t, s) {
    const { gl: r } = this, n = `${e}-${t}`;
    return this.currentBoundBufferBases[n] !== s ? (r.bindBufferBase(e, t, s), this.currentBoundBufferBases[n] = s, !0) : !1;
  }
  /**
   * Unbinds the current bound texture.
   *
   * This method caches the state so `gl.bindTexture()` is only
   * called when necessary.
   */
  unbindTexture() {
    const { gl: e, currentTextureSlot: t, currentBoundTextures: s } = this, r = s[t];
    r !== void 0 && r.type !== void 0 && (e.bindTexture(r.type, null), r.type = void 0, r.texture = void 0);
  }
  /**
   * Returns the value for the given parameter.
   *
   * @param {number} name - The paramter to get the value for.
   * @return {any} The value for the given parameter.
   */
  getParameter(e) {
    const { gl: t, parameters: s } = this;
    return s[e] !== void 0 ? s[e] : t.getParameter(e);
  }
  /**
   * Specifies a pixel storage mode.
   *
   * @param {number} name - The parameter to set.
   * @param {any} value - A value to set the parameter to.
   */
  pixelStorei(e, t) {
    const { gl: s, parameters: r } = this;
    r[e] !== t && (s.pixelStorei(e, t), r[e] = t);
  }
}
class zS {
  /**
   * Constructs a new utility object.
   *
   * @param {WebGLBackend} backend - The WebGL 2 backend.
   */
  constructor(e) {
    this.backend = e, this.gl = this.backend.gl, this.extensions = e.extensions;
  }
  /**
   * Converts the given three.js constant into a WebGL constant.
   * The method currently supports the conversion of texture formats
   * and types.
   *
   * @param {number} p - The three.js constant.
   * @param {string} [colorSpace=NoColorSpace] - The color space.
   * @return {?number} The corresponding WebGL constant.
   */
  convert(e, t = ir) {
    const { gl: s, extensions: r } = this;
    let n;
    const i = ye.getTransfer(t);
    if (e === $e) return s.UNSIGNED_BYTE;
    if (e === Il) return s.UNSIGNED_SHORT_4_4_4_4;
    if (e === Ol) return s.UNSIGNED_SHORT_5_5_5_1;
    if (e === na) return s.UNSIGNED_INT_5_9_9_9_REV;
    if (e === ia) return s.UNSIGNED_INT_10F_11F_11F_REV;
    if (e === Ys) return s.BYTE;
    if (e === js) return s.SHORT;
    if (e === Ss) return s.UNSIGNED_SHORT;
    if (e === Ue) return s.INT;
    if (e === _e) return s.UNSIGNED_INT;
    if (e === De) return s.FLOAT;
    if (e === Oe)
      return s.HALF_FLOAT;
    if (e === Pl) return s.ALPHA;
    if (e === ri) return s.RGB;
    if (e === ct) return s.RGBA;
    if (e === Gt) return s.DEPTH_COMPONENT;
    if (e === kt) return s.DEPTH_STENCIL;
    if (e === ei) return s.RED;
    if (e === ti) return s.RED_INTEGER;
    if (e === Rt) return s.RG;
    if (e === si) return s.RG_INTEGER;
    if (e === ca) return s.RGBA_INTEGER;
    if (e === An || e === vn || e === En || e === Cn)
      if (i === q)
        if (n = r.get("WEBGL_compressed_texture_s3tc_srgb"), n !== null) {
          if (e === An) return n.COMPRESSED_SRGB_S3TC_DXT1_EXT;
          if (e === vn) return n.COMPRESSED_SRGB_ALPHA_S3TC_DXT1_EXT;
          if (e === En) return n.COMPRESSED_SRGB_ALPHA_S3TC_DXT3_EXT;
          if (e === Cn) return n.COMPRESSED_SRGB_ALPHA_S3TC_DXT5_EXT;
        } else
          return null;
      else if (n = r.get("WEBGL_compressed_texture_s3tc"), n !== null) {
        if (e === An) return n.COMPRESSED_RGB_S3TC_DXT1_EXT;
        if (e === vn) return n.COMPRESSED_RGBA_S3TC_DXT1_EXT;
        if (e === En) return n.COMPRESSED_RGBA_S3TC_DXT3_EXT;
        if (e === Cn) return n.COMPRESSED_RGBA_S3TC_DXT5_EXT;
      } else
        return null;
    if (e === Wa || e === Ha || e === qa || e === Xa)
      if (n = r.get("WEBGL_compressed_texture_pvrtc"), n !== null) {
        if (e === Wa) return n.COMPRESSED_RGB_PVRTC_4BPPV1_IMG;
        if (e === Ha) return n.COMPRESSED_RGB_PVRTC_2BPPV1_IMG;
        if (e === qa) return n.COMPRESSED_RGBA_PVRTC_4BPPV1_IMG;
        if (e === Xa) return n.COMPRESSED_RGBA_PVRTC_2BPPV1_IMG;
      } else
        return null;
    if (e === go || e === mo || e === xo || e === To || e === yo || e === Gn || e === _o)
      if (n = r.get("WEBGL_compressed_texture_etc"), n !== null) {
        if (e === go || e === mo) return i === q ? n.COMPRESSED_SRGB8_ETC2 : n.COMPRESSED_RGB8_ETC2;
        if (e === xo) return i === q ? n.COMPRESSED_SRGB8_ALPHA8_ETC2_EAC : n.COMPRESSED_RGBA8_ETC2_EAC;
        if (e === To) return n.COMPRESSED_R11_EAC;
        if (e === yo) return n.COMPRESSED_SIGNED_R11_EAC;
        if (e === Gn) return n.COMPRESSED_RG11_EAC;
        if (e === _o) return n.COMPRESSED_SIGNED_RG11_EAC;
      } else
        return null;
    if (e === bo || e === So || e === No || e === Ro || e === Ao || e === vo || e === Eo || e === Co || e === wo || e === Mo || e === Bo || e === Fo || e === Uo || e === Do)
      if (n = r.get("WEBGL_compressed_texture_astc"), n !== null) {
        if (e === bo) return i === q ? n.COMPRESSED_SRGB8_ALPHA8_ASTC_4x4_KHR : n.COMPRESSED_RGBA_ASTC_4x4_KHR;
        if (e === So) return i === q ? n.COMPRESSED_SRGB8_ALPHA8_ASTC_5x4_KHR : n.COMPRESSED_RGBA_ASTC_5x4_KHR;
        if (e === No) return i === q ? n.COMPRESSED_SRGB8_ALPHA8_ASTC_5x5_KHR : n.COMPRESSED_RGBA_ASTC_5x5_KHR;
        if (e === Ro) return i === q ? n.COMPRESSED_SRGB8_ALPHA8_ASTC_6x5_KHR : n.COMPRESSED_RGBA_ASTC_6x5_KHR;
        if (e === Ao) return i === q ? n.COMPRESSED_SRGB8_ALPHA8_ASTC_6x6_KHR : n.COMPRESSED_RGBA_ASTC_6x6_KHR;
        if (e === vo) return i === q ? n.COMPRESSED_SRGB8_ALPHA8_ASTC_8x5_KHR : n.COMPRESSED_RGBA_ASTC_8x5_KHR;
        if (e === Eo) return i === q ? n.COMPRESSED_SRGB8_ALPHA8_ASTC_8x6_KHR : n.COMPRESSED_RGBA_ASTC_8x6_KHR;
        if (e === Co) return i === q ? n.COMPRESSED_SRGB8_ALPHA8_ASTC_8x8_KHR : n.COMPRESSED_RGBA_ASTC_8x8_KHR;
        if (e === wo) return i === q ? n.COMPRESSED_SRGB8_ALPHA8_ASTC_10x5_KHR : n.COMPRESSED_RGBA_ASTC_10x5_KHR;
        if (e === Mo) return i === q ? n.COMPRESSED_SRGB8_ALPHA8_ASTC_10x6_KHR : n.COMPRESSED_RGBA_ASTC_10x6_KHR;
        if (e === Bo) return i === q ? n.COMPRESSED_SRGB8_ALPHA8_ASTC_10x8_KHR : n.COMPRESSED_RGBA_ASTC_10x8_KHR;
        if (e === Fo) return i === q ? n.COMPRESSED_SRGB8_ALPHA8_ASTC_10x10_KHR : n.COMPRESSED_RGBA_ASTC_10x10_KHR;
        if (e === Uo) return i === q ? n.COMPRESSED_SRGB8_ALPHA8_ASTC_12x10_KHR : n.COMPRESSED_RGBA_ASTC_12x10_KHR;
        if (e === Do) return i === q ? n.COMPRESSED_SRGB8_ALPHA8_ASTC_12x12_KHR : n.COMPRESSED_RGBA_ASTC_12x12_KHR;
      } else
        return null;
    if (e === Po)
      if (n = r.get("EXT_texture_compression_bptc"), n !== null) {
        if (e === Po) return i === q ? n.COMPRESSED_SRGB_ALPHA_BPTC_UNORM_EXT : n.COMPRESSED_RGBA_BPTC_UNORM_EXT;
      } else
        return null;
    if (e === Lo || e === Io || e === kn || e === Oo)
      if (n = r.get("EXT_texture_compression_rgtc"), n !== null) {
        if (e === Lo) return n.COMPRESSED_RED_RGTC1_EXT;
        if (e === Io) return n.COMPRESSED_SIGNED_RED_RGTC1_EXT;
        if (e === kn) return n.COMPRESSED_RED_GREEN_RGTC2_EXT;
        if (e === Oo) return n.COMPRESSED_SIGNED_RED_GREEN_RGTC2_EXT;
      } else
        return null;
    return e === Vt ? s.UNSIGNED_INT_24_8 : s[e] !== void 0 ? s[e] : null;
  }
  /**
   * This method can be used to synchronize the CPU with the GPU by waiting until
   * ongoing GPU commands have been completed.
   *
   * @private
   * @return {Promise} A promise that resolves when all ongoing GPU commands have been completed.
   */
  _clientWaitAsync() {
    const { gl: e } = this, t = e.fenceSync(e.SYNC_GPU_COMMANDS_COMPLETE, 0);
    return e.flush(), new Promise((s, r) => {
      function n() {
        const i = e.clientWaitSync(t, e.SYNC_FLUSH_COMMANDS_BIT, 0);
        if (i === e.WAIT_FAILED) {
          e.deleteSync(t), r();
          return;
        }
        if (i === e.TIMEOUT_EXPIRED) {
          requestAnimationFrame(n);
          return;
        }
        e.deleteSync(t), s();
      }
      n();
    });
  }
}
let xl = !1, bn, ao, Tl;
class WS {
  /**
   * Constructs a new utility object.
   *
   * @param {WebGLBackend} backend - The WebGL 2 backend.
   */
  constructor(e) {
    this.backend = e, this.gl = e.gl, this.extensions = e.extensions, this.defaultTextures = {}, this._srcFramebuffer = null, this._dstFramebuffer = null, xl === !1 && (this._init(), xl = !0);
  }
  /**
   * Inits the state of the utility.
   *
   * @private
   */
  _init() {
    const e = this.gl;
    bn = {
      [oi]: e.REPEAT,
      [Vr]: e.CLAMP_TO_EDGE,
      [ii]: e.MIRRORED_REPEAT
    }, ao = {
      [Qe]: e.NEAREST,
      [Vl]: e.NEAREST_MIPMAP_NEAREST,
      [$r]: e.NEAREST_MIPMAP_LINEAR,
      [Ze]: e.LINEAR,
      [po]: e.LINEAR_MIPMAP_NEAREST,
      [Rs]: e.LINEAR_MIPMAP_LINEAR
    }, Tl = {
      [Hl]: e.NEVER,
      [Wl]: e.ALWAYS,
      [la]: e.LESS,
      [Qr]: e.LEQUAL,
      [zl]: e.EQUAL,
      [nr]: e.GEQUAL,
      [On]: e.GREATER,
      [$l]: e.NOTEQUAL
    };
  }
  /**
   * Returns the native texture type for the given texture.
   *
   * @param {Texture} texture - The texture.
   * @return {GLenum} The native texture type.
   */
  getGLTextureType(e) {
    const { gl: t } = this;
    let s;
    return e.isCubeTexture === !0 ? s = t.TEXTURE_CUBE_MAP : e.isArrayTexture === !0 || e.isDataArrayTexture === !0 || e.isCompressedArrayTexture === !0 ? s = t.TEXTURE_2D_ARRAY : e.isData3DTexture === !0 ? s = t.TEXTURE_3D : s = t.TEXTURE_2D, s;
  }
  /**
   * Returns the native texture type for the given texture.
   *
   * @param {?string} internalFormatName - The internal format name. When `null`, the internal format is derived from the subsequent parameters.
   * @param {GLenum} glFormat - The WebGL format.
   * @param {GLenum} glType - The WebGL type.
   * @param {string} colorSpace - The texture's color space.
   * @param {boolean} [forceLinearTransfer=false] - Whether to force a linear transfer or not.
   * @return {GLenum} The internal format.
   */
  getInternalFormat(e, t, s, r, n, i = !1) {
    const { gl: o, extensions: u } = this;
    if (e !== null) {
      if (o[e] !== void 0) return o[e];
      L("WebGLBackend: Attempt to use non-existing WebGL internal format '" + e + "'");
    }
    let l = null;
    r && (l = u.get("EXT_texture_norm16"), l || L("WebGLRenderer: Unable to use normalized textures without EXT_texture_norm16 extension"));
    let c = t;
    if (t === o.RED && (s === o.FLOAT && (c = o.R32F), s === o.HALF_FLOAT && (c = o.R16F), s === o.UNSIGNED_BYTE && (c = o.R8), s === o.BYTE && (c = o.R8_SNORM), s === o.UNSIGNED_SHORT && l && (c = l.R16_EXT), s === o.SHORT && l && (c = l.R16_SNORM_EXT)), t === o.RED_INTEGER && (s === o.UNSIGNED_BYTE && (c = o.R8UI), s === o.UNSIGNED_SHORT && (c = o.R16UI), s === o.UNSIGNED_INT && (c = o.R32UI), s === o.BYTE && (c = o.R8I), s === o.SHORT && (c = o.R16I), s === o.INT && (c = o.R32I)), t === o.RG && (s === o.FLOAT && (c = o.RG32F), s === o.HALF_FLOAT && (c = o.RG16F), s === o.UNSIGNED_BYTE && (c = o.RG8), s === o.BYTE && (c = o.RG8_SNORM), s === o.UNSIGNED_SHORT && l && (c = l.RG16_EXT), s === o.SHORT && l && (c = l.RG16_SNORM_EXT)), t === o.RG_INTEGER && (s === o.UNSIGNED_BYTE && (c = o.RG8UI), s === o.UNSIGNED_SHORT && (c = o.RG16UI), s === o.UNSIGNED_INT && (c = o.RG32UI), s === o.BYTE && (c = o.RG8I), s === o.SHORT && (c = o.RG16I), s === o.INT && (c = o.RG32I)), t === o.RGB) {
      const d = i ? za : ye.getTransfer(n);
      s === o.FLOAT && (c = o.RGB32F), s === o.HALF_FLOAT && (c = o.RGB16F), s === o.UNSIGNED_BYTE && (c = d === q ? o.SRGB8 : o.RGB8), s === o.BYTE && (c = o.RGB8_SNORM), s === o.UNSIGNED_SHORT && l && (c = l.RGB16_EXT), s === o.SHORT && l && (c = l.RGB16_SNORM_EXT), s === o.UNSIGNED_SHORT_5_6_5 && (c = o.RGB565), s === o.UNSIGNED_SHORT_5_5_5_1 && (c = o.RGB5_A1), s === o.UNSIGNED_SHORT_4_4_4_4 && (c = o.RGB4), s === o.UNSIGNED_INT_5_9_9_9_REV && (c = o.RGB9_E5), s === o.UNSIGNED_INT_10F_11F_11F_REV && (c = o.R11F_G11F_B10F);
    }
    if (t === o.RGB_INTEGER && (s === o.UNSIGNED_BYTE && (c = o.RGB8UI), s === o.UNSIGNED_SHORT && (c = o.RGB16UI), s === o.UNSIGNED_INT && (c = o.RGB32UI), s === o.BYTE && (c = o.RGB8I), s === o.SHORT && (c = o.RGB16I), s === o.INT && (c = o.RGB32I)), t === o.RGBA) {
      const d = i ? za : ye.getTransfer(n);
      s === o.FLOAT && (c = o.RGBA32F), s === o.HALF_FLOAT && (c = o.RGBA16F), s === o.UNSIGNED_BYTE && (c = d === q ? o.SRGB8_ALPHA8 : o.RGBA8), s === o.BYTE && (c = o.RGBA8_SNORM), s === o.UNSIGNED_SHORT && l && (c = l.RGBA16_EXT), s === o.SHORT && l && (c = l.RGBA16_SNORM_EXT), s === o.UNSIGNED_SHORT_4_4_4_4 && (c = o.RGBA4), s === o.UNSIGNED_SHORT_5_5_5_1 && (c = o.RGB5_A1);
    }
    return t === o.RGBA_INTEGER && (s === o.UNSIGNED_BYTE && (c = o.RGBA8UI), s === o.UNSIGNED_SHORT && (c = o.RGBA16UI), s === o.UNSIGNED_INT && (c = o.RGBA32UI), s === o.BYTE && (c = o.RGBA8I), s === o.SHORT && (c = o.RGBA16I), s === o.INT && (c = o.RGBA32I)), t === o.DEPTH_COMPONENT && (s === o.UNSIGNED_SHORT && (c = o.DEPTH_COMPONENT16), s === o.UNSIGNED_INT && (c = o.DEPTH_COMPONENT24), s === o.FLOAT && (c = o.DEPTH_COMPONENT32F)), t === o.DEPTH_STENCIL && s === o.UNSIGNED_INT_24_8 && (c = o.DEPTH24_STENCIL8), (c === o.R16F || c === o.R32F || c === o.RG16F || c === o.RG32F || c === o.RGBA16F || c === o.RGBA32F) && u.get("EXT_color_buffer_float"), c;
  }
  /**
   * Sets the texture parameters for the given texture.
   *
   * @param {GLenum} textureType - The texture type.
   * @param {Texture} texture - The texture.
   */
  setTextureParameters(e, t) {
    const { gl: s, extensions: r, backend: n } = this, { state: i } = this.backend, o = ye.getPrimaries(ye.workingColorSpace), u = t.colorSpace === ir ? null : ye.getPrimaries(t.colorSpace), l = t.colorSpace === ir || o === u ? s.NONE : s.BROWSER_DEFAULT_WEBGL;
    i.pixelStorei(s.UNPACK_FLIP_Y_WEBGL, t.flipY), i.pixelStorei(s.UNPACK_PREMULTIPLY_ALPHA_WEBGL, t.premultiplyAlpha), i.pixelStorei(s.UNPACK_ALIGNMENT, t.unpackAlignment), i.pixelStorei(s.UNPACK_COLORSPACE_CONVERSION_WEBGL, l), s.texParameteri(e, s.TEXTURE_WRAP_S, bn[t.wrapS]), s.texParameteri(e, s.TEXTURE_WRAP_T, bn[t.wrapT]), (e === s.TEXTURE_3D || e === s.TEXTURE_2D_ARRAY) && (t.isArrayTexture || s.texParameteri(e, s.TEXTURE_WRAP_R, bn[t.wrapR])), s.texParameteri(e, s.TEXTURE_MAG_FILTER, ao[t.magFilter]);
    const c = t.mipmaps !== void 0 && t.mipmaps.length > 0, d = t.minFilter === Ze && c ? Rs : t.minFilter;
    if (s.texParameteri(e, s.TEXTURE_MIN_FILTER, ao[d]), t.compareFunction && (s.texParameteri(e, s.TEXTURE_COMPARE_MODE, s.COMPARE_REF_TO_TEXTURE), s.texParameteri(e, s.TEXTURE_COMPARE_FUNC, Tl[t.compareFunction])), r.has("EXT_texture_filter_anisotropic") === !0) {
      if (t.magFilter === Qe || t.minFilter !== $r && t.minFilter !== Rs || t.type === De && r.has("OES_texture_float_linear") === !1) return;
      if (t.anisotropy > 1) {
        const h = r.get("EXT_texture_filter_anisotropic");
        s.texParameterf(e, h.TEXTURE_MAX_ANISOTROPY_EXT, Math.min(t.anisotropy, n.capabilities.getMaxAnisotropy()));
      }
    }
  }
  /**
   * Creates a default texture for the given texture that can be used
   * as a placeholder until the actual texture is ready for usage.
   *
   * @param {Texture} texture - The texture to create a default texture for.
   */
  createDefaultTexture(e) {
    const { gl: t, backend: s, defaultTextures: r } = this, n = this.getGLTextureType(e);
    let i = r[n];
    i === void 0 && (i = t.createTexture(), s.state.bindTexture(n, i), t.texParameteri(n, t.TEXTURE_MIN_FILTER, t.NEAREST), t.texParameteri(n, t.TEXTURE_MAG_FILTER, t.NEAREST), r[n] = i), s.set(e, {
      textureGPU: i,
      glTextureType: n
    });
  }
  /**
   * Defines a texture on the GPU for the given texture object.
   *
   * @param {Texture} texture - The texture.
   * @param {Object} [options={}] - Optional configuration parameter.
   * @return {undefined}
   */
  createTexture(e, t) {
    const { gl: s, backend: r } = this, { levels: n, width: i, height: o, depth: u } = t, l = r.utils.convert(e.format, e.colorSpace), c = r.utils.convert(e.type), d = this.getInternalFormat(e.internalFormat, l, c, e.normalized, e.colorSpace, e.isVideoTexture), h = s.createTexture(), f = this.getGLTextureType(e);
    r.state.bindTexture(f, h), this.setTextureParameters(f, e), e.isArrayTexture || e.isDataArrayTexture || e.isCompressedArrayTexture ? s.texStorage3D(s.TEXTURE_2D_ARRAY, n, d, i, o, u) : e.isData3DTexture ? s.texStorage3D(s.TEXTURE_3D, n, d, i, o, u) : e.isVideoTexture || s.texStorage2D(f, n, d, i, o), r.set(e, {
      textureGPU: h,
      glTextureType: f,
      glFormat: l,
      glType: c,
      glInternalFormat: d
    });
  }
  /**
   * Uploads texture buffer data to the GPU memory.
   *
   * @param {WebGLBuffer} buffer - The buffer data.
   * @param {Texture} texture - The texture,
   */
  copyBufferToTexture(e, t) {
    const { gl: s, backend: r } = this, { state: n } = r, { textureGPU: i, glTextureType: o, glFormat: u, glType: l } = r.get(t), { width: c, height: d } = t.source.data;
    s.bindBuffer(s.PIXEL_UNPACK_BUFFER, e), r.state.bindTexture(o, i), n.pixelStorei(s.UNPACK_FLIP_Y_WEBGL, !1), n.pixelStorei(s.UNPACK_PREMULTIPLY_ALPHA_WEBGL, !1), s.texSubImage2D(o, 0, 0, 0, c, d, u, l, 0), s.bindBuffer(s.PIXEL_UNPACK_BUFFER, null), r.state.unbindTexture();
  }
  /**
   * Uploads the updated texture data to the GPU.
   *
   * @param {Texture} texture - The texture.
   * @param {Object} [options={}] - Optional configuration parameter.
   */
  updateTexture(e, t) {
    const { gl: s } = this, { width: r, height: n } = t, { textureGPU: i, glTextureType: o, glFormat: u, glType: l, glInternalFormat: c } = this.backend.get(e);
    if (!(e.isRenderTargetTexture || i === void 0))
      if (this.backend.state.bindTexture(o, i), this.setTextureParameters(o, e), e.isCompressedTexture) {
        const d = e.mipmaps, h = t.image;
        for (let f = 0; f < d.length; f++) {
          const p = d[f];
          e.isCompressedArrayTexture ? e.format !== s.RGBA ? u !== null ? s.compressedTexSubImage3D(s.TEXTURE_2D_ARRAY, f, 0, 0, 0, p.width, p.height, h.depth, u, p.data) : L("WebGLBackend: Attempt to load unsupported compressed texture format in .uploadTexture()") : s.texSubImage3D(s.TEXTURE_2D_ARRAY, f, 0, 0, 0, p.width, p.height, h.depth, u, l, p.data) : u !== null ? s.compressedTexSubImage2D(s.TEXTURE_2D, f, 0, 0, p.width, p.height, u, p.data) : L("WebGLBackend: Unsupported compressed texture format");
        }
      } else if (e.isCubeTexture) {
        const d = t.images, h = e.mipmaps;
        for (let f = 0; f < 6; f++) {
          const p = Sn(d[f]);
          s.texSubImage2D(s.TEXTURE_CUBE_MAP_POSITIVE_X + f, 0, 0, 0, r, n, u, l, p);
          for (let g = 0; g < h.length; g++) {
            const m = h[g], y = Sn(m.images[f]);
            s.texSubImage2D(s.TEXTURE_CUBE_MAP_POSITIVE_X + f, g + 1, 0, 0, y.width, y.height, u, l, y);
          }
        }
      } else if (e.isDataArrayTexture || e.isArrayTexture) {
        const d = t.image;
        if (e.layerUpdates.size > 0) {
          const h = zh(d.width, d.height, e.format, e.type);
          for (const f of e.layerUpdates) {
            const p = d.data.subarray(
              f * h / d.data.BYTES_PER_ELEMENT,
              (f + 1) * h / d.data.BYTES_PER_ELEMENT
            );
            s.texSubImage3D(s.TEXTURE_2D_ARRAY, 0, 0, 0, f, d.width, d.height, 1, u, l, p);
          }
          e.clearLayerUpdates();
        } else
          s.texSubImage3D(s.TEXTURE_2D_ARRAY, 0, 0, 0, 0, d.width, d.height, d.depth, u, l, d.data);
      } else if (e.isData3DTexture) {
        const d = t.image;
        s.texSubImage3D(s.TEXTURE_3D, 0, 0, 0, 0, d.width, d.height, d.depth, u, l, d.data);
      } else if (e.isVideoTexture)
        e.update(), s.texImage2D(o, 0, c, u, l, t.image);
      else if (e.isHTMLTexture)
        typeof s.texElementImage2D == "function" && s.texElementImage2D(s.TEXTURE_2D, 0, s.RGBA, s.RGBA, s.UNSIGNED_BYTE, t.image);
      else {
        const d = e.mipmaps;
        if (d.length > 0)
          for (let h = 0, f = d.length; h < f; h++) {
            const p = d[h], g = Sn(p);
            s.texSubImage2D(o, h, 0, 0, p.width, p.height, u, l, g);
          }
        else {
          const h = Sn(t.image);
          s.texSubImage2D(o, 0, 0, 0, r, n, u, l, h);
        }
      }
  }
  /**
   * Generates mipmaps for the given texture.
   *
   * @param {Texture} texture - The texture.
   */
  generateMipmaps(e) {
    const { gl: t, backend: s } = this, { textureGPU: r, glTextureType: n } = s.get(e);
    s.state.bindTexture(n, r), t.generateMipmap(n);
  }
  /**
   * Deallocates the render buffers of the given render target.
   *
   * @param {RenderTarget} renderTarget - The render target.
   */
  deallocateRenderBuffers(e) {
    const { gl: t, backend: s } = this;
    if (e) {
      const r = s.get(e);
      if (r.renderBufferStorageSetup = void 0, r.framebuffers) {
        for (const n in r.framebuffers)
          t.deleteFramebuffer(r.framebuffers[n]);
        delete r.framebuffers;
      }
      if (r.depthRenderbuffer && (t.deleteRenderbuffer(r.depthRenderbuffer), delete r.depthRenderbuffer), r.stencilRenderbuffer && (t.deleteRenderbuffer(r.stencilRenderbuffer), delete r.stencilRenderbuffer), r.msaaFrameBuffer && (t.deleteFramebuffer(r.msaaFrameBuffer), delete r.msaaFrameBuffer), r.msaaRenderbuffers) {
        for (let n = 0; n < r.msaaRenderbuffers.length; n++)
          t.deleteRenderbuffer(r.msaaRenderbuffers[n]);
        delete r.msaaRenderbuffers;
      }
    }
  }
  /**
   * Destroys the GPU data for the given texture object.
   *
   * @param {Texture} texture - The texture.
   * @param {boolean} [isDefaultTexture=false] - Whether the texture uses a default GPU texture or not.
   */
  destroyTexture(e, t = !1) {
    const { gl: s, backend: r } = this, { textureGPU: n, renderTarget: i } = r.get(e);
    this.deallocateRenderBuffers(i), t === !1 && s.deleteTexture(n), r.delete(e);
  }
  /**
   * Copies data of the given source texture to the given destination texture.
   *
   * @param {Texture} srcTexture - The source texture.
   * @param {Texture} dstTexture - The destination texture.
   * @param {?(Box3|Box2)} [srcRegion=null] - The region of the source texture to copy.
   * @param {?(Vector2|Vector3)} [dstPosition=null] - The destination position of the copy.
   * @param {number} [srcLevel=0] - The source mip level to copy from.
   * @param {number} [dstLevel=0] - The destination mip level to copy to.
   */
  copyTextureToTexture(e, t, s = null, r = null, n = 0, i = 0) {
    const { gl: o, backend: u } = this, { state: l } = this.backend, { textureGPU: c, glTextureType: d, glType: h, glFormat: f } = u.get(t);
    l.bindTexture(d, c);
    let p, g, m, y, _, v, C, w, M;
    const E = e.isCompressedTexture ? e.mipmaps[i] : e.image;
    if (s !== null)
      p = s.max.x - s.min.x, g = s.max.y - s.min.y, m = s.isBox3 ? s.max.z - s.min.z : 1, y = s.min.x, _ = s.min.y, v = s.isBox3 ? s.min.z : 0;
    else {
      const K = Math.pow(2, -n);
      p = Math.floor(E.width * K), g = Math.floor(E.height * K), e.isDataArrayTexture || e.isArrayTexture ? m = E.depth : e.isData3DTexture ? m = Math.floor(E.depth * K) : m = 1, y = 0, _ = 0, v = 0;
    }
    r !== null ? (C = r.x, w = r.y, M = r.z) : (C = 0, w = 0, M = 0), l.pixelStorei(o.UNPACK_FLIP_Y_WEBGL, t.flipY), l.pixelStorei(o.UNPACK_PREMULTIPLY_ALPHA_WEBGL, t.premultiplyAlpha), l.pixelStorei(o.UNPACK_ALIGNMENT, t.unpackAlignment);
    const U = l.getParameter(o.UNPACK_ROW_LENGTH), P = l.getParameter(o.UNPACK_IMAGE_HEIGHT), $ = l.getParameter(o.UNPACK_SKIP_PIXELS), Y = l.getParameter(o.UNPACK_SKIP_ROWS), ie = l.getParameter(o.UNPACK_SKIP_IMAGES);
    l.pixelStorei(o.UNPACK_ROW_LENGTH, E.width), l.pixelStorei(o.UNPACK_IMAGE_HEIGHT, E.height), l.pixelStorei(o.UNPACK_SKIP_PIXELS, y), l.pixelStorei(o.UNPACK_SKIP_ROWS, _), l.pixelStorei(o.UNPACK_SKIP_IMAGES, v);
    const se = e.isDataArrayTexture || e.isData3DTexture || t.isArrayTexture, oe = t.isDataArrayTexture || t.isData3DTexture || t.isArrayTexture;
    if (e.isDepthTexture) {
      const K = u.get(e), ae = u.get(t), Le = u.get(K.renderTarget), Ce = u.get(ae.renderTarget), ht = Le.framebuffers[K.cacheKey], bi = Ce.framebuffers[ae.cacheKey];
      l.bindFramebuffer(o.READ_FRAMEBUFFER, ht), l.bindFramebuffer(o.DRAW_FRAMEBUFFER, bi);
      for (let Gs = 0; Gs < m; Gs++)
        se && (o.framebufferTextureLayer(o.READ_FRAMEBUFFER, o.COLOR_ATTACHMENT0, K.textureGPU, n, v + Gs), o.framebufferTextureLayer(o.DRAW_FRAMEBUFFER, o.COLOR_ATTACHMENT0, c, i, M + Gs)), o.blitFramebuffer(y, _, p, g, C, w, p, g, o.DEPTH_BUFFER_BIT, o.NEAREST);
      l.bindFramebuffer(o.READ_FRAMEBUFFER, null), l.bindFramebuffer(o.DRAW_FRAMEBUFFER, null);
    } else if (n !== 0 || e.isRenderTargetTexture || u.has(e)) {
      const K = u.get(e);
      this._srcFramebuffer === null && (this._srcFramebuffer = o.createFramebuffer()), this._dstFramebuffer === null && (this._dstFramebuffer = o.createFramebuffer()), l.bindFramebuffer(o.READ_FRAMEBUFFER, this._srcFramebuffer), l.bindFramebuffer(o.DRAW_FRAMEBUFFER, this._dstFramebuffer);
      for (let ae = 0; ae < m; ae++)
        se ? o.framebufferTextureLayer(o.READ_FRAMEBUFFER, o.COLOR_ATTACHMENT0, K.textureGPU, n, v + ae) : o.framebufferTexture2D(o.READ_FRAMEBUFFER, o.COLOR_ATTACHMENT0, o.TEXTURE_2D, K.textureGPU, n), oe ? o.framebufferTextureLayer(o.DRAW_FRAMEBUFFER, o.COLOR_ATTACHMENT0, c, i, M + ae) : o.framebufferTexture2D(o.DRAW_FRAMEBUFFER, o.COLOR_ATTACHMENT0, o.TEXTURE_2D, c, i), n !== 0 ? o.blitFramebuffer(y, _, p, g, C, w, p, g, o.COLOR_BUFFER_BIT, o.NEAREST) : oe ? o.copyTexSubImage3D(d, i, C, w, M + ae, y, _, p, g) : o.copyTexSubImage2D(d, i, C, w, y, _, p, g);
      l.bindFramebuffer(o.READ_FRAMEBUFFER, null), l.bindFramebuffer(o.DRAW_FRAMEBUFFER, null);
    } else
      oe ? e.isDataTexture || e.isData3DTexture ? o.texSubImage3D(d, i, C, w, M, p, g, m, f, h, E.data) : t.isCompressedArrayTexture ? o.compressedTexSubImage3D(d, i, C, w, M, p, g, m, f, E.data) : o.texSubImage3D(d, i, C, w, M, p, g, m, f, h, E) : e.isDataTexture ? o.texSubImage2D(o.TEXTURE_2D, i, C, w, p, g, f, h, E.data) : e.isCompressedTexture ? o.compressedTexSubImage2D(o.TEXTURE_2D, i, C, w, E.width, E.height, f, E.data) : o.texSubImage2D(o.TEXTURE_2D, i, C, w, p, g, f, h, E);
    l.pixelStorei(o.UNPACK_ROW_LENGTH, U), l.pixelStorei(o.UNPACK_IMAGE_HEIGHT, P), l.pixelStorei(o.UNPACK_SKIP_PIXELS, $), l.pixelStorei(o.UNPACK_SKIP_ROWS, Y), l.pixelStorei(o.UNPACK_SKIP_IMAGES, ie), i === 0 && t.generateMipmaps && o.generateMipmap(d), l.unbindTexture();
  }
  /**
   * Copies the current bound framebuffer to the given texture.
   *
   * @param {Texture} texture - The destination texture.
   * @param {RenderContext} renderContext - The render context.
   * @param {Vector4} rectangle - A four dimensional vector defining the origin and dimension of the copy.
   */
  copyFramebufferToTexture(e, t, s) {
    const { gl: r } = this, { state: n } = this.backend, { textureGPU: i } = this.backend.get(e), { x: o, y: u, z: l, w: c } = s, d = e.isDepthTexture === !0 || t.renderTarget && t.renderTarget.samples > 0, h = t.renderTarget ? t.renderTarget.height : this.backend.getDrawingBufferSize().y;
    if (d) {
      const f = o !== 0 || u !== 0;
      let p, g;
      if (e.isDepthTexture === !0 ? (p = r.DEPTH_BUFFER_BIT, g = r.DEPTH_ATTACHMENT, t.stencil && (p |= r.STENCIL_BUFFER_BIT)) : (p = r.COLOR_BUFFER_BIT, g = r.COLOR_ATTACHMENT0), f) {
        const m = this.backend.get(t.renderTarget), y = m.framebuffers[t.getCacheKey()], _ = m.msaaFrameBuffer;
        n.bindFramebuffer(r.DRAW_FRAMEBUFFER, y), n.bindFramebuffer(r.READ_FRAMEBUFFER, _);
        const v = h - u - c;
        r.blitFramebuffer(o, v, o + l, v + c, o, v, o + l, v + c, p, r.NEAREST), n.bindFramebuffer(r.READ_FRAMEBUFFER, y), n.bindTexture(r.TEXTURE_2D, i), r.copyTexSubImage2D(r.TEXTURE_2D, 0, 0, 0, o, v, l, c), n.unbindTexture();
      } else {
        const m = r.createFramebuffer();
        n.bindFramebuffer(r.DRAW_FRAMEBUFFER, m), r.framebufferTexture2D(r.DRAW_FRAMEBUFFER, g, r.TEXTURE_2D, i, 0), r.blitFramebuffer(0, 0, l, c, 0, 0, l, c, p, r.NEAREST), r.deleteFramebuffer(m);
      }
    } else
      n.bindTexture(r.TEXTURE_2D, i), r.copyTexSubImage2D(r.TEXTURE_2D, 0, 0, 0, o, h - c - u, l, c), n.unbindTexture();
    e.generateMipmaps && this.generateMipmaps(e), this.backend._setFramebuffer(t);
  }
  /**
   * SetupS storage for internal depth/stencil buffers and bind to correct framebuffer.
   *
   * @param {WebGLRenderbuffer} renderbuffer - The render buffer.
   * @param {RenderContext} renderContext - The render context.
   * @param {number} samples - The MSAA sample count.
   * @param {boolean} [useMultisampledRTT=false] - Whether to use WEBGL_multisampled_render_to_texture or not.
   */
  setupRenderBufferStorage(e, t, s, r = !1) {
    const { gl: n } = this, i = t.renderTarget, { depthTexture: o, depthBuffer: u, stencilBuffer: l, width: c, height: d } = i;
    if (n.bindRenderbuffer(n.RENDERBUFFER, e), u && !l) {
      let h = n.DEPTH_COMPONENT24;
      r === !0 ? this.extensions.get("WEBGL_multisampled_render_to_texture").renderbufferStorageMultisampleEXT(n.RENDERBUFFER, i.samples, h, c, d) : s > 0 ? (o && o.isDepthTexture && o.type === n.FLOAT && (h = n.DEPTH_COMPONENT32F), n.renderbufferStorageMultisample(n.RENDERBUFFER, s, h, c, d)) : n.renderbufferStorage(n.RENDERBUFFER, h, c, d), n.framebufferRenderbuffer(n.FRAMEBUFFER, n.DEPTH_ATTACHMENT, n.RENDERBUFFER, e);
    } else u && l && (s > 0 ? n.renderbufferStorageMultisample(n.RENDERBUFFER, s, n.DEPTH24_STENCIL8, c, d) : n.renderbufferStorage(n.RENDERBUFFER, n.DEPTH_STENCIL, c, d), n.framebufferRenderbuffer(n.FRAMEBUFFER, n.DEPTH_STENCIL_ATTACHMENT, n.RENDERBUFFER, e));
    n.bindRenderbuffer(n.RENDERBUFFER, null);
  }
  /**
   * Returns texture data as a typed array.
   *
   * @async
   * @param {Texture} texture - The texture to copy.
   * @param {number} x - The x coordinate of the copy origin.
   * @param {number} y - The y coordinate of the copy origin.
   * @param {number} width - The width of the copy.
   * @param {number} height - The height of the copy.
   * @param {number} faceIndex - The face index.
   * @return {Promise<TypedArray>} A Promise that resolves with a typed array when the copy operation has finished.
   */
  async copyTextureToBuffer(e, t, s, r, n, i) {
    const { backend: o, gl: u } = this, { textureGPU: l, glFormat: c, glType: d } = this.backend.get(e), h = u.createFramebuffer();
    o.state.bindFramebuffer(u.READ_FRAMEBUFFER, h);
    const f = e.isCubeTexture ? u.TEXTURE_CUBE_MAP_POSITIVE_X + i : u.TEXTURE_2D;
    u.framebufferTexture2D(u.READ_FRAMEBUFFER, u.COLOR_ATTACHMENT0, f, l, 0);
    const p = this._getTypedArrayType(d), g = this._getBytesPerTexel(d, c), y = r * n * g, _ = u.createBuffer();
    u.bindBuffer(u.PIXEL_PACK_BUFFER, _), u.bufferData(u.PIXEL_PACK_BUFFER, y, u.STREAM_READ), u.readPixels(t, s, r, n, c, d, 0), u.bindBuffer(u.PIXEL_PACK_BUFFER, null), await o.utils._clientWaitAsync();
    const v = new p(y / p.BYTES_PER_ELEMENT);
    return u.bindBuffer(u.PIXEL_PACK_BUFFER, _), u.getBufferSubData(u.PIXEL_PACK_BUFFER, 0, v), u.bindBuffer(u.PIXEL_PACK_BUFFER, null), o.state.bindFramebuffer(u.READ_FRAMEBUFFER, null), u.deleteFramebuffer(h), v;
  }
  /**
   * Returns the corresponding typed array type for the given WebGL data type.
   *
   * @private
   * @param {GLenum} glType - The WebGL data type.
   * @return {TypedArray.constructor} The typed array type.
   */
  _getTypedArrayType(e) {
    const { gl: t } = this;
    if (e === t.UNSIGNED_BYTE) return Uint8Array;
    if (e === t.UNSIGNED_SHORT_4_4_4_4 || e === t.UNSIGNED_SHORT_5_5_5_1 || e === t.UNSIGNED_SHORT_5_6_5 || e === t.UNSIGNED_SHORT) return Uint16Array;
    if (e === t.UNSIGNED_INT) return Uint32Array;
    if (e === t.HALF_FLOAT) return Uint16Array;
    if (e === t.FLOAT) return Float32Array;
    throw new Error(`Unsupported WebGL type: ${e}`);
  }
  /**
   * Returns the bytes-per-texel value for the given WebGL data type and texture format.
   *
   * @private
   * @param {GLenum} glType - The WebGL data type.
   * @param {GLenum} glFormat - The WebGL texture format.
   * @return {number} The bytes-per-texel.
   */
  _getBytesPerTexel(e, t) {
    const { gl: s } = this;
    let r = 0;
    if (e === s.UNSIGNED_BYTE && (r = 1), (e === s.UNSIGNED_SHORT_4_4_4_4 || e === s.UNSIGNED_SHORT_5_5_5_1 || e === s.UNSIGNED_SHORT_5_6_5 || e === s.UNSIGNED_SHORT || e === s.HALF_FLOAT) && (r = 2), (e === s.UNSIGNED_INT || e === s.FLOAT) && (r = 4), t === s.RGBA) return r * 4;
    if (t === s.RGB) return r * 3;
    if (t === s.ALPHA) return r;
  }
  /**
   * Frees the internal resources.
   */
  dispose() {
    const { gl: e } = this;
    this._srcFramebuffer !== null && e.deleteFramebuffer(this._srcFramebuffer), this._dstFramebuffer !== null && e.deleteFramebuffer(this._dstFramebuffer);
  }
}
function Sn(a) {
  return a.isDataTexture ? a.image.data : typeof HTMLImageElement < "u" && a instanceof HTMLImageElement || typeof HTMLCanvasElement < "u" && a instanceof HTMLCanvasElement || typeof ImageBitmap < "u" && a instanceof ImageBitmap || typeof OffscreenCanvas < "u" && a instanceof OffscreenCanvas ? a : a.data;
}
class HS {
  /**
   * Constructs a new utility object.
   *
   * @param {WebGLBackend} backend - The WebGL 2 backend.
   */
  constructor(e) {
    this.backend = e, this.gl = this.backend.gl, this.availableExtensions = this.gl.getSupportedExtensions(), this.extensions = {};
  }
  /**
   * Returns the extension object for the given extension name.
   *
   * @param {string} name - The extension name.
   * @return {Object} The extension object.
   */
  get(e) {
    let t = this.extensions[e];
    return t === void 0 && (t = this.gl.getExtension(e), this.extensions[e] = t), t;
  }
  /**
   * Returns `true` if the requested extension is available.
   *
   * @param {string} name - The extension name.
   * @return {boolean} Whether the given extension is available or not.
   */
  has(e) {
    return this.availableExtensions.includes(e);
  }
}
class qS {
  /**
   * Constructs a new utility object.
   *
   * @param {WebGLBackend} backend - The WebGL 2 backend.
   */
  constructor(e) {
    this.backend = e, this.maxAnisotropy = null, this.maxUniformBlockSize = null;
  }
  /**
   * Returns the maximum anisotropy texture filtering value. This value
   * depends on the device and is reported by the `EXT_texture_filter_anisotropic`
   * WebGL extension.
   *
   * @return {number} The maximum anisotropy texture filtering value.
   */
  getMaxAnisotropy() {
    if (this.maxAnisotropy !== null) return this.maxAnisotropy;
    const e = this.backend.gl, t = this.backend.extensions;
    if (t.has("EXT_texture_filter_anisotropic") === !0) {
      const s = t.get("EXT_texture_filter_anisotropic");
      this.maxAnisotropy = e.getParameter(s.MAX_TEXTURE_MAX_ANISOTROPY_EXT);
    } else
      this.maxAnisotropy = 0;
    return this.maxAnisotropy;
  }
  /**
   * Returns the maximum number of bytes available for uniform buffers.
   *
   * @return {number} The maximum number of bytes available for uniform buffers.
   */
  getUniformBufferLimit() {
    if (this.maxUniformBlockSize !== null) return this.maxUniformBlockSize;
    const e = this.backend.gl;
    return this.maxUniformBlockSize = e.getParameter(e.MAX_UNIFORM_BLOCK_SIZE), this.maxUniformBlockSize;
  }
}
const yl = {
  WEBGL_multi_draw: "WEBGL_multi_draw",
  WEBGL_compressed_texture_astc: "texture-compression-astc",
  WEBGL_compressed_texture_etc: "texture-compression-etc2",
  WEBGL_compressed_texture_etc1: "texture-compression-etc1",
  WEBGL_compressed_texture_pvrtc: "texture-compression-pvrtc",
  WEBGL_compressed_texture_s3tc: "texture-compression-s3tc",
  EXT_texture_compression_bptc: "texture-compression-bc",
  EXT_disjoint_timer_query_webgl2: "timestamp-query",
  OVR_multiview2: "OVR_multiview2"
};
class XS {
  constructor(e) {
    this.gl = e.gl, this.extensions = e.extensions, this.info = e.renderer.info, this.mode = null, this.index = 0, this.type = null, this.object = null;
  }
  render(e, t) {
    const { gl: s, mode: r, object: n, type: i, info: o, index: u } = this;
    u !== 0 ? s.drawElements(r, t, i, e) : s.drawArrays(r, e, t), o.update(n, t, 1);
  }
  renderInstances(e, t, s) {
    const { gl: r, mode: n, type: i, index: o, object: u, info: l } = this;
    s !== 0 && (o !== 0 ? r.drawElementsInstanced(n, t, i, e, s) : r.drawArraysInstanced(n, e, t, s), l.update(u, t, s));
  }
  renderMultiDraw(e, t, s) {
    const { extensions: r, mode: n, object: i, info: o } = this;
    if (s === 0) return;
    const u = r.get("WEBGL_multi_draw");
    if (u === null)
      for (let l = 0; l < s; l++)
        this.render(e[l], t[l]);
    else {
      this.index !== 0 ? u.multiDrawElementsWEBGL(n, t, 0, this.type, e, 0, s) : u.multiDrawArraysWEBGL(n, e, 0, t, 0, s);
      let l = 0;
      for (let c = 0; c < s; c++)
        l += t[c];
      o.update(i, l, 1);
    }
  }
  //
}
class Th {
  /**
   * Creates a new timestamp query pool.
   *
   * @param {number} [maxQueries=256] - Maximum number of queries this pool can hold.
   */
  constructor(e = 256) {
    this.trackTimestamp = !0, this.maxQueries = e, this.currentQueryIndex = 0, this.queryOffsets = /* @__PURE__ */ new Map(), this.isDisposed = !1, this.lastValue = 0, this.frames = [], this.pendingResolve = !1, this.timestamps = /* @__PURE__ */ new Map();
  }
  /**
   * Returns all timestamp frames.
   *
   * @return {Array<number>} The timestamp frames.
   */
  getTimestampFrames() {
    return this.frames;
  }
  /**
   * Returns the timestamp for a given render context.
   *
   * @param {string} uid - A unique identifier for the render context.
   * @return {?number} The timestamp, or undefined if not available.
   */
  getTimestamp(e) {
    let t = this.timestamps.get(e);
    return t === void 0 && (L(`TimestampQueryPool: No timestamp available for uid ${e}.`), t = 0), t;
  }
  /**
   * Returns whether a timestamp is available for a given render context.
   *
   * @param {string} uid - A unique identifier for the render context.
   * @return {boolean} True if a timestamp is available, false otherwise.
   */
  hasTimestamp(e) {
    return this.timestamps.has(e);
  }
  /**
   * Allocate queries for a specific uid.
   *
   * @abstract
   * @param {string} uid - A unique identifier for the render context.
   * @param {number} frameId - The current frame identifier.
   * @returns {?number}
   */
  allocateQueriesForContext() {
  }
  /**
   * Resolve all timestamps and return data (or process them).
   *
   * @abstract
   * @async
   * @returns {Promise<number>|number} The resolved timestamp value.
   */
  async resolveQueriesAsync() {
  }
  /**
   * Dispose of the query pool.
   *
   * @abstract
   */
  dispose() {
  }
}
class KS extends Th {
  /**
   * Creates a new WebGL timestamp query pool.
   *
   * @param {WebGLRenderingContext|WebGL2RenderingContext} gl - The WebGL context.
   * @param {string} type - The type identifier for this query pool.
   * @param {number} [maxQueries=2048] - Maximum number of queries this pool can hold.
   */
  constructor(e, t, s = 2048) {
    if (super(s), this.gl = e, this.type = t, this.ext = e.getExtension("EXT_disjoint_timer_query_webgl2") || e.getExtension("EXT_disjoint_timer_query"), !this.ext) {
      L("EXT_disjoint_timer_query not supported; timestamps will be disabled."), this.trackTimestamp = !1;
      return;
    }
    this.queries = [];
    for (let r = 0; r < this.maxQueries; r++)
      this.queries.push(e.createQuery());
    this.activeQuery = null, this.queryStates = /* @__PURE__ */ new Map();
  }
  /**
   * Allocates a pair of queries for a given render context.
   *
   * @param {string} uid - A unique identifier for the render context.
   * @returns {?number} The base offset for the allocated queries, or null if allocation failed.
   */
  allocateQueriesForContext(e) {
    if (!this.trackTimestamp) return null;
    if (this.currentQueryIndex + 2 > this.maxQueries)
      return Te(`WebGLTimestampQueryPool [${this.type}]: Maximum number of queries exceeded, when using trackTimestamp it is necessary to resolves the queries via renderer.resolveTimestampsAsync( THREE.TimestampQuery.${this.type.toUpperCase()} ).`), null;
    const t = this.currentQueryIndex;
    return this.currentQueryIndex += 2, this.queryStates.set(t, "inactive"), this.queryOffsets.set(e, t), t;
  }
  /**
   * Begins a timestamp query for the specified render context.
   *
   * @param {string} uid - A unique identifier for the render context.
   */
  beginQuery(e) {
    if (!this.trackTimestamp || this.isDisposed)
      return;
    const t = this.queryOffsets.get(e);
    if (t == null || this.activeQuery !== null)
      return;
    const s = this.queries[t];
    if (s)
      try {
        this.queryStates.get(t) === "inactive" && (this.gl.beginQuery(this.ext.TIME_ELAPSED_EXT, s), this.activeQuery = t, this.queryStates.set(t, "started"));
      } catch (r) {
        F("Error in beginQuery:", r), this.activeQuery = null, this.queryStates.set(t, "inactive");
      }
  }
  /**
   * Ends the active timestamp query for the specified render context.
   *
   * @param {string} uid - A unique identifier for the render context.
   */
  endQuery(e) {
    if (!this.trackTimestamp || this.isDisposed)
      return;
    const t = this.queryOffsets.get(e);
    if (t != null && this.activeQuery === t)
      try {
        this.gl.endQuery(this.ext.TIME_ELAPSED_EXT), this.queryStates.set(t, "ended"), this.activeQuery = null;
      } catch (s) {
        F("Error in endQuery:", s), this.queryStates.set(t, "inactive"), this.activeQuery = null;
      }
  }
  /**
   * Asynchronously resolves all completed queries and returns the total duration.
   *
   * @async
   * @returns {Promise<number>} The total duration in milliseconds, or the last valid value if resolution fails.
   */
  async resolveQueriesAsync() {
    if (!this.trackTimestamp || this.pendingResolve)
      return this.lastValue;
    this.pendingResolve = !0;
    try {
      const e = /* @__PURE__ */ new Map();
      for (const [n, i] of this.queryOffsets)
        if (this.queryStates.get(i) === "ended") {
          const u = this.queries[i];
          e.set(n, this.resolveQuery(u));
        }
      if (e.size === 0)
        return this.lastValue;
      const t = {}, s = [];
      for (const [n, i] of e) {
        const o = n.match(/^(.*):f(\d+)$/), u = parseInt(o[2]);
        s.includes(u) === !1 && s.push(u), t[u] === void 0 && (t[u] = 0);
        const l = await i;
        this.timestamps.set(n, l), t[u] += l;
      }
      const r = t[s[s.length - 1]];
      return this.lastValue = r, this.frames = s, this.currentQueryIndex = 0, this.queryOffsets.clear(), this.queryStates.clear(), this.activeQuery = null, r;
    } catch (e) {
      return F("Error resolving queries:", e), this.lastValue;
    } finally {
      this.pendingResolve = !1;
    }
  }
  /**
   * Resolves a single query, checking for completion and disjoint operation.
   *
   * @async
   * @param {WebGLQuery} query - The query object to resolve.
   * @returns {Promise<number>} The elapsed time in milliseconds.
   */
  async resolveQuery(e) {
    return new Promise((t) => {
      if (this.isDisposed) {
        t(this.lastValue);
        return;
      }
      let s, r = !1;
      const n = () => {
        s && (clearTimeout(s), s = null);
      }, i = (u) => {
        r || (r = !0, n(), t(u));
      }, o = () => {
        if (this.isDisposed) {
          i(this.lastValue);
          return;
        }
        try {
          if (this.gl.getParameter(this.ext.GPU_DISJOINT_EXT)) {
            i(this.lastValue);
            return;
          }
          if (!this.gl.getQueryParameter(e, this.gl.QUERY_RESULT_AVAILABLE)) {
            s = setTimeout(o, 1);
            return;
          }
          const c = this.gl.getQueryParameter(e, this.gl.QUERY_RESULT);
          t(Number(c) / 1e6);
        } catch (u) {
          F("Error checking query:", u), t(this.lastValue);
        }
      };
      o();
    });
  }
  /**
   * Releases all resources held by this query pool.
   * This includes deleting all query objects and clearing internal state.
   */
  dispose() {
    if (!this.isDisposed && (this.isDisposed = !0, !!this.trackTimestamp)) {
      for (const e of this.queries)
        this.gl.deleteQuery(e);
      this.queries = [], this.queryStates.clear(), this.queryOffsets.clear(), this.lastValue = 0, this.activeQuery = null;
    }
  }
}
class _l extends xh {
  /**
   * WebGLBackend options.
   *
   * @typedef {Object} WebGLBackend~Options
   * @property {boolean} [logarithmicDepthBuffer=false] - Whether logarithmic depth buffer is enabled or not.
   * @property {boolean} [reversedDepthBuffer=false] - Whether reversed depth buffer is enabled or not.
   * @property {boolean} [alpha=true] - Whether the default framebuffer (which represents the final contents of the canvas) should be transparent or opaque.
   * @property {boolean} [depth=true] - Whether the default framebuffer should have a depth buffer or not.
   * @property {boolean} [stencil=false] - Whether the default framebuffer should have a stencil buffer or not.
   * @property {boolean} [antialias=false] - Whether MSAA as the default anti-aliasing should be enabled or not.
   * @property {number} [samples=0] - When `antialias` is `true`, `4` samples are used by default. Set this parameter to any other integer value than 0 to overwrite the default.
   * @property {boolean} [forceWebGL=false] - If set to `true`, the renderer uses a WebGL 2 backend no matter if WebGPU is supported or not.
   * @property {WebGL2RenderingContext} [context=undefined] - A WebGL 2 rendering context.
   */
  /**
   * Constructs a new WebGPU backend.
   *
   * @param {WebGLBackend~Options} [parameters] - The configuration parameter.
   */
  constructor(e = {}) {
    super(e), this.isWebGLBackend = !0, this.attributeUtils = null, this.extensions = null, this.capabilities = null, this.textureUtils = null, this.bufferRenderer = null, this.gl = null, this.state = null, this.utils = null, this.vaoCache = {}, this.transformFeedbackCache = {}, this.discard = !1, this.disjoint = null, this.parallel = null, this._currentContext = null, this._knownBindings = /* @__PURE__ */ new WeakSet(), this._supportsInvalidateFramebuffer = typeof navigator > "u" ? !1 : /OculusBrowser/g.test(navigator.userAgent), this._xrFramebuffer = null;
  }
  /**
   * Initializes the backend so it is ready for usage.
   *
   * @param {Renderer} renderer - The renderer.
   */
  init(e) {
    super.init(e);
    const t = this.parameters, s = {
      antialias: e.currentSamples > 0,
      alpha: !0,
      // always true for performance reasons
      depth: e.depth,
      stencil: e.stencil
    }, r = t.context !== void 0 ? t.context : e.domElement.getContext("webgl2", s);
    function n(i) {
      i.preventDefault();
      const o = {
        api: "WebGL",
        message: i.statusMessage || "Unknown reason",
        reason: null,
        originalEvent: i
      };
      e.onDeviceLost(o);
    }
    this._onContextLost = n, e.domElement.addEventListener("webglcontextlost", n, !1), this.gl = r, this.extensions = new HS(this), this.capabilities = new qS(this), this.attributeUtils = new VS(this), this.textureUtils = new WS(this), this.bufferRenderer = new XS(this), this.state = new $S(this), this.utils = new zS(this), this.extensions.get("EXT_color_buffer_float"), this.extensions.get("WEBGL_clip_cull_distance"), this.extensions.get("OES_texture_float_linear"), this.extensions.get("EXT_color_buffer_half_float"), this.extensions.get("WEBGL_multisampled_render_to_texture"), this.extensions.get("WEBGL_render_shared_exponent"), this.extensions.get("WEBGL_multi_draw"), this.extensions.get("OVR_multiview2"), this.extensions.get("EXT_clip_control"), this.disjoint = this.extensions.get("EXT_disjoint_timer_query_webgl2"), this.parallel = this.extensions.get("KHR_parallel_shader_compile"), this.drawBuffersIndexedExt = this.extensions.get("OES_draw_buffers_indexed"), t.reversedDepthBuffer && (this.extensions.has("EXT_clip_control") ? e.reversedDepthBuffer = !0 : (L("WebGPURenderer: Unable to use reversed depth buffer due to missing EXT_clip_control extension. Fallback to default depth buffer."), e.reversedDepthBuffer = !1)), e.reversedDepthBuffer && this.state.setReversedDepth(!0);
  }
  /**
   * The coordinate system of the backend.
   *
   * @type {number}
   * @readonly
   */
  get coordinateSystem() {
    return Dn;
  }
  /**
   * This method performs a readback operation by moving buffer data from
   * a storage buffer attribute from the GPU to the CPU. ReadbackBuffer can
   * be used to retain and reuse handles to the intermediate buffers and prevent
   * new allocation.
   *
   * @async
   * @param {BufferAttribute} attribute - The storage buffer attribute to read frm.
   * @param {ReadbackBuffer|ArrayBuffer} target - The storage buffer attribute.
   * @param {number} offset - The storage buffer attribute.
   * @param {number} count - The offset from which to start reading the
   * @return {Promise<ArrayBuffer|ReadbackBuffer>} A promise that resolves with the buffer data when the data are ready.
   */
  async getArrayBufferAsync(e, t = null, s = 0, r = -1) {
    return await this.attributeUtils.getArrayBufferAsync(e, t, s, r);
  }
  /**
   * Ensures the backend is XR compatible.
   *
   * @async
   * @return {Promise} A Promise that resolve when the renderer is XR compatible.
   */
  async makeXRCompatible() {
    this.gl.getContextAttributes().xrCompatible !== !0 && await this.gl.makeXRCompatible();
  }
  /**
   * Sets the XR rendering destination.
   *
   * @param {WebGLFramebuffer} xrFramebuffer - The XR framebuffer.
   */
  setXRTarget(e) {
    this._xrFramebuffer = e;
  }
  /**
   * Configures the given XR render target with external textures.
   *
   * This method is only relevant when using the WebXR Layers API.
   *
   * @param {XRRenderTarget} renderTarget - The XR render target.
   * @param {WebGLTexture} colorTexture - A native color texture.
   * @param {?WebGLTexture} [depthTexture=null] - A native depth texture.
   */
  setXRRenderTargetTextures(e, t, s = null) {
    const r = this.gl;
    if (this.set(e.texture, { textureGPU: t, glInternalFormat: r.RGBA8 }), s !== null) {
      const n = e.stencilBuffer ? r.DEPTH24_STENCIL8 : r.DEPTH_COMPONENT24;
      this.set(e.depthTexture, { textureGPU: s, glInternalFormat: n }), this.extensions.has("WEBGL_multisampled_render_to_texture") === !0 && e._autoAllocateDepthBuffer === !0 && e.multiview === !1 && L("WebGLBackend: Render-to-texture extension was disabled because an external texture was provided"), e._autoAllocateDepthBuffer = !1;
    }
  }
  /**
   * Inits a time stamp query for the given render context.
   *
   * @param {string} type - The type of the timestamp query.
   * @param {string} uid - A unique identifier for the timestamp query.
   */
  initTimestampQuery(e, t) {
    if (!this.disjoint || !this.trackTimestamp) return;
    this.timestampQueryPool[e] || (this.timestampQueryPool[e] = new KS(this.gl, e, 2048));
    const s = this.timestampQueryPool[e];
    s.allocateQueriesForContext(t) !== null && s.beginQuery(t);
  }
  // timestamp utils
  /**
   * Prepares the timestamp buffer.
   *
   * @param {string} type - The type of the timestamp query.
   * @param {string} uid - A unique identifier for the timestamp query.
   */
  prepareTimestampBuffer(e, t) {
    if (!this.disjoint || !this.trackTimestamp) return;
    this.timestampQueryPool[e].endQuery(t);
  }
  /**
   * Returns the backend's rendering context.
   *
   * @return {WebGL2RenderingContext} The rendering context.
   */
  getContext() {
    return this.gl;
  }
  /**
   * This method is executed at the beginning of a render call and prepares
   * the WebGL state for upcoming render calls
   *
   * @param {RenderContext} renderContext - The render context.
   */
  beginRender(e) {
    const { state: t } = this, s = this.get(e);
    if (e.viewport)
      this.updateViewport(e);
    else {
      const { width: n, height: i } = this.getDrawingBufferSize();
      t.viewport(0, 0, n, i);
    }
    if (e.scissor)
      this.updateScissor(e);
    else {
      const { width: n, height: i } = this.getDrawingBufferSize();
      t.scissor(0, 0, n, i);
    }
    this.initTimestampQuery(Nt.RENDER, this.getTimestampUID(e)), s.previousContext = this._currentContext, this._currentContext = e, this._setFramebuffer(e), this.clear(e.clearColor, e.clearDepth, e.clearStencil, e, !1);
    const r = e.occlusionQueryCount;
    r > 0 && (s.currentOcclusionQueries = s.occlusionQueries, s.currentOcclusionQueryObjects = s.occlusionQueryObjects, s.lastOcclusionObject = null, s.occlusionQueries = new Array(r), s.occlusionQueryObjects = new Array(r), s.occlusionQueryIndex = 0);
  }
  /**
   * This method is executed at the end of a render call and finalizes work
   * after draw calls.
   *
   * @param {RenderContext} renderContext - The render context.
   */
  finishRender(e) {
    const { gl: t, state: s } = this, r = this.get(e), n = r.previousContext;
    s.resetVertexState();
    const i = e.occlusionQueryCount;
    i > 0 && (i > r.occlusionQueryIndex && t.endQuery(t.ANY_SAMPLES_PASSED), this.resolveOccludedAsync(e));
    const o = e.textures;
    if (o !== null)
      for (let u = 0; u < o.length; u++) {
        const l = o[u];
        l.generateMipmaps && this.generateMipmaps(l);
      }
    if (this._currentContext = n, this._resolveRenderTarget(e), n !== null) {
      if (this._setFramebuffer(n), n.viewport)
        this.updateViewport(n);
      else {
        const { width: u, height: l } = this.getDrawingBufferSize();
        s.viewport(0, 0, u, l);
      }
      if (n.scissor)
        this.updateScissor(n);
      else {
        const { width: u, height: l } = this.getDrawingBufferSize();
        s.scissor(0, 0, u, l);
      }
    }
    this.prepareTimestampBuffer(Nt.RENDER, this.getTimestampUID(e));
  }
  /**
   * This method processes the result of occlusion queries and writes it
   * into render context data.
   *
   * @async
   * @param {RenderContext} renderContext - The render context.
   */
  resolveOccludedAsync(e) {
    const t = this.get(e), { currentOcclusionQueries: s, currentOcclusionQueryObjects: r } = t;
    if (s && r) {
      const n = /* @__PURE__ */ new WeakSet(), { gl: i } = this;
      t.currentOcclusionQueryObjects = null, t.currentOcclusionQueries = null;
      const o = () => {
        let u = 0;
        for (let l = 0; l < s.length; l++) {
          const c = s[l];
          c !== null && i.getQueryParameter(c, i.QUERY_RESULT_AVAILABLE) && (i.getQueryParameter(c, i.QUERY_RESULT) === 0 && n.add(r[l]), s[l] = null, i.deleteQuery(c), u++);
        }
        u < s.length ? requestAnimationFrame(o) : t.occluded = n;
      };
      o();
    }
  }
  /**
   * Returns `true` if the given 3D object is fully occluded by other
   * 3D objects in the scene.
   *
   * @param {RenderContext} renderContext - The render context.
   * @param {Object3D} object - The 3D object to test.
   * @return {boolean} Whether the 3D object is fully occluded or not.
   */
  isOccluded(e, t) {
    const s = this.get(e);
    return s.occluded && s.occluded.has(t);
  }
  /**
   * Updates the viewport with the values from the given render context.
   *
   * @param {RenderContext} renderContext - The render context.
   */
  updateViewport(e) {
    const { state: t } = this, { x: s, y: r, width: n, height: i } = e.viewportValue;
    t.viewport(s, e.height - i - r, n, i);
  }
  /**
   * Updates the scissor with the values from the given render context.
   *
   * @param {RenderContext} renderContext - The render context.
   */
  updateScissor(e) {
    const { state: t } = this, { x: s, y: r, width: n, height: i } = e.scissorValue;
    t.scissor(s, e.height - i - r, n, i);
  }
  /**
   * Defines the scissor test.
   *
   * @param {boolean} boolean - Whether the scissor test should be enabled or not.
   */
  setScissorTest(e) {
    this.state.setScissorTest(e);
  }
  /**
   * Returns the clear color and alpha into a single
   * color object.
   *
   * @return {Color4} The clear color.
   */
  getClearColor() {
    const e = super.getClearColor();
    return e.r *= e.a, e.g *= e.a, e.b *= e.a, e;
  }
  /**
   * Performs a clear operation.
   *
   * @param {boolean} color - Whether the color buffer should be cleared or not.
   * @param {boolean} depth - Whether the depth buffer should be cleared or not.
   * @param {boolean} stencil - Whether the stencil buffer should be cleared or not.
   * @param {?Object} [descriptor=null] - The render context of the current set render target.
   * @param {boolean} [setFrameBuffer=true] - Controls whether the intermediate framebuffer should be set or not.
   * @param {boolean} [resolveRenderTarget=true] - Controls whether an active render target should be resolved
   * or not. Only relevant for explicit clears.
   */
  clear(e, t, s, r = null, n = !0, i = !0) {
    const { gl: o, renderer: u } = this;
    r === null && (r = {
      textures: null,
      clearColorValue: this.getClearColor()
    });
    let l = 0;
    if (e && (l |= o.COLOR_BUFFER_BIT), t && (l |= o.DEPTH_BUFFER_BIT), s && (l |= o.STENCIL_BUFFER_BIT), l !== 0) {
      let c;
      r.clearColorValue ? c = r.clearColorValue : c = this.getClearColor();
      const d = u.getClearDepth(), h = u.getClearStencil();
      if (t && this.state.setDepthMask(!0), r.textures === null)
        o.clearColor(c.r, c.g, c.b, c.a), o.clear(l);
      else {
        if (n && this._setFramebuffer(r), e)
          for (let f = 0; f < r.textures.length; f++)
            f === 0 ? o.clearBufferfv(o.COLOR, f, [c.r, c.g, c.b, c.a]) : o.clearBufferfv(o.COLOR, f, [0, 0, 0, 1]);
        t && s ? o.clearBufferfi(o.DEPTH_STENCIL, 0, d, h) : t ? o.clearBufferfv(o.DEPTH, 0, [d]) : s && o.clearBufferiv(o.STENCIL, 0, [h]), n && i && this._resolveRenderTarget(r);
      }
    }
  }
  /**
   * This method is executed at the beginning of a compute call and
   * prepares the state for upcoming compute tasks.
   *
   * @param {Node|Array<Node>} computeGroup - The compute node(s).
   */
  beginCompute(e) {
    const { state: t, gl: s } = this;
    t.bindFramebuffer(s.FRAMEBUFFER, null), this.initTimestampQuery(Nt.COMPUTE, this.getTimestampUID(e));
  }
  /**
   * Executes a compute command for the given compute node.
   *
   * @param {Node|Array<Node>} computeGroup - The group of compute nodes of a compute call. Can be a single compute node.
   * @param {Node} computeNode - The compute node.
   * @param {Array<BindGroup>} bindings - The bindings.
   * @param {ComputePipeline} pipeline - The compute pipeline.
   * @param {?number} [count=null] - The count of compute invocations. If `null`, the count is determined by the compute node.
   */
  compute(e, t, s, r, n = null) {
    const { state: i, gl: o } = this;
    this.discard === !1 && (i.enable(o.RASTERIZER_DISCARD), this.discard = !0);
    const { programGPU: u, transformBuffers: l, attributes: c } = this.get(r), d = this._getVaoKey(c), h = this.vaoCache[d];
    h === void 0 ? this.vaoCache[d] = this._createVao(c) : i.setVertexState(h), i.useProgram(u), this._bindUniforms(s);
    const f = this._getTransformFeedback(l);
    o.bindTransformFeedback(o.TRANSFORM_FEEDBACK, f), o.beginTransformFeedback(o.POINTS), n = n !== null ? n : t.count, Array.isArray(n) ? (Te("WebGLBackend.compute(): The count parameter must be a single number, not an array."), n = n[0]) : n && typeof n == "object" && n.isIndirectStorageBufferAttribute && (Te("WebGLBackend.compute(): The count parameter must be a single number, not IndirectStorageBufferAttribute"), n = t.count), c[0].isStorageInstancedBufferAttribute ? o.drawArraysInstanced(o.POINTS, 0, 1, n) : o.drawArrays(o.POINTS, 0, n), o.endTransformFeedback(), o.bindTransformFeedback(o.TRANSFORM_FEEDBACK, null);
    for (let p = 0; p < l.length; p++) {
      const g = l[p];
      g.pbo && this.has(g.pbo) && this.textureUtils.copyBufferToTexture(g.transformBuffer, g.pbo), g.switchBuffers();
    }
  }
  /**
   * This method is executed at the end of a compute call and
   * finalizes work after compute tasks.
   *
   * @param {Node|Array<Node>} computeGroup - The compute node(s).
   */
  finishCompute(e) {
    const { state: t, gl: s } = this;
    this.discard = !1, t.disable(s.RASTERIZER_DISCARD), this.prepareTimestampBuffer(Nt.COMPUTE, this.getTimestampUID(e)), this._currentContext && this._setFramebuffer(this._currentContext);
  }
  /**
   * Internal to determine if the current render target is a render target array with depth 2D array texture.
   *
   * @param {RenderContext} renderContext - The render context.
   * @return {boolean} Whether the render target is a render target array with depth 2D array texture.
   *
   * @private
   */
  _isRenderCameraDepthArray(e) {
    return e.depthTexture && e.depthTexture.isArrayTexture && e.camera.isArrayCamera;
  }
  /**
   * Internal draw function.
   *
   * @private
   * @param {Object3D} object - The object to render.
   * @param {WebGLBufferRenderer} renderer - The internal renderer.
   * @param {number} firstVertex - The first vertex to render.
   * @param {number} vertexCount - The vertex count.
   * @param {number} instanceCount - The intance count.
   * @param {WebGLProgram} programGPU - The raw WebGL shader program.
   */
  _draw(e, t, s, r, n, i) {
    if (e.isBatchedMesh)
      if (this.hasFeature("WEBGL_multi_draw") === !1) {
        const { gl: o } = this, u = o.getUniformLocation(i, "nodeUniformDrawId"), l = e._multiDrawStarts, c = e._multiDrawCounts, d = e._multiDrawCount;
        for (let h = 0; h < d; h++)
          o.uniform1ui(u, h), t.render(l[h], c[h]);
      } else
        t.renderMultiDraw(e._multiDrawStarts, e._multiDrawCounts, e._multiDrawCount);
    else n > 1 ? t.renderInstances(s, r, n) : t.render(s, r);
  }
  /**
   * Executes a draw command for the given render object.
   *
   * @param {RenderObject} renderObject - The render object to draw.
   * @param {Info} info - Holds a series of statistical information about the GPU memory and the rendering process.
   */
  draw(e) {
    const { object: t, pipeline: s, material: r, context: n, hardwareClippingPlanes: i } = e, { programGPU: o } = this.get(s), { gl: u, state: l } = this, c = this.get(n), d = e.getDrawParameters();
    if (d === null) return;
    this._bindUniforms(e.getBindings());
    const h = t.isMesh && t.matrixWorld.determinant() < 0;
    l.setMaterial(r, h, i), n.mrt !== null && n.textures !== null && l.setMRTBlending(n.textures, n.mrt, r), l.useProgram(o);
    const f = e.getAttributes(), p = this.get(f);
    let g = p.vaoGPU;
    if (g === void 0) {
      const E = this._getVaoKey(f);
      g = this.vaoCache[E], g === void 0 && (g = this._createVao(f), this.vaoCache[E] = g, p.vaoGPU = g);
    }
    const m = e.getIndex(), y = m !== null ? this.get(m).bufferGPU : null;
    l.setVertexState(g, y);
    const _ = c.lastOcclusionObject;
    if (_ !== t && _ !== void 0) {
      if (_ !== null && _.occlusionTest === !0 && (u.endQuery(u.ANY_SAMPLES_PASSED), c.occlusionQueryIndex++), t.occlusionTest === !0) {
        const E = u.createQuery();
        u.beginQuery(u.ANY_SAMPLES_PASSED, E), c.occlusionQueries[c.occlusionQueryIndex] = E, c.occlusionQueryObjects[c.occlusionQueryIndex] = t;
      }
      c.lastOcclusionObject = t;
    }
    const v = this.bufferRenderer;
    t.isPoints ? v.mode = u.POINTS : t.isLineSegments ? v.mode = u.LINES : t.isLine ? v.mode = u.LINE_STRIP : t.isLineLoop ? v.mode = u.LINE_LOOP : r.wireframe === !0 ? (l.setLineWidth(r.wireframeLinewidth * this.renderer.getPixelRatio()), v.mode = u.LINES) : v.mode = u.TRIANGLES;
    const { vertexCount: C, instanceCount: w } = d;
    let { firstVertex: M } = d;
    if (v.object = t, m !== null) {
      M *= m.array.BYTES_PER_ELEMENT;
      const E = this.get(m);
      v.index = m.count, v.type = E.type;
    } else
      v.index = 0;
    if (e.camera.isArrayCamera === !0 && e.camera.cameras.length > 0 && e.camera.isMultiViewCamera === !1) {
      const E = this.get(e.camera), U = e.camera.cameras, P = e.getBindingGroup("cameraIndex").bindings[0];
      if (E.indexesGPU === void 0 || E.indexesGPU.length !== U.length) {
        const K = new Uint32Array([0, 0, 0, 0]), ae = [];
        for (let Le = 0, Ce = U.length; Le < Ce; Le++) {
          const ht = u.createBuffer();
          K[0] = Le, u.bindBuffer(u.UNIFORM_BUFFER, ht), u.bufferData(u.UNIFORM_BUFFER, K, u.STATIC_DRAW), ae.push(ht);
        }
        E.indexesGPU = ae;
      }
      let $ = 0;
      e: for (const K of e.getBindings())
        for (const ae of K.bindings) {
          if (ae === P) break e;
          (ae.isUniformsGroup || ae.isUniformBuffer) && $++;
        }
      const Y = this.renderer.getPixelRatio(), ie = this._currentContext.renderTarget, se = this._isRenderCameraDepthArray(this._currentContext), oe = this._currentContext.activeCubeFace;
      if (se) {
        const K = this.get(ie.depthTexture);
        if (K.clearedRenderId !== this.renderer._nodes.nodeFrame.renderId) {
          K.clearedRenderId = this.renderer._nodes.nodeFrame.renderId;
          const { stencilBuffer: ae } = ie;
          for (let Le = 0, Ce = U.length; Le < Ce; Le++)
            this.renderer._activeCubeFace = Le, this._currentContext.activeCubeFace = Le, this._setFramebuffer(this._currentContext), this.clear(!1, !0, ae, this._currentContext, !1, !1);
          this.renderer._activeCubeFace = oe, this._currentContext.activeCubeFace = oe;
        }
      }
      for (let K = 0, ae = U.length; K < ae; K++) {
        const Le = U[K];
        if (t.layers.test(Le.layers)) {
          se && (this.renderer._activeCubeFace = K, this._currentContext.activeCubeFace = K, this._setFramebuffer(this._currentContext));
          const Ce = Le.viewport;
          if (Ce !== void 0) {
            const ht = Ce.x * Y, bi = Ce.y * Y, Gs = Ce.width * Y, Va = Ce.height * Y;
            l.viewport(
              Math.floor(ht),
              Math.floor(e.context.height - Va - bi),
              Math.floor(Gs),
              Math.floor(Va)
            );
          }
          l.bindBufferBase(u.UNIFORM_BUFFER, $, E.indexesGPU[K]), this._draw(t, v, M, C, w, o);
        }
        this._currentContext.activeCubeFace = oe, this.renderer._activeCubeFace = oe;
      }
    } else
      this._draw(t, v, M, C, w, o);
  }
  /**
   * Explain why always null is returned.
   *
   * @param {RenderObject} renderObject - The render object.
   * @return {boolean} Whether the render pipeline requires an update or not.
   */
  needsRenderUpdate() {
    return !1;
  }
  /**
   * Explain why no cache key is computed.
   *
   * @param {RenderObject} renderObject - The render object.
   * @return {string} The cache key.
   */
  getRenderCacheKey() {
    return "";
  }
  // textures
  /**
   * Creates a default texture for the given texture that can be used
   * as a placeholder until the actual texture is ready for usage.
   *
   * @param {Texture} texture - The texture to create a default texture for.
   */
  createDefaultTexture(e) {
    this.textureUtils.createDefaultTexture(e);
  }
  /**
   * Defines a texture on the GPU for the given texture object.
   *
   * @param {Texture} texture - The texture.
   * @param {Object} [options={}] - Optional configuration parameter.
   */
  createTexture(e, t) {
    this.textureUtils.createTexture(e, t);
  }
  /**
   * Uploads the updated texture data to the GPU.
   *
   * @param {Texture} texture - The texture.
   * @param {Object} [options={}] - Optional configuration parameter.
   */
  updateTexture(e, t) {
    this.textureUtils.updateTexture(e, t);
  }
  /**
   * Generates mipmaps for the given texture.
   *
   * @param {Texture} texture - The texture.
   */
  generateMipmaps(e) {
    this.textureUtils.generateMipmaps(e);
  }
  /**
   * Destroys the GPU data for the given texture object.
   *
   * @param {Texture} texture - The texture.
   * @param {boolean} [isDefaultTexture=false] - Whether the texture uses a default GPU texture or not.
   */
  destroyTexture(e, t = !1) {
    this.textureUtils.destroyTexture(e, t);
  }
  /**
   * Returns texture data as a typed array.
   *
   * @async
   * @param {Texture} texture - The texture to copy.
   * @param {number} x - The x coordinate of the copy origin.
   * @param {number} y - The y coordinate of the copy origin.
   * @param {number} width - The width of the copy.
   * @param {number} height - The height of the copy.
   * @param {number} faceIndex - The face index.
   * @return {Promise<TypedArray>} A Promise that resolves with a typed array when the copy operation has finished.
   */
  async copyTextureToBuffer(e, t, s, r, n, i) {
    return this.textureUtils.copyTextureToBuffer(e, t, s, r, n, i);
  }
  /**
   * This method does nothing since WebGL 2 has no concept of samplers.
   *
   * @param {Texture} texture - The texture to update the sampler for.
   * @return {string} The current sampler key.
   */
  updateSampler() {
    return "";
  }
  // node builder
  /**
   * Returns a node builder for the given render object.
   *
   * @param {RenderObject} object - The render object.
   * @param {Renderer} renderer - The renderer.
   * @return {GLSLNodeBuilder} The node builder.
   */
  createNodeBuilder(e, t) {
    return new OS(e, t);
  }
  // program
  /**
   * Creates a shader program from the given programmable stage.
   *
   * @param {ProgrammableStage} program - The programmable stage.
   */
  createProgram(e) {
    const t = this.gl, { stage: s, code: r } = e, n = s === "fragment" ? t.createShader(t.FRAGMENT_SHADER) : t.createShader(t.VERTEX_SHADER);
    t.shaderSource(n, r), t.compileShader(n), this.set(e, {
      shaderGPU: n
    });
  }
  /**
   * Destroys the shader program of the given programmable stage.
   *
   * @param {ProgrammableStage} program - The programmable stage.
   */
  destroyProgram(e) {
    this.delete(e);
  }
  /**
   * Creates a render pipeline for the given render object.
   *
   * @param {RenderObject} renderObject - The render object.
   * @param {Array<Promise>} promises - An array of compilation promises which are used in `compileAsync()`.
   */
  createRenderPipeline(e, t) {
    const s = this.gl, r = e.pipeline, { fragmentProgram: n, vertexProgram: i } = r, o = s.createProgram(), u = this.get(n).shaderGPU, l = this.get(i).shaderGPU;
    if (s.attachShader(o, u), s.attachShader(o, l), s.linkProgram(o), this.set(r, {
      programGPU: o,
      fragmentShader: u,
      vertexShader: l
    }), t !== null && this.parallel) {
      const c = new Promise((d) => {
        const h = this.parallel, f = () => {
          s.getProgramParameter(o, h.COMPLETION_STATUS_KHR) ? (this._completeCompile(e, r), d()) : requestAnimationFrame(f);
        };
        f();
      });
      t.push(c);
      return;
    }
    this._completeCompile(e, r);
  }
  /**
   * Formats the source code of error messages.
   *
   * @private
   * @param {string} string - The code.
   * @param {number} errorLine - The error line.
   * @return {string} The formatted code.
   */
  _handleSource(e, t) {
    const s = e.split(`
`), r = [], n = Math.max(t - 6, 0), i = Math.min(t + 6, s.length);
    for (let o = n; o < i; o++) {
      const u = o + 1;
      r.push(`${u === t ? ">" : " "} ${u}: ${s[o]}`);
    }
    return r.join(`
`);
  }
  /**
   * Gets the shader compilation errors from the info log.
   *
   * @private
   * @param {WebGL2RenderingContext} gl - The rendering context.
   * @param {WebGLShader} shader - The WebGL shader object.
   * @param {string} type - The shader type.
   * @return {string} The shader errors.
   */
  _getShaderErrors(e, t, s) {
    const r = e.getShaderParameter(t, e.COMPILE_STATUS), i = (e.getShaderInfoLog(t) || "").trim();
    if (r && i === "") return "";
    const o = /ERROR: 0:(\d+)/.exec(i);
    if (o) {
      const u = parseInt(o[1]);
      return s.toUpperCase() + `

` + i + `

` + this._handleSource(e.getShaderSource(t), u);
    } else
      return i;
  }
  /**
   * Logs shader compilation errors.
   *
   * @private
   * @param {WebGLProgram} programGPU - The WebGL program.
   * @param {WebGLShader} glFragmentShader - The fragment shader as a native WebGL shader object.
   * @param {WebGLShader} glVertexShader - The vertex shader as a native WebGL shader object.
   */
  _logProgramError(e, t, s) {
    if (this.renderer.debug.checkShaderErrors) {
      const r = this.gl, i = (r.getProgramInfoLog(e) || "").trim();
      if (r.getProgramParameter(e, r.LINK_STATUS) === !1)
        if (typeof this.renderer.debug.onShaderError == "function")
          this.renderer.debug.onShaderError(r, e, s, t);
        else {
          const o = this._getShaderErrors(r, s, "vertex"), u = this._getShaderErrors(r, t, "fragment");
          F(
            "THREE.WebGLProgram: Shader Error " + r.getError() + " - VALIDATE_STATUS " + r.getProgramParameter(e, r.VALIDATE_STATUS) + `

Program Info Log: ` + i + `
` + o + `
` + u
          );
        }
      else i !== "" && L("WebGLProgram: Program Info Log:", i);
    }
  }
  /**
   * Completes the shader program setup for the given render object.
   *
   * @private
   * @param {RenderObject} renderObject - The render object.
   * @param {RenderPipeline} pipeline - The render pipeline.
   */
  _completeCompile(e, t) {
    const { state: s, gl: r } = this, n = this.get(t), { programGPU: i, fragmentShader: o, vertexShader: u } = n;
    r.getProgramParameter(i, r.LINK_STATUS) === !1 && this._logProgramError(i, o, u), s.useProgram(i);
    const l = e.getBindings();
    this._setupBindings(l, i), this.set(t, {
      programGPU: i,
      pipeline: i
    });
  }
  /**
   * Creates a compute pipeline for the given compute node.
   *
   * @param {ComputePipeline} computePipeline - The compute pipeline.
   * @param {Array<BindGroup>} bindings - The bindings.
   */
  createComputePipeline(e, t) {
    const { state: s, gl: r } = this, n = {
      stage: "fragment",
      code: `#version 300 es
precision highp float;
void main() {}`
    };
    this.createProgram(n);
    const { computeProgram: i } = e, o = r.createProgram(), u = this.get(n).shaderGPU, l = this.get(i).shaderGPU, c = i.transforms, d = [], h = [];
    for (let m = 0; m < c.length; m++) {
      const y = c[m];
      d.push(y.varyingName), h.push(y.attributeNode);
    }
    r.attachShader(o, u), r.attachShader(o, l), r.transformFeedbackVaryings(
      o,
      d,
      r.SEPARATE_ATTRIBS
    ), r.linkProgram(o), r.getProgramParameter(o, r.LINK_STATUS) === !1 && this._logProgramError(o, u, l), s.useProgram(o), this._setupBindings(t, o);
    const f = i.attributes, p = [], g = [];
    for (let m = 0; m < f.length; m++) {
      const y = f[m].node.attribute;
      p.push(y), this.has(y) || this.attributeUtils.createAttribute(y, r.ARRAY_BUFFER);
    }
    for (let m = 0; m < h.length; m++) {
      const y = h[m].attribute;
      this.has(y) || this.attributeUtils.createAttribute(y, r.ARRAY_BUFFER);
      const _ = this.get(y);
      g.push(_);
    }
    this.set(e, {
      programGPU: o,
      transformBuffers: g,
      attributes: p
    });
  }
  /**
   * Creates bindings from the given bind group definition.
   *
   * @param {BindGroup} bindGroup - The bind group.
   * @param {Array<BindGroup>} bindings - Array of bind groups.
   * @param {number} cacheIndex - The cache index.
   * @param {number} version - The version.
   */
  createBindings(e, t) {
    if (this._knownBindings.has(t) === !1) {
      this._knownBindings.add(t);
      let s = 0, r = 0;
      for (const n of t) {
        this.set(n, {
          textures: r,
          uniformBuffers: s
        });
        for (const i of n.bindings)
          i.isUniformBuffer && s++, i.isSampledTexture && r++;
      }
    }
    this.updateBindings(e, t);
  }
  /**
   * Updates the given bind group definition.
   *
   * @param {BindGroup} bindGroup - The bind group.
   * @param {Array<BindGroup>} bindings - Array of bind groups.
   * @param {number} cacheIndex - The cache index.
   * @param {number} version - The version.
   */
  updateBindings(e) {
    const { gl: t } = this;
    for (const s of e.bindings) {
      const r = this.get(s);
      if (s.isUniformsGroup || s.isUniformBuffer) {
        const n = s.buffer;
        let { bufferGPU: i } = this.get(n);
        i === void 0 ? (i = t.createBuffer(), t.bindBuffer(t.UNIFORM_BUFFER, i), t.bufferData(t.UNIFORM_BUFFER, n.byteLength, t.DYNAMIC_DRAW), this.set(n, { bufferGPU: i })) : t.bindBuffer(t.UNIFORM_BUFFER, i);
        const o = s.updateRanges;
        if (t.bindBuffer(t.UNIFORM_BUFFER, i), o.length === 0)
          t.bufferData(t.UNIFORM_BUFFER, n, t.DYNAMIC_DRAW);
        else {
          const u = In(n), l = u ? 1 : n.BYTES_PER_ELEMENT;
          for (let c = 0, d = o.length; c < d; c++) {
            const h = o[c], f = h.start * l, p = h.count * l, g = f * (u ? n.BYTES_PER_ELEMENT : 1);
            t.bufferSubData(t.UNIFORM_BUFFER, g, n, f, p);
          }
        }
        r.bufferGPU = i, this.set(s, r);
      } else if (s.isSampledTexture) {
        const { textureGPU: n, glTextureType: i } = this.get(s.texture);
        r.textureGPU = n, r.glTextureType = i, this.set(s, r);
      }
    }
  }
  /**
   * Updates a buffer binding.
   *
   *  @param {Buffer} binding - The buffer binding to update.
   */
  updateBinding(e) {
    const t = this.gl;
    if (e.isUniformsGroup || e.isUniformBuffer) {
      const r = this.get(e).bufferGPU, n = e.buffer, i = e.updateRanges;
      if (t.bindBuffer(t.UNIFORM_BUFFER, r), i.length === 0)
        t.bufferData(t.UNIFORM_BUFFER, n, t.DYNAMIC_DRAW);
      else {
        const o = In(n), u = o ? 1 : n.BYTES_PER_ELEMENT;
        for (let l = 0, c = i.length; l < c; l++) {
          const d = i[l], h = d.start * u, f = d.count * u, p = h * (o ? n.BYTES_PER_ELEMENT : 1);
          t.bufferSubData(t.UNIFORM_BUFFER, p, n, h, f);
        }
      }
    }
  }
  // attributes
  /**
   * Creates the GPU buffer of an indexed shader attribute.
   *
   * @param {BufferAttribute} attribute - The indexed buffer attribute.
   */
  createIndexAttribute(e) {
    const t = this.gl;
    this.attributeUtils.createAttribute(e, t.ELEMENT_ARRAY_BUFFER);
  }
  /**
   * Creates the GPU buffer of a shader attribute.
   *
   * @param {BufferAttribute} attribute - The buffer attribute.
   */
  createAttribute(e) {
    if (this.has(e)) return;
    const t = this.gl;
    this.attributeUtils.createAttribute(e, t.ARRAY_BUFFER);
  }
  /**
   * Creates the GPU buffer of a storage attribute.
   *
   * @param {BufferAttribute} attribute - The buffer attribute.
   */
  createStorageAttribute(e) {
    if (this.has(e)) return;
    const t = this.gl;
    this.attributeUtils.createAttribute(e, t.ARRAY_BUFFER);
  }
  /**
   * Updates the GPU buffer of a shader attribute.
   *
   * @param {BufferAttribute} attribute - The buffer attribute to update.
   */
  updateAttribute(e) {
    this.attributeUtils.updateAttribute(e);
  }
  /**
   * Destroys the GPU buffer of a shader attribute.
   *
   * @param {BufferAttribute} attribute - The buffer attribute to destroy.
   */
  destroyAttribute(e) {
    this.attributeUtils.destroyAttribute(e);
  }
  /**
   * Checks if the given feature is supported  by the backend.
   *
   * @param {string} name - The feature's name.
   * @return {boolean} Whether the feature is supported or not.
   */
  hasFeature(e) {
    const t = Object.keys(yl).filter((r) => yl[r] === e), s = this.extensions;
    for (let r = 0; r < t.length; r++)
      if (s.has(t[r])) return !0;
    return !1;
  }
  /**
   * Copies data of the given source texture to the given destination texture.
   *
   * @param {Texture} srcTexture - The source texture.
   * @param {Texture} dstTexture - The destination texture.
   * @param {?(Box3|Box2)} [srcRegion=null] - The region of the source texture to copy.
   * @param {?(Vector2|Vector3)} [dstPosition=null] - The destination position of the copy.
   * @param {number} [srcLevel=0] - The source mip level to copy from.
   * @param {number} [dstLevel=0] - The destination mip level to copy to.
   */
  copyTextureToTexture(e, t, s = null, r = null, n = 0, i = 0) {
    this.textureUtils.copyTextureToTexture(e, t, s, r, n, i);
  }
  /**
   * Copies the current bound framebuffer to the given texture.
   *
   * @param {Texture} texture - The destination texture.
   * @param {RenderContext} renderContext - The render context.
   * @param {Vector4} rectangle - A four dimensional vector defining the origin and dimension of the copy.
   */
  copyFramebufferToTexture(e, t, s) {
    this.textureUtils.copyFramebufferToTexture(e, t, s);
  }
  /**
   * Checks if the given compatibility is supported by the backend.
   *
   * @param {string} name - The compatibility name.
   * @return {boolean} Whether the compatibility is supported or not.
   */
  hasCompatibility(e) {
    return e === Ps.TEXTURE_COMPARE ? !0 : super.hasCompatibility(e);
  }
  /**
   * Initializes the render target defined in the given render context.
   *
   * @param {RenderContext} renderContext - The render context.
   */
  initRenderTarget(e) {
    const { gl: t, state: s } = this;
    this._setFramebuffer(e), s.bindFramebuffer(t.FRAMEBUFFER, null);
  }
  /**
   * Configures the active framebuffer from the given render context.
   *
   * @private
   * @param {RenderContext} descriptor - The render context.
   */
  _setFramebuffer(e) {
    const { gl: t, state: s } = this;
    let r = null;
    if (e.textures !== null) {
      const n = e.renderTarget, i = this.get(n), { samples: o, depthBuffer: u, stencilBuffer: l } = n, c = n.isCubeRenderTarget === !0, d = n.isRenderTarget3D === !0, h = n.depth > 1, f = n.isXRRenderTarget === !0, p = f === !0 && n._hasExternalTextures === !0;
      let g = i.msaaFrameBuffer, m = i.depthRenderbuffer;
      const y = this.extensions.get("WEBGL_multisampled_render_to_texture"), _ = this.extensions.get("OVR_multiview2"), v = this._useMultisampledExtension(n), C = Qd(e);
      let w;
      if (c ? (i.cubeFramebuffers || (i.cubeFramebuffers = {}), w = i.cubeFramebuffers[C]) : f && p === !1 ? w = this._xrFramebuffer : (i.framebuffers || (i.framebuffers = {}), w = i.framebuffers[C]), w === void 0) {
        w = t.createFramebuffer(), s.bindFramebuffer(t.FRAMEBUFFER, w);
        const M = e.textures, E = [];
        if (c) {
          i.cubeFramebuffers[C] = w;
          const { textureGPU: P } = this.get(M[0]), $ = this.renderer._activeCubeFace, Y = this.renderer._activeMipmapLevel;
          t.framebufferTexture2D(t.FRAMEBUFFER, t.COLOR_ATTACHMENT0, t.TEXTURE_CUBE_MAP_POSITIVE_X + $, P, Y);
        } else {
          i.framebuffers[C] = w;
          for (let P = 0; P < M.length; P++) {
            const $ = M[P], Y = this.get($);
            Y.renderTarget = e.renderTarget, Y.cacheKey = C;
            const ie = t.COLOR_ATTACHMENT0 + P;
            if (n.multiview)
              _.framebufferTextureMultisampleMultiviewOVR(t.FRAMEBUFFER, ie, Y.textureGPU, 0, o, 0, 2);
            else if (d || h) {
              const se = this.renderer._activeCubeFace, oe = this.renderer._activeMipmapLevel;
              t.framebufferTextureLayer(t.FRAMEBUFFER, ie, Y.textureGPU, oe, se);
            } else if (v)
              y.framebufferTexture2DMultisampleEXT(t.FRAMEBUFFER, ie, t.TEXTURE_2D, Y.textureGPU, 0, o);
            else {
              const se = this.renderer._activeMipmapLevel;
              t.framebufferTexture2D(t.FRAMEBUFFER, ie, t.TEXTURE_2D, Y.textureGPU, se);
            }
          }
        }
        const U = l ? t.DEPTH_STENCIL_ATTACHMENT : t.DEPTH_ATTACHMENT;
        if (n._autoAllocateDepthBuffer === !0) {
          const P = t.createRenderbuffer();
          this.textureUtils.setupRenderBufferStorage(P, e, 0, v), i.xrDepthRenderbuffer = P, E.push(l ? t.DEPTH_STENCIL_ATTACHMENT : t.DEPTH_ATTACHMENT), t.bindRenderbuffer(t.RENDERBUFFER, P), t.framebufferRenderbuffer(t.FRAMEBUFFER, U, t.RENDERBUFFER, P);
        } else if (e.depthTexture !== null) {
          E.push(l ? t.DEPTH_STENCIL_ATTACHMENT : t.DEPTH_ATTACHMENT);
          const P = this.get(e.depthTexture);
          if (P.renderTarget = e.renderTarget, P.cacheKey = C, n.multiview)
            _.framebufferTextureMultisampleMultiviewOVR(t.FRAMEBUFFER, U, P.textureGPU, 0, o, 0, 2);
          else if (p && v)
            y.framebufferTexture2DMultisampleEXT(t.FRAMEBUFFER, U, t.TEXTURE_2D, P.textureGPU, 0, o);
          else if (e.depthTexture.isArrayTexture) {
            const $ = this.renderer._activeCubeFace;
            t.framebufferTextureLayer(t.FRAMEBUFFER, U, P.textureGPU, 0, $);
          } else if (e.depthTexture.isCubeTexture) {
            const $ = this.renderer._activeCubeFace;
            t.framebufferTexture2D(t.FRAMEBUFFER, U, t.TEXTURE_CUBE_MAP_POSITIVE_X + $, P.textureGPU, 0);
          } else
            t.framebufferTexture2D(t.FRAMEBUFFER, U, t.TEXTURE_2D, P.textureGPU, 0);
        }
        i.depthInvalidationArray = E;
      } else {
        if (this._isRenderCameraDepthArray(e)) {
          s.bindFramebuffer(t.FRAMEBUFFER, w);
          const E = this.renderer._activeCubeFace, U = this.get(e.depthTexture), P = l ? t.DEPTH_STENCIL_ATTACHMENT : t.DEPTH_ATTACHMENT;
          t.framebufferTextureLayer(
            t.FRAMEBUFFER,
            P,
            U.textureGPU,
            0,
            E
          );
        }
        if ((f || v || n.multiview) && n._isOpaqueFramebuffer !== !0) {
          s.bindFramebuffer(t.FRAMEBUFFER, w);
          const E = this.get(e.textures[0]);
          n.multiview ? _.framebufferTextureMultisampleMultiviewOVR(t.FRAMEBUFFER, t.COLOR_ATTACHMENT0, E.textureGPU, 0, o, 0, 2) : v ? y.framebufferTexture2DMultisampleEXT(t.FRAMEBUFFER, t.COLOR_ATTACHMENT0, t.TEXTURE_2D, E.textureGPU, 0, o) : t.framebufferTexture2D(t.FRAMEBUFFER, t.COLOR_ATTACHMENT0, t.TEXTURE_2D, E.textureGPU, 0);
          const U = l ? t.DEPTH_STENCIL_ATTACHMENT : t.DEPTH_ATTACHMENT;
          if (n._autoAllocateDepthBuffer === !0) {
            const P = i.xrDepthRenderbuffer;
            t.bindRenderbuffer(t.RENDERBUFFER, P), t.framebufferRenderbuffer(t.FRAMEBUFFER, U, t.RENDERBUFFER, P);
          } else {
            const P = this.get(e.depthTexture);
            n.multiview ? _.framebufferTextureMultisampleMultiviewOVR(t.FRAMEBUFFER, U, P.textureGPU, 0, o, 0, 2) : v ? y.framebufferTexture2DMultisampleEXT(t.FRAMEBUFFER, U, t.TEXTURE_2D, P.textureGPU, 0, o) : t.framebufferTexture2D(t.FRAMEBUFFER, U, t.TEXTURE_2D, P.textureGPU, 0);
          }
        }
      }
      if (o > 0 && v === !1 && !n.multiview) {
        if (g === void 0) {
          const M = [];
          g = t.createFramebuffer(), s.bindFramebuffer(t.FRAMEBUFFER, g);
          const E = [], U = e.textures;
          for (let P = 0; P < U.length; P++) {
            E[P] = t.createRenderbuffer(), t.bindRenderbuffer(t.RENDERBUFFER, E[P]), M.push(t.COLOR_ATTACHMENT0 + P);
            const $ = e.textures[P], Y = this.get($);
            t.renderbufferStorageMultisample(t.RENDERBUFFER, o, Y.glInternalFormat, e.width, e.height), t.framebufferRenderbuffer(t.FRAMEBUFFER, t.COLOR_ATTACHMENT0 + P, t.RENDERBUFFER, E[P]);
          }
          if (t.bindRenderbuffer(t.RENDERBUFFER, null), i.msaaFrameBuffer = g, i.msaaRenderbuffers = E, u && m === void 0) {
            m = t.createRenderbuffer(), this.textureUtils.setupRenderBufferStorage(m, e, o), i.depthRenderbuffer = m;
            const P = l ? t.DEPTH_STENCIL_ATTACHMENT : t.DEPTH_ATTACHMENT;
            M.push(P);
          }
          i.invalidationArray = M;
        }
        r = i.msaaFrameBuffer;
      } else
        r = w;
      s.drawBuffers(e, w);
    }
    s.bindFramebuffer(t.FRAMEBUFFER, r);
  }
  /**
   * Computes the VAO key for the given index and attributes.
   *
   * @private
   * @param {Array<BufferAttribute>} attributes - An array of buffer attributes.
   * @return {string} The VAO key.
   */
  _getVaoKey(e) {
    let t = "";
    for (let s = 0; s < e.length; s++) {
      const r = this.get(e[s]);
      t += ":" + r.id;
    }
    return t;
  }
  /**
   * Creates a VAO from the index and attributes.
   *
   * @private
   * @param {Array<BufferAttribute>} attributes - An array of buffer attributes.
   * @return {Object} The VAO data.
   */
  _createVao(e) {
    const { gl: t } = this, s = t.createVertexArray();
    t.bindVertexArray(s);
    for (let r = 0; r < e.length; r++) {
      const n = e[r], i = this.get(n);
      t.bindBuffer(t.ARRAY_BUFFER, i.bufferGPU), t.enableVertexAttribArray(r);
      let o, u;
      n.isInterleavedBufferAttribute === !0 ? (o = n.data.stride * i.bytesPerElement, u = n.offset * i.bytesPerElement) : (o = 0, u = 0), i.isInteger ? t.vertexAttribIPointer(r, n.itemSize, i.type, o, u) : t.vertexAttribPointer(r, n.itemSize, i.type, n.normalized, o, u), n.isInstancedBufferAttribute && !n.isInterleavedBufferAttribute ? t.vertexAttribDivisor(r, n.meshPerAttribute) : n.isInterleavedBufferAttribute && n.data.isInstancedInterleavedBuffer && t.vertexAttribDivisor(r, n.data.meshPerAttribute);
    }
    return t.bindBuffer(t.ARRAY_BUFFER, null), s;
  }
  /**
   * Creates a transform feedback from the given transform buffers.
   *
   * @private
   * @param {Array<DualAttributeData>} transformBuffers - The transform buffers.
   * @return {WebGLTransformFeedback} The transform feedback.
   */
  _getTransformFeedback(e) {
    let t = "";
    for (let n = 0; n < e.length; n++)
      t += ":" + e[n].id;
    let s = this.transformFeedbackCache[t];
    if (s !== void 0)
      return s;
    const { gl: r } = this;
    s = r.createTransformFeedback(), r.bindTransformFeedback(r.TRANSFORM_FEEDBACK, s);
    for (let n = 0; n < e.length; n++) {
      const i = e[n];
      r.bindBufferBase(r.TRANSFORM_FEEDBACK_BUFFER, n, i.transformBuffer);
    }
    return r.bindTransformFeedback(r.TRANSFORM_FEEDBACK, null), this.transformFeedbackCache[t] = s, s;
  }
  /**
   * Setups the given bindings.
   *
   * @private
   * @param {Array<BindGroup>} bindings - The bindings.
   * @param {WebGLProgram} programGPU - The WebGL program.
   */
  _setupBindings(e, t) {
    const s = this.gl;
    let r = 0, n = 0;
    for (const i of e)
      for (const o of i.bindings)
        if (o.isUniformsGroup || o.isUniformBuffer) {
          const u = r++, l = s.getUniformBlockIndex(t, o.name);
          s.uniformBlockBinding(t, l, u);
        } else if (o.isSampledTexture) {
          const u = n++, l = s.getUniformLocation(t, o.name);
          s.uniform1i(l, u);
        }
  }
  /**
   * Binds the given uniforms.
   *
   * @private
   * @param {Array<BindGroup>} bindings - The bindings.
   */
  _bindUniforms(e) {
    const { gl: t, state: s } = this;
    let r = 0, n = 0;
    for (const i of e)
      for (const o of i.bindings) {
        const u = this.get(o);
        if (o.isUniformsGroup || o.isUniformBuffer) {
          const l = r++;
          s.bindBufferBase(t.UNIFORM_BUFFER, l, u.bufferGPU);
        } else if (o.isSampledTexture) {
          const l = n++;
          s.bindTexture(u.glTextureType, u.textureGPU, t.TEXTURE0 + l);
        }
      }
  }
  /**
   * The method ensures multisampled render targets are resolved.
   *
   * @private
   * @param {RenderContext} renderContext - The render context.
   */
  _resolveRenderTarget(e) {
    const { gl: t, state: s } = this, r = e.renderTarget;
    if (e.textures !== null && r) {
      const n = this.get(r);
      if (r.samples > 0 && this._useMultisampledExtension(r) === !1) {
        const i = n.framebuffers[e.getCacheKey()];
        let o = t.COLOR_BUFFER_BIT;
        r.resolveDepthBuffer && (r.depthBuffer && (o |= t.DEPTH_BUFFER_BIT), r.stencilBuffer && r.resolveStencilBuffer && (o |= t.STENCIL_BUFFER_BIT));
        const u = n.msaaFrameBuffer, l = n.msaaRenderbuffers, c = e.textures, d = c.length > 1;
        if (s.bindFramebuffer(t.READ_FRAMEBUFFER, u), s.bindFramebuffer(t.DRAW_FRAMEBUFFER, i), d)
          for (let h = 0; h < c.length; h++)
            t.framebufferRenderbuffer(t.READ_FRAMEBUFFER, t.COLOR_ATTACHMENT0 + h, t.RENDERBUFFER, null), t.framebufferTexture2D(t.DRAW_FRAMEBUFFER, t.COLOR_ATTACHMENT0 + h, t.TEXTURE_2D, null, 0);
        for (let h = 0; h < c.length; h++) {
          if (d) {
            const { textureGPU: f } = this.get(c[h]);
            t.framebufferRenderbuffer(t.READ_FRAMEBUFFER, t.COLOR_ATTACHMENT0, t.RENDERBUFFER, l[h]), t.framebufferTexture2D(t.DRAW_FRAMEBUFFER, t.COLOR_ATTACHMENT0, t.TEXTURE_2D, f, 0);
          }
          if (e.scissor) {
            const { x: f, y: p, width: g, height: m } = e.scissorValue, y = e.height - m - p;
            t.blitFramebuffer(f, y, f + g, y + m, f, y, f + g, y + m, o, t.NEAREST);
          } else
            t.blitFramebuffer(0, 0, e.width, e.height, 0, 0, e.width, e.height, o, t.NEAREST);
        }
        if (d)
          for (let h = 0; h < c.length; h++) {
            const { textureGPU: f } = this.get(c[h]);
            t.framebufferRenderbuffer(t.READ_FRAMEBUFFER, t.COLOR_ATTACHMENT0 + h, t.RENDERBUFFER, l[h]), t.framebufferTexture2D(t.DRAW_FRAMEBUFFER, t.COLOR_ATTACHMENT0 + h, t.TEXTURE_2D, f, 0);
          }
        this._supportsInvalidateFramebuffer === !0 && t.invalidateFramebuffer(t.READ_FRAMEBUFFER, n.invalidationArray);
      } else if (r.resolveDepthBuffer === !1 && n.framebuffers) {
        const i = n.framebuffers[e.getCacheKey()];
        s.bindFramebuffer(t.DRAW_FRAMEBUFFER, i), t.invalidateFramebuffer(t.DRAW_FRAMEBUFFER, n.depthInvalidationArray);
      }
    }
  }
  /**
   * Returns `true` if the `WEBGL_multisampled_render_to_texture` extension
   * should be used when MSAA is enabled.
   *
   * @private
   * @param {RenderTarget} renderTarget - The render target that should be multisampled.
   * @return {boolean} Whether to use the `WEBGL_multisampled_render_to_texture` extension for MSAA or not.
   */
  _useMultisampledExtension(e) {
    return e.multiview === !0 ? !0 : e.samples > 0 && this.extensions.has("WEBGL_multisampled_render_to_texture") === !0 && e._autoAllocateDepthBuffer !== !1;
  }
  /**
   * Frees internal resources.
   */
  dispose() {
    this.textureUtils !== null && this.textureUtils.dispose();
    const e = this.extensions.get("WEBGL_lose_context");
    e && e.loseContext(), this.renderer.domElement.removeEventListener("webglcontextlost", this._onContextLost);
  }
}
const Nn = {
  PointList: "point-list",
  LineList: "line-list",
  LineStrip: "line-strip",
  TriangleList: "triangle-list"
}, At = typeof self < "u" && self.GPUShaderStage ? self.GPUShaderStage : { VERTEX: 1, FRAGMENT: 2, COMPUTE: 4 }, we = {
  Never: "never",
  Less: "less",
  Equal: "equal",
  LessEqual: "less-equal",
  Greater: "greater",
  NotEqual: "not-equal",
  GreaterEqual: "greater-equal",
  Always: "always"
}, Ie = {
  Store: "store"
}, te = {
  Load: "load",
  Clear: "clear"
}, bl = {
  CCW: "ccw",
  CW: "cw"
}, Sl = {
  None: "none",
  Back: "back"
}, Zn = {
  Uint16: "uint16",
  Uint32: "uint32"
}, x = {
  // 8-bit formats
  R8Unorm: "r8unorm",
  R8Snorm: "r8snorm",
  R8Uint: "r8uint",
  R8Sint: "r8sint",
  // 16-bit formats
  R16Uint: "r16uint",
  R16Sint: "r16sint",
  R16Float: "r16float",
  RG8Unorm: "rg8unorm",
  RG8Snorm: "rg8snorm",
  RG8Uint: "rg8uint",
  RG8Sint: "rg8sint",
  R16Unorm: "r16unorm",
  R16Snorm: "r16snorm",
  // 32-bit formats
  R32Uint: "r32uint",
  R32Sint: "r32sint",
  R32Float: "r32float",
  RG16Uint: "rg16uint",
  RG16Sint: "rg16sint",
  RG16Float: "rg16float",
  RGBA8Unorm: "rgba8unorm",
  RGBA8UnormSRGB: "rgba8unorm-srgb",
  RGBA8Snorm: "rgba8snorm",
  RGBA8Uint: "rgba8uint",
  RGBA8Sint: "rgba8sint",
  BGRA8Unorm: "bgra8unorm",
  BGRA8UnormSRGB: "bgra8unorm-srgb",
  RG16Unorm: "rg16unorm",
  RG16Snorm: "rg16snorm",
  // Packed 32-bit formats
  RGB9E5UFloat: "rgb9e5ufloat",
  RGB10A2Unorm: "rgb10a2unorm",
  RG11B10UFloat: "rg11b10ufloat",
  // 64-bit formats
  RG32Uint: "rg32uint",
  RG32Sint: "rg32sint",
  RG32Float: "rg32float",
  RGBA16Uint: "rgba16uint",
  RGBA16Sint: "rgba16sint",
  RGBA16Float: "rgba16float",
  RGBA16Unorm: "rgba16unorm",
  RGBA16Snorm: "rgba16snorm",
  // 128-bit formats
  RGBA32Uint: "rgba32uint",
  RGBA32Sint: "rgba32sint",
  RGBA32Float: "rgba32float",
  Depth16Unorm: "depth16unorm",
  Depth24Plus: "depth24plus",
  Depth24PlusStencil8: "depth24plus-stencil8",
  Depth32Float: "depth32float",
  // 'depth32float-stencil8' extension
  Depth32FloatStencil8: "depth32float-stencil8",
  // BC compressed formats usable if 'texture-compression-bc' is both
  // supported by the device/user agent and enabled in requestDevice.
  BC1RGBAUnorm: "bc1-rgba-unorm",
  BC1RGBAUnormSRGB: "bc1-rgba-unorm-srgb",
  BC2RGBAUnorm: "bc2-rgba-unorm",
  BC2RGBAUnormSRGB: "bc2-rgba-unorm-srgb",
  BC3RGBAUnorm: "bc3-rgba-unorm",
  BC3RGBAUnormSRGB: "bc3-rgba-unorm-srgb",
  BC4RUnorm: "bc4-r-unorm",
  BC4RSnorm: "bc4-r-snorm",
  BC5RGUnorm: "bc5-rg-unorm",
  BC5RGSnorm: "bc5-rg-snorm",
  BC6HRGBUFloat: "bc6h-rgb-ufloat",
  BC6HRGBFloat: "bc6h-rgb-float",
  BC7RGBAUnorm: "bc7-rgba-unorm",
  BC7RGBAUnormSRGB: "bc7-rgba-unorm-srgb",
  // ETC2 compressed formats usable if 'texture-compression-etc2' is both
  // supported by the device/user agent and enabled in requestDevice.
  ETC2RGB8Unorm: "etc2-rgb8unorm",
  ETC2RGB8UnormSRGB: "etc2-rgb8unorm-srgb",
  ETC2RGB8A1Unorm: "etc2-rgb8a1unorm",
  ETC2RGB8A1UnormSRGB: "etc2-rgb8a1unorm-srgb",
  ETC2RGBA8Unorm: "etc2-rgba8unorm",
  ETC2RGBA8UnormSRGB: "etc2-rgba8unorm-srgb",
  EACR11Unorm: "eac-r11unorm",
  EACR11Snorm: "eac-r11snorm",
  EACRG11Unorm: "eac-rg11unorm",
  EACRG11Snorm: "eac-rg11snorm",
  // ASTC compressed formats usable if 'texture-compression-astc' is both
  // supported by the device/user agent and enabled in requestDevice.
  ASTC4x4Unorm: "astc-4x4-unorm",
  ASTC4x4UnormSRGB: "astc-4x4-unorm-srgb",
  ASTC5x4Unorm: "astc-5x4-unorm",
  ASTC5x4UnormSRGB: "astc-5x4-unorm-srgb",
  ASTC5x5Unorm: "astc-5x5-unorm",
  ASTC5x5UnormSRGB: "astc-5x5-unorm-srgb",
  ASTC6x5Unorm: "astc-6x5-unorm",
  ASTC6x5UnormSRGB: "astc-6x5-unorm-srgb",
  ASTC6x6Unorm: "astc-6x6-unorm",
  ASTC6x6UnormSRGB: "astc-6x6-unorm-srgb",
  ASTC8x5Unorm: "astc-8x5-unorm",
  ASTC8x5UnormSRGB: "astc-8x5-unorm-srgb",
  ASTC8x6Unorm: "astc-8x6-unorm",
  ASTC8x6UnormSRGB: "astc-8x6-unorm-srgb",
  ASTC8x8Unorm: "astc-8x8-unorm",
  ASTC8x8UnormSRGB: "astc-8x8-unorm-srgb",
  ASTC10x5Unorm: "astc-10x5-unorm",
  ASTC10x5UnormSRGB: "astc-10x5-unorm-srgb",
  ASTC10x6Unorm: "astc-10x6-unorm",
  ASTC10x6UnormSRGB: "astc-10x6-unorm-srgb",
  ASTC10x8Unorm: "astc-10x8-unorm",
  ASTC10x8UnormSRGB: "astc-10x8-unorm-srgb",
  ASTC10x10Unorm: "astc-10x10-unorm",
  ASTC10x10UnormSRGB: "astc-10x10-unorm-srgb",
  ASTC12x10Unorm: "astc-12x10-unorm",
  ASTC12x10UnormSRGB: "astc-12x10-unorm-srgb",
  ASTC12x12Unorm: "astc-12x12-unorm",
  ASTC12x12UnormSRGB: "astc-12x12-unorm-srgb"
}, uo = {
  ClampToEdge: "clamp-to-edge",
  Repeat: "repeat",
  MirrorRepeat: "mirror-repeat"
}, Tt = {
  Linear: "linear",
  Nearest: "nearest"
}, j = {
  Zero: "zero",
  One: "one",
  Src: "src",
  OneMinusSrc: "one-minus-src",
  SrcAlpha: "src-alpha",
  OneMinusSrcAlpha: "one-minus-src-alpha",
  Dst: "dst",
  OneMinusDst: "one-minus-dst",
  DstAlpha: "dst-alpha",
  OneMinusDstAlpha: "one-minus-dst-alpha",
  SrcAlphaSaturated: "src-alpha-saturated",
  Constant: "constant",
  OneMinusConstant: "one-minus-constant"
}, hs = {
  Add: "add",
  Subtract: "subtract",
  ReverseSubtract: "reverse-subtract",
  Min: "min",
  Max: "max"
}, Nl = {
  None: 0,
  All: 15
}, Yt = {
  Keep: "keep",
  Zero: "zero",
  Replace: "replace",
  Invert: "invert",
  IncrementClamp: "increment-clamp",
  DecrementClamp: "decrement-clamp",
  IncrementWrap: "increment-wrap",
  DecrementWrap: "decrement-wrap"
}, lo = {
  Storage: "storage",
  ReadOnlyStorage: "read-only-storage"
}, co = {
  WriteOnly: "write-only",
  ReadOnly: "read-only",
  ReadWrite: "read-write"
}, Rl = {
  NonFiltering: "non-filtering",
  Comparison: "comparison"
}, fs = {
  Float: "float",
  UnfilterableFloat: "unfilterable-float",
  Depth: "depth",
  SInt: "sint",
  UInt: "uint"
}, Al = {
  TwoD: "2d",
  ThreeD: "3d"
}, Fe = {
  TwoD: "2d",
  TwoDArray: "2d-array",
  Cube: "cube",
  ThreeD: "3d"
}, QS = {
  All: "all"
}, Rn = {
  Vertex: "vertex",
  Instance: "instance"
}, Jn = {
  CoreFeaturesAndLimits: "core-features-and-limits",
  DepthClipControl: "depth-clip-control",
  Depth32FloatStencil8: "depth32float-stencil8",
  TextureCompressionBC: "texture-compression-bc",
  TextureCompressionBCSliced3D: "texture-compression-bc-sliced-3d",
  TextureCompressionETC2: "texture-compression-etc2",
  TextureCompressionASTC: "texture-compression-astc",
  TextureCompressionASTCSliced3D: "texture-compression-astc-sliced-3d",
  TimestampQuery: "timestamp-query",
  IndirectFirstInstance: "indirect-first-instance",
  ShaderF16: "shader-f16",
  RG11B10UFloat: "rg11b10ufloat-renderable",
  BGRA8UNormStorage: "bgra8unorm-storage",
  Float32Filterable: "float32-filterable",
  Float32Blendable: "float32-blendable",
  ClipDistances: "clip-distances",
  DualSourceBlending: "dual-source-blending",
  Subgroups: "subgroups",
  TextureFormatsTier1: "texture-formats-tier1",
  TextureFormatsTier2: "texture-formats-tier2"
}, vl = {
  "texture-compression-s3tc": "texture-compression-bc",
  "texture-compression-etc1": "texture-compression-etc2"
};
class YS extends gh {
  /**
   * Constructs a new node-based sampler.
   *
   * @param {string} name - The samplers's name.
   * @param {TextureNode} textureNode - The texture node.
   * @param {UniformGroupNode} groupNode - The uniform group node.
   */
  constructor(e, t, s) {
    super(e, t ? t.value : null), this.textureNode = t, this.groupNode = s;
  }
  /**
   * Updates the texture value of this sampler.
   *
   * @return {boolean} Whether the sampler needs an update or not.
   */
  update() {
    const { textureNode: e } = this;
    return this.texture !== e.value ? (this.texture = e.value, !0) : super.update();
  }
}
class jS extends dh {
  /**
   * Constructs a new uniform buffer.
   *
   * @param {string} name - The buffer's name.
   * @param {BufferAttribute} attribute - The buffer attribute.
   */
  constructor(e, t) {
    super(e, t ? t.array : null), this._attribute = t, this.isStorageBuffer = !0;
  }
  /**
   * The storage buffer attribute.
   *
   * @type {BufferAttribute}
   */
  get attribute() {
    return this._attribute;
  }
}
let ZS = 0;
class JS extends jS {
  /**
   * Constructs a new node-based storage buffer.
   *
   * @param {StorageBufferNode} nodeUniform - The storage buffer node.
   * @param {UniformGroupNode} groupNode - The uniform group node.
   */
  constructor(e, t) {
    super("StorageBuffer_" + ZS++, e ? e.value : null), this.nodeUniform = e, this.access = e ? e.access : nt.READ_WRITE, this.groupNode = t;
  }
  /**
   * The storage buffer attribute node.
   *
   * @type {StorageBufferAttribute}
   */
  get attribute() {
    return this.nodeUniform.value;
  }
  /**
   * The storage buffer.
   *
   * @type {Float32Array}
   */
  get buffer() {
    return this.nodeUniform.value.array;
  }
}
class eN extends ls {
  /**
   * Constructs a new utility object.
   *
   * @param {GPUDevice} device - The WebGPU device.
   */
  constructor(e) {
    super(), this.device = e;
    const t = `
struct VarysStruct {
	@builtin( position ) Position: vec4f,
	@location( 0 ) vTex : vec2f,
	@location( 1 ) @interpolate(flat, either) vBaseArrayLayer: u32,
};

@group( 0 ) @binding ( 2 )
var<uniform> flipY: u32;

@vertex
fn mainVS(
		@builtin( vertex_index ) vertexIndex : u32,
		@builtin( instance_index ) instanceIndex : u32 ) -> VarysStruct {

	var Varys : VarysStruct;

	var pos = array(
		vec2f( -1, -1 ),
		vec2f( -1,  3 ),
		vec2f(  3, -1 ),
	);

	let p = pos[ vertexIndex ];
	let mult = select( vec2f( 0.5, -0.5 ), vec2f( 0.5, 0.5 ), flipY != 0 );
	Varys.vTex = p * mult + vec2f( 0.5 );
	Varys.Position = vec4f( p, 0, 1 );
	Varys.vBaseArrayLayer = instanceIndex;

	return Varys;

}

@group( 0 ) @binding( 0 )
var imgSampler : sampler;

@group( 0 ) @binding( 1 )
var img2d : texture_2d<f32>;

@fragment
fn main_2d( Varys: VarysStruct ) -> @location( 0 ) vec4<f32> {

	return textureSample( img2d, imgSampler, Varys.vTex );

}

@group( 0 ) @binding( 1 )
var img2dArray : texture_2d_array<f32>;

@fragment
fn main_2d_array( Varys: VarysStruct ) -> @location( 0 ) vec4<f32> {

	return textureSample( img2dArray, imgSampler, Varys.vTex, Varys.vBaseArrayLayer );

}

const faceMat = array(
  mat3x3f(  0,  0,  -2,  0, -2,   0,  1,  1,   1 ),   // pos-x
  mat3x3f(  0,  0,   2,  0, -2,   0, -1,  1,  -1 ),   // neg-x
  mat3x3f(  2,  0,   0,  0,  0,   2, -1,  1,  -1 ),   // pos-y
  mat3x3f(  2,  0,   0,  0,  0,  -2, -1, -1,   1 ),   // neg-y
  mat3x3f(  2,  0,   0,  0, -2,   0, -1,  1,   1 ),   // pos-z
  mat3x3f( -2,  0,   0,  0, -2,   0,  1,  1,  -1 ),   // neg-z
);

@group( 0 ) @binding( 1 )
var imgCube : texture_cube<f32>;

@fragment
fn main_cube( Varys: VarysStruct ) -> @location( 0 ) vec4<f32> {

	return textureSample( imgCube, imgSampler, faceMat[ Varys.vBaseArrayLayer ] * vec3f( fract( Varys.vTex ), 1 ) );

}
`;
    this.mipmapSampler = e.createSampler({ minFilter: Tt.Linear }), this.flipYSampler = e.createSampler({ minFilter: Tt.Nearest }), this.flipUniformBuffer = e.createBuffer({
      size: 4,
      usage: GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST
    }), e.queue.writeBuffer(this.flipUniformBuffer, 0, new Uint32Array([1])), this.noFlipUniformBuffer = e.createBuffer({
      size: 4,
      usage: GPUBufferUsage.UNIFORM
    }), this.transferPipelines = {}, this.mipmapShaderModule = e.createShaderModule({
      label: "mipmap",
      code: t
    });
  }
  /**
   * Returns a render pipeline for the internal copy render pass. The pass
   * requires a unique render pipeline for each texture format.
   *
   * @param {string} format - The GPU texture format
   * @param {string?} textureBindingViewDimension - The GPU texture binding view dimension
   * @return {GPURenderPipeline} The GPU render pipeline.
   */
  getTransferPipeline(e, t) {
    t = t || "2d-array";
    const s = `${e}-${t}`;
    let r = this.transferPipelines[s];
    return r === void 0 && (r = this.device.createRenderPipeline({
      label: `mipmap-${e}-${t}`,
      vertex: {
        module: this.mipmapShaderModule
      },
      fragment: {
        module: this.mipmapShaderModule,
        entryPoint: `main_${t.replace("-", "_")}`,
        targets: [{ format: e }]
      },
      layout: "auto"
    }), this.transferPipelines[s] = r), r;
  }
  /**
   * Flip the contents of the given GPU texture along its vertical axis.
   *
   * @param {GPUTexture} textureGPU - The GPU texture object.
   * @param {Object} textureGPUDescriptor - The texture descriptor.
   * @param {number} [baseArrayLayer=0] - The index of the first array layer accessible to the texture view.
   */
  flipY(e, t, s = 0) {
    const r = t.format, { width: n, height: i } = t.size, o = this.device.createTexture({
      size: { width: n, height: i },
      format: r,
      usage: GPUTextureUsage.RENDER_ATTACHMENT | GPUTextureUsage.TEXTURE_BINDING
    }), u = this.getTransferPipeline(r, e.textureBindingViewDimension), l = this.getTransferPipeline(r, o.textureBindingViewDimension), c = this.device.createCommandEncoder({}), d = (h, f, p, g, m, y) => {
      const _ = h.getBindGroupLayout(0), v = this.device.createBindGroup({
        layout: _,
        entries: [{
          binding: 0,
          resource: this.flipYSampler
        }, {
          binding: 1,
          resource: f.createView({
            dimension: f.textureBindingViewDimension || "2d-array",
            baseMipLevel: 0,
            mipLevelCount: 1
          })
        }, {
          binding: 2,
          resource: { buffer: y ? this.flipUniformBuffer : this.noFlipUniformBuffer }
        }]
      }), C = c.beginRenderPass({
        colorAttachments: [{
          view: g.createView({
            dimension: "2d",
            baseMipLevel: 0,
            mipLevelCount: 1,
            baseArrayLayer: m,
            arrayLayerCount: 1
          }),
          loadOp: te.Clear,
          storeOp: Ie.Store
        }]
      });
      C.setPipeline(h), C.setBindGroup(0, v), C.draw(3, 1, 0, p), C.end();
    };
    d(u, e, s, o, 0, !1), d(l, o, 0, e, s, !0), this.device.queue.submit([c.finish()]), o.destroy();
  }
  /**
   * Generates mipmaps for the given GPU texture.
   *
   * @param {GPUTexture} textureGPU - The GPU texture object.
   * @param {?GPUCommandEncoder} [encoder=null] - An optional command encoder used to generate mipmaps.
   */
  generateMipmaps(e, t = null) {
    const s = this.get(e), r = s.layers || this._mipmapCreateBundles(e), n = t || this.device.createCommandEncoder({ label: "mipmapEncoder" });
    this._mipmapRunBundles(n, r), t === null && this.device.queue.submit([n.finish()]), s.layers = r;
  }
  /**
   * Since multiple copy render passes are required to generate mipmaps, the passes
   * are managed as render bundles to improve performance.
   *
   * @param {GPUTexture} textureGPU - The GPU texture object.
   * @return {Array<Object>} An array of render bundles.
   */
  _mipmapCreateBundles(e) {
    const t = e.textureBindingViewDimension || "2d-array", s = this.getTransferPipeline(e.format, t), r = s.getBindGroupLayout(0), n = [];
    for (let i = 1; i < e.mipLevelCount; i++)
      for (let o = 0; o < e.depthOrArrayLayers; o++) {
        const u = this.device.createBindGroup({
          layout: r,
          entries: [{
            binding: 0,
            resource: this.mipmapSampler
          }, {
            binding: 1,
            resource: e.createView({
              dimension: t,
              baseMipLevel: i - 1,
              mipLevelCount: 1
            })
          }, {
            binding: 2,
            resource: { buffer: this.noFlipUniformBuffer }
          }]
        }), l = {
          colorAttachments: [{
            view: e.createView({
              dimension: "2d",
              baseMipLevel: i,
              mipLevelCount: 1,
              baseArrayLayer: o,
              arrayLayerCount: 1
            }),
            loadOp: te.Clear,
            storeOp: Ie.Store
          }]
        }, c = this.device.createRenderBundleEncoder({
          colorFormats: [e.format]
        });
        c.setPipeline(s), c.setBindGroup(0, u), c.draw(3, 1, 0, o), n.push({
          renderBundles: [c.finish()],
          passDescriptor: l
        });
      }
    return n;
  }
  /**
   * Executes the render bundles.
   *
   * @param {GPUCommandEncoder} commandEncoder - The GPU command encoder.
   * @param {Array<Object>} passes - An array of render bundles.
   */
  _mipmapRunBundles(e, t) {
    const s = t.length;
    for (let r = 0; r < s; r++) {
      const n = t[r], i = e.beginRenderPass(n.passDescriptor);
      i.executeBundles(n.renderBundles), i.end();
    }
  }
}
const tN = {
  [Hl]: "never",
  [la]: "less",
  [zl]: "equal",
  [Qr]: "less-equal",
  [On]: "greater",
  [nr]: "greater-equal",
  [Wl]: "always",
  [$l]: "not-equal"
}, sN = [0, 1, 3, 2, 4, 5];
class rN {
  /**
   * Constructs a new utility object.
   *
   * @param {WebGPUBackend} backend - The WebGPU backend.
   */
  constructor(e) {
    this.backend = e, this._passUtils = null, this.defaultTexture = {}, this.defaultCubeTexture = {}, this.defaultVideoFrame = null, this._samplerCache = /* @__PURE__ */ new Map();
  }
  /**
   * Creates a GPU sampler for the given texture.
   *
   * @param {Texture} texture - The texture to create the sampler for.
   * @return {string} The current sampler key.
   */
  updateSampler(e) {
    const t = this.backend, s = e.minFilter + "-" + e.magFilter + "-" + e.wrapS + "-" + e.wrapT + "-" + (e.wrapR || "0") + "-" + e.anisotropy + "-" + (e.compareFunction || 0);
    let r = this._samplerCache.get(s);
    if (r === void 0) {
      const i = {
        addressModeU: this._convertAddressMode(e.wrapS),
        addressModeV: this._convertAddressMode(e.wrapT),
        addressModeW: this._convertAddressMode(e.wrapR),
        magFilter: this._convertFilterMode(e.magFilter),
        minFilter: this._convertFilterMode(e.minFilter),
        mipmapFilter: this._convertFilterMode(e.minFilter),
        maxAnisotropy: 1
      };
      e.isDepthTexture && e.compareFunction === null && (i.magFilter = Tt.Nearest, i.minFilter = Tt.Nearest, i.mipmapFilter = Tt.Nearest), i.magFilter === Tt.Linear && i.minFilter === Tt.Linear && i.mipmapFilter === Tt.Linear && (i.maxAnisotropy = e.anisotropy), e.isDepthTexture && e.compareFunction !== null && t.hasCompatibility(Ps.TEXTURE_COMPARE) && (i.compare = tN[e.compareFunction]), r = { sampler: t.device.createSampler(i), usedTimes: 0 }, this._samplerCache.set(s, r);
    }
    const n = t.get(e);
    if (n.sampler !== r.sampler) {
      if (n.sampler !== void 0) {
        const i = this._samplerCache.get(n.samplerKey);
        i.usedTimes--, i.usedTimes === 0 && this._samplerCache.delete(n.samplerKey);
      }
      n.samplerKey = s, n.sampler = r.sampler, r.usedTimes++;
    }
    return s;
  }
  /**
   * Creates a default texture for the given texture that can be used
   * as a placeholder until the actual texture is ready for usage.
   *
   * @param {Texture} texture - The texture to create a default texture for.
   */
  createDefaultTexture(e) {
    let t;
    const s = sa(e, this.backend.device);
    e.isCubeTexture ? t = this._getDefaultCubeTextureGPU(s) : t = this._getDefaultTextureGPU(s), this.backend.get(e).texture = t;
  }
  /**
   * Defines a texture on the GPU for the given texture object.
   *
   * @param {Texture} texture - The texture.
   * @param {Object} [options={}] - Optional configuration parameter.
   */
  createTexture(e, t = {}) {
    const s = this.backend, r = s.get(e);
    if (r.initialized)
      throw new Error("WebGPUTextureUtils: Texture already initialized.");
    if (e.isExternalTexture) {
      r.texture = e.sourceTexture, r.initialized = !0;
      return;
    }
    t.needsMipmaps === void 0 && (t.needsMipmaps = !1), t.levels === void 0 && (t.levels = 1), t.depth === void 0 && (t.depth = 1);
    const { width: n, height: i, depth: o, levels: u } = t;
    e.isFramebufferTexture && (t.renderTarget ? t.format = this.backend.utils.getCurrentColorFormat(t.renderTarget) : t.format = this.backend.utils.getPreferredCanvasFormat());
    const l = this._getDimension(e), c = e.internalFormat || t.format || sa(e, s.device);
    r.format = c;
    const { samples: d, primarySamples: h, isMSAA: f } = s.utils.getTextureSampleData(e);
    let p = GPUTextureUsage.TEXTURE_BINDING | GPUTextureUsage.COPY_DST | GPUTextureUsage.COPY_SRC;
    e.isStorageTexture === !0 && (p |= GPUTextureUsage.STORAGE_BINDING), e.isCompressedTexture !== !0 && e.isCompressedArrayTexture !== !0 && c !== x.RGB9E5UFloat && (p |= GPUTextureUsage.RENDER_ATTACHMENT);
    const g = {
      label: e.name,
      size: {
        width: n,
        height: i,
        depthOrArrayLayers: o
      },
      mipLevelCount: u,
      sampleCount: h,
      dimension: l,
      format: c,
      usage: p
    };
    if (c === void 0) {
      L("WebGPURenderer: Texture format not supported."), this.createDefaultTexture(e);
      return;
    }
    e.isCubeTexture && (g.textureBindingViewDimension = Fe.Cube);
    try {
      r.texture = s.device.createTexture(g);
    } catch {
      L("WebGPURenderer: Failed to create texture with descriptor:", g), this.createDefaultTexture(e);
      return;
    }
    if (f) {
      const m = Object.assign({}, g);
      m.label = m.label + "-msaa", m.sampleCount = d, m.mipLevelCount = 1, r.msaaTexture = s.device.createTexture(m);
    }
    r.initialized = !0, r.textureDescriptorGPU = g;
  }
  /**
   * Destroys the GPU data for the given texture object.
   *
   * @param {Texture} texture - The texture.
   * @param {boolean} [isDefaultTexture=false] - Whether the texture uses a default GPU texture or not.
   */
  destroyTexture(e, t = !1) {
    const s = this.backend, r = s.get(e);
    r.texture !== void 0 && t === !1 && r.texture.destroy(), r.msaaTexture !== void 0 && r.msaaTexture.destroy(), s.delete(e);
  }
  /**
   * Generates mipmaps for the given texture.
   *
   * @param {Texture} texture - The texture.
   * @param {?GPUCommandEncoder} [encoder=null] - An optional command encoder used to generate mipmaps.
   */
  generateMipmaps(e, t = null) {
    const s = this.backend.get(e);
    this._generateMipmaps(s.texture, t);
  }
  /**
   * Returns the color buffer representing the color
   * attachment of the default framebuffer.
   *
   * @return {GPUTexture} The color buffer.
   */
  getColorBuffer() {
    const e = this.backend, t = e.renderer.getCanvasTarget(), { width: s, height: r } = e.getDrawingBufferSize(), n = e.renderer.currentSamples, i = t.colorTexture, o = e.get(i);
    if (i.width === s && i.height === r && i.samples === n)
      return o.texture;
    let u = o.texture;
    return u && u.destroy(), u = e.device.createTexture({
      label: "colorBuffer",
      size: {
        width: s,
        height: r,
        depthOrArrayLayers: 1
      },
      sampleCount: e.utils.getSampleCount(e.renderer.currentSamples),
      format: e.utils.getPreferredCanvasFormat(),
      usage: GPUTextureUsage.RENDER_ATTACHMENT | GPUTextureUsage.COPY_SRC
    }), i.source.width = s, i.source.height = r, i.samples = n, o.texture = u, u;
  }
  /**
   * Returns the depth buffer representing the depth
   * attachment of the default framebuffer.
   *
   * @param {boolean} [depth=true] - Whether depth is enabled or not.
   * @param {boolean} [stencil=false] -  Whether stencil is enabled or not.
   * @return {GPUTexture} The depth buffer.
   */
  getDepthBuffer(e = !0, t = !1) {
    const s = this.backend, r = s.renderer.getCanvasTarget(), { width: n, height: i } = s.getDrawingBufferSize(), o = s.renderer.currentSamples, u = r.depthTexture;
    if (u.width === n && u.height === i && u.samples === o && u.depth === e && u.stencil === t)
      return s.get(u).texture;
    const l = s.get(u).texture;
    let c, d;
    if (t ? (c = kt, d = s.renderer.reversedDepthBuffer === !0 ? De : Vt) : e && (c = Gt, d = s.renderer.reversedDepthBuffer === !0 ? De : _e), l !== void 0) {
      if (u.image.width === n && u.image.height === i && u.format === c && u.type === d && u.samples === o)
        return l;
      this.destroyTexture(u);
    }
    return u.name = "depthBuffer", u.format = c, u.type = d, u.image.width = n, u.image.height = i, u.samples = o, this.createTexture(u, { width: n, height: i }), s.get(u).texture;
  }
  /**
   * Uploads the updated texture data to the GPU.
   *
   * @param {Texture} texture - The texture.
   * @param {Object} [options={}] - Optional configuration parameter.
   */
  updateTexture(e, t) {
    const s = this.backend.get(e), r = e.mipmaps, { textureDescriptorGPU: n } = s;
    if (!(e.isRenderTargetTexture || n === void 0)) {
      if (e.isDataTexture)
        if (r.length > 0)
          for (let i = 0, o = r.length; i < o; i++) {
            const u = r[i];
            this._copyBufferToTexture(u, s.texture, n, 0, e.flipY, 0, i);
          }
        else
          this._copyBufferToTexture(t.image, s.texture, n, 0, e.flipY);
      else if (e.isArrayTexture || e.isDataArrayTexture || e.isData3DTexture)
        if (e.layerUpdates && e.layerUpdates.size > 0) {
          for (const i of e.layerUpdates)
            this._copyBufferToTexture(t.image, s.texture, n, i, e.flipY, i);
          e.clearLayerUpdates();
        } else
          for (let i = 0; i < t.image.depth; i++)
            this._copyBufferToTexture(t.image, s.texture, n, i, e.flipY, i);
      else if (e.isCompressedTexture || e.isCompressedArrayTexture)
        this._copyCompressedBufferToTexture(e.mipmaps, s.texture, n);
      else if (e.isCubeTexture)
        this._copyCubeMapToTexture(e, s.texture, n);
      else if (e.isHTMLTexture) {
        const i = this.backend.device, o = this.backend.renderer.domElement, u = e.image;
        if (typeof i.queue.copyElementImageToTexture != "function") return;
        if (!s.hasPaintCallback) {
          s.hasPaintCallback = !0, o.requestPaint();
          return;
        }
        const l = n.size.width, c = n.size.height;
        i.queue.copyElementImageToTexture(
          u,
          l,
          c,
          { texture: s.texture }
        ), e.flipY && this._flipY(s.texture, n);
      } else if (r.length > 0)
        for (let i = 0, o = r.length; i < o; i++) {
          const u = r[i];
          this._copyImageToTexture(u, s.texture, n, 0, e.flipY, e.premultiplyAlpha, i);
        }
      else
        this._copyImageToTexture(t.image, s.texture, n, 0, e.flipY, e.premultiplyAlpha);
      s.version = e.version;
    }
  }
  /**
   * Returns texture data as a typed array.
   *
   * @async
   * @param {Texture} texture - The texture to copy.
   * @param {number} x - The x coordinate of the copy origin.
   * @param {number} y - The y coordinate of the copy origin.
   * @param {number} width - The width of the copy.
   * @param {number} height - The height of the copy.
   * @param {number} faceIndex - The face index.
   * @return {Promise<TypedArray>} A Promise that resolves with a typed array when the copy operation has finished.
   */
  async copyTextureToBuffer(e, t, s, r, n, i) {
    const o = this.backend.device, u = this.backend.get(e), l = u.texture, c = u.textureDescriptorGPU.format, d = this._getBytesPerTexel(c);
    let h = r * d;
    h = Math.ceil(h / 256) * 256;
    const f = o.createBuffer(
      {
        size: (n - 1) * h + r * d,
        // see https://github.com/mrdoob/three.js/issues/31658#issuecomment-3229442010
        usage: GPUBufferUsage.COPY_DST | GPUBufferUsage.MAP_READ
      }
    ), p = o.createCommandEncoder();
    p.copyTextureToBuffer(
      {
        texture: l,
        origin: { x: t, y: s, z: i }
      },
      {
        buffer: f,
        bytesPerRow: h
      },
      {
        width: r,
        height: n
      }
    );
    const g = this._getTypedArrayType(c);
    o.queue.submit([p.finish()]), await f.mapAsync(GPUMapMode.READ);
    const m = f.getMappedRange().slice();
    return f.destroy(), new g(m);
  }
  /**
   * Frees all internal resources.
   */
  dispose() {
    this._samplerCache.clear();
  }
  /**
   * Returns the default GPU texture for the given format.
   *
   * @private
   * @param {string} format - The GPU format.
   * @return {GPUTexture} The GPU texture.
   */
  _getDefaultTextureGPU(e) {
    let t = this.defaultTexture[e];
    if (t === void 0) {
      const s = new da();
      s.minFilter = Qe, s.magFilter = Qe, this.createTexture(s, { width: 1, height: 1, format: e }), this.defaultTexture[e] = t = s;
    }
    return this.backend.get(t).texture;
  }
  /**
   * Returns the default GPU cube texture for the given format.
   *
   * @private
   * @param {string} format - The GPU format.
   * @return {GPUTexture} The GPU texture.
   */
  _getDefaultCubeTextureGPU(e) {
    let t = this.defaultCubeTexture[e];
    if (t === void 0) {
      const s = new ai();
      s.minFilter = Qe, s.magFilter = Qe, this.createTexture(s, { width: 1, height: 1, depth: 6 }), this.defaultCubeTexture[e] = t = s;
    }
    return this.backend.get(t).texture;
  }
  /**
   * Uploads cube texture image data to the GPU memory.
   *
   * @private
   * @param {CubeTexture} texture - The cube texture.
   * @param {GPUTexture} textureGPU - The GPU texture.
   * @param {Object} textureDescriptorGPU - The GPU texture descriptor.
   */
  _copyCubeMapToTexture(e, t, s) {
    const r = e.images, n = e.mipmaps;
    for (let i = 0; i < 6; i++) {
      const o = r[i], u = e.flipY === !0 ? sN[i] : i;
      o.isDataTexture ? this._copyBufferToTexture(o.image, t, s, u, e.flipY) : this._copyImageToTexture(o, t, s, u, e.flipY, e.premultiplyAlpha);
      for (let l = 0; l < n.length; l++) {
        const d = n[l].images[i];
        d.isDataTexture ? this._copyBufferToTexture(d.image, t, s, u, e.flipY, 0, l + 1) : this._copyImageToTexture(d, t, s, u, e.flipY, e.premultiplyAlpha, l + 1);
      }
    }
  }
  /**
   * Uploads texture image data to the GPU memory.
   *
   * @private
   * @param {HTMLImageElement|ImageBitmap|HTMLCanvasElement} image - The image data.
   * @param {GPUTexture} textureGPU - The GPU texture.
   * @param {Object} textureDescriptorGPU - The GPU texture descriptor.
   * @param {number} originDepth - The origin depth.
   * @param {boolean} flipY - Whether to flip texture data along their vertical axis or not.
   * @param {boolean} premultiplyAlpha - Whether the texture should have its RGB channels premultiplied by the alpha channel or not.
   * @param {number} [mipLevel=0] - The mip level where the data should be copied to.
   */
  _copyImageToTexture(e, t, s, r, n, i, o = 0) {
    const u = this.backend.device, l = o > 0 ? e.width : s.size.width, c = o > 0 ? e.height : s.size.height;
    try {
      u.queue.copyExternalImageToTexture(
        {
          source: e,
          flipY: n
        },
        {
          texture: t,
          mipLevel: o,
          origin: { x: 0, y: 0, z: r },
          premultipliedAlpha: i
        },
        {
          width: l,
          height: c,
          depthOrArrayLayers: 1
        }
      );
    } catch {
    }
  }
  /**
   * Returns the pass utils singleton.
   *
   * @private
   * @return {WebGPUTexturePassUtils} The utils instance.
   */
  _getPassUtils() {
    let e = this._passUtils;
    return e === null && (this._passUtils = e = new eN(this.backend.device)), e;
  }
  /**
   * Generates mipmaps for the given GPU texture.
   *
   * @private
   * @param {GPUTexture} textureGPU - The GPU texture object.
   * @param {?GPUCommandEncoder} [encoder=null] - An optional command encoder used to generate mipmaps.
   */
  _generateMipmaps(e, t = null) {
    this._getPassUtils().generateMipmaps(e, t);
  }
  /**
   * Flip the contents of the given GPU texture along its vertical axis.
   *
   * @private
   * @param {GPUTexture} textureGPU - The GPU texture object.
   * @param {Object} textureDescriptorGPU - The texture descriptor.
   * @param {number} [originDepth=0] - The origin depth.
   */
  _flipY(e, t, s = 0) {
    this._getPassUtils().flipY(e, t, s);
  }
  /**
   * Uploads texture buffer data to the GPU memory.
   *
   * @private
   * @param {Object} image - An object defining the image buffer data.
   * @param {GPUTexture} textureGPU - The GPU texture.
   * @param {Object} textureDescriptorGPU - The GPU texture descriptor.
   * @param {number} originDepth - The origin depth.
   * @param {boolean} flipY - Whether to flip texture data along their vertical axis or not.
   * @param {number} [depth=0] - The depth offset when copying array or 3D texture data.
   * @param {number} [mipLevel=0] - The mip level where the data should be copied to.
   */
  _copyBufferToTexture(e, t, s, r, n, i = 0, o = 0) {
    const u = this.backend.device, l = e.data, c = this._getBytesPerTexel(s.format), d = e.width * c;
    u.queue.writeTexture(
      {
        texture: t,
        mipLevel: o,
        origin: { x: 0, y: 0, z: r }
      },
      l,
      {
        offset: e.width * e.height * c * i,
        bytesPerRow: d
      },
      {
        width: e.width,
        height: e.height,
        depthOrArrayLayers: 1
      }
    ), n === !0 && this._flipY(t, s, r);
  }
  /**
   * Uploads compressed texture data to the GPU memory.
   *
   * @private
   * @param {Array<Object>} mipmaps - An array with mipmap data.
   * @param {GPUTexture} textureGPU - The GPU texture.
   * @param {Object} textureDescriptorGPU - The GPU texture descriptor.
   */
  _copyCompressedBufferToTexture(e, t, s) {
    const r = this.backend.device, n = this._getBlockData(s.format), i = s.size.depthOrArrayLayers > 1;
    for (let o = 0; o < e.length; o++) {
      const u = e[o], l = u.width, c = u.height, d = i ? s.size.depthOrArrayLayers : 1, h = Math.ceil(l / n.width) * n.byteLength, f = h * Math.ceil(c / n.height);
      for (let p = 0; p < d; p++)
        r.queue.writeTexture(
          {
            texture: t,
            mipLevel: o,
            origin: { x: 0, y: 0, z: p }
          },
          u.data,
          {
            offset: p * f,
            bytesPerRow: h,
            rowsPerImage: Math.ceil(c / n.height)
          },
          {
            width: Math.ceil(l / n.width) * n.width,
            height: Math.ceil(c / n.height) * n.height,
            depthOrArrayLayers: 1
          }
        );
    }
  }
  /**
   * This method is only relevant for compressed texture formats. It returns a block
   * data descriptor for the given GPU compressed texture format.
   *
   * @private
   * @param {string} format - The GPU compressed texture format.
   * @return {Object} The block data descriptor.
   */
  _getBlockData(e) {
    if (e === x.BC1RGBAUnorm || e === x.BC1RGBAUnormSRGB) return { byteLength: 8, width: 4, height: 4 };
    if (e === x.BC2RGBAUnorm || e === x.BC2RGBAUnormSRGB) return { byteLength: 16, width: 4, height: 4 };
    if (e === x.BC3RGBAUnorm || e === x.BC3RGBAUnormSRGB) return { byteLength: 16, width: 4, height: 4 };
    if (e === x.BC4RUnorm || e === x.BC4RSnorm) return { byteLength: 8, width: 4, height: 4 };
    if (e === x.BC5RGUnorm || e === x.BC5RGSnorm) return { byteLength: 16, width: 4, height: 4 };
    if (e === x.BC6HRGBUFloat || e === x.BC6HRGBFloat) return { byteLength: 16, width: 4, height: 4 };
    if (e === x.BC7RGBAUnorm || e === x.BC7RGBAUnormSRGB) return { byteLength: 16, width: 4, height: 4 };
    if (e === x.ETC2RGB8Unorm || e === x.ETC2RGB8UnormSRGB) return { byteLength: 8, width: 4, height: 4 };
    if (e === x.ETC2RGB8A1Unorm || e === x.ETC2RGB8A1UnormSRGB) return { byteLength: 8, width: 4, height: 4 };
    if (e === x.ETC2RGBA8Unorm || e === x.ETC2RGBA8UnormSRGB) return { byteLength: 16, width: 4, height: 4 };
    if (e === x.EACR11Unorm) return { byteLength: 8, width: 4, height: 4 };
    if (e === x.EACR11Snorm) return { byteLength: 8, width: 4, height: 4 };
    if (e === x.EACRG11Unorm) return { byteLength: 16, width: 4, height: 4 };
    if (e === x.EACRG11Snorm) return { byteLength: 16, width: 4, height: 4 };
    if (e === x.ASTC4x4Unorm || e === x.ASTC4x4UnormSRGB) return { byteLength: 16, width: 4, height: 4 };
    if (e === x.ASTC5x4Unorm || e === x.ASTC5x4UnormSRGB) return { byteLength: 16, width: 5, height: 4 };
    if (e === x.ASTC5x5Unorm || e === x.ASTC5x5UnormSRGB) return { byteLength: 16, width: 5, height: 5 };
    if (e === x.ASTC6x5Unorm || e === x.ASTC6x5UnormSRGB) return { byteLength: 16, width: 6, height: 5 };
    if (e === x.ASTC6x6Unorm || e === x.ASTC6x6UnormSRGB) return { byteLength: 16, width: 6, height: 6 };
    if (e === x.ASTC8x5Unorm || e === x.ASTC8x5UnormSRGB) return { byteLength: 16, width: 8, height: 5 };
    if (e === x.ASTC8x6Unorm || e === x.ASTC8x6UnormSRGB) return { byteLength: 16, width: 8, height: 6 };
    if (e === x.ASTC8x8Unorm || e === x.ASTC8x8UnormSRGB) return { byteLength: 16, width: 8, height: 8 };
    if (e === x.ASTC10x5Unorm || e === x.ASTC10x5UnormSRGB) return { byteLength: 16, width: 10, height: 5 };
    if (e === x.ASTC10x6Unorm || e === x.ASTC10x6UnormSRGB) return { byteLength: 16, width: 10, height: 6 };
    if (e === x.ASTC10x8Unorm || e === x.ASTC10x8UnormSRGB) return { byteLength: 16, width: 10, height: 8 };
    if (e === x.ASTC10x10Unorm || e === x.ASTC10x10UnormSRGB) return { byteLength: 16, width: 10, height: 10 };
    if (e === x.ASTC12x10Unorm || e === x.ASTC12x10UnormSRGB) return { byteLength: 16, width: 12, height: 10 };
    if (e === x.ASTC12x12Unorm || e === x.ASTC12x12UnormSRGB) return { byteLength: 16, width: 12, height: 12 };
  }
  /**
   * Converts the three.js uv wrapping constants to GPU address mode constants.
   *
   * @private
   * @param {number} value - The three.js constant defining a uv wrapping mode.
   * @return {string} The GPU address mode.
   */
  _convertAddressMode(e) {
    let t = uo.ClampToEdge;
    return e === oi ? t = uo.Repeat : e === ii && (t = uo.MirrorRepeat), t;
  }
  /**
   * Converts the three.js filter constants to GPU filter constants.
   *
   * @private
   * @param {number} value - The three.js constant defining a filter mode.
   * @return {string} The GPU filter mode.
   */
  _convertFilterMode(e) {
    let t = Tt.Linear;
    return (e === Qe || e === Vl || e === $r) && (t = Tt.Nearest), t;
  }
  /**
   * Returns the bytes-per-texel value for the given GPU texture format.
   *
   * @private
   * @param {string} format - The GPU texture format.
   * @return {number} The bytes-per-texel.
   */
  _getBytesPerTexel(e) {
    if (e === x.R8Unorm || e === x.R8Snorm || e === x.R8Uint || e === x.R8Sint) return 1;
    if (e === x.R16Uint || e === x.R16Sint || e === x.R16Float || e === x.RG8Unorm || e === x.RG8Snorm || e === x.RG8Uint || e === x.RG8Sint || e === x.R16Unorm || e === x.R16Snorm) return 2;
    if (e === x.R32Uint || e === x.R32Sint || e === x.R32Float || e === x.RG16Uint || e === x.RG16Sint || e === x.RG16Float || e === x.RGBA8Unorm || e === x.RGBA8UnormSRGB || e === x.RGBA8Snorm || e === x.RGBA8Uint || e === x.RGBA8Sint || e === x.BGRA8Unorm || e === x.BGRA8UnormSRGB || e === x.RG16Unorm || e === x.RG16Snorm || // Packed 32-bit formats
    e === x.RGB9E5UFloat || e === x.RGB10A2Unorm || e === x.RG11B10UFloat || e === x.Depth32Float || e === x.Depth24Plus || e === x.Depth24PlusStencil8 || e === x.Depth32FloatStencil8) return 4;
    if (e === x.RG32Uint || e === x.RG32Sint || e === x.RG32Float || e === x.RGBA16Uint || e === x.RGBA16Sint || e === x.RGBA16Float || e === x.RGBA16Unorm || e === x.RGBA16Snorm) return 8;
    if (e === x.RGBA32Uint || e === x.RGBA32Sint || e === x.RGBA32Float) return 16;
  }
  /**
   * Returns the corresponding typed array type for the given GPU texture format.
   *
   * @private
   * @param {string} format - The GPU texture format.
   * @return {TypedArray.constructor} The typed array type.
   */
  _getTypedArrayType(e) {
    if (e === x.R8Uint) return Uint8Array;
    if (e === x.R8Sint) return Int8Array;
    if (e === x.R8Unorm) return Uint8Array;
    if (e === x.R8Snorm) return Int8Array;
    if (e === x.RG8Uint) return Uint8Array;
    if (e === x.RG8Sint) return Int8Array;
    if (e === x.RG8Unorm) return Uint8Array;
    if (e === x.RG8Snorm) return Int8Array;
    if (e === x.RGBA8Uint) return Uint8Array;
    if (e === x.RGBA8Sint) return Int8Array;
    if (e === x.RGBA8Unorm || e === x.RGBA8UnormSRGB) return Uint8Array;
    if (e === x.RGBA8Snorm) return Int8Array;
    if (e === x.R16Uint) return Uint16Array;
    if (e === x.R16Sint) return Int16Array;
    if (e === x.RG16Uint) return Uint16Array;
    if (e === x.RG16Sint) return Int16Array;
    if (e === x.RGBA16Uint) return Uint16Array;
    if (e === x.RGBA16Sint) return Int16Array;
    if (e === x.R16Float || e === x.RG16Float || e === x.RGBA16Float || e === x.R16Unorm) return Uint16Array;
    if (e === x.R16Snorm) return Int16Array;
    if (e === x.RG16Unorm) return Uint16Array;
    if (e === x.RG16Snorm) return Int16Array;
    if (e === x.RGBA16Unorm) return Uint16Array;
    if (e === x.RGBA16Snorm) return Int16Array;
    if (e === x.R32Uint) return Uint32Array;
    if (e === x.R32Sint) return Int32Array;
    if (e === x.R32Float) return Float32Array;
    if (e === x.RG32Uint) return Uint32Array;
    if (e === x.RG32Sint) return Int32Array;
    if (e === x.RG32Float) return Float32Array;
    if (e === x.RGBA32Uint) return Uint32Array;
    if (e === x.RGBA32Sint) return Int32Array;
    if (e === x.RGBA32Float) return Float32Array;
    if (e === x.BGRA8Unorm || e === x.BGRA8UnormSRGB) return Uint8Array;
    if (e === x.RGB10A2Unorm || e === x.RGB9E5UFloat || e === x.RG11B10UFloat) return Uint32Array;
    if (e === x.Depth32Float) return Float32Array;
    if (e === x.Depth24Plus || e === x.Depth24PlusStencil8) return Uint32Array;
    if (e === x.Depth32FloatStencil8) return Float32Array;
  }
  /**
   * Returns the GPU dimensions for the given texture.
   *
   * @private
   * @param {Texture} texture - The texture.
   * @return {string} The GPU dimension.
   */
  _getDimension(e) {
    let t;
    return e.is3DTexture || e.isData3DTexture ? t = Al.ThreeD : t = Al.TwoD, t;
  }
}
function sa(a, e) {
  const t = a.format, s = a.type, r = a.normalized, n = a.colorSpace, i = ye.getTransfer(n);
  let o, u = !1;
  if (r && (u = e.features.has(Jn.TextureFormatsTier1), u === !1 && L("WebGPURenderer: Unable to use normalized textures without texture-formats-tier1 feature.")), a.isCompressedTexture === !0 || a.isCompressedArrayTexture === !0)
    switch (t) {
      case An:
      case vn:
        o = i === q ? x.BC1RGBAUnormSRGB : x.BC1RGBAUnorm;
        break;
      case En:
        o = i === q ? x.BC2RGBAUnormSRGB : x.BC2RGBAUnorm;
        break;
      case Cn:
        o = i === q ? x.BC3RGBAUnormSRGB : x.BC3RGBAUnorm;
        break;
      case Lo:
        o = x.BC4RUnorm;
        break;
      case Io:
        o = x.BC4RSnorm;
        break;
      case kn:
        o = x.BC5RGUnorm;
        break;
      case Oo:
        o = x.BC5RGSnorm;
        break;
      case Po:
        o = i === q ? x.BC7RGBAUnormSRGB : x.BC7RGBAUnorm;
        break;
      case mo:
      case go:
        o = i === q ? x.ETC2RGB8UnormSRGB : x.ETC2RGB8Unorm;
        break;
      case xo:
        o = i === q ? x.ETC2RGBA8UnormSRGB : x.ETC2RGBA8Unorm;
        break;
      case To:
        o = x.EACR11Unorm;
        break;
      case yo:
        o = x.EACR11Snorm;
        break;
      case Gn:
        o = x.EACRG11Unorm;
        break;
      case _o:
        o = x.EACRG11Snorm;
        break;
      case bo:
        o = i === q ? x.ASTC4x4UnormSRGB : x.ASTC4x4Unorm;
        break;
      case So:
        o = i === q ? x.ASTC5x4UnormSRGB : x.ASTC5x4Unorm;
        break;
      case No:
        o = i === q ? x.ASTC5x5UnormSRGB : x.ASTC5x5Unorm;
        break;
      case Ro:
        o = i === q ? x.ASTC6x5UnormSRGB : x.ASTC6x5Unorm;
        break;
      case Ao:
        o = i === q ? x.ASTC6x6UnormSRGB : x.ASTC6x6Unorm;
        break;
      case vo:
        o = i === q ? x.ASTC8x5UnormSRGB : x.ASTC8x5Unorm;
        break;
      case Eo:
        o = i === q ? x.ASTC8x6UnormSRGB : x.ASTC8x6Unorm;
        break;
      case Co:
        o = i === q ? x.ASTC8x8UnormSRGB : x.ASTC8x8Unorm;
        break;
      case wo:
        o = i === q ? x.ASTC10x5UnormSRGB : x.ASTC10x5Unorm;
        break;
      case Mo:
        o = i === q ? x.ASTC10x6UnormSRGB : x.ASTC10x6Unorm;
        break;
      case Bo:
        o = i === q ? x.ASTC10x8UnormSRGB : x.ASTC10x8Unorm;
        break;
      case Fo:
        o = i === q ? x.ASTC10x10UnormSRGB : x.ASTC10x10Unorm;
        break;
      case Uo:
        o = i === q ? x.ASTC12x10UnormSRGB : x.ASTC12x10Unorm;
        break;
      case Do:
        o = i === q ? x.ASTC12x12UnormSRGB : x.ASTC12x12Unorm;
        break;
      case ct:
        o = i === q ? x.RGBA8UnormSRGB : x.RGBA8Unorm;
        break;
      default:
        F("WebGPURenderer: Unsupported texture format.", t);
    }
  else
    switch (t) {
      case ct:
        switch (s) {
          case Ys:
            o = x.RGBA8Snorm;
            break;
          case js:
            o = u ? x.RGBA16Snorm : x.RGBA16Sint;
            break;
          case Ss:
            o = u ? x.RGBA16Unorm : x.RGBA16Uint;
            break;
          case _e:
            o = x.RGBA32Uint;
            break;
          case Ue:
            o = x.RGBA32Sint;
            break;
          case $e:
            o = i === q ? x.RGBA8UnormSRGB : x.RGBA8Unorm;
            break;
          case Oe:
            o = x.RGBA16Float;
            break;
          case De:
            o = x.RGBA32Float;
            break;
          default:
            F("WebGPURenderer: Unsupported texture type with RGBAFormat.", s);
        }
        break;
      case ri:
        switch (s) {
          case na:
            o = x.RGB9E5UFloat;
            break;
          case ia:
            o = x.RG11B10UFloat;
            break;
          default:
            F("WebGPURenderer: Unsupported texture type with RGBFormat.", s);
        }
        break;
      case ei:
        switch (s) {
          case Ys:
            o = x.R8Snorm;
            break;
          case js:
            o = u ? x.R16Snorm : x.R16Sint;
            break;
          case Ss:
            o = u ? x.R16Unorm : x.R16Uint;
            break;
          case _e:
            o = x.R32Uint;
            break;
          case Ue:
            o = x.R32Sint;
            break;
          case $e:
            o = x.R8Unorm;
            break;
          case Oe:
            o = x.R16Float;
            break;
          case De:
            o = x.R32Float;
            break;
          default:
            F("WebGPURenderer: Unsupported texture type with RedFormat.", s);
        }
        break;
      case Rt:
        switch (s) {
          case Ys:
            o = x.RG8Snorm;
            break;
          case js:
            o = u ? x.RG16Snorm : x.RG16Sint;
            break;
          case Ss:
            o = u ? x.RG16Unorm : x.RG16Uint;
            break;
          case _e:
            o = x.RG32Uint;
            break;
          case Ue:
            o = x.RG32Sint;
            break;
          case $e:
            o = x.RG8Unorm;
            break;
          case Oe:
            o = x.RG16Float;
            break;
          case De:
            o = x.RG32Float;
            break;
          default:
            F("WebGPURenderer: Unsupported texture type with RGFormat.", s);
        }
        break;
      case Gt:
        switch (s) {
          case Ss:
            o = x.Depth16Unorm;
            break;
          case _e:
            o = x.Depth24Plus;
            break;
          case De:
            o = x.Depth32Float;
            break;
          default:
            F("WebGPURenderer: Unsupported texture type with DepthFormat.", s);
        }
        break;
      case kt:
        switch (s) {
          case Vt:
            o = x.Depth24PlusStencil8;
            break;
          case De:
            e && e.features.has(Jn.Depth32FloatStencil8) === !1 && F('WebGPURenderer: Depth textures with DepthStencilFormat + FloatType can only be used with the "depth32float-stencil8" GPU feature.'), o = x.Depth32FloatStencil8;
            break;
          default:
            F("WebGPURenderer: Unsupported texture type with DepthStencilFormat.", s);
        }
        break;
      case ti:
        switch (s) {
          case Ue:
            o = x.R32Sint;
            break;
          case _e:
            o = x.R32Uint;
            break;
          default:
            F("WebGPURenderer: Unsupported texture type with RedIntegerFormat.", s);
        }
        break;
      case si:
        switch (s) {
          case Ue:
            o = x.RG32Sint;
            break;
          case _e:
            o = x.RG32Uint;
            break;
          default:
            F("WebGPURenderer: Unsupported texture type with RGIntegerFormat.", s);
        }
        break;
      case ca:
        switch (s) {
          case Ue:
            o = x.RGBA32Sint;
            break;
          case _e:
            o = x.RGBA32Uint;
            break;
          default:
            F("WebGPURenderer: Unsupported texture type with RGBAIntegerFormat.", s);
        }
        break;
      default:
        F("WebGPURenderer: Unsupported texture format.", t);
    }
  return o;
}
const nN = /^[fn]*\s*([a-z_0-9]+)?\s*\(([\s\S]*?)\)\s*[\-\>]*\s*([a-z_0-9]+(?:<[\s\S]+?>)?)/i, iN = /([a-z_0-9]+)\s*:\s*([a-z_0-9]+(?:<[\s\S]+?>)?)/ig, El = {
  f32: "float",
  i32: "int",
  u32: "uint",
  bool: "bool",
  "vec2<f32>": "vec2",
  "vec2<i32>": "ivec2",
  "vec2<u32>": "uvec2",
  "vec2<bool>": "bvec2",
  vec2f: "vec2",
  vec2i: "ivec2",
  vec2u: "uvec2",
  vec2b: "bvec2",
  "vec3<f32>": "vec3",
  "vec3<i32>": "ivec3",
  "vec3<u32>": "uvec3",
  "vec3<bool>": "bvec3",
  vec3f: "vec3",
  vec3i: "ivec3",
  vec3u: "uvec3",
  vec3b: "bvec3",
  "vec4<f32>": "vec4",
  "vec4<i32>": "ivec4",
  "vec4<u32>": "uvec4",
  "vec4<bool>": "bvec4",
  vec4f: "vec4",
  vec4i: "ivec4",
  vec4u: "uvec4",
  vec4b: "bvec4",
  "mat2x2<f32>": "mat2",
  mat2x2f: "mat2",
  "mat3x3<f32>": "mat3",
  mat3x3f: "mat3",
  "mat4x4<f32>": "mat4",
  mat4x4f: "mat4",
  sampler: "sampler",
  texture_1d: "texture",
  texture_2d: "texture",
  texture_2d_array: "texture",
  texture_multisampled_2d: "cubeTexture",
  texture_depth_2d: "depthTexture",
  texture_depth_2d_array: "depthTexture",
  texture_depth_multisampled_2d: "depthTexture",
  texture_depth_cube: "depthTexture",
  texture_depth_cube_array: "depthTexture",
  texture_3d: "texture3D",
  texture_cube: "cubeTexture",
  texture_cube_array: "cubeTexture",
  texture_storage_1d: "storageTexture",
  texture_storage_2d: "storageTexture",
  texture_storage_2d_array: "storageTexture",
  texture_storage_3d: "storageTexture"
}, oN = (a) => {
  a = a.trim();
  const e = a.match(nN);
  if (e !== null && e.length === 4) {
    const t = e[2], s = [];
    let r = null;
    for (; (r = iN.exec(t)) !== null; )
      s.push({ name: r[1], type: r[2] });
    const n = [];
    for (let c = 0; c < s.length; c++) {
      const { name: d, type: h } = s[c];
      let f = h;
      f.startsWith("ptr") ? f = "pointer" : (f.startsWith("texture") && (f = h.split("<")[0]), f = El[f]), n.push(new Oa(f, d));
    }
    const i = a.substring(e[0].length), o = e[3] || "void", u = e[1] !== void 0 ? e[1] : "";
    return {
      type: El[o] || o,
      inputs: n,
      name: u,
      inputsCode: t,
      blockCode: i,
      outputType: o
    };
  } else
    throw new Error("FunctionNode: Function is not a WGSL code.");
};
class aN extends ka {
  /**
   * Constructs a new WGSL node function.
   *
   * @param {string} source - The WGSL source.
   */
  constructor(e) {
    const { type: t, inputs: s, name: r, inputsCode: n, blockCode: i, outputType: o } = oN(e);
    super(t, s, r), this.inputsCode = n, this.blockCode = i, this.outputType = o;
  }
  /**
   * This method returns the WGSL code of the node function.
   *
   * @param {string} [name=this.name] - The function's name.
   * @return {string} The shader code.
   */
  getCode(e = this.name) {
    const t = this.outputType !== "void" ? "-> " + this.outputType : "";
    return `fn ${e} ( ${this.inputsCode.trim()} ) ${t}` + this.blockCode;
  }
}
class uN extends uh {
  /**
   * The method parses the given WGSL code an returns a node function.
   *
   * @param {string} source - The WGSL code.
   * @return {WGSLNodeFunction} A node function.
   */
  parseFunction(e) {
    return new aN(e);
  }
}
const lN = {
  [nt.READ_ONLY]: "read",
  [nt.WRITE_ONLY]: "write",
  [nt.READ_WRITE]: "read_write"
}, Cl = {
  [oi]: "repeat",
  [Vr]: "clamp",
  [ii]: "mirror"
}, ho = {
  vertex: At.VERTEX,
  fragment: At.FRAGMENT,
  compute: At.COMPUTE
}, wl = {
  instance: !0,
  swizzleAssign: !1,
  storageBuffer: !0
}, cN = {
  "^^": "tsl_xor"
}, dN = {
  float: "f32",
  int: "i32",
  uint: "u32",
  bool: "bool",
  color: "vec3<f32>",
  vec2: "vec2<f32>",
  ivec2: "vec2<i32>",
  uvec2: "vec2<u32>",
  bvec2: "vec2<bool>",
  vec3: "vec3<f32>",
  ivec3: "vec3<i32>",
  uvec3: "vec3<u32>",
  bvec3: "vec3<bool>",
  vec4: "vec4<f32>",
  ivec4: "vec4<i32>",
  uvec4: "vec4<u32>",
  bvec4: "vec4<bool>",
  mat2: "mat2x2<f32>",
  mat3: "mat3x3<f32>",
  mat4: "mat4x4<f32>"
}, Ml = {}, Er = {
  tsl_xor: new Ee("fn tsl_xor( a : bool, b : bool ) -> bool { return ( a || b ) && !( a && b ); }"),
  mod_float: new Ee("fn tsl_mod_float( x : f32, y : f32 ) -> f32 { return x - y * floor( x / y ); }"),
  mod_vec2: new Ee("fn tsl_mod_vec2( x : vec2f, y : vec2f ) -> vec2f { return x - y * floor( x / y ); }"),
  mod_vec3: new Ee("fn tsl_mod_vec3( x : vec3f, y : vec3f ) -> vec3f { return x - y * floor( x / y ); }"),
  mod_vec4: new Ee("fn tsl_mod_vec4( x : vec4f, y : vec4f ) -> vec4f { return x - y * floor( x / y ); }"),
  equals_bool: new Ee("fn tsl_equals_bool( a : bool, b : bool ) -> bool { return a == b; }"),
  equals_bvec2: new Ee("fn tsl_equals_bvec2( a : vec2f, b : vec2f ) -> vec2<bool> { return vec2<bool>( a.x == b.x, a.y == b.y ); }"),
  equals_bvec3: new Ee("fn tsl_equals_bvec3( a : vec3f, b : vec3f ) -> vec3<bool> { return vec3<bool>( a.x == b.x, a.y == b.y, a.z == b.z ); }"),
  equals_bvec4: new Ee("fn tsl_equals_bvec4( a : vec4f, b : vec4f ) -> vec4<bool> { return vec4<bool>( a.x == b.x, a.y == b.y, a.z == b.z, a.w == b.w ); }"),
  repeatWrapping_float: new Ee("fn tsl_repeatWrapping_float( coord: f32 ) -> f32 { return fract( coord ); }"),
  mirrorWrapping_float: new Ee("fn tsl_mirrorWrapping_float( coord: f32 ) -> f32 { let mirrored = fract( coord * 0.5 ) * 2.0; return 1.0 - abs( 1.0 - mirrored ); }"),
  clampWrapping_float: new Ee("fn tsl_clampWrapping_float( coord: f32 ) -> f32 { return clamp( coord, 0.0, 1.0 ); }"),
  biquadraticTexture: new Ee(
    /* wgsl */
    `
fn tsl_biquadraticTexture( map : texture_2d<f32>, coord : vec2f, iRes : vec2u, level : u32 ) -> vec4f {

	let res = vec2f( iRes );

	let uvScaled = coord * res;
	let uvWrapping = ( ( uvScaled % res ) + res ) % res;

	// https://www.shadertoy.com/view/WtyXRy

	let uv = uvWrapping - 0.5;
	let iuv = floor( uv );
	let f = fract( uv );

	let rg1 = textureLoad( map, vec2u( iuv + vec2( 0.5, 0.5 ) ) % iRes, level );
	let rg2 = textureLoad( map, vec2u( iuv + vec2( 1.5, 0.5 ) ) % iRes, level );
	let rg3 = textureLoad( map, vec2u( iuv + vec2( 0.5, 1.5 ) ) % iRes, level );
	let rg4 = textureLoad( map, vec2u( iuv + vec2( 1.5, 1.5 ) ) % iRes, level );

	return mix( mix( rg1, rg2, f.x ), mix( rg3, rg4, f.x ), f.y );

}
`
  ),
  biquadraticTextureArray: new Ee(
    /* wgsl */
    `
fn tsl_biquadraticTexture_array( map : texture_2d_array<f32>, coord : vec2f, iRes : vec2u, layer : u32, level : u32 ) -> vec4f {

	let res = vec2f( iRes );

	let uvScaled = coord * res;
	let uvWrapping = ( ( uvScaled % res ) + res ) % res;

	// https://www.shadertoy.com/view/WtyXRy

	let uv = uvWrapping - 0.5;
	let iuv = floor( uv );
	let f = fract( uv );

	let rg1 = textureLoad( map, vec2u( iuv + vec2( 0.5, 0.5 ) ) % iRes, layer, level );
	let rg2 = textureLoad( map, vec2u( iuv + vec2( 1.5, 0.5 ) ) % iRes, layer, level );
	let rg3 = textureLoad( map, vec2u( iuv + vec2( 0.5, 1.5 ) ) % iRes, layer, level );
	let rg4 = textureLoad( map, vec2u( iuv + vec2( 1.5, 1.5 ) ) % iRes, layer, level );

	return mix( mix( rg1, rg2, f.x ), mix( rg3, rg4, f.x ), f.y );

}
`
  )
}, hN = {
  dFdx: "dpdx",
  dFdy: "- dpdy",
  mod_float: "tsl_mod_float",
  mod_vec2: "tsl_mod_vec2",
  mod_vec3: "tsl_mod_vec3",
  mod_vec4: "tsl_mod_vec4",
  equals_bool: "tsl_equals_bool",
  equals_bvec2: "tsl_equals_bvec2",
  equals_bvec3: "tsl_equals_bvec3",
  equals_bvec4: "tsl_equals_bvec4",
  inversesqrt: "inverseSqrt",
  bitcast: "bitcast<f32>",
  floatpack_snorm_2x16: "pack2x16snorm",
  floatpack_unorm_2x16: "pack2x16unorm",
  floatpack_float16_2x16: "pack2x16float",
  floatunpack_snorm_2x16: "unpack2x16snorm",
  floatunpack_unorm_2x16: "unpack2x16unorm",
  floatunpack_float16_2x16: "unpack2x16float"
};
let yh = "";
(typeof navigator < "u" && /Firefox|Deno/g.test(navigator.userAgent)) !== !0 && (yh += `diagnostic( off, derivative_uniformity );
`);
class fN extends ah {
  /**
   * Constructs a new WGSL node builder renderer.
   *
   * @param {Object3D} object - The 3D object.
   * @param {Renderer} renderer - The renderer.
   */
  constructor(e, t) {
    super(e, t, new uN()), this.uniformGroups = {}, this.uniformGroupsBindings = {}, this.builtins = {}, this.directives = {}, this.scopedArrays = /* @__PURE__ */ new Map(), this.allowEarlyReturns = !0, this.allowGlobalVariables = !0;
  }
  /**
   * Generates the WGSL snippet for sampled textures.
   *
   * @private
   * @param {Texture} texture - The texture.
   * @param {string} textureProperty - The name of the texture uniform in the shader.
   * @param {string} uvSnippet - A WGSL snippet that represents texture coordinates used for sampling.
   * @param {?string} depthSnippet - A WGSL snippet that represents 0-based texture array index to sample.
   * @param {?string} offsetSnippet - A WGSL snippet that represents the offset that will be applied to the unnormalized texture coordinate before sampling the texture.
   * @param {string} [shaderStage=this.shaderStage] - The shader stage this code snippet is generated for.
   * @return {string} The WGSL snippet.
   */
  _generateTextureSample(e, t, s, r, n, i = this.shaderStage) {
    return i === "fragment" ? r ? n ? `textureSample( ${t}, ${t}_sampler, ${s}, ${r}, ${n} )` : `textureSample( ${t}, ${t}_sampler, ${s}, ${r} )` : n ? `textureSample( ${t}, ${t}_sampler, ${s}, ${n} )` : `textureSample( ${t}, ${t}_sampler, ${s} )` : this.generateTextureSampleLevel(e, t, s, "0", r);
  }
  /**
   * Generates the WGSL snippet when sampling textures with explicit mip level.
   *
   * @private
   * @param {Texture} texture - The texture.
   * @param {string} textureProperty - The name of the texture uniform in the shader.
   * @param {string} uvSnippet - A WGSL snippet that represents texture coordinates used for sampling.
   * @param {string} levelSnippet - A WGSL snippet that represents the mip level, with level 0 containing a full size version of the texture.
   * @param {string} depthSnippet - A WGSL snippet that represents 0-based texture array index to sample.
   * @param {?string} offsetSnippet - A WGSL snippet that represents the offset that will be applied to the unnormalized texture coordinate before sampling the texture.
   * @return {string} The WGSL snippet.
   */
  generateTextureSampleLevel(e, t, s, r, n, i) {
    return this.isUnfilterable(e) === !1 ? n ? i ? `textureSampleLevel( ${t}, ${t}_sampler, ${s}, ${n}, ${r}, ${i} )` : `textureSampleLevel( ${t}, ${t}_sampler, ${s}, ${n}, ${r} )` : i ? `textureSampleLevel( ${t}, ${t}_sampler, ${s}, ${r}, ${i} )` : `textureSampleLevel( ${t}, ${t}_sampler, ${s}, ${r} )` : this.isFilteredTexture(e) ? this.generateFilteredTexture(e, t, s, i, r, n) : this.generateTextureLod(e, t, s, n, i, r);
  }
  /**
   * Generates a wrap function used in context of textures.
   *
   * @param {Texture} texture - The texture to generate the function for.
   * @return {string} The name of the generated function.
   */
  generateWrapFunction(e) {
    const t = `tsl_coord_${Cl[e.wrapS]}S_${Cl[e.wrapT]}_${e.is3DTexture || e.isData3DTexture ? "3d" : "2d"}T`;
    let s = Ml[t];
    if (s === void 0) {
      const r = [], n = e.is3DTexture || e.isData3DTexture ? "vec3f" : "vec2f";
      let i = `fn ${t}( coord : ${n} ) -> ${n} {

	return ${n}(
`;
      const o = (u, l) => {
        u === oi ? (r.push(Er.repeatWrapping_float), i += `		tsl_repeatWrapping_float( coord.${l} )`) : u === Vr ? (r.push(Er.clampWrapping_float), i += `		tsl_clampWrapping_float( coord.${l} )`) : u === ii ? (r.push(Er.mirrorWrapping_float), i += `		tsl_mirrorWrapping_float( coord.${l} )`) : (i += `		coord.${l}`, L(`WebGPURenderer: Unsupported texture wrap type "${u}" for vertex shader.`));
      };
      o(e.wrapS, "x"), i += `,
`, o(e.wrapT, "y"), (e.is3DTexture || e.isData3DTexture) && (i += `,
`, o(e.wrapR, "z")), i += `
	);

}
`, Ml[t] = s = new Ee(i, r);
    }
    return s.build(this), t;
  }
  /**
   * Generates the array declaration string.
   *
   * @param {string} type - The type.
   * @param {?number} [count] - The count.
   * @return {string} The generated value as a shader string.
   */
  generateArrayDeclaration(e, t) {
    return `array< ${this.getType(e)}, ${t} >`;
  }
  /**
   * Generates a WGSL variable that holds the texture dimension of the given texture.
   * It also returns information about the number of layers (elements) of an arrayed
   * texture as well as the cube face count of cube textures.
   *
   * @param {Texture} texture - The texture to generate the function for.
   * @param {string} textureProperty - The name of the video texture uniform in the shader.
   * @param {string} levelSnippet - A WGSL snippet that represents the mip level, with level 0 containing a full size version of the texture.
   * @return {string} The name of the dimension variable.
   */
  generateTextureDimension(e, t, s) {
    const r = this.getDataFromNode(e, this.shaderStage, this.cache);
    r.dimensionsSnippet === void 0 && (r.dimensionsSnippet = {});
    let n = r.dimensionsSnippet[s];
    if (r.dimensionsSnippet[s] === void 0) {
      let i, o;
      const { primarySamples: u } = this.renderer.backend.utils.getTextureSampleData(e), l = u > 1;
      e.is3DTexture || e.isData3DTexture ? o = "vec3<u32>" : o = "vec2<u32>", l || e.isStorageTexture ? i = t : i = `${t}${s ? `, u32( ${s} )` : ""}`, n = new Bn(new Fn(`textureDimensions( ${i} )`, o)), r.dimensionsSnippet[s] = n, (e.isArrayTexture || e.isDataArrayTexture || e.is3DTexture || e.isData3DTexture) && (r.arrayLayerCount = new Bn(
        new Fn(
          `textureNumLayers(${t})`,
          "u32"
        )
      )), e.isTextureCube && (r.cubeFaceCount = new Bn(
        new Fn("6u", "u32")
      ));
    }
    return n.build(this);
  }
  /**
   * Generates the WGSL snippet for a manual filtered texture.
   *
   * @param {Texture} texture - The texture.
   * @param {string} textureProperty - The name of the texture uniform in the shader.
   * @param {string} uvSnippet - A WGSL snippet that represents texture coordinates used for sampling.
   * @param {?string} offsetSnippet - A WGSL snippet that represents the offset that will be applied to the unnormalized texture coordinate before sampling the texture.
   * @param {string} [levelSnippet='0u'] - A WGSL snippet that represents the mip level, with level 0 containing a full size version of the texture.
   * @param {?string} depthSnippet - A WGSL snippet that represents 0-based texture array index to sample.
   * @return {string} The WGSL snippet.
   */
  generateFilteredTexture(e, t, s, r, n = "0u", i) {
    const o = this.generateWrapFunction(e), u = this.generateTextureDimension(e, t, n);
    return r && (s = `${s} + vec2<f32>(${r}) / ${u}`), i ? (this._include("biquadraticTextureArray"), `tsl_biquadraticTexture_array( ${t}, ${o}( ${s} ), ${u}, u32( ${i} ), u32( ${n} ) )`) : (this._include("biquadraticTexture"), `tsl_biquadraticTexture( ${t}, ${o}( ${s} ), ${u}, u32( ${n} ) )`);
  }
  /**
   * Generates the WGSL snippet for a texture lookup with explicit level-of-detail.
   * Since it's a lookup, no sampling or filtering is applied.
   *
   * @param {Texture} texture - The texture.
   * @param {string} textureProperty - The name of the texture uniform in the shader.
   * @param {string} uvSnippet - A WGSL snippet that represents texture coordinates used for sampling.
   * @param {?string} depthSnippet - A WGSL snippet that represents 0-based texture array index to sample.
   * @param {?string} offsetSnippet - A WGSL snippet that represents the offset that will be applied to the unnormalized texture coordinate before sampling the texture.
   * @param {string} [levelSnippet='0u'] - A WGSL snippet that represents the mip level, with level 0 containing a full size version of the texture.
   * @return {string} The WGSL snippet.
   */
  generateTextureLod(e, t, s, r, n, i = "0u") {
    if (e.isCubeTexture === !0) {
      n && (s = `${s} + vec3<f32>(${n})`);
      const f = e.isDepthTexture ? "u32" : "f32";
      return `textureSampleLevel( ${t}, ${t}_sampler, ${s}, ${f}( ${i} ) )`;
    }
    const o = this.generateWrapFunction(e), u = this.generateTextureDimension(e, t, i), l = e.is3DTexture || e.isData3DTexture ? "vec3" : "vec2", c = l === "vec3" ? "vec3<u32>( 1, 1, 1 )" : "vec2<u32>( 1, 1 )";
    n && (s = `${s} + ${l}<f32>(${n}) / ${l}<f32>( ${u} )`);
    const d = `${l}<f32>( 0 )`, h = `${l}<f32>( ${u} - ${c} )`;
    return s = `${l}<u32>( clamp( floor( ${o}( ${s} ) * ${l}<f32>( ${u} ) ), ${d}, ${h} ) )`, this.generateTextureLoad(e, t, s, i, r, null);
  }
  /**
   * Generates the WGSL snippet that reads a single texel from a storage texture.
   *
   * @param {Texture} texture - The texture.
   * @param {string} textureProperty - The name of the texture uniform in the shader.
   * @param {string} uvIndexSnippet - A WGSL snippet that represents texture coordinates used for sampling.
   * @param {?string} levelSnippet - A WGSL snippet that represents the mip level, with level 0 containing a full size version of the texture.
   * @param {?string} depthSnippet - A WGSL snippet that represents 0-based texture array index to sample.
   * @param {?string} offsetSnippet - A WGSL snippet that represents the offset that will be applied to the unnormalized texture coordinate before sampling the texture.
   * @return {string} The WGSL snippet.
   */
  generateStorageTextureLoad(e, t, s, r, n, i) {
    i && (s = `${s} + ${i}`);
    let o;
    return n ? o = `textureLoad( ${t}, ${s}, ${n} )` : o = `textureLoad( ${t}, ${s} )`, o;
  }
  /**
   * Generates the WGSL snippet that reads a single texel from a texture without sampling or filtering.
   *
   * @param {Texture} texture - The texture.
   * @param {string} textureProperty - The name of the texture uniform in the shader.
   * @param {string} uvIndexSnippet - A WGSL snippet that represents texture coordinates used for sampling.
   * @param {?string} levelSnippet - A WGSL snippet that represents the mip level, with level 0 containing a full size version of the texture.
   * @param {?string} depthSnippet - A WGSL snippet that represents 0-based texture array index to sample.
   * @param {?string} offsetSnippet - A WGSL snippet that represents the offset that will be applied to the unnormalized texture coordinate before sampling the texture.
   * @return {string} The WGSL snippet.
   */
  generateTextureLoad(e, t, s, r, n, i) {
    r === null && (r = "0u"), i && (s = `${s} + ${i}`);
    let o;
    return n ? o = `textureLoad( ${t}, ${s}, ${n}, u32( ${r} ) )` : (o = `textureLoad( ${t}, ${s}, u32( ${r} ) )`, this.renderer.backend.compatibilityMode && e.isDepthTexture && (o += ".x")), o;
  }
  /**
   * Generates the WGSL snippet that writes a single texel to a texture.
   *
   * @param {Texture} texture - The texture.
   * @param {string} textureProperty - The name of the texture uniform in the shader.
   * @param {string} uvIndexSnippet - A WGSL snippet that represents texture coordinates used for sampling.
   * @param {?string} depthSnippet - A WGSL snippet that represents 0-based texture array index to sample.
   * @param {string} valueSnippet - A WGSL snippet that represent the new texel value.
   * @return {string} The WGSL snippet.
   */
  generateTextureStore(e, t, s, r, n) {
    let i;
    return r ? i = `textureStore( ${t}, ${s}, ${r}, ${n} )` : i = `textureStore( ${t}, ${s}, ${n} )`, i;
  }
  /**
   * Returns `true` if the sampled values of the given texture should be compared against a reference value.
   *
   * @param {Texture} texture - The texture.
   * @return {boolean} Whether the sampled values of the given texture should be compared against a reference value or not.
   */
  isSampleCompare(e) {
    return e.isDepthTexture === !0 && e.compareFunction !== null && this.renderer.hasCompatibility(Ps.TEXTURE_COMPARE);
  }
  /**
   * Returns `true` if the given texture is unfilterable.
   *
   * @param {Texture} texture - The texture.
   * @return {boolean} Whether the given texture is unfilterable or not.
   */
  isUnfilterable(e) {
    return this.getComponentTypeFromTexture(e) !== "float" || !this.isAvailable("float32Filterable") && e.type === De || this.isSampleCompare(e) === !1 && e.minFilter === Qe && e.magFilter === Qe || this.renderer.backend.utils.getTextureSampleData(e).primarySamples > 1;
  }
  /**
   * Generates the WGSL snippet for sampling/loading the given texture.
   *
   * @param {Texture} texture - The texture.
   * @param {string} textureProperty - The name of the texture uniform in the shader.
   * @param {string} uvSnippet - A WGSL snippet that represents texture coordinates used for sampling.
   * @param {?string} depthSnippet - A WGSL snippet that represents 0-based texture array index to sample.
   * @param {?string} offsetSnippet - A WGSL snippet that represents the offset that will be applied to the unnormalized texture coordinate before sampling the texture.
   * @param {string} [shaderStage=this.shaderStage] - The shader stage this code snippet is generated for.
   * @return {string} The WGSL snippet.
   */
  generateTexture(e, t, s, r, n, i = this.shaderStage) {
    let o = null;
    return this.isUnfilterable(e) ? o = this.generateTextureLod(e, t, s, r, n, "0", i) : o = this._generateTextureSample(e, t, s, r, n, i), o;
  }
  /**
   * Generates the WGSL snippet for sampling/loading the given texture using explicit gradients.
   *
   * @param {Texture} texture - The texture.
   * @param {string} textureProperty - The name of the texture uniform in the shader.
   * @param {string} uvSnippet - A WGSL snippet that represents texture coordinates used for sampling.
   * @param {Array<string>} gradSnippet - An array holding both gradient WGSL snippets.
   * @param {?string} depthSnippet - A WGSL snippet that represents 0-based texture array index to sample.
   * @param {?string} offsetSnippet - A WGSL snippet that represents the offset that will be applied to the unnormalized texture coordinate before sampling the texture.
   * @param {string} [shaderStage=this.shaderStage] - The shader stage this code snippet is generated for.
   * @return {string} The WGSL snippet.
   */
  generateTextureGrad(e, t, s, r, n, i, o = this.shaderStage) {
    if (o === "fragment")
      return n ? i ? `textureSampleGrad( ${t}, ${t}_sampler, ${s}, ${n}, ${r[0]}, ${r[1]}, ${i} )` : `textureSampleGrad( ${t}, ${t}_sampler, ${s}, ${n}, ${r[0]}, ${r[1]} )` : i ? `textureSampleGrad( ${t}, ${t}_sampler, ${s}, ${r[0]}, ${r[1]}, ${i} )` : `textureSampleGrad( ${t}, ${t}_sampler, ${s}, ${r[0]}, ${r[1]} )`;
    F(`WebGPURenderer: THREE.TextureNode.gradient() does not support ${o} shader.`);
  }
  /**
   * Generates the WGSL snippet for sampling a depth texture and comparing the sampled depth values
   * against a reference value.
   *
   * @param {Texture} texture - The texture.
   * @param {string} textureProperty - The name of the texture uniform in the shader.
   * @param {string} uvSnippet - A WGSL snippet that represents texture coordinates used for sampling.
   * @param {string} compareSnippet -  A WGSL snippet that represents the reference value.
   * @param {?string} depthSnippet - A WGSL snippet that represents 0-based texture array index to sample.
   * @param {?string} offsetSnippet - A WGSL snippet that represents the offset that will be applied to the unnormalized texture coordinate before sampling the texture.
   * @param {string} [shaderStage=this.shaderStage] - The shader stage this code snippet is generated for.
   * @return {string} The WGSL snippet.
   */
  generateTextureCompare(e, t, s, r, n, i, o = this.shaderStage) {
    if (o === "fragment")
      return e.isDepthTexture === !0 && e.isArrayTexture === !0 ? i ? `textureSampleCompare( ${t}, ${t}_sampler, ${s}, ${n}, ${r}, ${i} )` : `textureSampleCompare( ${t}, ${t}_sampler, ${s}, ${n}, ${r} )` : i ? `textureSampleCompare( ${t}, ${t}_sampler, ${s}, ${r}, ${i} )` : `textureSampleCompare( ${t}, ${t}_sampler, ${s}, ${r} )`;
    F(`WebGPURenderer: THREE.DepthTexture.compareFunction() does not support ${o} shader.`);
  }
  /**
   * Generates the WGSL snippet when sampling textures with explicit mip level.
   *
   * @param {Texture} texture - The texture.
   * @param {string} textureProperty - The name of the texture uniform in the shader.
   * @param {string} uvSnippet - A WGSL snippet that represents texture coordinates used for sampling.
   * @param {string} levelSnippet - A WGSL snippet that represents the mip level, with level 0 containing a full size version of the texture.
   * @param {?string} depthSnippet - A WGSL snippet that represents 0-based texture array index to sample.
   * @param {?string} offsetSnippet - A WGSL snippet that represents the offset that will be applied to the unnormalized texture coordinate before sampling the texture.
   * @param {string} [shaderStage=this.shaderStage] - The shader stage this code snippet is generated for.
   * @return {string} The WGSL snippet.
   */
  generateTextureLevel(e, t, s, r, n, i) {
    return this.isUnfilterable(e) === !1 ? n ? i ? `textureSampleLevel( ${t}, ${t}_sampler, ${s}, ${n}, ${r}, ${i} )` : `textureSampleLevel( ${t}, ${t}_sampler, ${s}, ${n}, ${r} )` : i ? `textureSampleLevel( ${t}, ${t}_sampler, ${s}, ${r}, ${i} )` : `textureSampleLevel( ${t}, ${t}_sampler, ${s}, ${r} )` : this.isFilteredTexture(e) ? this.generateFilteredTexture(e, t, s, i, r, n) : this.generateTextureLod(e, t, s, n, i, r);
  }
  /**
   * Generates the WGSL snippet when sampling textures with a bias to the mip level.
   *
   * @param {Texture} texture - The texture.
   * @param {string} textureProperty - The name of the texture uniform in the shader.
   * @param {string} uvSnippet - A WGSL snippet that represents texture coordinates used for sampling.
   * @param {string} biasSnippet - A WGSL snippet that represents the bias to apply to the mip level before sampling.
   * @param {?string} depthSnippet - A WGSL snippet that represents 0-based texture array index to sample.
   * @param {?string} offsetSnippet - A WGSL snippet that represents the offset that will be applied to the unnormalized texture coordinate before sampling the texture.
   * @param {string} [shaderStage=this.shaderStage] - The shader stage this code snippet is generated for.
   * @return {string} The WGSL snippet.
   */
  generateTextureBias(e, t, s, r, n, i, o = this.shaderStage) {
    if (o === "fragment")
      return n ? i ? `textureSampleBias( ${t}, ${t}_sampler, ${s}, ${n}, ${r}, ${i} )` : `textureSampleBias( ${t}, ${t}_sampler, ${s}, ${n}, ${r} )` : i ? `textureSampleBias( ${t}, ${t}_sampler, ${s}, ${r}, ${i} )` : `textureSampleBias( ${t}, ${t}_sampler, ${s}, ${r} )`;
    F(`WebGPURenderer: THREE.TextureNode.biasNode does not support ${o} shader.`);
  }
  /**
   * Returns a WGSL snippet that represents the property name of the given node.
   *
   * @param {Node} node - The node.
   * @param {string} [shaderStage=this.shaderStage] - The shader stage this code snippet is generated for.
   * @return {string} The property name.
   */
  getPropertyName(e, t = this.shaderStage) {
    if (e.isNodeVarying === !0 && e.needsInterpolation === !0) {
      if (t === "vertex")
        return `varyings.${e.name}`;
    } else if (e.isNodeUniform === !0) {
      const s = e.name, r = e.type;
      return r === "texture" || r === "cubeTexture" || r === "cubeDepthTexture" || r === "storageTexture" || r === "texture3D" ? s : r === "buffer" || r === "storageBuffer" || r === "indirectStorageBuffer" ? this.isCustomStruct(e) ? s : s + ".value" : e.groupNode.name + "." + s;
    }
    return super.getPropertyName(e);
  }
  /**
   * Returns the output struct name.
   *
   * @return {string} The name of the output struct.
   */
  getOutputStructName() {
    return "output";
  }
  /**
   * Returns the native shader operator name for a given generic name.
   *
   * @param {string} op - The operator name to resolve.
   * @return {?string} The resolved operator name.
   */
  getFunctionOperator(e) {
    const t = cN[e];
    return t !== void 0 ? (this._include(t), t) : null;
  }
  /**
   * Returns the node access for the given node and shader stage.
   *
   * @param {StorageTextureNode|StorageBufferNode} node - The storage node.
   * @param {string} shaderStage - The shader stage.
   * @return {string} The node access.
   */
  getNodeAccess(e, t) {
    return t !== "compute" ? e.isAtomic === !0 ? (L("WebGPURenderer: Atomic operations are only supported in compute shaders."), nt.READ_WRITE) : nt.READ_ONLY : e.access;
  }
  /**
   * Returns A WGSL snippet representing the storage access.
   *
   * @param {StorageTextureNode|StorageBufferNode} node - The storage node.
   * @param {string} shaderStage - The shader stage.
   * @return {string} The WGSL snippet representing the storage access.
   */
  getStorageAccess(e, t) {
    return lN[this.getNodeAccess(e, t)];
  }
  /**
   * This method is one of the more important ones since it's responsible
   * for generating a matching binding instance for the given uniform node.
   *
   * These bindings are later used in the renderer to create bind groups
   * and layouts.
   *
   * @param {UniformNode} node - The uniform node.
   * @param {string} type - The node data type.
   * @param {string} shaderStage - The shader stage.
   * @param {?string} [name=null] - An optional uniform name.
   * @return {NodeUniform} The node uniform object.
   */
  getUniformFromNode(e, t, s, r = null) {
    const n = super.getUniformFromNode(e, t, s, r), i = this.getDataFromNode(e, s, this.globalCache);
    if (i.uniformGPU === void 0) {
      let o;
      const u = e.groupNode, l = u.name, c = this.getBindGroupArray(l, s);
      if (t === "texture" || t === "cubeTexture" || t === "cubeDepthTexture" || t === "storageTexture" || t === "texture3D") {
        let d = null;
        const h = this.getNodeAccess(e, s);
        if (t === "texture" || t === "storageTexture" ? e.value.is3DTexture === !0 ? d = new ta(n.name, n.node, u, h) : d = new _i(n.name, n.node, u, h) : t === "cubeTexture" || t === "cubeDepthTexture" ? d = new mh(n.name, n.node, u, h) : t === "texture3D" && (d = new ta(n.name, n.node, u, h)), d.store = e.isStorageTextureNode === !0, d.mipLevel = d.store ? e.mipLevel : 0, d.setVisibility(ho[s]), e.value.isCubeTexture === !0 || this.isUnfilterable(e.value) === !1 && d.store === !1) {
          const p = new YS(`${n.name}_sampler`, n.node, u);
          p.setVisibility(ho[s]), c.push(p, d), o = [p, d];
        } else
          c.push(d), o = [d];
      } else if (t === "buffer" || t === "storageBuffer" || t === "indirectStorageBuffer") {
        const d = this.getSharedDataFromNode(e);
        let h = d.buffer;
        if (h === void 0) {
          const f = t === "buffer" ? fh : JS;
          h = new f(e, u), d.buffer = h;
        }
        h.setVisibility(h.getVisibility() | ho[s]), c.push(h), o = h, n.name = r || "NodeBuffer_" + n.id;
      } else {
        let d = this.uniformGroups[l];
        d === void 0 && (d = new ph(l, u), d.setVisibility(At.VERTEX | At.FRAGMENT | At.COMPUTE), this.uniformGroups[l] = d), c.indexOf(d) === -1 && c.push(d), o = this.getNodeUniform(n, t);
        const h = o.name;
        d.uniforms.some((p) => p.name === h) || d.addUniform(o);
      }
      i.uniformGPU = o;
    }
    return n;
  }
  /**
   * This method should be used whenever builtins are required in nodes.
   * The internal builtins data structure will make sure builtins are
   * defined in the WGSL source.
   *
   * @param {string} name - The builtin name.
   * @param {string} property - The property name.
   * @param {string} type - The node data type.
   * @param {string} [shaderStage=this.shaderStage] - The shader stage this code snippet is generated for.
   * @return {string} The property name.
   */
  getBuiltin(e, t, s, r = this.shaderStage) {
    const n = this.builtins[r] || (this.builtins[r] = /* @__PURE__ */ new Map());
    return n.has(e) === !1 && n.set(e, {
      name: e,
      property: t,
      type: s
    }), t;
  }
  /**
   * Returns `true` if the given builtin is defined in the given shader stage.
   *
   * @param {string} name - The builtin name.
   * @param {string} [shaderStage=this.shaderStage] - The shader stage this code snippet is generated for.
   * @return {boolean} Whether the given builtin is defined in the given shader stage or not.
   */
  hasBuiltin(e, t = this.shaderStage) {
    return this.builtins[t] !== void 0 && this.builtins[t].has(e);
  }
  /**
   * Returns the vertex index builtin.
   *
   * @return {string} The vertex index.
   */
  getVertexIndex() {
    return this.shaderStage === "vertex" ? this.getBuiltin("vertex_index", "vertexIndex", "u32", "attribute") : "vertexIndex";
  }
  /**
   * Builds the given shader node.
   *
   * @param {ShaderNodeInternal} shaderNode - The shader node.
   * @return {string} The WGSL function code.
   */
  buildFunctionCode(e) {
    const t = e.layout, s = this.flowShaderNode(e), r = [];
    for (const i of t.inputs)
      r.push(i.name + " : " + this.getType(i.type));
    let n = `fn ${t.name}( ${r.join(", ")} ) -> ${this.getType(t.type)} {
${s.vars}
${s.code}
`;
    return s.result && (n += `	return ${s.result};
`), n += `
}
`, n;
  }
  /**
   * Contextually returns either the vertex stage instance index builtin
   * or the linearized index of an compute invocation within a grid of workgroups.
   *
   * @return {string} The instance index.
   */
  getInstanceIndex() {
    return this.shaderStage === "vertex" ? this.getBuiltin("instance_index", "instanceIndex", "u32", "attribute") : "instanceIndex";
  }
  /**
   * Returns a builtin representing the index of a compute invocation within the scope of a workgroup load.
   *
   * @return {string} The invocation local index.
   */
  getInvocationLocalIndex() {
    return this.getBuiltin("local_invocation_index", "invocationLocalIndex", "u32", "attribute");
  }
  /**
   * Returns a builtin representing the size of a subgroup within the current shader.
   *
   * @return {string} The subgroup size.
   */
  getSubgroupSize() {
    return this.enableSubGroups(), this.getBuiltin("subgroup_size", "subgroupSize", "u32", "attribute");
  }
  /**
   * Returns a builtin representing the index of a compute invocation within the scope of a subgroup.
   *
   * @return {string} The invocation subgroup index.
   */
  getInvocationSubgroupIndex() {
    return this.enableSubGroups(), this.getBuiltin("subgroup_invocation_id", "invocationSubgroupIndex", "u32", "attribute");
  }
  /**
   * Returns a builtin representing the index of a compute invocation's subgroup within its workgroup.
   *
   * @return {string} The subgroup index.
   */
  getSubgroupIndex() {
    return this.enableSubGroups(), this.getBuiltin("subgroup_id", "subgroupIndex", "u32", "attribute");
  }
  /**
   * Overwritten as a NOP since this method is intended for the WebGL 2 backend.
   *
   * @return {null} Null.
   */
  getDrawIndex() {
    return null;
  }
  /**
   * Returns the front facing builtin.
   *
   * @return {string} The front facing builtin.
   */
  getFrontFacing() {
    return this.getBuiltin("front_facing", "isFront", "bool");
  }
  /**
   * Returns the frag coord builtin.
   *
   * @return {string} The frag coord builtin.
   */
  getFragCoord() {
    return this.getBuiltin("position", "fragCoord", "vec4<f32>") + ".xy";
  }
  /**
   * Returns the frag depth builtin.
   *
   * @return {string} The frag depth builtin.
   */
  getFragDepth() {
    return "output." + this.getBuiltin("frag_depth", "depth", "f32", "output");
  }
  /**
   * Returns the clip distances builtin.
   *
   * @return {string} The clip distances builtin.
   */
  getClipDistance() {
    return "varyings.hw_clip_distances";
  }
  /**
   * Whether to flip texture data along its vertical axis or not.
   *
   * @return {boolean} Returns always `false` in context of WGSL.
   */
  isFlipY() {
    return !1;
  }
  /**
   * Enables the given directive for the given shader stage.
   *
   * @param {string} name - The directive name.
   * @param {string} [shaderStage=this.shaderStage] - The shader stage to enable the directive for.
   */
  enableDirective(e, t = this.shaderStage) {
    (this.directives[t] || (this.directives[t] = /* @__PURE__ */ new Set())).add(e);
  }
  /**
   * Returns the directives of the given shader stage as a WGSL string.
   *
   * @param {string} shaderStage - The shader stage.
   * @return {string} A WGSL snippet that enables the directives of the given stage.
   */
  getDirectives(e) {
    const t = [], s = this.directives[e];
    if (s !== void 0)
      for (const r of s)
        t.push(`enable ${r};`);
    return t.join(`
`);
  }
  /**
   * Enables the 'subgroups' directive.
   */
  enableSubGroups() {
    this.enableDirective("subgroups");
  }
  /**
   * Enables the 'subgroups-f16' directive.
   */
  enableSubgroupsF16() {
    this.enableDirective("subgroups-f16");
  }
  /**
   * Enables the 'clip_distances' directive.
   */
  enableClipDistances() {
    this.enableDirective("clip_distances");
  }
  /**
   * Enables the 'f16' directive.
   */
  enableShaderF16() {
    this.enableDirective("f16");
  }
  /**
   * Enables the 'dual_source_blending' directive.
   */
  enableDualSourceBlending() {
    this.enableDirective("dual_source_blending");
  }
  /**
   * Enables hardware clipping.
   *
   * @param {string} planeCount - The clipping plane count.
   */
  enableHardwareClipping(e) {
    this.enableClipDistances(), this.getBuiltin("clip_distances", "hw_clip_distances", `array<f32, ${e} >`, "vertex");
  }
  /**
   * Returns the builtins of the given shader stage as a WGSL string.
   *
   * @param {string} shaderStage - The shader stage.
   * @return {string} A WGSL snippet that represents the builtins of the given stage.
   */
  getBuiltins(e) {
    const t = [], s = this.builtins[e];
    if (s !== void 0)
      for (const { name: r, property: n, type: i } of s.values())
        t.push(`@builtin( ${r} ) ${n} : ${i}`);
    return t.join(`,
	`);
  }
  /**
   * This method should be used when a new scoped buffer is used in context of
   * compute shaders. It adds the array to the internal data structure which is
   * later used to generate the respective WGSL.
   *
   * @param {string} name - The array name.
   * @param {string} scope - The scope.
   * @param {string} bufferType - The buffer type.
   * @param {string} bufferCount - The buffer count.
   * @return {string} The array name.
   */
  getScopedArray(e, t, s, r) {
    return this.scopedArrays.has(e) === !1 && this.scopedArrays.set(e, {
      name: e,
      scope: t,
      bufferType: s,
      bufferCount: r
    }), e;
  }
  /**
   * Returns the scoped arrays of the given shader stage as a WGSL string.
   *
   * @param {string} shaderStage - The shader stage.
   * @return {string|undefined} The WGSL snippet that defines the scoped arrays.
   * Returns `undefined` when used in the vertex or fragment stage.
   */
  getScopedArrays(e) {
    if (e !== "compute")
      return;
    const t = [];
    for (const { name: s, scope: r, bufferType: n, bufferCount: i } of this.scopedArrays.values()) {
      const o = this.getType(n);
      t.push(`var<${r}> ${s}: array< ${o}, ${i} >;`);
    }
    return t.join(`
`);
  }
  /**
   * Returns the shader attributes of the given shader stage as a WGSL string.
   *
   * @param {string} shaderStage - The shader stage.
   * @return {string} The WGSL snippet that defines the shader attributes.
   */
  getAttributes(e) {
    const t = [];
    if (e === "compute" && (this.getBuiltin("global_invocation_id", "globalId", "vec3<u32>", "attribute"), this.getBuiltin("workgroup_id", "workgroupId", "vec3<u32>", "attribute"), this.getBuiltin("local_invocation_id", "localId", "vec3<u32>", "attribute"), this.getBuiltin("num_workgroups", "numWorkgroups", "vec3<u32>", "attribute"), this.renderer.hasFeature("subgroups") && (this.enableDirective("subgroups", e), this.getBuiltin("subgroup_size", "subgroupSize", "u32", "attribute"))), e === "vertex" || e === "compute") {
      const s = this.getBuiltins("attribute");
      s && t.push(s);
      const r = this.getAttributesArray();
      for (let n = 0, i = r.length; n < i; n++) {
        const o = r[n], u = o.name, l = this.getType(o.type);
        t.push(`@location( ${n} ) ${u} : ${l}`);
      }
    }
    return t.join(`,
	`);
  }
  /**
   * Returns the members of the given struct type node as a WGSL string.
   *
   * @param {StructTypeNode} struct - The struct type node.
   * @return {string} The WGSL snippet that defines the struct members.
   */
  getStructMembers(e) {
    const t = [];
    for (const s of e.members) {
      const r = e.output ? "@location( " + s.index + " ) " : "";
      let n = this.getType(s.type);
      s.atomic && (n = "atomic< " + n + " >"), t.push(`	${r + s.name} : ${n}`);
    }
    return e.output && t.push(`	${this.getBuiltins("output")}`), t.join(`,
`);
  }
  /**
   * Returns the structs of the given shader stage as a WGSL string.
   *
   * @param {string} shaderStage - The shader stage.
   * @return {string} The WGSL snippet that defines the structs.
   */
  getStructs(e) {
    let t = "";
    const s = this.structs[e];
    if (s.length > 0) {
      const r = [];
      for (const n of s) {
        let i = `struct ${n.name} {
`;
        i += this.getStructMembers(n), i += `
};`, r.push(i);
      }
      t = `
` + r.join(`

`) + `
`;
    }
    return t;
  }
  /**
   * Returns a WGSL string representing a variable.
   *
   * @param {string} type - The variable's type.
   * @param {string} name - The variable's name.
   * @param {?number} [count=null] - The array length.
   * @param {string} [qualifier=''] - The variable's qualifier.
   * @return {string} The WGSL snippet that defines a variable.
   */
  getVar(e, t, s = null, r = "") {
    let n = `var${r} ${t} : `;
    return s !== null ? n += this.generateArrayDeclaration(e, s) : n += this.getType(e), n;
  }
  /**
   * Returns the variables of the given shader stage as a WGSL string.
   *
   * @param {string} shaderStage - The shader stage.
   * @return {string} The WGSL snippet that defines the variables.
   */
  getVars(e, t = !1) {
    let s = "";
    t && (s = "<private>");
    const r = [], n = this.vars[e];
    if (n !== void 0)
      for (const i of n)
        r.push(`${this.getVar(i.type, i.name, i.count, s)};`);
    return t ? r.join(`
`) : `
	${r.join(`
	`)}
`;
  }
  /**
   * Returns the varyings of the given shader stage as a WGSL string.
   *
   * @param {string} shaderStage - The shader stage.
   * @return {string} The WGSL snippet that defines the varyings.
   */
  getVaryings(e) {
    const t = [];
    if (e === "vertex" && this.getBuiltin("position", "builtinClipSpace", "vec4<f32>", "vertex"), e === "vertex" || e === "fragment") {
      const n = this.varyings, i = this.vars[e];
      let o = 0;
      for (let u = 0; u < n.length; u++) {
        const l = n[u];
        if (l.needsInterpolation) {
          let c = `@location( ${o++} )`;
          if (l.interpolationType) {
            const d = l.interpolationSampling !== null ? `, ${l.interpolationSampling} )` : " )";
            c += ` @interpolate( ${l.interpolationType}${d}`;
          } else /^(int|uint|ivec|uvec)/.test(l.type) && (c += " @interpolate(flat, either)");
          t.push(`${c} ${l.name} : ${this.getType(l.type)}`);
        } else e === "vertex" && i.includes(l) === !1 && i.push(l);
      }
    }
    const s = this.getBuiltins(e);
    s && t.push(s);
    const r = t.join(`,
	`);
    return e === "vertex" ? this._getWGSLStruct("VaryingsStruct", "	" + r) : r;
  }
  isCustomStruct(e) {
    const t = e.value, s = e.node, r = (t.isBufferAttribute || t.isInstancedBufferAttribute) && s.structTypeNode !== null, n = s.value && s.value.array && typeof s.value.itemSize == "number" && s.value.array.length > s.value.itemSize;
    return r && !n;
  }
  /**
   * Returns the uniforms of the given shader stage as a WGSL string.
   *
   * @param {string} shaderStage - The shader stage.
   * @return {string} The WGSL snippet that defines the uniforms.
   */
  getUniforms(e) {
    const t = this.renderer.backend, s = this.uniforms[e], r = [], n = [], i = [], o = {};
    for (const l of s) {
      const c = l.groupNode.name, d = this.bindingsIndexes[c];
      if (l.type === "texture" || l.type === "cubeTexture" || l.type === "cubeDepthTexture" || l.type === "storageTexture" || l.type === "texture3D") {
        const h = l.node.value;
        (h.isCubeTexture === !0 || this.isUnfilterable(h) === !1 && l.node.isStorageTextureNode !== !0) && (this.isSampleCompare(h) ? r.push(`@binding( ${d.binding++} ) @group( ${d.group} ) var ${l.name}_sampler : sampler_comparison;`) : r.push(`@binding( ${d.binding++} ) @group( ${d.group} ) var ${l.name}_sampler : sampler;`));
        let p, g = "";
        const { primarySamples: m } = t.utils.getTextureSampleData(h);
        if (m > 1 && (g = "_multisampled"), h.isCubeTexture === !0 && h.isDepthTexture === !0)
          p = "texture_depth_cube";
        else if (h.isCubeTexture === !0)
          p = "texture_cube<f32>";
        else if (h.isDepthTexture === !0)
          t.compatibilityMode && h.compareFunction === null ? p = `texture${g}_2d<f32>` : p = `texture_depth${g}_2d${h.isArrayTexture === !0 ? "_array" : ""}`;
        else if (l.node.isStorageTextureNode === !0) {
          const y = sa(h, t.device), _ = this.getStorageAccess(l.node, e), v = l.node.value.is3DTexture, C = l.node.value.isArrayTexture;
          p = `texture_storage_${v ? "3d" : `2d${C ? "_array" : ""}`}<${y}, ${_}>`;
        } else if (h.isArrayTexture === !0 || h.isDataArrayTexture === !0 || h.isCompressedArrayTexture === !0)
          p = "texture_2d_array<f32>";
        else if (h.is3DTexture === !0 || h.isData3DTexture === !0)
          p = "texture_3d<f32>";
        else {
          const y = this.getComponentTypeFromTexture(h).charAt(0);
          p = `texture${g}_2d<${y}32>`;
        }
        r.push(`@binding( ${d.binding++} ) @group( ${d.group} ) var ${l.name} : ${p};`);
      } else if (l.type === "buffer" || l.type === "storageBuffer" || l.type === "indirectStorageBuffer") {
        const h = l.node, f = this.getType(h.getNodeType(this)), p = h.bufferCount, g = p > 0 && l.type === "buffer" ? ", " + p : "", m = h.isStorageBufferNode ? `storage, ${this.getStorageAccess(h, e)}` : "uniform";
        if (this.isCustomStruct(l))
          n.push(`@binding( ${d.binding++} ) @group( ${d.group} ) var<${m}> ${l.name} : ${f};`);
        else {
          const _ = `	value : array< ${h.isAtomic ? `atomic<${f}>` : `${f}`}${g} >`;
          n.push(this._getWGSLStructBinding(l.name, _, m, d.binding++, d.group));
        }
      } else {
        const h = l.groupNode.name;
        if (o[h] === void 0) {
          const f = this.uniformGroups[h];
          if (f !== void 0) {
            const p = [];
            for (const m of f.uniforms) {
              const y = m.getType(), _ = this.getType(this.getVectorType(y));
              p.push(`	${m.name} : ${_}`);
            }
            let g = this.uniformGroupsBindings[h];
            g === void 0 && (g = {
              index: d.binding++,
              id: d.group
            }, this.uniformGroupsBindings[h] = g), o[h] = {
              index: g.index,
              id: g.id,
              snippets: p
            };
          }
        }
      }
    }
    for (const l in o) {
      const c = o[l];
      i.push(this._getWGSLStructBinding(l, c.snippets.join(`,
`), "uniform", c.index, c.id));
    }
    return [...r, ...n, ...i].join(`
`);
  }
  /**
   * Controls the code build of the shader stages.
   */
  buildCode() {
    const e = this.material !== null ? { fragment: {}, vertex: {} } : { compute: {} };
    this.sortBindingGroups();
    for (const t in e) {
      this.shaderStage = t;
      const s = this.allowGlobalVariables, r = e[t];
      r.uniforms = this.getUniforms(t), r.attributes = this.getAttributes(t), r.varyings = this.getVaryings(t), r.structs = this.getStructs(t), r.vars = this.getVars(t, s), r.codes = this.getCodes(t), r.directives = this.getDirectives(t), r.scopedArrays = this.getScopedArrays(t);
      let n = `// code

`;
      n += this.flowCode[t];
      const i = this.flowNodes[t], o = i[i.length - 1], u = o.outputNode, l = u !== void 0 && u.isOutputStructNode === !0;
      for (const c of i) {
        const d = this.getFlowData(
          c
          /*, shaderStage*/
        ), h = c.name;
        if (h && (n.length > 0 && (n += `
`), n += `	// flow -> ${h}
`), n += `${d.code}
	`, c === o && t !== "compute") {
          if (n += `// result

	`, t === "vertex")
            n += `varyings.builtinClipSpace = ${d.result};`;
          else if (t === "fragment")
            if (l)
              r.returnType = u.getNodeType(this), r.structs += "var<private> output : " + r.returnType + ";", n += `return ${d.result};`;
            else {
              let f = "	@location( 0 ) color: vec4<f32>";
              const p = this.getBuiltins("output");
              p && (f += `,
	` + p), r.returnType = "OutputStruct", r.structs += this._getWGSLStruct("OutputStruct", f), r.structs += `
var<private> output : OutputStruct;`, n += `output.color = ${d.result};

	return output;`;
            }
        }
      }
      r.flow = n;
    }
    if (this.shaderStage = null, this.material !== null)
      this.vertexShader = this._getWGSLVertexCode(e.vertex), this.fragmentShader = this._getWGSLFragmentCode(e.fragment);
    else {
      const t = this.object.workgroupSize;
      this.computeShader = this._getWGSLComputeCode(e.compute, t);
    }
  }
  /**
   * Returns the native shader method name for a given generic name.
   *
   * @param {string} method - The method name to resolve.
   * @param {?string} [output=null] - An optional output.
   * @return {string} The resolved WGSL method name.
   */
  getMethod(e, t = null) {
    let s;
    return t !== null && (s = this._getWGSLMethod(e + "_" + t)), s === void 0 && (s = this._getWGSLMethod(e)), s || e;
  }
  /**
   * Returns the bitcast method name for a given input and outputType.
   *
   * @param {string} type - The output type to bitcast to.
   * @return {string} The resolved WGSL bitcast invocation.
   */
  getBitcastMethod(e) {
    return `bitcast<${this.getType(e)}>`;
  }
  /**
   * Returns the float packing method name for a given numeric encoding.
   *
   * @param {string} encoding - The numeric encoding that describes how the float values are mapped to the integer range.
   * @returns {string} The resolve WGSL float packing method name.
   */
  getFloatPackingMethod(e) {
    return this.getMethod(`floatpack_${e}_2x16`);
  }
  /**
   * Returns the float unpacking method name for a given numeric encoding.
   *
   * @param {string} encoding - The numeric encoding that describes how the integer values are mapped to the float range.
   * @returns {string} The resolve WGSL float unpacking method name.
   */
  getFloatUnpackingMethod(e) {
    return this.getMethod(`floatunpack_${e}_2x16`);
  }
  /**
   * Returns the native snippet for a ternary operation.
   *
   * @param {string} condSnippet - The condition determining which expression gets resolved.
   * @param {string} ifSnippet - The expression to resolve to if the condition is true.
   * @param {string} elseSnippet - The expression to resolve to if the condition is false.
   * @return {string} The resolved method name.
   */
  getTernary(e, t, s) {
    return `select( ${s}, ${t}, ${e} )`;
  }
  /**
   * Returns the WGSL type of the given node data type.
   *
   * @param {string} type - The node data type.
   * @return {string} The WGSL type.
   */
  getType(e) {
    return dN[e] || e;
  }
  /**
   * Whether the requested feature is available or not.
   *
   * @param {string} name - The requested feature.
   * @return {boolean} Whether the requested feature is supported or not.
   */
  isAvailable(e) {
    let t = wl[e];
    return t === void 0 && (e === "float32Filterable" ? t = this.renderer.hasFeature("float32-filterable") : e === "clipDistance" && (t = this.renderer.hasFeature("clip-distances")), wl[e] = t), t;
  }
  /**
   * Returns the native shader method name for a given generic name.
   *
   * @private
   * @param {string} method - The method name to resolve.
   * @return {string} The resolved WGSL method name.
   */
  _getWGSLMethod(e) {
    return Er[e] !== void 0 && this._include(e), hN[e];
  }
  /**
   * Includes the given method name into the current
   * function node.
   *
   * @private
   * @param {string} name - The method name to include.
   * @return {CodeNode} The respective code node.
   */
  _include(e) {
    const t = Er[e];
    return t.build(this), this.addInclude(t), t;
  }
  /**
   * Returns a WGSL vertex shader based on the given shader data.
   *
   * @private
   * @param {Object} shaderData - The shader data.
   * @return {string} The vertex shader.
   */
  _getWGSLVertexCode(e) {
    return `${this.getSignature()}
// directives
${e.directives}

// structs
${e.structs}

// uniforms
${e.uniforms}

// varyings
${e.varyings}
var<private> varyings : VaryingsStruct;

// vars
${e.vars}

// codes
${e.codes}

@vertex
fn main( ${e.attributes} ) -> VaryingsStruct {

	// flow
	${e.flow}

	return varyings;

}
`;
  }
  /**
   * Returns a WGSL fragment shader based on the given shader data.
   *
   * @private
   * @param {Object} shaderData - The shader data.
   * @return {string} The vertex shader.
   */
  _getWGSLFragmentCode(e) {
    return `${this.getSignature()}
// global
${yh}

// structs
${e.structs}

// uniforms
${e.uniforms}

// vars
${e.vars}

// codes
${e.codes}

@fragment
fn main( ${e.varyings} ) -> ${e.returnType} {

	// flow
	${e.flow}

}
`;
  }
  /**
   * Returns a WGSL compute shader based on the given shader data.
   *
   * @private
   * @param {Object} shaderData - The shader data.
   * @param {string} workgroupSize - The workgroup size.
   * @return {string} The vertex shader.
   */
  _getWGSLComputeCode(e, t) {
    const [s, r, n] = t;
    return `${this.getSignature()}
// directives
${e.directives}

// system
var<private> instanceIndex : u32;

// locals
${e.scopedArrays}

// structs
${e.structs}

// uniforms
${e.uniforms}

// vars
${this.allowGlobalVariables ? e.vars : ""}

// codes
${e.codes}

@compute @workgroup_size( ${s}, ${r}, ${n} )
fn main( ${e.attributes} ) {

	// local vars
	${this.allowGlobalVariables ? "" : e.vars}

	// system
	instanceIndex = globalId.x
		+ globalId.y * ( ${s} * numWorkgroups.x )
		+ globalId.z * ( ${s} * numWorkgroups.x ) * ( ${r} * numWorkgroups.y );

	// flow
	${e.flow}

}
`;
  }
  /**
   * Returns a WGSL struct based on the given name and variables.
   *
   * @private
   * @param {string} name - The struct name.
   * @param {string} vars - The struct variables.
   * @return {string} The WGSL snippet representing a struct.
   */
  _getWGSLStruct(e, t) {
    return `
struct ${e} {
${t}
};`;
  }
  /**
   * Returns a WGSL struct binding.
   *
   * @private
   * @param {string} name - The struct name.
   * @param {string} vars - The struct variables.
   * @param {string} access - The access.
   * @param {number} [binding=0] - The binding index.
   * @param {number} [group=0] - The group index.
   * @return {string} The WGSL snippet representing a struct binding.
   */
  _getWGSLStructBinding(e, t, s, r = 0, n = 0) {
    const i = e + "Struct";
    return `${this._getWGSLStruct(i, t)}
@binding( ${r} ) @group( ${n} )
var<${s}> ${e} : ${i};`;
  }
}
class pN {
  /**
   * Constructs a new utility object.
   *
   * @param {WebGPUBackend} backend - The WebGPU backend.
   */
  constructor(e) {
    this.backend = e;
  }
  /**
   * Returns the depth/stencil GPU format for the given render context.
   *
   * @param {RenderContext} renderContext - The render context.
   * @return {string} The depth/stencil GPU texture format.
   */
  getCurrentDepthStencilFormat(e) {
    let t;
    return e.depth && (e.depthTexture !== null ? t = this.getTextureFormatGPU(e.depthTexture) : e.stencil ? this.backend.renderer.reversedDepthBuffer === !0 ? t = x.Depth32FloatStencil8 : t = x.Depth24PlusStencil8 : this.backend.renderer.reversedDepthBuffer === !0 ? t = x.Depth32Float : t = x.Depth24Plus), t;
  }
  /**
   * Returns the GPU format for the given texture.
   *
   * @param {Texture} texture - The texture.
   * @return {string} The GPU texture format.
   */
  getTextureFormatGPU(e) {
    return this.backend.get(e).format;
  }
  /**
   * Returns an object that defines the multi-sampling state of the given texture.
   *
   * @param {Texture} texture - The texture.
   * @return {Object} The multi-sampling state.
   */
  getTextureSampleData(e) {
    let t;
    if (e.isFramebufferTexture)
      t = 1;
    else if (e.isDepthTexture && !e.renderTarget) {
      const n = this.backend.renderer, i = n.getRenderTarget();
      t = i ? i.samples : n.currentSamples;
    } else e.renderTarget && (t = e.renderTarget.samples);
    t = t || 1;
    const s = t > 1 && e.renderTarget !== null && e.isDepthTexture !== !0 && e.isFramebufferTexture !== !0;
    return { samples: t, primarySamples: s ? 1 : t, isMSAA: s };
  }
  /**
   * Returns the default color attachment's GPU format of the current render context.
   *
   * @param {RenderContext} renderContext - The render context.
   * @return {string} The GPU texture format of the default color attachment.
   */
  getCurrentColorFormat(e) {
    let t;
    return e.textures !== null ? t = this.getTextureFormatGPU(e.textures[0]) : t = this.getPreferredCanvasFormat(), t;
  }
  /**
   * Returns the GPU formats of all color attachments of the current render context.
   *
   * @param {RenderContext} renderContext - The render context.
   * @return {Array<string>} The GPU texture formats of all color attachments.
   */
  getCurrentColorFormats(e) {
    return e.textures !== null ? e.textures.map((t) => this.getTextureFormatGPU(t)) : [this.getPreferredCanvasFormat()];
  }
  /**
   * Returns the output color space of the current render context.
   *
   * @param {RenderContext} renderContext - The render context.
   * @return {string} The output color space.
   */
  getCurrentColorSpace(e) {
    return e.textures !== null ? e.textures[0].colorSpace : this.backend.renderer.outputColorSpace;
  }
  /**
   * Returns GPU primitive topology for the given object and material.
   *
   * @param {Object3D} object - The 3D object.
   * @param {Material} material - The material.
   * @return {string} The GPU primitive topology.
   */
  getPrimitiveTopology(e, t) {
    if (e.isPoints) return Nn.PointList;
    if (e.isLineSegments || e.isMesh && t.wireframe === !0) return Nn.LineList;
    if (e.isLine) return Nn.LineStrip;
    if (e.isMesh) return Nn.TriangleList;
  }
  /**
   * Returns a modified sample count from the given sample count value.
   *
   * That is required since WebGPU only supports either 1 or 4.
   *
   * @param {number} sampleCount - The input sample count.
   * @return {number} The (potentially updated) output sample count.
   */
  getSampleCount(e) {
    return e >= 4 ? 4 : 1;
  }
  /**
   * Returns the sample count of the given render context.
   *
   * @param {RenderContext} renderContext - The render context.
   * @return {number} The sample count.
   */
  getSampleCountRenderContext(e) {
    return e.textures !== null ? this.getSampleCount(e.sampleCount) : this.getSampleCount(this.backend.renderer.currentSamples);
  }
  /**
   * Returns the preferred canvas format.
   *
   * There is a separate method for this so it's possible to
   * honor edge cases for specific devices.
   *
   * @return {string} The GPU texture format of the canvas.
   */
  getPreferredCanvasFormat() {
    const t = this.backend.parameters.outputType;
    if (t === void 0)
      return navigator.gpu.getPreferredCanvasFormat();
    if (t === $e)
      return x.BGRA8Unorm;
    if (t === Oe)
      return x.RGBA16Float;
    throw new Error("Unsupported output buffer type.");
  }
}
const _h = /* @__PURE__ */ new Map([
  [Int8Array, ["sint8", "snorm8"]],
  [Uint8Array, ["uint8", "unorm8"]],
  [Int16Array, ["sint16", "snorm16"]],
  [Uint16Array, ["uint16", "unorm16"]],
  [Int32Array, ["sint32", "snorm32"]],
  [Uint32Array, ["uint32", "unorm32"]],
  [Float32Array, ["float32"]]
]);
typeof Float16Array < "u" && _h.set(Float16Array, ["float16"]);
const gN = /* @__PURE__ */ new Map([
  [gc, ["float16"]]
]), mN = /* @__PURE__ */ new Map([
  [Int32Array, "sint32"],
  [Int16Array, "sint32"],
  // patch for INT16
  [Uint32Array, "uint32"],
  [Uint16Array, "uint32"],
  // patch for UINT16
  [Float32Array, "float32"]
]);
class xN {
  /**
   * Constructs a new utility object.
   *
   * @param {WebGPUBackend} backend - The WebGPU backend.
   */
  constructor(e) {
    this.backend = e;
  }
  /**
   * Creates the GPU buffer for the given buffer attribute.
   *
   * @param {BufferAttribute} attribute - The buffer attribute.
   * @param {GPUBufferUsage} usage - A flag that indicates how the buffer may be used after its creation.
   */
  createAttribute(e, t) {
    const s = this._getBufferAttribute(e), r = this.backend, n = r.get(s);
    let i = n.buffer;
    if (i === void 0) {
      const o = r.device;
      let u = s.array;
      if (e.normalized === !1) {
        if (u.constructor === Int16Array || u.constructor === Int8Array)
          u = new Int32Array(u);
        else if ((u.constructor === Uint16Array || u.constructor === Uint8Array) && (u = new Uint32Array(u), t & GPUBufferUsage.INDEX))
          for (let d = 0; d < u.length; d++)
            u[d] === 65535 && (u[d] = 4294967295);
      }
      if (s.array = u, (s.isStorageBufferAttribute || s.isStorageInstancedBufferAttribute) && s.itemSize === 3) {
        u = new u.constructor(s.count * 4);
        for (let d = 0; d < s.count; d++)
          u.set(s.array.subarray(d * 3, d * 3 + 3), d * 4);
        s.itemSize = 4, s.array = u, n._force3to4BytesAlignment = !0;
      }
      const l = u.byteLength, c = l + (4 - l % 4) % 4;
      i = o.createBuffer({
        label: s.name,
        size: c,
        usage: t,
        mappedAtCreation: !0
      }), new u.constructor(i.getMappedRange()).set(u), i.unmap(), n.buffer = i;
    }
  }
  /**
   * Updates the GPU buffer of the given buffer attribute.
   *
   * @param {BufferAttribute} attribute - The buffer attribute.
   */
  updateAttribute(e) {
    const t = this._getBufferAttribute(e), s = this.backend, r = s.device, n = s.get(t), i = s.get(t).buffer;
    let o = t.array;
    if (n._force3to4BytesAlignment === !0) {
      o = new o.constructor(t.count * 4);
      for (let l = 0; l < t.count; l++)
        o.set(t.array.subarray(l * 3, l * 3 + 3), l * 4);
      t.array = o;
    }
    const u = t.updateRanges;
    if (u.length === 0)
      r.queue.writeBuffer(
        i,
        0,
        o,
        0
      );
    else {
      const l = In(o), c = l ? 1 : o.BYTES_PER_ELEMENT;
      for (let d = 0, h = u.length; d < h; d++) {
        const f = u[d];
        let p, g;
        if (n._force3to4BytesAlignment === !0) {
          const y = Math.floor(f.start / 3), _ = Math.ceil(f.count / 3);
          p = y * 4 * c, g = _ * 4 * c;
        } else
          p = f.start * c, g = f.count * c;
        const m = p * (l ? o.BYTES_PER_ELEMENT : 1);
        r.queue.writeBuffer(
          i,
          m,
          o,
          p,
          g
        );
      }
      t.clearUpdateRanges();
    }
  }
  /**
   * This method creates the vertex buffer layout data which are
   * require when creating a render pipeline for the given render object.
   *
   * @param {RenderObject} renderObject - The render object.
   * @return {Array<Object>} An array holding objects which describe the vertex buffer layout.
   */
  createShaderVertexBuffers(e) {
    const t = e.getAttributes(), s = /* @__PURE__ */ new Map();
    for (let r = 0; r < t.length; r++) {
      const n = t[r], i = n.array.BYTES_PER_ELEMENT, o = this._getBufferAttribute(n);
      let u = s.get(o);
      if (u === void 0) {
        let d, h;
        n.isInterleavedBufferAttribute === !0 ? (d = n.data.stride * i, h = n.data.isInstancedInterleavedBuffer ? Rn.Instance : Rn.Vertex) : (d = n.itemSize * i, h = n.isInstancedBufferAttribute ? Rn.Instance : Rn.Vertex), n.normalized === !1 && (n.array.constructor === Int16Array || n.array.constructor === Uint16Array) && (d = 4), u = {
          arrayStride: d,
          attributes: [],
          stepMode: h
        }, s.set(o, u);
      }
      const l = this._getVertexFormat(n), c = n.isInterleavedBufferAttribute === !0 ? n.offset * i : 0;
      u.attributes.push({
        shaderLocation: r,
        offset: c,
        format: l
      });
    }
    return Array.from(s.values());
  }
  /**
   * Destroys the GPU buffer of the given buffer attribute.
   *
   * @param {BufferAttribute} attribute - The buffer attribute.
   */
  destroyAttribute(e) {
    const t = this.backend;
    t.get(this._getBufferAttribute(e)).buffer.destroy(), t.delete(e);
  }
  /**
   * This method performs a readback operation by moving buffer data from
   * a storage buffer attribute from the GPU to the CPU. ReadbackBuffer can
   * be used to retain and reuse handles to the intermediate buffers and prevent
   * new allocation.
   *
   * @async
   * @param {BufferAttribute} attribute - The storage buffer attribute to read frm.
   * @param {number} count - The offset from which to start reading the
   * @param {number} offset - The storage buffer attribute.
   * @param {ReadbackBuffer|ArrayBuffer} target - The storage buffer attribute.
   * @return {Promise<ArrayBuffer|ReadbackBuffer>} A promise that resolves with the buffer data when the data are ready.
   */
  async getArrayBufferAsync(e, t = null, s = 0, r = -1) {
    const n = this.backend, i = n.device, u = n.get(this._getBufferAttribute(e)).buffer, l = r === -1 ? u.size - s : r;
    let c;
    if (t !== null && t.isReadbackBuffer) {
      const f = n.get(t);
      if (t._mapped === !0)
        throw new Error("WebGPURenderer: ReadbackBuffer must be released before being used again.");
      if (t._mapped = !0, f.readBufferGPU === void 0) {
        c = i.createBuffer({
          label: `${t.name}_readback`,
          size: t.maxByteLength,
          usage: GPUBufferUsage.COPY_DST | GPUBufferUsage.MAP_READ
        });
        const p = () => {
          t.buffer = null, t._mapped = !1, c.unmap();
        }, g = () => {
          t.buffer = null, t._mapped = !1, c.destroy(), n.delete(t), t.removeEventListener("release", p), t.removeEventListener("dispose", g);
        };
        t.addEventListener("release", p), t.addEventListener("dispose", g), f.readBufferGPU = c;
      } else
        c = f.readBufferGPU;
    } else
      c = i.createBuffer({
        label: `${e.name}_readback`,
        size: l,
        usage: GPUBufferUsage.COPY_DST | GPUBufferUsage.MAP_READ
      });
    const d = i.createCommandEncoder({
      label: `readback_encoder_${e.name}`
    });
    d.copyBufferToBuffer(
      u,
      s,
      c,
      0,
      l
    );
    const h = d.finish();
    if (i.queue.submit([h]), await c.mapAsync(GPUMapMode.READ, 0, l), t === null) {
      const p = c.getMappedRange(0, l).slice();
      return c.destroy(), p;
    } else {
      if (t.isReadbackBuffer)
        return t.buffer = c.getMappedRange(0, l), t;
      {
        const f = c.getMappedRange(0, l);
        return new Uint8Array(t).set(new Uint8Array(f)), c.destroy(), t;
      }
    }
  }
  /**
   * Returns the vertex format of the given buffer attribute.
   *
   * @private
   * @param {BufferAttribute} geometryAttribute - The buffer attribute.
   * @return {string|undefined} The vertex format (e.g. 'float32x3').
   */
  _getVertexFormat(e) {
    const { itemSize: t, normalized: s } = e, r = e.array.constructor, n = e.constructor;
    let i;
    if (t === 1)
      i = mN.get(r);
    else {
      const u = (gN.get(n) || _h.get(r))[s ? 1 : 0];
      if (u) {
        const l = r.BYTES_PER_ELEMENT * t, d = Math.floor((l + 3) / 4) * 4 / r.BYTES_PER_ELEMENT;
        if (d % 1)
          throw new Error("THREE.WebGPUAttributeUtils: Bad vertex format item size.");
        i = `${u}x${d}`;
      }
    }
    return i || F("WebGPUAttributeUtils: Vertex format not supported yet."), i;
  }
  /**
   * Utility method for handling interleaved buffer attributes correctly.
   * To process them, their `InterleavedBuffer` is returned.
   *
   * @private
   * @param {BufferAttribute} attribute - The attribute.
   * @return {BufferAttribute|InterleavedBuffer}
   */
  _getBufferAttribute(e) {
    return e.isInterleavedBufferAttribute && (e = e.data), e;
  }
}
class TN {
  /**
   * Constructs a new layout.
   *
   * @param {GPUBindGroupLayout} layoutGPU - A GPU Bind Group Layout.
   */
  constructor(e) {
    this.layoutGPU = e, this.usedTimes = 0;
  }
}
class yN {
  /**
   * Constructs a new utility object.
   *
   * @param {WebGPUBackend} backend - The WebGPU backend.
   */
  constructor(e) {
    this.backend = e, this._bindGroupLayoutCache = /* @__PURE__ */ new Map();
  }
  /**
   * Creates a GPU bind group layout for the given bind group.
   *
   * @param {BindGroup} bindGroup - The bind group.
   * @return {GPUBindGroupLayout} The GPU bind group layout.
   */
  createBindingsLayout(e) {
    const t = this.backend, s = t.device, r = t.get(e);
    if (r.layout)
      return r.layout.layoutGPU;
    const n = this._createLayoutEntries(e), i = lr(JSON.stringify(n));
    let o = this._bindGroupLayoutCache.get(i);
    return o === void 0 && (o = new TN(s.createBindGroupLayout({ entries: n })), this._bindGroupLayoutCache.set(i, o)), o.usedTimes++, r.layout = o, r.layoutKey = i, o.layoutGPU;
  }
  /**
   * Creates bindings from the given bind group definition.
   *
   * @param {BindGroup} bindGroup - The bind group.
   * @param {Array<BindGroup>} bindings - Array of bind groups.
   * @param {number} cacheIndex - The cache index.
   * @param {number} version - The version.
   */
  createBindings(e, t, s, r = 0) {
    const { backend: n } = this, i = n.get(e), o = this.createBindingsLayout(e);
    let u;
    s > 0 && (i.groups === void 0 && (i.groups = [], i.versions = []), i.versions[s] === r && (u = i.groups[s])), u === void 0 && (u = this.createBindGroup(e, o), s > 0 && (i.groups[s] = u, i.versions[s] = r)), i.group = u;
  }
  /**
   * Updates a buffer binding.
   *
   *  @param {Buffer} binding - The buffer binding to update.
   */
  updateBinding(e) {
    const t = this.backend, s = t.device, r = e.buffer, n = t.get(e).buffer, i = e.updateRanges;
    if (i.length === 0)
      s.queue.writeBuffer(
        n,
        0,
        r,
        0
      );
    else {
      const o = In(r), u = o ? 1 : r.BYTES_PER_ELEMENT;
      for (let l = 0, c = i.length; l < c; l++) {
        const d = i[l], h = d.start * u, f = d.count * u, p = h * (o ? r.BYTES_PER_ELEMENT : 1);
        s.queue.writeBuffer(
          n,
          p,
          r,
          h,
          f
        );
      }
    }
  }
  /**
   * Creates a GPU bind group for the camera index.
   *
   * @param {Uint32Array} data - The index data.
   * @param {GPUBindGroupLayout} layoutGPU - The GPU bind group layout.
   * @return {GPUBindGroup} The GPU bind group.
   */
  createBindGroupIndex(e, t) {
    const r = this.backend.device, n = GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST, i = e[0], o = r.createBuffer({
      label: "bindingCameraIndex_" + i,
      size: 16,
      // uint(4) * 4
      usage: n
    });
    r.queue.writeBuffer(o, 0, e, 0);
    const u = [{ binding: 0, resource: { buffer: o } }];
    return r.createBindGroup({
      label: "bindGroupCameraIndex_" + i,
      layout: t,
      entries: u
    });
  }
  /**
   * Creates a GPU bind group for the given bind group and GPU layout.
   *
   * @param {BindGroup} bindGroup - The bind group.
   * @param {GPUBindGroupLayout} layoutGPU - The GPU bind group layout.
   * @return {GPUBindGroup} The GPU bind group.
   */
  createBindGroup(e, t) {
    const s = this.backend, r = s.device;
    let n = 0;
    const i = [];
    for (const o of e.bindings) {
      if (o.isUniformBuffer) {
        const u = s.get(o);
        if (u.buffer === void 0) {
          const l = o.byteLength, c = GPUBufferUsage.UNIFORM | GPUBufferUsage.COPY_DST, d = [];
          o.visibility & At.VERTEX && d.push("vertex"), o.visibility & At.FRAGMENT && d.push("fragment"), o.visibility & At.COMPUTE && d.push("compute");
          const h = `(${d.join(",")})`, f = r.createBuffer({
            label: `bindingBuffer${o.id}_${o.name}_${h}`,
            size: l,
            usage: c
          });
          u.buffer = f;
        }
        i.push({ binding: n, resource: { buffer: u.buffer } });
      } else if (o.isStorageBuffer) {
        const u = s.get(o.attribute).buffer;
        i.push({ binding: n, resource: { buffer: u } });
      } else if (o.isSampledTexture) {
        const u = s.get(o.texture);
        let l;
        if (u.externalTexture !== void 0)
          l = r.importExternalTexture({ source: u.externalTexture });
        else {
          const c = o.store ? 1 : u.texture.mipLevelCount, d = o.store ? o.mipLevel : 0;
          let h = `view-${u.texture.width}-${u.texture.height}`;
          if (u.texture.depthOrArrayLayers > 1 && (h += `-${u.texture.depthOrArrayLayers}`), h += `-${c}-${d}`, l = u[h], l === void 0) {
            const f = QS.All;
            let p;
            o.isSampledCubeTexture ? p = Fe.Cube : o.isSampledTexture3D ? p = Fe.ThreeD : o.texture.isArrayTexture || o.texture.isDataArrayTexture || o.texture.isCompressedArrayTexture ? p = Fe.TwoDArray : p = Fe.TwoD, l = u[h] = u.texture.createView({ aspect: f, dimension: p, mipLevelCount: c, baseMipLevel: d });
          }
        }
        i.push({ binding: n, resource: l });
      } else if (o.isSampler) {
        const u = s.get(o.texture);
        i.push({ binding: n, resource: u.sampler });
      }
      n++;
    }
    return r.createBindGroup({
      label: "bindGroup_" + e.name,
      layout: t,
      entries: i
    });
  }
  /**
   * Creates a GPU bind group layout entries for the given bind group.
   *
   * @private
   * @param {BindGroup} bindGroup - The bind group.
   * @return {Array<GPUBindGroupLayoutEntry>} The GPU bind group layout entries.
   */
  _createLayoutEntries(e) {
    const t = [];
    let s = 0;
    for (const r of e.bindings) {
      const n = this.backend, i = {
        binding: s,
        visibility: r.visibility
      };
      if (r.isUniformBuffer || r.isStorageBuffer) {
        const o = {};
        r.isStorageBuffer && (r.visibility & At.COMPUTE && (r.access === nt.READ_WRITE || r.access === nt.WRITE_ONLY) ? o.type = lo.Storage : o.type = lo.ReadOnlyStorage), i.buffer = o;
      } else if (r.isSampledTexture && r.store) {
        const o = {};
        o.format = this.backend.get(r.texture).texture.format;
        const u = r.access;
        u === nt.READ_WRITE ? o.access = co.ReadWrite : u === nt.WRITE_ONLY ? o.access = co.WriteOnly : o.access = co.ReadOnly, r.texture.isArrayTexture ? o.viewDimension = Fe.TwoDArray : r.texture.is3DTexture && (o.viewDimension = Fe.ThreeD), i.storageTexture = o;
      } else if (r.isSampledTexture) {
        const o = {}, { primarySamples: u } = n.utils.getTextureSampleData(r.texture);
        if (u > 1 && (o.multisampled = !0, r.texture.isDepthTexture || (o.sampleType = fs.UnfilterableFloat)), r.texture.isDepthTexture)
          n.compatibilityMode && r.texture.compareFunction === null ? o.sampleType = fs.UnfilterableFloat : o.sampleType = fs.Depth;
        else if (r.texture.isDataTexture || r.texture.isDataArrayTexture || r.texture.isData3DTexture || r.texture.isStorageTexture) {
          const l = r.texture.type;
          l === Ue ? o.sampleType = fs.SInt : l === _e ? o.sampleType = fs.UInt : l === De && (this.backend.hasFeature("float32-filterable") ? o.sampleType = fs.Float : o.sampleType = fs.UnfilterableFloat);
        }
        r.isSampledCubeTexture ? o.viewDimension = Fe.Cube : r.texture.isArrayTexture || r.texture.isDataArrayTexture || r.texture.isCompressedArrayTexture ? o.viewDimension = Fe.TwoDArray : r.isSampledTexture3D && (o.viewDimension = Fe.ThreeD), i.texture = o;
      } else if (r.isSampler) {
        const o = {};
        r.texture.isDepthTexture && (r.texture.compareFunction !== null && n.hasCompatibility(Ps.TEXTURE_COMPARE) ? o.type = Rl.Comparison : o.type = Rl.NonFiltering), i.sampler = o;
      } else
        F(`WebGPUBindingUtils: Unsupported binding "${r}".`);
      t.push(i), s++;
    }
    return t;
  }
  /**
   * Delete the data associated with a bind group.
   *
   * @param {BindGroup} bindGroup - The bind group.
   */
  deleteBindGroupData(e) {
    const { backend: t } = this, s = t.get(e);
    s.layout && (s.layout.usedTimes--, s.layout.usedTimes === 0 && this._bindGroupLayoutCache.delete(s.layoutKey), s.layout = void 0, s.layoutKey = void 0);
  }
  /**
   * Frees internal resources.
   */
  dispose() {
    this._bindGroupLayoutCache.clear();
  }
}
class _N {
  /**
   * Constructs a new utility object.
   *
   * @param {WebGPUBackend} backend - The WebGPU backend.
   */
  constructor(e) {
    this.backend = e;
  }
  /**
   * Returns the maximum anisotropy texture filtering value.
   *
   * @return {number} The maximum anisotropy texture filtering value.
   */
  getMaxAnisotropy() {
    return 16;
  }
  /**
   * Returns the maximum number of bytes available for uniform buffers.
   *
   * @return {number} The maximum number of bytes available for uniform buffers.
   */
  getUniformBufferLimit() {
    return this.backend.device.limits.maxUniformBufferBindingSize;
  }
}
class bN {
  /**
   * Constructs a new utility object.
   *
   * @param {WebGPUBackend} backend - The WebGPU backend.
   */
  constructor(e) {
    this.backend = e, this._activePipelines = /* @__PURE__ */ new WeakMap();
  }
  /**
   * Sets the given pipeline for the given pass. The method makes sure to only set the
   * pipeline when necessary.
   *
   * @param {(GPURenderPassEncoder|GPUComputePassEncoder)} pass - The pass encoder.
   * @param {(GPURenderPipeline|GPUComputePipeline)} pipeline - The pipeline.
   */
  setPipeline(e, t) {
    this._activePipelines.get(e) !== t && (e.setPipeline(t), this._activePipelines.set(e, t));
  }
  /**
   * Returns the sample count derived from the given render context.
   *
   * @private
   * @param {RenderContext} renderContext - The render context.
   * @return {number} The sample count.
   */
  _getSampleCount(e) {
    return this.backend.utils.getSampleCountRenderContext(e);
  }
  /**
   * Creates a render pipeline for the given render object.
   *
   * @param {RenderObject} renderObject - The render object.
   * @param {Array<Promise>} promises - An array of compilation promises which are used in `compileAsync()`.
   */
  createRenderPipeline(e, t) {
    const { object: s, material: r, geometry: n, pipeline: i } = e, { vertexProgram: o, fragmentProgram: u } = i, l = this.backend, c = l.device, d = l.utils, h = l.get(i), f = [];
    for (const se of e.getBindings()) {
      const oe = l.get(se), { layoutGPU: K } = oe.layout;
      f.push(K);
    }
    const p = l.attributeUtils.createShaderVertexBuffers(e);
    let g;
    r.blending !== ss && (r.blending !== Lt || r.transparent !== !1) && (g = this._getBlending(r));
    let m = {};
    r.stencilWrite === !0 && (m = {
      compare: this._getStencilCompare(r),
      failOp: this._getStencilOperation(r.stencilFail),
      depthFailOp: this._getStencilOperation(r.stencilZFail),
      passOp: this._getStencilOperation(r.stencilZPass)
    });
    const y = this._getColorWriteMask(r), _ = [];
    if (e.context.textures !== null) {
      const se = e.context.textures, oe = e.context.mrt;
      for (let K = 0; K < se.length; K++) {
        const ae = se[K], Le = d.getTextureFormatGPU(ae);
        let Ce;
        if (oe !== null)
          if (this.backend.compatibilityMode !== !0) {
            const ht = oe.getBlendMode(ae.name);
            ht.blending === nc ? Ce = g : ht.blending !== ss && (Ce = this._getBlending(ht));
          } else
            Te("WebGPURenderer: Multiple Render Targets (MRT) blending configuration is not fully supported in compatibility mode. The material blending will be used for all render targets."), Ce = g;
        else
          Ce = g;
        _.push({
          format: Le,
          blend: Ce,
          writeMask: y
        });
      }
    } else {
      const se = d.getCurrentColorFormat(e.context);
      _.push({
        format: se,
        blend: g,
        writeMask: y
      });
    }
    const v = l.get(o).module, C = l.get(u).module, w = this._getPrimitiveState(s, n, r), M = this._getDepthCompare(r), E = d.getCurrentDepthStencilFormat(e.context), U = this._getSampleCount(e.context), P = {
      label: `renderPipeline_${r.name || r.type}_${r.id}`,
      vertex: Object.assign({}, v, { buffers: p }),
      fragment: Object.assign({}, C, { targets: _ }),
      primitive: w,
      multisample: {
        count: U,
        alphaToCoverageEnabled: r.alphaToCoverage && U > 1
      },
      layout: c.createPipelineLayout({
        bindGroupLayouts: f
      })
    }, $ = {}, Y = e.context.depth, ie = e.context.stencil;
    if ((Y === !0 || ie === !0) && (Y === !0 && ($.format = E, $.depthWriteEnabled = r.depthWrite, $.depthCompare = M), ie === !0 && ($.stencilFront = m, $.stencilBack = m, $.stencilReadMask = r.stencilFuncMask, $.stencilWriteMask = r.stencilWriteMask), r.polygonOffset === !0 && ($.depthBias = r.polygonOffsetUnits, $.depthBiasSlopeScale = r.polygonOffsetFactor, $.depthBiasClamp = 0), P.depthStencil = $), c.pushErrorScope("validation"), t === null)
      h.pipeline = c.createRenderPipeline(P), c.popErrorScope().then((se) => {
        se !== null && (h.error = !0, F(se.message));
      });
    else {
      const se = new Promise(async (oe) => {
        try {
          h.pipeline = await c.createRenderPipelineAsync(P);
        } catch {
        }
        const K = await c.popErrorScope();
        K !== null && (h.error = !0, F(K.message)), oe();
      });
      t.push(se);
    }
  }
  /**
   * Creates GPU render bundle encoder for the given render context.
   *
   * @param {RenderContext} renderContext - The render context.
   * @param {?string} [label='renderBundleEncoder'] - The label.
   * @return {GPURenderBundleEncoder} The GPU render bundle encoder.
   */
  createBundleEncoder(e, t = "renderBundleEncoder") {
    const s = this.backend, { utils: r, device: n } = s, i = r.getCurrentDepthStencilFormat(e), o = r.getCurrentColorFormats(e), u = this._getSampleCount(e), l = {
      label: t,
      colorFormats: o,
      depthStencilFormat: i,
      sampleCount: u
    };
    return n.createRenderBundleEncoder(l);
  }
  /**
   * Creates a compute pipeline for the given compute node.
   *
   * @param {ComputePipeline} pipeline - The compute pipeline.
   * @param {Array<BindGroup>} bindings - The bindings.
   */
  createComputePipeline(e, t) {
    const s = this.backend, r = s.device, n = s.get(e.computeProgram).module, i = s.get(e), o = [];
    for (const u of t) {
      const l = s.get(u), { layoutGPU: c } = l.layout;
      o.push(c);
    }
    i.pipeline = r.createComputePipeline({
      compute: n,
      layout: r.createPipelineLayout({
        bindGroupLayouts: o
      })
    });
  }
  /**
   * Returns the blending state as a descriptor object required
   * for the pipeline creation.
   *
   * @private
   * @param {Material|BlendMode} object - The object containing blending information.
   * @return {Object} The blending state.
   */
  _getBlending(e) {
    let t, s;
    const r = e.blending, n = e.blendSrc, i = e.blendDst, o = e.blendEquation;
    if (r === Zs) {
      const u = e.blendSrcAlpha !== null ? e.blendSrcAlpha : n, l = e.blendDstAlpha !== null ? e.blendDstAlpha : i, c = e.blendEquationAlpha !== null ? e.blendEquationAlpha : o;
      t = {
        srcFactor: this._getBlendFactor(n),
        dstFactor: this._getBlendFactor(i),
        operation: this._getBlendOperation(o)
      }, s = {
        srcFactor: this._getBlendFactor(u),
        dstFactor: this._getBlendFactor(l),
        operation: this._getBlendOperation(c)
      };
    } else {
      const u = e.premultipliedAlpha, l = (c, d, h, f) => {
        t = {
          srcFactor: c,
          dstFactor: d,
          operation: hs.Add
        }, s = {
          srcFactor: h,
          dstFactor: f,
          operation: hs.Add
        };
      };
      if (u)
        switch (r) {
          case Lt:
            l(j.One, j.OneMinusSrcAlpha, j.One, j.OneMinusSrcAlpha);
            break;
          case Xs:
            l(j.One, j.One, j.One, j.One);
            break;
          case qs:
            l(j.Zero, j.OneMinusSrc, j.Zero, j.One);
            break;
          case Hs:
            l(j.Dst, j.OneMinusSrcAlpha, j.Zero, j.One);
            break;
        }
      else
        switch (r) {
          case Lt:
            l(j.SrcAlpha, j.OneMinusSrcAlpha, j.One, j.OneMinusSrcAlpha);
            break;
          case Xs:
            l(j.SrcAlpha, j.One, j.One, j.One);
            break;
          case qs:
            F(`WebGPURenderer: "SubtractiveBlending" requires "${e.isMaterial ? "material" : "blendMode"}.premultipliedAlpha = true".`);
            break;
          case Hs:
            F(`WebGPURenderer: "MultiplyBlending" requires "${e.isMaterial ? "material" : "blendMode"}.premultipliedAlpha = true".`);
            break;
        }
    }
    if (t !== void 0 && s !== void 0)
      return { color: t, alpha: s };
    F("WebGPURenderer: Invalid blending: ", r);
  }
  /**
   * Returns the GPU blend factor which is required for the pipeline creation.
   *
   * @private
   * @param {number} blend - The blend factor as a three.js constant.
   * @return {string} The GPU blend factor.
   */
  _getBlendFactor(e) {
    let t;
    switch (e) {
      case jt:
        t = j.Zero;
        break;
      case rc:
        t = j.One;
        break;
      case sc:
        t = j.Src;
        break;
      case jl:
        t = j.OneMinusSrc;
        break;
      case tc:
        t = j.SrcAlpha;
        break;
      case Yl:
        t = j.OneMinusSrcAlpha;
        break;
      case Jl:
        t = j.Dst;
        break;
      case Ql:
        t = j.OneMinusDst;
        break;
      case Zl:
        t = j.DstAlpha;
        break;
      case Kl:
        t = j.OneMinusDstAlpha;
        break;
      case ec:
        t = j.SrcAlphaSaturated;
        break;
      case s_:
        t = j.Constant;
        break;
      case r_:
        t = j.OneMinusConstant;
        break;
      default:
        F("WebGPURenderer: Blend factor not supported.", e);
    }
    return t;
  }
  /**
   * Returns the GPU stencil compare function which is required for the pipeline creation.
   *
   * @private
   * @param {Material} material - The material.
   * @return {string} The GPU stencil compare function.
   */
  _getStencilCompare(e) {
    let t;
    const s = e.stencilFunc;
    switch (s) {
      case xf:
        t = we.Never;
        break;
      case mf:
        t = we.Always;
        break;
      case gf:
        t = we.Less;
        break;
      case pf:
        t = we.LessEqual;
        break;
      case ff:
        t = we.Equal;
        break;
      case hf:
        t = we.GreaterEqual;
        break;
      case df:
        t = we.Greater;
        break;
      case cf:
        t = we.NotEqual;
        break;
      default:
        F("WebGPURenderer: Invalid stencil function.", s);
    }
    return t;
  }
  /**
   * Returns the GPU stencil operation which is required for the pipeline creation.
   *
   * @private
   * @param {number} op - A three.js constant defining the stencil operation.
   * @return {string} The GPU stencil operation.
   */
  _getStencilOperation(e) {
    let t;
    switch (e) {
      case Af:
        t = Yt.Keep;
        break;
      case Rf:
        t = Yt.Zero;
        break;
      case Nf:
        t = Yt.Replace;
        break;
      case Sf:
        t = Yt.Invert;
        break;
      case bf:
        t = Yt.IncrementClamp;
        break;
      case _f:
        t = Yt.DecrementClamp;
        break;
      case yf:
        t = Yt.IncrementWrap;
        break;
      case Tf:
        t = Yt.DecrementWrap;
        break;
      default:
        F("WebGPURenderer: Invalid stencil operation.", t);
    }
    return t;
  }
  /**
   * Returns the GPU blend operation which is required for the pipeline creation.
   *
   * @private
   * @param {number} blendEquation - A three.js constant defining the blend equation.
   * @return {string} The GPU blend operation.
   */
  _getBlendOperation(e) {
    let t;
    switch (e) {
      case Pt:
        t = hs.Add;
        break;
      case Xl:
        t = hs.Subtract;
        break;
      case ql:
        t = hs.ReverseSubtract;
        break;
      case Ef:
        t = hs.Min;
        break;
      case vf:
        t = hs.Max;
        break;
      default:
        F("WebGPUPipelineUtils: Blend equation not supported.", e);
    }
    return t;
  }
  /**
   * Returns the primitive state as a descriptor object required
   * for the pipeline creation.
   *
   * @private
   * @param {Object3D} object - The 3D object.
   * @param {BufferGeometry} geometry - The geometry.
   * @param {Material} material - The material.
   * @return {Object} The primitive state.
   */
  _getPrimitiveState(e, t, s) {
    const r = {}, n = this.backend.utils;
    r.topology = n.getPrimitiveTopology(e, s), t.index !== null && e.isLine === !0 && e.isLineSegments !== !0 && (r.stripIndexFormat = t.index.array instanceof Uint16Array ? Zn.Uint16 : Zn.Uint32);
    let i = s.side === Ge;
    return e.isMesh && e.matrixWorld.determinant() < 0 && (i = !i), r.frontFace = i === !0 ? bl.CW : bl.CCW, r.cullMode = s.side === Ot ? Sl.None : Sl.Back, r;
  }
  /**
   * Returns the GPU color write mask which is required for the pipeline creation.
   *
   * @private
   * @param {Material} material - The material.
   * @return {number} The GPU color write mask.
   */
  _getColorWriteMask(e) {
    return e.colorWrite === !0 ? Nl.All : Nl.None;
  }
  /**
   * Returns the GPU depth compare function which is required for the pipeline creation.
   *
   * @private
   * @param {Material} material - The material.
   * @return {string} The GPU depth compare function.
   */
  _getDepthCompare(e) {
    let t;
    if (e.depthTest === !1)
      t = we.Always;
    else {
      const s = this.backend.parameters.reversedDepthBuffer ? pc[e.depthFunc] : e.depthFunc;
      switch (s) {
        case hc:
          t = we.Never;
          break;
        case dc:
          t = we.Always;
          break;
        case cc:
          t = we.Less;
          break;
        case lc:
          t = we.LessEqual;
          break;
        case uc:
          t = we.Equal;
          break;
        case ac:
          t = we.GreaterEqual;
          break;
        case oc:
          t = we.Greater;
          break;
        case ic:
          t = we.NotEqual;
          break;
        default:
          F("WebGPUPipelineUtils: Invalid depth function.", s);
      }
    }
    return t;
  }
}
class SN extends Th {
  /**
   * Creates a new WebGPU timestamp query pool.
   *
   * @param {GPUDevice} device - The WebGPU device to create queries on.
   * @param {string} type - The type identifier for this query pool.
   * @param {number} [maxQueries=2048] - Maximum number of queries this pool can hold.
   */
  constructor(e, t, s = 2048) {
    super(s), this.device = e, this.type = t, this.querySet = this.device.createQuerySet({
      type: "timestamp",
      count: this.maxQueries,
      label: `queryset_global_timestamp_${t}`
    });
    const r = this.maxQueries * 8;
    this.resolveBuffer = this.device.createBuffer({
      label: `buffer_timestamp_resolve_${t}`,
      size: r,
      usage: GPUBufferUsage.QUERY_RESOLVE | GPUBufferUsage.COPY_SRC
    }), this.resultBuffer = this.device.createBuffer({
      label: `buffer_timestamp_result_${t}`,
      size: r,
      usage: GPUBufferUsage.COPY_DST | GPUBufferUsage.MAP_READ
    });
  }
  /**
   * Allocates a pair of queries for a given render context.
   *
   * @param {string} uid - A unique identifier for the render context.
   * @returns {?number} The base offset for the allocated queries, or null if allocation failed.
   */
  allocateQueriesForContext(e) {
    if (!this.trackTimestamp || this.isDisposed) return null;
    if (this.currentQueryIndex + 2 > this.maxQueries)
      return Te(`WebGPUTimestampQueryPool [${this.type}]: Maximum number of queries exceeded, when using trackTimestamp it is necessary to resolves the queries via renderer.resolveTimestampsAsync( THREE.TimestampQuery.${this.type.toUpperCase()} ).`), null;
    const t = this.currentQueryIndex;
    return this.currentQueryIndex += 2, this.queryOffsets.set(e, t), t;
  }
  /**
   * Asynchronously resolves all pending queries and returns the total duration.
   * If there's already a pending resolve operation, returns that promise instead.
   *
   * @async
   * @returns {Promise<number>} The total duration in milliseconds, or the last valid value if resolution fails.
   */
  async resolveQueriesAsync() {
    if (!this.trackTimestamp || this.currentQueryIndex === 0 || this.isDisposed)
      return this.lastValue;
    if (this.pendingResolve)
      return this.pendingResolve;
    this.pendingResolve = this._resolveQueries();
    try {
      return await this.pendingResolve;
    } finally {
      this.pendingResolve = null;
    }
  }
  /**
   * Internal method to resolve queries and calculate total duration.
   *
   * @async
   * @private
   * @returns {Promise<number>} The total duration in milliseconds.
   */
  async _resolveQueries() {
    if (this.isDisposed)
      return this.lastValue;
    try {
      if (this.resultBuffer.mapState !== "unmapped")
        return this.lastValue;
      const e = new Map(this.queryOffsets), t = this.currentQueryIndex, s = t * 8;
      this.currentQueryIndex = 0, this.queryOffsets.clear();
      const r = this.device.createCommandEncoder();
      r.resolveQuerySet(
        this.querySet,
        0,
        t,
        this.resolveBuffer,
        0
      ), r.copyBufferToBuffer(
        this.resolveBuffer,
        0,
        this.resultBuffer,
        0,
        s
      );
      const n = r.finish();
      if (this.device.queue.submit([n]), this.resultBuffer.mapState !== "unmapped")
        return this.lastValue;
      if (await this.resultBuffer.mapAsync(GPUMapMode.READ, 0, s), this.isDisposed)
        return this.resultBuffer.mapState === "mapped" && this.resultBuffer.unmap(), this.lastValue;
      const i = new BigUint64Array(this.resultBuffer.getMappedRange(0, s)), o = {}, u = [];
      for (const [c, d] of e) {
        const h = c.match(/^(.*):f(\d+)$/), f = parseInt(h[2]);
        u.includes(f) === !1 && u.push(f), o[f] === void 0 && (o[f] = 0);
        const p = i[d], g = i[d + 1], m = Number(g - p) / 1e6;
        this.timestamps.set(c, m), o[f] += m;
      }
      const l = o[u[u.length - 1]];
      return this.resultBuffer.unmap(), this.lastValue = l, this.frames = u, l;
    } catch (e) {
      return F("Error resolving queries:", e), this.resultBuffer.mapState === "mapped" && this.resultBuffer.unmap(), this.lastValue;
    }
  }
  /**
   * Dispose of the query pool.
   *
   * @async
   * @returns {Promise} A Promise that resolves when the dispose has been executed.
   */
  async dispose() {
    if (!this.isDisposed) {
      if (this.isDisposed = !0, this.pendingResolve)
        try {
          await this.pendingResolve;
        } catch (e) {
          F("Error waiting for pending resolve:", e);
        }
      if (this.resultBuffer && this.resultBuffer.mapState === "mapped")
        try {
          this.resultBuffer.unmap();
        } catch (e) {
          F("Error unmapping buffer:", e);
        }
      this.querySet && (this.querySet.destroy(), this.querySet = null), this.resolveBuffer && (this.resolveBuffer.destroy(), this.resolveBuffer = null), this.resultBuffer && (this.resultBuffer.destroy(), this.resultBuffer = null), this.queryOffsets.clear(), this.pendingResolve = null;
    }
  }
}
const lt = { r: 0, g: 0, b: 0, a: 1 };
class NN extends xh {
  /**
   * WebGPUBackend options.
   *
   * @typedef {Object} WebGPUBackend~Options
   * @property {boolean} [logarithmicDepthBuffer=false] - Whether logarithmic depth buffer is enabled or not.
   * @property {boolean} [reversedDepthBuffer=false] - Whether reversed depth buffer is enabled or not.
   * @property {boolean} [alpha=true] - Whether the default framebuffer (which represents the final contents of the canvas) should be transparent or opaque.
   * @property {boolean} [depth=true] - Whether the default framebuffer should have a depth buffer or not.
   * @property {boolean} [stencil=false] - Whether the default framebuffer should have a stencil buffer or not.
   * @property {boolean} [antialias=false] - Whether MSAA as the default anti-aliasing should be enabled or not.
   * @property {number} [samples=0] - When `antialias` is `true`, `4` samples are used by default. Set this parameter to any other integer value than 0 to overwrite the default.
   * @property {boolean} [forceWebGL=false] - If set to `true`, the renderer uses a WebGL 2 backend no matter if WebGPU is supported or not.
   * @property {boolean} [trackTimestamp=false] - Whether to track timestamps with a Timestamp Query API or not.
   * @property {string} [powerPreference=undefined] - The power preference.
   * @property {Object} [requiredLimits=undefined] - Specifies the limits that are required by the device request. The request will fail if the adapter cannot provide these limits.
   * @property {GPUDevice} [device=undefined] - If there is an existing GPU device on app level, it can be passed to the renderer as a parameter.
   * @property {number} [outputType=undefined] - Texture type for output to canvas. By default, device's preferred format is used; other formats may incur overhead.
   */
  /**
   * Constructs a new WebGPU backend.
   *
   * @param {WebGPUBackend~Options} [parameters] - The configuration parameter.
   */
  constructor(e = {}) {
    super(e), this.isWebGPUBackend = !0, this.parameters.alpha = e.alpha === void 0 ? !0 : e.alpha, this.parameters.requiredLimits = e.requiredLimits === void 0 ? {} : e.requiredLimits, this.compatibilityMode = null, this.device = null, this.defaultRenderPassdescriptor = null, this.utils = new pN(this), this.attributeUtils = new xN(this), this.bindingUtils = new yN(this), this.capabilities = new _N(this), this.pipelineUtils = new bN(this), this.textureUtils = new rN(this), this.occludedResolveCache = /* @__PURE__ */ new Map();
    const t = typeof navigator > "u" ? !0 : /Android/.test(navigator.userAgent) === !1;
    this._compatibility = {
      [Ps.TEXTURE_COMPARE]: t
    };
  }
  /**
   * Initializes the backend so it is ready for usage.
   *
   * @async
   * @param {Renderer} renderer - The renderer.
   * @return {Promise} A Promise that resolves when the backend has been initialized.
   */
  async init(e) {
    await super.init(e);
    const t = this.parameters;
    let s;
    if (t.device === void 0) {
      const r = {
        powerPreference: t.powerPreference,
        featureLevel: "compatibility"
      }, n = typeof navigator < "u" ? await navigator.gpu.requestAdapter(r) : null;
      if (n === null)
        throw new Error("WebGPUBackend: Unable to create WebGPU adapter.");
      const i = Object.values(Jn), o = [];
      for (const l of i)
        n.features.has(l) && o.push(l);
      const u = {
        requiredFeatures: o,
        requiredLimits: t.requiredLimits
      };
      s = await n.requestDevice(u);
    } else
      s = t.device;
    this.compatibilityMode = !s.features.has("core-features-and-limits"), this.compatibilityMode && (e._samples = 0), s.lost.then((r) => {
      if (r.reason === "destroyed") return;
      const n = {
        api: "WebGPU",
        message: r.message || "Unknown reason",
        reason: r.reason || null,
        originalEvent: r
      };
      e.onDeviceLost(n);
    }), this.device = s, this.trackTimestamp = this.trackTimestamp && this.hasFeature(Jn.TimestampQuery), this.updateSize();
  }
  /**
   * A reference to the context.
   *
   * @type {?GPUCanvasContext}
   * @default null
   */
  get context() {
    const e = this.renderer.getCanvasTarget(), t = this.get(e);
    let s = t.context;
    if (s === void 0) {
      const r = this.parameters;
      e.isDefaultCanvasTarget === !0 && r.context !== void 0 ? s = r.context : s = e.domElement.getContext("webgpu"), "setAttribute" in e.domElement && e.domElement.setAttribute("data-engine", `three.js r${ua} webgpu`);
      const n = r.alpha ? "premultiplied" : "opaque", i = r.outputType === Oe ? "extended" : "standard";
      s.configure({
        device: this.device,
        format: this.utils.getPreferredCanvasFormat(),
        usage: GPUTextureUsage.RENDER_ATTACHMENT | GPUTextureUsage.COPY_SRC,
        alphaMode: n,
        toneMapping: {
          mode: i
        }
      }), t.context = s;
    }
    return s;
  }
  /**
   * The coordinate system of the backend.
   *
   * @type {number}
   * @readonly
   */
  get coordinateSystem() {
    return kr;
  }
  /**
   * This method performs a readback operation by moving buffer data from
   * a storage buffer attribute from the GPU to the CPU. ReadbackBuffer can
   * be used to retain and reuse handles to the intermediate buffers and prevent
   * new allocation.
   *
   * @async
   * @param {BufferAttribute} attribute - The storage buffer attribute to read frm.
   * @param {number} count - The offset from which to start reading the
   * @param {number} offset - The storage buffer attribute.
   * @param {ReadbackBuffer|ArrayBuffer} target - The storage buffer attribute.
   * @return {Promise<ArrayBuffer|ReadbackBuffer>} A promise that resolves with the buffer data when the data are ready.
   */
  async getArrayBufferAsync(e, t = null, s = 0, r = -1) {
    return await this.attributeUtils.getArrayBufferAsync(e, t, s, r);
  }
  /**
   * Returns the backend's rendering context.
   *
   * @return {GPUCanvasContext} The rendering context.
   */
  getContext() {
    return this.context;
  }
  /**
   * Returns the default render pass descriptor.
   *
   * In WebGPU, the default framebuffer must be configured
   * like custom framebuffers so the backend needs a render
   * pass descriptor even when rendering directly to screen.
   *
   * @private
   * @return {Object} The render pass descriptor.
   */
  _getDefaultRenderPassDescriptor() {
    const e = this.renderer, t = e.getCanvasTarget(), s = this.get(t), r = e.currentSamples;
    let n = s.descriptor;
    if (n === void 0 || s.samples !== r) {
      n = {
        colorAttachments: [{
          view: null
        }]
      }, (e.depth === !0 || e.stencil === !0) && (n.depthStencilAttachment = {
        view: this.textureUtils.getDepthBuffer(e.depth, e.stencil).createView()
      });
      const o = n.colorAttachments[0];
      r > 0 ? o.view = this.textureUtils.getColorBuffer().createView() : o.resolveTarget = void 0, s.descriptor = n, s.samples = r;
    }
    const i = n.colorAttachments[0];
    return r > 0 ? i.resolveTarget = this.context.getCurrentTexture().createView() : i.view = this.context.getCurrentTexture().createView(), n;
  }
  /**
   * Internal to determine if the current render target is a render target array with depth 2D array texture.
   *
   * @param {RenderContext} renderContext - The render context.
   * @return {boolean} Whether the render target is a render target array with depth 2D array texture.
   *
   * @private
   */
  _isRenderCameraDepthArray(e) {
    return e.depthTexture && e.depthTexture.image.depth > 1 && e.camera.isArrayCamera;
  }
  /**
   * Returns the render pass descriptor for the given render context.
   *
   * @private
   * @param {RenderContext} renderContext - The render context.
   * @param {Object} colorAttachmentsConfig - Configuration object for the color attachments.
   * @return {Object} The render pass descriptor.
   */
  _getRenderPassDescriptor(e, t = {}) {
    const s = e.renderTarget, r = this.get(s);
    let n = r.descriptors;
    (n === void 0 || r.width !== s.width || r.height !== s.height || r.samples !== s.samples) && (n = {}, r.descriptors = n);
    const i = e.getCacheKey();
    let o = n[i];
    if (o === void 0) {
      const l = e.textures, c = [];
      let d;
      const h = this._isRenderCameraDepthArray(e);
      for (let f = 0; f < l.length; f++) {
        const p = this.get(l[f]), g = {
          label: `colorAttachment_${f}`,
          baseMipLevel: e.activeMipmapLevel,
          mipLevelCount: 1,
          baseArrayLayer: e.activeCubeFace,
          arrayLayerCount: 1,
          dimension: Fe.TwoD
        };
        if (s.isRenderTarget3D)
          d = e.activeCubeFace, g.baseArrayLayer = 0, g.dimension = Fe.ThreeD, g.depthOrArrayLayers = l[f].image.depth;
        else if (s.isRenderTarget && l[f].image.depth > 1)
          if (h === !0) {
            const m = e.camera.cameras;
            for (let y = 0; y < m.length; y++) {
              const _ = {
                ...g,
                baseArrayLayer: y,
                arrayLayerCount: 1,
                dimension: Fe.TwoD
              }, v = p.texture.createView(_);
              c.push({
                view: v,
                resolveTarget: void 0,
                depthSlice: void 0
              });
            }
          } else
            g.dimension = Fe.TwoDArray, g.depthOrArrayLayers = l[f].image.depth;
        if (h !== !0) {
          const m = p.texture.createView(g);
          let y, _;
          p.msaaTexture !== void 0 ? (y = p.msaaTexture.createView(), _ = m) : (y = m, _ = void 0), c.push({
            view: y,
            resolveTarget: _,
            depthSlice: d
          });
        }
      }
      if (o = { textureViews: c }, e.depth) {
        const f = this.get(e.depthTexture), p = {};
        (e.depthTexture.isArrayTexture || e.depthTexture.isCubeTexture) && (p.dimension = Fe.TwoD, p.arrayLayerCount = 1, p.baseArrayLayer = e.activeCubeFace), o.depthStencilView = f.texture.createView(p);
      }
      n[i] = o, r.width = s.width, r.height = s.height, r.samples = s.samples, r.activeMipmapLevel = e.activeMipmapLevel, r.activeCubeFace = e.activeCubeFace;
    }
    const u = {
      colorAttachments: []
    };
    for (let l = 0; l < o.textureViews.length; l++) {
      const c = o.textureViews[l];
      let d = { r: 0, g: 0, b: 0, a: 1 };
      l === 0 && t.clearValue && (d = t.clearValue), u.colorAttachments.push({
        view: c.view,
        depthSlice: c.depthSlice,
        resolveTarget: c.resolveTarget,
        loadOp: t.loadOp || te.Load,
        storeOp: t.storeOp || Ie.Store,
        clearValue: d
      });
    }
    return o.depthStencilView && (u.depthStencilAttachment = {
      view: o.depthStencilView
    }), u;
  }
  /**
   * This method is executed at the beginning of a render call and prepares
   * the WebGPU state for upcoming render calls
   *
   * @param {RenderContext} renderContext - The render context.
   */
  beginRender(e) {
    const t = this.get(e), s = this.device, r = e.occlusionQueryCount;
    let n;
    r > 0 && (t.currentOcclusionQuerySet && t.currentOcclusionQuerySet.destroy(), t.currentOcclusionQueryBuffer && t.currentOcclusionQueryBuffer.destroy(), t.currentOcclusionQuerySet = t.occlusionQuerySet, t.currentOcclusionQueryBuffer = t.occlusionQueryBuffer, t.currentOcclusionQueryObjects = t.occlusionQueryObjects, n = s.createQuerySet({ type: "occlusion", count: r, label: `occlusionQuerySet_${e.id}` }), t.occlusionQuerySet = n, t.occlusionQueryIndex = 0, t.occlusionQueryObjects = new Array(r), t.lastOcclusionObject = null);
    let i;
    e.textures === null ? i = this._getDefaultRenderPassDescriptor() : i = this._getRenderPassDescriptor(e, { loadOp: te.Load }), this.initTimestampQuery(Nt.RENDER, this.getTimestampUID(e), i), i.occlusionQuerySet = n;
    const o = i.depthStencilAttachment;
    if (e.textures !== null) {
      const l = i.colorAttachments;
      for (let c = 0; c < l.length; c++) {
        const d = l[c];
        e.clearColor ? (c === 0 ? d.clearValue = e.clearColorValue : (lt.r = 0, lt.g = 0, lt.b = 0, lt.a = 1, d.clearValue = lt), d.loadOp = te.Clear) : d.loadOp = te.Load, d.storeOp = Ie.Store;
      }
    } else {
      const l = i.colorAttachments[0];
      e.clearColor ? (l.clearValue = e.clearColorValue, l.loadOp = te.Clear) : l.loadOp = te.Load, l.storeOp = Ie.Store;
    }
    e.depth && (e.clearDepth ? (o.depthClearValue = e.clearDepthValue, o.depthLoadOp = te.Clear) : o.depthLoadOp = te.Load, o.depthStoreOp = Ie.Store), e.stencil && (e.clearStencil ? (o.stencilClearValue = e.clearStencilValue, o.stencilLoadOp = te.Clear) : o.stencilLoadOp = te.Load, o.stencilStoreOp = Ie.Store);
    const u = s.createCommandEncoder({ label: "renderContext_" + e.id });
    if (this._isRenderCameraDepthArray(e) === !0) {
      const l = e.camera.cameras;
      !t.layerDescriptors || t.layerDescriptors.length !== l.length ? this._createDepthLayerDescriptors(e, t, i, l) : this._updateDepthLayerDescriptors(e, t, l), t.bundleEncoders = [], t.bundleSets = [];
      for (let c = 0; c < l.length; c++) {
        const d = this.pipelineUtils.createBundleEncoder(
          e,
          "renderBundleArrayCamera_" + c
        ), h = {
          attributes: {},
          bindingGroups: [],
          pipeline: null,
          index: null
        };
        t.bundleEncoders.push(d), t.bundleSets.push(h);
      }
      t.currentPass = null;
    } else {
      const l = u.beginRenderPass(i);
      t.currentPass = l, e.viewport && this.updateViewport(e), e.scissor && this.updateScissor(e);
    }
    t.descriptor = i, t.encoder = u, t.currentSets = { attributes: {}, bindingGroups: [], pipeline: null, index: null }, t.renderBundles = [];
  }
  /**
   * This method creates layer descriptors for each camera in an array camera
   * to prepare for rendering to a depth array texture.
   *
   * @param {RenderContext} renderContext - The render context.
   * @param {Object} renderContextData - The render context data.
   * @param {Object} descriptor  - The render pass descriptor.
   * @param {ArrayCamera} cameras - The array camera.
   *
   * @private
   */
  _createDepthLayerDescriptors(e, t, s, r) {
    const n = s.depthStencilAttachment;
    t.layerDescriptors = [];
    const i = this.get(e.depthTexture);
    i.viewCache || (i.viewCache = []);
    for (let o = 0; o < r.length; o++) {
      const u = {
        ...s,
        colorAttachments: [{
          ...s.colorAttachments[0],
          view: s.colorAttachments[o].view
        }]
      };
      if (s.depthStencilAttachment) {
        const l = o;
        i.viewCache[l] || (i.viewCache[l] = i.texture.createView({
          dimension: Fe.TwoD,
          baseArrayLayer: o,
          arrayLayerCount: 1
        })), u.depthStencilAttachment = {
          view: i.viewCache[l],
          depthLoadOp: n.depthLoadOp || te.Clear,
          depthStoreOp: n.depthStoreOp || Ie.Store,
          depthClearValue: n.depthClearValue || 1
        }, e.stencil && (u.depthStencilAttachment.stencilLoadOp = n.stencilLoadOp, u.depthStencilAttachment.stencilStoreOp = n.stencilStoreOp, u.depthStencilAttachment.stencilClearValue = n.stencilClearValue);
      } else
        u.depthStencilAttachment = { ...n };
      t.layerDescriptors.push(u);
    }
  }
  /**
   * This method updates the layer descriptors for each camera in an array camera
   * to prepare for rendering to a depth array texture.
   *
   * @param {RenderContext} renderContext - The render context.
   * @param {Object} renderContextData - The render context data.
   * @param {ArrayCamera} cameras - The array camera.
   *
   */
  _updateDepthLayerDescriptors(e, t, s) {
    for (let r = 0; r < s.length; r++) {
      const n = t.layerDescriptors[r];
      if (n.depthStencilAttachment) {
        const i = n.depthStencilAttachment;
        e.depth && (e.clearDepth ? (i.depthClearValue = e.clearDepthValue, i.depthLoadOp = te.Clear) : i.depthLoadOp = te.Load), e.stencil && (e.clearStencil ? (i.stencilClearValue = e.clearStencilValue, i.stencilLoadOp = te.Clear) : i.stencilLoadOp = te.Load);
      }
    }
  }
  /**
   * This method is executed at the end of a render call and finalizes work
   * after draw calls.
   *
   * @param {RenderContext} renderContext - The render context.
   */
  finishRender(e) {
    const t = this.get(e), s = e.occlusionQueryCount;
    t.renderBundles.length > 0 && t.currentPass.executeBundles(t.renderBundles), s > t.occlusionQueryIndex && t.currentPass.endOcclusionQuery();
    const r = t.encoder;
    if (this._isRenderCameraDepthArray(e) === !0) {
      const n = [];
      for (let i = 0; i < t.bundleEncoders.length; i++) {
        const o = t.bundleEncoders[i];
        n.push(o.finish());
      }
      for (let i = 0; i < t.layerDescriptors.length; i++)
        if (i < n.length) {
          const o = t.layerDescriptors[i], u = r.beginRenderPass(o);
          if (e.viewport) {
            const { x: l, y: c, width: d, height: h, minDepth: f, maxDepth: p } = e.viewportValue;
            u.setViewport(l, c, d, h, f, p);
          }
          if (e.scissor) {
            const { x: l, y: c, width: d, height: h } = e.scissorValue;
            u.setScissorRect(l, c, d, h);
          }
          u.executeBundles([n[i]]), u.end();
        }
    } else t.currentPass && t.currentPass.end();
    if (s > 0) {
      const n = s * 8;
      let i = this.occludedResolveCache.get(n);
      i === void 0 && (i = this.device.createBuffer(
        {
          size: n,
          usage: GPUBufferUsage.QUERY_RESOLVE | GPUBufferUsage.COPY_SRC
        }
      ), this.occludedResolveCache.set(n, i));
      const o = this.device.createBuffer(
        {
          size: n,
          usage: GPUBufferUsage.COPY_DST | GPUBufferUsage.MAP_READ
        }
      );
      t.encoder.resolveQuerySet(t.occlusionQuerySet, 0, s, i, 0), t.encoder.copyBufferToBuffer(i, 0, o, 0, n), t.occlusionQueryBuffer = o, this.resolveOccludedAsync(e);
    }
    if (this.device.queue.submit([t.encoder.finish()]), e.textures !== null) {
      const n = e.textures;
      for (let i = 0; i < n.length; i++) {
        const o = n[i];
        o.generateMipmaps === !0 && this.textureUtils.generateMipmaps(o);
      }
    }
  }
  /**
   * Returns `true` if the given 3D object is fully occluded by other
   * 3D objects in the scene.
   *
   * @param {RenderContext} renderContext - The render context.
   * @param {Object3D} object - The 3D object to test.
   * @return {boolean} Whether the 3D object is fully occluded or not.
   */
  isOccluded(e, t) {
    const s = this.get(e);
    return s.occluded && s.occluded.has(t);
  }
  /**
   * This method processes the result of occlusion queries and writes it
   * into render context data.
   *
   * @async
   * @param {RenderContext} renderContext - The render context.
   * @return {Promise} A Promise that resolves when the occlusion query results have been processed.
   */
  async resolveOccludedAsync(e) {
    const t = this.get(e), { currentOcclusionQueryBuffer: s, currentOcclusionQueryObjects: r } = t;
    if (s && r) {
      const n = /* @__PURE__ */ new WeakSet();
      t.currentOcclusionQueryObjects = null, t.currentOcclusionQueryBuffer = null, await s.mapAsync(GPUMapMode.READ);
      const i = s.getMappedRange(), o = new BigUint64Array(i);
      for (let u = 0; u < r.length; u++)
        o[u] === BigInt(0) && n.add(r[u]);
      s.destroy(), t.occluded = n;
    }
  }
  /**
   * Updates the viewport with the values from the given render context.
   *
   * @param {RenderContext} renderContext - The render context.
   */
  updateViewport(e) {
    const { currentPass: t } = this.get(e), { x: s, y: r, width: n, height: i, minDepth: o, maxDepth: u } = e.viewportValue;
    t.setViewport(s, r, n, i, o, u);
  }
  /**
   * Updates the scissor with the values from the given render context.
   *
   * @param {RenderContext} renderContext - The render context.
   */
  updateScissor(e) {
    const { currentPass: t } = this.get(e), { x: s, y: r, width: n, height: i } = e.scissorValue;
    t.setScissorRect(s, r, n, i);
  }
  /**
   * Returns the clear color and alpha into a single
   * color object.
   *
   * @return {Color4} The clear color.
   */
  getClearColor() {
    const e = super.getClearColor();
    return this.renderer.alpha === !0 && (e.r *= e.a, e.g *= e.a, e.b *= e.a), e;
  }
  /**
   * Performs a clear operation.
   *
   * @param {boolean} color - Whether the color buffer should be cleared or not.
   * @param {boolean} depth - Whether the depth buffer should be cleared or not.
   * @param {boolean} stencil - Whether the stencil buffer should be cleared or not.
   * @param {?RenderContext} [renderTargetContext=null] - The render context of the current set render target.
   */
  clear(e, t, s, r = null) {
    const n = this.device, i = this.renderer;
    let o = [], u, l, c;
    if (e) {
      const f = this.getClearColor();
      lt.r = f.r, lt.g = f.g, lt.b = f.b, lt.a = f.a;
    }
    if (r === null) {
      l = i.depth, c = i.stencil;
      const f = this._getDefaultRenderPassDescriptor();
      if (e) {
        o = f.colorAttachments;
        const p = o[0];
        p.clearValue = lt, p.loadOp = te.Clear, p.storeOp = Ie.Store;
      }
      (l || c) && (u = f.depthStencilAttachment);
    } else {
      l = r.depth, c = r.stencil;
      const f = {
        loadOp: e ? te.Clear : te.Load,
        clearValue: e ? lt : void 0
      };
      l && (f.depthLoadOp = t ? te.Clear : te.Load, f.depthClearValue = t ? i.getClearDepth() : void 0, f.depthStoreOp = Ie.Store), c && (f.stencilLoadOp = s ? te.Clear : te.Load, f.stencilClearValue = s ? i.getClearStencil() : void 0, f.stencilStoreOp = Ie.Store);
      const p = this._getRenderPassDescriptor(r, f);
      o = p.colorAttachments, u = p.depthStencilAttachment;
    }
    l && u && (t ? (u.depthLoadOp = te.Clear, u.depthClearValue = i.getClearDepth(), u.depthStoreOp = Ie.Store) : (u.depthLoadOp = te.Load, u.depthStoreOp = Ie.Store)), c && u && (s ? (u.stencilLoadOp = te.Clear, u.stencilClearValue = i.getClearStencil(), u.stencilStoreOp = Ie.Store) : (u.stencilLoadOp = te.Load, u.stencilStoreOp = Ie.Store));
    const d = n.createCommandEncoder({ label: "clear" });
    d.beginRenderPass({
      colorAttachments: o,
      depthStencilAttachment: u
    }).end(), n.queue.submit([d.finish()]);
  }
  // compute
  /**
   * This method is executed at the beginning of a compute call and
   * prepares the state for upcoming compute tasks.
   *
   * @param {Node|Array<Node>} computeGroup - The compute node(s).
   */
  beginCompute(e) {
    const t = this.get(e), s = {
      label: "computeGroup_" + e.id
    };
    this.initTimestampQuery(Nt.COMPUTE, this.getTimestampUID(e), s), t.cmdEncoderGPU = this.device.createCommandEncoder({ label: "computeGroup_" + e.id }), t.passEncoderGPU = t.cmdEncoderGPU.beginComputePass(s);
  }
  /**
   * Executes a compute command for the given compute node.
   *
   * @param {Node|Array<Node>} computeGroup - The group of compute nodes of a compute call. Can be a single compute node.
   * @param {Node} computeNode - The compute node.
   * @param {Array<BindGroup>} bindings - The bindings.
   * @param {ComputePipeline} pipeline - The compute pipeline.
   * @param {number|Array<number>|IndirectStorageBufferAttribute} [dispatchSize=null]
   * - A single number representing count, or
   * - An array [x, y, z] representing dispatch size, or
   * - A IndirectStorageBufferAttribute for indirect dispatch size.
   */
  compute(e, t, s, r, n = null) {
    const i = this.get(t), { passEncoderGPU: o } = this.get(e), u = this.get(r).pipeline;
    this.pipelineUtils.setPipeline(o, u);
    for (let l = 0, c = s.length; l < c; l++) {
      const d = s[l], h = this.get(d);
      o.setBindGroup(l, h.group);
    }
    if (n === null && (n = t.dispatchSize || t.count), n && n.isIndirectStorageBufferAttribute) {
      const l = this.get(n).buffer;
      o.dispatchWorkgroupsIndirect(l, 0);
      return;
    }
    if (typeof n == "number") {
      const l = n;
      if (i.dispatchSize === void 0 || i.count !== l) {
        i.dispatchSize = [0, 1, 1], i.count = l;
        const c = t.workgroupSize;
        let d = c[0];
        for (let p = 1; p < c.length; p++)
          d *= c[p];
        const h = Math.ceil(l / d), f = this.device.limits.maxComputeWorkgroupsPerDimension;
        n = [h, 1, 1], h > f && (n[0] = Math.min(h, f), n[1] = Math.ceil(h / f)), i.dispatchSize = n;
      }
      n = i.dispatchSize;
    }
    o.dispatchWorkgroups(
      n[0],
      n[1] || 1,
      n[2] || 1
    );
  }
  /**
   * This method is executed at the end of a compute call and
   * finalizes work after compute tasks.
   *
   * @param {Node|Array<Node>} computeGroup - The compute node(s).
   */
  finishCompute(e) {
    const t = this.get(e);
    t.passEncoderGPU.end(), this.device.queue.submit([t.cmdEncoderGPU.finish()]);
  }
  /**
   * Internal draw function that performs the draw with the given pass encoder.
   *
   * @private
   * @param {RenderObject} renderObject - The render object.
   * @param {Info} info - Holds a series of statistical information about the GPU memory and the rendering process.
   * @param {Object} renderContextData - The render context data object, holding current pass state and occlusion query tracking.
   * @param {GPURenderPipeline} pipelineGPU - The GPU render pipeline.
   * @param {Array<BindGroup>} bindings - The bind groups.
   * @param {Array<BufferAttribute>} vertexBuffers - The vertex buffers.
   * @param {{vertexCount: number, firstVertex: number, instanceCount: number, firstInstance: number}} drawParams - The draw parameters.
   * @param {GPURenderPassEncoder|GPURenderBundleEncoder} passEncoderGPU - The GPU pass encoder used for recording draw commands.
   * @param {Object} currentSets - Tracking object for currently set pipeline, attributes, bind groups, and index state.
   */
  _draw(e, t, s, r, n, i, o, u, l) {
    const { object: c, material: d, context: h } = e, f = e.getIndex(), p = f !== null;
    this.pipelineUtils.setPipeline(u, r), l.pipeline = r;
    const g = l.bindingGroups;
    for (let m = 0, y = n.length; m < y; m++) {
      const _ = n[m], v = this.get(_);
      g[m] !== _.id && (u.setBindGroup(m, v.group), g[m] = _.id);
    }
    if (p === !0 && l.index !== f) {
      const m = this.get(f).buffer, y = f.array instanceof Uint16Array ? Zn.Uint16 : Zn.Uint32;
      u.setIndexBuffer(m, y), l.index = f;
    }
    for (let m = 0, y = i.length; m < y; m++) {
      const _ = i[m];
      if (l.attributes[m] !== _) {
        const v = this.get(_).buffer;
        u.setVertexBuffer(m, v), l.attributes[m] = _;
      }
    }
    if (h.stencil === !0 && d.stencilWrite === !0 && s.currentStencilRef !== d.stencilRef && (u.setStencilReference(d.stencilRef), s.currentStencilRef = d.stencilRef), c.isBatchedMesh === !0) {
      const m = c._multiDrawStarts, y = c._multiDrawCounts, _ = c._multiDrawCount;
      let v = p === !0 ? f.array.BYTES_PER_ELEMENT : 1;
      d.wireframe && (v = c.geometry.attributes.position.count > 65535 ? 4 : 2);
      for (let C = 0; C < _; C++)
        p === !0 ? u.drawIndexed(y[C], 1, m[C] / v, 0, C) : u.draw(y[C], 1, m[C], C), t.update(c, y[C], 1);
    } else if (p === !0) {
      const { vertexCount: m, instanceCount: y, firstVertex: _ } = o, v = e.getIndirect();
      if (v !== null) {
        const C = this.get(v).buffer, w = e.getIndirectOffset(), M = Array.isArray(w) ? w : [w];
        for (let E = 0; E < M.length; E++)
          u.drawIndexedIndirect(C, M[E]);
      } else
        u.drawIndexed(m, y, _, 0, 0);
      t.update(c, m, y);
    } else {
      const { vertexCount: m, instanceCount: y, firstVertex: _ } = o, v = e.getIndirect();
      if (v !== null) {
        const C = this.get(v).buffer, w = e.getIndirectOffset(), M = Array.isArray(w) ? w : [w];
        for (let E = 0; E < M.length; E++)
          u.drawIndirect(C, M[E]);
      } else
        u.draw(m, y, _, 0);
      t.update(c, m, y);
    }
  }
  // render object
  /**
   * Executes a draw command for the given render object.
   *
   * @param {RenderObject} renderObject - The render object to draw.
   * @param {Info} info - Holds a series of statistical information about the GPU memory and the rendering process.
   */
  draw(e, t) {
    const { object: s, context: r, pipeline: n } = e, i = this.get(r), o = this.get(n), u = o.pipeline;
    if (o.error === !0) return;
    const l = e.getDrawParameters();
    if (l === null) return;
    const c = e.getBindings(), d = e.getVertexBuffers();
    if (e.camera.isArrayCamera && e.camera.cameras.length > 0) {
      const h = this.get(e.camera), f = e.camera.cameras, p = e.getBindingGroup("cameraIndex");
      if (h.indexesGPU === void 0 || h.indexesGPU.length !== f.length) {
        const m = this.get(p), y = [], _ = new Uint32Array([0, 0, 0, 0]);
        for (let v = 0, C = f.length; v < C; v++) {
          _[0] = v;
          const { layoutGPU: w } = m.layout, M = this.bindingUtils.createBindGroupIndex(_, w);
          y.push(M);
        }
        h.indexesGPU = y;
      }
      const g = this.renderer.getPixelRatio();
      for (let m = 0, y = f.length; m < y; m++) {
        const _ = f[m];
        if (s.layers.test(_.layers)) {
          const v = _.viewport;
          let C = i.currentPass, w = i.currentSets;
          if (i.bundleEncoders) {
            const M = i.bundleEncoders[m], E = i.bundleSets[m];
            C = M, w = E;
          }
          if (v && C.setViewport(
            Math.floor(v.x * g),
            Math.floor(v.y * g),
            Math.floor(v.width * g),
            Math.floor(v.height * g),
            r.viewportValue.minDepth,
            r.viewportValue.maxDepth
          ), p && h.indexesGPU) {
            const M = c.indexOf(p);
            C.setBindGroup(M, h.indexesGPU[m]), w.bindingGroups[M] = p.id;
          }
          this._draw(e, t, i, u, c, d, l, C, w);
        }
      }
    } else if (i.currentPass) {
      if (i.occlusionQuerySet !== void 0) {
        const h = i.lastOcclusionObject;
        h !== s && (h !== null && h.occlusionTest === !0 && (i.currentPass.endOcclusionQuery(), i.occlusionQueryIndex++), s.occlusionTest === !0 && (i.currentPass.beginOcclusionQuery(i.occlusionQueryIndex), i.occlusionQueryObjects[i.occlusionQueryIndex] = s), i.lastOcclusionObject = s);
      }
      this._draw(e, t, i, u, c, d, l, i.currentPass, i.currentSets);
    }
  }
  // cache key
  /**
   * Returns `true` if the render pipeline requires an update.
   *
   * @param {RenderObject} renderObject - The render object.
   * @return {boolean} Whether the render pipeline requires an update or not.
   */
  needsRenderUpdate(e) {
    const t = this.get(e), { object: s, material: r } = e, n = this.utils, i = n.getSampleCountRenderContext(e.context), o = n.getCurrentColorSpace(e.context), u = n.getCurrentColorFormat(e.context), l = n.getCurrentDepthStencilFormat(e.context), c = n.getPrimitiveTopology(s, r);
    let d = !1;
    return (t.material !== r || t.materialVersion !== r.version || t.transparent !== r.transparent || t.blending !== r.blending || t.premultipliedAlpha !== r.premultipliedAlpha || t.blendSrc !== r.blendSrc || t.blendDst !== r.blendDst || t.blendEquation !== r.blendEquation || t.blendSrcAlpha !== r.blendSrcAlpha || t.blendDstAlpha !== r.blendDstAlpha || t.blendEquationAlpha !== r.blendEquationAlpha || t.colorWrite !== r.colorWrite || t.depthWrite !== r.depthWrite || t.depthTest !== r.depthTest || t.depthFunc !== r.depthFunc || t.stencilWrite !== r.stencilWrite || t.stencilFunc !== r.stencilFunc || t.stencilFail !== r.stencilFail || t.stencilZFail !== r.stencilZFail || t.stencilZPass !== r.stencilZPass || t.stencilFuncMask !== r.stencilFuncMask || t.stencilWriteMask !== r.stencilWriteMask || t.side !== r.side || t.alphaToCoverage !== r.alphaToCoverage || t.sampleCount !== i || t.colorSpace !== o || t.colorFormat !== u || t.depthStencilFormat !== l || t.primitiveTopology !== c || t.clippingContextCacheKey !== e.clippingContextCacheKey) && (t.material = r, t.materialVersion = r.version, t.transparent = r.transparent, t.blending = r.blending, t.premultipliedAlpha = r.premultipliedAlpha, t.blendSrc = r.blendSrc, t.blendDst = r.blendDst, t.blendEquation = r.blendEquation, t.blendSrcAlpha = r.blendSrcAlpha, t.blendDstAlpha = r.blendDstAlpha, t.blendEquationAlpha = r.blendEquationAlpha, t.colorWrite = r.colorWrite, t.depthWrite = r.depthWrite, t.depthTest = r.depthTest, t.depthFunc = r.depthFunc, t.stencilWrite = r.stencilWrite, t.stencilFunc = r.stencilFunc, t.stencilFail = r.stencilFail, t.stencilZFail = r.stencilZFail, t.stencilZPass = r.stencilZPass, t.stencilFuncMask = r.stencilFuncMask, t.stencilWriteMask = r.stencilWriteMask, t.side = r.side, t.alphaToCoverage = r.alphaToCoverage, t.sampleCount = i, t.colorSpace = o, t.colorFormat = u, t.depthStencilFormat = l, t.primitiveTopology = c, t.clippingContextCacheKey = e.clippingContextCacheKey, d = !0), d;
  }
  /**
   * Returns a cache key that is used to identify render pipelines.
   *
   * @param {RenderObject} renderObject - The render object.
   * @return {string} The cache key.
   */
  getRenderCacheKey(e) {
    const { object: t, material: s } = e, r = this.utils, n = e.context, i = t.isMesh && t.matrixWorld.determinant() < 0;
    return [
      s.transparent,
      s.blending,
      s.premultipliedAlpha,
      s.blendSrc,
      s.blendDst,
      s.blendEquation,
      s.blendSrcAlpha,
      s.blendDstAlpha,
      s.blendEquationAlpha,
      s.colorWrite,
      s.depthWrite,
      s.depthTest,
      s.depthFunc,
      s.stencilWrite,
      s.stencilFunc,
      s.stencilFail,
      s.stencilZFail,
      s.stencilZPass,
      s.stencilFuncMask,
      s.stencilWriteMask,
      s.side,
      i,
      r.getSampleCountRenderContext(n),
      r.getCurrentColorSpace(n),
      r.getCurrentColorFormat(n),
      r.getCurrentDepthStencilFormat(n),
      r.getPrimitiveTopology(t, s),
      e.getGeometryCacheKey(),
      e.clippingContextCacheKey
    ].join();
  }
  // textures
  /**
   * Updates a GPU sampler for the given texture.
   *
   * @param {Texture} texture - The texture to update the sampler for.
   * @return {string} The current sampler key.
   */
  updateSampler(e) {
    return this.textureUtils.updateSampler(e);
  }
  /**
   * Creates a default texture for the given texture that can be used
   * as a placeholder until the actual texture is ready for usage.
   *
   * @param {Texture} texture - The texture to create a default texture for.
   * @return {boolean} Whether the sampler has been updated or not.
   */
  createDefaultTexture(e) {
    return this.textureUtils.createDefaultTexture(e);
  }
  /**
   * Defines a texture on the GPU for the given texture object.
   *
   * @param {Texture} texture - The texture.
   * @param {Object} [options={}] - Optional configuration parameter.
   */
  createTexture(e, t) {
    this.textureUtils.createTexture(e, t);
  }
  /**
   * Uploads the updated texture data to the GPU.
   *
   * @param {Texture} texture - The texture.
   * @param {Object} [options={}] - Optional configuration parameter.
   */
  updateTexture(e, t) {
    this.textureUtils.updateTexture(e, t);
  }
  /**
   * Generates mipmaps for the given texture.
   *
   * @param {Texture} texture - The texture.
   */
  generateMipmaps(e) {
    this.textureUtils.generateMipmaps(e);
  }
  /**
   * Destroys the GPU data for the given texture object.
   *
   * @param {Texture} texture - The texture.
   * @param {boolean} [isDefaultTexture=false] - Whether the texture uses a default GPU texture or not.
   */
  destroyTexture(e, t = !1) {
    this.textureUtils.destroyTexture(e, t);
  }
  /**
   * Returns texture data as a typed array.
   *
   * @async
   * @param {Texture} texture - The texture to copy.
   * @param {number} x - The x coordinate of the copy origin.
   * @param {number} y - The y coordinate of the copy origin.
   * @param {number} width - The width of the copy.
   * @param {number} height - The height of the copy.
   * @param {number} faceIndex - The face index.
   * @return {Promise<TypedArray>} A Promise that resolves with a typed array when the copy operation has finished.
   */
  async copyTextureToBuffer(e, t, s, r, n, i) {
    return this.textureUtils.copyTextureToBuffer(e, t, s, r, n, i);
  }
  /**
   * Inits a time stamp query for the given render context.
   *
   * @param {string} type - The type of the timestamp query (e.g. 'render', 'compute').
   * @param {number} uid - Unique id for the context (e.g. render context id).
   * @param {Object} descriptor - The query descriptor.
   */
  initTimestampQuery(e, t, s) {
    if (!this.trackTimestamp) return;
    this.timestampQueryPool[e] || (this.timestampQueryPool[e] = new SN(this.device, e, 2048));
    const r = this.timestampQueryPool[e], n = r.allocateQueriesForContext(t);
    s.timestampWrites = {
      querySet: r.querySet,
      beginningOfPassWriteIndex: n,
      endOfPassWriteIndex: n + 1
    };
  }
  // node builder
  /**
   * Returns a node builder for the given render object.
   *
   * @param {RenderObject} object - The render object.
   * @param {Renderer} renderer - The renderer.
   * @return {WGSLNodeBuilder} The node builder.
   */
  createNodeBuilder(e, t) {
    return new fN(e, t);
  }
  // program
  /**
   * Creates a shader program from the given programmable stage.
   *
   * @param {ProgrammableStage} program - The programmable stage.
   */
  createProgram(e) {
    const t = this.get(e);
    t.module = {
      module: this.device.createShaderModule({ code: e.code, label: e.stage + (e.name !== "" ? `_${e.name}` : "") }),
      entryPoint: "main"
    };
  }
  /**
   * Destroys the shader program of the given programmable stage.
   *
   * @param {ProgrammableStage} program - The programmable stage.
   */
  destroyProgram(e) {
    this.delete(e);
  }
  // pipelines
  /**
   * Creates a render pipeline for the given render object.
   *
   * @param {RenderObject} renderObject - The render object.
   * @param {Array<Promise>} promises - An array of compilation promises which are used in `compileAsync()`.
   */
  createRenderPipeline(e, t) {
    this.pipelineUtils.createRenderPipeline(e, t);
  }
  /**
   * Creates a compute pipeline for the given compute node.
   *
   * @param {ComputePipeline} computePipeline - The compute pipeline.
   * @param {Array<BindGroup>} bindings - The bindings.
   */
  createComputePipeline(e, t) {
    this.pipelineUtils.createComputePipeline(e, t);
  }
  /**
   * Prepares the state for encoding render bundles.
   *
   * @param {RenderContext} renderContext - The render context.
   */
  beginBundle(e) {
    const t = this.get(e);
    t._currentPass = t.currentPass, t._currentSets = t.currentSets, t.currentSets = { attributes: {}, bindingGroups: [], pipeline: null, index: null }, t.currentPass = this.pipelineUtils.createBundleEncoder(e);
  }
  /**
   * After processing render bundles this method finalizes related work.
   *
   * @param {RenderContext} renderContext - The render context.
   * @param {RenderBundle} bundle - The render bundle.
   */
  finishBundle(e, t) {
    const s = this.get(e), n = s.currentPass.finish();
    this.get(t).bundleGPU = n, s.currentSets = s._currentSets, s.currentPass = s._currentPass;
  }
  /**
   * Adds a render bundle to the render context data.
   *
   * @param {RenderContext} renderContext - The render context.
   * @param {RenderBundle} bundle - The render bundle to add.
   */
  addBundle(e, t) {
    this.get(e).renderBundles.push(this.get(t).bundleGPU);
  }
  // bindings
  /**
   * Creates bindings from the given bind group definition.
   *
   * @param {BindGroup} bindGroup - The bind group.
   * @param {Array<BindGroup>} bindings - Array of bind groups.
   * @param {number} cacheIndex - The cache index.
   * @param {number} version - The version.
   */
  createBindings(e, t, s, r) {
    this.bindingUtils.createBindings(e, t, s, r);
  }
  /**
   * Updates the given bind group definition.
   *
   * @param {BindGroup} bindGroup - The bind group.
   * @param {Array<BindGroup>} bindings - Array of bind groups.
   * @param {number} cacheIndex - The cache index.
   * @param {number} version - The version.
   */
  updateBindings(e, t, s, r) {
    this.bindingUtils.createBindings(e, t, s, r);
  }
  /**
   * Updates a buffer binding.
   *
   *  @param {Buffer} binding - The buffer binding to update.
   */
  updateBinding(e) {
    this.bindingUtils.updateBinding(e);
  }
  /**
   * Delete data associated with the current bind group.
   *
   * @param {BindGroup} bindGroup - The bind group.
   */
  deleteBindGroupData(e) {
    this.bindingUtils.deleteBindGroupData(e);
  }
  // attributes
  /**
   * Creates the buffer of an indexed shader attribute.
   *
   * @param {BufferAttribute} attribute - The indexed buffer attribute.
   */
  createIndexAttribute(e) {
    let t = GPUBufferUsage.INDEX | GPUBufferUsage.COPY_SRC | GPUBufferUsage.COPY_DST;
    (e.isStorageBufferAttribute || e.isStorageInstancedBufferAttribute) && (t |= GPUBufferUsage.STORAGE), this.attributeUtils.createAttribute(e, t);
  }
  /**
   * Creates the GPU buffer of a shader attribute.
   *
   * @param {BufferAttribute} attribute - The buffer attribute.
   */
  createAttribute(e) {
    this.attributeUtils.createAttribute(e, GPUBufferUsage.VERTEX | GPUBufferUsage.COPY_SRC | GPUBufferUsage.COPY_DST);
  }
  /**
   * Creates the GPU buffer of a storage attribute.
   *
   * @param {BufferAttribute} attribute - The buffer attribute.
   */
  createStorageAttribute(e) {
    this.attributeUtils.createAttribute(e, GPUBufferUsage.STORAGE | GPUBufferUsage.VERTEX | GPUBufferUsage.COPY_SRC | GPUBufferUsage.COPY_DST);
  }
  /**
   * Creates the GPU buffer of an indirect storage attribute.
   *
   * @param {BufferAttribute} attribute - The buffer attribute.
   */
  createIndirectStorageAttribute(e) {
    this.attributeUtils.createAttribute(e, GPUBufferUsage.STORAGE | GPUBufferUsage.INDIRECT | GPUBufferUsage.COPY_SRC | GPUBufferUsage.COPY_DST);
  }
  /**
   * Updates the GPU buffer of a shader attribute.
   *
   * @param {BufferAttribute} attribute - The buffer attribute to update.
   */
  updateAttribute(e) {
    this.attributeUtils.updateAttribute(e);
  }
  /**
   * Destroys the GPU buffer of a shader attribute.
   *
   * @param {BufferAttribute} attribute - The buffer attribute to destroy.
   */
  destroyAttribute(e) {
    this.attributeUtils.destroyAttribute(e);
  }
  // canvas
  /**
   * Triggers an update of the default render pass descriptor.
   */
  updateSize() {
    this.delete(this.renderer.getCanvasTarget());
  }
  // utils public
  /**
   * Checks if the given feature is supported by the backend.
   *
   * @param {string} name - The feature's name.
   * @return {boolean} Whether the feature is supported or not.
   */
  hasFeature(e) {
    return vl[e] !== void 0 && (e = vl[e]), this.device.features.has(e);
  }
  /**
   * Copies data of the given source texture to the given destination texture.
   *
   * @param {Texture} srcTexture - The source texture.
   * @param {Texture} dstTexture - The destination texture.
   * @param {?(Box3|Box2)} [srcRegion=null] - The region of the source texture to copy.
   * @param {?(Vector2|Vector3)} [dstPosition=null] - The destination position of the copy.
   * @param {number} [srcLevel=0] - The mipmap level to copy.
   * @param {number} [dstLevel=0] - The destination mip level to copy to.
   */
  copyTextureToTexture(e, t, s = null, r = null, n = 0, i = 0) {
    let o = 0, u = 0, l = 0, c = 0, d = 0, h = 0, f = e.image.width, p = e.image.height, g = 1;
    s !== null && (s.isBox3 === !0 ? (c = s.min.x, d = s.min.y, h = s.min.z, f = s.max.x - s.min.x, p = s.max.y - s.min.y, g = s.max.z - s.min.z) : (c = s.min.x, d = s.min.y, f = s.max.x - s.min.x, p = s.max.y - s.min.y, g = 1)), r !== null && (o = r.x, u = r.y, l = r.z || 0);
    const m = this.device.createCommandEncoder({ label: "copyTextureToTexture_" + e.id + "_" + t.id }), y = this.get(e).texture, _ = this.get(t).texture;
    m.copyTextureToTexture(
      {
        texture: y,
        mipLevel: n,
        origin: { x: c, y: d, z: h }
      },
      {
        texture: _,
        mipLevel: i,
        origin: { x: o, y: u, z: l }
      },
      [
        f,
        p,
        g
      ]
    ), this.device.queue.submit([m.finish()]), i === 0 && t.generateMipmaps && this.textureUtils.generateMipmaps(t);
  }
  /**
   * Copies the current bound framebuffer to the given texture.
   *
   * @param {Texture} texture - The destination texture.
   * @param {RenderContext} renderContext - The render context.
   * @param {Vector4} rectangle - A four dimensional vector defining the origin and dimension of the copy.
   */
  copyFramebufferToTexture(e, t, s) {
    const r = this.get(t);
    let n = null;
    t.renderTarget ? e.isDepthTexture ? n = this.get(t.depthTexture).texture : n = this.get(t.textures[0]).texture : e.isDepthTexture ? n = this.textureUtils.getDepthBuffer(t.depth, t.stencil) : n = this.context.getCurrentTexture();
    const i = this.get(e).texture;
    if (n.format !== i.format) {
      F("WebGPUBackend: copyFramebufferToTexture: Source and destination formats do not match.", n.format, i.format);
      return;
    }
    let o;
    if (r.currentPass ? (r.currentPass.end(), o = r.encoder) : o = this.device.createCommandEncoder({ label: "copyFramebufferToTexture_" + e.id }), o.copyTextureToTexture(
      {
        texture: n,
        origin: [s.x, s.y, 0]
      },
      {
        texture: i
      },
      [
        s.z,
        s.w
      ]
    ), e.generateMipmaps && this.textureUtils.generateMipmaps(e, o), r.currentPass) {
      const { descriptor: u } = r;
      for (let l = 0; l < u.colorAttachments.length; l++)
        u.colorAttachments[l].loadOp = te.Load;
      t.depth && (u.depthStencilAttachment.depthLoadOp = te.Load), t.stencil && (u.depthStencilAttachment.stencilLoadOp = te.Load), r.currentPass = o.beginRenderPass(u), r.currentSets = { attributes: {}, bindingGroups: [], pipeline: null, index: null }, t.viewport && this.updateViewport(t), t.scissor && this.updateScissor(t);
    } else
      this.device.queue.submit([o.finish()]);
  }
  /**
   * Checks if the given compatibility is supported by the backend.
   *
   * @param {string} name - The compatibility name.
   * @return {boolean} Whether the compatibility is supported or not.
   */
  hasCompatibility(e) {
    return this._compatibility[e] !== void 0 ? this._compatibility[e] : super.hasCompatibility(e);
  }
  dispose() {
    if (this.bindingUtils.dispose(), this.textureUtils.dispose(), this.occludedResolveCache) {
      for (const e of this.occludedResolveCache.values())
        e.destroy();
      this.occludedResolveCache.clear();
    }
    if (this.timestampQueryPool)
      for (const e of Object.values(this.timestampQueryPool))
        e !== null && e.dispose();
    this.parameters.device === void 0 && this.device !== null && this.device.destroy();
  }
}
class RN extends ra {
  /**
   * Constructs a new IES spot light.
   *
   * @param {(number|Color|string)} [color=0xffffff] - The light's color.
   * @param {number} [intensity=1] - The light's strength/intensity measured in candela (cd).
   * @param {number} [distance=0] - Maximum range of the light. `0` means no limit.
   * @param {number} [angle=Math.PI/3] - Maximum angle of light dispersion from its direction whose upper bound is `Math.PI/2`.
   * @param {number} [penumbra=0] - Percent of the spotlight cone that is attenuated due to penumbra. Value range is `[0,1]`.
   * @param {number} [decay=2] - The amount the light dims along the distance of the light.
   */
  constructor(e, t, s, r, n, i) {
    super(e, t, s, r, n, i), this.iesMap = null;
  }
  copy(e, t) {
    return super.copy(e, t), this.iesMap = e.iesMap, this;
  }
}
class AN extends ra {
  /**
   * Constructs a new projector light.
   *
   * @param {(number|Color|string)} [color=0xffffff] - The light's color.
   * @param {number} [intensity=1] - The light's strength/intensity measured in candela (cd).
   * @param {number} [distance=0] - Maximum range of the light. `0` means no limit.
   * @param {number} [angle=Math.PI/3] - Maximum angle of light dispersion from its direction whose upper bound is `Math.PI/2`.
   * @param {number} [penumbra=0] - Percent of the spotlight cone that is attenuated due to penumbra. Value range is `[0,1]`.
   * @param {number} [decay=2] - The amount the light dims along the distance of the light.
   */
  constructor(e, t, s, r, n, i) {
    super(e, t, s, r, n, i), this.aspect = null;
  }
  copy(e, t) {
    return super.copy(e, t), this.aspect = e.aspect, this;
  }
}
class vN extends lh {
  /**
   * Constructs a new standard node library.
   */
  constructor() {
    super(), this.addMaterial(IT, "MeshPhongMaterial"), this.addMaterial(zd, "MeshStandardMaterial"), this.addMaterial(Dy, "MeshPhysicalMaterial"), this.addMaterial(Oy, "MeshToonMaterial"), this.addMaterial(MT, "MeshBasicMaterial"), this.addMaterial(PT, "MeshLambertMaterial"), this.addMaterial(AT, "MeshNormalMaterial"), this.addMaterial(Vy, "MeshMatcapMaterial"), this.addMaterial(bT, "LineBasicMaterial"), this.addMaterial(NT, "LineDashedMaterial"), this.addMaterial(qy, "PointsMaterial"), this.addMaterial(Hd, "SpriteMaterial"), this.addMaterial(Yy, "ShadowMaterial"), this.addLight(Ab, Nh), this.addLight(tS, Rh), this.addLight(aS, Ah), this.addLight(Ga, ra), this.addLight(eS, vh), this.addLight(sS, Eh), this.addLight(nS, Ch), this.addLight(rS, RN), this.addLight(oS, AN), this.addToneMapping(M_, wh), this.addToneMapping(B_, Mh), this.addToneMapping(F_, Bh), this.addToneMapping(D_, Fh), this.addToneMapping(O_, Uh), this.addToneMapping(G_, Dh);
  }
}
class wN extends CS {
  /**
   * WebGPURenderer options.
   *
   * @typedef {Object} WebGPURenderer~Options
   * @property {boolean} [logarithmicDepthBuffer=false] - Whether logarithmic depth buffer is enabled or not.
   * @property {boolean} [reversedDepthBuffer=false] - Whether reversed depth buffer is enabled or not.
   * @property {boolean} [alpha=true] - Whether the default framebuffer (which represents the final contents of the canvas) should be transparent or opaque.
   * @property {boolean} [depth=true] - Whether the default framebuffer should have a depth buffer or not.
   * @property {boolean} [stencil=false] - Whether the default framebuffer should have a stencil buffer or not.
   * @property {boolean} [antialias=false] - Whether MSAA as the default anti-aliasing should be enabled or not.
   * @property {number} [samples=0] - When `antialias` is `true`, `4` samples are used by default. Set this parameter to any other integer value than 0 to overwrite the default.
   * @property {boolean} [forceWebGL=false] - If set to `true`, the renderer uses a WebGL 2 backend no matter if WebGPU is supported or not.
   * @property {boolean} [multiview=false] - If set to `true`, the renderer will use multiview during WebXR rendering if supported.
   * @property {number} [outputType=undefined] - Texture type for output to canvas. By default, device's preferred format is used; other formats may incur overhead.
   * @property {number} [outputBufferType=HalfFloatType] - Defines the type of output buffers. The default `HalfFloatType` is recommend for best
   * quality. To save memory and bandwidth, `UnsignedByteType` might be used. This will reduce rendering quality though.
   */
  /**
   * Constructs a new WebGPU renderer.
   *
   * @param {WebGPURenderer~Options} [parameters] - The configuration parameter.
   */
  constructor(e = {}) {
    let t;
    e.forceWebGL ? t = _l : (t = NN, e.getFallback = () => (L("WebGPURenderer: WebGPU is not available, running under WebGL2 backend."), new _l(e)));
    const s = new t(e);
    super(s, e), this.library = new vN(), this.isWebGPURenderer = !0, typeof __THREE_DEVTOOLS__ < "u" && __THREE_DEVTOOLS__.dispatchEvent(new CustomEvent("observe", { detail: this }));
  }
}
export {
  Fh as ACESFilmicToneMapping,
  sT as AONode,
  Pt as AddEquation,
  Cf as AddOperation,
  FN as AdditiveAnimationBlendMode,
  Xs as AdditiveBlending,
  Uh as AgXToneMapping,
  Pl as AlphaFormat,
  Wl as AlwaysCompare,
  dc as AlwaysDepth,
  mf as AlwaysStencilFunc,
  vh as AmbientLight,
  eS as AmbientLightNode,
  Os as AnalyticLightNode,
  UN as AnimationAction,
  DN as AnimationClip,
  PN as AnimationMixer,
  Ph as ArrayCamera,
  dr as ArrayElementNode,
  nu as ArrayNode,
  Fp as AssignNode,
  Ft as AtomicFunctionNode,
  LN as AttachedBindMode,
  rd as AttributeNode,
  Ge as BackSide,
  z_ as BarrierNode,
  IN as BasicDepthPacking,
  Da as BasicEnvironmentNode,
  CT as BasicLightMapNode,
  Uf as BasicShadowMap,
  Xx as BatchNode,
  ON as BezierInterpolant,
  Yd as BitcastNode,
  Cs as BitcountNode,
  GN as Bone,
  kN as BooleanKeyframeTrack,
  VN as Box3,
  xc as BoxGeometry,
  Ri as BufferAttribute,
  Xt as BufferAttributeNode,
  ha as BufferGeometry,
  Fa as BufferNode,
  Em as BuiltinNode,
  ax as BumpMapNode,
  dm as BypassNode,
  Ys as ByteType,
  $N as Cache,
  zN as Camera,
  vS as CanvasTarget,
  Bh as CineonToneMapping,
  Vr as ClampToEdgeWrapping,
  ot as ClippingNode,
  WN as Clock,
  Ee as CodeNode,
  Ht as Color,
  HN as ColorKeyframeTrack,
  ye as ColorManagement,
  Yc as ColorSpaceNode,
  Ps as Compatibility,
  qN as CompressedArrayTexture,
  XN as CompressedCubeTexture,
  KN as CompressedTexture,
  am as ComputeNode,
  Ig as ConditionalNode,
  Bt as ConstNode,
  QN as ConstantAlphaFactor,
  YN as ConstantColorFactor,
  Kc as ContextNode,
  jN as Controls,
  bc as ConvertNode,
  Ff as CubeCamera,
  Df as CubeDepthTexture,
  vT as CubeMapNode,
  Vn as CubeReflectionMapping,
  $n as CubeRefractionMapping,
  Bd as CubeRenderTarget,
  ai as CubeTexture,
  qm as CubeTextureNode,
  fo as CubeUVReflectionMapping,
  ZN as CubicInterpolant,
  Hh as CullFaceBack,
  qh as CullFaceFront,
  Wh as CullFaceNone,
  Zs as CustomBlending,
  JN as CustomToneMapping,
  Ih as CylinderGeometry,
  e0 as Data3DTexture,
  Vf as DataArrayTexture,
  fc as DataTexture,
  t0 as DataTextureLoader,
  s0 as DataUtils,
  mm as DebugNode,
  _f as DecrementStencilOp,
  Tf as DecrementWrapStencilOp,
  r0 as DefaultLoadingManager,
  Gt as DepthFormat,
  kt as DepthStencilFormat,
  vt as DepthTexture,
  n0 as DetachedBindMode,
  Rh as DirectionalLight,
  tS as DirectionalLightNode,
  i0 as DiscreteInterpolant,
  Ot as DoubleSide,
  Zl as DstAlphaFactor,
  Jl as DstColorFactor,
  Ln as DynamicDrawUsage,
  My as EnvironmentNode,
  zl as EqualCompare,
  uc as EqualDepth,
  ff as EqualStencilFunc,
  oa as EquirectangularReflectionMapping,
  aa as EquirectangularRefractionMapping,
  o0 as Euler,
  ni as EventDispatcher,
  Ke as EventNode,
  Fn as ExpressionNode,
  a0 as ExternalTexture,
  u0 as FileLoader,
  op as FlipNode,
  gc as Float16BufferAttribute,
  Ka as Float32BufferAttribute,
  De as FloatType,
  Gl as FramebufferTexture,
  Pm as FrontFacingNode,
  Ns as FrontSide,
  Sh as Frustum,
  bh as FrustumArray,
  Dp as FunctionCallNode,
  k_ as FunctionNode,
  l0 as GLSL3,
  OS as GLSLNodeBuilder,
  hS as GLSLNodeParser,
  On as GreaterCompare,
  oc as GreaterDepth,
  nr as GreaterEqualCompare,
  ac as GreaterEqualDepth,
  hf as GreaterEqualStencilFunc,
  df as GreaterStencilFunc,
  c0 as Group,
  Oe as HalfFloatType,
  Eh as HemisphereLight,
  sS as HemisphereLightNode,
  RN as IESSpotLight,
  rS as IESSpotLightNode,
  d0 as ImageBitmapLoader,
  h0 as ImageLoader,
  f0 as ImageUtils,
  bf as IncrementStencilOp,
  yf as IncrementWrapStencilOp,
  me as IndexNode,
  ma as InputNode,
  sd as InspectorBase,
  Tm as InspectorNode,
  Wx as InstanceNode,
  Gf as InstancedBufferAttribute,
  kf as InstancedInterleavedBuffer,
  p0 as InstancedMesh,
  Hx as InstancedMeshNode,
  Ue as IntType,
  Wf as InterleavedBuffer,
  zf as InterleavedBufferAttribute,
  g0 as Interpolant,
  m0 as InterpolateBezier,
  x0 as InterpolateDiscrete,
  T0 as InterpolateLinear,
  y0 as InterpolateSmooth,
  Sf as InvertStencilOp,
  iT as IrradianceNode,
  lm as IsolateNode,
  sp as JoinNode,
  Af as KeepStencilOp,
  _0 as KeyframeTrack,
  b0 as Layers,
  la as LessCompare,
  cc as LessDepth,
  Qr as LessEqualCompare,
  lc as LessEqualDepth,
  pf as LessEqualStencilFunc,
  gf as LessStencilFunc,
  S0 as Light,
  Ch as LightProbe,
  nS as LightProbeNode,
  xS as Lighting,
  rT as LightingContextNode,
  pi as LightingModel,
  mr as LightingNode,
  sh as LightsNode,
  N0 as Line,
  ef as LineBasicMaterial,
  bT as LineBasicNodeMaterial,
  tf as LineDashedMaterial,
  NT as LineDashedNodeMaterial,
  R0 as LineLoop,
  A0 as LineSegments,
  Ze as LinearFilter,
  v0 as LinearInterpolant,
  Rs as LinearMipmapLinearFilter,
  po as LinearMipmapNearestFilter,
  $f as LinearSRGBColorSpace,
  wh as LinearToneMapping,
  za as LinearTransfer,
  E0 as Loader,
  C0 as LoaderUtils,
  w0 as LoadingManager,
  jx as LoopNode,
  M0 as LoopOnce,
  B0 as LoopPingPong,
  F0 as LoopRepeat,
  U0 as MOUSE,
  $a as Material,
  nc as MaterialBlending,
  R as MaterialNode,
  Qm as MaterialReferenceNode,
  T as MathNode,
  Bf as MathUtils,
  mc as Matrix2,
  Kr as Matrix3,
  dt as Matrix4,
  vf as MaxEquation,
  bm as MaxMipLevelNode,
  ap as MemberNode,
  is as Mesh,
  gs as MeshBasicMaterial,
  MT as MeshBasicNodeMaterial,
  D0 as MeshDepthMaterial,
  P0 as MeshDistanceMaterial,
  jh as MeshLambertMaterial,
  PT as MeshLambertNodeMaterial,
  Jh as MeshMatcapMaterial,
  Vy as MeshMatcapNodeMaterial,
  Zh as MeshNormalMaterial,
  AT as MeshNormalNodeMaterial,
  Xh as MeshPhongMaterial,
  IT as MeshPhongNodeMaterial,
  Qh as MeshPhysicalMaterial,
  Dy as MeshPhysicalNodeMaterial,
  Kh as MeshStandardMaterial,
  zd as MeshStandardNodeMaterial,
  Yh as MeshToonMaterial,
  Oy as MeshToonNodeMaterial,
  Ef as MinEquation,
  ii as MirroredRepeatWrapping,
  wf as MixOperation,
  at as ModelNode,
  eT as MorphNode,
  Hs as MultiplyBlending,
  Mf as MultiplyOperation,
  Qe as NearestFilter,
  $r as NearestMipmapLinearFilter,
  Vl as NearestMipmapNearestFilter,
  Dh as NeutralToneMapping,
  Hl as NeverCompare,
  hc as NeverDepth,
  xf as NeverStencilFunc,
  ss as NoBlending,
  ir as NoColorSpace,
  Ni as NoNormalPacking,
  bs as NoToneMapping,
  G as Node,
  nt as NodeAccess,
  tl as NodeAttribute,
  ah as NodeBuilder,
  eo as NodeCache,
  Fb as NodeCode,
  Nm as NodeError,
  rl as NodeFrame,
  Oa as NodeFunctionInput,
  Ne as NodeMaterial,
  Xf as NodeMaterialObserver,
  tn as NodeShaderStage,
  Mb as NodeUniform,
  z as NodeUpdateType,
  oh as NodeVar,
  Bb as NodeVarying,
  L0 as NormalAnimationBlendMode,
  Lt as NormalBlending,
  If as NormalGAPacking,
  nx as NormalMapNode,
  Tc as NormalRGPacking,
  $l as NotEqualCompare,
  ic as NotEqualDepth,
  cf as NotEqualStencilFunc,
  I0 as NumberKeyframeTrack,
  O0 as Object3D,
  fe as Object3DNode,
  Of as ObjectSpaceNormalMap,
  rc as OneFactor,
  G0 as OneMinusConstantAlphaFactor,
  k0 as OneMinusConstantColorFactor,
  Kl as OneMinusDstAlphaFactor,
  Ql as OneMinusDstColorFactor,
  Yl as OneMinusSrcAlphaFactor,
  jl as OneMinusSrcColorFactor,
  xe as OperatorNode,
  kl as OrthographicCamera,
  Fl as PCFShadowMap,
  Pf as PCFSoftShadowMap,
  _y as PMREMGenerator,
  Ey as PMREMNode,
  __ as ParameterNode,
  Ti as PassNode,
  Pn as PerspectiveCamera,
  Pd as PhongLightingModel,
  Vd as PhysicalLightingModel,
  Vh as Plane,
  Lh as PlaneGeometry,
  Nh as PointLight,
  Ab as PointLightNode,
  Sb as PointShadowNode,
  V0 as Points,
  sf as PointsMaterial,
  qy as PointsNodeMaterial,
  AN as ProjectorLight,
  oS as ProjectorLightNode,
  $0 as PropertyBinding,
  z0 as PropertyMixer,
  ee as PropertyNode,
  jd as QuadMesh,
  Oh as Quaternion,
  W0 as QuaternionKeyframeTrack,
  H0 as QuaternionLinearInterpolant,
  To as R11_EAC_Format,
  kn as RED_GREEN_RGTC2_Format,
  Lo as RED_RGTC1_Format,
  ua as REVISION,
  Gn as RG11_EAC_Format,
  ct as RGBAFormat,
  ca as RGBAIntegerFormat,
  Fo as RGBA_ASTC_10x10_Format,
  wo as RGBA_ASTC_10x5_Format,
  Mo as RGBA_ASTC_10x6_Format,
  Bo as RGBA_ASTC_10x8_Format,
  Uo as RGBA_ASTC_12x10_Format,
  Do as RGBA_ASTC_12x12_Format,
  bo as RGBA_ASTC_4x4_Format,
  So as RGBA_ASTC_5x4_Format,
  No as RGBA_ASTC_5x5_Format,
  Ro as RGBA_ASTC_6x5_Format,
  Ao as RGBA_ASTC_6x6_Format,
  vo as RGBA_ASTC_8x5_Format,
  Eo as RGBA_ASTC_8x6_Format,
  Co as RGBA_ASTC_8x8_Format,
  Po as RGBA_BPTC_Format,
  xo as RGBA_ETC2_EAC_Format,
  Xa as RGBA_PVRTC_2BPPV1_Format,
  qa as RGBA_PVRTC_4BPPV1_Format,
  vn as RGBA_S3TC_DXT1_Format,
  En as RGBA_S3TC_DXT3_Format,
  Cn as RGBA_S3TC_DXT5_Format,
  ri as RGBFormat,
  Ll as RGBIntegerFormat,
  q0 as RGB_BPTC_SIGNED_Format,
  X0 as RGB_BPTC_UNSIGNED_Format,
  go as RGB_ETC1_Format,
  mo as RGB_ETC2_Format,
  Ha as RGB_PVRTC_2BPPV1_Format,
  Wa as RGB_PVRTC_4BPPV1_Format,
  An as RGB_S3TC_DXT1_Format,
  Rt as RGFormat,
  si as RGIntegerFormat,
  K0 as RawShaderMaterial,
  Q0 as Ray,
  Ah as RectAreaLight,
  aS as RectAreaLightNode,
  ei as RedFormat,
  ti as RedIntegerFormat,
  jc as ReferenceBaseNode,
  Ua as ReferenceNode,
  Mh as ReinhardToneMapping,
  gm as RenderOutputNode,
  Ds as RenderTarget,
  em as RendererReferenceNode,
  oi as RepeatWrapping,
  Nf as ReplaceStencilOp,
  ql as ReverseSubtractEquation,
  $y as RotateNode,
  yo as SIGNED_R11_EAC_Format,
  Oo as SIGNED_RED_GREEN_RGTC2_Format,
  Io as SIGNED_RED_RGTC1_Format,
  _o as SIGNED_RG11_EAC_Format,
  Bl as SRGBColorSpace,
  q as SRGBTransfer,
  Ul as Scene,
  de as ScreenNode,
  ip as SetNode,
  Y0 as ShaderMaterial,
  K_ as ShadowBaseNode,
  nf as ShadowMaterial,
  nh as ShadowNode,
  Yy as ShadowNodeMaterial,
  js as ShortType,
  j0 as Skeleton,
  Z0 as SkinnedMesh,
  Qx as SkinningNode,
  J0 as Source,
  Lf as Sphere,
  kh as SphereGeometry,
  eR as Spherical,
  tR as SphericalHarmonics3,
  np as SplitNode,
  ra as SpotLight,
  Ga as SpotLightNode,
  rf as SpriteMaterial,
  Hd as SpriteNodeMaterial,
  tc as SrcAlphaFactor,
  ec as SrcAlphaSaturateFactor,
  sc as SrcColorFactor,
  b_ as StackNode,
  Pe as StackTrace,
  fa as StaticDrawUsage,
  Vx as StorageArrayElementNode,
  zx as StorageBufferNode,
  sR as StringKeyframeTrack,
  Hg as SubBuildNode,
  J as SubgroupFunctionNode,
  Xl as SubtractEquation,
  qs as SubtractiveBlending,
  rR as TOUCH,
  Si as TangentSpaceNormalMap,
  Re as TempNode,
  da as Texture,
  nR as TextureLoader,
  Zr as TextureNode,
  _m as TextureSizeNode,
  Nt as TimestampQuery,
  sm as ToneMappingNode,
  iR as Triangle,
  oR as TriangleFanDrawMode,
  aR as TriangleStripDrawMode,
  uR as TrianglesDrawMode,
  af as UVMapping,
  lf as Uint16BufferAttribute,
  uf as Uint32BufferAttribute,
  vm as UniformArrayNode,
  Pc as UniformGroupNode,
  jr as UniformNode,
  $e as UnsignedByteType,
  ia as UnsignedInt101111Type,
  Vt as UnsignedInt248Type,
  na as UnsignedInt5999Type,
  _e as UnsignedIntType,
  Il as UnsignedShort4444Type,
  Ol as UnsignedShort5551Type,
  Ss as UnsignedShortType,
  Br as VSMShadowMap,
  Bn as VarNode,
  qg as VaryingNode,
  je as Vector2,
  H as Vector3,
  Se as Vector4,
  lR as VectorKeyframeTrack,
  xT as VertexColorNode,
  ze as ViewportDepthNode,
  uT as ViewportDepthTextureNode,
  Nd as ViewportTextureNode,
  _l as WebGLBackend,
  qS as WebGLCapabilities,
  Dn as WebGLCoordinateSystem,
  cR as WebGLRenderTarget,
  NN as WebGPUBackend,
  kr as WebGPUCoordinateSystem,
  wN as WebGPURenderer,
  Gh as WebXRController,
  dR as WrapAroundEnding,
  hR as ZeroCurvatureEnding,
  jt as ZeroFactor,
  fR as ZeroSlopeEnding,
  Rf as ZeroStencilOp,
  $h as createCanvasElement,
  vi as defaultBuildStages,
  Jf as defaultShaderStages,
  F as error,
  Hf as log,
  Ei as shaderStages,
  cr as vectorComponents,
  L as warn,
  Te as warnOnce
};
